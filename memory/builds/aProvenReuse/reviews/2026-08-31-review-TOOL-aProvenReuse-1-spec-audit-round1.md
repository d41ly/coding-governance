## Verdict: BLOCKED

**Serves:** spec-audit TOOL-aProvenReuse-1 TOOL-aProvenReuse-2

Round 1 · 2026-08-31 · node a · base `3bfc5e87` · harness `tools/workflows/tier2-review.js`,
run `wf_4c1006c7-c72` · 4 spec lenses, 5 batched skeptics, 10 agents, 1 302 755 subagent tokens.

**41 raw findings · 20 confirmed · 21 refuted · precision 0.49.** Confirmed blockers: 4.

**This record was derived from the run journal, not written by the harness.** The synthesis
agent died on a session limit (`[synth] failed: You've hit your session limit`), so the harness
returned `report: null` and its own note said the confirmed findings were in the run log only.
The findings and verdicts below are read from
`subagents/workflows/wf_4c1006c7-c72/journal.jsonl` and re-joined on the orchestrator's id rule
(`tools/workflows/tier2-review.js:367` — flat index over the lens array, in `SPEC_LENSES`
order, NOT journal completion order). The four blockers were each re-measured by hand before
folding; the commands and their output are in the fold notes at the end.

## Confirmed

### 12 — BLOCKER · contradiction · `2026-08-31-spec-TOOL-aProvenReuse-2.md` · section 2 S1/S6 against section 6 AC6 and section 7

**Claim.** Adding `reuse-probed` to DOD_CORE obliges two edits to `tools/unattended/PROTOCOL.template.md` that section 2 never puts in scope: a row in the Definition-of-Done table, and the count sentence at line 313, which today reads `Ten kit-owned core items.`

**Impact.** `check-unattended.sh` check 16 arm E joins DOD_CORE to that table in BOTH directions (`a CORE Definition-of-Done item is enforced by --close and absent from the protocol's table`) and separately word-compares the count sentence against the driver's set. That leg is the unguarded `unattended kit gate` this unit's own section 7 says runs on the bar, so as scoped the unit reds the merge bar twice and AC6's `check-unattended.sh exits 0` is unreachable. Section 4's Inventory hints at it with the two words `the new item`, and the count sentence is named nowhere in either spec.

**Fix.** Add to section 2 an explicit S: the `reuse-probed` row joins the protocol's DoD table in `tools/unattended/PROTOCOL.template.md` and its render, and the count sentence above it moves `Ten` -> `Eleven`. Add an AC that names check 16 arm E's two joins, since a table row and a word count are two separate failures.

**Skeptic.** Verified live. `unattended kit gate` is guard=None in tools/gate-legs.json, so check-unattended.sh runs on every bar; its check 16 word-compares the sentence at PROTOCOL.template.md:313 and memory/guides/UNATTENDED-PROTOCOL.md:313 — both read `Ten kit-owned core items.` — against `ndod` derived from DOD_CORE (check-unattended.sh:1523-1533). Adding an 11th core item makes that arm fail, and neither §2 nor §4 names the count sentence anywhere, so AC6's `check-unattended.sh exits 0` is unreachable as scoped. The finding overstates it as two unscoped edits — §4's Inventory does put "the new item" in PROTOCOL.template.md, covering the table row — but the count sentence is genuinely named nowhere and reds the bar on its own.

### 13 — BLOCKER · contradiction · `2026-08-31-spec-TOOL-aProvenReuse-2.md` · section 2 S4 against section 4 'What this item claims' and section 6 AC4

**Claim.** S4's fold rule is wrong about what the log file contains: the raw JSONL carries JSON-escaped separators, so the bytes on disk are `C:\\projects\\coding-governance\\...` (two backslashes per separator), and folding backslash to slash yields `C://projects//coding-governance//...`, never the shell's `C:/projects/coding-governance/...`.

**Impact.** Verified against the live log: `grep '"type": "query"' <log> | tr '\\' '/' | grep -cF "$(git rev-parse --show-toplevel)"` returns 0 on this very worktree, which holds two real query rows. Built as specified, the item reports the `zero` outcome — UNMET, 'run the probe' — for every conforming Windows run, which is the false verdict section 4 says the item exists to prevent. It is invisible off Windows, where the logged value already carries `/`, so only node `a` sees it. S7's fixtures would hide it too: a hand-written fixture with single backslashes passes while the shipped read is broken.

**Fix.** Restate S4: the comparison squeezes the doubled separator (`sed 's|\\\\|/|g'`) or, better, matches the shell's root re-escaped to the log's own spelling. Add to S7 that the log fixture is either written by `tools/memory-recall/query.py` itself or carries the doubled-backslash bytes verbatim, and make AC4 name the escaping it asserts against.

**Skeptic.** Reproduced on this worktree. query.py writes the row through `json.dumps` (line 771) with `"worktree": str(repo)` (line 1232), so the bytes on disk are `"worktree": "C:\\projects\\coding-governance\\.claude\\worktrees\\unattended-kit-gaps-a3b869"` — doubled backslashes. Folding one backslash to one slash, as S4 states, yields `C://projects//...`, never `git rev-parse --show-toplevel`'s `C:/projects/...`: `grep '"type": "query"' <log> | tr '\\' '/' | grep -cF "$ROOT"` returns 0 against a log holding 120 query rows, two of them for this very tree. Built as S4 specifies, the item reports `zero` — UNMET, "run the probe" — for every conforming Windows run, which is the false verdict §4 says it exists to prevent, and S7's hand-written fixtures would hide it.

### 21 — BLOCKER · unstated-assumption · `2026-08-31-spec-TOOL-aProvenReuse-1.md` · section 4 Migration (and section 6 AC4, section 8 Q2)

**Claim.** "The cutoff is set to this build's own date, so every tracked spec is grandfathered" is true only of THIS branch's index; 21 Tier-2 specs dated 2026-08-30/31 sit on five live sibling branches and fail the predicate.

**Impact.** Enumerated with the exact arm logic over `git for-each-ref refs/heads`: aGatheredDeclaration (6 of 8), aGradedMandate (8 of 9), aPairedLexer (7 of 7 dated 08-31 plus 4 dated 08-30), aScouredKit (2), all WOULD RED. Whichever merges second — this unit or those branches — reds `memory hygiene` on main for five other in-flight runs, which is exactly the build README's own "neither unit may red a landed spec" rule. Q2 weighed only the landing-date-vs-build-date axis and never the concurrent-branch population, so a resolved fork closed on an incomplete option set.

**Fix.** Set SPEC10_EVIDENCE_CUTOFF strictly AHEAD of every dated spec on every branch — 2026-09-01 at the earliest — per the idiom check-memory-hygiene.sh:627 already states ("a dated cutoff set strictly ahead of every committed record"). Add an acceptance criterion that re-runs the enumeration across all refs, not just the worktree index, immediately before pinning the value.

**Skeptic.** Replicated with the exact arm logic (case-folded index() over the §10 body, Tier-1 skipped) over `git for-each-ref refs/heads`. Tier-2 specs dated 2026-08-31 that fail both arms: branch/gate-bar-tooling-review-020565 aGatheredDeclaration 6 of 8; branch/paired-lexer-followup-9c31a2 aPairedLexer 7 of 7; branch/unattended-kit-adversarial-review-6810dc aGradedMandate 8 of 9. That is 21, matching the finding's headline count exactly. main and the other live branches carry 0 failing Tier-2 08-31 specs. §4 Migration's "every tracked spec is grandfathered and the first file the predicate grades is the next one anybody writes" is therefore false for the merged tree, and Q2 weighed only build-date-vs-landing-date. The build README's own rule ("Neither unit may red a landed spec") is the standard this breaks once either side merges second. The finding's parenthetical additions (the 08-30 specs, aScouredKit's 2) are noise — those are pre-cutoff or Tier-1 and would not red — but they do not touch the substance.

### 22 — BLOCKER · unstated-assumption · `2026-08-31-spec-TOOL-aProvenReuse-2.md` · section 2 S4

**Claim.** "the logged value ... carries `\` while the shell's own root carries `/`" is wrong about the bytes on disk: the value is JSON-escaped, so the raw line holds `C:\\projects\\coding-governance\\...` — two backslash bytes per separator.

**Impact.** Measured against the live log: `grep -Fc "$(git rev-parse --show-toplevel)"` returns 0, and after the S4 fold it still returns 0 because `tr '\\' '/'` produces `C://projects//coding-governance//.claude//worktrees//...`. The `zero` outcome fires on every correct run, `reuse-probed` is permanently UNMET, and AC4 ("reports MET and the message carries the row count") cannot be satisfied. The one thing a shell `grep` over this file must know is the one thing S4 states incorrectly.

**Fix.** Fold the two-character sequence `\\` to `/` (`sed 's/\\\\/\//g'`) or collapse repeated slashes after folding, and observe a MET run against the real log before landing. Also pin the byte shape S2's row filter greps for: query.py writes `"type": "query"` (json.dumps default separators) while recall-opened.js writes `"type":"opened"` with none — the spec never states which spelling it matches.

**Skeptic.** Measured against the live log at C:/projects/coding-governance/.git/recall/queries.jsonl: every worktree value is JSON-escaped, e.g. "worktree": "C:\\projects\\coding-governance\\.claude\\worktrees\\..." — two backslash bytes per separator. `grep -Fc "$(git rev-parse --show-toplevel)"` returns 0 while `grep -c unattended-kit-gaps-a3b869` returns 3, so rows for this tree exist and a forward-slash comparison misses all of them. `tr '\\' '/'` maps each backslash byte to a slash and yields C://projects//... — still no match. S4's stated fact ("the logged value ... carries `\`") is wrong about the bytes at the one point the unit's whole join hinges on, and §4's own rejected-alternatives block commits the read to shell substring matching over those raw bytes rather than to a JSON parse. AC4 would eventually catch a spec-conformant implementation, but the spec states the fact incorrectly.

### 1 — HIGH · underspecification · `2026-08-31-spec-TOOL-aProvenReuse-1.md` · section 6 AC5

**Claim.** AC5 pins the liveness run at "reds and names 253 files", but 253 is an ALL-TIERS count; check 12 exits on `if (hdr ~ /Tier-1/) next` before the canon block, so the arms grade Tier-2 only — measured, that population is 264 specs and 188 of them fail S3's arms, while 65 of the 253 are Tier-1 files the assertion never reaches.

**Impact.** The one criterion that proves the predicate can move names a number it cannot produce. A builder who observes 188 either reports AC5 failed on a correct implementation, or widens the assertion above the Tier-1 cut to reach 253 — which directly violates N4 and would red 65 landed Tier-1 specs the cutoff was supposed to grandfather.

**Fix.** Change AC5 to `reds and names 188 files`, and add the derivation in one clause: 188 is the Tier-2 population S2 actually grades; the 253 in the build README is all-tiers and includes 65 Tier-1 specs check 12 never reaches. Re-measure at build time rather than copying either figure.

**Skeptic.** Reproduced exactly. Specs dated >= SPEC10_CUTOFF (2026-08-04) carrying a §10: 348 (346 landed + this build's 2). Failing S3's two arms: 253 all-tiers, which decomposes as 188 Tier-2 + 65 Tier-1. check-memory-hygiene.sh executes `if (hdr ~ /Tier-1/) next` immediately before the `# ---- Tier-2 body assertions ----` block that owns `want == canon10`, so S2's assertion (declared to sit inside that block) and N4 both scope it to Tier-2. A conforming implementation therefore names 188 at the lowered cutoff, never 253. AC5 is the one criterion proving the predicate can move, and it names a number the specified predicate cannot produce; reaching 253 requires grading Tier-1, which N4 forbids.

### 3 — HIGH · underspecification · `2026-08-31-spec-TOOL-aProvenReuse-1.md` · section 2 S5 and section 4 Inventory

**Claim.** S5 edits `memory/TEMPLATE-SPEC.md`, which is a RENDER of `tools/memory-tree/SPEC-TEMPLATE.template.md`; that source is named in neither S5 nor the Inventory table, and no section-6 criterion observes §10's new text on either side.

**Impact.** `tools/memory-tree/kit-dogfood-parity.test.sh` (gate leg `kit/dogfood doc parity`, guarded on `memory/TEMPLATE-SPEC.md`) diffs that exact pair and its header says "Edit the template, then re-render — never hand-edit the live copy". Doing S5 as written reds a merge-bar leg, and the adopter-facing template keeps describing an obligation the shipped checker now refuses. The template also carries the `{{TOOL_ROOT}}` placeholder a hand-edit of the render would drop. No AC catches any of it — compare unit 2's AC8, which does grep the tree for doc content.

**Fix.** Add `tools/memory-tree/SPEC-TEMPLATE.template.md` to S5 and to the Inventory as the authored source, with `memory/TEMPLATE-SPEC.md` produced by `bash tools/memory-tree/kit-dogfood-parity.test.sh --render`. Add an AC: `bash tools/memory-tree/kit-dogfood-parity.test.sh` exits 0, and the rendered §10 names both required facts and the cutoff key by name.

**Skeptic.** Verified end to end. `memory/TEMPLATE-SPEC.md` is a render: kit-dogfood-parity.test.sh's PAIRS line carries `$M/TEMPLATE-SPEC.md:$KITREL/SPEC-TEMPLATE.template.md`, its header says `Edit the template, then re-render — never hand-edit the live copy`, and the leg `kit/dogfood doc parity` in tools/gate-legs.json guards on `memory/TEMPLATE-SPEC.md` with subject `repo`, so it runs on this diff. The template's §10 carries `{{TOOL_ROOT}}codebase-map/reuse_lookup.py`, which a hand-edit of the render drops. `tools/memory-tree/SPEC-TEMPLATE.template.md` appears in neither S5 nor the Inventory, and S6 covers only version carriers. S5 as written reds a merge-bar leg, and `--render` would discard the edit.

### 4 — HIGH · underspecification · `2026-08-31-spec-TOOL-aProvenReuse-2.md` · section 6 AC8

**Claim.** AC8 asserts "no tracked file still asserts that waiving `reuse-first` is silent" and justifies it by "the claim appears in two templates and two renders" — measured, it appears in exactly two files, `tools/unattended/SKILL.template.md:114` and `.claude/skills/unattended/SKILL.md:114`. Neither PROTOCOL carrier holds it. A third tracked file does: `memory/builds/aProvenReuse/README.md:19` quotes the sentence verbatim as this build's problem statement.

**Impact.** The criterion cannot pass as written without editing the build README's own problem statement, destroying the record of why the build exists; a builder who instead scopes the grep by hand has an AC whose observable nobody wrote down. S6's prose is wrong for the same reason — the PROTOCOL template loses no sentence, it gains a DoD row.

**Fix.** Restate AC8's grep as scoped to the kit and its renders, excluding `memory/builds/`: the sentence must be gone from `tools/unattended/SKILL.template.md` and `.claude/skills/unattended/SKILL.md`, and present nowhere outside a build record quoting it as history. Correct S6 to say the SKILL template and its render lose the sentence, while the PROTOCOL template and its render gain the `reuse-probed` row that check-unattended.sh arm E joins to DOD_CORE.

**Skeptic.** Measured: the claim lives in exactly two files — tools/unattended/SKILL.template.md and .claude/skills/unattended/SKILL.md (twice each, at the SILENT sentence and at the waiver-cost line). `git grep reuse-first` over tools/unattended/PROTOCOL.template.md and memory/guides/UNATTENDED-PROTOCOL.md returns nothing at all, so S6's 'the kit's SKILL and PROTOCOL templates lose the sentence' and AC8's 'two templates and two renders' are both false about the tree; the PROTOCOL pair gains a DoD row (its item table is at PROTOCOL.template.md:320) rather than losing a sentence. memory/builds/aProvenReuse/README.md:18 quotes the sentence verbatim as this build's problem statement, so AC8's stated observable — a grep over the tree — hits a record the build must not rewrite.

### 5 — HIGH · underspecification · `2026-08-31-spec-TOOL-aProvenReuse-2.md` · section 2 S4 and section 6 AC4

**Claim.** S4 says the logged value "carries `\`" and folds backslashes to forward slashes, but the log is JSONL: every real row in `<git-common-dir>/recall/queries.jsonl` on this node reads `"worktree": "C:\\projects\\coding-governance\\.claude\\worktrees\\unattended-kit-gaps-a3b869"` — JSON-escaped double backslashes. Folding single `\` to `/` yields `C://projects//...`, which matches no shell-derived root. No AC pins a fixture row in the logged byte form.

**Impact.** The join fails on every genuine row written on the node this kit runs on, so `reuse-probed` reports the `zero` outcome for a run that DID probe — the exact false negative the item exists to prevent, wearing the message that says the operator should go run a probe. AC4 as written passes against a hand-written fixture using single separators, so the self-test certifies coverage it does not have.

**Fix.** State in S4 that the raw line carries `\\` and that the fold must collapse one-or-more backslashes to a single `/` on the log side (and apply the same normalization to the shell root, naming which expression supplies it — `git rev-parse --show-toplevel` gives `C:/…` while `pwd` under Git-Bash gives `/c/…`). Amend AC4 to require the fixture row be a byte copy of a real `query` row, escapes included.

**Skeptic.** Checked the live log at `$(git rev-parse --git-common-dir)/recall/queries.jsonl`: every row reads `"worktree": "C:\\projects\\coding-governance\\.claude\\worktrees\\..."` — JSON-escaped double backslashes in the file's bytes. S4's stated mechanism (fold `\` to `/` on a value that 'carries `\`') applied to the raw line yields `C://projects//...`, matching no shell-derived root, and §4's own rejected-alternatives block pins the reader to a shell `grep` over the raw JSONL rather than a Python parse. recall-opened.js is no help: it joins by common-dir membership and never reads the `worktree` field, so no existing idiom covers this. No AC pins a fixture row in the logged byte form, so AC4 passes against a single-separator fixture while every genuine row on this node falls to the `zero` outcome.

### 23 — HIGH · unstated-assumption · `2026-08-31-spec-TOOL-aProvenReuse-1.md` · section 2 (S1/S2), section 4 Inventory, section 7 Gates — same gap in unit 2 section 2 S5 and section 7

**Claim.** Both units edit files on the kickoff manifest's watch list and neither carries the `last-audit` re-stamp in scope, inventory, acceptance or gates.

**Impact.** `memory/guides/SESSION-KICKOFF.md` line 6 names `tools/memory-tree/check-memory-hygiene.sh`, `.memory-tree.conf` and `.unattended.conf` — all three edited across the two units. `kickoff-manifest ratchet` is the FIRST leg in `tools/gate-legs.json`, and manifest-check.sh C5 fails on "watched files changed since last-audit with no re-stamp at/after the change" (it has a `--staged` arm too, so the pre-commit hook reds first). Both section 7s assert the full bar passes; it will not, and neither inventory lists the manifest.

**Fix.** Add `memory/guides/SESSION-KICKOFF.md` to both inventories and an acceptance criterion that `bash skills/session-kickoff/manifest-check.sh` exits 0 after the re-stamp. Check C9's ten-watched-commit counter at the same time — if it trips, `last-body-change` must advance as well, which is a second carrier the specs do not name.

**Skeptic.** memory/guides/SESSION-KICKOFF.md's manifest-audit block watches tools/memory-tree/check-memory-hygiene.sh, .memory-tree.conf and .unattended.conf — unit 1 edits the first two, unit 2 the third. manifest-check.sh C5 (line 285ff) is mechanical, not judgment-based: the newest watch-touching commit must be an ancestor of a commit that changed the last-audit VALUE, else `fail 5 "watched files changed since last-audit with no re-stamp at/after the change"`. C5s (line 408ff) is the blocking pre-commit arm and requires the re-stamp bundled into the same commit, so the hook reds before the bar does. "kickoff-manifest ratchet" is the first entry in tools/gate-legs.json. Neither spec's §4 Inventory names memory/guides/SESSION-KICKOFF.md, neither §2 carries the re-stamp, and both §7s assert the full bar passes. No non-goal withholds it.

### 24 — HIGH · unstated-assumption · `2026-08-31-spec-TOOL-aProvenReuse-2.md` · section 2 S1 and section 4 Inventory

**Claim.** Putting a memory-recall-dependent item in the unattended kit's DOD_CORE creates a cross-kit dependency that `tools/unattended/kit.toml` does not declare — it ships `requires = ["memory-tree"]` and nothing else.

**Impact.** An adopter who takes the unattended kit without memory-recall has no `<git-common-dir>/recall/queries.jsonl` ever, so the `no log` arm fires forever, a CORE Definition-of-Done item is structurally unmeetable, and every `--close` in that repo needs an override. The build README's third rule refuses to close the reuse_lookup asymmetry precisely because it "needs a cross-kit dependency"; unit 2 takes one for the other probe without ever naming it. Prior art missed: `tools/memory-tree/kit.toml` already ships `requires_if = [{ kit = "memory-recall", when_any_key_set = [...], why = ... }]` for exactly this shape.

**Fix.** Add the edge to `tools/unattended/kit.toml` as a `requires_if` (or a hard `requires`), and/or add a fifth S2 outcome: when the memory-recall kit is absent from the adopter's tree the arm ANNOUNCES a skip rather than reporting UNMET — the `pieces-complete` skip idiom S2 already says it copies.

**Skeptic.** Verified in source: tools/unattended/kit.toml line 7 is `requires = ["memory-tree"]` and declares nothing else, while tools/memory-tree/kit.toml line 8 already ships `requires_if = [{ kit = "memory-recall", when_any_key_set = [...], why = ... }]` — the exact conditional-edge idiom for this shape, unused here. S1 puts a memory-recall-log reader in DOD_CORE, so an adopter who takes unattended without memory-recall never has <git-common-dir>/recall/queries.jsonl and the `no log` arm fires on every --close forever. The spec is aware of the boundary in its own rejected-alternatives ("would make the driver depend on the memory-recall kit being installed, which no adopter is required to do") and rejects only the code-level hop while taking the data-level one, and the build README's third rule refuses the sibling asymmetry on precisely that ground. Real, and the prior art was missed.

### 25 — HIGH · unstated-assumption · `2026-08-31-spec-TOOL-aProvenReuse-1.md` · section 6 AC5

**Claim.** AC5's "reds and names 253 files" is a number measured over the §10-BEARING population, which includes 84 Tier-1 specs the check never grades.

**Impact.** Replicated with the exact arm logic: 253 files fail over the 348 post-SPEC10 specs carrying a §10, but only 188 fail over the 264 Tier-2 specs the awk actually reaches (`if (hdr ~ /Tier-1/) next` fires before the Tier-2 body assertions, and N4 keeps it that way). AC5 is the declared liveness assertion for AC4, so a correct implementation fails its own liveness check by 65 files — and the builder's likely repair is to widen the arm to Tier-1, contradicting N4.

**Fix.** Restate AC5 over the population the predicate grades — 188 Tier-2 specs at the time of writing — or make the criterion derive the count in the run rather than pinning a literal, and name the population in the same line so the figure cannot be read against the wrong one.

**Skeptic.** Replicated exactly. Over the tree at HEAD with the arm logic and cutoff 2026-08-04: 348 post-SPEC10 specs carry a §10, of which 84 are Tier-1 and 264 Tier-2; 253 of the 348 fail, but only 188 of the 264 Tier-2 do — a 65-file gap that is precisely the Tier-1 population. `if (hdr ~ /Tier-1/) next` at check-memory-hygiene.sh line 1013 fires before every Tier-2 body assertion, and S2 places the new arm after that inside the `want == canon10` block, so the check never reaches those 84. N4 makes that deliberate. AC5's "reds and names 253 files" is therefore unreachable by a correct implementation, and AC5 is the declared liveness assertion for AC4, so the unit fails its own liveness check by 65 files. The build README's "253 specs that would fail the predicate today" carries the same population error.

### 33 — HIGH · prior-art · `2026-08-31-spec-TOOL-aProvenReuse-1.md` · section 4 Inventory (row `memory/TEMPLATE-SPEC.md`) and section 7 Gates

**Claim.** S5 edits `memory/TEMPLATE-SPEC.md` but the inventory omits its template source `tools/memory-tree/SPEC-TEMPLATE.template.md`. The bar leg `kit/dogfood doc parity` in `tools/gate-legs.json` is guarded on exactly `memory/TEMPLATE-SPEC.md` and pairs it with that template (`tools/memory-tree/kit-dogfood-parity.test.sh:53`), rendering and diffing the two.

**Impact.** S5 as scoped reds the full bar in the same commit that introduces it, and section 7 names neither the leg nor the file, so the builder discovers the pairing from a failing run rather than from the spec. Same trap applies to any BUILD-METHOD edit made under finding 1.

**Fix.** Add `tools/memory-tree/SPEC-TEMPLATE.template.md` to the section 4 inventory as the S5 source of truth, note that `bash tools/memory-tree/kit-dogfood-parity.test.sh --render` regenerates the live copy, and name the `kit/dogfood doc parity` leg in section 7.

**Skeptic.** Verified in both directions. `tools/gate-legs.json` leg `kit/dogfood doc parity` guards on `memory/HYGIENE.md`, `memory/TEMPLATE-SPEC.md`, `memory/guides/BUILD-METHOD.md`, `tools/lib/`, `tools/memory-tree/`, and `tools/memory-tree/kit-dogfood-parity.test.sh` line 53 pairs `$M/TEMPLATE-SPEC.md:$KITREL/SPEC-TEMPLATE.template.md`, rendering and diffing them. The template carries the §10 prose S5 rewrites at `SPEC-TEMPLATE.template.md:224-229` (with a `{{TOOL_ROOT}}` placeholder), and the test's own header states the direction: "Edit the template, then re-render — never hand-edit the live copy." S5 as scoped instructs the forbidden edit, the §4 inventory omits the authored source, and §7 names neither the leg nor the file — so the leg reds in the same commit that introduces the change, and the builder learns the pairing from a failing bar. S6's "check-kit-versions.sh is the authority on which carriers" covers version stamps, not this render pair.

### 34 — HIGH · prior-art · `2026-08-31-spec-TOOL-aProvenReuse-2.md` · section 2 S4

**Claim.** S4's comparison rule is wrong against the log's actual bytes. The logged value is JSON-ESCAPED — measured, the raw line reads `"worktree": "C:\\projects\\coding-governance"` — so folding each `\` to `/` yields `C://projects//coding-governance`, which never equals the shell's `$ROOT`. The correct fold is the JSON unescape `\\` -> `/`, not `\` -> `/`.

**Impact.** On win32, the platform this repo runs on, the match never fires: every run lands in S2's `zero` outcome and reports UNMET with "run the probe, or override" on a tree where probes demonstrably ran. S7's arms would be authored from this same rule, so the fixture agrees with the bug and the self-test passes.

**Fix.** State the fold as `\\` -> `/` in S4 (a `sed 's|\\\\|/|g'` over the line, not `tr`), and make one S7 arm use a fixture row copied verbatim out of a real `queries.jsonl` rather than one hand-authored from the spec.

**Skeptic.** Measured against the live log: the raw line reads `"worktree": "C:\\projects\\coding-governance"` — JSON-escaped, two bytes per separator — across all 120 `type: "query"` rows. Spec 2 rejects reading through `query.py` (§4: "the read is one `grep` over a line-oriented file"), so the shell sees those raw bytes and never a decoded value. S4's stated rule, "the logged value ... carries `\`" folded "backslashes to forward slashes", yields `C://projects//coding-governance`, which never equals `$ROOT`; the correct fold is the JSON unescape `\\` -> `/`. On win32 every run therefore lands in S2's `zero` outcome and reports UNMET with "run the probe, or override" on a tree where 120 probes are logged. S7's fixtures, authored from the same sentence, would agree with the bug.

### 6 — MEDIUM · underspecification · `2026-08-31-spec-TOOL-aProvenReuse-1.md` · status header (rev-1 line; unit 2 carries the same)

**Claim.** Both specs declare `Tier-2` in the status header while the build README's build-level rules classify both units as "authored by this run at Tier 1", and nothing in either spec reconciles the two.

**Impact.** The tier is load-bearing here, not bookkeeping: check 12 skips the entire canon block for a Tier-1 spec, so under the README's reading the new §10 evidence arm would not grade this build's own two specs — which is precisely the property section 4 Migration claims ("the first file the predicate grades is ... including this spec and its sibling"). It also decides whether M4's spec audit is owed. One of the two documents is wrong and the specs do not say which.

**Fix.** Pick one and make both documents agree. If Tier-2 stands, correct the README's build-level rule and say in section 4 Migration that the Tier-2 header is what puts these two specs inside the graded set. If Tier-1 stands, delete the Migration claim about self-grading and say the first graded spec is the next Tier-2 one anybody writes.

**Skeptic.** The contradiction is verbatim and unreconciled. memory/builds/aProvenReuse/README.md:61-63 records the M2 classification as 'authored by this run at Tier 1' with its reasoning (no write path, migration, auth surface or shared contract), while both status headers declare Tier-2 and the generated build-units table derives Tier 2 from them. The tier is load-bearing: check-memory-hygiene.sh skips the entire canon block on `if (hdr ~ /Tier-1/) next`, so under the README's classification the new evidence arm would not grade this build's own specs — contradicting §4 Migration's claim that the first files the predicate grades are 'this spec and its sibling', and contradicting AC4's graded set. It also decides whether M4's spec audit is owed.

### 26 — MEDIUM · unstated-assumption · `2026-08-31-spec-TOOL-aProvenReuse-1.md` · section 5 Error states (and section 2 S1)

**Claim.** The blank-resolves-forward guard is copied from the wrong sibling, and its stated justification is refuted three lines above where the new arm would sit.

**Impact.** SPEC10_CUTOFF resolves forward because it SELECTS between two canons and must have a value; SPEC10_EVIDENCE_CUTOFF is an optional RULE, like STREAMS_CUTOFF/SPEC_WITNESS_CUTOFF/FORK_MARK_CUTOFF, and all three of those already solve blank-means-off with a non-empty guard inside this very awk block (`scut != "" && fdate != "" && fdate >= scut` at :846, `wcut` at :862, `fcut` at :997). So section 5's claim that "resolving blank to blank would arm the predicate over the whole corpus" is false. The cost is borne by adopters: with forward resolution an adopter cannot turn the rule off, and pulling the kit arms it at the shipped literal over a corpus that has never seen it — the retroactive red the cutoff mechanism exists to prevent, one repo over.

**Fix.** Use the STREAMS_CUTOFF guard shape, let blank mean off, drop the forward resolution and the `_SPEC10E_SHIPPED` capture, and ship the `.example` key blank. Rewrite section 5's Error states to say which sibling's semantics it takes and why.

**Skeptic.** Verified in check-memory-hygiene.sh: the three optional RULE cutoffs all guard on non-empty — `scut != ""` at :846, `wcut != ""` at :862, `fcut != ""` at :997 — and ship blank at :32-34 with "blank = never required"; tools/memory-tree/.memory-tree.conf.example likewise ships STREAMS_CUTOFF, SPEC_WITNESS_CUTOFF and ACCEPTANCE_LEDGER_CUTOFF empty. SPEC10_CUTOFF resolves forward (:42-43, :63) because it SELECTS between two canons and must hold a value, and it is the only cutoff in the example with a live literal. §5's justification — that blank-to-blank "would arm the predicate over the whole corpus" — is true only of a predicate written without the `!= ""` guard its three siblings already use in the same awk block, so it does not support the conclusion it is offered for. The consequence is adopter-facing and named: SPEC10_EVIDENCE_CUTOFF becomes the only optional rule that cannot ship off, and a repo pulling the kit later is armed at the shipped literal over specs it wrote before adoption.

### 27 — MEDIUM · unstated-assumption · `2026-08-31-spec-TOOL-aProvenReuse-2.md` · section 2 S6 and section 6 AC8

**Claim.** The carrier set for the retired sentence is wrong in both directions: PROTOCOL.template.md and UNATTENDED-PROTOCOL.md contain ZERO occurrences of `reuse-first`, while SKILL.template.md and its render carry the silent-waiver claim TWICE each.

**Impact.** Counted: `grep -c reuse-first` gives 0 for `tools/unattended/PROTOCOL.template.md` and 0 for `memory/guides/UNATTENDED-PROTOCOL.md`; SKILL.template.md and `.claude/skills/unattended/SKILL.md` each carry it at lines 112-113 ("Waiving it is SILENT") and again at line 157 ("`reuse-first` is silent and is recommended against"). AC8's model — "two templates and two renders" — would certify a fold that edits only line 112 while line 157 still tells every future run the waiver is silent. That is the precise failure AC8 was written to prevent, passing AC8.

**Fix.** Restate S6's carriers as SKILL.template.md plus its render, BOTH occurrences, and drop the PROTOCOL rows from the retired-sentence half (keep them for the new DoD table row, which PROTOCOL.template.md:319-327 really does own). Make AC8 grep for the phrase and assert zero hits tree-wide rather than counting files. Note the count prose that must move with it: "Two rows carry a consequence" and "the two handles that have a consequence" both become one.

**Skeptic.** Counted directly. `grep -c reuse-first` gives 0 for tools/unattended/PROTOCOL.template.md and 0 for memory/guides/UNATTENDED-PROTOCOL.md; tools/unattended/SKILL.template.md and .claude/skills/unattended/SKILL.md give 3 each, of which two are the silent-waiver claim — line 112-113 ("Waiving it is SILENT: the bar stays green ... because nothing machine-checks a spec's reuse section for content") and line 157 ("`reuse-first` is silent and is recommended against"). AC8's stated model, "the claim appears in two templates and two renders," is wrong in both directions, and S6/§4's Inventory routes the retired sentence to a file pair that has never contained it. A builder working the named carrier set edits two files and can leave line 157 standing in both SKILL copies — the exact contradiction AC8 exists to prevent. (PROTOCOL.template.md is still legitimately in scope for the new DoD row in its §4 table; only the retired-sentence half of that inventory row is wrong.)

### 35 — MEDIUM · prior-art · `2026-08-31-spec-TOOL-aProvenReuse-2.md` · section 2 S4 and section 5 Performance

**Claim.** S4 states a VALUE equality ("its `worktree` value names this run's tree") while section 5 implements it as "one `grep -c` over a file", with no anchoring stated. Every worktree path in this repo is `C:/projects/coding-governance/.claude/worktrees/<name>`, so the primary tree's own logged value is a strict PREFIX of all 100+ linked-worktree rows (measured: 18 primary rows, the rest nested beneath them).

**Impact.** A run whose tree is the primary tree, or a worktree whose name is a prefix of a sibling's, reports MET off another tree's probes and prints an inflated count in `DOD_OUT`. This is `memory/gotchas/id-matched-as-a-substring.md` on paths; `query.py` already treats worktree identity as an exact-value question (`cache_dir()` sub-keys on `sha1(str(repo))`, `evict_dead_siblings` compares `man.get("worktree")` whole).

**Fix.** Spell the match with both delimiters — the field prefix `"worktree": "` and the closing `"` — in S4, and add an S7 arm with two rows whose paths are prefix-related, asserting the nested one is not counted for the parent.

**Skeptic.** S4 states value identity ("its `worktree` value names this run's tree") while §5 fixes the mechanism as "one `grep -c` over a file" with no anchoring, and the population makes that difference load-bearing: measured, 18 rows carry the primary tree `C:\\projects\\coding-governance` and every remaining row is `...\\.claude\\worktrees\\<name>` nested beneath it, so the primary tree's own value is a strict prefix of all 100+ sibling rows. A run in the primary tree reports MET off other trees' probes and prints an inflated count in `DOD_OUT` — the `id-matched-as-a-substring` class on paths, against a corpus whose own reader treats worktree identity as whole-value (`query.py` `evict_dead_siblings` compares `man.get("worktree")` entire). Distinct axis from finding 34; both must be fixed for the item to answer its question.

### 41 — MEDIUM · prior-art · `2026-08-31-spec-TOOL-aProvenReuse-1.md` · status header (line 3), same in unit 2's status header

**Claim.** Both specs carry `Tier-2` in the status header while `memory/builds/aProvenReuse/README.md`'s build-level rules record the M2 classification as "authored by this run at Tier 1". The two records decide the tier differently.

**Impact.** The tier is load-bearing for this build specifically: `check-memory-hygiene.sh` does `if (hdr ~ /Tier-1/) next` before the ten-section canon and every section-body assertion, so under the README's classification unit 1's own predicate would never grade either of this build's specs — AC4 would pass over a population of zero. It also decides whether hygiene check 22's acceptance ledger and M8's closing review are owed.

**Fix.** Settle the tier in one record and make the other follow. If Tier-2 stands (it should — both units change a merge-bar predicate and a DoD set), correct the README's build-level rule; if Tier-1 stands, unit 1 needs a scoping rule other than N4's ride-along on the existing Tier-2 cut.

**Skeptic.** Two records in one build folder decide the tier differently, and the tier is load-bearing. `README.md`'s build-level rules say the units are "authored by this run at Tier 1", justified with charter §8's Tier-1 predicate verbatim ("Neither adds a write path, a migration, an auth surface or a shared contract"), while both status headers carry `Tier-2`. This is not two vocabularies: BUILD-METHOD.md M2 says MISSING units are authored "at the tier `memory/guides/SESSION-KICKOFF.md` assigns", so the README slot IS a tier assignment for these units. The impact reproduces in source: `check-memory-hygiene.sh:1014` is `if (hdr ~ /Tier-1/) next`, sitting above the ten-section canon and every section-body assertion, so under the README's classification unit 1's own new predicate would never grade either of this build's specs — which directly falsifies unit 1 §4 Migration's claim that "the first file the predicate grades is the next one anybody writes — including this spec and its sibling". It also decides whether check 23's acceptance ledger (`:1285-1287`) and M8's closing review are owed. Nothing catches it: the generated build-units table derives Tier 2 from the headers, so the README prose is unchecked.

### 20 — LOW · contradiction · `2026-08-31-spec-TOOL-aProvenReuse-2.md` · section 4 Rollout against TOOL-aProvenReuse-1 section 4 Rollout

**Claim.** Unit 2's Rollout offers 'One commit with TOOL-aProvenReuse-1's, or immediately after it', while unit 1's Rollout says 'One commit' of its own; the shared-commit option puts two units-built passes in one commit.

**Impact.** BUILD-METHOD M6 requires a commit at the end of every pass 'with the unit id in the subject', and M2's decompose rule exists so a closing diff can tell which half a finding lands on. Taking the shared-commit option makes the M6 bug-class checklist over HEAD~1..HEAD cover both mechanisms at once and leaves the closing review unable to attribute a finding. The two specs also then disagree about what unit 1's commit contains.

**Fix.** Delete the shared-commit option from unit 2's Rollout: two units, two passes, two commits, unit 1 first — which is what the `order 1` / `order 2` headers already say.

**Skeptic.** Verified against memory/guides/BUILD-METHOD.md M6: "A PASS is exactly one of: ... a unit built" and "Commit at the end of every pass, on the run's branch, with the unit id in the subject." Two units built are two passes and therefore two commits. Unit 2 §4 Rollout sanctions "One commit with TOOL-aProvenReuse-1's", which is one commit for two passes, and collides with unit 1 §4 Rollout's own "One commit." The build README's second build-level rule gives the same reason M2 does ("would make the closing diff unable to say which half a finding lands on"), and the gen:build-order table sequences the two units with Parallel=no. Low severity — the alternative clause "or immediately after it" is compliant — but the sanctioned option is a real method violation and the two specs do disagree.

### 40 — LOW · prior-art · `2026-08-31-spec-TOOL-aProvenReuse-2.md` · section 5 Production-readiness checklist, Performance bullet

**Claim.** "the file is tens of KB after 169 records" is wrong by an order of magnitude — measured 358,453 bytes over 174 rows — and `memory/builds/aQuarriedLantern/spec/2026-08-03-spec-aQuarriedLantern-1.md:236` already recorded the shape ("Re-measured upstream: 2,414,771 bytes over 676 rows"). "Bounded by nothing today" also omits the standing decision at `memory/builds/aDrainedSluice/spec/units/2026-08-08-spec-aDrainedSluice-7-v6-recall-cache-cap.md`, whose non-goals say capping `queries.jsonl` was considered and declined.

**Impact.** The cheapness argument for the read is sized on a number ten times too small, and a future reader treating section 5 as the measurement inherits it; the growth question reads as unexamined when a record already examined and closed it.

**Fix.** Replace the figure with the measured one and cite `aQuarriedLantern-1`'s row-size record, and add one clause naming `aDrainedSluice-7`'s decision not to cap the log as the reason "bounded by nothing" is acceptable.

**Skeptic.** Measured independently and the spec is wrong by an order of magnitude. `git rev-parse --git-common-dir` resolves to `C:/projects/coding-governance/.git`, and `.git/recall/queries.jsonl` is 365,995 bytes over 179 lines (120 `query`, 58 `opened`, 1 `refused`) — hundreds of KB, not "tens of KB after 169 records", under any reading of the denominator. That sentence sits in §5's Production-readiness checklist, where it reads as a measurement a later reader inherits, and the build's own §10 recall probe did not surface the standing decision that already examined this: `memory/builds/aDrainedSluice/spec/units/2026-08-08-spec-aDrainedSluice-7-v6-recall-cache-cap.md:67` declines capping `queries.jsonl` on the ground that "it is small" — a claim the same measurement now strains. The performance CONCLUSION survives (one grep over 358 KB is still cheap), which is why this is low and not a blocker, but the stated figure is a verifiable falsehood, not a style preference.

## Refuted and dropped

- **2** · high · section 6 AC4 — AC4 requires "ZERO hits over 346 landed §10 sections" at the declared cutoff, but with SPEC10_EVIDENCE_CUTOFF at 2026-08-31 the graded set is exactly two files — this build's own two specs. The 346 se
  - *Refuted:* The 346 figure is exactly right (348 §10 sections minus this build's own two), and AC4 is not vacuous: at the declared cutoff the arms grade the two Tier-2 specs dated 2026-08-31 (aScouredKit-35/-36 are Tier-1), which §4 Migration names explicitly — they would red had their §10 lacked the tokens. The spec itself labels AC5 as AC4's liveness assertion precisely because AC4 reds nothing on landed fi
- **7** · medium · section 2 S5/S6 and section 4 Inventory — `memory/HYGIENE.md` lines 175-185 are check 12's public description — they enumerate what it asserts, arm by arm, including the STREAMS and SPEC_WITNESS ratchets — and neither that file nor its source
  - *Refuted:* The factual half checks out (HYGIENE.template.md:175-185 and its render enumerate check 12's arms including the STREAMS and SPEC_WITNESS ratchets, and neither file is in scope), but the stated impact does not survive the spec. S4 requires the failure line to name the missing arm AND the cutoff, and S5 puts the two required facts plus the cutoff into memory/TEMPLATE-SPEC.md §10 — which is precisely
- **8** · medium · section 2 S3 — S3 requires the log path be derived via `--git-common-dir` and "never spelled as a path literal", but no section-6 criterion distinguishes that from `--git-dir` or from a literal: AC2, AC3 and AC4 all
  - *Refuted:* The spec declares this blind spot in the words the finding uses. §7 states: 'What no gate here checks: that the log this item reads is the log the agent's probe wrote. The two are joined by a path both readers derive the same way, and a divergence would show as AC2's `absent` outcome rather than as a wrong verdict.' That is the charter's sanctioned form for an exemption, and the failure mode is fa
- **9** · low · section 2 S2 waived arm, against section 6 AC1 — S2 sources the waived outcome from `recorded_waivers "$rel"`, which by its own definition returns only the sorted HANDLES (`sed -n 's/… waiver · item \([^ ]*\) · reason .*$/\1/p'`), while AC1 requires
  - *Refuted:* Over-reads S2. `recorded_waivers` (unattended.sh:1135-1141) is named as the MEMBERSHIP test — 'reuse-first appears in recorded_waivers "$rel"' — not as the source of the message text; the sentence about DOD_OUT naming the reason is a separate clause about the output. The reason sits on the same anchored park line in the run-state file (park writes `waiver · item X · reason …`), so composing the me
- **10** · low · section 2 S4 — S4 requires the failure line to name both the missing arm AND "the cutoff that armed it", but AC1 and AC2 observe only `§10` and the arm name; no criterion observes the cutoff in the message.
  - *Refuted:* S4 itself binds the cutoff into the failure line, and §5 Observability restates it ("the failure line names the file, the missing arm and the cutoff"). AC1/AC2 name a subset of the message's fields; neither says "and nothing else", so no criterion contradicts S4. An AC that observes fewer fields than a scope item requires is an ACCEPTANCE-precision preference, not a defect that makes the spec unbu
- **11** · low · section 6 AC6 — AC6 requires "the arm count the suite reports moves by four", but `tools/memory-tree/check-memory-hygiene.test.sh` reports `PASS ($n assertions)`, and existing fixtures increment `n` more than once ea
  - *Refuted:* The factual half checks out — the suite prints `PASS ($n assertions)` (check-memory-hygiene.test.sh:1619) and the check-16 fixture does bump `n` twice for one arm (lines 1603, 1605). But AC6 is satisfiable rather than self-defeating: many existing arms in that suite are exactly one `n=$((n+1))` plus a `case` over `$out`, so four arms at one assertion each moves the reported number by four without 
- **14** · high · section 2 S3 arm T against section 8 Q1, and against TOOL-aProvenReuse-2 section 2 S2's waived arm — Arm P was given a waiver-shaped satisfier (`reuse-first`) precisely so a waived run can still land; arm T was given none, so a run that waived `reuse-first` and therefore ran no recall probe still can
  - *Refuted:* The causal claim fails. Arm T is a case-insensitive substring test for `recall terms` or `--terms` (S3), so a waived run lands a post-cutoff spec with one honest line — `Recall terms used: none — reuse-first waived (<reason>)` — which satisfies arm T and arm P together. `reuse-first` is not made unwaivable, so Q1's out-of-bounds outcome is not produced and unit 2's waived-MET arm does not contradi
- **15** · high · section 4 Design, 'the assertion sits ... inside the block the awk already gates on want == canon10' — No such block exists. In `tools/memory-tree/check-memory-hygiene.sh` the Tier-2 body assertions compute `want` at line ~1018 and use it in exactly one comparison, `if (got != want)`; the empty-body lo
  - *Refuted:* The factual observation is right — `canon10` appears only at check-memory-hygiene.sh:1019-1020, where `want` feeds the ternary `wantn` and the single `if (got != want)`; there is no existing block gated on `want == canon10`, and the empty-body loop is ungated. But that is a mis-description of prior art, not a wrong requirement: S2 binds both conditions explicitly ("for a spec graded against the te
- **16** · medium · section 2 S6, section 4 Inventory, section 6 AC8 — The carrier set is mis-stated. The 'waiving it is SILENT' claim appears only in `tools/unattended/SKILL.template.md` and its render `.claude/skills/unattended/SKILL.md`, TWICE in each (line 112 and ag
  - *Refuted:* The carrier count is indeed wrong — grep confirms the claim lives only in tools/unattended/SKILL.template.md:112 and :157 and its render .claude/skills/unattended/SKILL.md at the same two lines, with no `reuse-first` hit in PROTOCOL.template.md or memory/guides/UNATTENDED-PROTOCOL.md. But AC8's operative check is the backstop the finding's impact needs: "no tracked file still asserts that waiving 
- **17** · medium · section 2 S3 arm P against section 1 Goal — Arm P accepts the bare token `reuse-first` unconditionally, but section 8 Q1 justifies that token only for a run that actually recorded a waiver — a fact the checker cannot see.
  - *Refuted:* This asks for exactly what N1 withholds: "grading the QUALITY of what §10 records. A citation that is wrong is out of reach of any substring test, and pretending otherwise is the false-confidence class". Q1 also resolves the `reuse-first` token deliberately and on the record, with the reason that rejecting it would make the directive effectively unwaivable — a rule change M3 veto 2 puts outside th
- **18** · medium · section 2 S2 against TOOL-aProvenReuse-2 section 10 — The two specs disagree on whether the reuse obligation is per-SET or per-SPEC: unit 2's section 10 records 'The same query as this build's other unit, per M5's rule that the obligation is satisfied on
  - *Refuted:* M5 does not say what the finding needs it to. Its own text (BUILD-METHOD.md:141-151) makes the PROBE once-for-the-set and then says "**Write into the spec's §10** … the seam you will extend … **and the recall terms you used**" — singular, per spec — and its first line already reads "The obligation is memory/TEMPLATE-SPEC.md §10 and is machine-checked there". Both units of this build demonstrate th
- **19** · medium · section 6 AC4 — AC4 claims the declared-cutoff run 'must produce ZERO hits over 346 landed §10 sections', but with SPEC10_EVIDENCE_CUTOFF at 2026-08-31 the predicate grades none of those 346 — S2's date gate excludes
  - *Refuted:* The population figure is indeed misdescribed (at ECUT=2026-08-31 only 2 Tier-2 specs are graded on this branch, not 346/348), but the spec already says exactly what the finding says it omits. AC5 is labelled verbatim as "the liveness assertion for AC4: a predicate that reds nothing at its declared cutoff must be shown capable of redding, or AC4 is indistinguishable from a check that matches nothin
- **28** · medium · section 2 S3 arm P (and section 8 Q1) — Accepting the bare token `reuse-first` makes both arms satisfiable by prose alone, with nothing joining the token to a recorded waiver.
  - *Refuted:* Asks for exactly what N1 withholds: "grading the QUALITY of what §10 records ... is out of reach of any substring test, and pretending otherwise is the false-confidence class". Q1 resolves the token on a stated ground the finding does not engage — rejecting `reuse-first` makes the directive unwaivable for every post-cutoff spec, an M3-veto-2 rule change. The proposed join is also architecturally u
- **29** · low · section 4 Design (the §10-body collection paragraph) — The predicate runs over the UNFENCED body — check 12 builds `body[]` through the fence machine — and section 4 never says so.
  - *Refuted:* The mechanism is real (`check-memory-hygiene.sh:803-818` drops fenced lines before `body[++n] = line`), but §4 does cite the exact array — "the same `body[]` array the empty-body loop walks" — whose fence semantics are defined ten lines above it in the same awk block, so the spec points at the source rather than misdescribing it. The failure needs an author who fences the probe command AND writes 
- **30** · low · section 2 S3 — "the same rule both existing readers use" is not what S3 spells: `git rev-parse --git-common-dir` returns a RELATIVE value in the primary tree, and both existing readers resolve it while S3's literal 
  - *Refuted:* The measurements hold (`.git` at the primary root, `../.git` from `tools/`, absolute in a linked worktree), but the finding concedes S3's expression is correct today, and I verified why it stays correct: `cd "$ROOT"` at unattended.sh:275 is the ONLY `cd` in the whole driver, so cwd is invariant for every verb. S3's claim is about the location RULE — git common dir + `recall/queries.jsonl`, which b
- **31** · blocker · section 2 S3 arm T (with section 3 Non-goals, and section 4 Inventory) — Arm T converts BUILD-METHOD M5's explicitly SET-level obligation into a per-spec merge-bar requirement, and neither `memory/guides/BUILD-METHOD.md` nor its render source `tools/memory-tree/BUILD-METHO
  - *Refuted:* The cited section says what the finding claims it does not. M5's first sentence DELEGATES the obligation's content — "The obligation is `memory/TEMPLATE-SPEC.md` §10 and is machine-checked there" — to the file S5 edits; "Satisfy it once for the SET" governs the two probe COMMANDS in the code block it introduces ("in this order — map dossier first, decision records second:"). M5's next paragraph is
- **32** · high · section 10 Reuse audit (and section 4 Alternatives rejected, "Requiring arm P only") — Section 10 cites dFramedEntrypoint's RECALL-1 as corroboration and omits both that its own header reads "CONFIRMED · raised at MEDIUM, final LOW" and that round 2's R2-7 returned REFUTED/DROP on the i
  - *Refuted:* RECALL-1's status line reads CONFIRMED, and its own skeptic wrote "Reproduced ... So M7 step 5 is unperformable for every unit" — precisely what §10 cites it for ("reproduced this defect on an eight-spec build"). The final-LOW triage was about fold priority inside that build, not about whether the defect is real, so omitting the severity is not a misrepresentation. R2-7 refuted a narrower proposit
- **36** · medium · section 2 S3 and section 10 Reuse audit — S3's claim that the location is "DERIVED by the same rule both existing readers use" is false, and the seam list in section 10 misses the one inside the file being edited. `tools/memory-recall/recall-
  - *Refuted:* Every load-bearing half fails. The bare-`git` concern is refuted by the kit's own gate: `check-unattended.sh` check 28 exempts an invocation where every argument after the verb is a flag, because it "cannot name a revision" — and its measured note names `rev-parse --show-toplevel` and friends as exactly that population, so `git rev-parse --git-common-dir` is exempt by property and the replace-ref 
- **37** · medium · section 3 N5 — N5's premise is refuted by the recorded rationale it paraphrases. `tools/unattended/unattended.sh:361-363` says `pieces-complete` joins `DOD_NO_OVERRIDE` "because it is the item that says the run prod
  - *Refuted:* The cited source refutes the finding, not N5. `tools/unattended/unattended.sh:361-363` reads: "`pieces-complete` joins it because it is the item that says the run produced what the owner asked for, over content nothing else in the bar can grade. An override on it is the run certifying its own output, WHICH IS THE SAME SHAPE THE AUTHORIZATION ITEM IS PROTECTED FROM." The operative clause — the one 
- **38** · medium · section 6 AC4 and AC5 — Both criteria pin a derived count in prose — "ZERO hits over 346 landed §10 sections" and "reds and names 253 files" — against AGENTS.md §7's recorded rule that no count of a derived population is wri
  - *Refuted:* Misreads the cited rule and is contradicted by ratified corpus idiom. AGENTS.md §7's rule is about a CHECKER's own reported figures ("The checker derives every figure it reports; a number typed beside the thing it counts") — it governs live claims sitting beside a source that owns them, not a dated observation recorded as evidence in a build spec. This repo's own `.memory-tree.conf` does exactly w
- **39** · medium · section 2 S1 and section 4 Inventory — S1 says the key is "declared in `.memory-tree.conf`, in the same shape ... as `SPEC10_CUTOFF`", but `SPEC10_CUTOFF` is the ONE cutoff this repo does not declare there — it lives only as the shipped de
  - *Refuted:* The premise checks out — SPEC10_CUTOFF is absent from `.memory-tree.conf`, living only at `check-memory-hygiene.sh:42` and `.memory-tree.conf.example:44` — but the harm story inverts the mechanism. A conf declaration winning over a moved shipped default is the DESIGNED behaviour of the whole cutoff family and the thing that protects an adopter's grandfathered corpus from a kit release silently re-

