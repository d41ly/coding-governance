# Run mandate — aRatifiedRulings

**Serves:** journal TOOL-aRatifiedRulings-1

The owner's prompt, verbatim, as handed to `/unattended --prompt` on node `a`, 2026-09-13. The bytes
travel here rather than as a reference, because the build folder is the authorization and may not
point at a file that can be edited after the run starts.

## The prompt

> Open a full build for the rulings TOOL-aLeakedHandle-6..9 per the protocol.

## What the rulings are

Four owner rulings recorded in `memory/DECISIONS.md` on 2026-09-13, each answering a decision
`aLeakedHandle` parked or inherited. Read them there; this record does not restate them, because a
restated ruling is the two-answers class and the decision log is the one that binds.

| Ruling | Unit here | Mechanism |
|---|---|---|
| `TOOL-aLeakedHandle-6` | `TOOL-aRatifiedRulings-1` | a converged subject's later blocker is disposed, never re-rounded |
| `TOOL-aLeakedHandle-7` | `TOOL-aRatifiedRulings-2` | check 23 excludes the path a `brief · item` row names |
| `TOOL-aLeakedHandle-8` | `TOOL-aRatifiedRulings-3` | the hygiene self-test gets cheaper, not a raised ceiling |
| `TOOL-aLeakedHandle-9` | `TOOL-aRatifiedRulings-4` | the rc=137 tail for a leg with no declared ceiling |

## What the run was told about scope

"A full build" is read as the whole protocol: spec, audit, build, closing review, close, land. "Per
the protocol" is `memory/guides/UNATTENDED-PROTOCOL.md` with `memory/guides/BUILD-METHOD.md`, both
read whole before the first unit. Four rulings, four units, one mechanism each.

No clarification was asked. Acceptance and gates are derivable for every unit from the ruling text,
the parks that motivated it under `builds/aLeakedHandle/RUN.md`, and the source each ruling names.
