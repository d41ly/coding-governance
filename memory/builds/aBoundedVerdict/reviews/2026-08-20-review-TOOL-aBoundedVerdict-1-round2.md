**Serves:** diff-review TOOL-aBoundedVerdict-1 TOOL-aBoundedVerdict-2 TOOL-aBoundedVerdict-3 TOOL-aBoundedVerdict-4 TOOL-aBoundedVerdict-5 TOOL-aBoundedVerdict-13 TOOL-aBoundedVerdict-17 TOOL-aBoundedVerdict-18 TOOL-aBoundedVerdict-19

## Verdict: BLOCKED

**Shape:** raw 61 · confirmed 57 · refuted 4 · unverified 0 · lenses 5/5 (correctness · vacuity · sequencing · contracts · records). The 57 confirmed collapse to **33 distinct defects** after folding cross-lens duplicates; the fold is recorded in the `dup` column.

**Range:** `43a6c13e853ba6149600a2053cd439962413dc9d..HEAD` · **BASE** `43a6c13e853ba6149600a2053cd439962413dc9d` · 55 files, +4391/-658, nine commits, one author.

**Why BLOCKED:** F1 is a confirmed blocker — the `memory-hygiene self-test` gate leg is RED at HEAD. Its guard names `tools/memory-tree/`, which this range touches, so it reds a scoped bar as well as the `GATE_FULL=1` run `.githooks/pre-push` performs. Nothing in this range can land until F1 is fixed.

## Confirmed findings

| id | lens | sev | ref | what is wrong | the fix | dup |
|----|------|-----|-----|---------------|---------|-----|
| F1 | correctness | **blocker** | `tools/memory-tree/check-memory-hygiene.test.sh:469` | Check 22's six review fixtures are written AFTER the suite's only `git add -A && git commit` (line 466) and never staged, while the engine's population is `FILES=$(git ls-files "$M/")` (`check-memory-hygiene.sh:98`), tracked-only. Check 22 grades an empty population, five arms fail, the suite exits 1 with no PASS line. The whole of check 22 landed with zero coverage and the leg is red. | Move the review-fixture block (469-484) ABOVE the commit at 466. Do not add a bare second `git add` after 467 — it would also stage the `rm -f …tFixture-13.md` and kill the `tracked but missing from worktree` arm. Verified: with the block moved the suite prints `PASS` and exits 0. | 1·14·26 |
| F2 | vacuity | high | `tools/unattended/SKILL.template.md` · `unattended.sh:2512` | `--review`, the verb implementing this build's headline remedy, appears on NO agent-facing surface: zero hits in the Skill template and its render, no row in protocol §7, no verb named by BUILD-METHOD M4, and it is missing from all three of the driver's own synopsis strings (header 5-13, refusal 2506, usage 2512) — directly under the comment claiming "THE SAME SET, in all three places". No run can learn the verb exists, so the new leg check grades a population no procedure creates. | Add a review-rounds section to `SKILL.template.md` with the four flags, the closed verdict set and the four states, re-render; add the `--review` row to §7 of `PROTOCOL.template.md` and re-render; add the verb to all three driver strings. Then add a leg arm joining the driver's `case "$VERB"` arms to the documented set. | 15·28·39·49·11·40 |
| F3 | contracts | high | `tools/unattended/check-unattended.sh:148` | The review-loop leg's third clause — F3's ratified "an exited blocker was disposed of" check — is vacuous. It tests `index(units, it)`, whether the SUBJECT appears in the `gen:build-units` region; per spec -1 S5 the subject is the build slug or a spec path, and both are substrings of every generated row already. Verified against the real region: slug, spec path and unit id all SILENT, only `NOT-A-UNIT` fires. A promotion adds a NEW id, which the clause never looks for. AC4a's red/green fixture pair does not exist; neither of the clause's two messages is asserted anywhere. | Grade a DELTA against the roster at the pinned BASE: require at least one unit id the BASE region lacked, matched as a whole id, not a substring. Then land AC4a's two fixtures and observe the red one before wiring. | 5·27·38·52 |
| F4 | correctness | high | `unattended.sh:1224` · `check-memory-hygiene.sh:943` | The §8 resolution-mark regex is matched line by line, so a mark wrapped at a line end — this corpus's own house style — never matches. Byte-identical defect in both readers. Twelve tracked specs carry a wrapped mark; running the shipped `plan_state` over the corpus, seven non-terminal specs come back FORKED with the mark present, and `verb_plan` routes `THIN|FORKED` into `next` (`unattended.sh:1378`) with no cutoff — `--plan` sends the run at already-resolved forks. `FORK_MARK_CUTOFF` keeps hygiene green today; every terminal spec authored from tomorrow reds the bar. | Grade the mark over the item's JOINED text: accumulate each item's lines into one buffer and match at the item boundary and at END. Apply identically to both readers and add a wrapped-mark row to `marker-contract.test.sh`, whose case table is the agreement proof and currently has no such row. | 2 |
| F5 | correctness | high | `unattended.sh:1219` · `check-memory-hygiene.sh:939` | Every bullet inside §8 opens a new ITEM, so the OPTION bullets belonging to one fork each demand their own mark. Live non-terminal instance: `memory/builds/aTetheredRecord/spec/2026-08-16-spec-TOOL-aTetheredRecord-2.md` (INPROGRESS) states the fork in prose, lists three option bullets, carries `RESOLVED (owner, 2026-08-17)` as a following paragraph — items=3, resolved=1, so `plan_state` prints FORKED on a resolved fork and `--plan` routes the run there. | Encode TEMPLATE-SPEC's actual rule ("one fork per bullet or `###` sub-head") rather than "any bullet": treat option lists as continuations of the open item. Note the reported predicate `^[-*][[:space:]]+\*\*` does NOT fix it — the cited option bullets are `- **Option A…**` and would still count. The reported reader asymmetry is also wrong: `plan_state` trims leading whitespace at line 1203 and counts indented bullets exactly as the hygiene reader does. | 3 |
| F6 | correctness | high | `unattended.sh:291` | `norm_endpoint` recognises scp-style only when the URL carries a user (`*@*:*`). A userless scp URL falls through and `host=${u%%/*}` + `host=${host%%:*}` silently DELETES the first path segment. Run against the shipped function: `github.com:alice/repo.git` and `github.com:bob/repo.git` both → `github.com/repo` (two repositories compare EQUAL, so a split fetch/push across different repos takes the NOTE branch and the run anchors on one repo while landing on another); `git@github.com:alice/repo.git` → `github.com/alice/repo` vs `github.com:alice/repo.git` → `github.com/repo` (one place, two spellings, DIFFERENT — the wedge TOOL-aBoundedVerdict-17 exists to remove). | Detect scp-style by git's own rule — a colon before any slash — and split there: `case "$u" in *://*) u=${u#*://} ;; *) u=${u#*@}; case "${u%%/*}" in *:*) u="${u%%:*}/${u#*:}" ;; esac ;; esac`. Add both pairs as arms; all four existing arms are user@ or https. | 4 |
| F7 | records | high | `tools/unattended/unattended.test.sh:2699` | Both split fetch/push fixtures produce fetch == push, so the warn-and-continue branch at `unattended.sh:450-459` — the entire runtime behaviour of TOOL-aBoundedVerdict-17 — is never executed. MECHANISM ONE's sed strips the `ssh://` prefix back off a drive-letter path (identical URL on Windows, the nodes that built this); MECHANISM TWO sets `url.<ORIGIN>.pushInsteadOf = <ORIGIN>`, an identity rewrite, so it is a no-op on EVERY platform. Both `miss` assertions pass by finding nothing. Reproduced in a scratch clone. This is `memory/gotchas/fixture-passes-by-finding-nothing.md` inside the arm written to prevent it. | Make MECHANISM ONE split on Windows too, map a DIFFERENT base in MECHANISM TWO so the rewrite is not the identity, and assert POSITIVELY (`hit "$out" "normalise to the SAME endpoint"`) — an absence assertion cannot distinguish "the branch warned" from "the branch never ran". | 47 |
| F8 | records | high | `.unattended.conf:122` · `push-main.sh:80` | The new `LANDER_MARKER=".git/unattended-landed"` is resolved against a different tree by each half and cannot exist in a linked worktree, where `.git` is a FILE (measured here: `printf x > .git/unattended-landed` → `Not a directory`). `push-main.sh` writes relative to its own `$top`; `--landed` reads relative to the driver's `cd "$ROOT"`. Two further defects in the same block: the lander parses the key with `sed -n 's/^LANDER_MARKER="\(.*\)"$/\1/p'` while the driver SOURCES the conf, so an unquoted or single-quoted value is honoured by one and invisible to the other; and the write is `> "$lm" 2>/dev/null \|\| true`, so a failed write still prints `landed` and `--landed` then refuses with check 34 blaming the lander. | Resolve the marker through `git rev-parse --git-common-dir` in BOTH halves, read the key the way the driver does, and drop the `\|\| true` — a lander that pushed but could not record it must say so. | 31·48 |
| F9 | vacuity | high | `memory/guides/UNATTENDED-PROTOCOL.md:385` | §8, the BINDING table of `.unattended.conf` keys, omits all three keys this range adds — `HALT_CODES_EXTRA`, `HALT_FLOOR`, `LANDER_MARKER` — and they appear nowhere else in either protocol carrier. `HALT_FLOOR` is MANDATORY: `check-unattended.sh:160` reds when it is undeclared, the same treatment the table marks MANDATORY for `CORE_FLOOR`. An adopter configuring from the contract gets a red bar naming a key the contract never mentions. The parity leg compares template to render, and both are identically incomplete. | Add the three rows (marking `HALT_FLOOR` MANDATORY) to `PROTOCOL.template.md` §8 and re-render; add the pre-existing `UNITS_REGION_CUTOFF` while there. Close the class with a leg arm asserting every `^[A-Z_]+=` key in `.unattended.conf.example` appears in the table and vice versa. | 16·56 |
| F10 | sequencing | high | `unattended.sh:78-81` · `check-unattended.sh:385` | `REMOTE_BOUND_LIVE` is computed and never emitted. The comment two lines up states the obligation — "it must SAY the bound is inert, because a skip that looks like a pass is indistinguishable from coverage" — and the flag's only reader is the `if`. On a node with no working `timeout -k`, every remote observation runs with the wall-clock bound silently absent and byte-identical output. Same hole in the leg. The AC1a arm measures the SUITE node's `timeout` and never reaches a run. | Emit one line from both files when the probe fails, naming the bound as INERT, and add an arm that stubs `timeout` off PATH and asserts it. Note `http.lowSpeed*` and `ssh -o ConnectTimeout` still apply on that path, so it is the wall-clock bound specifically that is lost. | 29·51 |
| F11 | records | high | `memory/guides/BUILD-METHOD.md:181` · `:109` | Two paragraphs were DELETED, not displaced, and the method now carries a dangling pointer plus a vanished rule. M6 says "Why clause 3 is worded as it is … is in the memory-tree README" — it is not there; `tools/memory-tree/README.md` gained exactly one section in this range (M12). M4's "**Not the harness.** `tier2-review.js` reviews DIFFS … calling a spec 'reviewed' by it is false" is gone with no replacement, while M4 still says "Run it as a `Workflow` script" and the only Workflow script the method names is that harness at M8. `spec-TOOL-aBoundedVerdict-1` rev-11 records both as moved, so AC6's displacement criterion is recorded as met and is not; `memory/map/features/build-method.md:86` still lists the deleted rule as live. | Actually move both paragraphs into the README's displaced-sections block, or drop the M6 pointer and restore M4's one-line rule. Correct rev-11 and the dossier bullet either way. Budget headroom is 2 lines (288 of 290), so displacing for real is the cheaper option. | 42·45·50 |
| F12 | records | high | `unattended.sh:1216` | `plan_state` classifies a §8 with an EMPTY body as READY while the hygiene reader reds it, and `TEMPLATE-SPEC.md` names both readers and states "A §8 with neither an item nor a `none` form is a refusal, not a pass." Verified by running the sliced function: empty §8 → READY; the hygiene gate refuses the same fixture. This contradicts the build's OWN ratified fork — TOOL-aBoundedVerdict-4 §8 F1, "RESOLVED (owner, 2026-08-17): refuse. Each gate therefore gains one new `fail` branch" — and `plan_state` did not gain it. The marker-contract table's "hollow" row uses a PROSE line, which both readers refuse, so the one case they split on is the case the conformance artifact does not pin. | Make the `items == 0` branch print FORKED when `forkline == ""`, keeping READY for the `none`/`n/a` forms, and add `mark_case "empty: no body at all" red FORKED ''` as an eleventh row. | 55 |
| F13 | correctness | medium | `unattended.sh:2367` | The terminal-round guard greps `(CONVERGED\|NON-CONVERGENT)` and omits CEILING, while the leg's own `term` set is all three (`check-unattended.sh:136`). Reproduced with the shipped `review_state` and `RUNAWAY_CEILING=8`: round 8 → CEILING, and round 9 is accepted and appended. `check-unattended.sh:141` then reds on `n[it] > ceil` — permanently, on an append-only committed record no verb can rewrite, which is the exact hazard the BYPASS_BAN guard on this same verb exists to prevent. | Add `CEILING` to the alternation so the two sides agree, and arm a fixture that records a round after a CEILING round. | 6·19·53 |
| F14 | vacuity | medium | `unattended.sh:2368` | The same guard interpolates caller-supplied `$subj` into an ERE unescaped. Reproduced: with `subj='F1 (fork)'` the grep MISSES its own recorded terminal line, so a further round is accepted after CONVERGED and the append-only history says the opposite of what happened; a subject containing `.` over-matches and produces a spurious refusal. `review_counts` (2333) compares the item EXACTLY in awk, so the two readers of one field disagree on what "same subject" means. | Stop matching the subject as a regex: `grep -F -- "· item $subj · reason " "$rel" \| grep -qE '(CONVERGED\|NON-CONVERGENT\|CEILING)'`, or better, have `review_counts`'s awk emit the terminal flag so one reader owns subject identity. Add a parenthesised-subject arm. | 18 |
| F15 | sequencing | medium | `unattended.sh:2347` | `verb_review` is the fifth caller of `park()` and the only one that does not guard the row grammar it writes. `verb_park` refuses a newline in the item ("would forge a second parked row nothing wrote") and refuses an item spelling the ` · ` separator; `verb_review` screens only the bypass flag. A newline in the subject forges an arbitrary parked row — including a `decision` row that inflates the count `parked-decisions-surfaced` compares against — and a subject containing ` · reason ` silently re-keys the group, because both readers split on the FIRST occurrence. | Lift `verb_park`'s three text guards into one helper and call it from both verbs; `park()`'s grammar is shared, so the guard belongs with it. | 32 |
| F16 | contracts | medium | `check-unattended.sh:382` | The leg's "single source" read of the three remote bounds is dead. `core_of` matches only `KEY="value"`; the driver declares `REMOTE_BOUND=60`, `REMOTE_CONNECT_BOUND=20`, `REMOTE_LOWSPEED_BYTES=1000` UNQUOTED (68, 69, 73). Executing the leg's own function returns all three empty (against `RUNAWAY_CEILING=[8]`, which is quoted), so the `:-60`/`:-20`/`:-1000` fallbacks always apply and the comment directly above them describes something the code does not do. Tuning the driver leaves the leg on 60. | Quote the three driver constants and DROP the fallbacks so an unreadable bound refuses the way `RUNAWAY_CEILING` already does at line 110 — a defaulted read is indistinguishable from a successful one, which is why this went green. | 8·41 |
| F17 | vacuity | medium | `check-unattended.sh:521` | Check 8's newly-unexempted malformed-marker refusal is wrapped in `if [ -f "$rd" ]` where `rd=${f%/RUN.md}/README.md`. For an ARCHIVED record — `RUN.<PHASE>.<8hex>.md`, which the selector at 284 deliberately admits — the suffix strip is a no-op and `rd` names a path INSIDE the record file, so the guard is always false and the WHOLE of check 8 is skipped. Three tracked archived records are silently exempt, falsifying the tightening's own comment ("a marker pair is either well-formed or it is not, in every phase") and weakening its "unexempting reds NOTHING" measurement, taken over a population three records short. `rd` is read nowhere else. | Delete `rd` and the guard; the block reads only `$f`. Keep the `term` exemption on the emptiness arm alone. Add a red `RUN.ABORTED.deadbeef.md` fixture — the existing tMarker arm uses `RUN.md` and cannot catch this. | 20 |
| F18 | records | medium | `check-unattended.sh:143` | The non-convergence clause reds only at `flat[it] >= 2` — two consecutive non-shrinks, i.e. three rounds — while the check's own header states the invariant as one. A two-round `2, 2` record with no exit token passes silently. The fixture pair cannot distinguish the thresholds: the red fixture is a three-round `2, 2, 3` and the "control" is a different two-round `2, 2` that is green with or without its token, so the comment's claim that the token is what makes it green is over-determined. | Change to `flat[it] >= 1`, or state the tolerance and its reason in the header. Either way add the two-round-no-token fixture and observe it RED. | 54 |
| F19 | correctness | medium | `check-unattended.sh:557` | The leg collapses three distinct outcomes into one message: the remote genuinely advertising nothing, the wall-clock bound firing (rc 124), and `mktemp` failing so nothing was observed at all. `adv_f=$(mktemp) \|\| adv_f=""` skips both observations silently; `observe_remote`'s status is discarded by the `&&` at 417 and 419; the only branch left is `fail 9 "the remote advertised no tips"`. The driver separates the same three one file over (`unattended.sh:472` scratch refusal, `:475` "KILLED by this kit's own wall-clock bound rather than answered") — the fix was made once and not carried across. | Capture the status (`observe_remote …; rc=$?`) and emit distinct fails for rc 124 and for the mktemp failure, keeping "advertised no tips" for the case where git actually answered. | 10 |
| F20 | correctness | medium | `unattended.sh:456` | The split-URL NOTE appended to `DOD_OUT` reaches no reader. `verb_close` calls `observe_anchor` at 1947, the DoD loop starts at 1975, `DOD_CORE` begins with `gates-green`, and that arm opens with an unconditional `DOD_OUT=""` (2060) — so the note is destroyed on every ordinary close, contradicting the comment's "rides the close-path diagnostic channel … a caller that discards output does not discard the fact". When `gates-green` IS overridden the arm is skipped, the stale note survives, and the next unmet item prints it indented as ITS detail — an endpoint note attributed to `records-current`. | Drop the `DOD_OUT` write and keep the `echo` (the channel that works), or hold the note in its own variable printed once before the loop. Either way clear `DOD_OUT` on entry to `dod_met` so no item inherits another's text. | 9·23·30·58 |
| F21 | vacuity | medium | `memory/guides/UNATTENDED-PROTOCOL.md:296` | The `landed-via-lander` row still reads "the run-state record names no bypass flag", but this range DELETED that grep — the diff removes `[ -n "$LANDER" ] && [ -n "$BYPASS_BAN" ] && ! grep -qF …` and the arm now only asserts `[ -n "$LANDER" ]`. The two neighbouring rows were rewritten in the same hunk, so this one was passed over. §7's `--landed` bullet is likewise silent about the new marker refusal (fail 34). | Rewrite the row to what the predicate now asserts — a declaration check, not an observation of the push — and add the marker refusal to the `--landed` bullet. Edit the template and re-render. | 17 |
| F22 | sequencing | medium | `unattended.sh:1491` | The marker check tightens `--landed` from "HEAD is an ancestor of the advertised tip" to "HEAD is exactly the commit the lander pushed", and no agent-facing carrier says so: the verb's own header still states the ancestry rule, and the Skill's "Mark it landed" section still says only "refuses unless HEAD is an ancestor of the tip the remote advertises". `grep -i marker` over the Skill, its template and the protocol returns only generated-region hits. Any commit between the push and `--landed` now hits check 34. | State the equality rule in `SKILL.template.md` and beside protocol fact 8, and have the refusal name the commit the marker DOES hold alongside the one it wanted, so "stale marker" is distinguishable from "HEAD moved". (The inline comment at 1479-1490 does justify the rule — this is a carrier gap, and the refusal does name a remedy.) | 34 |
| F23 | contracts | medium | `check-memory-hygiene.sh:641` | The closed review-verdict vocabulary is spelled independently in two kits with nothing pairing them: the hygiene engine hardcodes the three tokens, `unattended.sh:245` hardcodes `REVIEW_VERDICTS`. The sibling grammar that landed in the same range got contract 2 in `marker-contract.test.sh` precisely so a cross-kit edit reds; this pair got nothing, so a drift lets a run record a token the hygiene gate then refuses. AC3 of `spec-TOOL-aBoundedVerdict-1` explicitly required "a row in `marker-contract.test.sh`'s case table [that] reds when the driver's set and the memory-tree kit's set diverge" — that row is absent, so a stated acceptance criterion is unmet. | Add the third contract to `marker-contract.test.sh` (the harness already slices awk out of the shipped driver bytes for `plan_state`, so the same technique applies). | 46 |
| F24 | vacuity | medium | `tools/memory-tree/README.md:18` | Check 22 landed and neither carrier of the gate's check COUNT moved: the kit README still says "the gate — 21 checks (1-12 and 21 in the shell…)" and `memory/map/features/memory-tree-hygiene.md:31` still says "21 checks". AGENTS.md designates the README as THE carrier of that number, and the gate-leg names carry none, so this is the one figure a reader is told to trust and it is wrong. Nothing gates it. | Update both to 22 and extend the parenthetical split. Better, since this is the third retyping: have the self-test assert the README figure equals the engine's own derived check count, so the prose cannot drift again. | 21 |
| F25 | correctness | low | `tools/memory-tree/marker-contract.test.sh:247` | `MARK_NAMES` accumulates space-separated multi-word case names, then `set -- $MARK_NAMES` word-splits them: ten cases become ~33 positional parameters, so case 1's label reads `none,`, case 2's `zero`, case 3's `items`. Grading is unaffected (the fixture index is driven off the want lists) and the suite is green, so the misattribution surfaces only on the run where a row goes red — the one run where the label has to be right. | Use a bash array (`MARK_NAMES+=("$1")`, indexed `${MARK_NAMES[i-1]}`) or a delimiter-separated list split with `IFS`. | 12·35 |
| F26 | contracts | low | `tools/push-main.sh:76` | The comment claims "The unattended verb compares the sha AND the build slug it is about to record, so evidence of an earlier landing cannot stand in for this one." Neither half is true: the line written is `landed <default-branch> at <sha> by push-main` — no slug — and `verb_landed` greps the sha alone. The driver's own comment ("It carries IDENTITY, not existence") is accurate; this one overstates the anti-replay property. | Delete "AND the build slug" — the sha-only check is defensible, the sentence is not. `push-main` has no slug in scope on that path. | 24·43·59 |
| F27 | vacuity | low | `tools/unattended/check-unattended.test.sh:806` | The arm that BOUNDS the read-only exemption uses `> *"?$` — a bare `$`, an end-of-line anchor in ERE — while the arm it bounds (798) uses `> *"?\$`, a literal dollar. Line 806 also drops the `(^\|[^-[:alnum:]])` prefix its sibling carries. Every `>"$scratch"` redirect the exemption removes is therefore outside the bounding arm's population, so the stated bound does not hold over redirects; it still fires on `mv`/`rm`/`cp`, which is why it reads as armed. | Factor one shared pattern variable and use it in both arms, so the bound provably covers the exemption's whole population. | 25 |
| F28 | sequencing | low | `unattended.sh:542` | `branch_tip_quiet` returns 5 on a local `mktemp` failure, and `emit_branch_fail` renders 5 as "the remote could not be reached … or the wall-clock bound fired". A dead TMPDIR sends the operator at the network — the misdiagnosis the new 2-vs-5 split was introduced to remove, per its own header. `observe_anchor` got a named refusal (`fail 27`) for the identical fault one function away. | Return a distinct code and give `emit_branch_fail` a branch naming the scratch path, mirroring check 27's wording. | 36 |
| F29 | correctness | low | `unattended.sh:1491` · `:2236` | `_lm_head` in `verb_landed` and `_pv`/`_pn`/`_pa` in `dod_met` are assigned without `local`, unlike every sibling temporary in both functions (`local _bcids …` and `local _crfound …` sit in the two neighbouring arms). No read-before-write exists, so nothing misbehaves today, and there is no shellcheck leg to catch it — but `dod_met` is called in a loop, which is the shape where a leaked temporary becomes a cross-item bug. `_lm_head` also duplicates `head=$(GIT rev-parse HEAD)` twelve lines later. | Add `_lm_head` to `verb_landed`'s `local` list (or reuse `head`), and `local _pv _pn _pa` in the parked-decisions arm. | 13 |
| F30 | contracts | low | `memory/guides/UNATTENDED-PROTOCOL.md:158` | "Authored, carrying exactly twelve facts and nothing else" undercounts by three. The driver also writes `mode` unconditionally at preflight (present in the live `RUN.md` as `mode: slug`), and `--attest` writes `keepalive-reaped` and `parked-surfaced` into the same region. This range re-derived the figure (eleven → twelve), so the number was touched and is still wrong; the leg's count-word arm grades the DoD table's sentence, not this one. | Enumerate `mode` and the two attestation keys, or scope the sentence to the preflight-authored subset and name the others. Both copies. Better: arm the count against the driver's `set_fact` key set the way check 16 arms the DoD count. | 44 |
| F31 | records | low | `tools/unattended/unattended.test.sh:2756` | The lander-marker acceptance arm is two `miss` assertions and no `hit`, under prose claiming the marker naming THIS commit is accepted. That acceptance is unreachable in this fixture and the file says so 1100 lines earlier: the check-32 arm uses identical prep and asserts the ancestry refusal, because the unit branch is one commit ahead of the anchor by construction. So `--landed` refuses at check 32 here and both misses hold under an unrelated refusal. | Capture the output once and assert positively on the accepted path plus the resulting phase; if the fixture cannot reach acceptance, fix the fixture or state the gap rather than leaving absence assertions to stand in for it. | 60 |
| F32 | records | low | `tools/memory-tree/README.md:138` | The displaced-sections header says "≤20 KB and ≤250 lines" while the method's own line 8 declares "≤22 KB, ≤290 lines", and two lines down "These two sections live here" now covers five `###` sections — this range added the M12 one and left the count. The budget half was already stale at BASE; the section count is the in-range half. | Delete both figures: point at the method's line 8 for the cap and say "the sections below" instead of a count, rather than re-typing numbers, which is how this went stale. | 57 |
| F33 | records | low | `memory/map/features/build-method.md:73` | The range moved `BUILD-METHOD.md` from 283 lines / 20567 B to 288 / 22069 and left the dossier claiming "Eleven lines of headroom (239 of 250; 16,680 of 20,480 B)" — wrong in the measurement AND in the caps it names. `spec-TOOL-aBoundedVerdict-1`'s Files-touched list names this dossier and the charter's DoD requires dossier prose refreshed on touch; the dossier is absent from the range's `--stat`. Line 86 also still states M4's deleted "no harness" rule (see F11). | Replace the headroom bullet with the command against the method's declared cap rather than a re-measured pair, and correct the M4 bullet. | 61 |

## Unverified

None. Every raw finding was either confirmed against the bytes or by execution, or refuted and dropped (4 refuted, not carried here). Three sub-claims inside otherwise-confirmed findings were falsified by the skeptic and are recorded inline above rather than silently kept: F5's proposed predicate and its reader-asymmetry claim, F1's `pop_guard`/`--staged` sub-claims (the correct account is F1's fix line), and F8's `.git`-is-a-file measurement, which does not reach `push-main` itself because that script refuses off the default branch — the reachable case is `--landed` invoked from a worktree, plus ordinary write failures.

## What this review did NOT cover

Scope was the nine-commit range `43a6c13e..HEAD` only. Everything outside it was out of bounds, including the PARKED `TOOL-aBoundedVerdict-21` spec sitting untouched in the tree.

Three things were not exercised. **No full merge-bar run** was performed — the verdict rests on the `memory-hygiene self-test` leg, which was run to completion and observed RED; the other legs' state at HEAD is unknown, and F1 must be fixed before a full bar tells anyone anything. **The unattended suite was not run to completion**: `unattended.test.sh` had not reached the arm F31 concerns after roughly fifteen minutes, so F31 and F7 rest on the fixture bytes, on sibling arms' own assertions, and on mechanisms reproduced in scratch repos rather than on observed suite output. **No end-to-end unattended run** was driven — `--review`, `--landed` and the marker path were reasoned about and probed function-by-function (functions were sliced out of the shipped files and executed), never exercised as a real run against a real remote.

Two classes were probed only where the diff pointed. The corpus-wide impact of F4 and F5 was measured over tracked specs by running the sliced `plan_state`, but the hygiene side's future exposure past `FORK_MARK_CUTOFF` is an inference from the cutoff constant, not an observation. And no review was made of the specs themselves as documents — acceptance criteria were read to check whether the code meets them (F3's AC4a, F12's ratified fork, F23's AC3, F11's rev-11 claim), not to judge whether the criteria were the right ones.
