**Serves:** spec-audit TOOL-aBoundedVerdict-1 TOOL-aBoundedVerdict-2 TOOL-aBoundedVerdict-3 TOOL-aBoundedVerdict-4 TOOL-aBoundedVerdict-5 TOOL-aBoundedVerdict-13 TOOL-aBoundedVerdict-17 TOOL-aBoundedVerdict-18 TOOL-aBoundedVerdict-19 TOOL-aBoundedVerdict-21

## Verdict: BLOCKED

**Shape:** raw 79 · confirmed 66 · refuted 13 · unverified 0 · lenses 5/5 (facts · cross-read ·
acceptance · assumption · fold-check). The 66 confirmed findings consolidate to **49 distinct
defects** — 8 blockers, 22 high, 15 medium, 4 low. Every reference below was re-read at HEAD before
it was written down; three prior-audit findings are re-reported because their bytes are unchanged
(B1, B2, H1), and the ordinal-in-S3 half, the 14 resolved forks, and the -21 F3 veto are treated as
by-design per the brief.

Two structural facts explain most of the list. First, this build's specs were measured at
`098bebd9`, five units have landed since, and the measured figures they carry moved — including two
that are CONSUMABLE (the charter read-path budget and the merge-bar wall clock) rather than merely
illustrative. Second, the 2026-08-20 fork sweep resolved forks whose resolutions add work to scope
items and acceptance criteria that were never amended, so four units now carry a ratified decision
that no scope item names and no criterion observes.

## Confirmed findings

| id | lens | sev | ref | what is wrong | the fix |
|----|------|-----|-----|---------------|---------|
| B1 | facts | blocker | `spec/…-2.md:166` (and `:111`, `:248`, `:286`) | §4 Migration states "four tracked run-state files … one ABORTED and carries no code". At the spec's own base there are 7 files / 3 codeless ABORTED; at HEAD 16 files / 6 (incl. two archived `RUN.<phase>.<blob8>.md`). §3:111 caps the retrofit at "exactly one" while AC4:286 forbids an exemption and a waiver, so obeying §3 lands a leg check that reds the bar on five records. Prior audit B1, unfolded. | Re-derive the population at build time; enumerate every codeless ABORTED record in §4 and AC4a with the code read from its own parked text; move §3's cap to "the records §4 enumerates at build time"; add them all to Files touched; state whether archived records are exempt. |
| B2 | facts | blocker | `spec/…-2.md:65-76`, `:247` | S7 — the bullet that performs the edits — spells `eight→nine`, "NINTH entry", and a driver comment that "says the region carries FIVE"; Files touched says "the seven-fact pin". Live: both protocol halves read `eleven` at :130, `unattended.sh:1582` reads seven. S3 in the same spec says every ordinal was deleted. A builder edits a numeral that exists in no carrier and the pin move silently does not happen. Prior audit B2, half folded. | Strip every numeral from S7 and Files touched, leaving a path list with each site's role, plus "read the current value from the protocol at build time", as S3 already mandates. Note in the same bullet that -21 F2 also moves the pin, so the two movers share one carrier list. |
| B3 | assume | blocker | `spec/…-2.md:90-94` | S10 asserts "rev-6 adds them to Files touched and gives S10 its own acceptance criteria". Neither happened: no memory-tree carrier appears in Files touched (`:238-254`) and none of AC1–AC12 mentions a verdict token, a review record's first line, or hygiene check 5. `check-verdict-epoch.sh` will red on the change and is absent from §7. | Add `memory/HYGIENE.md`, `tools/memory-tree/check-memory-hygiene.sh` + sibling test, `HYGIENE.template.md` and the tracked `*.template.md` markers to Files touched; add `check-verdict-epoch.sh` and the hygiene test to §7; add a red/green AC over check 5. Or split S10 into its own unit and delete the false self-claim. |
| B4 | accept | blocker | `spec/…-5.md:70`, `:75-77`, `:89-95` | The DECISION class has three incompatible definitions. S7 lists abort + override + park and never classifies `waiver`; S7a1 names waiver without assigning it; S6a says only `decision` is in the class, and `:70` still reads "inflated by three kinds". The shipped counter at `unattended.sh:1566` is `(decision\|abort\|override\|waiver)`, so with waiver in DECISION the inflation is zero and S6a's "the code moves" names no defect. AC5's arm is green against the unmodified driver. | Put `waiver` in DECISION explicitly in S7, citing the protocol's waiver paragraph. Restate the inflation arithmetic once, delete `:70`'s stale figure, and withdraw or re-argue "the code moves". Give AC5/AC6 a fixture carrying one line of every live kind with the expected per-class count. |
| B5 | accept | blocker | `spec/…-21.md:15`, `:48` | S1 bounds "the invocation of `$LANDER`" in the driver, and the driver never invokes the lander — the agent does, from the Skill. `LANDER` appears only as a conf default, an echo, the DoD predicate and a fenced block in `SKILL.template.md`. §3's non-goal names a verb that does not exist; AC1, AC2, AC4 and AC6 assert over a code path with no host. | Either add a scope item making the driver invoke the lander (a `--land` verb running `$LANDER` under the bound and writing the attempt fact) and restate §3, or move the bound to the Skill and say plainly the driver cannot enforce it — rewriting AC1/AC2/AC4 against whatever does. |
| B6 | facts | blocker | `spec/…-21.md:56`, `:174-179` | F1's 900s bound is sized against "335s serial and ~95s at width 8". The tracked measurement at HEAD is 873 s concurrent against a 4018 s leg-sum (`AGENTS.md:494`), replaced by a landed unit. `.githooks/pre-push` runs the full bar inside the push the deadline wraps, so 900s leaves ~27 s of margin and is 4.5x below the documented `GATE_JOBS=1` rollback. The bound converts a normal push into a bounded-out landing. | Re-measure at build time and re-resolve F1 against the real pair; the bound must exceed the SERIAL bar plus an object transfer. Replace §4's sentence with the re-measured figures, the command that produced them, and the run mode the bound is sized for. |
| B7 | assume | blocker | `spec/…-4.md:68-72` vs `:162-174` | §3 says the leg's display-name move is a four-part cost and "S2 carries all four"; Files touched names only the test script. The name `marker contract` lives at `tools/gate-legs.json:763`, `tools/memory-tree/kit.toml:150`, `memory/map/features/row-grammar.md:11`, `memory/map/generated/MAP.md:51` and `inventories.json:44`. | Add all five carriers to Files touched and `python tools/codebase-map/test_codebase_map.py` to §7. |
| B8 | assume | blocker | `spec/…-18.md:29-32` vs `:128-132` | S1a declares a new `.unattended.conf` key and S1c requires the adopter to seed it, the conf example to document the compliant-lander contract, and `tools/push-main.sh` to write the marker; AC4b/AC4c grade exactly those. Files touched names none of `.unattended.conf`, `tools/unattended/.unattended.conf.example`, `tools/unattended/adopt-unattended.sh` (which today only reads the conf — seeding is new code), `tools/push-main.sh`. | Add those four plus `memory/guides/SESSION-KICKOFF.md` (the re-stamp; `.unattended.conf` is watched) to Files touched, and `adopt-unattended.test.sh` + `push-main.test.sh` to §7. |
| H1 | facts | high | `spec/…-2.md:306-313` | AC8 claims a number-agnostic pattern; the alternation stops at `ten` while the pin reads `eleven`, so run as written it returns zero hits from either protocol carrier and 31 lines of unrelated "of them" prose. Green today over a stale pin — the failure its own text says it was rewritten to prevent. Prior audit H1, unfolded. | Anchor on the noun phrase (`facts and nothing else`) plus the per-carrier statements, extract each numeral with `[[:alpha:]]+\|[0-9]+`, and assert the extracted values are EQUAL across carriers, per carrier path, so a carrier that stops matching reds. |
| H2 | x-read | high | `spec/…-2.md:41-43` | S3 still says "this build … moves the pin exactly once". The README's rule was corrected 2026-08-20 to name TWO movers, and -21 F2 resolved on that basis. The build's two authorities disagree on the number of movers. | Replace the clause with the README's corrected content: a fact is added for a per-run singleton, both -2 and -21 move the pin, each reads the live ordinal at build time and moves every spelling. |
| H3 | x-read | high | `spec/…-2.md:81-94`; `spec/…-1.md:28-32`, `:322-324` | The review-verdict token set is made kit-owned and closed by -1 and -14, and AC3 demands byte-identity with "the one -2 defines" — but -2 S10 never enumerates a member and names no source. The two homes are in mutually non-importable kits (`spec/…-4.md:120-123` records why), and no gate joins them. | Enumerate the set in -2 S10 as S1 enumerates the halt set (`BUILD-METHOD.md:103`'s three tokens are the floor), name the canonical kit, and replace AC3's unobservable "byte-identical" with -4's own answer: one shared case table, or a stated duplication with the drift declared unarmed. |
| H4 | assume | high | `spec/…-2.md:249-252`; `README.md:170` | Both instruct an edit to "the matching count in `AGENTS.md`'s gate-suite bullet". That bullet no longer exists — the enumeration was replaced by a pointer at `tools/gate-legs.json` — and the charter body is generated between `gov:playbook` markers, so a hand-edit reds the playbook parity leg and a template edit is out of scope. | Delete the `AGENTS.md` carrier from -2's Files touched and from the README rule; keep `tools/unattended/check-unattended.sh:2` ("TWENTY-ONE checks over the tree") as the only carrier. Do not recreate the bullet. |
| H5 | facts | high | `README.md:161-165`; `spec/…-1.md:245-249` | The shared charter read-path budget is a consumable and it is spent: the stated 91997/112987/20990 with the protocol at 27582 and the method at 17460 was exact at base, but HEAD reads 104090 B, i.e. **8897 B of headroom**, protocol 32258, method 20567. Five landed units took 58% of the declared allowance; five units still grow the protocol and three the method against what remains, and no unit owns a displacement. | Delete the byte figures from both carriers, keep the command (`python tools/memory-tree/corpus_ids.py --report`), and state a per-unit allocation or an explicit displacement requirement. Twice-stale in four days is the argument against the snapshot. |
| H6 | x-read | high | `README.md:166-169`; `spec/…-1.md:247-250` | The README declares the spender set "HERE and nowhere else" and lists seven, omitting -18 and -21, whose Files-touched lists grow the protocol pair (-21 unconditionally after F2). spec-1 then enumerates a competing set of six that omits -19, in the same paragraph that cites the single-statement rule. | Restate the set in the README to include -18 and -21; delete spec-1's enumeration, leaving only "this unit spends from the shared budget; the set and the live pair are in the build README". |
| H7 | facts | high | `spec/…-3.md:130-136`; `spec/…-1.md:238` | "236 lines and 16466 bytes" is a retired base's size — at `098bebd9` the method is 245/17460 (which spec-1 states for the same commit, so two specs disagree about one tree), and at HEAD 283/20567 against a line-8 self-declaration of ≤22 KB/≤290 lines, not 20 KB/250. The derived "14 lines M1's budget leaves" reproduces in no era: 5 at base, 7 at HEAD. Prior audit medium 12, unfolded. | Restate the paragraph on a build-time `wc -lc` and the live self-declaration; note the headroom is 7 lines / 1961 B, which makes the displacement mandatory and a budget raise an owner turn per M3 veto 2. Fix spec-1:238's stale self-declaration sentence too. |
| H8 | facts | high | `spec/…-19.md:26`, `:60`, `:150-152` | S3, the §4 row and AC6 rest on `Opt-in by presence` being live in both protocol halves with two file:line hits and a `git log -S` returning exactly the adding commit. It was removed by `fd8f6fa` (-11's landing, CLOSED), so `log -S` returns two commits and AC6's "two file:line hits" cannot be written. S4's hand-off to -11 is an orphan. | Restate S3 and the §4 row in the past tense against the base and the removing sha; have AC6 cite the `git log -S` history plus that sha instead of live hits; delete S4. |
| H9 | facts | high | `spec/…-13.md:27`, `:95` | The inventory says "3 in the leg" and S4 lists three leg locations; `check-unattended.sh` has exactly TWO `ls-remote` calls at base and at HEAD. The third ref (`:211-212`) is the rationale COMMENT, not a call site. A builder hunts a site that does not exist. (The other line refs in S1/S4/§4 are correct at the declared base and must NOT be renumbered.) | Fix the inventory row to 2 driver network calls + 1 offline `--get-url` + 2 leg calls, and drop or relabel S4's `:211-212`. Prefer function names to line numbers. |
| H10 | accept | high | `spec/…-13.md:179-181` | AC5 derives a location property from `grep -cE`, which prints per-file counts. The counts also include a prose hit (`unattended.sh:323` is a comment), so no count distinguishes call sites — the class named at `memory/gotchas/absence-assertion-over-whole-file-text.md`, which §5 flags and the AC ignores. | Rewrite AC5 over `grep -n`: every hit's line is inside the bounded helper's body or the `--get-url` line, with the sanctioned line set pinned in the arm and the comment hit excluded deliberately. |
| H11 | accept | high | `spec/…-13.md:157-158`, `:188-191` | AC8 — the false-positive arm, the only one proving the cap is not merely tight enough to pass AC1 — needs a remote that transfers slowly but succeeds. The one declared fixture mechanism is a blackhole IP, which transfers nothing and cannot exercise a rate below `http.lowSpeedLimit`. | Name the mechanism: drive the bounded helper with a stub that sleeps then succeeds (wall-clock cap, no network), and record explicitly that the `lowSpeedLimit` arm is NOT covered and why — a skip that announces itself. |
| H12 | folded | high | `spec/…-13.md` (whole file) | The unit half-closes `TOOL-aBoundedVerdict-10` (OPEN, `memory/backlog/TOOL.md:98`) — the traced 240s hang that wedged the bar at 46/65 — cites it nowhere, and delivers only the first of the row's "Two fixes". The `run-gates.sh` per-leg deadline is neither scoped nor out-scoped, so the row reads as closed once -13 lands. | Cite the row in §1 or §10, add a §3 non-goal naming the per-leg deadline as out of scope with the id that will own it, and amend the row on landing. While there, weigh `TOOL-aPromptedMandate-9`'s measurement (check-wiring 1m22s vs ls-remote 2.1s) against §4's grounds. |
| H13 | accept | high | `spec/…-21.md:15`, `:145-152` | AC1/AC2 are unrunnable: the bound is a file constant the reused -13 design forbids raising from the environment, the suite drives the real driver in place and never seds it, and no test seam is named — so "a LANDER that sleeps past the bound" costs ≥900 s inside the merge bar. | Specify the seam in S1: the bounded helper takes the bound as its first parameter, arms call it with 1–2 s, and one source-level arm asserts the real call site passes the file constant. Restate AC1/AC2 against the helper. |
| H14 | accept | high | `spec/…-21.md:155-156` | AC5 asserts "no site parses the lander's output" from `grep -c 'LANDER'`, which returns an integer, states no expected value, and moves UPWARD if the bound is implemented by scraping — the direction it does not assert. | State it as -2's AC6 does: the invocation site contains no command substitution, pipe or redirect capturing stdout, enumerated by `grep -n` over the invocation's own lines. |
| H15 | accept | high | `spec/…-21.md:182-195` | F2 resolves S4's attempt record to a new authored FACT, which moves the region's fact pin — and S1–S6 never mention the pin, Files touched still hedges "the fact list, if S4 adds a fact", and no AC observes a stale count. The sibling mover carries both (S7 + AC8). | Add a scope item enumerating the pin's carriers by path, delete the hedge, and add a number-agnostic AC asserting no carrier states a stale count — coordinated with B2's single carrier list. |
| H16 | assume | high | `spec/…-4.md:260-267` | F3 binds the hardened predicate to read the fact-question prefix "from one place". No scope item mentions it, no AC exercises a §8 item carrying one, and the prefix's grammar belongs to -3 S4 — sequenced FIVE units later — so as written -4 implements a mark whose definition does not exist. -3 F2 calls the coordination "a hard constraint": an unrecognised mark blocks that spec from ever going terminal. | Add a scope item to -4 SPELLING the prefix grammar beside the resolution mark in S2's single case table, plus an AC pair (prefix alone; prefix + resolution mark) with the expected verdict per reader, and have -3 S4 cite -4's grammar so the dependency points forwards. |
| H17 | assume | high | `spec/…-5.md:96-100`, `:175-181` | S7a puts the DECISION set in the driver "read by the leg through the same helper", and AC6c grades that leg read — but `tools/unattended/check-unattended.sh` is absent from Files touched, and its `core_of` reads are one explicit assignment per key, so a new key is a mandatory edit. The same file carries `KIT_UNATTENDED_VERSION`. | Add `tools/unattended/check-unattended.sh` to Files touched, naming both the new `core_of` read and the version constant with its same-line marker. |
| H18 | assume | high | `spec/…-3.md:245-248`; `spec/…-5.md:123-125`; `spec/…-1.md:388-392` | Three specs commit to writing or superseding a backlog row and none names `memory/backlog/TOOL.md` in Files touched. -1's F3 says it "supersedes" `TOOL-aBoundedVerdict-8`, whose OPEN row currently prescribes the opposite ("write it once all three have landed, not before"). -19 gets this right, so the convention is understood. | Add `memory/backlog/TOOL.md` to Files touched in -1, -3 and -5, and in -1 state the row's new status and superseding text. |
| H19 | accept | high | `spec/…-1.md:48-51`, `:330` | S6's third clause and AC4 turn on "a terminal disposition line" — a term whose only two occurrences in the whole tree are those two lines. S4 defines the `review` line's fields and names no disposition line, and F3 says the disposition's observable is a new unit row in the generated region. The leg check cannot be written and AC4's fixture cannot be built. | Name the observable once in S4 and reuse it in S6 and AC4 — either a `review` line whose reason carries the exit token, or F3's promoted unit row — and delete the undefined phrase. |
| H20 | assume | high | `spec/…-17.md:100-105`; `spec/…-21.md:98-105` | "the kit version constant" names one file where `tools/check-kit-versions.sh:127-150` forces five: the constant AND its same-line `gov:kit unattended@` marker in `unattended.sh` and `check-unattended.sh`, the marker in every tracked `tools/unattended/*.template.md`, and the re-rendered `.claude/skills/unattended/SKILL.md`. | Replace the phrase in both specs with the enumerated five carriers. |
| H21 | folded | high | `spec/…-18.md:215-223` | F1 resolves check 8's terminal exemption as "stays" without citing `TOOL-cSettledDocket-11` (OPEN, `memory/backlog/TOOL.md:87`), which prescribes the opposite: the exemption clears `rd` for any terminal phase and so skips the malformed-MARKER refusal as well as the emptiness one. F1's grounds reach only the emptiness half, closing the row by silence. Measured: scoping to emptiness reds nothing — all 13 tracked records have well-formed marker pairs. | Name the row in F1 and re-resolve: keep the exemption for the emptiness branch only. If "stays" wholesale survives, say why the row is wrong and amend or close it in the same commit. |
| H22 | folded | high | `memory/DECISIONS.md:68` | The ratified, append-only -2 row still pins the halt code as "the region's EIGHTH fact", false against the live pin of eleven, and -2 cites the row nowhere — while S3 insists the ordinal is read at build time and never restated. A builder grepping the decision log gets the stale ordinal as the ratified answer. | Have -2 name `memory/DECISIONS.md:68` in S3 or its rev log, stating which clauses survive (code-over-phase, owner-ratified) and that the ordinal does not, and commit the superseding row on landing with the ordinal dropped rather than re-spelled. |
| M1 | accept | medium | `spec/…-1.md:76-85`, `:318-321` | S10 claims the leg asserts BOTH ceiling carriers; one is the run's own stdout, which no gate can observe, and AC2b correctly narrows to the README line. The scope item promises an enforcement its own criteria decline. | Reword S10's last sentence: the leg asserts the README line, the output line is observed at the verb by AC2a. One carrier per observer. |
| M2 | accept | medium | `spec/…-1.md:86-89` | S9 — the residual park when a blocker cannot be promoted, the escape hatch S8 depends on — has no acceptance criterion; AC6 enumerates exactly three method rules and this is not one. §5 flags it as the third risk with no observation, and "the build does not close" is ambiguous between prose and a `--close` refusal. | Add an AC over the method's review section stating the park case with its scope-not-difficulty phrasing, plus one arm if the non-close is meant to bind; otherwise say in S9 that it is advisory. |
| M3 | x-read | medium | `spec/…-1.md:225-234`, `:304-347` | -2 and the README both sequence the unattended leg's header check COUNT as a two-mover coordination with this unit, and -1 names neither the header count in Files touched nor any criterion over it. The count is observed by no gate, so a missed move is silent. | Add "`check-unattended.sh`'s header check COUNT, moved one past the value -2 leaves" to Files touched and an AC tying the header figure to the number of checks the leg runs. Drop the `AGENTS.md` half per H4. |
| M4 | assume | medium | `spec/…-1.md:218-223` | The Rollout sequences -2, -11, this unit's verb, the leg and the method prose and names -14 as free-floating, but never `TOOL-aBoundedVerdict-5`, whose taxonomy S4a takes by name and whose decision-kind counter AC3a grades. A builder working the Rollout alone implements `record` before the taxonomy exists. | Insert -5 ahead of this unit's verb with the reason, matching the README's dependency order. |
| M5 | x-read | medium | `spec/…-5.md:80-95` | S7a1 rules that "decision" names both the literal KIND TOKEN and the CLASS, and that "the two must be named apart before either the driver constant or the counter can be built" — then supplies no second name, while S7 keeps calling the class "a decision kind". -1 S4a meanwhile pins the class names as "`decision` and `record`, taken verbatim from the owning spec". | Spell both class names in S7/S7a1, name the driver constant that holds the surfaced set, and update -1 S4a and AC3a to the chosen name. |
| M6 | x-read | medium | `spec/…-5.md:150-151`, `:331` | rev-7's two-field / four-caller correction reached the prose and skipped the table: the inventory says required fields are "three, refused by the driver", gives park()'s callers as two-then-three, and §10 says "already has two callers". Source: `unattended.sh:1940-1942` refuses on two fields; park() has four callers. Second rev in a row where a reconciliation missed the table. | Set the required-fields cell to two, the callers row to four/unchanged-at-four, and §10 to four — re-checking the whole table against S1 in one edit. |
| M7 | accept | medium | `spec/…-3.md:46-50`, `:174-177` | S8 — the fifth entry in the protocol's cost list, the one thing here the owner explicitly ratified (F1) — has no acceptance criterion. AC1 observes the awaiting-approval disposition and the render parity; nothing observes that the list grew. The list has exactly four entries today. | Add an AC: the protocol pair's cost list carries a fifth entry stating that a spec reachable at the pinned base is scope-approved by the same act, in both halves, with check 10's byte-diff clean. |
| M8 | accept | medium | `spec/…-3.md:204-205` | AC7 is green before any work: this unit changes no kickoff exit (§3 says so, and the engine is not in Files touched), so "the leg still finds at least the declared floor" passes today, on an empty diff, and on a wrong implementation of S1–S8. | Delete AC7, or replace it with the observation that distinguishes this unit: the three new dispositions are written in the protocol/method and are NOT enumerated as kickoff exits, so the floor is unchanged for a stated reason. |
| M9 | accept | medium | `spec/…-4.md:42-45`, `:216-218` | S4 names two carriers for the new marker grammar; AC7 observes only `BUILD-METHOD.md` M3. `memory/TEMPLATE-SPEC.md:100-103` still says the mark is prose read only on §8's first non-blank line — the statement S3 falsifies — and it is the document every future spec author reads. | Add an AC: `memory/TEMPLATE-SPEC.md`'s §8 guidance states the shaped mark and the per-item rule, its kit template matches byte-for-byte, and `kit-dogfood-parity.test.sh` is green. |
| M10 | x-read | medium | `spec/…-4.md:3` | This spec alone declares base `febba16b`; the other fourteen declare `098bebd9`. Every figure in it is stamped "at the merge base", and one is demonstrably stale (118→136 spec files; HEAD has 216). It is unit 4 in the dependency order, so it is built early on evidence the 08-19 re-decomposition never re-verified. | Reground onto `098bebd9`: re-run the corpus measurement and the per-item split, re-check the four marker-contract name sites, move the header's base field, and say in the rev entry which figures were unchanged. |
| M11 | x-read | medium | `README.md:94` | The authored roster gives unit 13 (`TOOL-aBoundedVerdict-18`) Tier 1; the spec header reads Tier-2 and its rev log records the move twice. Two documents, two tiers, and the tier picks the review shape. | Change the roster's Tier cell to 2. |
| M12 | accept | medium | `spec/…-18.md:181-182` | AC3 states no expected value and cannot isolate its subject: `grep -c 'BYPASS_BAN' unattended.sh` counts ten lines across five unrelated call sites, so a delta of one identifies nothing and any unrelated edit forges it. | Assert on the arm: after S2, the `landed-via-lander` case body no longer contains `grep -qF -- "$BYPASS_BAN" "$rel"`, extracted by line range, with the pre-change body as the control. |
| M13 | x-read | medium | `README.md:152-158`; `spec/…-1.md:233`, `-13:120`, `-17:104`, `-18:128`, `-21:104` | Five unbuilt specs edit a watched path (`.memory-tree.conf`, `.unattended.conf`, `BUILD-METHOD.md`) and omit `memory/guides/SESSION-KICKOFF.md` from Files touched, so the manifest ratchet reds the commit both staged and committed. The README rule meanwhile asserts its own compliance twice, and both halves are false: -19 touches no watched path, and the file is in four lists, not five. | Add `memory/guides/SESSION-KICKOFF.md` with the re-stamp note to -1, -13, -17, -18 and -21; restate the rule as the obligation only ("a unit whose Files-touched list names a watched path re-stamps in the same commit"), with no count and no "every unit". |
| M14 | x-read | medium | `README.md:20-21`, `:204` | "Out of scope" opens with "No implementation. This build lands a README, five specs and their review records", in a file carrying a fifteen-unit roster, five CLOSED units and a section titled "what it built and what it cost". The section exists to hold the cut-line in one place, so the stale line is live guidance contradicting every remaining spec. | Replace the bullet with what is actually out — no unit outside the roster, no implementation beyond the declared scope items — and delete or date-stamp the "builds nothing" sentence. |
| M15 | facts | medium | `spec/…-1.md:132`; `README.md:33` | "`dClosedLexicon` holds 10 review records in one day" — it holds 10 across two days (8 on 2026-08-16, 2 on 2026-08-17), and the README says eight in one day. Two documents in one build disagree about one corpus; the cap-relocation argument is unaffected. | Write "10 records over two days, 8 of them on 2026-08-16" and align the README row. |
| L1 | facts | low | `spec/…-4.md:130` | "none of the six under `memory/project/`" — there are seven registries. Correct at `febba16b`, and the load-bearing claim (check 12 consults no registry) is unaffected. | Write "none of the registries under `memory/project/`" and drop the count. |
| L2 | accept | low | `spec/…-13.md:181` | AC5 attributes the source-level rule to "S5's checklist item"; S5 is the comment correction, and the rule is in §5's testing + left-shift bullet. F2's resolution naming `unattended.test.sh` as the arm's home is not carried into the AC. | Cite "§5's testing + left-shift item" and name the arm's file. |
| L3 | assume | low | `spec/…-19.md:161` | §7 lists `tools/workflows/check-protocol-parity.test.sh`, which gates the REVIEW protocol shipped by the workflows kit — a tree this unit does not touch. The unattended pair's byte-diff is check 10, already listed. A reader trap, not coverage. | Drop that row; add `tools/unattended/adopt-unattended.test.sh` (`adopt-unattended.sh --check` is already there). |
| L4 | facts | low | `spec/…-2.md:240`, `:246` | Files touched still carries "the stale five-fact comment" and "the seven-fact pin" — the same numerals B2 removes from S7, in the list a builder works from. | Fold with B2: strip both numerals when S7's are stripped. |

## Unverified

None. Every finding above carries a skeptic verdict; thirteen raw findings were refuted and are not
reported, including the claim that -13's line references are stale (they resolve exactly at the
spec's declared base and must not be renumbered to HEAD — see H9, which keeps only the count half).

## Per-unit disposition

| unit | disposition | deciding findings |
|------|-------------|-------------------|
| `TOOL-aBoundedVerdict-1` | NEEDS A FOLD FIRST | H3, H5, H6, H18, H19, M1, M2, M3, M4, M13, M15 |
| `TOOL-aBoundedVerdict-2` | NEEDS A FOLD FIRST | **B1, B2, B3**, H1, H2, H3, H4, H22, L4 |
| `TOOL-aBoundedVerdict-3` | NEEDS A FOLD FIRST | H7, H18, M7, M8 |
| `TOOL-aBoundedVerdict-4` | NEEDS A FOLD FIRST | **B7**, H16, M9, M10, L1 |
| `TOOL-aBoundedVerdict-5` | NEEDS A FOLD FIRST | **B4**, H17, H18, M5, M6 |
| `TOOL-aBoundedVerdict-13` | NEEDS A FOLD FIRST | H9, H10, H11, H12, M13, L2 |
| `TOOL-aBoundedVerdict-17` | NEEDS A FOLD FIRST | H20, M13 |
| `TOOL-aBoundedVerdict-18` | NEEDS A FOLD FIRST | **B8**, H21, M11, M12, M13 |
| `TOOL-aBoundedVerdict-19` | NEEDS A FOLD FIRST | H8, L3 |
| `TOOL-aBoundedVerdict-21` | NEEDS A FOLD FIRST | **B5, B6**, H13, H14, H15, H20, M13 |

Build-level folds that no single unit owns, and that several units read as authority:
`README.md` for H4, H5, H6, M11, M13, M14, M15; `memory/DECISIONS.md:68` for H22 (superseded on -2's
landing); `memory/backlog/TOOL.md` rows `TOOL-aBoundedVerdict-8`, `TOOL-aBoundedVerdict-10` and
`TOOL-cSettledDocket-11` for H18, H12 and H21.

Two folds are cheap and unblock the most: re-measure the read path and the merge bar ONCE, at build
time, and delete every snapshot of both from the specs (H5, B6, H7) — those figures have now gone
stale twice in four days, and two of them are consumables rather than illustrations. Then strip the
remaining ordinals (B2, L4) and re-derive the run-state population (B1), which together account for
three of the eight blockers.
