**Serves:** diff-review TOOL-aBoundedVerdict-1 TOOL-aBoundedVerdict-2 TOOL-aBoundedVerdict-3 TOOL-aBoundedVerdict-4 TOOL-aBoundedVerdict-5 TOOL-aBoundedVerdict-13 TOOL-aBoundedVerdict-17 TOOL-aBoundedVerdict-18 TOOL-aBoundedVerdict-19 TOOL-aBoundedVerdict-21

# Review round 3 — the fix range for round 2's 33 defects

**Range:** `ff91301130ec3483c9b25e32468cc1aa018f6f01...ebe5012577271a86bed653739e267c4049560995` (two commits: `570f810` the 33 findings, `ebe5012` the fold's own defects).
**Round:** 3.
**Kind:** FIX REVIEW. The underlying build was not re-reviewed; the question asked of every hunk was whether the fix is correct and complete, and whether the fix introduced a new instance of the class it closes.

## Verdict: BLOCKED

**Shape:** raw 23 · confirmed 20 · refuted 3 · unverified 0 · precision 0.87. The 20 confirmed collapse to **15 distinct defects** after folding cross-lens duplicates; the fold is recorded in the `dup` column.

**Why BLOCKED:** three confirmed blockers, two of them measured RED on the merge bar at `ebe5012`.

- `bash tools/unattended/cross-component.test.sh` fails **four** assertions (F1). Its leg guard names `tools/unattended/`, which this range touches, and `.githooks/pre-push` sets `GATE_FULL=1` anyway, so the bar is red at the push boundary either way.
- `bash tools/memory-tree/kit-dogfood-parity.test.sh --check` prints `DRIFT` (F3), and the remedy it prints would DELETE the fix that caused the drift.
- `marker-contract.test.sh` contract 3 (F2) is green and grades nothing — a brand-new check that cannot fail, landed in the fold whose stated theme is removing exactly that shape.

**The pattern worth naming.** Nine of the fifteen defects are fixes applied to one of two paired carriers: one fixture of two (F1), one conf carrier of three (F7), one `sed` site of three (F14), one channel of two (F13), one reader of a paired pair (F6), one count carrier of two (F10). The round-2 fixes were correct in substance and half-delivered in reach. The cheapest left-shift for the whole class is a pairing arm per pair, not fifteen individual gates.

## Confirmed findings

| id | sev | ref | what is wrong | the fix | left-shift gate | dup |
|----|-----|-----|---------------|---------|-----------------|-----|
| F1 | **blocker** | `tools/unattended/cross-component.test.sh:41` | The fixture never copies `.unattended.conf.example`, so the new check-22 branch ("a missing example is a REFUSAL, not a skip") fires on every leg run inside it. Measured: FOUR failing assertions — the fixture precondition, both arm-3 assertions (`exit code` 0→1, `output` empty→the refusal), and arm 5. The identical fix WAS applied to the sibling fixture at `check-unattended.test.sh:27` in this same commit, with a comment explaining why. One of two carriers. Worse than a red bar: arm 3 exists precisely because a silent leg and a failing leg share an exit code, so the one assertion that distinguishes them is now permanently noisy. | Add `"$HERE/.unattended.conf.example"` to the `cp` at line 41, carrying the sibling's comment (a fixture omitting it models a broken install, not a repo). Re-run; the four assertions go green. | An arm asserting the two fixture roots copy the SAME tracked kit file set: derive it from `git ls-files tools/unattended/` and refuse when either `cp` list is a proper subset. This is the pairing arm that would have caught F1, F7, F13 and F14 as one class. | — |
| F2 | **blocker** | `tools/memory-tree/marker-contract.test.sh:352` `:354` | Contract 3, the new cross-kit review-verdict vocabulary join, carries a raw **0x01 SOH byte** where `\1` belongs — in BOTH `sed` replacements. `od -c` shows `"$/ 001 /p'` and `\)"/ 001 /'`. Each side replaces the entire match with SOH, so `_cv_drv` and `_cv_hyg` are always the single byte 0x01: always equal, always non-empty. Proven by mutating `REVIEW_VERDICTS` to a disjoint four-token set and by mutating the hygiene engine's `v != "BLOCKED"` — still green both times. The `[ -z ]` liveness guard is defeated too, because SOH is non-empty, so it survives only as a crude "the line exists" probe. The suite still prints `PASS (38 cases across 3 contracts …)` and `marker contracts` has no guard in `tools/gate-legs.json`, so the false green ships on every bar run. Guards a real hazard: a `--review` verdict the hygiene gate then refuses forever on an append-only record. | Write the two characters `\1` in both replacements, with a tool that does not eat the backslash (this repo's own `bash-heredoc-eats-a-backslash-level` gotcha). Verified: with `\1` both sides read `BLOCKED / CLEAN / CLEAN WITH FIXES`. Then stage the break in a scratch copy and observe RED before landing. | Two gates, both cheap. (a) A byte-level scan of tracked `.sh` files for control characters outside tab/newline — this defect is invisible in every text-mode read. (b) Make each contract in this harness prove it can fail: assert the extracted token count is ≥2 and that the two sides are non-trivial, so "compared nothing" is a refusal rather than a pass. | 2·12·18 |
| F3 | **blocker** | `memory/guides/BUILD-METHOD.md:109` `:183` | The M4 "Not the harness" restoration and the M6 rewording landed in the RENDERED method only; `tools/memory-tree/BUILD-METHOD.template.md` was not updated. Measured: `kit-dogfood-parity.test.sh --check` exits 1 with `DRIFT`, naming lines 109-110 and 183. That script is a merge-bar leg (`tools/gate-legs.json:199`). Three separate harms. The bar is red. Every adopter who renders the kit still gets a method with no `tier2-review reviews DIFFS` rule — the rule spec-1 rev-12 records as restored and `memory/map/features/build-method.md:87` claims is present. And the leg's own printed remedy, `--render`, copies template→live and would DELETE the restored line: following the fix message undoes the fix. | Apply both edits to `tools/memory-tree/BUILD-METHOD.template.md`, keeping `{{KIT_DIR}}`/`{{TOOL_ROOT}}` where the live copy has literal paths. Re-run `--check` and confirm exit 0. **Do NOT run `--render`.** | The leg already detects the drift; what it lacks is a safe remedy. Make `--render` refuse when the LIVE copy is newer than the template in git history, or print `--render (template→live, DESTROYS live-only edits)` so the direction is unmissable. A fix message that undoes the fix is its own defect class. | — |
| F4 | high | `tools/unattended/check-unattended.sh:457` `:468` | The rewritten "three outcomes kept apart" block omits the fourth: the remote answered with a tip **this clone does not have**. `is_published` calls `merge-base --is-ancestor` against `ADV_HEAD` and each `ADV_TIPS` entry with no object-presence guard, and that command exits 128 on a missing object — so an unfetched clone collapses every base into "not published" and check 9 emits a forgery-shaped refusal ("it names a commit that exists only where this run could have authored it") against honest tracked records, `memory/builds/aBoundedVerdict/RUN.md` among them. The guard that fixes it sits 143 lines below in the same file (`:611`, `rev-parse --verify --quiet "$b^{commit}"`) under a comment naming this exact failure; the `ADV_RC` split rewritten here reproduced the split for `mktemp` and the wall clock and did not carry that one across. Caveat: transient, not permanent — origin's advertised HEAD is present in this clone at the moment of writing, so the leg is not red at this instant. | In `is_published`, verify presence before comparing: skip any advertised sha failing `rev-parse --verify --quiet "$t^{commit}"`. If EVERY advertised sha is absent locally, set a fourth `ADV_RC` (97) and emit its own check-9 refusal — "the remote advertised tips this clone does not have; fetch and re-run" — rather than routing to the not-published message. | A fixture advertising a sha absent from the local object store, asserting the new message and a `miss` on the not-published one. Add a source-level arm: every `merge-base --is-ancestor` in this file must be preceded by a presence check on its arguments — the same predicate, gated as a class rather than at the one site. | 5 |
| F5 | high | `memory/TEMPLATE-SPEC.md:170` `:179-181` | The spec contract still states the §8 mark "is graded PER ITEM" and that "with any item present, every item is graded and no first line suppresses that" — the exact behaviour BOTH readers withdrew in this range. `plan_state` (`unattended.sh:1266-1269`) and the hygiene reader (`check-memory-hygiene.sh:960-974`) now grade the whole section: any conforming mark anywhere resolves it, and a `none`/`N/A` first line resolves it outright. The document every spec author reads, and the one both readers' comments cite as their contract, describes a rule neither implements — and re-asserts by name the suppression the fix deliberately restored. Identically stale in `tools/memory-tree/SPEC-TEMPLATE.template.md:170`, so `kit-dogfood-parity` is green over it: **a claim false in both copies**, the class the protocol pair warns about one kit over. An author who marks one fork of several is told the rest are graded, and they are not. | Rewrite the §8 grading paragraph in `tools/memory-tree/SPEC-TEMPLATE.template.md` and re-render `memory/TEMPLATE-SPEC.md`: a section with items resolves on ANY conforming mark; a `none`/`N/A` opening line resolves it whatever follows; a section with neither an item nor a `none` form refuses. Name the unresolved-fork-below-`none` gap the two new fixtures pin. | Parity between two copies cannot catch a claim false in both, and this is the second time that shape has landed in this build. Extract the §8 grading rule into ONE machine-readable case table (`marker-contract.test.sh` already owns the vocabulary) and have both the prose and both readers cite it, so the contract is a fixture rather than a sentence. | 13 |
| F6 | high | `tools/unattended/unattended.sh:1268` · `tools/memory-tree/check-memory-hygiene.sh:971` `:973` | The rewritten §8 readers now both consult the section's opening line in the `items>0` branch, but with **different case rules**: `plan_state` tests `lf`, which is trimmed and lowercased (`:1226-1227`), while the hygiene awk tests raw untrimmed `q8` against case-sensitive `/^none/` and `/^N\/A/`. Measured on a scratch tree with a terminal spec past `FORK_MARK_CUTOFF`: `None - all resolved.`, `NONE - …`, `n/a - …` and `  none - …` each give `plan_state` READY and hygiene RED. Only exact `none…` and `N/A…` agree. The divergence is NEW — before this fix neither `items>0` branch looked at the opening line at all. Consequence for a run: `--plan` reports the unit READY, the run builds it, and the bar blocks at close on a spec nobody is present to edit — the stall this build exists to remove. The marker-contract harness exists to pin these two readers against each other and passes, because its case table has no case-variant row. | Make the two predicates one rule: either lowercase in the hygiene awk (`lc = tolower(q8)`) or drop `tolower` in `plan_state` and match `/^([Nn]one\|[Nn]\/[Aa]\|N\/A)/` identically in both. | Add `None`, `NONE`, `n/a` and a leading-whitespace opener to `marker-contract.test.sh`'s case table WITH items present, so the contract can fail on this class. The harness is the right home; it simply had no row for the axis the fix moved. | 19 |
| F7 | medium | `tools/unattended/.unattended.conf.example:113` | The kit's shipped example conf — edited by this same commit — still documents `LANDER_MARKER` as a path ("the lander writes this path on a successful push"). Both other carriers were corrected in this range: `.unattended.conf:123-129` ("A BARE NAME, resolved by both halves against `git rev-parse --git-common-dir` … It was a tree-relative `.git/unattended-landed`, which is wrong twice over") and `UNATTENDED-PROTOCOL.md:412` / `PROTOCOL.template.md:412`. The example is the file an adopter copies. The damage path is live: `push-main.sh` lost its `mkdir -p "$(dirname "$lm")"` in this same hunk and its `\|\| true` became a hard `exit 1`, so a path-shaped value resolves to `<git-common-dir>/.git/unattended-landed`, whose parent does not exist, the redirect fails, and push-main exits 1 **after a successful push** — its own message reads "The push SUCCEEDED and is not recorded." `--landed` then refuses with "the lander wrote none". Check 22 joins key NAMES only and its header says so, so nothing on the bar sees the prose. Mitigation: the example ships `LANDER_MARKER=""`, so only an adopter who follows the stale prose is bitten. | Replace lines 113-119 with the corrected `.unattended.conf` text. Then enforce the rule rather than only asserting it: refuse a `LANDER_MARKER` containing `/` in `verb_landed` and in `push-main.sh`, so a stale path-shaped value is a named refusal at the verb instead of a failed write after a push. | An arm joining the three carriers: for each key in the protocol §8 table, require the `.unattended.conf.example` comment block and the `.unattended.conf` comment block to be byte-equal to each other on the value-shape sentence. Check 22 grades names; this grades the one prose property that has now broken twice. | 6·14·21 |
| F8 | medium | `tools/unattended/unattended.sh:2442` | The terminal-round guard's `-F` rewrite dropped BOTH the `review` kind qualifier and the reason-field scoping. The old predicate anchored the token after ` · reason `; the new one is `grep -F -- " · item $subj · reason " "$rel" \| grep -qE '(CONVERGED\|NON-CONVERGENT\|CEILING)'`, whose second grep scans the WHOLE line. Two reproduced misfires. (a) A parked `decision` row sharing the subject's item text and mentioning an exit token in free-text prose fires the guard — reachable through a documented verb, since `verb_park` takes an arbitrary item and free-text reason — and `--review --subject <that item>` then refuses forever on an append-only record no verb rewrites. (b) A subject literally containing a token, e.g. `CONVERGED-in-name-only`, matches its own round-1 line and wedges on its second, genuinely converging round. The leg's reader disagrees: its awk (`check-unattended.sh:141-152`) matches only `review · ` lines and tests only the reason substring, so the two readers of "has this subject terminated?" grade different populations. | Restore the kind and the field scope while keeping `-F` for the subject: `grep -F -- "Z review · item $subj · reason " "$rel" \| sed 's/.* · reason //' \| grep -qE '(CONVERGED\|NON-CONVERGENT\|CEILING)'`. Better: reuse `review_counts`' awk parser so driver and leg split the row identically and one reader owns the field. | Two red fixtures, both currently absent: a parked `decision` row whose item equals the review subject and whose reason names an exit token must NOT block the next round; and a subject containing one of the three tokens must not block itself. The existing arm uses `F1 (fork)`, which covers only the regex-metacharacter half of the original fix. | 8·20 |
| F9 | medium | `tools/unattended/check-unattended.sh:604` `:386` | The new "THREE OUTCOMES, THREE MESSAGES" split in check 9 enumerates three causes of an empty advertisement; there are at least **five**. `adv_remote` is blanked at `:381-386` whenever the clone has zero or two-plus remotes, under a comment claiming "the fail-closed branch in check 9 reports it" — no branch does. The observation block at `:447` is gated on `[ -n "$adv_remote" ]`, so `ADV_RC` stays 0 and the else-arm emits "the remote advertised no tips". A two-remote clone — the case check 24 treats as a distinct refusal, because the kit must not pick which endpoint to believe — reads as a remote that answered nothing and sends the operator at the network for a local configuration fault. A fifth cause lands the same way: any non-124 `ls-remote` failure (auth, DNS, unknown host). Verdict stays fail-closed, so this is diagnosis, not a wrong merge verdict. | Set a fourth sentinel where the remote count is decided (`ADV_RC=97` for both `adv_nrem = 0` and `> 1`) and add the matching arm naming the count: "this clone declares N remotes rather than one, so no endpoint was observed — a clone configuration, not a remote that advertises nothing". Capture the non-124 `ls-remote` rc as a fifth. | A red fixture per new arm, beside the existing 124 and 98 arms (add a second remote; assert the new message and `miss` the no-tips one), or the fourth branch ships as unarmed as the three it joins. Structurally: an arm asserting every `ADV_RC` value the file SETS has a check-9 branch that READS it — the sentinel set and the message set are two halves of one table. | 3 |
| F10 | medium | `tools/unattended/check-unattended.sh:2` | The leg's header still says "TWENTY-ONE checks over the tree"; `grep -oE 'fail [0-9]+'` yields ids 1..22 contiguous, including the check-22 block this range added. The round-2 record this range is fixing names this line explicitly as the sole surviving carrier of that number. In the same range the sibling count in `tools/memory-tree/README.md:18` and `memory/map/features/memory-tree-hygiene.md:31` was moved 21→22 **and** gated by a new derived arm at `check-memory-hygiene.test.sh:1424-1449`, whose own comment records that the figure "has now been retyped wrong three times". The unattended carrier got neither the bump nor the arm, so the one figure a reader is told to trust is wrong for the fourth time and still ungated. | Change line 2 to "TWENTY-TWO checks over the tree". | Port the derived arm the sibling already has: in `check-unattended.test.sh`, derive `grep -oE 'fail [0-9]+' "$HERE/check-unattended.sh" \| grep -oE '[0-9]+' \| sort -nu \| grep -c .` and assert it equals the figure in the header, refusing when the header figure cannot be read. The arm exists 200 lines away in another kit; this is a copy, not a design. | 7 |
| F11 | medium | `tools/unattended/check-unattended.test.sh:223` | The new "every dispatched verb is documented" arm derives its population only from the `case "$VERB" in` block. Ran its awk: nine verbs (`preflight status resume close landed abort park review attest`). `--plan` (`unattended.sh:2571`), `--phase` (`:2572`) and `--version` (`:2577`) dispatch-and-exit from the argv `while` loop and never enter that `case`, so the join never grades them — and `unattended.sh:2580-2583` states the drift this arm exists to prevent in as many words: "the usage line was already two verbs behind (it omitted `--plan` and `--phase`)". Not a live break today; both verbs are currently in the usage line. But the arm structurally cannot see the two verbs with the recorded drift history, and any future argv-loop verb repeats it silently. Green-by-absence in a gate written to close exactly that class. | Union the `case` arms with `grep -oE '^ +--[a-z]+\)'` over the argv loop, excluding pure option flags (`--witness`, `--item`, `--reason`), and assert the derived count is at least 11 so a shrunken population refuses rather than passing. | The count floor IS the left-shift: this arm's existing liveness check only refuses an EMPTY population, which is why a nine-of-twelve population passed. Every derived-population arm in this kit should carry a floor, not just a non-empty test. | 9 |
| F12 | medium | `tools/unattended/check-unattended.sh:160` `:135-136` | The rewritten promotion clause computes one per-FILE unit-id delta (`rv_new`, a `comm` over the whole `gen:build-units` region at HEAD vs the file's pinned base) and consumes it as the scalar `newids` **inside the per-SUBJECT loop** (`for (it in n)`). So with two non-convergent subjects in one run-state file, promoting ONE satisfies the clause for BOTH, and any unrelated unit id added during the run satisfies it for every subject. The prior round confirmed the old form vacuous; the replacement shipped without a failing case ever being observed. Neither of its two messages is asserted anywhere: grep over `check-unattended.test.sh` finds neither "gained NO unit id this run BASE lacked" nor "the roster at this run BASE cannot be read", at HEAD or in the working tree. Worse, the `rev2` fixture (NON-CONVERGENT, no `base:` line) actually TRIPS the readable≠1 branch unasserted — the branch fires in the suite and nothing grades it. The check's own header says these clauses "cannot be exercised by the corpus, which is exactly why they need fixtures". | Key the delta to the subject: emit the new-id set into awk and require at least one id absent at BASE **per non-convergent subject**. If that is genuinely not derivable, say so in the message — a run-level delta described as a per-subject one is the defect. | The two missing fixtures, which the check's own header already prescribes: a non-convergent record whose README gained no new id, and one whose BASE blob cannot be read. Then add the general arm: every `fail`-branch message in this leg must have a positive assertion in the sibling — `check-arms.py` already implements exactly that predicate for the memory-tree kit and is not pointed at this one. | 15 |
| F13 | low | `tools/unattended/check-unattended.sh:413` | The new inert-wall-clock NOTE is a bare `echo` to stdout, and `fail()` at `:41` is also a bare `echo` to stdout — so on a node without working `timeout -k` an advisory lands on the same channel as a violation while the leg exits 0, contradicting the file's own header at line 7: "Exit 0 + no output = clean. Anything printed is a violation." The twin note added to the driver in the SAME commit (`unattended.sh:83`) ends in `>&2`. Same note, two channels, one commit. | Append `>&2` to line 413. | An arm asserting the leg emits nothing on stdout over a clean fixture even when `timeout` is stubbed off PATH — the existing clean-tree precondition already has the shape, it just needs the stub. That also arms the note itself, which is currently unexercised. | 10 |
| F14 | low | `tools/unattended/unattended.test.sh:2866` | The `slice_fn` helper this commit introduced to kill magic-span function slices was applied to `plan_state` (`:2525`) and `norm_endpoint` (`:2709`) but not to the third site, `review_state`, still `eval "$(sed -n "${rv_ln},$((rv_ln + 9))p" "$SCRIPT")"`. The commit message claims "All three now derive the end from the closing brace". `review_state` spans 8 lines today, so `+9` over-reaches by two harmless lines — but this is exactly the shape the commit removed elsewhere, and the fix's own comment records that `plan_state`'s `+ 45` had silently begun truncating the seven lines deciding READY vs FORKED as comments grew. `review_state` is the predicate the entire convergence loop rests on. Severity qualifier: on truncation the seven arms below compare against an empty verdict and FAIL loudly rather than passing green, so the cost is a misleading diagnosis, not silent coverage loss. | Replace lines 2865-2866 with `slice_fn review_state`. The helper is 340 lines above and already carries the not-found and no-closing-brace refusals. | A source-level arm in this suite refusing any `sed -n "…,$((… + N))p" "$SCRIPT"` construct — one grep, and it makes the helper the only way to slice. Without it the next added slice repeats this. | 22 |
| F15 | low | `tools/unattended/check-unattended.test.sh:134` | `TMPBIN=$(mktemp -d)/bin` creates a temp directory the EXIT trap never removes. The trap at `:14` is `rm -rf "$TMP" "${ORIGIN_DIR:-}"` and never learns the mktemp parent; `:519` removes only the stub binary inside it. Every invocation leaks one `/tmp/tmp.XXXX`. This range's own new gotcha bullet in `memory/gotchas/bounded-through-a-pipe-is-unbounded.md` records 99 stale `/tmp/tmp.*` directories from this kit's suites causing `Device or resource busy` and twenty-minute zero-output hangs. Attribution note: `git log -S TMPBIN` puts the leak in `570f810` and the gotcha bullet in `ebe5012` — same range, different commits. The recorded 99-dir incident was caused by killed suites holding subtrees open, not by this leak, which matches the low rating. | Capture the parent and add it to the trap. Placing it under `$TMP` is NOT safe here — `reset_tree` runs `git clean -qfd` inside `$TMP` and would delete a `bin/` there — so the trap is the fix, not the location. | A suite-teardown arm: record `ls -d /tmp/tmp.*` before and after a run and refuse on a net increase. One arm covers every future leak in this kit, which is the class the gotcha documents. | 23 |

## Refuted (3 of 23)

Three raw findings did not survive the skeptic and are not carried here. Two overstated a diagnosis defect as a wrong merge verdict; one asserted a divergence the code does not reach because an earlier refusal prevents the second round the leg would need. The surviving instances of those two areas are F9 and F8 respectively, at the severity the evidence supports.

## Corrections to the raw finder claims

Kept because a review record that silently launders its inputs is not a record.

- **F1** — the finder reported three failing assertions; four fail (arm 5 as well). The finder also said the leg has no `guard` in `tools/gate-legs.json`; it does (`["tools/lib/","tools/unattended/"]`). The conclusion is unchanged: `GATE_FULL=1` in `.githooks/pre-push` bypasses every guard, and this range touches `tools/unattended/` anyway.
- **F4** — the finder reported the leg RED on 16 tracked run-state files. Thirteen `RUN.md` files are tracked, and origin's advertised HEAD is present in this clone at review time, so the reproduction was of a transient but entirely routine unfetched-clone state rather than a standing red. The missing presence guard is unconditional.
- **F14** — the finder called the truncation risk silent coverage loss; it is loud. The arms fail rather than pass. Still the same class, still one line.
- **F15** — the finder attributed the leak and the gotcha bullet to one commit; they are two commits in this range.

## Working-tree note

The tree at review time carries uncommitted modifications to `.memory-tree.conf`, `memory/gotchas/bounded-through-a-pipe-is-unbounded.md`, `tools/unattended/check-unattended.sh` and `tools/unattended/check-unattended.test.sh`, including what appears to be a three-way `is_published` rework matching F4's remedy. Every finding above was verified against `ebe5012` via `git show`, not against the dirty tree. Whoever lands these fixes should reconcile with that work rather than re-doing it.
