# TOOL-dDerivedDocket-16 — acceptance ledger

**Serves:** journal TOOL-dDerivedDocket-16

The driver learns what an ask mandate is. A build README may carry one `asks:` line, preflight pins
three facts beside the base from it, `--plan` grows an ASK projection and a fifth `next:` shape, and
three verbs gain a refusal each. No merge bar, no gate leg and no `*.test.sh` suite ran in this
pass. The direct check was the DRIVER itself, run over a scratch fixture repository built under this
run's scratch root — a work tree with a bare origin one directory up, a build whose README carries a
mandate, a filing home carrying a `BACKLOG.md` and nothing else, and a stub standing in for the
declared generator — and every refusal below was READ off that run rather than asserted.

Two things were measured rather than reasoned about. The protocol pair's size is a `wc -c` at this
commit against `git cat-file -s` at the parent. The arm counts are a read of the meta-gate's own
extraction regexes over the two files, replicated in the scratch root so no gate was run: 256 fail
branches and 246 arms at the parent, 267 and 257 here, and every one of the eleven new branches
armed by a line carrying its WHOLE signature rather than a prefix of it.

**One Definition-of-Done item is OWED rather than met, and it is named here rather than quietly
skipped.** `memory/map/features/unattended.md` is the dossier §4 names, and its prose was not
refreshed. It measures 20467 B against the hygiene engine's 20480 B cap for that class — thirteen
bytes of headroom — and the cap's own remedy is to SPLIT the dossier into two, never to rotate it
and never to raise it. A split creates a second map feature record and is a structural change to the
map that this unit has no spec for and that no criterion here observes, so the alternative was
funding a refresh by gutting prose other units wrote, which is worse than an omission that says so.
This unit adds no inventory key, no gate leg, no hook and no guide, so nothing in the map's
machine-enforced half is stale; what is missing is the human half. PARKED for the orchestrator: the
dossier owes both a split and the ask-mandate paragraphs, and the two are one piece of work.

Five divergences were folded into the spec as rev-7 BEFORE the code was written, and the revision
log says why each one did. The one that mattered is AC13's line half: it asked for a line count at
or below the parent's over an edit that adds a table row, which no trim of a clause INSIDE a row can
fund. The byte half, which is the half S1's funding argument rests on, is unchanged and still
strictly below the parent.

**Evidences:** TOOL-dDerivedDocket-16
- AC1 — `--new-build` — `--preflight EXMP-aFoo-3` and `--preflight "tRun EXMP-aFoo-3"` both refuse
  under check 6 with the scaffold recipe carrying the tokens exactly as typed, under the kit default
  `ANCHOR_SCOPE` and under `ANCHOR_SCOPE=published` on a branch nobody pushed. `git status
  --porcelain` counts zero paths after each. Neither message names a missing README, the slug
  grammar or a push.
- AC2 — `FILING HOME` — `--preflight aFoo`, over a folder holding a `BACKLOG.md` and nothing else at
  the first anchor's merge-base, refuses under check 6 and prints the recipe; with `ASKS_CMD` blank
  it says in one line that it cannot list that folder's live asks and names the generator form to
  run by hand. Under `ANCHOR_SCOPE=published` on an unpushed branch it still answers with the
  recipe, ahead of the second anchor's push refusal. Nothing was written.
- AC3 — `m-base` — over a README carrying `asks: EXMP-aFoo-3..4` with both rows filed at the merge
  base, the record gains `m-base:`, `asks:` and `asks-ready:`; `m-base:` equals `git merge-base main
  HEAD` and the stub's recorded argv carries `--at` equal to it. With a mandate naming an id filed
  nowhere at that tree, preflight refuses under check 72 naming the id and the blob it read, and the
  porcelain count is zero.
- AC4 — `--resume` — with the README's `asks:` line changed at HEAD after the fact was pinned,
  `--resume` refuses under check 73 printing both lines. Observed on the row an agent actually runs
  after a compaction, the holder refreshing its own lease, which is why P6 sits above the matrix.
- AC5 — `ASKS_CMD` — a README carrying `asks:` while the key is blank refuses under check 70 naming
  the key, and pins nothing.
- AC6 — `DEAD PROBE` — five witness shapes, each read off the same stub: a ten-field row is a parse
  refusal quoting the line; one row fewer than the mandate is a dead probe naming the missing id; a
  producer that exits 1 names the exit status; one that writes to neither stream is a dead probe; and
  one that outlives a two-second bound is reported as never answered rather than as a red. A mandate
  whose every id grades not-ready refuses under check 75 and prints each id's failing rules.
- AC7 — `MISSING` — over a build whose roster plans `EXMP-tPlanA-2` and `EXMP-tPlanA-10` with no
  spec at all, `--plan` lists both, `-2` first, and closes with the MISSING shape naming `-2`. Check
  19 fires only where the roster, the specs and the mandate are all empty.
- AC8 — `unit` — a folder filing `EXMP-tUnitAsk-9 · filed … · unit · …` with no roster row has that
  id listed MISSING by `--plan`, while the ordinary ask filed beside it is not. The
  `--close` half is owed to the post-build bar: `build-complete` reads the same `missing_units`
  call this listing does, and the arm that drives the verb is in the suite.
- AC9 — `--paths` — `--plan --asks --paths` prints three TAB fields per ASK row and no more, counted
  by a pattern that admits exactly two tabs; `next:` reads the undecided shape once no unit shape
  remains. Check 30's half is a gate leg and is owed to the post-build bar.
- AC10 — `code 49` — `--dispatch` on a unit whose spec `closes` an ask held on a live mandated ask
  refuses under check 49 naming both asks. The green control is the same dispatch over the same
  fixture with the held-on ask terminal, which is admitted.
- AC11 — `--rescope` — an `add` naming a filed ask that is not a `unit` ask refuses under check 76;
  a `retire` of a `unit` ask with no generated row succeeds and says why it was admitted; a retire
  of an id neither carrier knows still refuses under check 48.
- AC12 — `BACKLOG_MODE` — with the anchor's `.memory-tree.conf` declaring nothing and HEAD's
  declaring `builds`, preflight prints one notice naming both.
- AC14 — `derived_phase` — a foreign live spec closing a mandated ask prints one report-only line in
  all four fixtures, and labels the build terminal in exactly one: record absent, record tracked and
  LANDED, record tracked and HELD, record present only on a branch this tree does not carry. The
  stub's recorded argv carries no `--live-builds`.
- AC16 — `git status --porcelain` — a `prompt`-mode README carrying `asks:` and a `recipe`-mode one
  each refuse under check 71, pin no fact, and leave the porcelain count at zero.
- AC17 — `UNDECIDED` — a build carrying an empty roster pair, no spec and an `asks:` line naming two
  filed asks exits 0, prints one ASK row per mandated ask, and closes with the undecided shape.
- AC18 — `rank` — over the hold-edge fixture the later-listed mandated ask ranks 1 and prints the
  duplicate-closer refusal row, the earlier-listed ranks 2, the unmandated filed ask ranks 3 with
  `ready=-`, `-2` sorts before `-10`, every summary carries the four keys in order, and `next:`
  reads the MISSING shape naming `-2`. Re-run with both MISSING units retired from the roster, the
  same fixture flips to the undecided shape naming the EARLIER-listed ask at rank 2 — not the
  rank-first one, which carries two closers and is contested rather than undecided.
