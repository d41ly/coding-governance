**Serves:** journal TOOL-dDerivedDocket-3

# TOOL-dDerivedDocket-3 — acceptance ledger

Every observation below was made inside the build pass by a DIRECT check: the driver run against a
scratch fixture repository with its own bare origin and a stub lander whose exits are env-driven,
the kit gate run over a fixture copy of the kit with a break staged into it, or a read of the
tracked files. No merge bar, no gate leg and no self-test suite ran in this pass. Three criteria
carry a `permission:` line deferring their observation and get no line here: AC3, AC11 and AC13.

**Evidences:** TOOL-dDerivedDocket-3

- AC1 — `gates-green` — in the fixture the bare branch tip passes its bar and the tip the remote
  advertises then gains a commit the same bar reds on; the prepared merge carries it, and `--close`
  under `in-place` reports that item unmet with the failing leg's own line beneath the refusal. Two
  anti-vacuity reads bracket it: the branch tip does not carry the file, and the prepared merge does.
- AC2 — `--prepare` — with the lander stub's `--prepared` exiting 1 the close refuses with a
  numbered message naming that verb and the run-state blob is unchanged; with it exiting 3 the
  refusal names the observation, the output carries no `--prepare` at all, and the blob is again
  unchanged. The exit-2 arm refuses naming `LANDER_MODE`.
- AC4 — `--land` — the kit gate run over a fixture copy reds on four staged breaks of the Land
  section: `--prepare` renamed, `--land` renamed, a sentence directing a merge into the node's own
  default branch, and an emptied section. The shipped template passes the same run. The shipped
  RENDER and the skill-wiring leg are the criterion's deferred half and are not read here.
- AC5 — `--preflight` — a stub exiting 2 on `--carry` and a stub exiting 2 on `--prepared` each
  refuse preflight naming `LANDER_MODE`; a stub exiting 3 on either refuses naming the observation,
  and the whole output carries the string `LANDER_MODE` zero times; a stub exiting 1 on both reaches
  `preflight OK`. Under `primary` a stub exiting 2 on both is never asked.
- AC6 — `--carry` — the stub's carry exiting 1 with a sha on stdout makes the close refuse, quote
  that sha under the refusal, leave the run-state blob byte-identical and leave the phase at
  `RUNNING`. The exit-2 and exit-3 arms refuse with their own messages.
- AC7 — `git status --porcelain` — the successful in-place close leaves that listing empty, the
  newest commit's subject is exactly `records(tRun): close — LANDING`, and the record reads
  `LANDING`. The subject names the slug and no unit id, so no unit's build commit moves.
- AC8 — `--reaped` — read on the rendered Skill: the Land section's incomplete-landing route is a
  branch push then `--hold --code platform-unavailable ... --reaped`, it covers the failed branch
  push and a close refused on the lander's observation failure, and the check-34 predicate finds
  zero sentences directing a merge into the node's own default branch. The `While it runs` bullet
  still sends the main loop to run the `GATE_SELFTESTS=1` bar by hand at `VERIFYING` and now names
  the announcement an in-place close prints.
- AC9 — `LANDER_MODE` — with the key blank the fixture close produces BASE's output and exit: no
  probe fires, no carry check runs, no commit is made, the record is staged at `LANDING`, the verb
  prints `COMMIT the run-state file`, and the bar's own environment reports `GATE_FULL` unset.
- AC10 — `GATE_FULL=1` — a fixture bar that dumps its environment reports `GATE_FULL=1` and no
  `GATE_SELFTESTS` when the close is run from a shell that does not set the name, and
  `GATE_SELFTESTS=1` when the owner exports it into the close, so the driver adds nothing and removes
  nothing. A range touching a declared entry carries the announcement naming the by-hand run; a
  range touching none carries the same environment and no announcement.
- AC12 — `gates-green` — with one untracked file in an otherwise prepared tree the close refuses by
  number naming the stamp precondition, and the fixture bar's environment dump is never written, so
  no bar ran.
- AC14 — amended rev-6 — the `memory/DECISIONS.md` half was removed because a unit pass structurally
  cannot write that file: `--dispatch` refuses a declared write overlapping a `SHARED_RECORDS` entry
  and the kit gate's subset test reds an undeclared one. The surviving half was observed:
  `grep -c 'verb here commits'` prints 0 on both protocol carriers, `NO driver verb commits` prints
  0 on the driver, `nothing commits it` prints 0 on the driver suite, and the rotation paragraph
  still names a shared witness as its reason.
- AC15 — `git rev-parse HEAD` — over a fixture whose Definition of Done is satisfied outright, the
  first close commits its record; the remote then moves and the landing is re-prepared, and the
  second close exits 0, names the existing close commit, leaves HEAD on the re-prepared merge and
  leaves `git status --porcelain` empty.
- AC16 — `git cat-file -s` — both protocol carriers end this pass at 60275 bytes and 673 lines
  against 60313 and 674 at the parent commit, so neither grew on either half, and both sit under the
  `GUIDE_CAP_BYTES=61440` / `GUIDE_CAP_LINES=750` pair the hygiene gate declares on one line. Both
  trims landed and both texts moved: `Listing two anchors` and `The move was a BYTE decision` each
  count 1 at the parent and 0 at the build commit, the first is present in `UNATTENDED-STOPS.md` and
  the second in `tools/unattended/README.md`. Both owed key-table rows arrived: `LANDER_MODE` and
  `SELFTESTS_OWED_PATHS` each count 0 at the parent and 1 or more at the build commit. The companion
  guide reads 15832 bytes and 258 lines, under both halves of the same cap.
