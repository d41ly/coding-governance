# TOOL-dDerivedDocket-30 — acceptance ledger

**Serves:** journal TOOL-dDerivedDocket-30

The three stop-census defects are closed in the unattended kit's leg and its library. Check 23's
brief exclusion now needs the unit, the build's own `prompts/` and the blob prefix at the pass
commit, all three inside `read_brief_paths`, so `pass_commit` and check 23 still excuse one set.
The conf import and the report channel sit above the `--only 28` guard, and every numbered check
after the 28 region is an announced skip derived from its header. Check 22 joins the import
allow-list to the keys the example declares and the leg initialises. Check 26 joins every flag
between the new `gov:argv` sentinels, plus the verb sets, to the driver header and to each
sibling suite. The driver header documents the seven flags it parsed and never named. The spec was
bumped to rev-7 first, because the origin/main merge had moved AC3's population.

No merge bar, no gate leg and no `*.test.sh` suite ran in this pass. What ran instead, from the
scratch root outside the tree:

- A replica of the leg suite's prologue with arms A to F, the four new brief fixtures G to J and the
  unit's end block, executed by hand over its own scratch fixture: 51 assertions, 43 of them new,
  all green. `--only 28` exits 0, announces the seven post-region checks and a staged
  `# ---- check 97` header, and prints nothing without the report channel. The four allow-list
  shapes, both flag directions, the dropped slug verb, the two driver-suite drops, the leg-suite
  drop and the deleted argv sentinel each red on the message their arm names.
- The same replica over three scratch kit copies, one change reverted in each. With HEAD's library,
  G, I and J fail and H holds. With HEAD's leg, `--only 28` dies on `line 4552: MEMORY_ROOT:
  unbound variable` and every hit of the end block fails, while its absence controls and its
  fixture edits hold, bar the two edits that need a sentinel HEAD lacks. With the
  skip list typed as today's seven, the staged 97 header alone fails. The brief arms were re-run
  green after the last library edit, which detaches the blob read from the loop's stdin.
- A replica of the driver suite's prologue and its `--plan --paths` block: 17 assertions green
  over the real driver, and three FAIL lines over a scratch copy whose `--framed` arm was deleted
  and whose `--version` prints a wrong version.
- `check-arms.py --report`, read-only: 233 branches and 224 armed on the leg, every new branch
  armed. The unarmed set is the one the merge left: check 2 ordinals 15 to 17, whose pinned rows
  still name 11 to 13, check 36 branch 3, and check 16 ordinals 14 to 18.

Every criterion observed through the leg suite, the `unattended kit gate` leg over the real tree
or the `kickoff-manifest ratchet` leg carries a `permission:` line deferring it to VERIFYING, so
none of them gets a line here: AC1, AC2, AC4 to AC9, AC11, AC12 and AC13, and AC3's real-tree half.

**Evidences:** TOOL-dDerivedDocket-30
- AC3 — `# ---- check 97` — the fixture half, over the replica fixture: `--only 28` exited 0,
  named checks 30, 31, 32, 33, 39, 40 and 15 on the report channel, and named 97 once that header
  was staged after the region. The real-tree half is owed at VERIFYING.
- AC10 — `grep -n 'belt and braces' tools/unattended/check-unattended.sh` — one hit, in check 31's
  comment, which gives the hoist and the announced skip as the reason and no longer the crash.
- AC14 — `wc -c < memory/guides/SESSION-KICKOFF.md` — the carrier is the same size at the parent
  and in the staged tree, and `git diff --numstat` reads one line added and one removed.
