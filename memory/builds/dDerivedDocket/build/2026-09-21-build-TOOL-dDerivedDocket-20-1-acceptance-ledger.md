# TOOL-dDerivedDocket-20 — acceptance ledger

**Serves:** journal TOOL-dDerivedDocket-20

The unattended carriers now describe the per-build ask model without saying anything false in shards
mode, a companion guide carries the ask contract, the harness hands a unit's `closes` ids to its SPEC
writer, and the kit refuses a path declared under both of condition 3's keys. No merge bar, no gate
leg and no `*.test.sh` suite ran in this pass. What ran instead, from a scratch root outside the tree:

- the driver and the leg themselves over a scratch fixture repository carrying a copy of the kit and a
  conf built from the kit's example, for AC1's three cases and a control;
- the same fixture for check 10's third pair: the installed ask guide drifted, then deleted, then the
  clean pair read on the report channel;
- a node probe evaluating the rendered build harness with stub hooks, as its runtime does, printing
  each SPEC writer's prompt, and the same probe over a staged break of the harness;
- `bash tools/workflows/check-protocol-parity.test.sh --render` and `--check`, a read-only verb the
  gate guard admits, and `node tools/workflows/check-workflow-syntax.js` over the rendered harness;
- byte counts, line counts and greps over the tree and over `HEAD`, this unit's parent.

AC1's hand observation, over the fixture: `bash tools/unattended/unattended.sh --status` refused at
conf load, exit 2, naming `SHARED_RECORDS memory overlaps the GENERATED_INDEXES index memory/LIVE.md`,
then the reverse nesting, then an undeclared `SHARED_RECORDS` against a `memory/backlog` index; the
leg reported check 38 naming the same pair in each of the three, and neither printed either line over
the example's own values. Two staged breaks each silenced the arm they target: the leg reading the
undeclared key as empty, and the predicate comparing strings for equality. The library call over
gov's own `.unattended.conf` values returned no pair. The gov-conf half is the leg over the real tree,
owed to the post-build bar.

AC3's in-pass reads: both protocol copies are byte-identical, 59649 B and 671 lines at the parent and
59634 B and 670 lines here, under the 61440 B and 750-line guide cap. Per copy,
`minted as a backlog row` and `deferred costs the whole finding` count 1 at the parent and 0 here;
each of §4's five phrases counts 0 at the parent and 1 here, the pointer row's among them; the
installed ask guide counts the moved phrase once. The check-10 run is owed to the post-build bar.

AC4's in-pass reads: `memory/guides/UNATTENDED-ASKS.md` is 12206 B, byte-identical to
`tools/unattended/ASKS.template.md`, and its eight level-2 headings are §4's list in order. Over the
fixture, check 10 reported `byte-compared 3 of its 3 pairs: protocol verbs asks` on the report channel
and named each of its two new refusals. The adopter's `--check` and the real-tree check 10 are owed to
the post-build bar.

AC7's in-pass reads: the build-method template 27058 B and 345 lines at the parent, 26910 B and 344
here; the rendered copy 27033 B and 345 lines, 26891 B and 344 here, under 27648 B and 350 lines, and
a direct substitution of the template's two placeholders reproduces the rendered copy byte for byte.
`memory/backlog` counts 0 in the rendered copy and `resolves to an empty range` counts 1 in the
memory-tree README, where the parent counts 0. The size leg is owed to the post-build bar.

AC8's in-pass reads: the probe found the unit's ids in its own writer's prompt, none in the other
writer's, that writer's prompt byte-identical to a run with no `closes` anywhere, and a string
`closes` thrown by name; with the clause handed to every writer instead, the second and third of
those reads went false. `--check` reported both rendered pairs in parity. The suite arm is observed
at VERIFYING.

AC1, AC3, AC4, AC7, AC8 and AC10 carry `permission:` lines and get no line here; the orchestrator
writes them after the post-build bar.

**Evidences:** TOOL-dDerivedDocket-20
- AC2 — `git grep -n "section 1 MANDATES" -- tools/unattended` — returns nothing, exit 1, and the
  comment above `build_commit` now says a backlog row is owed only by a unit planned before its spec,
  and that the exclusion is whatever `SHARED_RECORDS` and the index halves of `GENERATED_INDEXES`
  declare, in either backlog mode.
- AC5 — `--preflight` — the rendered Skill's routing table carries a row sending ids, a range, a
  prompt naming ids and a filing-home slug to "Ids go through the scaffold", whose first step hands
  the value to `--preflight` and relays the recipe its refusal prints; the opening fence above the
  table refuses a value mixing a slug and ids before any verb; the prompt path sends a prompt naming
  ids to the same route and writes nothing.
- AC6 — `migrate_backlog.py --recipe` — step 5 of the slug path names the guide section each of its
  orientation, parking, filing, repoint and `status: OPEN` steps follows, and points the authority
  step at protocol section 1; step 6 parks a pre-flip BASE with the recipe
  `migrate_backlog.py --recipe` prints and says never to run `--relocate`.
- AC9 — `memory/builds/dDerivedDocket/build/` — the sweep record there,
  `2026-09-21-build-TOOL-dDerivedDocket-20-2-carrier-sweep.md`, lists every protocol and Skill
  sentence naming ids, slugs, the prompt path or backlog rows with its verdict and the driver
  behaviour it was read against, and pairs each leg rule units 16 to 19 added with its carrier
  sentence.
