# Brief — TOOL-aHoistedPass-4

**Provenance.** The standing mandate's roster named
`memory/builds/aHoistedPass/prompts/brief-TOOL-aHoistedPass-4.md` and no file existed at it — true of
all ten units — and that name is unwritable anyway, because hygiene check 5 derives the record kind
from the `prompts/` subfolder. This brief was DERIVED by the building run from the mandate's roster
line and the unit's spec, and is recorded as such. A `brief · item` row over a self-authored brief
proves a file existed at the pass, never that a harness wrote it.

## What this unit is

`memory/builds/aHoistedPass/spec/2026-09-04-spec-TOOL-aHoistedPass-4.md`, rev-5, Tier-1, order 1, in
the parallel group with `TOOL-aHoistedPass-1` and `-8`. Read it whole.

The unit is NARROWED and section 2 says so in bold: S1, S2 and S3 landed without it, under
`TOOL-aWeldedTribunal-1`, between the drafting base and the run's BASE. What it owes for those three
is VERIFICATION at BASE. What it BUILDS is S4 (the mutual-exclusivity note in two carriers), S5 (the
`agent-cap` kit 1.12 to 1.13 across both marker carriers) and S6 (closing the backlog row it answers).

## What the run must not do here

Do not touch the predicate block or the test arms — AC7 forbids both and AC8 caps the diff at exactly
three files under `tools/`. Do not restate the landed constants under the dead `LOOP_HEAD`/`LOOP_TAIL`
names section 4 used to carry; rev-5 restated that block to what shipped. The note may state no bound
as a bare number (`agent-cap restatement`) and may name no path outside its own file (the hooks kit's
authoring rule, and `install-prefix` grades it).

## The loss this unit inherits

Old AC6 wanted the failing case staged RED before the widening landed. Unobservable now: the widening
is already in the tree. This run inherits arms whose failing case it has not seen, and section 6 says
so rather than calling them covered.

## The acts this pass owes the record

`--dispatch` with the write set, `--brief` with this file, one commit carrying the unit id, then
`python tools/memory-tree/gotchas.py --for-diff HEAD~1..HEAD` acted on before the next unit.
`memory/backlog/TOOL.md` is a declared SHARED_RECORD, so the dispatch declaration cannot carry it.
