# Brief — TOOL-aHoistedPass-8

**Provenance.** The mandate's roster named
`memory/builds/aHoistedPass/prompts/brief-TOOL-aHoistedPass-8.md` and no file existed at it, for any
of the ten units; that name is also unwritable, because hygiene check 5 derives the record kind from
the `prompts/` subfolder. Derived by the building run from the roster line and the spec, recorded as
such. It proves a file existed at the pass, not that a harness wrote it.

## What this unit is

`memory/builds/aHoistedPass/spec/2026-09-04-spec-TOOL-aHoistedPass-8.md`, rev-4, Tier-1, order 1,
parallel with `TOOL-aHoistedPass-1` and `-4`. Read it whole.

S1 through S4 were performed while the spec was written, at `c4fcf5ad`, and section 4 carries their
results. What the build pass owes is **S5** — land the measurement as a record under this build's
`build/` folder carrying `**Serves:** research TOOL-aHoistedPass-8` — and **S6**, which rev-3 and
rev-4 already discharged: F1 is carried in section 8 with a recommendation and marked NOT TAKEN, and
parked to the owner in the run-state file.

## The one judgement this pass makes

rev-4 folded a finding that the harness's empty-`units` guard MOVED between `c4fcf5ad` and BASE. A
record whose whole value is that a later reader can re-open it must not transcribe a measurement
taken at a base that no longer exists. **Re-run the probe at BASE and record what BASE says**, with
the two controls that make each observation attributable. Where BASE and `c4fcf5ad` disagree, the
record says so; where they agree, it says that too.

## What the run must not do here

Take no side. AC9 requires the commit to touch no path under `tools/` and requires
`"Recipe mode does not take it"` to still return one hit in each of its two carriers. The
carrier/registry disagreement stays live on the bar, as section 3 says. Do not stage the fixture in
the live tree — `readme-contract.txt` would owe a row and a pin move for a folder nobody keeps.

## The acts this pass owes the record

`--dispatch` with the write set, `--brief` with this file, one commit carrying the unit id, then
`python tools/memory-tree/gotchas.py --for-diff HEAD~1..HEAD` acted on before the next unit begins.
