**Serves:** journal TOOL-aHoistedPass-5

# Brief — TOOL-aHoistedPass-5

**Provenance.** The mandate's roster named
`memory/builds/aHoistedPass/prompts/brief-TOOL-aHoistedPass-5.md` and no file existed at it, for any
of the ten units; that spelling is also unwritable under hygiene check 5. Derived by the building run
from the roster line and the spec. (A different file, `2026-09-05-prompt-TOOL-aHoistedPass-5-fold-round1.md`,
already sits in this folder — it is the round-1 FOLD prompt, not this unit's build brief.)

## What this unit is

`memory/builds/aHoistedPass/spec/2026-09-04-spec-TOOL-aHoistedPass-5.md`, **rev-5**, Tier-2, order 4,
alone in its step. Read it whole.

Land `tools/workflows/unattended-unit.js`: a workflow script that builds exactly ONE unit, oriented in
that unit's spec and brief and never in the roster. The subject is Appendix A of the design record,
which carries the candidate verbatim so the unit has a durable subject rather than a path in a
session scratchpad.

## The one thing rev-5 changed, and it is the merge bar

F1 resolved to "the child yields": one top-level definition, so the `kit-js` liveness floor has a
symbol to index. rev-4 fold-confirmed that definition as `function need(key, why)`. **`need` is not
in the declared verb table and the verb pin has ZERO headroom** — `offenders=467` against
`VERB_OFFENDER_PIN="467"` — and the leg is guarded on `tools/`, so that commit reds. The definition
is `check`, which the table carries and whose gloss is what the eight argument refusals do. Do not
raise the pin: a pin raised so one name may sit outside the table turns the table into a synonym
list.

## What the run must not do here

Touch nothing that calls this file: not the parent, not the Skill, not the protocol, not
BUILD-METHOD. Widen no fan-out predicate. Do not teach the codebase-map floor about the export scan —
that is F1 option (b), refused under M3 veto 2 as another kit's public surface. Spell no `tools/`
literal: the driver spelling, the ground text, the checklist command and both document paths all
arrive in `args`.

## What has no standing reader

Section 7 says no standing leg calls this file at all. So the prompt's five acts are observable only
by AC16's grep of the landed string, and the properties in the header — it never nests, it holds one
straight line, `export const meta` is a selector — are asserted once at landing and held by review
after that. The header must say so rather than implying a guarantee.

## The acts this pass owes the record

`--dispatch` with the write set, `--brief` with this file, one commit carrying the unit id, then
`python tools/memory-tree/gotchas.py --for-diff HEAD~1..HEAD`.
