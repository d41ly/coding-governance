**Serves:** journal TOOL-aHoistedPass-2

# Brief — TOOL-aHoistedPass-2

**Provenance.** The mandate's roster named
`memory/builds/aHoistedPass/prompts/brief-TOOL-aHoistedPass-2.md` and no file existed at it, for any
of the ten units; that spelling is also unwritable under hygiene check 5. Derived by the building run
from the roster line and the spec.

## What this unit is

`memory/builds/aHoistedPass/spec/2026-09-04-spec-TOOL-aHoistedPass-2.md`, **rev-5**, Tier-2, order 3,
alone in its step. Read it whole — thirteen scope items, nineteen criteria, four resolved forks.

`passes-harnessed:M6` binds every unattended run to a route that does not exist: 0 of 17 core
directive handles appear in `BUILD-METHOD.md` at all, and check 16 arm B asserts only that the cited
section EXISTS. This unit writes the route into M6, anchors all seventeen handles in their own
sections, and gives arm B a second term that reads the cited section's BODY.

## The three things that decide the shape

**The comment strip is BLOCK-WISE.** A line-prefix filter drops only a comment's first line, so a
multi-line `<!-- … -->` carrying the anchor on line two satisfies it while the section states no
rule. Measured on four fixtures: the naive form PASSES that evasion and the block form REDS it, with
an honest sentence green in both.

**Anchors are WOVEN into existing sentences, never listed.** The line axis is ungated: the render is
at 350's cap minus roughly 22 lines after this unit's other additions, and a list would approach the
cap with nothing measuring it.

**Order matters and is measured.** The term reds 17 of 17 handles against the file as it stands, so
the ratchet before its anchors reds the bar and the anchors before it are unguarded prose. Both land
in ONE commit.

## What the run must not do here

Move no kit version and no marker: `DEPL-aHoistedPass-1` and `TOOL-aHoistedPass-3` already set them
at order 2, and rev-5 corrected three Files-touched cells that still said otherwise. Do not touch
`unattended-build.js`, `tools/hooks/`, or check 31. Do not strike the pass-order clause at §12 —
rev-3 reversed that and NARROWS it instead, because `memory/DECISIONS.md:125` ratifies it.

## The two dangling references this unit ships knowingly

`tools/workflows/unattended-unit.js` does not exist until order 4, and `--plan <slug> --paths` does
not exist until order 5 — and `verb_plan` silently DISCARDS an unrecognised trailing argument rather
than refusing it. Both windows are HELD BY THE RUN, disclosed in F2, and caught by nothing.

## The acts this pass owes the record

`--dispatch` with the write set, `--brief` with this file, one commit carrying the unit id, then
`python tools/memory-tree/gotchas.py --for-diff HEAD~1..HEAD`. AC1 and AC2 are staged breaks observed
RED before the anchors land, and `check-unattended.test.sh` is run by hand because no boundary runs
this kit's self-tests.
