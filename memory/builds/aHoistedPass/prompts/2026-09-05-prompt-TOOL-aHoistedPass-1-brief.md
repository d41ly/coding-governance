# Brief — TOOL-aHoistedPass-1

**Provenance, stated first because the row that hashes this file asserts what the builder was
handed.** The standing mandate's roster named
`memory/builds/aHoistedPass/prompts/brief-TOOL-aHoistedPass-1.md` and NO FILE EXISTED AT IT — the
same is true of all ten units. That name is also unwritable: hygiene check 5 derives the record kind
from the `prompts/` subfolder, so the only legal spelling is
`YYYY-MM-DD-prompt-<FAMILY>-<slug>-<seq>[-tail].md`, which is this file's name. The parent that would
have written it is `TOOL-aHoistedPass-5`, which this build has not built yet. So this brief was
DERIVED by the building run itself from the mandate's roster line and the unit's spec, and it is
recorded as such rather than as something a harness produced. A `brief · item` row over a
self-authored brief proves a file existed at the pass, never that a harness wrote it — which is
`TOOL-aHoistedPass-7` section 3's own position and the `dRetiredFork` finding the build README opens
with.

## What this unit is

`memory/builds/aHoistedPass/spec/2026-09-04-spec-TOOL-aHoistedPass-1.md`, rev-3, Tier-1, order 1.
Read it whole; it is the design and this file does not restate it.

The record catches up with the verdicts that superseded it. Five scope items, all inside `memory/`:
append one superseding decision row (S1), amend one stale backlog row without closing it (S2), file
one backlog row per residual bullet of the design record's section 10 (S3), file one further row for
the four tracked non-memory carriers that still state the superseded verdict (S4), and regenerate the
derived artifacts in the same commit because minting ids rewrites this build's generated roster (S5).

## What the run must not do here

No file outside `memory/`. The four live carriers under `tools/` are section 3 non-goals: correcting
them makes a records unit an owner turn under M3 veto 2. `memory/DECISIONS.md` line 65 is not
rewritten, softened or annotated — supersession is a new id and a note.

## Order and gates

Order 1, in the parallel group with `TOOL-aHoistedPass-4` and `TOOL-aHoistedPass-8`. Gates are
`memory hygiene` and `build README slot contract`; no new leg, no leg moved or waived.

## The acts this pass owes the record

Declare the write set with `--dispatch`, record this brief with `--brief`, commit with the unit id in
the subject, then run `python tools/memory-tree/gotchas.py --for-diff HEAD~1..HEAD` and act on what it
names. `memory/DECISIONS.md` and `memory/backlog/` are declared SHARED_RECORDS, so `--dispatch`
refuses them by construction and the declared set names only the paths outside that surface.
