**Serves:** spec-audit KICK-aReplayedCard-1 KICK-aReplayedCard-2 KICK-aReplayedCard-3 TOOL-aReplayedCard-1 TOOL-aReplayedCard-2 TOOL-aReplayedCard-3 TOOL-aReplayedCard-4 TOOL-aReplayedCard-5

# aReplayedCard — spec audit of the one rev-3 unit round 2 left BLOCKED, round 3

*Node `a`, 2026-09-14. A Tier-2 adversarial pass over `TOOL-aReplayedCard-1` at its rev-3 blob: a
fan of four primed finder lenses, a skeptic stage in five batches prompted to REFUTE each finding,
one synthesis. The mandate was the FOLD, not the original text: for each of round 2's eight TOOL-1
defects (B1, H2, H3, H4, M1, M12, L3, L4), does the rev-3 line that claims to close it actually close
it, and did closing it open something else — an absent-card allow at predicate step 5, a value-flag
grammar and a `-C` toplevel, an inline-span class arm with a floor of 8, a tree-cell rewrite that
leans on `KICK-aReplayedCard-2` S10, a `sed`-based cross-kit arm, a pathspec `*builds/*/README.md`
at the resolved toplevel. The unit was cross-read against its rev-3 siblings KICK-1, KICK-2, KICK-3,
TOOL-2 and TOOL-3 on the four M2 axes, and every code claim was checked against the cited file and
line. The synthesis re-read at source every claim a blocker or high below rests on, counted the
engine's spans at base, and reproduced four claims on this node; what it re-read and what it did not
run is listed at the end. The binding line names all eight units because the cross-read read the
five rev-3 siblings and three fixes below land on them.*

**Round: 3.** Subject, pinned at the blob it was read at:

- `memory/builds/aReplayedCard/spec/2026-09-13-spec-TOOL-aReplayedCard-1.md@2ed4ddc91ec8e0aee3aa1c96b8e985225d3ce71e`

The siblings were read at the tree of `5677452c`, where each carries the rev the README's
generated table states (KICK-1, KICK-2, KICK-3, TOOL-2, TOOL-3 at rev-3). They are cross-read
context, not subjects, and drew no finding on their own text.

## Verdict: BLOCKED

One blocker stands, and it is round 2's blocker moved one window later by the fold that closed it.
Rev-3 made an ABSENT card allow, so nothing denies the landing run at order 3; but `TOOL-aReplayedCard-2`
at order 4 wires `resume|compact` to `--card --replay` into this worktree's `.claude/settings.json`,
`KICK-aReplayedCard-1` S5 has replay WRITE a fresh card ending `READY — none yet` for a session that has
none, and TOOL-1 step 6 then denies every main-loop `git commit` the landing run makes from its first
compaction after order 4 to the end of the run — orders 5, 6 and the closing pass — with a remedy,
`/session-kickoff`, that on this node is a junction to the PRIMARY tree's OLD engine and cannot append
until the landing merge. Rollout calls that window "the designed state". The only exits are the
evasions S6 names, which is what round 2 rated blocker as B1. Five high defects follow: the `-C`
target resolution is undefined for the `/c/…` spelling the charter and the engine prescribe and for a
walk that finds no `.git`; a QUOTED `-C` value is two quote tokens in the blanked view, so
`git -C "$ROOT" commit` never reaches `commit`; every exit-0 criterion over a present card is now
satisfiable by the card not being found, and the cross-kit arm has no discriminator at all; the one
stderr line that is the absence-allow's compensating control goes to the debug log and reaches nobody;
and the exemption keys on `authorized-by: prompt` while the unattended kit's own set is
`prompt recipe`. Of round 2's eight TOOL-1 defects, three are closed clean (H2, L3, L4), three are
closed with a residue named below (H4, M1, M12), and two are reopened one layer down (B1 as the
blocker, H3 as H1 and H2 here).

## Review shape

Raw 46, confirmed 34, refuted 12, unverified 0, precision 0.74. Precision cleared §8's ~0.5 floor by
a wide margin for the first time in this build, up from 0.49 in round 2 and 0.48 in round 1; the
priming asked each lens to name the mechanism a rev-3 sentence relies on and to reproduce it where a
scratch repository or a node one-liner could, and eleven of the confirmed findings carry a
reproduction. The refutations concentrated in findings that read the fold's new prose for internal
consistency without checking the sibling it leans on.

**Run integrity.** Lenses 4/4 returned, 0 died. Skeptic batches 5/5 returned, 0 died. 0 contradictory
verdicts demoted to unverified, 0 spurious verdicts discarded, 0 duplicates removed by the pipeline's
dedup stage. The run is complete on its own terms. Because no lens died, the zero on the five rev-3
siblings' own text is a zero from a full cross-read by all four lenses, not a zero from absence; it
remains a spec audit's zero, which grades what a document says and not what a mechanism will do. The
pipeline's dedup found 0, but 34 confirmed findings name 19 distinct defects — four lenses found the
`-C` resolution gap, three found the quoted value, three found the green-by-absence over exit 0, three
found the landing window, three found the pathspec over-match. The fold below is editorial and keeps
every raw id. Each raw id takes the severity of the defect it evidences, so the per-id table and the
integers in the state block agree by construction.

| Defect | Severity | Raw ids folded in |
|---|---|---|
| B1 the landing run is denied from its first post-order-4 compaction with a remedy that cannot append | blocker | 4, 13, 35 |
| H1 the `-C` target resolution is undefined for the `/c/…` spelling and for a walk that finds no `.git` | high | 2, 14, 23, 37 |
| H2 a quoted `-C`/`-c` value is two quote tokens in the blanked view and the target extracts as blanks | high | 1, 15, 27 |
| H3 every exit-0 criterion over a present card is satisfiable by absence; AC10 has no discriminator | high | 3, 24, 39 |
| H4 the exit-0 stderr line reaches nobody, so the absence-allow's compensating control is inert | high | 25, 36 |
| H5 the exemption keys on `prompt` alone while `SECOND_ANCHOR_MODES` is `prompt recipe` | high | 40 |
| M1 the fixture repository is never located; AC11 guards a directory the EXIT trap deletes | medium | 33, 42 |
| M2 the pathspec is wider than the rule it claims to derive from, and the path half is unobserved | medium | 5, 30, 44 |
| M3 AC4's red-when is unobservable: staged and worktree bytes are identical | medium | 6, 31 |
| M4 AC10 still executes the file KICK-2 writes in the same parallel step | medium | 7 |
| M5 a second real-READY append is undefined in KICK-2 and may be refused under the cap | medium | 8 |
| M6 S2 compares the `-C` TARGET's toplevel; KICK-2 S10 rewrites the cell to the PROCESS cwd's | medium | 28 |
| M7 the self-test leg's guard omits `skills/session-kickoff/`, so the two class arms skip their subject | medium | 26 |
| M8 the predicate's numbered list and the paragraph beneath give two evaluation orders | medium | 16, 43 |
| M9 no criterion resolves the common dir through a `.git` FILE, the path every registered node takes | medium | 29 |
| L1 every deny "prints the card path", stated twice and asserted nowhere | low | 12 |
| L2 the `cwd`-absent fail-open is observed by no criterion | low | 21 |
| L3 AC9 feeds `git log --grep push`, not the motivating `git log --grep commit` | low | 22 |
| L4 the README's third build-level rule restates the exemption reading §8 resolved AGAINST | low | 46 |

Tally by raw id: **3 blocker · 13 high · 14 medium · 4 low** = 34. Nine severities were RAISED from
the skeptic's verdict and are argued where they sit: 4 and 13 (skeptic: high) take B1's blocker; 15
and 27 (skeptic: medium) take H2's high; 25 (skeptic: medium) takes H4's high; 30 and 44 (skeptic: low)
take M2's medium; 31 (skeptic: low) takes M3's medium; 33 (skeptic: low) takes M1's medium; 43
(skeptic: low) takes M8's medium. None was lowered. In every raised case the id evidences the one
defect its cluster names, and a defect has one severity.

## The fold, defect by defect

What rev-3 did with each of round 2's eight TOOL-1 defects, read against the mechanism rather than
the §9 line.

| Round 2 | Rev-3 fold | Verdict |
|---|---|---|
| B1 KICK-1 creates the real `orientation/` at order 2; the landing run is denied from order 3 | step 5 allows an ABSENT card with one line; the directory probe is gone; AC1 expects exit 0 | closed for order 3; REOPENED one window later: TOOL-2 at order 4 wires replay, replay writes a sentinel, the landing run is denied until the merge (B1); the compensating line is inert (H4); every present-card allow is now satisfiable by absence (H3) |
| H2 the class arm's population is empty | inline `git ` spans of Steps 0–4, eight at base, floor 8 beside the figure, refusal on zero | closed: counted eight at `c4f02308`; the near-miss population never holds `commit` (L3) and the leg never runs on a commit that moves the spans (M7) |
| H3 `git -C <dir> commit` escapes the grammar | S1 skips the one value token of seven flags; S2 resolves the toplevel from the `-C` target; AC9 adds two DENY literals | closed for the unquoted `-C /c/x` token walk; REOPENED one layer down: `/c/x` resolves to `C:\c\x` under node and no step says what a failed walk does (H1); a quoted value is two tokens (H2); the target compare disagrees with KICK-2's cell (M6) |
| H4 the `tree —` cell is written once | S3's remedy is a kickoff IN the target tree; KICK-2 S10/AC10 rewrite the cell; TOOL-1 AC3 rewrites it by hand | closed for the first real append onto a sentinel card — TOOL-1 S3 and AC3 agree with KICK-2 S10 and AC10; a SECOND real append is undefined and may be refused under the cap (M5); AC3 proves the compare, not the remedy (M5, H3) |
| M1 AC10 reads `--card --append` from a same-step sibling | the sentinel is replaced by the test's own `sed` | closed for the verb; the arm still EXECUTES `manifest-check.sh`, the file KICK-2 writes at the same order (M4) |
| M12 `'*/builds/*/README.md'` misses a top-level `builds/` | `'*builds/*/README.md'` at the resolved toplevel, both depths pinned in AC4 | closed for the two depths, reproduced; the same pathspec lists four more shapes and nothing reds a key-bearing README outside `builds/` (M2) |
| L3 twelve arms, eleven rows | the count left the prose; §7 holds twelve `New arm:` rows | closed |
| L4 `tool_use_id` fails open on a field never read | S5 names `session_id` and `cwd` only; AC7 arms a missing `tool_use_id` expecting exit 2 | closed; the `cwd` half of S5's own fail-open is unobserved (L2) |

The six questions the mandate asked, answered in one line each and argued under the finding named.

- **Does the absent-card allow close B1 without a hole the sentinel deny cannot cover?** No. It
  closes the order-3 window and opens the order-4-to-merge window, where the sentinel deny is exactly
  what refuses the landing run (B1). The allow's own compensating control, one stderr line on exit 0,
  reaches nobody (H4).
- **Do S1's value-flag grammar and S2's `-C` resolution hold against the charter's `git -C`
  spellings?** The grammar holds for the unquoted `git -C /c/x commit` — verified by walking the
  blanked view's tokens — and fails for the quoted `git -C "$ROOT" commit`, which the view splits into
  `git`, `-C`, `"`, `"`, `commit` (H2). The resolution does not hold: `/c/…` is the spelling the
  charter §11 and `SKILL.md:44-45` prescribe, node resolves it to `C:\c\…`, which does not exist, and
  no predicate step states what a walk that finds no `.git` does (H1).
- **Does the inline-span arm extract eight spans from Steps 0–4 at base?** Yes. Lines 33–194 of
  `SKILL.md` at `c4f02308` hold exactly eight inline spans beginning `git `: `rev-parse
  --show-toplevel`, `remote`, `symbolic-ref --short refs/remotes/<remote>/HEAD`, `-C`, `worktree
  list`, `merge-base <remote>/<default> HEAD`, `merge-base <local-default> HEAD`, `rev-list --count
  <old-stamp-sha>..HEAD -- <watch…>`. The floor of 8 and the empty-population refusal are sound. None
  of the eight holds the token `commit` outside the subcommand slot, and AC9's four near-miss
  literals do not either, so the rule S1 motivates with `git log --grep commit` is fed nowhere (L3).
- **Does the tree-cell rewrite (S3, AC3) agree with KICK-2 rev-3 S10 and AC10?** For the first real
  append onto a sentinel card, yes: KICK-2 S10 rewrites the cell to the toplevel the append runs in,
  AC10 observes a card written in A and appended from B, and TOOL-1 S3 says the same thing. The
  case the remedy exists for — a session that ALREADY holds a real READY line from A and commits from
  B — needs a second real-READY append, which KICK-2 S5 defines only as sentinel replacement (M5); and
  S10's "toplevel the append runs in" is the process cwd, while TOOL-1 S2 compares the `-C` target
  (M6).
- **Does the `sed`-based cross-kit arm still guard the spelling fold?** Only if the card is found,
  and nothing in AC10 proves it is. `process.cwd()` in a bash suite whose `run()` helper launches node
  from the test's own cwd is this worktree, whose common dir holds no card for a fixture session, so
  step 5 allows with exit 0 and the arm is green (H3, M1). It also still executes the file KICK-2
  writes in the same parallel step (M4).
- **Does `*builds/*/README.md` at the resolved toplevel list both depths?** Yes, reproduced in a
  scratch repository at git 2.54: `builds/x/README.md` and `memory/builds/y/README.md`, for both the
  `--others` and the `--diff-filter=A` spawn. It also lists `rebuilds/z/README.md`,
  `mybuilds/q/README.md`, `builds/a/b/README.md` and `memory/builds/y/reviews/README.md`, because a
  plain pathspec `*` crosses `/`; the `:(glob)` forms list the two alone. From a subdirectory cwd the
  same pathspec lists only that subtree, relative to it, so the toplevel rule is right and the
  derivation claim is not (M2).

## Findings

| # | Sev | Unit(s) | Address | One line |
|---|---|---|---|---|
| B1 | blocker | TOOL-1, TOOL-2, KICK-1, KICK-3 | TOOL-1 §4 Rollout, §2 S5, §8 third mark; TOOL-2 §4 Rollout and Files touched; KICK-1 §2 S5; KICK-3 §4 Migration | After order 4 the landing run's first compaction writes a sentinel card and every later main-loop commit is denied; the remedy is the primary's old engine through the junction. |
| H1 | high | TOOL-1 | §2 S2; §4 Data model (no step for an unresolved toplevel); §6 AC9 `git -C /c/x commit -m y` | `/c/x` resolves to `C:\c\x` under node; the walk finds nothing; no step says what then; AC9's DENY can only be met by an unstated fallback. |
| H2 | high | TOOL-1 | §2 S1, S6; §4 Inventory `extractCommitTarget`; §6 AC9 | A quoted `-C`/`-c` value blanks to two `"` tokens; the grammar never reaches `commit` and the target is whitespace. |
| H3 | high | TOOL-1 | §2 S7; §6 AC2b, AC3b, AC6, AC9, AC10, AC12 | Every present-card ALLOW asserts exit 0 alone, which step 5 also returns when the card is not found; AC10 has no deny twin and its `process.cwd()` is the test's. |
| H4 | high | TOOL-1 | §2 S4, S5; §4 Rollout; §5 observability; §8 third mark | Stderr from a hook exiting 0 goes to the debug log only; the line "they see" is seen by nobody, and `scratch-guard.js:37` says allow prints nothing. |
| H5 | high | TOOL-1 | §2 S4; §4 Alternatives; §8 first mark; against `unattended.sh:497` and `SKILL.template.md:332-347` | The unattended kit's second mode, `recipe`, commits before its kickoff too; S4's bytes deny it. |
| M1 | medium | TOOL-1 | §2 S7; §6 AC10, AC11 | "Its fixture repository" is never made; under mktemp AC11 cannot fail, in-repo it writes into the shared common dir KICK-1 moved out of. |
| M2 | medium | TOOL-1 | §2 S4; §4 step 4; §6 AC4, AC5, AC8 | The pathspec lists four shapes outside the rule; no criterion plants a key-bearing README outside `builds/` expecting exit 2. |
| M3 | medium | TOOL-1 | §6 AC4 Red-when | Staged and worktree bytes are identical in AC4's observation; a worktree read passes. |
| M4 | medium | TOOL-1, KICK-2 | TOOL-1 §6 AC10, §3 Edges; KICK-2 Files touched row 1; build order step 3 | AC10 runs `manifest-check.sh` live while KICK-2 edits it in the same parallel step; M6 clause 2, unenforced by the driver. |
| M5 | medium | TOOL-1, KICK-2 | TOOL-1 §2 S3, §6 AC3, §3 Edges; KICK-2 §2 S4, S5, §6 AC5, AC10 | The moved-session remedy needs a real-over-real append that KICK-2 defines nowhere and its cap may refuse. |
| M6 | medium | TOOL-1, KICK-2 | TOOL-1 §2 S2, S3; KICK-2 §2 S10; `SKILL.md` Step 0 | From the worktrees' parent, `<repo>` is the primary; the cell is rewritten to the primary while `git -C <worktree> commit` compares the worktree. |
| M7 | medium | TOOL-1 | §7 Gates; §4 Files touched; `tools/gate-legs.json:252-263` | The leg's guard is `tools/hooks/`, `tools/lib/`; the two class arms read `skills/session-kickoff/`. |
| M8 | medium | TOOL-1 | §4 Data model steps 4–6 and the paragraph beneath; §6 AC8 | The list runs step 4 before 5; the paragraph runs it "only when step 6 would deny"; AC8 pins no card state. |
| M9 | medium | TOOL-1 | §6 AC3, AC10; §4 Inventory `resolveCommonDir` | No arm resolves through `gitdir:` + `commondir`; a second `git init` satisfies "sibling worktree". |
| L1 | low | TOOL-1 | §2 S3; §5 observability; §6 AC2, AC3 | The card path in the deny is claimed twice and asserted by no criterion. |
| L2 | low | TOOL-1 | §2 S5; §6 AC7 | S5 says AC7 observes the `cwd`-absent fail-open; AC7 arms `session_id` only. |
| L3 | low | TOOL-1 | §2 S1; §6 AC9; §7 row 9 | The near-miss literal is `git log --grep push`; nothing fed carries `commit` outside the subcommand slot. |
| L4 | low | README | Build-level rules, bullet 3; against the Classification paragraph and TOOL-1 §8 | The rejected "cwd holds a build folder" reading stands as a rule in force. |

---

### B1 — blocker — TOOL-1 §4 Rollout and §2 S5 and §8 third mark; against TOOL-2 §4 Rollout and Files touched (order 4), KICK-1 §2 S5, KICK-3 §4 Migration — raw 4, 13, 35

**The defect.** Rev-3 closed round 2's B1 with step 5: "No card for this `session_id` under the
resolved common dir → allow, one stderr line". Rollout (lines 149–153) then says: "Sessions already
open when the wiring lands keep passing until their next compaction writes a card, and are then
denied until they kick off — which is the designed state, and the line they see names it." Read
against the siblings in build order: `TOOL-aReplayedCard-2` carries order 4, its Files touched lists
`.claude/settings.json` ("four matchers, two new entries, produced by the merger"), its S1 wires
`orientation-replay.fragment.json` with matcher `resume|compact` and args `--card --replay`, and its
Rollout reads "Live at the commit. The first SessionStart after it writes the card; the first
compaction replays." The hook command in `.claude/settings.json:18` is
`node "${CLAUDE_PROJECT_DIR}/tools/hooks/scratch-guard.js"`, the working tree's copy, and settings
edits are picked up by the file watcher without a restart (hooks-guide, "File edits are normally
picked up automatically"). `KICK-aReplayedCard-1` S5 (lines 78–80): "A missing card under replay
writes a fresh one by S3's derivation", and S3's derivation ends `READY — none yet`. So the landing
session — the one unattended run that carries this build from order 3 to the merge, in this
worktree, the only tree whose settings carry the new entries before the merge — gets a sentinel card
at its first compaction after order 4, or at a `--resume` where `startup|clear` writes one. TOOL-1
step 6 then denies its next main-loop `git commit`, and every one after it: the order-5 and order-6
pass commits, the folds, the closing pass. BUILD-METHOD M6 has the run commit on the main loop, and
the driver commits nothing itself.

The remedy the deny prints is `/session-kickoff`. `KICK-aReplayedCard-3` §4 Migration (lines 91–96)
states that on this node the installed skill "is a junction to the PRIMARY tree's
`skills/session-kickoff/`, on `main`, so a session on this branch invokes the OLD engine until the
landing merge fast-forwards the primary" — an engine with no append clause, because KICK-3 lands the
append at order 5 in this worktree and the junction still points at `main` afterwards. KICK-3's own
AC1 handles this for its fixture session by re-pointing the junction for the observation and
restoring it; no spec makes that a step of the landing run. A hand-fed READY-only body to
`--card --append` is `KICK-aReplayedCard-2` S3's `DEAD PROBE` (no path-shaped or id-shaped token).
The exits are S6's ceiling: a subagent's shell (`agent_id`, step 2), a script, a heredoc, a
hand-written card under the session id the deny's own stderr names. One sub-claim in raw 13 is wrong
and dropped: the landing merge is a `git merge`, which S1 excludes, so it is not itself refused —
but every commit between the compaction and that merge is.

**Why blocker, raised from the skeptic's high on 4 and 13.** Severity is what ships. What ships is a
Rollout sentence that is false for the one session that lands the build, a landing run that cannot
make a main-loop commit from a routine compaction to the end, and an exit that is the guard's named
evasion — the class round 1 called B4 and round 2 called B1. The fold moved the window from order 3
to the first compaction after order 4 and called the new window designed. S5's own sentence, "the
landing run itself between orders 3 and 5", names the window as though it closed at order 5; it
closes at the merge, and only because the merge is a `git merge`.

**Fix.** State the window and close it, in whichever of two places the owner prefers, and say which
in TOOL-1 §4 Rollout and §4 Migration in one sentence each. (a) TOOL-2 §4 Rollout: the four fragments
are applied to THIS worktree's `.claude/settings.json` in the landing pass after order 6 — its AC4
already reads "at the landing commit" and AC9 "after landing" — so no card is written into the
landing session before the merge; TOOL-2 gains a `hands-off` edge to TOOL-1 naming the reason, and
TOOL-2 AC9's forced compaction is pinned to a fixture session, never the landing one. (b) Keep the
order and record the bridge: from TOOL-2's landing until the merge, a landing session whose replay
wrote a sentinel card lands its READY line through the shipped verb with the full six-section body
(`bash skills/session-kickoff/manifest-check.sh --card --append --session <sid>`, base = HEAD), recorded
in the acceptance ledger as the one sanctioned hand-run; from order 5 the bridge is the re-pointed
junction KICK-3 §4 Migration already prescribes for its fixture, made a recorded run step. Under either,
TOOL-1 Rollout excepts the landing run from the "designed state" sentence rather than including it.

**Left-shift.** An arm in `scratch-guard.test.sh` that stages exactly the window's state — a card
written by `--card --replay` for a session with no prior card, in a fixture whose `.git` is a linked
worktree's file — and asserts the verdict Rollout claims for it, observed RED first. And a
`gen_build_index.py` join: a unit whose Files touched names `.claude/settings.json` and whose Rollout
says "live at the commit" at an order below the last unit reds unless a `hands-off` edge names the
session it will fire in. Two builds have now shipped a Rollout sentence that the next order falsifies.

---

### H1 — high — TOOL-1 §2 S2, §4 Data model (no step for an unresolved toplevel), §6 AC9 — raw 2, 14, 23, 37

**The defect.** S2: "The check resolves the commit's toplevel from the `-C` target when the command
carries one, relative to the payload's `cwd`, else from `cwd` itself, by walking up to the directory
holding `.git`". The six predicate steps state nothing for a walk that finds no `.git`. Reproduced on
this node: `path.resolve('C:/projects/coding-governance', '/c/projects/coding-governance')` is
`C:\c\projects\coding-governance`, which does not exist; `/c/x` resolves to `C:\c\x`, likewise; a
bash `mktemp -d` path such as `/tmp/tmp.abc/fixture` resolves to `C:\tmp\tmp.abc\fixture`, likewise.
The `/c/…` spelling is not an edge: the charter §11 and the engine's Step 0 (`SKILL.md:44-45`,
identical at HEAD and at `c4f02308`) prescribe "give `git -C` forward-slash paths (`/c/repo`)" on
every registered node, all of which are Windows plus Git-Bash, and the spec's own Alternatives
paragraph says the shell's `pwd` prints `/c/…`. `buildComparablePath` (`scratch-guard.js:63-69`)
folds `/c/` onto `c:/`, and its header (lines 55–62) records that the fold exists for exactly this
alphabet mismatch — but S2 applies it to the COMPARE, after the walk, and §3 forbids a second
normaliser.

AC9 then expects `git -C /c/x commit -m y` to exit 2 on a sentinel card. `/c/x` exists in no fixture,
and even a drive-folded `c:/x` does not, so the walk from it finds nothing under every reading; the
criterion can be met only by an unstated fallback to the payload `cwd`'s toplevel — which silently
discards the resolution the H3 fold added, so `git -C <sibling> commit` from the primary compares the
card against the PRIMARY and allows a commit landing in the sibling — or reds if the builder fails
open on no `.git`, under which the charter-prescribed spelling bypasses the deny silently on every
node and S6's ceiling does not name it. Every `-C` case in AC9 uses a sentinel card, so step 6 denies
whichever tree resolved and no criterion observes a `-C` resolution succeeding against a real tree.
The gap also decides every fixture arm: a suite that passes its `$TMP` path as the payload `cwd`
hands node an unwalkable start, and every arm's outcome is then the unstated rule, not the predicate.

**Why high.** Round 2's H3 was rated high because a routine spelling the header would say is covered
passed silently; the fold closed the token grammar and left the target's spelling and the
no-repository case undefined, which is the same defect one layer down.

**Fix.** S2: the `-C` value and the payload `cwd` pass through the `/x/` → `x:/` drive fold (the second
replace of `buildComparablePath`, without its lowercasing) BEFORE the walk; a numbered predicate step
between 3 and 4 states the outcome of a walk that finds no `.git` — allow with one stderr line naming
the start directory, or deny naming the target as a tree the hook cannot locate — and S6's ceiling
names whichever is chosen. AC9: replace the `/c/x` literal with the fixture's own toplevel in `/c/`
spelling from a payload `cwd` OUTSIDE the fixture, expecting 2 on a sentinel card and 0 with empty
stderr on a READY card whose cell is the fixture, so the fold and not the fallback is what the arm
observes; §7 row 9 gains it; and S7 pins that every fixture arm passes the payload `cwd` as `pwd -W`
output, never the MSYS path.

**Left-shift.** The resolution pair is the gate. Beyond it, a `check-spec-tokens.py` rule that a
criterion naming a literal path under `/[a-z]/` in a spec whose unit touches a `.js` file carries a
`fixture:` line — the spelling that a node walk cannot follow is the one this repository keeps
writing into its criteria.

---

### H2 — high — TOOL-1 §2 S1 and S6, §4 Inventory `extractCommitTarget`, §6 AC9 — raw 1, 15, 27

**The defect.** S1 tokenises "the command's string-blanked view" and skips "the one value token that
follows" each of seven flags. The Inventory has `extractCommitTarget` read "the `-C` value out of the
blanked view". Reproduced with the file's own exported `buildCommandView` (`scratch-guard.js:146-187`,
exported at `:394`): `git -C "C:/projects/coding-governance" commit -m x` views as
`git -C "                             " commit -m x`, whose whitespace-split tokens are `git`, `-C`,
`"`, `"`, `commit`, `-m`, `x`; `git -c "user.name=x" commit` and `git -C "$ROOT" commit` split the
same way. The quote characters survive blanking and the contents become spaces, so the one-value-token
skip lands on the first `"`, the next token is `"`, which is neither dash-prefixed nor `commit`, and
the grammar never reaches `commit`: `git -C "$ROOT" commit` — the only legal spelling for a path with
a space, and an agent's habitual quoting — allows silently. S6's ceiling (script, heredoc, non-git
tool, deleted or hand-written card) does not name the quoted form. The extracted target, read from
the view, is whitespace, after which the walk lands in H1's undefined case. The file's own header
(lines 21–26) states the opposite rule for exactly this reason — locate the operator in the view, read
the token from the ORIGINAL — and ships `readTokenAt` (`:190-210`) to do it; the Inventory departs
from it without a word. AC9's two value-flag DENY hits, `-C /c/x` and `-c a=b`, are both unquoted, so
the natural `\S+` implementation of `COMMIT_SHAPED` passes every criterion.

**Why high on all three, raised from the skeptic's medium on 15 and 27.** One defect, one grammar;
the two medium verdicts read the quoted value as a path-with-space edge and the extraction as a
one-phrase slip. It is a routine spelling the header would say is covered — round 2's H3 class, one
quote away — and the two halves are the same missing sentence.

**Fix.** S1: a quoted run in the view (`"` … `"` or `'` … `'`, the quotes survive blanking) counts as
ONE value token. Inventory row and S2: `extractCommitTarget` locates `-C` in the view and reads its
value from the original command at that offset via `readTokenAt`, the header's two-views rule. AC9:
add `git -C "<fixture toplevel>" commit -m y` and `git -c "a=b" commit` to the DENY set, and §7 row 9
reads "four value-flag hits".

**Left-shift.** The two quoted DENY literals are the gate. Beyond them, an arm that feeds every S1
grammar literal through `buildCommandView` first and asserts the token `commit` is still reachable
from the blanked bytes — the one check that would have shown this before any implementation.

---

### H3 — high — TOOL-1 §2 S7 (no assertion rule), §6 AC2 second clause, AC3 second clause, AC6, AC9, AC10, AC12 — raw 3, 24, 39

**The defect.** Since rev-3, exit 0 is the outcome of step 3 (missing field, silent), step 5 (absent
card, one line) and a real match (nothing printed). Every exit-0 criterion where a card is meant to
exist asserts the exit code alone: AC2's `READY — t · node a` clause, AC3's rewritten-cell clause,
AC6's `agent_id` payload, AC9's must-allow population of nineteen payloads, AC10's both clauses,
AC12's `ls` payload. Under S5 a card the hook cannot find — wrong common dir, an 8.3-contracted
fixture path, a card written under one `.git` and read under another, a session id spelled two ways —
also exits 0, with a line nobody reads. The existing `run()` helper (`scratch-guard.test.sh:33-50`)
compares exit only and discards `$TMP/err` on success; its own liveness comment at lines 39–42 records
why an ALLOW that can pass for the wrong reason is a failure, for the payload builder, and the fold
added eleven such ALLOWs without the same guard.
`memory/gotchas/fixture-passes-by-finding-nothing.md` is universal on this: assert the SPECIFIC message
the branch emits, never an exit code.

AC10 is the acute case. It exists to red on a cross-kit spelling fold, and it asserts exit 0 in both
clauses, names no resolution of the writer's card path, and has no deny twin on the same fixture.
"Feeds the resulting file … with `process.cwd()` as the payload `cwd`" in a bash self-test names no
process; under the existing helper node is launched from the test's cwd, never `cd`'d, so
`process.cwd()` is this worktree, the resolved common dir is the real shared `.git`, no card exists
there for the fixture session, and step 5 allows — the arm is green by absence and its Red-when ("the
compare is on bytes") can never fire. AC2 and AC3 have the same exit-0 shape but at least sit beside a
sentinel deny on the same card, which proves that card is found at that path; AC3's payload-from-B
clause and AC10's writer-written card resolve through a different path (a second tree, the writer's
own location), and those are precisely the arms the H4 and M1 folds rest on. S7 requires each arm
observed RED first, but a RED staged through the deny proves nothing about an allow arm on a different
card.

**Fix.** S7 states the arm rule once: an ALLOW with a present card asserts stderr byte-EMPTY; an ALLOW
on absence (AC1) or exemption (AC4, AC8) asserts its one line by text; the suite gains a `run` variant
that greps `$TMP/err`. Each criterion above gains "and stderr is empty". AC10 pins the payload `cwd`
to the fixture toplevel as node spells it (`node -p process.cwd()` run INSIDE the fixture) and gains a
twin: the same writer-written card with the sentinel left in place → exit 2 naming the sentinel,
proving the card at the writer's path was read before the `sed`'d copy is fed. One control arm: the
same payload with the card removed exits 0 WITH the one line, so the suite has observed that the
discriminator moves.

**Left-shift.** The empty-stderr assertion is the gate. Beyond it, a `check-arms.py`-style rule for
this suite: a `run` call whose expected exit is 0 and whose name does not carry `absent` or `exempt`
must go through the stderr-asserting variant — a structural pin on the class, so the next allow arm
cannot forget it.

---

### H4 — high — TOOL-1 §2 S4 and S5, §4 Rollout ("the line they see names it"), §5 observability, §8 third mark ("is told so") — raw 25, 36

**The defect.** The fold's compensating control for the absence-allow is ONE stderr line on exit 0,
in three places: S4's exemption line, S5's writer-did-not-run line, and §8's third mark, "a session
whose writer never ran is told so and allowed". The hooks reference the design record itself cites
(scratchpad `hooks.md:822`) says verbatim that stderr from a hook that exits 0 goes to the debug log
only, never the transcript, and Claude never sees it. Design-record verdict 7 (line 471) confirms
stderr reaching the model for the exit-2 DENY only; `aNumeralWarden`'s probes cover deny and
non-blocking codes, not exit 0; and the file's own protocol line (`scratch-guard.js:37-38`) reads
"Allow = print nothing, exit 0. Matches agent-cap.js", which rev-3 overrides without recording the
change. The self-test reads the line from a pipe, so AC1 and AC4 pass while production shows nothing.

Absence also has causes the mark does not name: KICK-1 S2's id refusal, S7's over-cap refusal, any
SessionStart failure, a harness that skipped the hook. Under every one of them the session is allowed
every commit for its life with nothing anyone sees, so "absence means the harness never ran the hook"
(§1, S5, §8) is one of several causes, and §7's "a skip must announce itself" is satisfied on paper
only.

**Why high, raised from the skeptic's medium on 25.** The absence-allow is the fold that closed the
blocker; its only compensating control is a channel the harness discards; and the spec claims the
channel in four places. A control that reaches nobody is not a control, and the mark that rests on it
is unfounded.

**Fix.** Lazy and honest: S4 and S5 say the line is a self-test and debug-log witness only; strike
"the line they see names it" and "is told so"; add writer-refusal absence to S6's ceiling; and record
in the header that the allow-with-a-line departs from the agent-cap parity note. If the owner wants
the session TOLD, the only model-visible channel on an allow is JSON stdout
`{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"allow","additionalContext":"…"}}`,
which is a protocol extension the header at line 37 must record and AC1/AC4 must observe on THAT
channel; probe it the way `aNumeralWarden` probed the payload — one throwaway hook, one commit, what
the transcript and the model saw — and stamp the verdict verified-(date, node) per §6.

**Left-shift.** None machine-gateable in this repository: the harness's channel is not under test
here. The documented check is the §6 stamp: an environment claim in a spec carries a verified-(date,
node) mark or a citation to a design-record verdict that covers THAT exit code.

---

### H5 — high — TOOL-1 §2 S4 (`authorized-by: prompt` bytes), §4 Alternatives "Keying the exemption", §8 first mark; against `tools/unattended/unattended.sh:497` and `tools/unattended/SKILL.template.md:332-347` — raw 40

**The defect.** S4 keys the exemption on the bytes `authorized-by: prompt`. The driver already
decided which modes author their own build folder before any kickoff can run, and the set has two
members: `SECOND_ANCHOR_MODES="prompt recipe"` at `unattended.sh:497`. The Skill's playbook path
(`SKILL.template.md:332-347`) has the run write the build folder with `authorized-by: recipe` (step
3), "Commit, then PUSH THE BRANCH" (step 4), preflight (step 5), then the kickoff hand-back (step 6) —
the identical commit-before-kickoff shape the prompt path has at its lines 260–287 and owner decision
3 exempted. Once wired, a recipe run's session starts with a sentinel card, its step-4 commit is its
first durable act, S4's bytes do not match, and the deny refuses it with a remedy the Skill forbids
before preflight ("after preflight, never before", line 185), which needs the pushed branch. The
second owner-facing entry point of the unattended kit is deadlocked by a deny written for the first.

**Why high.** A flow a sibling kit instructs, refused by construction, with no exit but the S6
evasions. The owner answered the question as it was asked, about the prompt path; the kit's own code
already treats both modes as one set.

**Fix.** S4 keys the exemption on the driver's closed set — the bytes `authorized-by: prompt` or
`authorized-by: recipe` — citing `SECOND_ANCHOR_MODES` as the owner of the set; AC4 and AC8 gain the
`recipe` bytes. Or, if the owner narrows it back to `prompt`, §8 records why and
`TOOL-aReplayedCard-3`'s Skill edit reorders the playbook run's kickoff ahead of its step-4 commit —
never silence.

**Left-shift.** A parity arm in the hooks self-test that reads the accepted bytes from the same
constant the driver reads (`SECOND_ANCHOR_MODES`, or a one-line file both ship), so a third mode
cannot be added to one reader and not the other — the single-source rule §7 names for every contract
duplicated across layers.

---

### M1 — medium — TOOL-1 §2 S7 ("in the fixture repository", `process.cwd()`), §6 AC10, AC11 ("the fixture's real common dir") — raw 33, 42

**The defect.** TOOL-1 never says how "the fixture repository" is made. KICK-1's Rollout says
"scratch clone of this repository, never in this worktree" and its AC11 lists "this repository's real
common dir" for exactly that reason; TOOL-1 says neither, and the existing `scratch-guard.test.sh` has
no git fixture at all — `TMP=$(mktemp -d); trap 'rm -rf "$TMP"' EXIT` at line 19 and nothing initialised
under it. Two readings, each breaking a different criterion. Under the hermetic one the conventions
mandate, AC11's "fixture's real common dir" is a mktemp `.git` the EXIT trap deletes: its Red-when ("a
fixture card is left behind for a sibling session … to trip on") is unreachable because nothing else
reads that `.git`, so the arm is an assertion about nothing and the round-2 B1 left-shift it claims to
fold — cards left in the REPOSITORY's common dir, which the suite never touches — is asserted nowhere.
Under the other reading, a worktree of this repository, the suite writes `t1`-style cards into the
shared common dir KICK-1 rev-3 just moved its own criteria out of, and a suite killed mid-run leaves
one for a sibling session to trip on. AC10's `process.cwd()` is the test's cwd under either reading
unless the spec says otherwise (H3).

**Why medium on both, raised from the skeptic's low on 33.** Raw 33 read AC11 alone as vacuous; raw 42
read the pair. The pair is one gap — the fixture is unlocated — and the vacuous arm is its symptom.

**Fix.** S7 states the fixture is a scratch repository under the suite's existing `mktemp -d`, made
by `git init` plus one commit and, for the arms that need one, `git worktree add` of it; every hook
payload's `cwd` is that fixture's path as node spells it; AC11 lists THIS repository's
`git rev-parse --git-common-dir` from the test's cwd exactly as KICK-1 AC11 does, with the suite's
session-id prefix (`sgtest-`) stated so the assertion is scoped to what the suite could have written.

**Left-shift.** AC11 rewritten IS the left-shift. Beyond it, none: a fixture-location sentence is a
read.

---

### M2 — medium — TOOL-1 §2 S4, §4 Data model step 4, §6 AC4, AC5, AC8 — raw 5, 30, 44

**The defect.** S4: "The rule is spelled once and the spawn derives from it: git runs at the resolved
toplevel with the pathspec `*builds/*/README.md`, which lists a top-level `builds/x/README.md` and a
nested `memory/builds/y/README.md` alike." Reproduced in a scratch repository at git 2.54, both for
`git ls-files --others --exclude-standard -- '*builds/*/README.md'` and for
`git diff --cached --name-only --diff-filter=A -- '*builds/*/README.md'` at the toplevel: the two
intended depths are listed, AND `rebuilds/z/README.md`, `mybuilds/q/README.md`, `builds/a/b/README.md`
and `memory/builds/y/reviews/README.md`, because a plain git pathspec `*` crosses `/` and the leading
`*` has no segment boundary. `:(glob)builds/*/README.md` plus `:(glob)**/builds/*/README.md` list the
two alone. S4's rule is "a `README.md` under a `builds/` path segment" as a build folder; the pathspec
is a second, wider spelling, so "spelled once" is false and the round-2 M12 fold verified hits and not
near-misses, exactly what §7 says to print. From a subdirectory cwd the same pathspec lists only that
subtree, relative to it, so the toplevel rule is correct as written.

No criterion observes the path half of the rule at all: AC4 and AC8 plant under `builds/` and expect
exit 0, AC5 is a HEAD-folder deny, and nothing plants a new key-bearing `docs/README.md` or
`rebuilds/z/README.md` expecting exit 2. An implementation exempting ANY new key-bearing README passes
the suite. Bounded by the `authorized-by:` bytes check, which is why the two low verdicts were low.

**Why medium on all three, raised from the skeptic's low on 30 and 44.** The over-match alone is low.
The over-match plus no criterion on the path half is the exemption's key unobserved — the same
green-by-absence class §7 names — and raw 5 supplies that half.

**Fix.** Keep the pathspec as the coarse filter and state one JS filter over the listed paths,
`/(^|\/)builds\/[^/]+\/README\.md$/`, as the rule the spawn derives from — or use the two `:(glob)`
pathspecs and say so. AC5 or a new arm: a NEW `docs/README.md` carrying `authorized-by: prompt` and a
new `rebuilds/z/README.md` carrying it, sentinel card → exit 2.

**Left-shift.** The two outside-`builds/` DENY arms are the gate.

---

### M3 — medium — TOOL-1 §6 AC4 Red-when — raw 6, 31

**The defect.** AC4 stages a new README carrying the key and expects exit 0 with the exemption line.
In that observation the staged blob and the worktree file are byte-identical, so an implementation
that reads the worktree copy passes AC4 exactly as one reading `git show :<path>` does; the Red-when
"the staged-added check reads the worktree file instead of the staged blob" has no producible failing
case, which contradicts S7's "each observed RED before it lands". A later partial stage or a worktree
edit after staging then behaves differently from the spec with a green suite.

**Why medium on both, raised from the skeptic's low on 31.** A Red-when the arm cannot produce is the
"assertion about nothing" shape §7 forbids for a new gate; it is one defect whether read as a missing
state (raw 6) or an unobservable clause (raw 31).

**Fix.** AC4 gains two states: stage the key-bearing README, then overwrite the worktree copy WITHOUT
the key → exit 0 with the exemption line; stage a key-less blob and leave a key-bearing worktree copy
→ exit 2.

**Left-shift.** The two states are the gate.

---

### M4 — medium — TOOL-1 §6 AC10 and §3 Edges; against KICK-2 §4 Files touched row 1 and build-order step 3 — raw 7

**The defect.** The M1 fold dropped the `--card --append` VERB from AC10 and left the FILE: the arm
executes `bash skills/session-kickoff/manifest-check.sh --card --write` as its acceptance input, and
`KICK-aReplayedCard-2`'s Files touched row 1 is that file — "three functions, the two verbs, the miss
annotation, the BASE refusal" — at the same order 3, rendered as one parallel step in the README's
build-order table. BUILD-METHOD `parallel-when-disjoint` clause 2 (`BUILD-METHOD.md:191-192`) forbids
exactly "writes a file the other reads … as an acceptance input", and the driver's dispatch comment
(`unattended.sh:4658-4659`) says clause 2 "is a judgement about meaning and is NOT enforced". A
half-edited `manifest-check.sh` in KICK-2's pass reds or greens TOOL-1's cross-kit arm for reasons
unrelated to the spelling fold. This is the third instance of the clause-2 class in one build.

**Fix.** AC10 runs the writer from the step's base blob —
`git show <step-3 base sha>:skills/session-kickoff/manifest-check.sh` extracted into the fixture —
and §3 Edges records the acceptance-input read as frozen at that blob; or KICK-2 takes its own step
after TOOL-1 (write sets are disjoint from TOOL-2's, but TOOL-2 AC9 also executes the writer live, so
a later slot needs the same check). Either way the spec says which.

**Left-shift.** The `gen_build_index.py` join round 2 asked for twice: a `consumes-from` edge, or a
criterion naming a path in a same-order sibling's Files touched, reds the order table.

---

### M5 — medium — TOOL-1 §2 S3, §6 AC3, §3 Edges `consumes-from KICK-aReplayedCard-2`; against KICK-2 §2 S4, S5, §6 AC5, AC10 — raw 8

**The defect.** S3's tree-mismatch remedy — "a kickoff run IN the tree the commit targets, because
`KICK-aReplayedCard-2`'s append rewrites the `tree —` cell" — exists for a session that ALREADY kicked
off in A (a real READY line, cell A) and then commits from B. That needs a SECOND real-READY append,
from B. KICK-2 S5 (lines 41–43) defines READY handling only as replacing the `READY — none yet`
sentinel "so the card holds exactly one READY line"; AC5's second-append clause is "a second append
WITHOUT one leaves it"; S10 and AC10 append onto a `--write` card that still holds the sentinel. The
real-over-real case is defined nowhere and observed nowhere in the sibling that owns it. KICK-2 S4
refuses any append past the shared 8192 B cap (KICK-1 S7), and a startup card plus two six-section
bodies is plausibly over it, in which case the moved session is refused for life — the round-2 H4
outcome the fold claimed to remove. TOOL-1 AC3's second clause rewrites the cell "the way the append
does" by hand, so it proves the compare and not the remedy.

**Fix.** S3 states the dependency and the `consumes-from KICK-aReplayedCard-2` edge names it: a
second real-READY append rewrites the cell, leaves ONE READY line, and is not refused under the cap for
a card already holding one body. KICK-2 S5 gains the real-over-real sentence and AC10 a clause: a card
holding a real READY line from A, appended again from B → one READY line, cell B. If the cap can refuse
it, KICK-2 S4 states the limit and TOOL-1's deny says so in its stderr.

**Left-shift.** KICK-2 AC10's new clause is the gate; the cap case is an arm on KICK-2 AC4 with a
card already carrying one body.

---

### M6 — medium — TOOL-1 §2 S2 and S3, §4 Rollout; against KICK-2 §2 S10 and `SKILL.md` Step 0 — raw 28

**The defect.** S2 compares the `-C` TARGET's toplevel; KICK-2 S10 rewrites the cell to "the toplevel
the append runs in", the PROCESS cwd. For a session whose shell cwd is the worktrees' parent — the
layout §16 names and round-2 H3 cited as the reason `git -C` is routine — verified:
`git -C C:/projects/coding-governance/.claude/worktrees rev-parse --show-toplevel` prints the PRIMARY,
because that directory sits inside the primary tree. `SKILL.md` Step 0's first bullet therefore
resolves `<repo>` to the primary from that cwd, the append records the primary in the cell, and every
`git -C <worktree> commit` compares the worktree: denied. S3's remedy, "a kickoff run IN the tree the
commit targets", followed literally from the same cwd re-runs Step 0 there and rewrites the cell to
the primary again — the H4 denied-for-life shape reopened by the H3 fold. Neither S3's stderr text nor
KICK-2 states that the shell must `cd` first.

**Fix.** The tree-mismatch deny prints the executable remedy, `cd <target-tree> && /session-kickoff`,
and S3 states that the cell follows the append's process cwd (KICK-2 S10), not the tree `<repo>`
resolved to. Alternatively compare the payload `cwd`'s toplevel and list `-C` under S6's ceiling — but
say which.

**Left-shift.** An AC3 clause with the payload `cwd` at the fixture's parent and a `-C` naming the
fixture, sentinel card, asserting the deny's stderr carries the `cd` form.

---

### M7 — medium — TOOL-1 §7 Gates and §4 Files touched; `tools/gate-legs.json:252-263`, `run-gates.sh:158,1230-1237` — raw 26

**The defect.** The `scratch-guard self-test` leg carries `guard: ["tools/hooks/", "tools/lib/"]`,
`chunk: selftests`, `subject: kit`. `run-gates.sh` holds it unless `GATE_SELFTESTS=1` and then still
applies the guard (lines 1230–1237); `changed()` at line 158 bypasses guards only under `GATE_FULL`.
The two rev-3 class arms read `skills/session-kickoff/SKILL.md` and run
`skills/session-kickoff/manifest-check.sh`, neither under the guard, and §4 Files touched does not add
`tools/gate-legs.json`. A commit that moves the sentinel bytes, the `tree —` spelling or the engine's
inline spans skips the leg that exists to red on it; the spelling drift lands green and is caught, if
at all, at a later unrelated full run.

**Fix.** Add `skills/session-kickoff/` to that leg's `guard`, list the manifest under Files touched,
and note in §7 that the manifest moved, which the push boundary treats as forcing a total run.

**Left-shift.** A run-gates canary rule: a self-test leg whose script reads a tracked path outside
its guard set reds — derivable from the test's own `grep -o` of repo-relative paths against the
guard list.

---

### M8 — medium — TOOL-1 §4 Data model predicate steps 4–6 and the paragraph beneath; §6 AC8 — raw 16, 43

**The defect.** §4 gives two evaluation orders. The numbered list is "in order, each step returning
ALLOW on its own condition", with step 4 (the new-README exemption, two git spawns) before step 5
(absent card). The paragraph beneath runs step 4's spawns "only after steps 1–3 passed and only when
step 6 would otherwise deny", which requires the card read first: 1, 2, 3, 5, 6, 4. They diverge
observably. A builder following the list spawns git twice on every commit that passes steps 1–3,
contradicting the paragraph's "the common case pays no spawn" and §5's "zero spawns on the common
path" — on every commit in a hooks-only adopter with no card. For an absent card plus a new authorized
README, the list prints the exemption line and the paragraph prints the absence line. AC4 pins its
card as the sentinel; AC8 pins no card state, so its expected exemption line depends on which reading
the implementer took.

**Why medium on both, raised from the skeptic's low on 43.** Two orders in one section is
`memory/gotchas/two-answers-to-one-question.md`, and one of the two answers costs two spawns on every
commit in an adopter.

**Fix.** Renumber: absent card before the exemption, and say once that the exemption is evaluated only
after the card is read and only when it would deny. AC8 states the card holds the sentinel, as AC4
does.

**Left-shift.** AC1's arm asserts zero git spawns (the shadowed-`git` counting KICK-2 AC1 already
uses), so a list-order implementation reds on the absent-card path.

---

### M9 — medium — TOOL-1 §6 AC3 and AC10; §4 Inventory `resolveCommonDir` — raw 29

**The defect.** Verified: this worktree's `.git` is a FILE, `gitdir: C:/projects/coding-governance/.git/worktrees/<name>`,
with `commondir` = `../..` — the path every session on the four registered nodes takes, since all
worktrees live under `.claude/worktrees/`. The Inventory names the `gitdir` + `commondir` branch, and
§10 reuses `agent-cap.js`'s walk (`:1512-1535`) for it. No criterion exercises that branch: AC3's
"sibling worktree" and "a payload from B" never pin `git worktree add` over a second `git init`
(which shares no common dir and exits 0 through step 5 — H3), and AC10 runs the writer in a plain
fixture whose `.git` is a directory. A defect in the file branch — a relative `gitdir`, CRLF in
`commondir`, a backslash-spelled `gitdir` line — denies or allows every real session while the suite
passes over directory-`.git` fixtures only.

**Fix.** AC3 pins that B is `git worktree add` of the fixture (one common dir), that the card is found
from B through the `.git` file with a sentinel deny observed there first, and AC10's second leg runs
the writer from that linked worktree so `git rev-parse --git-common-dir`'s absolute spelling and
node's walk are compared on one fixture.

**Left-shift.** The linked-worktree arm is the gate.

---

### L1 — low — TOOL-1 §2 S3, §5 observability, §6 AC2 and AC3 — raw 12

S3 says the deny's stderr names "the card path, the condition that failed, and the one remedy",
"Observed by AC2 and AC3"; §5 repeats "every deny prints the card path". AC2 asserts only "naming the
sentinel and `/session-kickoff`", AC3 only "naming both trees and a kickoff in the commit's tree".
The card path — the one string that lets a session find and inspect its card — is stated twice and
asserted nowhere, the same stated-but-unobserved shape round 1's M2 folded. **Fix:** AC2's sentinel
clause asserts stderr contains the resolved card path. **Left-shift:** that phrase is the gate.

### L2 — low — TOOL-1 §2 S5 against §6 AC7 — raw 21

S5 fails open silently on a missing `session_id` OR `cwd` and claims "Observed by AC1, AC7 and
AC11"; AC7 arms a missing `session_id` and a present-but-ignored `tool_use_id`, AC1 is the absent
card, AC11 the cleanup. The `cwd`-absent fail-open is asserted by no criterion — the exact "assumed
rather than asserted" shape AC7's own Red-when names and the L4 fold just removed for `tool_use_id`.
**Fix:** AC7 gains a payload lacking `cwd` with a sentinel card, expecting exit 0 and no output; §7
row 7 names it. **Left-shift:** the arm is the gate.

### L3 — low — TOOL-1 §2 S1 against §6 AC9 near-miss literals, §7 row 9 — raw 22

S1's motivating never-match is `git log --grep commit`, the one near-miss holding the token `commit`
outside the subcommand slot. AC9 feeds `git log --grep push` instead, which no grammar could match, and
its other three near-misses (`merge-base`, `merge --ff-only`, `push`) carry no `commit` either; the
eight engine spans, counted, hold none. A regressed grammar matching `commit` anywhere passes every
AC9 arm green. **Fix:** replace `git log --grep push` with `git log --grep commit` in AC9 and §7 row
9; optionally add `git commit-tree` as the dash-joined near-miss. **Left-shift:** the literal is the
gate.

### L4 — low — `memory/builds/aReplayedCard/README.md` Build-level rules bullet 3; against its Classification paragraph and TOOL-1 §8 first mark — raw 46

README lines 43–44 state, as a rule in force, "EXEMPTED by the deny when the cwd holds a build folder
declaring `authorized-by: prompt`" — the literal reading TOOL-1 §8 resolved AGAINST ("every commit in
this repository is exempt") and §3 lists as a non-goal. The Classification paragraph five bullets down
carries the shipped reading ("keys on a NEW build README, staged or untracked"). The overview a
resumed run or an adopter reads first restates the rejected rule; the fold updated one carrier and
not the other, `memory/gotchas/two-answers-to-one-question.md` verbatim. **Fix:** rewrite the bullet
to the delegated mark's wording — a commit that CREATES a build README carrying `authorized-by:
prompt`, staged-added or untracked — and point at TOOL-1 §8. **Left-shift:** none beyond a read; the
README is prose the hygiene gate grades for shape only.

---

## The cross-read on the four M2 axes

Where two specs disagree, both are named, because a fix to one that leaves the other is a fold.

- **Interface.** TOOL-1 S2's compare on the `-C` TARGET versus KICK-2 S10's cell on the PROCESS cwd
  (M6). TOOL-1 S3's second-append remedy versus KICK-2 S5's sentinel-only replacement and S4's cap
  (M5). TOOL-1 S4's `prompt` bytes versus the driver's `prompt recipe` set and the Skill's playbook
  path (H5). TOOL-1 S1's blanked-view grammar versus `scratch-guard.js`'s own two-views header (H2).
  TOOL-1 S5's exit-0 line versus the file's protocol line and the harness's channel (H4). TOOL-1 S2's
  walk versus the `/c/…` spelling KICK-1's Alternatives, the charter and the engine all name (H1).
- **Ordering.** TOOL-2's "live at the commit" wiring at order 4 versus TOOL-1's Rollout for a landing
  run that continues to order 6, with KICK-3's junction as the remedy (B1). TOOL-1 AC10's live
  execution of `manifest-check.sh` versus KICK-2's edit of it at the same order 3 (M4).
- **Scope.** TOOL-1 §4 Files touched omits `tools/gate-legs.json` while its two class arms read a path
  outside the leg's guard (M7). TOOL-1's fixture unlocated versus KICK-1's "scratch clone, never in
  this worktree" and its AC11 on the real common dir (M1). The README's third build-level rule versus
  TOOL-1 §8 (L4).
- **Acceptance.** Every present-card exit-0 criterion versus step 5's exit 0 (H3). AC9's `/c/x` DENY
  versus a walk that cannot find it (H1); AC9's unquoted value-flag hits versus the quoted routine
  form (H2). AC4's Red-when versus its byte-identical observation (M3). AC3's hand-rewritten cell
  versus KICK-2 AC10's shipped rewrite, and neither on a real-over-real append (M5). AC3's "sibling
  worktree" versus a second `git init` (M9). AC8's unpinned card state versus the predicate's two
  orders (M8). AC7 versus S5's `cwd` clause (L2). AC2/AC3 versus S3's card-path claim (L1).

**The rev-3 siblings.** `KICK-aReplayedCard-1` drew no finding on its own text; one fix above touches
its S5's interaction with TOOL-2's order (B1). `KICK-aReplayedCard-2` drew none on its own text; two
fixes land on it — the real-over-real append clause (M5) and the cell's cwd (M6). `KICK-aReplayedCard-3`
drew none; its §4 Migration is the reason B1's remedy cannot act. `TOOL-aReplayedCard-2` drew none on
its own text; the B1 fix's option (a) lands on its Rollout and AC9. `TOOL-aReplayedCard-3` drew none;
H5's alternative fix lands on its Skill edit. `TOOL-aReplayedCard-4` (rev-1) and
`TOOL-aReplayedCard-5` (rev-2) were not re-read this round: neither is a subject, neither was touched
by the fold, and round 2 recorded no finding on either.

**Prior art the fold re-invents or overlooks.** `readTokenAt` and the two-views header, in the file
the unit edits (H2). `buildComparablePath`'s drive fold, applied one step too late (H1). The suite's
own liveness guard on the payload builder, not extended to the eleven new ALLOW arms (H3).
`SECOND_ANCHOR_MODES`, the set the exemption's bytes should read (H5). `agent-cap.js`'s `gitCommonDir`
file branch, cited in §10 and exercised by no arm (M9). The shadowed-`git` spawn counter KICK-2 AC1
already uses, which would decide M8.

**Harness assumptions, and which were verified.** That the hook runs the working tree's
`scratch-guard.js` — `.claude/settings.json:18`, read (B1). That settings edits reach a running
session — the hooks guide's troubleshooting line, read from the scratchpad copy (B1). That stderr on
exit 0 is discarded — `hooks.md:822` in the same copy, read, and consistent with design-record verdict
7's scope (H4). That the common dir is shared and this worktree's `.git` is a file — read (M9, B1).
That the installed skill is a junction — KICK-3 §4 Migration's statement, verified by round 2 with
`Get-Item`, not re-measured here.

## What this pass did NOT do, said rather than implied

- **It read documents and the source they cite; it drove no hook and no self-test.** The synthesis
  re-read at source: `tools/hooks/scratch-guard.js:1-75,140-215,360-394`;
  `tools/hooks/scratch-guard.test.sh:1-60,236-240`; `tools/gate-legs.json:248-264`;
  `tools/run-gates/run-gates.sh:150-165,1226-1242`; `tools/hooks/agent-cap.js:1508-1535`;
  `tools/unattended/unattended.sh:494-500,4655-4662`; `tools/unattended/SKILL.template.md` (the
  grep'd step lines of the prompt and playbook paths); `memory/guides/BUILD-METHOD.md:188-195`;
  `.claude/settings.json:9-47`; `skills/session-kickoff/SKILL.md` Steps 0–4 at `c4f02308` and at
  HEAD (identical); the five rev-3 siblings' §2/§4/§6 and status headers; the build README; the
  design record's verdict table at line 471; the round-2 record whole. It reproduced on this node:
  the quoted-value token split through the file's own exported `buildCommandView` (H2); node's
  `path.resolve` of three MSYS spellings against this tree (H1); the pathspec's population, both
  spawns, toplevel and subdirectory, plus the `:(glob)` alternative, in a scratch repository (M2);
  the parent-cwd toplevel and this worktree's `.git` file and `commondir` (M6, M9). It counted the
  eight spans by `grep -o` over lines 33–194 and listed them above.
- **The hooks-reference claim rests on a fetched copy.** `hooks.md` and `hooks-guide.md` in this
  session's scratchpad were fetched by a lens; the synthesis read the cited lines and treated them
  as data. It did not run the exit-0 probe H4 asks for, so H4's fix names the probe rather than
  asserting the channel.
- **A spec audit grades what a document says.** B1 was found by reading four specs against the build
  order; H3 by reading the suite's own helper. The defect a correct-sounding sentence hides in an
  unbuilt mechanism is out of reach here, and the five siblings' zero is subject to it.
- **The design record was not re-audited.** Owner decisions 1–3 were read for what they say where a
  finding rests on them (H5 on the exemption's purpose, B1 on the absence rule's delegation); their
  reasoning was not re-opened.
- **Precision was 0.74, above §8's floor.** The priming change from round 2 — name the mechanism,
  reproduce where a one-liner can — is what moved it, and it should hold for round 4. Round 4 should
  re-audit TOOL-1 at its rev-4 blob with one lens on the landing window and the bridge it states, one
  on the `-C` resolution's new step and the quoted-token rule against the file's `buildCommandView`,
  one on the stderr-empty rule over every allow arm, and one on the recipe bytes against
  `SECOND_ANCHOR_MODES`; and it should re-read TOOL-2's Rollout and KICK-2's S5 for the fixes that
  land there.
