# TOOL-aBoundedCeiling-3 — a unit planned and dropped before any spec existed

**Status:** WONTDO · rev-1 · 2026-08-28 · node a · Tier-1 · base 965b1afb · streams tooling · order 4 · never built; this record exists so the id its own build's roster cites RESOLVES, and it reconstructs no scope

<!-- gen:spec-records -->

*No record names this unit.*

<!-- /gen:spec-records -->

## 1. Goal

Make `TOOL-aBoundedCeiling-3` a DEFINED id rather than a cited one.

This build minted four ids beyond the three it built. One of them, `-13`, carries `-5`'s remaining
scope and has its own row. The other three — this unit, `TOOL-aBoundedCeiling-2` and
`TOOL-aBoundedCeiling-4` — were planned and dropped before any spec was written, so the build closed
citing three ids that nothing defines. `README.md` §Build-level rules records the three REASONS:

> one needed an owner ruling on pre-push predicate 5, one had its premise refuted (the guard bypass
> is load-bearing for the green stamp), and one was refused because the two ceiling mechanisms
> differ on the behaviour they would share.

**Which reason belongs to which number was never written down, and this record does not guess.** The
three stubs are therefore interchangeable in content and differ only in the id each defines. That is
the honest shape: inventing a mapping would put a fact in the corpus that no one observed, which is
worse than the gap it would close.

## 8. Open questions

- **F1 — which of the three recorded reasons dropped THIS id.** RESOLVED as UNANSWERABLE (agent,
  2026-08-28): the mapping was not recorded at the time and cannot be recovered from `RUN.md`,
  `README.md`, `memory/DECISIONS.md` or the build's records, all of which were read. The three
  reasons are preserved above as a set. Nothing downstream depends on the mapping — a dropped unit
  has no product commit, no acceptance and no successor.
- **F2 — whether a stub is the right drain at all, against a waiver row.** RESOLVED (owner,
  2026-08-28): a stub. A waiver row would have moved `ORPHAN_ID_PIN` the wrong way on a shrink-only
  ratchet, and `TOOL-aThawedCorpus-5`'s own measurement found a second-order trap in it — the row
  comment naming the waived id makes that id RESOLVE, staling the row in the same breath it is
  written. A spec H1 is what `spec_ids` already reads as a definition, so this drain adds no
  mechanism.

## 9. Revision log

- rev-1 · 2026-08-28 · written while landing the `aThawedCorpus` merge, whose `RUN.md` cites this id
  in prose and so returns it to this build's generated roster, where hygiene check 14 reds on it.
  `aThawedCorpus` reached three options and refused all three in writing as an owner turn; the owner
  ruled for this one — the build adjudicates its own parked units.
