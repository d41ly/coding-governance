**Serves:** spec-audit TOOL-aProbedUnit-1 TOOL-aProbedUnit-2 TOOL-aProbedUnit-3 TOOL-aProbedUnit-4 TOOL-aProbedUnit-5 TOOL-aProbedUnit-6 TOOL-aProbedUnit-7

# Tier-2 spec audit — the `aProbedUnit` spec set, round 2

Written 2026-09-14 on node `a` by the unattended-build harness's spec-audit stage (`tools/workflows/unattended-build.js`): four primed finder lenses, an adversarial skeptic per finding in five batches, one synthesis pass, over the specs that folded round 1 (rev-2 throughout, spec 6 at rev-3). Subject: the seven SPECCED units of `memory/builds/aProbedUnit/`, read at the blobs named on the range line, which are the blobs at worktree HEAD `79174865`. Source claims below stand as the skeptic stage confirmed them, and this record re-derived the following against that tree before adjudicating: the seven blob hashes; `grep -c unattended.sh tools/unattended/VERBS.template.md` printing 0; `mkconf` at `tools/unattended/unattended.test.sh:108` taking five positionals; `# ---- REGION ONE` at `:445` and `slice_fn()` at `:3930`; the four `FLOOR_` lines at `:5403`, `:5434`, `:5458` and `:5461`; the driver comment blocks at `tools/unattended/unattended.sh:457-458` and `:3966-3969`, `review_exit_note` at `:4025-4026`, the `fail 37` at `:4107`, `grep -c 'blocker still standing'` printing 3 there, and the suite arm at `unattended.test.sh:4634`; `CHILD_ARGS` at `tools/workflows/unattended-build.test.sh:551` carrying `"ground":"G. "`, the inputs block at `unattended-build.template.js:135-146` with no `scratch` row, and that suite's counts of 2, 1 and 0 for `blockers were not disposed`, `UNREACHABLE HERE` and `REACHABLE HERE SINCE`; `memory/guides/REVIEW-PROTOCOL.md:228-234`; `env A=1 -u A sh -c 'echo ok'` exiting 127 under GNU coreutils 8.32; and the MSYS rewrite — `TEMP=/tmp TMP=/tmp TMPDIR= node -e 'console.log(require("os").tmpdir())'` printing `C:\Users\DAILY-~1\AppData\Local\Temp` both as a shell prefix and through `env`, while `process.env.TEMP="/tmp"` set inside `node -e` prints `/tmp`. Nothing else was independently re-measured here.

Reviewed subjects, pinned: `memory/builds/aProbedUnit/spec/2026-09-14-spec-TOOL-aProbedUnit-1.md@6bfdca720850a98f6e0d0244129f95060fa4ad2c` · `memory/builds/aProbedUnit/spec/2026-09-14-spec-TOOL-aProbedUnit-2.md@4861ea9194a4c4f2a014569bc457a25db96e8d43` · `memory/builds/aProbedUnit/spec/2026-09-14-spec-TOOL-aProbedUnit-3.md@062928746e5c44e663462826e390e17726efe0f6` · `memory/builds/aProbedUnit/spec/2026-09-14-spec-TOOL-aProbedUnit-4.md@c423a5a4a152dc422ba4affe51bbc9ff3d142a0e` · `memory/builds/aProbedUnit/spec/2026-09-14-spec-TOOL-aProbedUnit-5.md@4d9a985b763328a3683463e67832245be89f253d` · `memory/builds/aProbedUnit/spec/2026-09-14-spec-TOOL-aProbedUnit-6.md@c33af31b1774bb4500755ed460760d35876095fd` · `memory/builds/aProbedUnit/spec/2026-09-14-spec-TOOL-aProbedUnit-7.md@247efd71af13365e4fc6a818d3129d50f516cf3c` · ROUND 2.

## Verdict: CLEAN WITH FIXES

No finding this round needs a settlement above the spec that holds it. Round 1's BLOCKER class — the pass told to make an observation the build forbids — is closed: the README's rule five now carries the observation split, and every spec routes its leg and suite halves to `--close`. Three HIGH ids remain, and each is a criterion that reds against a correct build of its own spec, so the set is dispatchable only after they are fixed: spec 6's AC12 pins two suite floors at base values that spec 3, which lands first, moves (ids 1, 10); and spec 5's discriminating pair cannot pass on any registered node, because the MSYS runtime rewrites the `/tmp` the suite hands to `node` (id 24). The ten MEDIUM ids are mostly the round-1 fix applied unevenly — three specs give the harness suite's whole run a criterion and three do not; one fixture edit contradicts its own criterion; two carrier inventories miss the driver's own comment prose; one run-alone recipe names a helper the preamble does not define. Every fix below is local to the spec it names, and the token above means exactly that: nothing here asks the roster for a rule, only for edits.

**Review shape:** raw 41 · confirmed 18 · refuted 23 · unverified 0 · precision 0.44.

**Run integrity:** lenses 4/4 returned, 0 DIED; skeptic batches 5/5 returned, 0 DIED; 0 contradictory verdicts demoted to unverified, 0 spurious verdicts discarded, 0 duplicates. Every lens and every skeptic batch came back, so the finding set is complete for the lenses that ran, and a zero count below is positive evidence rather than the artefact of a dead lens. The pipeline's duplicate pass found none; the clustering below is this record's own, because ids 1 and 10, and ids 11 and 26, state one defect each from two lenses, and the fix is written once per defect. The per-id table keeps all 18 rows so the counts returned to the caller agree with what was adjudicated here.

**Adjudicated severities:** BLOCKER 0 · HIGH 3 · MEDIUM 10 · LOW 5. Twelve clusters. Cluster A's two ids are one defect; cluster B's three ids are three defects in one mechanism, of which the LOW one is resolved by the HIGH one's fix; cluster C's two ids are one defect; cluster D's three ids are one class in three specs, fixed once per spec. The pipeline's own severities were kept for every id; none was raised or lowered here, and the reasons are given per cluster.

Severity meaning in this record, unchanged from round 1 so the counts compare: BLOCKER — the set cannot be dispatched as written, a contradiction between the build's own rule and its specs that the roster must settle once, by one rule, before any pass starts. HIGH — a criterion cannot be observed, or reds, under a correct build of its own spec, or an owner ruling is reversed without the owner; fixable inside that spec. MEDIUM — a claim is false against source or a scope item has no observer, so a reader acting on it does the wrong thing, but a build can still land green. LOW — a figure, a carrier or a gate-list row does not reproduce; no verdict changes.

---

## Per-id severity table

| id | severity | unit | address | cluster |
|----|----------|------|---------|---------|
| 1 | HIGH | 6 | §6 AC12, against spec 3 §6 AC9 | A |
| 10 | HIGH | 6 | §6 AC12 figure line, against spec 3 §6 AC9 | A |
| 24 | HIGH | 5 | §4 The self-test · §6 AC7 discriminating pair · §2 S2 | B |
| 12 | MEDIUM | 5 | §4 The self-test · §6 AC2 · §6 AC5 | B |
| 17 | LOW | 5 | §4 The self-test, against §6 AC7 fixture line | B |
| 11 | MEDIUM | 4 | §2 S6 · §4 Files touched, against §4 The child and §6 AC5 | C |
| 26 | MEDIUM | 4 | §2 S6 · §4 Files touched `CHILD_ARGS` row · §6 AC5 | C |
| 4 | MEDIUM | 4 | §2 S6 second half · §6 · §7 | D |
| 5 | MEDIUM | 7 | §2 S8 · §6 · §4 The suite · §5 | D |
| 3 | MEDIUM | 1 | §2 S5 · §6 AC6 fixture line · §7 | D |
| 2 | MEDIUM | 3 | §6 AC6, against §4 The carriers | E |
| 27 | MEDIUM | 6 | §4 The suite, "How one arm is run alone" · §6 AC3 AC12 | F |
| 28 | MEDIUM | 7 | §4 The driver's own words · §2 S7 · §6 AC9 | G |
| 33 | MEDIUM | 6 | §4 The verb · §4 Files touched | H |
| 7 | LOW | 3 | §2 S6 · §4 The carriers · §6 AC6 | I |
| 8 | LOW | 4 | §4 The parent, inputs-block paragraph · §6 AC6 · §2 S1 | J |
| 15 | LOW | 3 | §4 The carriers · §6 AC1 AC4, against spec 6 §2 S9 and §4 The suite | K |
| 37 | LOW | 1 | §4 "Four carriers, one rule, and which text binds", first paragraph | L |

---

## HIGH

### A — `TOOL-aProbedUnit-6` §6 AC12 against `TOOL-aProbedUnit-3` §6 AC9 (ids 1, 10)

**Finding.** Spec 6 AC12 pins `FLOOR_ASSERTIONS` at `tools/unattended/unattended.test.sh:5434` and `FLOOR_SHARD_2` at `:5461` as standing "exactly the added assertion count above their base values of 706 and 510", and its figure line says the two floors are "PINNED to base plus that count" — an absolute pin, not a delta. Spec 3 is order 3 and lands first; its AC9 raises the same two constants by its own arms' executed assertions, and its Red-when already names the collision: unit 6's "exactly the added arms above 706" would measure "against a base already carrying these". At a correct build of both units the constants read 706 + k3 + k6 and 510 + k3 + k6. A builder who keeps spec 3's floor fails AC12's literal; a builder who meets AC12's literal LOWERS a shrink-only floor to 706 + k6, below the value spec 3 landed, so a stranded check-51 arm is no longer visible, which is the property both criteria exist to hold. Rev-2 cluster D fixed exactly this class for AC11 — unit 1 edits `BUILD-METHOD.md` ahead of unit 6, so AC11 measures its 13-byte delta against the parent commit — and left AC12 on the build base one criterion down.

**Why HIGH and not BLOCKER.** Two specs pin one constant, but only one of them is wrong, the pattern that fixes it is already in the same spec, and no rule above the spec is missing: the README's figure discipline (DERIVED at observation, PINNED from a dated measurement) is followed by both; spec 6 pinned against the wrong commit. One edit, one spec.

**Fix.** Rewrite AC12's floor half on AC11's pattern: read both constants from the pass's parent commit — `git show HEAD~1:tools/unattended/unattended.test.sh | grep '^FLOOR_'` — and assert HEAD's values stand exactly this pass's added assertion count above THOSE. Keep 706 and 510 in the figure line as base-of-build figures that spec 3 moves first, never as the oracle. The rest of AC12 (the per-arm runs, the `NO scheduled run grades FLOOR_SHARD_2` skip announcement) stands.

**Left-shift.** A join in `tools/check-spec-tokens.py` across one build's specs: a section-6 line that pins a `FLOOR_[A-Z0-9_]+` constant "above its base value" REDS when a spec of lower `order` in the same build names the same constant in its own section 6, with the remedy "measure against the parent commit, the way AC11 does". Stage the red on spec 6 as written; spec 3 passes because nothing ordered below it names the constants.

### B — `TOOL-aProbedUnit-5` §4 The self-test, §6 AC2 AC5 AC7, §2 S2 (id 24 HIGH; id 12 MEDIUM; id 17 LOW)

**Finding, id 24.** Section 4 and AC7 rest on one claim: that `TEMP=/tmp TMP=/tmp` handed to `node` through `run()`'s fifth argument makes `os.tmpdir()` derive `/tmp` "on Windows too", so `echo x > /tmp/claude/x` is allowed under the new `<os.tmpdir()>/claude` root and `echo x > /tmp/other` is denied by rule 2. Measured on node `a` (Git-Bash, MINGW64, 2026-09-14) and re-measured for this record: both the shell-prefix form and `env TEMP=/tmp TMP=/tmp TMPDIR= node -e 'console.log(require("os").tmpdir())'` print `C:\Users\DAILY-~1\AppData\Local\Temp`. The MSYS runtime rewrites a POSIX-shaped `TEMP`/`TMP` value for a native `node.exe`, and the skeptic stage reports that `MSYS2_ENV_CONV_EXCL=TEMP:TMP`, `MSYS2_ENV_CONV_EXCL='*'` and `MSYS_NO_PATHCONV=1` exported first do not stop it. So the new root becomes `<real TEMP>/claude`, `/tmp/claude/x` sits under no root, rule 2 denies it, and the allow half of the discriminating pair reds at a CORRECT tip on every registered node — all four rows of `AGENTS.md` §2 are Windows. The deny half passes for the plain reason and proves nothing about S2's variable clause. AC7 is therefore unreachable as written, AC10's `PASS` with it, and the rev-2 cluster-G claim that rule 2 has an observable failing case on a Windows node is false against the host.

**Finding, id 12.** Section 4 defines the fifth argument as "words handed to `env` between the fixed assignments and `node`, such as `-u TEMP`", and AC2 and AC5 hand `-u TEMP` and `-u TMP` there. GNU `env` stops option parsing at the first `NAME=VALUE`: `env A=1 -u A sh -c 'echo ok'` prints `env: '-u': No such file or directory` and exits 127 on this host (coreutils 8.32). Placing `-u` before the assignments cannot work either, because the fixed `TEMP=`/`TMP=` assignments that follow re-set the variable; and the base `run()` at `tools/hooks/scratch-guard.test.sh:44-45` uses shell-prefix assignments, where an expanded `$5` is the command word and never an assignment. AC2's braced-unset denial and AC5's same-command-assignment control cannot be run as written, so rule 1's `TEMP`-unset branch has no observed failing case.

**Finding, id 17.** Section 4 says the derived `<os.tmpdir()>/claude` arm reads `os.tmpdir()` "under the same fixture environment" and that under `run()`'s fixed `TEMP` it "resolves to the fixture TEMP on every host" — then says it proves the root only where the derived tmpdir is `/tmp`, "a POSIX host with the three variables unset", which cannot occur under the fixed `TEMP`. AC7's fixture line resolves it the other way: derive "with all three temp variables unset", which on node `a` is `C:\Windows\temp`. Two environments and two targets for one arm; rev-2's cluster-G fold updated AC7 and left section 4 stale.

**Why these severities.** Id 24 is HIGH because a criterion reds under a correct build on every node that could run it. Id 12 is MEDIUM because the mechanism is wrong against source but the arms it carries are re-spellable without changing what they prove. Id 17 is LOW because the fix for id 24 settles the environment question and only section 4's prose moves.

**Fix.** Hand the environment to the hook from INSIDE node, where MSYS cannot reach it: `tools/hooks/scratch-guard.js:394` already exports `checkCommand` and `resolveAllowedRoots`, so the discriminating pair's arm runs `node -e 'process.env.TEMP="/tmp";process.env.TMP="/tmp";delete process.env.TMPDIR;const g=require(HOOK);process.exit(g.checkCommand(CMD,process.env).bad.length?2:0)'` — measured for this record, `os.tmpdir()` then prints `/tmp`. Rewrite section 4's environment paragraph around that form, state the MSYS measurement there so the next reader does not re-derive it, and re-observe both halves on node `a` before the arm is wired. For the unset cases (id 12), drop `-u` and spell every fifth-argument override as an in-node assignment or deletion in the same form — `delete process.env.TEMP`, `delete process.env.TMP` — since the suite's `run()` cannot pass a bare word through as an assignment; if `run()` keeps a fifth argument at all, define it as JavaScript prepended to the `node -e` body, not as `env` words. For id 17, pick the emptied environment (the one AC7 prices) and say so in section 4, spelled with the same in-node deletions. If the roster prefers not to touch `run()`'s shape, the alternative is to drop the Windows-observability claim from S2 and section 4 and label the pair POSIX-only, which leaves rule 2 with no failing case on any registered node and must then be said in the spec's own words.

**Left-shift.** Two. First, the liveness assertion §7 requires of any probe: the discriminating arm asserts, before grading, that `os.tmpdir()` under the environment it built equals the value it handed — `same "derived tmpdir is /tmp" "$(...)" "/tmp"` — so a rewrite by the runtime prints a FAIL naming the derived path rather than a deny for the wrong reason. Stage the red by running the arm through the shell-prefix form as written. Second, the pre-dispatch oracle run round 1's cluster H proposed: every backticked command a spec's section 6 hands to a shell is executed once at base, and `env … -u X` after an assignment exits 127 there before a child meets it.

---

## MEDIUM

### C — `TOOL-aProbedUnit-4` §2 S6 and §4 Files touched, against its own §4 The child and §6 AC5 (ids 11, 26)

**Finding.** S6 and the Files-touched `CHILD_ARGS` row say the fixture at `tools/workflows/unattended-build.test.sh:551` "gains a `scratch`" and nothing else. That fixture carries `"ground":"G. "`, and `childU`/`childA` at `:552-553` are computed from it and fed to every `F2` child-prompt `has` arm. Section 4's child join, `String(cfg.ground).indexOf(cfg.scratch) === -1`, throws `names no` when the ground does not name the scratch path, and AC5's second case states that `"scratch":"/tmp/s"` added with `"ground":"G. "` unchanged is exactly that THROW. So the fixture edit the spec prescribes is, by the spec's own criterion, the throwing shape, and only AC5's third hand-composed case — `"ground":"G. goes under /tmp/s. "` — spells what `CHILD_ARGS` itself would need. The two inline child fixtures at `:571` and `:573` are not named either and now throw on `scratch` before naming the misspelt mode they exist to test. A literal build reds every `F2` arm and the two `has` arms units 1 and 2 add, and reds them only at the close's whole-suite run — the pass's single arms paste their own fixtures and never see it.

**Fix.** S6 and the `CHILD_ARGS` row: the fixture gains BOTH `"scratch":"/tmp/s"` and a `ground` that names it, `"G. goes under /tmp/s. "`, because the child refuses a pair that disagrees, not a pair that is merely incomplete; say the same for the two inline child fixtures at `:571` and `:573` and for any other fixture that reaches the child.

**Left-shift.** Cluster D's criterion below — the harness suite whole at the close, no `FAIL` line — is the run that sees this. Nothing cheaper exists: the defect is a fixture the pass never loads.

### D — the harness suite's whole run has no ledger row: `TOOL-aProbedUnit-4` §2 S6 second half and §7 (id 4); `TOOL-aProbedUnit-7` §2 S8, §4 The suite, §5 (id 5); `TOOL-aProbedUnit-1` §2 S5 and §6 AC6 (id 3)

**Finding.** Round 1's cluster-A fix split every criterion into the pass's grep or single arm and the close's leg or suite half, and the README now carries it as rule five. Specs 3, 5 and 6 applied it to their own suites with a criterion each — AC9, AC10, AC12 — whose ledger row reads `observed at --close`. Specs 1, 4 and 7, which all edit `tools/workflows/unattended-build.test.sh`, did not. Spec 4 S6's second half (nine `*_UNITS` fixtures, `CHILD_ARGS` and the two inline fixtures gain `scratch`; the arms land in the file) is observed by no numbered criterion: AC1–AC5 run single arms with pasted fixtures and AC6 is a grep over five non-suite files; section 7 alone says the close's whole-suite run "is what sees every fixture this unit's required argument reds". Spec 7 S8 says "Observed by AC1 through AC6", but AC1–AC5 observe single-arm runs with `review_out` redefined in the shell, and AC6's greps run over the template and the render only; the 18 fixture substitutions, the `review_out`/`returns`/`DISPOSE_OK` moves and the three moved literals at suite lines 376, 471 and 590 have no grep, while §5 lists the whole run as hands-off and §7 prices it at 237 arms and 335 s. Spec 1 S5's arm is "observed by AC6", but AC6's fixture line says to source `run_wf` and `CHILD_ARGS` or copy the `node -e` body, never the suite whole, and no grep over the suite asserts the arm exists — "the new `has` arm reads `ok`" is satisfied by pasting the `has` line into the shell. `memory/HYGIENE.md`'s ledger is one row per numbered criterion, so the run that catches a fixture left without `scratch`, a suite still quoting `blockers were not disposed`, or a landed child with the paragraph and no arm, has no row to be marked `observed at --close` in, and the close can skip it with nothing red. Spec 2's AC2 received exactly the missing file grep in rev-2 (cluster M, id 3 of round 1); its three siblings did not.

**Fix.** One criterion per spec, on spec 3 AC9's shape. Spec 4, a new AC: at the landed tip `bash tools/workflows/unattended-build.test.sh` redirected to a file prints no `FAIL` line and its `--- <n> arms` line reads 237 plus the arms added — observed at `--close`; pass half: `grep -c scratch tools/workflows/unattended-build.test.sh` at or above the fixture count (13 at base per Files touched, 0 at base for the key itself) and one grep per new arm's label, each 0 at base. Spec 7, a new AC: `grep -c 'blockers were not disposed' tools/workflows/unattended-build.test.sh` prints 0 (base 2), `grep -c 'UNREACHABLE HERE'` over it prints 0 (base 1), `grep -c 'REACHABLE HERE SINCE'` prints 1 (base 0); leg half at `--close`: the whole suite redirected to a file, no `FAIL` line, the arms line at 237 plus the additions. Spec 1, added to AC6: `grep -cF "NO GATE, SUITE OR BAR RUNS INSIDE THIS PASS" tools/workflows/unattended-build.test.sh` prints 1 at the landed tip and 0 at base — the S5 arm existing at all; red when 0 at the tip or 2, which is the phrase duplicated into a comment. Spec 7 §5's hands-off bullet then names the criterion that owns the run rather than saying nothing runs it.

**Left-shift.** A branch of hygiene check 23: a spec whose Files-touched section names a `*.test.sh` that no `tools/gate-legs.json` row carries must hold a section-6 line naming that file whole with `observed at --close`, or REDS with the remedy "give the suite a row". Stage the red on spec 4 as written; specs 3, 5 and 6 pass by construction.

### E — `TOOL-aProbedUnit-3` §6 AC6, against its own §4 The carriers (id 2)

**Finding.** AC6 says `grep -c 'unattended.sh --audit'` over `tools/unattended/SKILL.template.md` is at least 2, "while `grep -c` of the same over `tools/unattended/VERBS.template.md` finds the verb's bullet". The carriers table pins that bullet in the file's own form, `- `--audit` — …`, after `--status`, and VERBS bullets carry no `unattended.sh` at all: `grep -c unattended.sh tools/unattended/VERBS.template.md` prints 0 at base, and the `--status` bullet at `:72` is the shape. Check 26 of `tools/unattended/check-unattended.sh` joins VERBS by exactly the `- `<verb>` — ` shape and requires `unattended.sh <verb> ` only in the SKILL. As written the VERBS half of AC6 prints 0 against a correctly spelled bullet, so either the criterion reds a correct tip or the builder spells the bullet to satisfy the grep rather than the carrier's convention, and nobody can tell which from the spec.

**Fix.** Name the grep that matches the bullet form: `grep -c '^- \`--audit\`' tools/unattended/VERBS.template.md` prints 1 at the tip and 0 at base; leave the `unattended.sh --audit` grep to the Skill's two invocation lines only.

**Left-shift.** The pre-dispatch oracle run: a grep a criterion pins at a non-zero tip count is run at base over the carrier its own section 4 spells, and a pattern that cannot match the pinned form is refused before a child meets it. Cheaper still, and already on the bar: check 26 at the close reds a VERBS bullet spelled to the grep instead of to the carrier, so the criterion's own oracle is the one that disagrees with the gate.

### F — `TOOL-aProbedUnit-6` §4 The suite, "How one arm is run alone", and §6 AC3 AC12 (id 27)

**Finding.** The run-alone form is the preamble sourced — `sed -n '1,/^# ---- REGION ONE/p' unattended.test.sh`, lines 1–445 — then `bcsetup`, then `slice_fn review_state`, then the arm's own `same` or `hit` line. The only definition of `slice_fn` is at `:3930`, inside region two; lines 1–445 define `HERE`, `SCRIPT`, `hit`, `same`, `miss`, `run`, `fixture`, `bcsetup` and `bcopen` and not `slice_fn`, so after sourcing the preamble `slice_fn review_state` is `command not found`. The spec's "defines every helper" is false against the tree, and the sliced `review_state` arms AC3 and AC12 name cannot be run alone by the form given, so the pass's one check for the predicate does not exist as written. Spec 7 AC9 uses the preamble, `bcsetup` and `bcopen` without `slice_fn` and does not inherit the break.

**Fix.** Add to the paragraph that the form also sources the helper's definition before use — `eval "$(sed -n '/^slice_fn()/,/^}/p' tools/unattended/unattended.test.sh)"` — or cite `:3930` so the builder pastes it; the `hit`/`same`/`miss` helpers at `:69-71` are in the preamble and need nothing.

**Left-shift.** The pre-dispatch oracle run again, with one more step: for a spec whose section 4 gives a run-alone recipe, source the recipe's preamble in a scratch shell and `type` every helper the recipe then names; a `not found` reds the spec before dispatch. Stage it on spec 6 as written.

### G — `TOOL-aProbedUnit-7` §4 The driver's own words, §2 S7 and §6 AC9 (id 28)

**Finding.** The carrier inventory assumes the driver's only by-nature disposal prose besides the `fail 37` at `unattended.sh:4096` is check 2's field sentence in `check-unattended.sh:537`. Verified at base, `grep -c 'blocker still standing' tools/unattended/unattended.sh` prints 3, at `:3969`, `:4025` and `:4026`. `review_exit_note` at `:4025-4026` prints "every blocker still standing was FOLDED into the specs it belongs to" or "is PROMOTED to a unit" on every terminal echo — and, per spec 6 §4, on the new `BOUNDED` echo directly after "every standing blocker is disposed by severity", which is one line carrying two answers. The requires-disposition `fail 37` at `:4107` says "the method admits BOTH fold and promote at the exit", its comment at `:4101-4103` says "M4 admits BOTH", and spec 6 keeps that body verbatim; AC9's grep for the literal `fold or promote` does not match "BOTH fold and promote", and AC6's retired-phrase grep runs over the harness files only. After the M4 swap a `--disposition fold` at a `BOUNDED` or `NON-CONVERGENT` exit makes the driver assert that standing blockers were folded, which M4 then forbids, and the requires-disposition refusal cites a rule M4 no longer states — the two-answers class S7 exists to close, left in the one carrier every recorded round prints. The suite arm at `unattended.test.sh:4634`, which quotes "because the method admits BOTH fold", is said by spec 6 to stand unchanged and moves with the clause.

**Fix.** Add `review_exit_note`'s two sentences and the `:4107` clause with its comment to S7 with severity-rule wording; extend AC9's zero-count grep to `blocker still standing` and `admits BOTH` over the driver (base 3 and 1); and record that the arm at `:4634` moves with the clause, so spec 6's "stands unchanged" is corrected in the same fold.

**Left-shift.** A retired-phrase list this build owns, shrink-only, joined against the driver, its suite, the harness and the method by one check: the same shape as AC6's greps, held in one file so the next carrier a spec forgets is caught by the list rather than by a reviewer. Stage the red with the driver at base.

### H — `TOOL-aProbedUnit-6` §4 The verb and §4 Files touched (id 33)

**Finding.** The driver carries the withdrawn-cap ruling (`TOOL-aBoundedVerdict-11`: "a count does not give a loop an exit; it moves the stall earlier") in two comment blocks the spec does not inventory: `WHY A PREDICATE AND NOT A COUNT` at `tools/unattended/unattended.sh:3966-3969`, directly above `review_state`, and the `RUNAWAY_CEILING` header at `:457-458`, "bounded by a CONVERGENCE PREDICATE, not by a count". Section 4 rewrites only the "four states" line at `:3971`. After the pass `review_state` exits `BOUNDED` on a count for spec subjects while the paragraph above it says a count is the wrong variable. The spec's own §10 finds the identical sentence in the Skill's opening paragraph, calls it "superseded by the 2026-09-14 ruling" and rewrites it "so the two answers agree", and leaves the same sentence uninventoried in the driver, where no parity or method-carriers leg can see a comment.

**Fix.** Add both blocks to the verb edits and Files touched, rewritten to: a SPEC subject is bounded by `REVIEW_ROUNDS` and exits in a disposition, the diff review by convergence, the ceiling backstops both. Add a retired-phrase grep to AC7 or AC12: `grep -c 'moves the stall earlier' tools/unattended/unattended.sh` prints 0 at the tip and 1 at base.

**Left-shift.** Cluster G's retired-phrase list covers it once the phrase is on the list; the gap it closes is that comment prose is invisible to every parity leg by design, so the list is the only reader a comment gets.

---

## LOW

### I — `TOOL-aProbedUnit-3` §2 S6, §4 The carriers and §6 AC6 (id 7)

**Finding.** S6 says the Skill's keepalive section names the prompt AND "what the main session does with each verdict", observed by AC6; the carriers table pins the `STALLED` remedy (stop the unit's task, record a `--park` or a brief note, re-dispatch with a brief naming the stalled command as skipped) and the `PROGRESSING` no-op. AC6 observes only `grep -c 'unattended.sh --audit'` at least 2 plus `adopt-unattended.sh --check`, which is render parity. Neither can see the verdict prose, so a Skill naming the prompt and saying nothing about verdicts passes, leaving the keepalive with a probe and no remedy — the half of S6 the goal statement exists for. Spec 6 AC10 got a content grep for this Skill-prose class in rev-2 (cluster H).

**Fix.** Add to AC6 a grep for one pinned phrase of the `STALLED` remedy in the keepalive section of `tools/unattended/SKILL.template.md` — `re-dispatch` or `stops the unit's task` — printing 1 at the tip and 0 at base.

**Left-shift.** None beyond the criterion; prose content is the class §10 keeps as a documented check, and the criterion is the check.

### J — `TOOL-aProbedUnit-4` §4 The parent, the inputs-block paragraph, and §6 AC6 (id 8)

**Finding.** Rev-2 (round 1 cluster S, id 44) added the `scratch: "<absolute session scratchpad>", // REQUIRED` row to the inputs block at `tools/workflows/unattended-build.template.js:135-146`, in section 4's parent paragraph and in Files touched, calling its absence "the missing-carrier class". No section-2 item names it and no criterion observes it: AC6's `grep -c scratch` at least 1 over the template is satisfied by the refusal line alone (the base count is 0, and the refusal makes it 1). The carrier the fold was added to fix can be omitted with every AC green.

**Fix.** Add to AC6: `grep -cF 'scratch: "<absolute session scratchpad>"' tools/workflows/unattended-build.template.js` and the same over the render each print 1, 0 at base; name the row in S1.

**Left-shift.** Round 1 cluster S's arm, still unbuilt: the harness suite asserts every key the refusal names appears in the header's inputs block, a grep over the file's own first 150 lines, staged red by the template as written.

### K — `TOOL-aProbedUnit-3` §4 The carriers and §6 AC1 AC4, against `TOOL-aProbedUnit-6` §2 S9 and §4 The suite (id 15)

**Finding.** Spec 3 AC1 needs a fixture conf carrying `UNIT_STALL_BOUND=60` and AC4 one omitting it, and nowhere says how: `mkconf` at `unattended.test.sh:108` has five positionals, `${1..5}`, and is unmentioned. Leaving it alone is not neutral, because `reset_tree` calls `mkconf`, `run()` merges stderr, and the new reader's NOTE line then precedes every verb's output, which reds any arm comparing a whole line by `same` — the arm at `:1872` reads `run --status tRun | sed 's/.*· next //'` against a single unit id. So spec 3 must declare the key in `mkconf`, and the file's `${4-3600}`/`${5-}` convention makes that a sixth positional — the slot spec 6 S9 and §4 claim as `REVIEW_ROUNDS="${6-8}"` on the assumption `mkconf` still has five. One fixture slot, two passes, neither aware of the other; whichever lands second either breaks the other's positional order or leaves every fixture announcing the default on stderr.

**Why LOW.** A builder of unit 6 who reads the tree at order 6 sees six positionals and takes the seventh; the defect is a silence in spec 3 and a stale assumption in spec 6, not a red at a correct build of either.

**Fix.** Spec 3 pins its fixture interface — `UNIT_STALL_BOUND="${6-1800}"` in `mkconf` plus a second `hit` on its NOTE in the `NOCONF` arm at `:5219`, or an explicit `echo >>` and no positional — and spec 6 takes the slot spec 3 leaves, spelled as the seventh if spec 3 takes the sixth.

**Left-shift.** A join in `tools/check-spec-tokens.py`: two specs in one build whose section 4 assigns the same `${N-` positional of the same named function REDS the higher-ordered one. Stage it on the pair as written.

### L — `TOOL-aProbedUnit-1` §4 "Four carriers, one rule, and which text binds", first paragraph (id 37)

**Finding.** "A sidechain child holds `AGENTS.md` and the hooks; it does not hold the unattended Skill. That is MEASURED, not assumed" hangs the MEASURED label on both halves. The cited record, `memory/guides/REVIEW-PROTOCOL.md:228-234`, measures `CLAUDE.md`, `AGENTS.md`, a `SubagentStart` hook and `PreToolUse`; it says nothing about Skills, and it records an unobserved injection "as not-observed rather than as absent" — the opposite of what the spec does for the Skill. The Skill half rests on the derivation "a child invokes nothing", which is an inference: a sidechain agent's own prompt lists the `unattended` skill as invokable. The round-1 fix text is where "does not hold the Skill" was first labelled the measured fact and the fold copied it — the class `TOOL-cRefutedPremise-1` records, a premise stated as fact while its conclusion keeps testing true, and `TOOL-dUnstalledConvoy-16` records recurring on this sidechain-inheritance claim.

**Fix.** Split the sentence: the charter and hooks are measured (keep the citation); the Skill's absence is INFERRED from its loading path — a Skill is invoked by the main loop, and a child invokes nothing — and is marked inferred, or measured once with a probe that lists the child's invokable skills and cited.

**Left-shift.** A §10 documented check rather than a gate: a `MEASURED` label covers only the nouns the cited record names; a claim about a noun the record does not mention is `inferred` until a probe names it. Ungateable, because the join is semantic, and it is the third recurrence of the class on one claim.

---

## What this round did not cover

The build README's owner-answer paragraph and its build-level rules, rule five included, were read as binding and not audited; every finding above measures a spec against them and none contests them. The specs' cost figures — spec 7's 237 arms and 335 s, spec 6's 13-byte method delta, spec 5's floor of 60 — are taken as the specs and the skeptics report them; only the suite phrase counts, the floor lines and the two host measurements named in the provenance paragraph were re-derived here. `RUN.md` and the `prompts/` folder were not in scope. The refuted 23 are not listed here; the pipeline holds them, and none was demoted rather than refuted.

Precision 0.44 sits below the ~0.5 floor `AGENTS.md` §8 sets, down from 0.53 in round 1 over a surface that is now hardened: the survivors are concentrated in the three specs that edit the harness suite and in spec 5's test mechanism, and the refuted majority re-reported round-1 fixes as defects. If a round 3 is ever owed, tighten the lens priming to the rev-2 deltas and the cross-spec seams (shared fixtures, shared constants, shared carriers) before adding agents; the README's one-round default says none is owed, and the disposal that follows this record is the stage's, by severity, not this record's.
