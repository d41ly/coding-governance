# TOOL-aProbedUnit-7 — acceptance ledger

**Serves:** journal TOOL-aProbedUnit-7

Every leg-shaped half below reads `observed at --close`: this pass ran no gate leg, no bar and no
suite whole, per the build README's rule five. Each pass half was observed by the grep or the single
arm the criterion names, by the form the spec's section 6 gives. The harness arms were run from
`tools/workflows` with the suite's preamble sourced by `bash -s` up to `# ---- AC2: THE ARGS GUARD`
so `HERE` resolves, then `F` re-pointed at the render under test: the tip's, and a copy of the base
render (`git show HEAD:tools/workflows/unattended-build.js`, HEAD being the pass's parent 47b29bb9)
frozen in the session scratchpad for the RED half. The driver arms were run from `tools/unattended`
with that suite's preamble sourced by `bash -s` up to `# ---- REGION ONE`, `SCRIPT` re-pointed
through an environment variable because the preamble refuses a positional argument, `TMPDIR` at
`%TEMP%/apu7`, then `bcsetup` and each arm's own `bcopen` block through its `hit` line; the RED half
ran the same script against a frozen `git archive HEAD tools/unattended tools/lib` of the base kit.

**Evidences:** TOOL-aProbedUnit-7
- AC1 — the V1 arm alone: `run_wf "$UNITS"` with `review_out 0 4 1`, `rec CONVERGED`, disposal `{"disposed":true,"standing":[],"promoted":1,"folded":3,"summary":"d"}` — OBSERVED: the trace carried `agent:dispose:tB`, the log line `disposing by severity, on CONVERGED too`, the prompt line `1 at HIGH`, `--rescope tB --act add --item` and `--reason`, and the RESULT `"promoted":1,"folded":3` and `"roster":[{`; all seven `has` silent at the tip; against the base render six printed `FAIL` (the `"roster":[{` one held, since the old skip path also handed out the roster).
- AC2 — the AC4 arm relabelled and extended, `returns CONVERGED 0` — OBSERVED: `disposal: skipped` present, `agent:dispose:` absent, RESULT carried `"promoted":0,"folded":0` and `"roster":[{`; the V2 line printed `FAIL … lacked '"promoted":0,"folded":0'` against the base render, the three old lines held there as the spec predicted.
- AC3 — the two V4 invocations, workflow `{"blockers":0,"report":"r.md"}` with no `confirmed`, then `{"blockers":2,"confirmed":1,"highs":0,"report":"r.md"}` — OBSERVED: each printed a `THROW` line carrying `returned confirmed`, neither trace carried `phase:Disposal`; against the base render both returned a RESULT and both traces carried `phase:Disposal`, six `FAIL` lines.
- AC4 — the V5 arm, `A_UNITS` with workflow `{"blockers":0,"confirmed":2,"highs":0,…}` and disposal `promoted 0, folded 2` — OBSERVED: `agent:dispose:tB` in the trace, the prompt line carried `authored Units table` and not `--rescope tB`, RESULT carried `"promoted":0,"folded":2`; against the base render no agent spawned and three `FAIL`s printed, the `hasnt_` holding vacuously. The `T_UNITS` arm alone with V7's line — OBSERVED: RESULT carried `"promoted":0,"folded":0` beside `"roster":[]`; `FAIL` against the base render.
- AC5 — the V3 arm, `review_out 0 3 0` with disposal `{"disposed":true,"standing":[],"promoted":1,"folded":1,"summary":"x"}` — OBSERVED: RESULT `"roster":[]` and `"promoted":1,"folded":1`, the note carried `do not reconcile — promoted 1 + folded 1 + standing 0 is not confirmed 3`, `disposal: done` absent; against the base render the roster was full and three `FAIL`s printed.
- AC6 — `grep -c 'REACHABLE HERE SINCE'` over template and render — OBSERVED: `1` and `1`; `grep -c 'UNREACHABLE HERE'` `0` and `0`; `grep -c 'blocker still standing'` `0` and `0`, where `git show HEAD:` prints `2`. The render was re-made by `bash tools/workflows/check-protocol-parity.test.sh --render --tracked-only`, which printed `rendered tools/workflows/unattended-build.js` and left `memory/guides/REVIEW-PROTOCOL.md` byte-identical; the render was parsed as the runtime does (`new AsyncFunction(…, src)`) and constructed. One comment block beyond the spec's four moved too — the `THE GATE` paragraph above the `CONVERGING` return, which said the two non-clean verdicts carry the disposal instruction — because it restated the verdict predicate the same way. The `workflow script syntax` and `review-protocol parity` legs — observed at --close.
- AC7 — `grep -cF 'DISPOSED BY SEVERITY'` over `tools/memory-tree/BUILD-METHOD.template.md` and `memory/guides/BUILD-METHOD.md` — OBSERVED: `1` and `1`; `grep -cF "takes the severity rule's disposition"` `1` and `1`; `grep -cF 'FOLD or PROMOTE'` `0` and `0`; `wc -l` of the render `341`; `wc -c` of the render `26895` against `26833` from `git show HEAD:`, exactly 62 more (sentence one 228 → 303 bytes, sentence two 74 → 61, swapped in binary mode and measured before writing). The render re-made by `bash tools/memory-tree/kit-dogfood-parity.test.sh --render`, whose three sibling renders came back byte-identical; 26895 is under the 27648 cap in `tools/template-size-limits.txt` and under the 26941 high-water, so no `WARN` is owed; `last-audit` re-stamped in the same commit at the merge-base `6074d521`. The `kit/dogfood doc parity`, `build-method size` and `kickoff-manifest ratchet` legs — observed at --close.
- AC8 — `grep -c 'DISPOSED BY SEVERITY'` over `tools/unattended/SKILL.template.md` and `.claude/skills/unattended/SKILL.md` — OBSERVED: `1` and `1`; `grep -c 'still disposed'` `1` and `1`; `grep -c 'severity rule'` over `tools/unattended/VERBS.template.md` and `memory/guides/UNATTENDED-VERBS.md` `1` and `1`; `grep -c 'as readily as promoting'` over VERBS `0`. Both renders re-made by `bash tools/unattended/adopt-unattended.sh`, which left `memory/guides/UNATTENDED-PROTOCOL.md` untouched. The `unattended skill wiring` leg — observed at --close.
- AC9 — over `tools/unattended/unattended.sh`: `grep -c 'fold or promote'` `0`, `grep -c 'blocker still standing'` `0` (it read `2` when this pass opened, unit 6 having rewritten the comment-block one), `grep -c 'admits BOTH'` `0`, `grep -c 'by the severity rule, and never re-rounded'` `1`, `grep -c 'PROMOTED to a unit of this build'` `1`, `grep -c 'FOLDED into the specs it belongs to'` `1`; over `tools/unattended/unattended.test.sh`: `fold or promote` `0`, `admits BOTH` `0` — OBSERVED. Run alone by the form above: the C1 arm (the `CONVERGED`-subject refusal) and the D1 requires-disposition arm were silent against the tip driver and each printed `FAIL missing` against the frozen base kit; the two standing arms, `PROMOTED` on the S1 promote exit and `FOLDED into the specs it belongs to` on the D2 fold exit, were silent against BOTH drivers, which is the criterion — the substrings were kept inside the rewritten sentences. The state-gate comment's `none of these four tokens`, which unit 6 handed to this pass, now reads `none of the state tokens`, spelling no count. The `harness arms` leg — observed at --close.
- AC10 — over `tools/workflows/unattended-build.test.sh`: `grep -c 'blockers were not disposed'` `0`, `grep -c 'UNREACHABLE HERE'` `0`, `grep -c 'REACHABLE HERE SINCE'` `1` — OBSERVED. `git diff` over the suite adds 29 `has`/`hasnt_` lines and removes 6, the 6 being the relabelled AC4 trio, the header arm and the two `findings were not disposed` arms rewritten in place, so 23 assertion lines are NEW and the suite's own `--- <n> arms` line at the close is expected to read 260 (237 + 23). The 33-arm single-run script — the seven new and moved arms plus the existing AC5, AC5b, AC7 and F4 arms whose fixtures this pass moved — printed `--- 33 arms, exit 0` against the tip render and 22 `FAIL`s with `exit 1` against the base render. The suite whole — observed at --close, by the direct run section 7 prices.

## What this ledger does not evidence

No merge-bar leg ran inside this pass — not `workflow script syntax`, `review-protocol parity`,
`kit/dogfood doc parity`, `build-method size`, `kickoff-manifest ratchet`, `unattended skill
wiring`, `harness arms`, `method carriers`, `memory hygiene` or `spec tokens` — and no suite ran
whole; each is `--close`'s and each row above says so. `adopt-unattended.sh --check` was not run
after the render because it IS the wiring leg. One live carrier of `admits BOTH` was read and left:
the `WHY IT EXISTS` comment above `DISPOSITION_CUTOFF` in `.unattended.conf:256`, which recounts
the measured state that introduced the cutoff (`TOOL-dBriefedPass-9`) rather than stating the
disposal rule, is not in this unit's declared write set, and is a watched file; the closing pass
may reword it if it reads as a rule. Check 2's `fold or promote` in `check-unattended.sh:537`
speaks of the field's two legal values and stays, as section 4 says.
