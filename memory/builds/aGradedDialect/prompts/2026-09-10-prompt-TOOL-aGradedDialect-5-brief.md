**Serves:** journal TOOL-aGradedDialect-5

# Build brief — TOOL-aGradedDialect-5, the records

Your spec is `memory/builds/aGradedDialect/spec/2026-09-10-spec-TOOL-aGradedDialect-5.md`, at rev-4
and through three audit rounds. It is Tier-1 and it moves no mechanism. Build what it says. Where you
would diverge, change the spec first with a rev bump and a §9 line, then edit.

**You are the last unit, and you are the one that makes the other four legible.** Four carriers in
this repository currently assert that the lexicon kit cannot read TypeScript. Three of them sit
inside the kit whose code now reads it. That is the `two-answers-to-one-question` class, committed
inside the fix for it, and it is what you exist to close.

## The facts your prose must agree with, all now on disk

- `KNOWN_EXTS` reads `ts: ("ts-tokens", "parser")` and `tsx: ("tsx-tokens", "parser")`. `PARSERS`
  holds `python-ast`, `shell-tokens`, `ts-tokens`, `tsx-tokens` — **FOUR parsers ship now, not two.**
- The mode was EARNED, not chosen: 115 of 115 frozen records in exact agreement, functions 122/122,
  types 20/20, refusal share 0.0000 against a 0.02 ceiling. `TOOL-aGradedDialect-3` measured it and
  `TOOL-aGradedDialect-4` transcribed it. **Do not quote that score in a carrier that cannot
  re-derive it** — point at the conformance arm, which prints it every run.
- `SEED_CONVENTIONS` seeds `ts.function` camel, `ts.type` pascal, `tsx.type` pascal, and
  `tsx.function` **dark as a declared refusal** with the role rule beside it.
- The corpus was frozen at `72979248`, before the reader's first commit `a69e4af1`. Both facts are
  observable and I re-verified them: the shas differ and the ancestor test holds.

## AC6 is two clauses over two legs, and that is deliberate

`check-kit-versions.sh` pairs FOUR carriers. The rendered Skill is NOT one of them — `lexicon wiring`
(`adopt-lexicon.sh --check`) is what guards it. A single-clause AC6 passed while the Skill went
un-re-rendered, which is round 3's finding 11. Bump the version once, in the four carriers that gate
pairs, and separately confirm `lexicon wiring` green over the re-rendered Skill.

## AC4 is two clauses too

The backlog row moving off `DEFERRED` is one half. The other is that
`memory/builds/dScaffoldedMirror/spec/2026-08-24-spec-dScaffoldedMirror-13.md`'s status-header TAIL
names `builds/aGradedDialect/` as the superseding record. A DEFERRED spec whose header points nowhere
is the half that keeps it honest, and it was observed by nothing until rev-4.

**That spec's status token does NOT move.** §8 F1 records why, as a FACT-QUESTION. Read it before
touching that file.

## S7 — the runbook section, which does not exist yet

`WIRE-INTO-PROJECT.md` has NO lexicon section. Its anchors are `kickoff-manifest`, `playbook`,
`memory-tree`, `drift-audit`, `codebase-map`, `memory-recall` and `push-main`; the kit's only mention
is the §0 menu bullet. So S7 writes the MINIMAL anchored section in the shape of the `codebase-map`
one at `WIRE-INTO-PROJECT.md:333`, and its `rm -f` line names the gov-only GRADING INSTRUMENTS a
`cp -r` delivers — `tools/lexicon/selftest.py` and `tools/lexicon/ts-conformance-fixtures.json` —
and deliberately NOT `SKILL.template.md` or the waiver seeds. §4 owns that derivation; do not
restate it inside AC7.

Why this matters rather than being tidiness: `TOOL-aGradedDialect-2` §8 F1 prices its disclosure of
adopter excerpts on a containment that `govkit apply` honours and `cp -r` does not. Without this
section that price is unpaid.

## One pre-existing red that YOUR landing drains

`python tools/drift-audit/drift_report.py --check` exits 1 today:
`non_terminal_specs_cited_by_product_source = 3` against a shrink-only pin of 2. The third entry is
`tools/lexicon/kit.toml` citing `TOOL-aGradedDialect-5` — you. Flipping your own spec to CLOSED in
the same commit as your edits removes it. **Confirm it exits 0 before you finish**, and if it does
not, say so rather than leaving it for the closing review to discover.

## Bounds

- No mechanism. No predicate. No gate arm of its own — §5 says every criterion rests on an existing
  merge-bar leg. If you find yourself designing something, that is a rescope and not a quiet
  widening.
- Do not touch the corpus, the reader, `KNOWN_EXTS` or `SEED_CONVENTIONS`.
- Corrections are REWRITES, never deletions. "Arming a language this kit does not ship" still has to
  teach the `PATTERNS:` block and still name Go, Rust and C#; it simply stops naming TypeScript.
  AC2's positive half exists because a zero-hit grep is satisfied by deleting the section.
- AC2's grep spans THREE files: `tools/lexicon/README.md`, `tools/lexicon/lexicon.py` and
  `memory/map/features/lexicon.md`. `TWO PARSERS SHIP` has two carriers and the stale language
  sentence has three.
- Your declared write set is `tools/lexicon/LEXICON.md`, `tools/lexicon/README.md`,
  `tools/lexicon/lexicon.py`, `tools/lexicon/kit.toml`, `.claude/skills/lexicon/SKILL.md`,
  `WIRE-INTO-PROJECT.md`, `memory/map/features/lexicon.md`,
  `memory/builds/dScaffoldedMirror/spec/2026-08-24-spec-dScaffoldedMirror-13.md`, your own spec, and
  your acceptance ledger. Widen through `--dispatch` BEFORE the commit if you need more.
- **`memory/backlog/TOOL.md` IS NOT YOURS, and the reason is mechanical rather than a scoping
  preference.** `--dispatch` REFUSES it: the build method names the backlog a shared mutable record
  outright, so no pass may declare it. AC4's backlog half is therefore written by the parent run
  directly, in a pass of its own, and your ledger records that split rather than claiming the edit.
  Do everything else AC4 asks: the superseding pointer in `-13`'s status-header tail is yours, and
  that file IS in your write set.
