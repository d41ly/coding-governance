# The acceptance ledger — the act refusal, its key and its fifteen arms

**Serves:** journal TOOL-aDeferredBar-3

Every line is OBSERVED or AMENDED, and every observation is the direct command the criterion names,
run by the build pass at rev-5 of the spec. No merge bar and no self-test suite was run for any of
them: each hook-fed criterion is one payload built with `python -c 'json.dumps(...)'` and piped into
`node "$HOOK"` against a scratch tree under `mktemp -d` holding a `.git/HEAD`, a conf and one
record, with the exit code and stderr read back; the suite `gate-guard.test.sh` carries the same
payloads and is the main loop's at `VERIFYING`. The wiring (S4) was the last write of the pass, so
every hook-fed observation below precedes it and the ones after it (AC7, AC8, AC12, AC13, AC15) are
greps and checkers with no deny shape. The build commit's own sha is observed by the main loop,
because a commit cannot observe itself.

**Evidences:** TOOL-aDeferredBar-3
- AC1 — `node "$HOOK"` — `rc=2` and `BLOCKED by gate-guard` on stderr with `flag: GATE_SELFTESTS=1`, the record at `BUILDING` keyed by `branch-ref: refs/heads/fx` against a `HEAD` of `ref: refs/heads/fx`. Observed RED first: the same payload fed before the file existed printed `MODULE_NOT_FOUND` and `rc=1`.
- AC2 — `node "$HOOK"` — eleven payloads, each `rc=2`: the `GATE_FULL=1` prefix, after `export`, behind `env`, behind `GATE_JOBS=1`, behind `timeout 30`, after `&&`, after `then` (each `flag: GATE_FULL=1`), D2 (`runner: tools/run-gates/run-selftests.sh`), D3 (`runner: tools/unattended/run-unattended-gates.sh`), D4 (`suite: tools/unattended/check-unattended.test.sh`) and D5 wrapping `timeout 5400 bash tools/unattended/unattended.test.sh` in `bash -c` quotes (`suite: tools/unattended/unattended.test.sh`). AMENDED at the closing round (rev-6), each payload fed the same way: `python tools/govkit/selftest.py` `rc=2` naming `suite: tools/govkit/selftest.py` and `python3 tools/memory-tree/gotchas.py --selftest` `rc=0` (F2); a suite inside a double-quoted `$( … )`, `timeout -k 5 120 bash <suite>`, `(stdbuf -oL -eL bash <suite>) &` and `/usr/bin/time -f FMT bash <suite>` each `rc=2` (F7); under `PowerShell`, `$env:GATE_SELFTESTS=1; bash <bar>` `rc=2` naming `flag: $env:GATE_SELFTESTS=1` and `$env:GATE_FULL=""; bash <bar>` `rc=0` (F8); the manifest parity probe: 44 whole-suite `chunk = selftests` argvs, 43 denied at `BUILDING` and `test_recall_floor.py` announced exempt. Observed RED-first on the rev-5 hook at `8b5b3f0c`: every one of those payloads printed `rc=0` there, and the parity probe listed the six `selftest.py` legs as not denied.
- AC3 — `node "$HOOK"` — with the record moved through `VERIFYING`, `LANDING`, `LANDED` and `ABORTED`, rows D1 to D4 printed `rc=0` with an empty stderr, sixteen payloads; at `BUILDING` the plain bar with `GATE_JOBS=1`, `run-selftests.sh --list`, `--check` and `--rank`, `run-unattended-gates.sh --help`, `kit-dogfood-parity.test.sh --render` and `--check`, and the empty assignment `GATE_FULL=` each printed `rc=0`; the bare suite at `BUILDING` still printed `rc=2` as the control; `bash -n` in front of the suite printed `rc=0` and `bash -x` `rc=2` (rev-5, after the wired hook denied this pass's own syntax check).
- AC4 — `node "$HOOK"` — `rc=0` for a `branch-ref:` naming `refs/heads/other`, a record with neither fact, a `HEAD` holding a bare sha, no record, and no `.unattended.conf`; `rc=2` when `.git` is a FILE whose `gitdir:` target holds `ref: refs/heads/fx` while the common dir's `HEAD` names `main`; `rc=2` for `anchor-kind: default-branch` with `run-branch: refs/heads/fx` and no `branch-ref:`, observed RED first against a scratch copy with the `run-branch` read removed, which printed `rc=0`; `rc=2` for `branch-ref: refs/heads/fx` and no `run-branch:`.
- AC5 — `node "$HOOK"` — `rc=0` on empty stdin, `not json`, `tool_name` `Read`, and a `tool_input` with no `command`; `rc=0` on `git commit -m "the unattended.test.sh arm for run-branch"`, on a python heredoc whose body spells `GATE_FULL=1 bash tools/run-gates/run-gates.sh`, on `grep -c gate-guard.test.sh tools/run-gates/selftest-budgets.txt` and on `grep -n 'check-unattended.test.sh' tools/unattended/kit.toml`; the same suite payload under `PowerShell` printed `rc=2` and under `Zsh` `rc=0`.
- AC6 — `sed -n 's/^PHASES_CORE="\(.*\)"/\1/p' tools/unattended/unattended.sh | grep -o 'VERIFYING.*'` — `VERIFYING LANDING LANDED ABORTED`; `node -p "require(require('path').resolve(process.argv[1])).PHASES_ALLOW.join(' ')" "$HOOK"` printed the same four words.
- AC7 — `python tools/settings-merge.py --fragment tools/unattended/gate-guard.fragment.json` — the first run appended one entry into the existing `Bash|PowerShell` group beside `scratch-guard.js`; the second run changed no byte, `git diff --stat` between them printing nothing; `bash tools/check-hook-destinations.sh` printed `ok tools/unattended/gate-guard.fragment.json -> tools/unattended/gate-guard.js` and exited 0.
- AC8 — `bash tools/unattended/adopt-unattended.sh --check` — exit 0 with `in sync` after the wiring, exit 1 naming the hook UNWIRED before it; `bash tools/check-kit-versions.sh` exited 0; `git grep -l` over `unattended@1.21|KIT_UNATTENDED_VERSION=1.21` lists sixteen paths, the fifteen carriers unit 1 counted plus the hook's own courtesy marker, and the same over `1.20` lists none.
- AC9 — `grep -c 'gate-guard.test.sh' tools/run-gates/selftest-budgets.txt` — `1`; `grep -cP 'selftest-budgets\.txt\t15\t' tools/install-prefix-carried.txt` — `1`; the `resolve_entry` one-liner printed the seven `project-owned` suites under `tools/unattended/` with `gate-guard.test.sh` among them; `bash tools/run-gates/run-selftests.sh --kit tools/unattended --list` lists the row at `220s`; `bash tools/check-install-prefix.sh` exited 0 once a hand-justified row for the suite's fixture-internal command strings was written beside the budgets row's raise — a row the spec did not foresee, recorded in section 9. AMENDED at the closing review's F1: that exit 0 was taken at 37 literals, before the rev-5 `bash -n` and `bash -x` arms of the same build commit added two more, so it observed a tree the commit never held and the leg was RED at `082da5cd` (`ROSE 37 -> 39`). The row is re-keyed to 39 with the reason, and the checker exited 0 on the re-keyed tree, taken on `082da5cd` plus the fold's staged edit; the fold commit's own sha is the main loop's, as a commit cannot observe itself. The class is `memory/gotchas/observation-before-the-last-fold-of-the-same-commit.md`. RE-KEYED AGAIN in the same closing round, 39 to 42 (F2: the selftest.py payload, its token check and the flag control) and 42 to 48 (F7, F8: four run-shape payloads with a token check, two PowerShell payloads with a token check), each with its reason in the row; `bash tools/check-install-prefix.sh` exited 0 on the tree staged for the unit-3 fold commit, whose parent is `50c4418a`.
- AC10 — `bash tools/unattended/adopt-unattended.sh --check` — on a tree seeded as the adopter suite's `seed()` seeds one, then installed with no verb, then with `.claude/settings.json` moved aside: `rc=1`, stdout naming the hook UNWIRED and the `--fragment` remedy; with the file restored `rc=0` and the arm silent. Observed RED first: the adopter at `d59e7022` on the same unwired tree printed `in sync` and `rc=0`. The precondition itself failed on the committed `seed()`, which copied neither `VERBS.template.md` nor `playbook.fixture.template.md`, so the install refused before rendering the Skill; `seed()` now copies both, recorded in section 9. AMENDED at the closing round (rev-6) on the same seeded-then-installed fixture: the marker under `PostToolUse` `rc=1` UNWIRED, under matcher `Bash` alone `rc=1` UNWIRED, the settings file moved out of tree and declared through `GOV_SETTINGS_JSON` `rc=0` `in sync`, undeclared `rc=1` UNWIRED, the fragment deleted `rc=1` naming `gate-guard.fragment.json is missing from the kit`, the wired tree `rc=0`. Observed RED-first on the adopter at `8b5b3f0c`: `in sync` under `PostToolUse` and under matcher `Bash`, UNWIRED for the declared out-of-tree file, and `in sync` with the fragment deleted. The suite's arm 1a re-runs the six at `VERIFYING`; that suite carries no assertion floor, which is recorded as a backlog row rather than added here.
- AC11 — `node "$PROBE" tools/unattended/gate-guard.js ~/.claude/projects` — the corpus record carries the probe verbatim and its table: 3134 files, 104 559 commands, D4 1518 hits of which 574 sidechain and 7244 near-misses, the plain bar 422; the first reading listed 38 runs among the D4 near-misses (`time`, `nohup`, `{`), which is this criterion's RED, so the spec went to rev-4 and the second reading over the shipped predicate lists one, a shell function invoking `"$@"`, the run-time-assembly ceiling section 3 states. About 14 s on node `a`. AMENDED at the closing round (rev-6): the corpus record's Reading 3 re-takes the table as an A/B of the rev-5 hook and the fold over one store (3157 files, 105 497 commands) with a `D4 selftest.py` row — 782 whole-suite `selftest.py` runs the shipped hook admitted, 357 from a sidechain — and corrects the near-miss walk: five run-shaped near-misses under rev-5 (the three F7 shapes, `time` by path, and two shell functions), two under the fold, both the `"$@"` ceiling; the record's earlier `ONE` and its claim that rev-5 moved no table figure are both retracted there. About 28 s for the pair on node `a`.
- AC12 — `python tools/codebase-map/test_codebase_map.py` — exit 0 after `python tools/codebase-map/gen_map.py --write` re-rendered `symbols.json` with the hook's definitions; `inventories.json` and `MAP.md` unchanged, no key claimed; `grep -c 'gate-guard.js' memory/map/features/unattended.md` prints `1` with the dossier at 20469 of 20480 bytes. Observed RED first: `gen_map.py --check` with the hook staged and no regen printed `STALE ... symbols.json — regen: python tools/codebase-map/gen_map.py --write`. `python tools/lexicon/lexicon.py` printed `lexicon OK` with offenders at the pin of 984; three helper names it refused on the first cut (`classify…`, `flush`, `fact`) were renamed to `resolveFileRow`, `readSegment` and `readFact`.
- AC13 — `grep -c 'gate-guard' memory/guides/SESSION-KICKOFF.md` — `1`; `bash skills/session-kickoff/manifest-check.sh` exited 0 with the manifest at 25561 of 25600 bytes after two traps AGENTS.md already carries were deleted; `last-audit` re-stamped at `c4f02308`, the `git merge-base origin/main HEAD` sha; `last-body-change` at `d59e7022`, the build commit's parent; the `manifest-audit: delta` line is in the commit message, its grep the main loop's.
- AC14 — `grep -c 'set_fact "$rel" run-branch' tools/unattended/unattended.sh` — `1`; `grep -c "^13\. \*\*The run's local branch ref"` prints `1` for the template and `1` for the render; `wc -c memory/guides/UNATTENDED-PROTOCOL.md` prints 58411 against 61440, and the pair diffs empty; the driver suite's arm 50e asserts the fact against `$(git symbolic-ref HEAD)` read from the fixture and is the main loop's at `VERIFYING`.
- AC15 — `grep -c 'refused at the tool call, not only forbidden' tools/unattended/README.md` — `1`; `grep -c 'gate-guard.js refuses it at the tool call'` prints `1` for `SKILL.template.md` and `1` for the render; `grep -c 'gate-guard.js' memory/map/features/unattended.md` prints `1`.
