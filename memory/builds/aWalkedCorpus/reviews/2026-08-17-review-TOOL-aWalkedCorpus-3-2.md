# Review — aWalkedCorpus, round 2 (M4 spec audit of TOOL-aWalkedCorpus-3, UNBUILT)

**Serves:** spec-audit TOOL-aWalkedCorpus-3

## Verdict: BLOCKED

*Synthesis pass over the single UNBUILT spec at HEAD `e3b62fb`, declared BASE `3e5c6d43`. Four lenses
-> batched skeptics -> this pass; **4/4 lenses live, 0 dead, 0 unverified findings.** **23 raw judged
— 20 confirmed, 3 refuted, 0 unverified.** The 20 confirmed collapse to **6 findings** after merging
five duplicate clusters: three lenses filed the absent fixture (F1), three the ceiling-normalisation
failure (F2), three the baseline key count and two the omitted generated map artifacts (both in F4),
three the adopter deployment (F5), and three separate S1 interface gaps (F6). **Every row was
re-executed here against the working tree before it entered the table.** Nothing is inherited on a
lens's word. **Dropped on re-execution, for the six-finding cap rather than for failing to
reproduce:** the missing pin-direction rule, the lexicon verb-pin headroom, and §5's `0.61 s` bench
figure — all three verified, all three recorded under "Verified, not filed" below.*

*The tree is green and stays green: `python tools/memory-recall/selftest.py` 34/34 ·
`python tools/codebase-map/test_codebase_map.py` 5 ok · `python tools/drift-audit/drift_report.py
--check` every signal at pin · `python tools/lexicon/lexicon.py` OK over 513 files. Both byte-compare
simulations below were restored and `git status --short -uall` re-confirmed as the pre-existing
`RUN.md` edit alone. **Not one finding reds a leg today** — this is a spec.*

## Answering the question this pass was convened on

**The predecessor's four §1 requirements are answered in form, and two of them fail on measurement.**

- *A data-dir producer and a comparator as their own executables* — answered. S1 names
  `tools/memory-recall/check-recall.py` and the `extract` -> `bench --json` -> compare flow. Round-1
  F1 is discharged in shape, though not in interface (F6).
- *A floor that names a CELL* — answered. `records:fts5:r@5` as one token discharges round-1 F3.
- *A widened provenance rule* — answered in prose, observed by nothing (F1).
- *A degradation that actually moves the chosen metric* — answered, and I reproduced it. Dropping
  `memory/DECISIONS.md` moves `records` 256 -> 225 docs and ceiling 1.00 -> 0.83, **cell for cell as
  §4's table states**, on a fixture I authored independently. Round-1 F2 is genuinely closed.

What is not answered is the design round-1 F5 asked for. The two-pin split is the spec's answer to
"a legitimately removed record drags the retrieval number down with it", and **the normalisation it
rests on does not separate the two directions**. I measured that (F2). Combined with F1 — the
question set every number was measured against is in no tracked file, no untracked file and no build
folder — the unit's centre is a mechanism whose stated behaviour is false and whose inputs no reader
can reach.

Measured on this tree, at the spec's own base, with a twelve-question fixture I wrote from
`memory/DECISIONS.md` rows without consulting the index:

```
$ python tools/memory-recall/extract.py . <tmp> --chunk-max 600 --rev 3e5c6d43
records         256 docs,     111738 indexed chars          <- §4 baseline row, exact

$ python tools/memory-recall/bench.py <tmp> <my-12q> --sets records --subs fts5 --ks 1,5,10
=== records: 256 docs, ceiling 1.00
fts5        0.83    0.83    0.83                            <- §4 baseline row, exact
```

I landed on the spec's exact baseline shape by accident, which is the strongest available evidence
that §4's corpus arithmetic is sound. Every retrieval number in the spec is nonetheless
unreproducible, because the fixture that produced them does not exist.

## Findings

| id | sev | where | finding | fix |
|---|---|---|---|---|
| **F1** | **blocker** | `spec/…-aWalkedCorpus-3.md:19` | **Every retrieval number the unit rests on was measured against a fixture that exists nowhere, and the AC meant to keep that fixture honest is satisfied by a tautology.** `git ls-files \| grep -i fixture` returns only `memory/gotchas/fixture-passes-by-finding-nothing.md`; `find . -iname "*fixture*.json"` returns nothing; `git status --short -uall tools/memory-recall/` is empty; `git show --stat e3b62fb` carries records and the spec only. So `§3:40`'s `chunks:fts5:r@5` 0.08, `§4:67`'s `fts5w` r@10 0.92, `§4:95-96`'s `0.83 / 0.17 / 0.20`, `§4:111`'s 16th/13th ranks and the term-overlap claim are checkable by nobody, while `§9:212` stakes the unit's whole answer to the deferral on their having been measured. The corpus halves DO reproduce exactly (256 -> 225 docs, ceiling 1.00 -> 0.83 on my own fixture), which isolates this as a fixture gap rather than an arithmetic one. Two of those unauditable numbers carry design arguments that **reverse** on an independently authored fixture of the same size: mine gives `chunks:fts5:r@5` 0.25 (not 0.08) and `fts5w` r@10 **0.83, equal to `fts5`, not better** — so §3's reason for cutting `chunks` and §4's reason for grading `fts5` over `fts5w` both rest on fixture artifacts. Meanwhile `§4:110-112`'s anti-tautology property ("eleven of the twelve questions share under half their content terms with the record they target") is observed by no AC: AC9 (`:187-189`) checks only that a `from` field is present and that the ceiling is 1.00. I built the tautology §4 warns against — twelve questions whose query text IS the target record's own text, each carrying a `from` — and it scores `ceiling 1.00, r@1/r@5/r@10 all 1.00, MRR 1.000`, satisfying AC1, AC9's stated shape and S3's 0.83 pin while proving nothing. That is `memory/gotchas/fixture-passes-by-finding-nothing.md` shipped as the closure of that class. Nothing binds the fixture the builder commits to the fixture `0.83` came from, so README:78-80's "A floor is measured, never chosen" inverts into a target to hit. | Put the twelve `question` / `expected_ids` / `from` triples into §4 as an `### Inventory` table so the pin is auditable before S2 is committed, or commit the draft `recall-fixture.json` alongside the spec. Spell the full reproduction argv (`--chunk-max`, `--sets`, `--subs`, `--ks`, `--rev`) beside the table. If neither, leave S3's literal as `<measured>` and mark the four unreproducible cells `UNVERIFIED` per `memory/TEMPLATE-SPEC.md:65`. Separately, add an AC that observes the anti-tautology property mechanically — `check-recall.py --audit-fixture` printing per-question content-term overlap against its target and failing above a declared share — so §4's honesty guarantee is a gate rather than a sentence. |
| **F2** | **blocker** | `spec/…-aWalkedCorpus-3.md:84` | **The two-pin split's load-bearing claim is arithmetically false in the majority case, and no arm in the unit can see it.** §4:83-84 says `RECALL_FLOOR` is compared against `r@k / ceiling` "so a ceiling movement reds exactly one pin". Over one query list `r@k = h/Q` and `ceiling = R/Q` (bench.py:381 scores 1.0 on any target hit, :376 scores 0.0 when no target resolves, :440 divides the resolvable count by `len(queries)`, :471 means over ALL queries), so `r@k / ceiling` is exactly `h/R`. Retiring a record a HITTING question targets removes it from numerator and denominator both: `(h-1)/(R-1) < h/R` for any fixture under 1.00. Measured on my 12-question fixture at the spec's own baseline shape (h=10, R=12, normalised 0.8333): **retire a hit** (`TOOL-aSiftedPlaybook-1`, single-homed) -> r@5 0.75, ceiling 0.9167, **normalised 0.8182 — below the pinned 0.83, so `RECALL_FLOOR` reds too**; **retire a miss** (`TOOL-aQuarriedLantern-1`) -> r@5 0.8333, ceiling 0.9167, normalised 0.9091 — the isolation the spec claims, and the ONLY degradation that produces it. Ten of my twelve questions hit, so the benign event §4:78 assigns to `RECALL_CEILING_FLOOR` alone with remedy "retarget the fixture" instead reds `RECALL_FLOOR` with remedy "investigate the change that moved it" for an archive that changed no retrieval. **At `>=0.83` against a measured 0.8333 the floor has literally zero headroom for one retirement.** Nothing in §6 can distinguish a working split from a broken one: AC2's sole red lever is the `memory/DECISIONS.md` drop, and both §4:95-96's table and my own run of that degradation (ceiling 0.83, normalised 0.50) put BOTH pins under — §4:98 says so outright. S5's fourth arm, "the two failure directions print distinguishable messages" (`:31`), therefore names a state no specified degradation produces, and `RECALL_CEILING_FLOOR` appears at `:23`, `:78`, `:84` and `:162` and in **no acceptance criterion at all**. | Two changes, both in this spec. (a) Fix the arithmetic claim: either derive the pin from the worst case — pin `(h-1)/(R-1)` (0.81 at 10/12) and state in S3 that the floor carries exactly one retirement's headroom by construction — or delete the "reds exactly one pin" sentence and state in §4 and §5 that a retired expected record reds both pins with `RECALL_CEILING_FLOOR` naming the cause, which is the alternative round-1 F5 offered. (b) Arm the separation: add the isolating lever to §4's red-proof table — retire a record targeted by a question the fixture already MISSES (measured ceiling 0.9167, normalised 0.9091) — and split AC2 into AC2a (floor-only red, message names `RECALL_FLOOR`, ceiling stays green) and AC2b (ceiling-only red, message names `RECALL_CEILING_FLOOR`, floor stays green), keeping the `DECISIONS.md` drop as the both-pins plumbing arm it is. State the evaluation order so AC2's message assertion is determinate. |
| F3 | high | `spec/…-aWalkedCorpus-3.md:188` | **AC9 rests the per-id resolution assertion on a metric that cannot express it.** "every `expected_ids` entry resolves in the `records` set — the `ceiling` of 1.00 is that assertion in measured form" is false, and I measured it. `ceiling` counts QUERIES, not ids: `bench.py:440` is `ceiling = sum(1 for w in wants if w) / max(1, len(queries))`, and `expected_by_target` drops an unresolvable target outright (`bench.py:360-361`, `if hits: out[tid] = hits`). Ran a one-question fixture with `expected_ids: ["TOOL-aMendedLedger-8", "NO-SUCH-ID-9999"]` -> **`ceiling 1.00`, `r@1/r@5/r@10` 1.00, `f@5` 1.00**. The consequence reaches the design, not just the AC: `RECALL_CEILING_FLOOR="records>=1.00"` is the pin §4:78 and §5:162-163 charge with catching "an expected id no longer resolves anywhere in the set", and §5:163 says "its red names the unresolved id" — neither is reachable through `ceiling` on any multi-id question. S2 (`:19-21`) makes multi-id the normal case by saying each question carries "the record ids" that answer it. | Stop resting the assertion on `ceiling`. Have `check-recall.py` compute `expected_by_target` over the `records` set itself and exit non-zero on ANY expected id with an empty hit set, naming the id — that is also what makes §5:163's "its red names the unresolved id" true. Restate AC9 against that check and add it to S1's description. Keep `RECALL_CEILING_FLOOR` as the aggregate if wanted, but delete the claim that 1.00 proves per-id resolution. |
| F4 | high | `spec/…-aWalkedCorpus-3.md:120` | **§4's Files-touched table and AC7 are wrong about the same carrier set in two directions.** (a) **Three GENERATED codebase-map artifacts are omitted**, and AC7 (`:182-184`) requires their byte-compare gate green. Simulated with the tree restored and re-verified green afterwards: appending an S4-shaped leg to `tools/gate-legs.json` turned `python tools/codebase-map/test_codebase_map.py` from 5 ok into `UNCLAIMED {'gate-legs': [...]}` **and** `FAIL test_generated_artifacts_are_fresh / STALE inventories.json`; comparing each renderer in-process shows `inventories.json`, `MAP.md` **and** `symbols.json` all stale, with `check-recall` present in the fresh symbols render and absent from the committed one (`map_extractors._live_py` walks the filesystem, so tracking is irrelevant, and S5's new `selftest.py` arms restale it independently). No prose edit satisfies a byte compare and no regen command appears anywhere in the spec. **This is round-1 F8's exact class, filed against `TOOL-aWalkedCorpus-1` in this same build's own review, recurring.** (b) **The baseline arithmetic is wrong in the opposite direction.** S6 (`:34-35`) says the dossier claims "the two memory-recall keys sitting unclaimed in the baseline today, which shrinks it by two" and AC7 asserts `baseline.toml` ends "two keys shorter". `grep -n 'memory.recall' memory/map/baseline.toml` returns **four**: `:32` and `:33` under `gate-legs`, `:52` under `kits`, `:71` under `rendered-skills`. The house convention is uniform — `unattended.md` claims `kits = ["unattended"]` and `rendered-skills = ["unattended"]`, `lexicon.md` and `codebase-map.md` claim their own `kits` — so a dossier written the way the others are shrinks the baseline by four and **falsifies AC7's literal**, while one that claims exactly two leaves a `memory-recall` kit key baselined next to a `memory-recall` dossier, forgoing half the shrink on a file the charter calls shrink-only. `test_codebase_map.py:89` reds only on LAZY BASELINE, so the gate forces neither. | Add three Files-touched rows — `memory/map/generated/inventories.json`, `MAP.md`, `symbols.json`, each marked GENERATED with the regen command `python tools/codebase-map/gen_map.py --write` — and extend AC7 to name the regen step rather than only the green verdict. Then replace S6's count with the claim SET — `gate-legs: memory-recall kit selftest, memory-recall skill wiring, <the new leg>; kits: memory-recall; rendered-skills: memory-recall` — and restate AC7 as "every `baseline.toml` key naming memory-recall is claimed by `memory/map/features/memory-recall.md` and deleted from the baseline", so the gate derives the number instead of the spec declaring it. |
| F5 | med | `spec/…-aWalkedCorpus-3.md:205` | **§8 reads "none" while the adopter-deployment fork is live, and round-1 F11 asked for exactly this decision.** `tools/memory-recall/kit.toml:9-11` is `[[files]] include = "**"` with `role = "engine"`, so BOTH new files — `recall-fixture.json`, a question set keyed on gov record ids, and `check-recall.py` — plus the README section S5 promises ship into every adopting tree. A leg reaches a target ONLY through a `[[gate_leg]]` block; that file declares exactly two (`:36-44`), S4 adds none, and `tools/govkit/registry.toml:148` states the reason verbatim: "a target's leg list is emitted from the selected kits' [[gate_leg]] blocks, never copied". So an adopter receives a floor program, a foreign question set and documentation for two pins their `.memory-tree.conf` does not carry, and nothing reds — no parity check exists anywhere between a kit's `gate_leg` blocks and `tools/gate-legs.json`. `memory/gotchas/pin-copied-from-another-corpus.md` is this repo's name for the class. Searching the spec for a disposition: `grep -niE 'adopt\|kit\.toml\|adopter\|deploy\|govkit'` returns ONE hit, `:53`, and it is the §3 `Conf`-field non-goal discussing what `adopt-memory-recall.sh` parses. §3's five non-goals, §4's Files touched, §5's risks and §8 are all silent. Among the round-1 findings whose design survives into unit 3, **F11 is the only one carried nowhere** — F3 is answered by S3's cell token, F5 by the two-pin split, F6 by the widened provenance rule, F15 by S4's three-path guard. | Record the decision in §3 as a non-goal or in §8 as a resolved fork, and carry it in Files touched. Either add a `[[files]]` exclude for `recall-fixture.json` and `check-recall.py` and state in §3 that the graded floor is gov-only, or declare a third `[[gate_leg]]` in `tools/memory-recall/kit.toml` plus the adoption-time path by which an adopter seeds their own fixture and MEASURES their own floor — and add `tools/memory-recall/kit.toml` to the Files-touched table if the latter. Cite `pin-copied-from-another-corpus` in §5 risks either way. |
| F6 | med | `spec/…-aWalkedCorpus-3.md:14` | **S1 describes a program with no interface and no error contract, and three states the unit needs are unreachable or unspecified because of it.** (a) **No seam for the degradation.** S1 says the program "builds a data dir into a `tempfile.mkdtemp()` by running `extract.py`" — an argument-free flow — while AC2 and S5 require an outside actor to present the gov corpus minus `memory/DECISIONS.md`. `recall_conf.repo_root()` (`recall_conf.py:50-67`) anchors on `__file__`, so the committed program always extracts the real tree, and `selftest.py`'s only harness `make_repo` (`:79-104`) builds a synthetic two-record corpus from `CORPUS`. Nothing in the kit can present the degraded gov corpus today; the spec leaves the builder to invent the harness for what §5:164 calls "the unit's centre". (b) **Nothing requires the pin token to drive the run**, which is the property S3 claims the single token buys. The report's key set is entirely a function of `--sets`/`--subs`/`--ks` (`bench.py:415-417`): I ran `--ks 1,10` and the `fts5` object came back `['b@1','b@10','f@1','f@10','r@1','r@10','rr']` — **`r@5` absent**, so a perfectly well-formed pin naming `records:fts5:r@5` resolves to nothing. S5 arms "a malformed pin" and AC4 covers "absent, or present and unparseable"; neither covers a well-formed token whose cell the report lacks. (c) **Degenerate fixtures are unspecified in both directions and both are reachable.** With `{"queries": []}` bench emits `"substrates": {}` and exits 0 — the cell S1 promises to read is ABSENT, so a program indexing `sets.records.substrates.fts5['r@5']` raises rather than redding by name. With an all-miss fixture the cell is present at 0.0 and `ceiling` is 0.0, making §4:84's `r@k / ceiling` a 0/0. §5:157-159 enumerates exactly three error states and names neither. | Name the surface in S1: `check-recall.py [--data-dir DIR] [--repo ROOT]` so the arm can grade an already-extracted dir, and add the flag to AC2's command and the Files-touched line. State in S1 that the pin token DERIVES `--sets`, `--subs` and `--ks` so re-aiming the pin at `records:fts5w:r@10` re-aims the run. Spell the evaluation order and the degenerate cases: `RECALL_CEILING_FLOOR` is checked FIRST and a `ceiling` of 0 reds naming it without ever normalising; a missing `sets.<set>.substrates.<sub>` entry reds naming the cell as unmeasured rather than raising. In S5, state how the degraded dir is built (extract once into a tmpdir, delete the `records.jsonl` rows whose `path` is `memory/DECISIONS.md`, re-run against it) and add arms for the empty fixture, the all-miss fixture and the absent cell. |

## Verified, not filed (six-finding cap)

Each of these reproduces; none displaced a row above.

- **The floor pin has no direction rule.** `grep -niE "shrink\|ratchet\|high.?water\|may only\|lower\|raise"` over the spec hits `:35`, `:85` and `:225`, none of them a rule about the pin. The DEFERRED predecessor carried one (`…-aWalkedCorpus-2.md:55-57`, "the score may rise and the floor may follow, but a fall reds") and the successor dropped it, so `RECALL_FLOOR="records:fts5:r@5>=0.00"` is a legal, parseable, permanently-green one-line edit that AC4 accepts. Held below the cap because the repo has a landed decision not to build pin-direction guards (`memory/map/features/lexicon.md` Gaps, `TOOL-aNumeralWarden-3`) and README:78-80 already states the rule in prose. **Cheapest fold: restore the predecessor's sentence to S3 and name a lowered floor in §5 risks.**
- **The lexicon leg has zero headroom for the arm-naming convention S5 will use.** `.lexicon.conf:49` sets `VERB_OFFENDER_PIN="417"` at exactly today's count and `lexicon.py:537` is one-sided (`> pin`). Adding one tracked `tools/memory-recall/_lexprobe.py` containing only `def t_floor_falls()` took `python tools/lexicon/lexicon.py` from OK over 513 files to **EXIT=1, "verb offenders 418 over pin 417"**; removed and re-verified green. `selftest.py` uses `t_` 29 times against `test_` 4 times, and `t` is not a declared verb while `test` is. The leg is guarded on `tools/`, so every commit in this unit runs it. Avoidable by naming against the file's own convention, which is why it is a build-time note rather than a finding — but §4 should say which way the builder goes.
- **§5's `bench over 12 questions 0.61 s` is plausible and not filed.** Measured here: extract 0.43 s (matching §5 exactly), bench 0.07 s at the narrow argv, 0.50 s at `records,chunks`/`fts5`, 0.82 s at bench defaults. 0.61 s sits inside that band and these figures are machine-variable. **No perf claim in the spec contradicts a measurement I took.**

## Checked and clean

- **The corpus arithmetic in §4 is exact.** `extract.py . <tmp> --chunk-max 600 --rev 3e5c6d43` gives `records 256 docs, 111738 indexed chars`; `memory/DECISIONS.md` contributes 31 record rows, so the degraded row's 225 is right. Both cells reproduce.
- **The substrate justification checks out against source.** `query.py:655` ranks with `bm25(d, 1.0, 1.0, B.ALIAS_WEIGHT)` and bench's `fts5` is the unweighted expression — §4:65-66's "verified against source rather than chosen" holds. Only the *comparative* number attached to it (F1) is unauditable.
- **Round-1 F2 is genuinely closed.** The replacement degradation moves the chosen metric hard on an independently authored fixture (r@5 0.83 -> 0.42, ceiling 1.00 -> 0.83), where the deferred unit's arm moved `records` by nothing.
- **`bench.py` and `union.py` are untouched and byte-pinned**, and `python tools/memory-recall/selftest.py` is 34/34 on this tree — AC10's premise holds.
- **§10's corrections are sound.** The `check_recall.py` non-existence claim, the `extract.py:128` / `query.py:302` forked-comment reading and the `aQuarriedLantern-1:85` citation all check out, and §10 correctly declines to inherit the predecessor's "already treats" claim. Round-1 F14's attribution class does not recur.
- **The §3 `Conf`-field non-goal is right.** `recall_conf.load_conf()` is public and returns the raw mapping, so reading the pins through it genuinely avoids `digest()`, the `KEY=VALUE` protocol and the rendered Skill.
- **`recall_conf.py`'s exclusion from Files touched is correctly reasoned** — `KIT_MEMORY_RECALL_VERSION` feeds `Conf.digest()` and this unit changes no extraction or schema behaviour.
- **The guard set answers round-1 F15.** S4 guards on `tools/memory-recall/`, `memory/` and `.memory-tree.conf` — the third path is the one F15 said was missing.

## Report limits

- **Three raw lens claims were refuted by their skeptics and are absent above:** that §4's remedy split "names no producible state" (single-direction states are producible — I produced one; the spec simply specifies neither), that a hardcoded-argv `check-recall.py` "grades a cell nobody declared" (the pin token declares it), and that the gate's pinned prose headings and affordance section need restating in S6 (AC7's named command already runs `test_dossier_prose_headings_pinned` and `test_dossier_affordance_present_or_graced`).
- **Two confirmed sub-claims were dropped in re-execution for being beside the point.** A supporting multi-homing count attached to F3 ("28 of 31 DECISIONS ids also appear in a second record") measures multi-HOMED ids, not multi-ID questions, and F3 stands without it — I do observe multi-homing directly (7 of my 12 targets resolve in two records), but it bears on F2's mechanics, not F3's. And the framing that S3's `0.83` was "chosen before the object that measures it exists" overstates: §9 asserts it was measured, and the defect is that nothing binds the committed fixture to the one it came from.
- **Every measurement is against this worktree at HEAD `e3b62fb`, base `3e5c6d43`.** Corpus figures: 323 files at base, 256 records, 10291 chunks at `--chunk-max 600`, 213 anchored ids. They will move.
- **The twelve-question fixture used throughout is mine**, authored from `memory/DECISIONS.md` and `memory/backlog/TOOL.md` rows without consulting the index, and not a proposal for S2. That it lands on the spec's exact baseline shape (ceiling 1.00, `records:fts5:r@5` 0.83) is a useful coincidence, not a reconstruction of the author's.
- **This is a spec audit, not a code review.** No finding reds a leg today. Both blockers are late-discovery by construction: F1 surfaces when a builder authors a fixture that scores something other than 0.83, and F2 when the first legitimate archive reds the wrong pin with the wrong remedy.

**Disposition — the build may NOT proceed to its first code pass.** F2 is the same shape the lineage
keeps repeating and that this build was convened to stop: a mechanism whose stated property is false,
whose one arm cannot distinguish it working from it broken, and whose cheapest exit at build time is
to weaken the arm. F1 is the deferral's own charge unanswered — the predecessor was deferred because
its numbers were never measured, and this successor's were measured against an artifact no reader can
reach. Take the spec to rev-2 first. §8 is no longer "none": the ceiling-normalisation choice (F2),
the per-id resolution check (F3), the baseline claim set (F4) and the adopter deployment (F5) are all
open forks. Nothing here says the unit is the wrong unit — S1's flow, S3's cell token, S4's guard set
and the replacement degradation are all correct, and three of the predecessor's four requirements are
genuinely discharged.
