# Review — aSiftedPlaybook spec set, round 2 (M4 spec audit)

**Serves:** spec-audit PLAY-aSiftedPlaybook-3 TOOL-aSiftedPlaybook-1 TOOL-aSiftedPlaybook-2 TOOL-aSiftedPlaybook-3 PLAY-aCandidStub-1 TOOL-aGuardedTally-1 PLAY-aPrunedCeremony-1 PLAY-aSiftedPlaybook-2 PLAY-aSiftedPlaybook-1

## Verdict: BLOCKED

*Workflow `wf_98677a7a-009`, run against `4b38e39` (post owner fork-resolution). Four lenses -> three batched skeptics -> one synthesis, all 8 agents completed. **64 raw findings, 52 confirmed, 12 refuted, 0 unverified.** Per lens: factual 16, decomposition 14, premise 15, underspec 19. This is the coverage round 1 could not buy — it lost 7 of 8 agents to a usage limit and ran the underspecification lens only.*


Two blockers: a build-level obligation the README delegates to a spec that does not contain it, and an owner-resolved fork whose value is absent, making three acceptance criteria unbuildable. Twelve high findings follow. Line numbers below were re-derived from source; every quoted file content was read at `4b38e39`.

| id | sev | file:line | finding | fix |
|---|---|---|---|---|
| B1 | blocker | `memory/builds/aSiftedPlaybook/README.md:73-79` | The v2.8 marker bump and the v2.7 archive snapshot are assigned to `PLAY-aSiftedPlaybook-3` ("That unit carries it as a scope item and an AC, naming … line 12, … line 3, and the v2.7 snapshot under `memory/archive/`"). PLAY-3 contains none of it: `grep -rn 'v2\.8\|governance-template\|marker\|archive'` over all seven specs returns two hits, neither in PLAY-3 and neither about the bump. PLAY-3 §2 is S1-S7 with no marker item; §4 Files touched (`:107-116`) has no `memory/archive/` row and no `:12`/`:3` line; §6 AC1-AC6 has no marker criterion. The work is real and unstarted — `parallel-coding-governance.template.md:12` and `parallel-coding-governance.domain-rules.md:3` both read `<!-- governance-template: v2.7 -->`, and `memory/archive/` holds `-v-2-0` … `-v-2-6` with no v-2-7. Round 1's finding #4 was folded into the README only; the receiving unit was never told, which is worse than the original state because a reader now believes it is handled. A builder working PLAY-3's ACs ships a v2.7 marker over v2.8 content and passes. | Bump PLAY-3 to rev-3: add S8 (bump both markers, copy the pre-bump template to `memory/archive/parallel-coding-governance.template-v-2-7.md`), add all three paths to §4 Files touched, add an AC observing all three, log in §9. If B1's ordering fix (H1) moves the lane, name the unit that actually lands last. |
| B2 | blocker | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:39-42` | S8 is the entire build of owner fork F2 and specifies only "a second named constant beside `MAX_BYTES`, env-overridable on the same pattern". No name, no value, no relation to `MAX_BYTES`, no stream, no behaviour when the two cross. TOOL-1 AC7 (`:160-163`) and TOOL-2 arms A6/A7 (`spec-TOOL-aSiftedPlaybook-2.md:28-29`) all require constructing "a file between the soft threshold and `MAX_BYTES`" — unbuildable. The value is also the mechanism: this build's own spends are +238 (PLAY-2 `:92`) and ~2043 gated (PLAY-3 `:91`), landing at ~34963 = 71% of 49152, so any conventional fraction (80% = 39321, 90% = 44236) is silent through the entire build and 4-9 KiB beyond it; the only thresholds that would price these edits sit at or below 32768 and fire on every run forever — the permanently-red-decoration shape `tools/drift-audit/drift_signals.py:76-80` names as an anti-pattern. No spec picks a side. F2 is marked RESOLVED with "no residual fork" (`:191`), so nothing routes the gap back to anyone. | Name the constant and its default in S8 with the policy it derives from, state where the template sits relative to it at BASE and after PLAY-2+PLAY-3, and re-word AC7 against specific byte counts. If the value is the owner's, reopen it as a bounded fork — a RESOLVED item cannot be missing its own subject. |
| H1 | high | `memory/builds/aSiftedPlaybook/README.md:58-66` | The Order column has no edge between the three template-touching units: TOOL-1 first · TOOL-2 after TOOL-1 · TOOL-3 last · PLAY-1 **independent** · PLAY-2 after TOOL-1, after PLAY-4 · PLAY-3 after TOOL-1 · PLAY-4 before PLAY-2. PLAY-1 (`:108`), PLAY-2 (`:107`) and PLAY-3 (`:109`) all edit the template and none is ordered against the others. Two consequences. (a) "the last TEMPLATE-touching unit to land" (`:73`) is under-determined, so B1's rule has no determinate owner and the v2.7 snapshot cannot be timed. (b) PLAY-3 S1-S3 insert ~1593 gated bytes into template §5 (`:98-110`), §6 (`:111-125`) and §7 (`:126-143`) — all ABOVE and INSIDE the anchors PLAY-2 S2 cites (`:100`, `:157`, `:218`, `:219`) and PLAY-1 §4 cites (`:150`, `:157`, `:107`, `:51`, `:24`). If PLAY-3 lands first every one is stale, including PLAY-1 S4's `:107`, which is the very bullet PLAY-3 adds a sibling to. PLAY-2 §4 (`:63-66`) diagnosed exactly this hazard for `customize.md` and declined to extend it to the template. Separately PLAY-1 and PLAY-2 both rewrite template `:51` (verified: `:51` carries both "a committed build plan" and two of the 17 branch senses), and PLAY-1/PLAY-3/PLAY-4 all edit `customize.md` — collisions declared nowhere in PLAY-1, whose §4 opens "Independent of every other unit in this build" (`:114`). | Add the template-lane edges to the Order table (PLAY-1 → PLAY-2 → PLAY-3 puts every insertion after every anchored substitution), restate the marker rule as "the unit sequenced last in the template lane", and replace PLAY-1 §4's independence claim with the real constraint. Or re-express PLAY-1's and PLAY-2's anchors by sense/section as PLAY-3 already does, demoting the BASE line numbers to measurements. |
| H2 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:74` | §4 Inventory still reads `| .claude/SESSION-KICKOFF.md | 100-107 | measured figure + funding policy |` — the pre-rev-2 range. §2 S5 (`:25-30`) says the opposite and says why: the edit is `:99` and `:100-106`, and "**`:107-109` SURVIVES the rewrite**". Verified: `.claude/SESSION-KICKOFF.md:107` = `  headings and are the next candidate. (Paraphrased rather than cited by id on purpose — the drift`, with `:108-109` completing the warning that §7's landmine (`:182-187`) depends on. Round 1 folded §2 and left §4, so the spec instructs two ranges and the Inventory — the section a builder works from — takes the load-bearing line. Neither range is byte-accurate: the keep/delete boundary falls **mid-line-107**. | Restate S5 by sentence ("delete from 'It sits at 32682/32768' through '…are the next candidate.'; keep the parenthetical from '(Paraphrased' through `:109`") and make the §4 row spell the identical boundary, with `107-109 · SURVIVES` as its own visible row. |
| H3 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:151-153` | AC3's pattern is `32768\|32 ?KiB\|never raise the limit`. S2 (`:17-20`) scopes three sites in the gate and calls the failure message "the highest-value line in the file to get right". Verified against source: `tools/check-template-size.sh:4` = `# (parallel-coding-governance.customize.md / .domain-rules.md), never inflating the template.` and `:25` = `  echo "  Do NOT raise the limit. Trim non-instructional prose…"` — neither contains any of the three alternatives ("Do NOT raise the limit" ≠ "never raise the limit"). Confirmed by running the pattern: the 9 hits under `':!memory/'` do not include `:4` or `:25`. So S1 can land (`:8`, `:14` → 49152), S2 can be skipped entirely, and AC1-AC8 all pass — shipping a gate that tells an over-budget agent not to raise a limit the owner just raised. The same hole reaches `.claude/SESSION-KICKOFF.md:102-106`: only `:100` carries a matched token. | Add `Do NOT raise` / `[Rr]aise the limit` / `inflating` to AC3's alternation, and add a positive AC that observes the rewritten strings (run the gate against an over-limit file and assert the emitted message text) rather than only the absence of old numbers. |
| H4 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-3.md:104` | `tools/playbook-kit-waivers.txt` has two declared creators. TOOL-3 §4 Files touched: `| tools/playbook-kit-waivers.txt | new, seeded from the measured population |`; PLAY-3 S7 (`:35-40`): "**This unit creates it; `TOOL-aSiftedPlaybook-3` consumes it.**" TOOL-3 §2 S1 (`:15-18`) is silent on ownership. The file does not exist (`tools/` holds only `install-prefix-waivers.txt`). Ordering makes it live: TOOL-3 §4 Rollout (`:119-121`) says the owner may defer TOOL-3 indefinitely, and PLAY-3 is only "after TOOL-1". If TOOL-3 re-seeds "from the measured population" at its own build time it either overwrites PLAY-3's three curated rows, or — if PLAY-3 is deferred — permanently waives drift-audit, pytest-parallel-guardrails, agent-instructions and gate-lint, the four omissions PLAY-3 exists to close, and the gate then certifies coverage that does not exist. TOOL-3 rev-3/rev-4 (`:205-216`) resolved the identical collision for `memory/map/features/playbook.md` with "EXTENDED, never created" (`:106`) and did not apply it here. | Mirror the dossier resolution: TOOL-3's row becomes "consumed, never created — `PLAY-aSiftedPlaybook-3` S7 seeds it; reds and stops if absent", add PLAY-3 as a stated prerequisite in TOOL-3 §4 Rollout, add an AC observing the refusal, add a non-goal banning TOOL-3 from adding rows, and pin the row format in one spec. |
| H5 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:166-168` · `:267-271` | AC6 requires the new `memory/DECISIONS.md` row to name "`PLAY-aCandidStub-1`, `TOOL-aGuardedTally-1` and the `aCandidStub` review's refuted id 19". §10 says the recall query "returned the three records this unit's §2 S6 **must name** — `PLAY-aCandidStub-1` §3 …, `TOOL-aGuardedTally-1` …, and `PLAY-aPrunedCeremony-1` RD7". The third member differs; a builder satisfying the ACs mints a row missing RD7 — verified real at `memory/builds/aPrunedCeremony/build/2026-07-19-build-PLAY-aPrunedCeremony-1.md:25`, and the record that states the superseded rule as a design constraint. All four cited records verify. | Make the two lists identical, most likely all four, since each is independently a premise the reversal falsifies. |
| H6 | high | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:99-103` | §4 says the finding is "landing a finding whose **only** blocking objection the owner has now removed", two lines after conceding "**partly** on the ground that". Verified at `memory/builds/aCandidStub/reviews/2026-08-10-review-aCandidStub-1.md:222-224`, id 19's refutation carries three grounds — "no line claims the menu is exhaustive; each bullet is individually true; the template is at its ceiling" — and the dispositive framing ("An omission, not a contradicted claim") depends on neither the ceiling nor the raise. Two of three grounds survive untouched. PLAY-3 `:102-103` then requires the build's decision row to cite id 19, and TOOL-1 AC6 mints it — so the mischaracterization is scheduled into a record the repo declares append-only and never edits. | Restate §4: id 19 rested on three grounds, one removed and two surviving; the case for landing now is that the ceiling was the practical blocker while the other two were dispositions about the template's contract that S1-S4 change by giving each kit a placement rather than extending a menu. |
| H7 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:196-205` | F1's consequence paragraph names the problem honestly — an in-place `baseline.toml` key swap "is a delete plus an add through that file" — and then nothing records it. Verified carriers, all still asserting shrink-only after this build's declared scope: `memory/map/baseline.toml:3-4` ("This file only SHRINKS… additions are reserved for the initial backfill"), `AGENTS.md:7` ("the keys not yet claimed by one are in the shrink-only `baseline.toml`"), `tools/codebase-map/map_lib.py` `compute_coverage` docstring. TOOL-1 S3 scopes `AGENTS.md` at `:16`, `:97`, `:178` — not `:7`. AC6 enumerates exactly three records the decision row must name and the baseline convention is not among them, so the SECOND rule reversal in this build is recorded nowhere but §8 of a spec. TOOL-3 §3 (`:51-56`) declares enforcement out of scope and says "the swap it would have caught needs a waiver first" — no unit creates that waiver and no follow-up row exists. | Extend S6's decision row to record the baseline exception as a second reversal and add it to AC6's enumeration, and/or annotate the `baseline.toml` header in place with the dated exception. Precedent: `tools/drift-audit/drift_signals.py` states an exclusion from a shrink-only promise "deliberately and in writing rather than by omission". |
| H8 | high | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-2.md:109` | §4 Files touched lists `| WIRE-INTO-PROJECT.md | :98's 36 in the restated claim |`, but §2's S1-S4 (`:14-33`) name only the template and `customize.md`, and §6's AC1-AC6 (`:146-173`) never mention the file — AC4 recomputes placeholder sets over the template and companions only. Verified: `WIRE-INTO-PROJECT.md:98` reads "the companion carries 13 of the 36 placeholders" and is that file's only `36`. A builder working §2 and §6 ships a correct template and `customize.md` while the runbook still says 36, with every AC green. This is round 1's PLAY-1 S8 finding one unit over. | Add S5 for `WIRE-INTO-PROJECT.md:98` (36→37, 13 unchanged) and an AC that greps it. Note in §4 Rollout that PLAY-3 S6 also edits this file (`:81`'s dead §2a reference), so whichever lands second re-derives its anchor. |
| H9 | high | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-1.md:166-169` | AC2 ("a workflow script declaring a 5-element lens array is fed to `node tools/hooks/agent-cap.js`… when one declaring 6 is fed, it is DENIED") is not runnable as written. Reproduced three ways. (1) The hook reads a PreToolUse JSON object from fd 0 and exits 0 on unparseable input by design (`tools/hooks/agent-cap.test.sh:31-33`), so piping raw script text passes the ALLOW arm for the wrong reason and the DENY arm cannot fire. (2) With a correct payload, a script that merely DECLARES the array exits 0 at both 5 and 6; the deny requires an actual fan-out over the literal — `boundedParallel(LENSES.map(d=>()=>agent(d)),5)` measured exit 0 at 5 and exit 2 at 6. (3) "DENIED" is unanchored against the hook's 0/2 contract. | Rewrite AC2 naming the payload and the fixture, modelled on the `js()` helper at `tools/hooks/agent-cap.test.sh:27-35`, with expected exit codes 0 and 2. |
| H10 | high | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-1.md:114-118` | "It does not depend on `TOOL-aSiftedPlaybook-1`'s ceiling raise: S3 is the only item that adds material and its cost is well under the 86 bytes available at BASE." S3 is not the only adder: S7 (`:64-68`) adds to §0, S8 (`:47-53`) rewrites `:157` to name the `Agent` tool call as well, S2 turns `Workflow` into `Workflow|Agent` (+6). Against ~17 bytes removed by S4 and S6, S3's two rule clauses have on the order of 45 bytes. The figure is asserted, not measured — PLAY-2 (`:92`) and PLAY-3 (`:82-91`) both measured theirs by simulation. Confirmed live: `bash tools/check-template-size.sh` reports `32682 / 32768 (86 under)`. If the true cost exceeds 86, AC6 reds and the declared independence — and README `:63`'s "independent" cell — is false. | Measure by simulation, put the number in §4 Rollout, and state the dependency either way. |
| H11 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-2.md:28-29` | A6/A7 reproduce the exact `MAX_BYTES`-discovery defect rev-3 folded, and have no red proof. The discovery discipline at `:36-41` covers `MAX_BYTES` only ("A1, A2 and A4 therefore run the gate with no override and read the limit back out of its own OK line"); the SOFT threshold is not derivable — `tools/check-template-size.sh:29`'s OK line prints only bytes and `MAX_BYTES`, and per TOOL-1 S8 the threshold is printed only by the warn line, which fires only once you are already above it. The only route left is exporting the harness's own value, which is the vacuity rev-3 folded this section to prevent. Separately AC1 (`:137`) requires only exit 0, AC2 (`:139`) inverts `-gt`→`-lt` (touching A1/A2, not the warn branch) and AC3 (`:142`) arms A4 — A6/A7 are the only arms in the unit with no observation that they can fail, inside the unit whose §1 quotes "a gate you have only ever seen pass is an assertion about nothing". | Once B2 names the constant, state how A6/A7 learn it with no override (e.g. the OK line gains a third field printing the soft threshold on every run), and add an AC on AC3's model: when the warn comparison is inverted or its constant deleted by hand, A6 or A7 reds naming itself. |
| H12 | high | `memory/builds/aSiftedPlaybook/README.md:14-16` | "A read-only audit … confirmed eleven defects and refuted eight suspected ones." `git log --diff-filter=A --name-only -- memory/builds/aSiftedPlaybook/` returns only README.md, one review and seven specs — no audit report, against `AGENTS.md`'s rule that reports and research live under the build's own folder. Only five of the eleven carry ids anywhere: PLAY-1 §2's D1-D5 (`:14-15`); D6/D7/D8 are explicitly NOT from the audit. PLAY-3 implies three more by calling gate-lint "a fourth absent kit the audit did not catch" (`:28`); PLAY-4 covers roughly two; PLAY-2 never claims audit provenance and its §10 says "No prior record proposes parameterizing the branch name". At most ten are traceable with no way to identify the rest, and the eight refuted findings are recorded nowhere — so completeness against the commissioning input is unfalsifiable, and a later session cannot tell a dropped defect from one that never existed. | Commit the audit under the build folder with the eleven confirmed and eight refuted findings numbered, and add a coverage table to the README mapping each of the eleven to the scope item that fixes it. Round 1's own "Owed" list (`reviews/2026-08-16-review-aSiftedPlaybook-1.md:58-60`) asks for exactly this. |
| M1 | medium | `memory/builds/aSiftedPlaybook/README.md:113-116` | "Still open, and NOT put to the owner because they are unit-internal rather than build-level: `TOOL-2` F1 …, `PLAY-1` F1 …, and `PLAY-3` F1 …". `memory/guides/BUILD-METHOD.md:64-66` has no such category: "a fork whose options differ in *what gets built* is not yours — park it. With no mandate, forks go to the owner". There is no standing mandate here. TOOL-2 F1 changes the gate's source shape at three call sites and adds an `.memory-tree.conf` `ARMS_FLOORS` key; PLAY-1 F1 and PLAY-3 F1 both change a governance carrier — M3's veto 2, explicitly an owner turn. M2 (`:37-38`) also classifies all three units FORKED rather than READY, which the Units table does not record, so PLAY-1 is shown as buildable now while it cannot be classified READY. PLAY-3 §8 (`:182-183`) contradicts the README outright: "This is a real fork and it is the owner's". | Put all three to the owner in one turn, or resolve those that survive M3's ratify rule and mark them `(agent, …)` with the authority named. Delete the "unit-internal" category. Reconcile PLAY-3 §8 with README `:113-116`. |
| M2 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:55` | "`PLAY-aSiftedPlaybook-3` spends 1593 bytes of it". PLAY-3 §4's cost table (`:82-91`) measures 645+527+421+450 and states "roughly **2043 bytes**". 645+527+421 = 1593 exactly — TOOL-1 carries the pre-S4 figure, from before PLAY-3's rev-1 log records discovering the fourth kit. TOOL-1 is the one that under-reports the headroom draw the raise is justified by. | Replace with a pointer — "PLAY-3 spends the headroom; its §4 cost table owns the figure" — per the same single-sourcing discipline TOOL-1's other items use. |
| M3 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:21-23` | S3 scopes "`AGENTS.md:16` (the most emphatic never-raise carrier)". Verified: `:16` = "…**≤32 KiB, strictly gated** by `tools/check-template-size.sh` — trim or externalize, never" and `:17` = "raise the limit). Companions: …". The sentence spans the break; an edit scoped literally to `:16` orphans "raise the limit)". Identical class to H2. AC3 cannot catch it: after S1 the pattern no longer matches `:16` and never matched `:17`. | Cite `AGENTS.md:16-17` in S3 and in the §4 Inventory row. |
| M4 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-2.md:19` | "**S2 — the arms.** Five, each an OBSERVED failure or pass" is immediately followed by a table (`:21-29`) of A1-A7. rev-3 (`:198-202`) added A6/A7 and did not update the count; the same staleness recurs at `:38`, "a harness that simply exports its own value tests the override path **five times**". This is the hardcoded-count-that-ratchets class the build exists to close, reproduced inside it (cf. PLAY-1 §4 Alternatives rejected, `:122-126`). | "Seven", or delete the number and let the table be the count; `:38` becomes "every time". |
| M5 | medium | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-1.md:93` | §4 Inventory: `| S4 hygiene checks | 19 | 20 | check-memory-hygiene.sh:713 |`. §2 S4 (`:39-46`) explicitly disqualifies that file — "**The authority is not the engine** … The count is single-sourced by the gate-leg name in `tools/gate-legs.json:3` and the kit README's table row at `tools/memory-tree/README.md:18`." AC1 (`:160-162`) then makes the table the derivation set ("The **eight** derivations are the §4 Inventory table's third column"), routing the builder to the disqualified file. rev-2 corrected §2 and left the table. Verified: `gate-legs.json:3` = `"name": "memory hygiene (20 checks)"`; `tools/memory-tree/README.md:18` = the 20-check row; `check-memory-hygiene.sh:713` is a delegation comment, not a fail-number. Secondary: AC1 over-quantifies — S4's fix is a DELETION, so it has no value to re-derive. | Change the S4 row's source cell to `tools/gate-legs.json:3` · `tools/memory-tree/README.md:18`, and exempt S4 from AC1's derivation set (its AC is AC5's `[0-9]+-check` grep). |
| M6 | medium | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:35-37` · `:76-78` | S7 seeds the waiver file with `lib/`, `hooks/` and `workflows/`. Measured against the trio at BASE with TOOL-3 S1's own anchored path-segment rule (`spec-TOOL-aSiftedPlaybook-3.md:20-21`): lib = 0 path-segment hits, hooks = 0, **workflows = 1** — `parallel-coding-governance.template.md:150` spells `tools/workflows/tier2-review.js`. So `workflows` already passes S1 unwaived and its row excuses nothing. (`lib` and `hooks` genuinely need waivers — all 7 `lib` hits are substrings and the single `hooks` mention is the English word.) TOOL-3's staleness arm AC6 (`:168`) reds only when a waiver names a kit that no longer exists, so a waiver for a kit that IS named sits permanently in a file both specs describe as shrink-only and model on `tools/install-prefix-waivers.txt`, whose contract is that a waiver whose hit is gone reds as stale. | Seed with `lib/` and `hooks/` only; record in PLAY-3 §4 that `workflows` is covered by template `:150`. Optionally add a TOOL-3 arm that reds a waiver whose kit IS named, so the registry drains. |
| M7 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-2.md:178-184` | §8 F2 "RESOLVED (agent, 2026-08-16, downstream of TOOL-1 F1): THIS UNIT mints it". The mint then appears in §4 prose (`:88-94`), §4 Files touched (`:104`) and AC6 (`:149`) — but §2 Scope stays S1-S4 with no item for it. `memory/TEMPLATE-SPEC.md` §2 requires a bounded numbered list where every item is verifiable at DoD; round 1 applied this exact correction to PLAY-3, which grew S7 for the same reason. A new `memory/map/features/playbook.md` must satisfy `map_lib.py`'s pinned heading contract plus the `## Reuse affordance` section and a `[claims]` block — comparable to the 76-line `codebase-map.md` the spec names as its model. M3 also reserves a fork that changes what gets built for the owner; the `(agent, …)` signature is honest and the reasoning defensible as a consequence of the owner's F1, but the deliverable never reached §2. | Add TOOL-2 S5 for the dossier mint and cross-reference it from §4 and AC6. Either note in §8 F2 that the owner's F1 is the authority for the consequence, or put the added deliverable to the owner. |
| M8 | medium | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:172-183` | §8 F1 recommends option (b) (one-line pointers plus a companion §5a) while S1-S4 (`:14-32`), §4's cost table (`:82-90`, all four priced "yes — template"), §4's conclusion ("**This unit is the one that genuinely requires `TOOL-aSiftedPlaybook-1` to land first**", `:91-94`), AC5 (`:157`) and README `:65`'s "after TOOL-1" are all written for option (a). Under (b) the gated cost collapses, the TOOL-1 dependency disappears, and `:93-94` becomes false — while that sentence is the load-bearing justification the README (`:25-27`) and TOOL-1 §3 (`:55`) both lean on for the raise. M3 parks the fork correctly; nothing tells the owner that their recommended option requires a rev of §2, §4 and §6 and removes an ordering edge. | State per-option cost in F1: under (b)/(c), S1-S4 re-scope to pointers plus companion §5a, §4's table is re-measured, the "requires TOOL-1 first" claim and AC5 are dropped or re-derived, and README's Order cell becomes independent. |
| M9 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:21-23` | S3 puts `AGENTS.md:97` in scope — verified as `- template size ≤32 KiB — \`tools/check-template-size.sh\``, the gate-suite section's **only** carrier of that script path. `_charter_mentions_every_leg` (`tools/drift-audit/drift_signals.py:70-100`) credits a leg only when one of its argv paths appears verbatim in that section, and `handkept_inventories_disagreeing_with_source` is pinned at 0 with zero tolerance (`:145`). A rewrite that reflows the bullet away from the path reds the bar. §4's blast-radius analysis (`:83-102`) reasons only about the `gate-legs.json` rename and concludes drift-audit is unaffected because the probe matches paths not display names — true of the rename, and exactly why the `AGENTS.md` edit is the dangerous one. §7 (`:177-178`) says the drift run is "expected unaffected by the rename per §4". No AC observes it. TOOL-2 adds AC5 for precisely this signal because it touches the same section. | Add the equivalent AC to TOOL-1, and state in §4 that the `AGENTS.md:97` rewrite — not the leg rename — is the edit that can move that signal. |
| M10 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-3.md:40-43` | S4 creates `tools/check-playbook-parity.test.sh` and §7 (`:176`) runs it as a gate, per the charter convention TOOL-2 §4 (`:113-116`) invokes to insist its own test be wired. S5 then scopes exactly one of each: "A `tools/gate-legs.json` entry, the charter citation in `AGENTS.md`'s gate-suite section, and the codebase-map dossier claim for the new leg key" — singular throughout; Files touched (`:105-107`) carries no count. Two legs mean two `gate-legs` inventory keys to claim and two script paths `_charter_mentions_every_leg` must find under a pin of 0. AC7 (`:169-170`) observes only that the test runs. | Make S5 explicit about two leg entries, two claimed inventory keys and two cited script paths — or state that the self-test is deliberately not a leg and reconcile that with TOOL-2 §4's rejection of exactly that choice. |
| M11 | medium | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:28-32` | S4 states gate-lint is "absent from the trio **and from `AGENTS.md` and `README.md` entirely**" and then scopes only "the template placement, the customize row, and the charter's kit list". §4 Files touched (`:112`) carries `AGENTS.md`; `README.md` appears in no Files-touched row and in no §3 non-goal — a named defect silently dropped. Nothing observes the charter half either: AC1 quantifies over the three playbook files plus the waiver file, AC2 over `customize.md`, AC3 over kit READMEs, AC4 over cross-references, AC5/AC6 are gates. §7 (`:166`) leans on `drift_report.py --check`, but the relevant signal matches gate-leg argv paths against the gate-suite section and is blind to a missing kit bullet in "What ships here". A builder can skip `AGENTS.md` entirely and pass. | Add an AC greping `AGENTS.md` for a `gate-lint` bullet in the "What ships here" list. Either add `README.md` to scope and Files touched, or move it to §3 with a follow-up row so the defect is deferred deliberately rather than lost. |
| M12 | medium | `memory/builds/aSiftedPlaybook/README.md:109-111` | "At landing, `PLAY-aCandidStub-2`'s backlog row needs rewriting." TOOL-1 §8 F2 (`:230-233`) repeats it as "a landing task". Verified: `memory/backlog/PLAY.md:7` reads "…the template is effectively FULL at v2.5 and the §11 externalization spent the cheap slack…". TOOL-1's §2 (S1-S8), §4 Inventory (`:67-79`) and §6 (AC1-AC8) contain no `memory/backlog/PLAY.md` row, and no other spec claims it. The build's own README (`:20-23`) argues that a constant changed under records that cite it "leaves them reading as current guidance" — this is that failure mode inside the unit whose purpose is to prevent it. | Add the row rewrite to TOOL-1 beside S6, list `memory/backlog/PLAY.md` in §4 Inventory, and add an AC observing the rewritten row's new justification. |
| M13 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:123-124` | "Make the ceiling a `.memory-tree.conf`-style declared pin. Rejected as out of scope: it is a real improvement and it is `TOOL-aSiftedPlaybook-3`'s territory". TOOL-3 does not accept it: §2 is S1-S5 with no ceiling pin, and §3 (`:57-58`) forecloses growth — "Extending the pair list beyond the two seeded rows … is ordinary maintenance, not this unit's job." No backlog row is specced either, so the improvement is deferred to a unit that declines it. The prior art the deferral ignores is live: `.memory-tree.conf:43-50` declares `READ_PATH_CEILING="71891"` for the charter's mandatory read path, with the in-conf comment recording each of its three raises and why ("a session's mandatory reading is a budget, and two binding docs growing at once spends from it visibly"). That is this repo's established answer to a mandatory-reading byte budget under pressure, and its pattern — declared number, per-raise written justification beside it, check still reds — is structurally what TOOL-1 rejects. No spec's §10 recall terms would surface it. | Either scope the declared pin into TOOL-1 (moving `MAX_BYTES` into a declared conf with the raise's justification recorded beside it), or record an explicit follow-up backlog row. Cite `READ_PATH_CEILING` in TOOL-1 §10 as the prior art. |
| L1 | low | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:62-63` | "Twelve tracked lines outside historical records, in **seven** files." Measured: `git grep -nE '32768\|32 ?KiB\|32KiB\|never raise' -- ':!memory/builds' ':!memory/archive' ':!memory/DECISIONS.md' ':!memory/backlog'` returns exactly 12 lines across **eight** distinct files (SESSION-KICKOFF `:99,:100` · AGENTS `:16,:97,:178` · README `:12` · baseline.toml `:35` · MAP.md `:53` · inventories.json `:46` · check-template-size.sh `:8,:14` · gate-legs.json `:17`). The twelve is right; the seven is not. The table below it lists nine File values (DECISIONS.md included) whose ranges cover ~25 lines, so the preamble is also ambiguous about what it counts. | "Eight files, plus the append-only `memory/DECISIONS.md`", or delete the count and let the table be the inventory — the `AGENTS.md` precedent PLAY-1 §4 cites for the hygiene number. |
| L2 | low | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-3.md:21-24` | "the kit `lib` scores seven hits, **every one of them** the `lib` inside 'deliberate' and 'deliberately'." Measured with `grep -oE '[A-Za-z]*lib[A-Za-z]*'` over the trio: template → deliberate, **stdlib**, deliberate, deliberate; domain-rules → deliberately ×3. Seven total, but the second is `stdlib` at `parallel-coding-governance.template.md:109`. Six of seven. The conclusion is unaffected. | "Six of them inside 'deliberate'/'deliberately' and one inside 'stdlib'" — two distinct false-positive words strengthens the point. |
| L3 | low | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-2.md:37-38` | "`domain-rules.md`'s **five** `main` hits and `customize.md`'s three are all other senses." Measured: `grep -o 'main' parallel-coding-governance.domain-rules.md \| wc -l` = **4** — `domain` at `:1` and `:5`, `maintained` at `:33`, `push-main` at `:100`. The customize figure of three is correct. The non-goal's conclusion holds for all four. | "Four", and name `push-main` at `:100` explicitly — it is a script name a careless global substitution would corrupt. |
| L4 | low | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-1.md:80-81` · `:220-222` | rev-2's log records two stale-19 follow-ups ("`tools/memory-tree/README.md:6` and root `README.md:33`"); §3 Non-goals names only the kit README. Verified both: `README.md:33` = "…and a 19-check hygiene gate." and `tools/memory-tree/README.md:6` = "…a 19-check hygiene gate that keeps it that way", against the true 20. `memory/TEMPLATE-SPEC.md` makes §3 the explicit cut-line; a carrier declared out of scope only in a revision log is cut nowhere a builder reads — and root `README.md` is the adopter-facing front door TOOL-1 S4 is separately editing at `:12` in the same build. | Add root `README.md:33` to §3 beside the kit README line, or fold it into TOOL-1 S4. |
| L5 | low | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-2.md:223-225` | §10 still offers the absent playbook dossier as "evidence for `TOOL-aSiftedPlaybook-1` F1 **option 3**, which would mint one". Option 3 was rejected by the owner (README `:96`), and once TOOL-2 lands the same paragraph's "the map has seven and none covers this product" is false. | Re-date the paragraph as a BASE-time observation and drop the option-3 pointer. |

## Folds — rev-bump instructions

- **`memory/builds/aSiftedPlaybook/README.md`** — B1 (re-point or delete the marker-owner claim once the lane is ordered), H1 (add template-lane edges to the Units table), H12 (commit the audit + add the eleven-to-scope coverage table), M1 (delete the "unit-internal" fork category; route the three forks), M12 (name the owner of the `PLAY-aCandidStub-2` row rewrite). No rev header; update in place with a note in the build log.
- **`spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md` → rev-4** — B2 (name S8's constant and value), H2 (§4 Inventory row + S5 restated by sentence), H3 (AC3 alternation + a positive message AC), H5 (reconcile AC6 and §10), H7 (baseline.toml exception into S6/AC6), M2 (1593 → pointer), M3 (`AGENTS.md:16-17`), M9 (drift-signal AC for the `:97` rewrite), M12 (backlog row as a scope item + AC), M13 (`READ_PATH_CEILING` prior art in §10, or a follow-up row), L1 (file count).
- **`spec/2026-08-16-spec-TOOL-aSiftedPlaybook-2.md` → rev-4** — H11 (A6/A7 discovery route + a red arm), M4 ("Five" → seven; `:38`), M7 (add S5 for the dossier mint).
- **`spec/2026-08-16-spec-TOOL-aSiftedPlaybook-3.md` → rev-5** — H4 (waiver file consumed, never created; PLAY-3 prerequisite; refusal AC; row-format pin), M10 (two legs, two keys, two citations), L2 (`stdlib`).
- **`spec/2026-08-16-spec-PLAY-aSiftedPlaybook-1.md` → rev-3** — H1 (drop "Independent of every other unit"; declare the `:51` and `customize.md:61` collisions), H9 (AC2 payload + fixture + exit codes), H10 (measure the byte cost), M5 (S4 source-of-truth cell + AC1 exemption), L4 (root `README.md:33` into §3).
- **`spec/2026-08-16-spec-PLAY-aSiftedPlaybook-2.md` → rev-4** — H1 (template-side ordering dependency; re-express AC2(b) by sense rather than line/count), H8 (S5 for `WIRE-INTO-PROJECT.md:98` + AC), L3 (four, not five), L5 (§10 option-3 pointer).
- **`spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md` → rev-3** — B1 (S8 marker bump + archive snapshot + §4 rows + AC), H6 (restate id 19's three grounds), M6 (drop `workflows/` from the S7 seed), M8 (per-option cost in F1), M11 (`AGENTS.md` AC; `README.md` into scope or §3).
- **`spec/2026-08-16-spec-PLAY-aSiftedPlaybook-4.md`** — no findings this round.

## Checked and clean

- **Every line-number citation spot-checked** in TOOL-1 §2/§4/§6/§10, PLAY-1 §2/§4/§6, PLAY-2 §2/§4/§6/§10, PLAY-3 §2/§4/§6/§8, TOOL-2 §2/§4/§6, TOOL-3 §2/§4. Beyond the findings above, the cited targets resolve: `parallel-coding-governance.template.md:12` and `.domain-rules.md:3` both carry `v2.7`; `.claude/SESSION-KICKOFF.md:107-109` is the drift-signal parenthetical; `AGENTS.md:7,16,97,178`; `README.md:12,33`; `tools/gate-legs.json:3,17`; `tools/memory-tree/README.md:6,18`; `memory/map/baseline.toml:1-4,35`; `memory/map/generated/{MAP.md:53,inventories.json:46}`; `tools/check-template-size.sh:8,14,17-19,22-26,29`; `.memory-tree.conf:43-50`; `tools/drift-audit/drift_signals.py:70-100,145`; `memory/backlog/PLAY.md:7`; `memory/builds/aCandidStub/reviews/2026-08-10-review-aCandidStub-1.md:222-224`; `memory/builds/aPrunedCeremony/build/2026-07-19-build-PLAY-aPrunedCeremony-1.md:25`; `WIRE-INTO-PROJECT.md:98`.
- **The reversal itself is honestly scoped.** TOOL-1 §4's verified negatives hold: the three `parallel-coding-governance*.md` files, `WIRE-INTO-PROJECT.md`, every kit `*.template.md` and every rendered `.claude/skills/**` state the limit nowhere — reproduced. The raise touches no shipped adopter artifact.
- **PLAY-2's measurements at BASE are all correct** — 30 `main` substrings, 11 inside `domain` (10 filename + `:4`), 19 word-boundary hits on 16 lines, 2 non-branch senses, 17 branch senses on 14 lines, +238 bytes (17 × 14), and `:51` carrying two senses on one line. AC1's line-vs-occurrence split (17 / 14) is right.
- **PLAY-3's `lib`/`hooks` waiver rows are correct** — both score zero path-segment hits across the trio; only `workflows/` is redundant.
- **TOOL-2's A1/A2/A4 discovery discipline is sound** — the gate's OK line at `tools/check-template-size.sh:29` really does print `%d / %d bytes`, so reading `MAX_BYTES` back out of it works with no override, and A4's CRLF arm correctly targets the `tr -d '\r'` normalization at `:17-19`.
- **TOOL-3's dossier collision was already resolved correctly** (`:106`, `:115-119`: EXTENDED, never created, reds and stops if absent) — the defect is that the same treatment was not applied to the waiver file.
- **The build-level rules hold as written**: `memory/DECISIONS.md` append-only, the `non_terminal_specs_cited_by_product_source` pin and its `PRODUCT_GLOBS` reasoning, the "each cell is a label, not a description" discipline on the Units table.
- **Round 1's nine folds verified as landed** — each was re-checked against the current spec text and none has regressed. The three that were folded into only one of two carriers are re-reported above as H2, H5 and B1.
- **The twelve refuted findings from this round's refute pass were not folded** and are not restated here.

## Coverage

This round ran **four lenses**, closing the gap round 1 left when 7 of 8 agents were lost to a usage limit.

| Lens | Survivors | After dedup |
|---|---|---|
| Factual accuracy (line numbers, constants, gate behaviour verified against source) | 14 | 8 unique |
| Decomposition / fork authority (M2 one-mechanism-per-spec, M3 fork ownership, unit boundaries) | 12 | 8 unique |
| Unstated assumptions / prior art (premises the specs rely on but do not state or check) | 13 | 9 unique |
| Underspecification (scope items, ACs and deliverables that cannot be built or observed as written) | 13 | 7 unique |

52 findings survived the adversarial refute pass; 12 were refuted and are recorded in the review's refuted list, not folded. The 52 collapse to **32 distinct defects** after merging cross-lens duplicates — notably the marker bump (found independently by three lenses), the SESSION-KICKOFF range (four), the WARN threshold (three), and the template-lane ordering (three). Nothing was left UNVERIFIED. Not examined this round: the Tier-2 production-readiness checklists (§5) of any unit beyond the items above, PLAY-4 in depth, and whether the eleven audit defects are individually correct — that last is blocked by H12 until the audit is committed.