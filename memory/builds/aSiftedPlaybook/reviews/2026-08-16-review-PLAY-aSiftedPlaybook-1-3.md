# Review — aSiftedPlaybook spec set, round 3 (M4 spec audit)

**Serves:** spec-audit PLAY-aSiftedPlaybook-3 TOOL-aSiftedPlaybook-1 TOOL-aSiftedPlaybook-2 PLAY-aSiftedPlaybook-1 PLAY-aSiftedPlaybook-2 TOOL-aSiftedPlaybook-3

## Verdict: BLOCKED

*Workflow `wf_bcdc00bc-ef1`, scoped to the `4b38e39..HEAD` delta. Four lenses -> three batched skeptics -> one synthesis; all 8 agents completed. **60 raw, 46 confirmed, 14 refuted, 0 unverified.** Per lens: new-material 14, factual 14, cross-unit 16, premise 16.*

*The run launched BEFORE the `origin/main` reconcile and the tree moved under it; the synthesis re-derived every anchor at `2c9d833`, so the findings below are against the reconciled tree. All three blockers and the highs were folded at `d429a1e`; the full bar is green 55/55 after.*


*Synthesis pass over the `4b38e39..HEAD` delta (six specs, the build README, the new audit report). Four lenses, findings adversarially refuted before folding; 14 refuted and dropped. Every line anchor below was re-derived against the working tree at `2c9d833` — several survivor citations were themselves stale and have been corrected here.*

Three blockers. Two of them share one root cause the `2c9d833` reconcile missed: it re-derived path anchors but not the two **populations** this build quantifies over, and `main` grew a twelfth kit underneath it. The third is an acceptance criterion no unit can observe.

| id | sev | file:line | finding | fix |
|---|---|---|---|---|
| B1 | blocker | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:99` | **A twelfth kit dir arrived from main and no spec knows it exists.** §4:99 says "`tools/` holds eleven tracked kit dirs" and §1:7 "Four of the eleven". The derivation both PLAY-3 AC1 (:182) and TOOL-3 S1 (:15) name as authoritative — `tools/check-install-prefix.sh:38`, `git ls-files -- 'tools/*/*' \| awk -F/ 'NF>2 {print $2}' \| sort -u` — returns **twelve** at HEAD; the twelfth is `govkit` (`a4caea9`, merged at `8712ac0`). Scored under TOOL-3 S1's own anchored path-segment rule over the trio, seven kits score 0: agent-instructions, drift-audit, gate-lint, hooks, lib, pytest-parallel-guardrails, **govkit**. PLAY-3 S1-S4 name four; S7 (:43) waives `lib/` and `hooks/` only. `govkit` is in no scope item, no waiver seed, no §3 non-goal, and `grep -rn govkit memory/builds/aSiftedPlaybook/` returns nothing. PLAY-3 AC1 is therefore false at its own DoD, and TOOL-3 AC1 (:175) reds the merge bar the day unit 7 lands. | Re-derive the population from the tree instead of restating a count; delete both spelled counts (§1:7, §4:99) — a hardcoded kit count inside the unit that exists to close hardcoded counts is the same defect. Then give `govkit` a disposition: a template placement + customize row on the S4 pattern (it is the deployer, arguably the most adopter-facing kit in the tree), or a third seed row in S7 with its reason. Re-check TOOL-3 AC1 against the same enumeration. |
| B2 | blocker | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:124` | **Four new depth-1 files under `tools/` red the `govkit selfcheck` merge-bar leg, and no spec touches the registry.** `tools/govkit/registry.toml:22-32` declares the asserted surface as `tools/*` (depth-1) and `govkit.py:516-519` fails with "tracked path '…' is in the declared surface but is neither an entry member nor an exemption — a new moving part must red until a declaration claims it". This build creates four such paths: `tools/template-size-highwater.txt` (TOOL-1 S8), `tools/playbook-kit-waivers.txt` (PLAY-3 S7), `tools/check-playbook-parity.sh` and `tools/check-playbook-parity.test.sh` (TOOL-3 S2/S4). `grep -rn 'govkit\|registry.toml' memory/builds/aSiftedPlaybook/` returns nothing: no §4 Files-touched row, no §7 gate line, no AC. The leg is live at `tools/gate-legs.json:525`, so `run-gates.sh` — an acceptance item in TOOL-1, PLAY-1 AC7, PLAY-3 AC6 and TOOL-2 AC4 — goes red in three separate units. Precedent exists in both shapes: `tools/install-prefix-waivers.txt` is a member of the `check-install-prefix` entry's file rules (`registry.toml:113-114`), and `tools/check-template-size.sh` is an `[[exempt]]` row (`:152`). | Add `tools/govkit/registry.toml` to §4 Files touched and `python tools/govkit/govkit.py selfcheck` to §7 Gates in TOOL-1, PLAY-3 and TOOL-3, each with the declaration it owes: an `[[exempt]]` row for the high-water file (its gate is already exempt, and exemptions are per-path) and entry/exemption rows for the waiver registry and the new parity gate + self-test. Add an AC observing the leg green. |
| B3 | blocker | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:230` | **AC9 quantifies over a state its own unit cannot reach, and no later unit re-seeds.** S8:80-81 ships the high-water "seeded with the size measured at the end of THIS build"; AC9:230-233 requires a quiet gate "at the end of this build". TOOL-1 is unit **1** of 7 in the README's TOTAL order (`README.md:70`), and the template is not edited until units 4-6 (PLAY-2 +238 measured, PLAY-3 ~2043 gated). Both horns fail: seed at the measurable 32682 and every run from PLAY-2 onward prints WARN forever — the permanently-red decoration S8:75-78 exists to avoid; seed at the forecast ~34963 and the ratchet sits above the tracked size through every spend, which is the silence F2 was resolved to end, and carries a number out of a spec paragraph in violation of `README.md:125-126`. `grep -rn 'high-water\|highwater\|--bump' memory/builds/aSiftedPlaybook/` finds no scope item, AC or Files-touched row in PLAY-1, PLAY-2 or PLAY-3. | Name the seeding act and its owner. Give PLAY-3 — the last template-touching unit, which already owns the marker bump as S8/AC7 — a `--bump` scope item with a Files-touched row and an AC observing a quiet gate afterwards, and re-word TOOL-1 S8/AC9 so unit 1 seeds at ITS landed size and states that units 5 and 6 are expected to fire the warn. |
| H1 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:66` | **One un-keyed high-water record, two gate consumers.** S1:18-24 reasons carefully that `tools/gate-legs.json:23-29` runs the same script over `skills/session-kickoff/SKILL.md` at 18432 and is insulated from the raise by the positional. S8:66-81 then specifies a single tracked file holding "the template's size" with no binding to which file was measured; §4:124 is likewise singular. Measured: SKILL.md is 18215 bytes against a template of 32682, so the kickoff leg can never warn under a template-sized high-water, and `--bump` invoked on that leg's argv rewrites the record to ~18215 — after which the template leg prints WARN on every run forever. TOOL-2's A6-A8 (`spec-TOOL-…-2.md:30-32`) name `H` with no file binding and inherit it. | Key the record by measured file (`<path>\t<bytes>` rows), state the shape in S8, say what the kickoff leg's ratchet behaviour is, and add an arm in TOOL-2 S2 proving a bump on one subject leaves the other untouched. Extend AC7 to the second consumer the way S1 extended the constant. |
| H2 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-2.md:69` | **"An undeclared floor is its own refusal" is false, and it is the entire justification for S6's mandatory conf entry.** Verified: `tools/memory-tree/check-arms.py:277-279` reads `want = floors.get(gate_rel)` / `if not want:` / `continue` — a discovered gate with no `ARMS_FLOORS` entry is silently skipped. The only refusals are a missing sibling test (:172-173), an unarmed unpinned branch, and a floor naming a gate NOT in the population (:269-273 — the opposite direction). This spec's own §4:93 states the truth two sections earlier ("an undeclared floor is the quietest way to be outside a pin"); TOOL-3 AC8:196 repeats the false claim. Consequence: TOOL-2's AC1-AC6 (:158-171) contain no criterion that reads `.memory-tree.conf` or runs `check-arms.py`, and §7:180 still gates it on a resolved conditional — so the whole S6 half can be skipped with every AC and `run-gates.sh` green. | Delete the false premise from TOOL-2 S6, §7:180 and TOOL-3 AC8. Replace it with the refusals that do exist, and add an AC to TOOL-2 that observes the entry directly: `python tools/memory-tree/check-arms.py --report` names `tools/check-template-size.sh` with a numeric floor rather than `unset`, and lowering the floor by hand reds `--check`. |
| H3 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-2.md:41` | **The arms require a scratch high-water file; nothing specifies how to point the gate at one.** S2:38-42 requires "Each arm runs against a scratch copy of that file, never the tracked one", and A8 (:32) runs `--bump`, which writes. TOOL-1 S8:66-68 gives the file a fixed repo-relative path with no positional and no env var — unlike `MAX_BYTES`, whose three-layer resolution S1:14-24 documents precisely because a gate leg cannot set an environment variable (`tools/check-template-size.sh:14-19`; `ROOT` resolves from `git rev-parse --show-toplevel` at `:12`). A8 as written either writes into the tracked file or cannot run. This is round-2 H11's class recurring one mechanism over. | Specify the high-water path's resolution in TOOL-1 S8 the way S1 specifies `MAX_BYTES`'s — a third positional or an env var, tracked path as default — including the `--bump` write target, and cite that mechanism in TOOL-2 S2 as how the arms get their scratch copy. |
| H4 | high | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:59` | **S8 and AC7 specify two different archive blobs, ~2.3 KB apart.** S8:59-60 copies "the **pre-bump** template" to `memory/archive/parallel-coding-governance.template-v-2-7.md`; AC7:205-206 requires it "byte-identical to the template **as it stood before this build's first template edit**". PLAY-3 is unit 6, after PLAY-1 and PLAY-2 have both edited the template, so pre-bump ≠ pre-first-edit. Precedent settles it in S8's favour and was measured: the v-2-6 archive was added by `5ed9b4b`, and `git show 5ed9b4b^:parallel-coding-governance.template.md` is byte-identical to it. Under AC7 as written a builder archives the BASE blob as v2.7, and the content actually labelled v2.7 is never archived. | Re-word AC7 to match S8 and precedent: byte-identical to the template at the parent of the bump commit, naming `git show <bump>^:parallel-coding-governance.template.md` as the observation. |
| H5 | high | `README.md:131` | **The build-level PRODUCT_GLOBS rule is false and contradicts TOOL-1 §7.** The README enumerates the globs as "`tools/`, `skills/`, `.claude/`, the three playbook files and `WIRE-INTO-PROJECT.md`" and concludes "`AGENTS.md` and `memory/` are outside the globs and may cite freely". Source at HEAD, `tools/drift-audit/drift_signals.py:20-33`, also lists `"memory/guides/SESSION-KICKOFF.md"` with the comment "The kickoff manifest, by FILE path." TOOL-1 §7:265-266 says the opposite and is correct. Five of seven units re-stamp `last-audit` in that file (PLAY-1:114, PLAY-2 §7, PLAY-3:152, TOOL-2:127, TOOL-3:122), so the README is the carrier that would let one of them write a non-terminal spec id into a file sitting at a zero-tolerance pin. | Add `memory/guides/SESSION-KICKOFF.md` to the enumeration and replace "`memory/` are outside the globs" with "`memory/` except `memory/guides/SESSION-KICKOFF.md`". |
| H6 | high | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-1.md:83` | **§3 cuts root `README.md:33` by routing it to a unit that does not receive it — round-2 B1's shape, introduced by the fold meant to close L4.** §3:80-85 (new at rev-5) says "`TOOL-aSiftedPlaybook-1` S4 is separately editing that file at `:12` in this same build, so **that unit is the cheaper home for `:33`**". TOOL-1 S4 reads in full "`README.md:12`" (`spec-TOOL-…-1.md:38`); its §4 row is `README.md \| 12` (:122); no AC mentions `:33` or `19-check`, and AC3's alternation (`32768\|32 ?KiB\|never raise the limit\|[Dd]o NOT raise\|raise the limit\|inflating`) cannot match it. A third carrier disagrees: the audit report (`build/…-playbook-audit.md:80-82`) records `README.md:33` as a follow-up, not fixed here. Verified live: `README.md:33` still reads "and a 19-check hygiene gate." against the true 20 (`tools/gate-legs.json:3`, `tools/memory-tree/README.md:18`). Both units pass with the defect shipped and a reader believes it is handled. | Add `README.md:33` to TOOL-1 S4 with a §4 inventory row and an AC greping `[0-9]+-check` over root `README.md`, and correct the audit report's bullet to say the carrier was pulled into scope. Or drop the routing sentence from PLAY-1 §3 and leave it a follow-up in all three places — but not two answers. |
| H7 | high | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:103` | **§4 still puts `workflows/` on the exemption side against S7's "`lib/` and `hooks/` ONLY".** S7:43-49 (rev-3, folding round-2 M6) and §4 Files touched (:149) both say the seed is two rows, because `workflows` scores a path-segment hit. §4:102-105 was never folded: "Three kits are legitimately not adopter-facing … `lib/` …, `hooks/` … and `workflows/`". Reproduced: `parallel-coding-governance.template.md:150` spells `tools/workflows/tier2-review.js`, so `workflows` scores 1 and needs no waiver. A builder working §4 seeds three rows and TOOL-3 AC6's second arm (`spec-TOOL-…-3.md:186-190`) reds the third as excusing nothing — the gate this build ships failing on the registry this build seeds. | Rewrite §4:102-105 to two exempt kits and record that `workflows` is covered by template `:150`. Re-derive the exempt set against the twelve-kit population (see B1) rather than restating it. |
| H8 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:247` | **AC11's two load-bearing citations are both wrong and its uniqueness premise is false, so the risk it guards cannot occur as described.** AC11 cites `_charter_mentions_every_leg` at `tools/drift-audit/drift_signals.py:70-100`; at HEAD that range is the `SHRINK_ONLY` dict and `DECLARED_EMPTY` set — the function is at `:104-136`, and its argv-path match is at `:132-136`. The mechanism claim is right; the uniqueness claim is not. AC11 says "`:97` is that section's only carrier of `tools/check-template-size.sh`"; the merge added `AGENTS.md:98`, which spells `tools/check-template-size.sh skills/session-kickoff/SKILL.md 18432`, so the substring appears at **both** `:97` and `:98`. A reflow away from `:97` therefore does not red the signal, and the AC passes for the wrong reason. The same rev-5 block cites `:76-80` for the permanently-red-decoration phrase, which is at `:113`. | Re-cite `:104-136` and `:113`. Restate the premise: at least one surviving line in the gate-suite section must spell `tools/check-template-size.sh` verbatim after S3's edits, not `:97` specifically. |
| H9 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:239` | **AC6 asserts an identity its own §10 falsifies.** AC6:236-240 (rewritten to close round-2 H5) requires the decision row to name "all four records" and adds "§10's recall result and this list are the same four; a mismatch between them was how RD7 nearly went unrecorded." §10:377-382 was not touched and reads "The query returned **the three records** this unit's §2 S6 must name" — the `aCandidStub` review's refuted id 19 is absent from it. The fold moved one carrier of a two-carrier claim and then asserted the two are identical, inside the acceptance criterion for an append-only record. | Extend §10 to "the four records" with id 19 and its citation (`memory/builds/aCandidStub/reviews/2026-08-10-review-PLAY-aCandidStub-1-1.md:222-224`), or delete AC6's identity clause and let AC6 be the sole enumeration. |
| H10 | high | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-2.md:35` | **A tracked OPEN backlog row says 14, the audit's R2 says 13, and PLAY-2 S5 ships 13 into an adopter-facing file.** `memory/backlog/PLAY.md:8` — `PLAY-aSealedCaravan-1` · OPEN — records that `customize.md` and `WIRE-INTO-PROJECT.md` "give the companion's share as 13 of 36 where the measured value is 14". `grep -rn aSealedCaravan memory/builds/aSiftedPlaybook/` returns nothing: no spec, README row or coverage entry cites it. R2 (`build/…-playbook-audit.md:62`) refutes it — "13 is the companion-only count" — and S5:34-35 then edits that exact claim and preserves it ("the **13 stays 13**"), with AC7:178-179 checking it. Measured: `domain-rules.md` carries **14** unique placeholders (13 companion-exclusive plus the shared `{{MEMORY_ROOT}}`), which PLAY-2's own §4:67 and PLAY-4 §4:50-53 both state. So `WIRE-INTO-PROJECT.md:98`'s "the companion **carries** 13 of the 36 placeholders" is false against this build's own measurement, and `customize.md:15`'s "13 … sit unfilled in the companion" undercounts by one after a template-only grep. The row's first clause is the audit's A3, which PLAY-4 S1/S2 fix — so the build closes a tracked row's subject and leaves the row OPEN. | Reconcile before ratifying. Downgrade R2 from refuted to confirmed for the "carries" wording, give the 13→14 correction a scope item (PLAY-4 S1 is the natural home — it already restates the arithmetic), and name `PLAY-aSealedCaravan-1` in the coverage table with a disposition, on the precedent TOOL-1 S9 sets for a falsified backlog row. |
| M1 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-2.md:66` | **S6 miscounts the gate's exit sites and omits two requirements the population predicate imposes.** S6:66-67 says "three `echo`+`exit` sites"; the gate has **two** exit paths (`tools/check-template-size.sh:22`, `:33`) and **four** echo lines (`:22`, `:30-32`). Neither count is three, and the branch count is what the floor pins. Unstated: `check-arms.py`'s discovery predicate is `FAIL_RE = re.compile(r'\bfail (\d+) "(.*)$')` (`:53`), so every call site needs a **check number** the gate has never had, and `branches()` raises unless each message carries a literal run of ≥12 chars (`:153-156`). The `ARMS_FLOORS` grammar `<gate>:<branches>:<armed>` (`parse_floors`, `:200-206`; live at `.memory-tree.conf:87`) is never stated, so the spec does not say enough to produce a correct entry. | Delete the site count (the gate's source is the count), state that the refactor numbers the checks and how, require each message to carry an assertable literal run, and spell the entry as `tools/check-template-size.sh:<branches>:<armed>` with values read from `--report` after the refactor. |
| M2 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-3.md:295` | **The waiver row's key spelling is unpinned, and the two specs write it two ways.** §10:295 gives the format as "one `<key>` per row with a reason after whitespace", modelled on `tools/install-prefix-waivers.txt` — whose real keys are `<path>:<line>` (`.githooks/pre-commit:37`, `tools/check-wiring.sh:158`), a different shape from a kit name. S1:15-16 derives the kit set from `tools/`, yielding bare names (`lib`, `hooks`), while PLAY-3 S7:43 and its Files-touched row (:149) both spell the seed `lib/` and `hooks/` with a trailing slash. Nothing states whether the comparison is verbatim, so both specs can be satisfied and still not join — a waiver matching nothing leaves the kit unexcused and reds AC1, the vacuous-selector class S1 exists to prevent. | Pin the grammar once with a worked example: bare kit dir name as derived, reason after whitespace, comparison verbatim. Make PLAY-3 S7 and its Files-touched row use that identical spelling. |
| M3 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:66` | **S8 never says what the gate does when the high-water file is missing or malformed.** Not hypothetical: `tools/check-template-size.sh:11` sets `set -u` and the numeric comparison at `:28` is `[ "$bytes" -gt "$MAX_BYTES" ]`, so an empty operand is a shell error, not a graceful skip — on a merge-bar leg that runs twice. §4:157-158's Migration note ("a project that copied the gate keeps its own number") describes a tree with no high-water file at all. TOOL-2 arms a missing TEMPLATE (A3, exit 2) but nothing arms a missing or malformed record. | State the contract in S8 — absent file means no ratchet plus one explicit line, non-numeric content is a named failure — and add the matching arm to TOOL-2 S2 beside A3. |
| M4 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-2.md:158` | **A6/A7/A8 are the only arms with no proof they can fail, and AC1 cannot detect an omitted arm.** §1 quotes the repo's rule that "a gate you have only ever seen pass is an assertion about nothing"; A1/A2 get a red proof (AC2:160, invert `-gt`) and A4 gets one (AC3:163-164, remove `tr -d '\r'`). The three ratchet arms get neither — AC2's inversion touches `:28`, not the warn branch. AC1:158-159 requires "one line per arm", but S2's lead sentence (:19-21) deliberately deleted the arm COUNT, so nothing tells a reader how many lines to expect. A6-A8 can be omitted entirely and AC1-AC6 all pass. | Add an AC on AC3's model: inverting the warn comparison or deleting the high-water read reds A6/A7 by name, and making `--bump` a no-op reds A8. If the count stays unspelled, have AC1 assert one line per row of S2's table. |
| M5 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-2.md:122` | **§4 and §7 still gate both resolved forks on conditionals.** §8 F1:188-189 is "RESOLVED … yes, refactor. Built as S6"; F2:206-208 fixes S5 as the minter. Three carriers were not folded: §4:122 "`tools/check-template-size.sh` \| only under F1 — the `fail()` refactor"; §4:125 "`memory/map/features/playbook.md` \| **new or extended**, per the coupling above"; §7:180-181 "`check-arms.py` — **only if** F1 resolves to the `fail()` refactor". §7 is the section a builder reads to know which gates must run. | Make §4:122 and §7:180 unconditional and change §4:125 to "new — minted by S5". |
| M6 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:308` | **§8 F2 still describes the pre-round-2 mechanism and still routes the backlog rewrite to nobody.** F2:307-317 reads "a soft WARN threshold in the gate, built as S8" and then "a file above the **soft threshold** and below `MAX_BYTES` must exit 0". S8:66 is explicit that this is "a HIGH-WATER RATCHET … **Not a fixed threshold**", and `README.md:143` records the refinement. The same paragraph (:314-317) still calls the `PLAY-aCandidStub-2` rewrite "a landing task" with no pointer to S9 (:82-88), which rev-5 added because a landing task belonging to no scope item is the failure §1 names. | Annotate F2's resolution with the round-2 refinement, replace "soft threshold" with "recorded high-water", and point the backlog sentence at S9/AC10. |
| M7 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:40` | **S5 says the manifest moved to the path it says no longer exists.** S5:39-41: "The manifest moved to `memory/guides/SESSION-KICKOFF.md` … the old `memory/guides/SESSION-KICKOFF.md` no longer exists." A path-substitution artifact from the `2c9d833` reconcile. Verified: `.claude/SESSION-KICKOFF.md` is gone, `memory/guides/SESSION-KICKOFF.md` exists (12858 bytes) and its trap is at `:121-123` as S5 states. | Restore the old spelling in the second clause: "the old `.claude/SESSION-KICKOFF.md` no longer exists". |
| M8 | medium | `build/2026-08-16-build-aSiftedPlaybook-1-playbook-audit.md:84` | **The `session-kickoff/SKILL.md` follow-up describes a defect that does not exist.** The bullet records that SKILL.md "says the spec canon is nine sections where `memory/TEMPLATE-SPEC.md` has been ten since `SPEC10_CUTOFF`". `skills/session-kickoff/SKILL.md:151` says the opposite: "the template states its own section count and the gate that enforces it; **do not restate that number here, because it has already gone stale in three other carriers.**" `git log -S "nine sections" -- skills/session-kickoff/SKILL.md` returns nothing, so the string was never there. (The `SPEC10_CUTOFF=2026-08-04` half is correct — `tools/memory-tree/check-memory-hygiene.sh:506`.) This is the one fabricated claim in a report whose stated purpose is to make completeness falsifiable. | Delete the bullet, or replace it with the real state: SKILL.md:150-152 already implements the correct pattern. If a follow-up row was minted from it, withdraw the row. |
| M9 | medium | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-2.md:17` | **S1's anchor was path-substituted but not re-derived.** S1:17 says "this repo's own filled manifest carries the result at `memory/guides/SESSION-KICKOFF.md:39`". Measured: `:39` is "> moves, an observed behavior; *not* an unrelated green check): …"; the default-branch line is `:52` — "- **Remote · default branch:** `origin` · `main`." S1's whole argument, that `{{DEFAULT_BRANCH}}` is not invented, rests on a builder finding that line. | Change to `:52`, or cite by content ("the `Remote · default branch:` line") since five units re-stamp that file. |
| M10 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:177` | **The `READ_PATH_CEILING` prior-art citation points at the wrong block and undercounts its movements.** §2:174-181 and §10:369 both cite "`.memory-tree.conf:43-50` declares `READ_PATH_CEILING` … and records each of its three raises". `:43-50` is the `corpus_ids.py` block (`CHARTER`, `ORPHAN_ID_PIN`); the constant is at `:71` with its justification at `:57-70`. The comment records **four** movements: ":58 It has grown three times", then ":65-70 The fourth movement was a RAISE and a LOWER inside one build". The quoted sentence is accurate and sits at `:62-63`. The omitted fourth is materially the one that matters: it is the kickoff-manifest relocation this spec's S5 is re-anchored against. | Cite `.memory-tree.conf:57-71`, say four recorded movements, and note the fourth is the manifest move S5 depends on. |
| L1 | low | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:211` | AC3's justification cites `tools/check-template-size.sh:4` for "never inflating the template" and `:25` for "Do NOT raise the limit". Measured: `:5` and `:31` (`:4` is the companion-prose line, `:25` is the `tr -d '\r'` measurement). The same spec's S2:25-26 and §4:120 were reconciled to `:2-5` and `:31-32`, so the spec cites two locations for one string. Lines 1-10 are unchanged since `4b38e39`, so `:4` was wrong when written. | Re-cite `:5` and `:31-32`, matching S2, or drop the anchors and quote the strings alone. |
| L2 | low | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:127` | §4's generated-mirror rows read `memory/map/generated/inventories.json \| 46` and `MAP.md \| 53`. Measured: `inventories.json:50` and `MAP.md:57` — both shifted by the new `kickoff engine size <=18KiB` leg. The blast-radius paragraph's `drift_signals.py:86-92` for argv-path matching is now `:132-136`. The reconcile updated every other row in this table and missed these three. | Update to `:50`, `:57` and `:132-136`, or mark the two generated rows "regenerated, never anchored" since S7 re-renders them. |
| L3 | low | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:367` | §10 calls `memory/map/features/codebase-map.md` "(76 lines, the smallest of the seven)". `wc -l memory/map/features/*.md` returns **nine** at HEAD — the merge added `govkit.md` (120) and `session-kickoff.md` (81). It is still 76 and still the smallest; only the count is wrong. PLAY-2 §10:241-243 handled the same fact correctly by dating it to BASE. | Say nine, or date the observation to BASE as PLAY-2 does. |
| L4 | low | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:109` | §4's cost table prices S1-S5 only and concludes "The gated subtotal is roughly **2043 bytes**", which TOOL-1 §3:101-104 defers to as the owning figure for the whole build's headroom draw. S8:56-62 adds a gated v2.8 line to the template's header paragraph — real structure at `parallel-coding-governance.template.md:3-9`, where the v2.7 and v2.6 entries run ~230 and ~180 bytes — and is unpriced. Whether v2.6 is dropped when v2.8 is added is unstated. | Add an S8 row with a measured figure and say whether v2.6 is dropped, so the one table TOOL-1 points at stays the complete gated total. |
| L5 | low | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:44` | S7:44 keeps the pre-fold measurement: "`lib` scores 0 (all seven apparent hits are the substring inside \"deliberate\")". TOOL-3 S1:25-26 folded round-2 L2 and now reads "six inside \"deliberate\"/\"deliberately\" and one inside **`stdlib`**". Measured: template `:85`, `:140`, `:194` deliberate; `:109` stdlib; domain-rules `:68`, `:72`, `:95` deliberately. TOOL-3 is right. The conclusion is unaffected, but two specs now state different evidence for one measurement — inside the build that exists to close that class. | Mirror TOOL-3 S1's wording in PLAY-3 S7, or point at TOOL-3 S1 rather than restating. |
| L6 | low | `build/2026-08-16-build-aSiftedPlaybook-1-playbook-audit.md:80` | The follow-up says "`WIRE-INTO-PROJECT.md:463` calls `agent-cap` \"the review protocol's TWO rules\" against four"; PLAY-1 §3:77 repeats `:463`. `grep -n "TWO rules"` returns exactly one hit, `:464`. The defect is real; the anchor is off by one. Relatedly R6 (`:66`) puts the `tier2-review.js` install at `:465-475` where the install bullet is `:469-472` (`:465-468` is the REVIEW-PROTOCOL install) — contained, so not false, only imprecise. | Change both citations to `:464` and tighten R6's range to `:469-472`. |
| L7 | low | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:110` | §4's Inventory lead — new in this delta — reads "Tracked lines outside historical records, across the files below plus the append-only `memory/DECISIONS.md` — the table is the inventory and **the count is stated once here** rather than being maintained in two places." No count is stated. rev-5's log (:327-328) still claims "Corrected 'seven files' to eight plus the append-only log", so the log describes an edit the section no longer carries. The de-numbering is the right call (it is what §2 S4 argues for); only the sentence and the log entry disagree with it. | Reword to "the table IS the inventory and no count is stated" and correct the rev-5 log line. |
| L8 | low | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-1.md:14` | §2's provenance sentence reads "D1–D5 come from the 2026-08-16 audit; D6 and D7 were found while writing this spec." The scope list runs S1-S8 carrying D1-D8. D8 is neither: the audit report records it as B2, "Found: discovery `wf_4e13d9e7-550`" (`build/…-playbook-audit.md:50`), and PLAY-1's own rev-2 log (:254-257) agrees. §2 is the one place a reader checks provenance, under a README claiming (`:112`) "no scope item claims an audit provenance it does not have". | "…D6 and D7 were found while writing this spec; D8 came from discovery `wf_4e13d9e7-550` and is recorded as B2 in the audit report." |

## Per-spec rev-bump fold list

- **`TOOL-aSiftedPlaybook-1` → rev-6** — B2 (govkit registry row in §4 + §7 gate + AC), B3 (S8/AC9 re-worded; the bump obligation named as PLAY-3's), H1 (key the high-water by measured file; extend AC7 to the second consumer), H3 (specify the path override in S8 and AC7), H8 (AC11 re-cited at `:104-136`/`:113`; uniqueness premise restated), H9 (§10 extended to four records), M3 (degenerate-case contract in S8), M6 (§8 F2 re-worded to the ratchet; backlog sentence → S9/AC10), M7 (S5's second clause restored to `.claude/SESSION-KICKOFF.md`), M10 (`.memory-tree.conf:57-71`, four movements), L1 (AC3 → `:5`/`:31-32`), L2 (`:50`/`:57`/`:132-136`), L3 (nine dossiers), L7 (§4 lead + rev-5 log).
- **`TOOL-aSiftedPlaybook-2` → rev-5** — H2 (delete the false premise from S6 and §7:180; add the observing AC), H3 (S2 cites TOOL-1's override), M1 (site count, check numbering, literal-run rule, `ARMS_FLOORS` grammar), M4 (red-proof ACs for A6-A8; AC1 anchored to S2's table), M5 (§4:122, §4:125, §7:180 made unconditional).
- **`TOOL-aSiftedPlaybook-3` → rev-8** — B1 (AC1 re-checked against the twelve-kit enumeration), B2 (registry declaration for the new gate + self-test), H2 (AC8's false premise deleted, `--report` named), M2 (row grammar pinned with a worked example).
- **`PLAY-aSiftedPlaybook-1` → rev-6** — H6 (§3 restated: either TOOL-1 receives `:33` or it is a genuine deferral), L6 (`:464`), L8 (D8 provenance).
- **`PLAY-aSiftedPlaybook-2` → rev-6** — H10 (S5/AC7 reconciled against the measured 14 and the OPEN backlog row), M9 (anchor → `:52`).
- **`PLAY-aSiftedPlaybook-3` → rev-6** — B1 (govkit disposition; both spelled kit counts deleted), B2 (registry declaration for `tools/playbook-kit-waivers.txt`), B3 (the `--bump` re-seed as a scope item + AC), H4 (AC7 aligned to S8 and the `git show <bump>^` precedent), H7 (§4:102-105 → two exempt kits), L4 (S8 priced in the cost table), L5 (`lib` measurement mirrored from TOOL-3 S1).
- **`PLAY-aSiftedPlaybook-4` → rev-3, only if H10 routes here** — the 13→14 correction for `customize.md:15` and `WIRE-INTO-PROJECT.md:98`, plus the `PLAY-aSealedCaravan-1` disposition.
- **`README.md`** — H5 (PRODUCT_GLOBS enumeration + the `memory/` sentence), B1/B2 (coverage rows for govkit and the registry obligation), B3 (the seed/bump owner named beside the marker-bump rule).
- **`build/2026-08-16-build-aSiftedPlaybook-1-playbook-audit.md`** — M8 (delete or replace the SKILL.md bullet), H10 (R2 re-adjudicated), L6 (`:464`, R6 → `:469-472`).

## Checked and clean

- **All seven fork resolutions are consistently implemented at the mechanism level.** TOOL-1 F1 → S7's in-place `baseline.toml:35` swap with the three-gate blast radius; TOOL-1 F2 → S8's ratchet; TOOL-2 F1 → S6; TOOL-2 F2 → S5 as minter with TOOL-3 extending; PLAY-1 F1 → S7 in §0; PLAY-2 F1 → S3's parameterized micro-formats; PLAY-3 F1 → inline. The residues found are carrier-level (M5, M6), not resolution-level. TOOL-3's two delegated resolutions are correctly signed `(agent, …, delegated)` and correctly reasoned from M3's veto 1 and tie-break; the shrink-only→declared-exemption correction propagated to all three carriers it names (TOOL-3 S1:17-21, §10:297-299, PLAY-3 S7:51-55).
- **The README's TOTAL build order is sound and its justification reproduces.** PLAY-1 and PLAY-2 work from BASE anchors; PLAY-3 inserts above and inside them, including `:107`; PLAY-1/PLAY-2 share template `:51`; PLAY-1/PLAY-3/PLAY-4 share `customize.md`. TOOL-3 last with PLAY-3 S7 as a hard prerequisite is right.
- **The coverage table joins.** All eleven `A*` and four `B*` defects map to a scope item that exists; spot-checked A1→PLAY-1 S2, A3→PLAY-4 S1/S2, A5→PLAY-1 S4, B4→PLAY-3 S4. No scope item claims an audit provenance it lacks (L8 is an omission in §2's prose, not a false claim).
- **TOOL-1 S1's second-consumer analysis is correct and load-bearing.** `tools/gate-legs.json:23-29` does pass `18432` positionally, `tools/check-template-size.sh:14-19` does resolve positional → env → default, and the kickoff leg is insulated from the raise by construction.
- **PLAY-2's measurements reproduce**: 17 branch senses on 14 lines, +238 bytes, the `\bmain\b`-is-blind-to-`domain` argument (AC2's two observations are the right shape), and `{{DEFAULT_BRANCH}}` genuinely pre-exists at `skills/session-kickoff/MANIFEST-TEMPLATE.md`.
- **TOOL-3's anti-vacuity design holds.** The `memory-tree` sentinel, the non-empty-both-sides rule, and the read-don't-rewrite kit derivation are all correct against `tools/check-install-prefix.sh:38` and `map_extractors.py`.
- **PLAY-4** is unchanged in substance by the delta; its AC1 rewrite is a strict improvement (it now names which files' sets are recomputed against which file's arithmetic) and its 23/14/1/36 measurement is exact.
- **Refuted and not folded** (recorded so a later round does not re-spend them): TOOL-1 S1 does not disable the kickoff leg; TOOL-1 S5/§4/§7 target the manifest's live path, not a deleted one; the rev-1-then-descending revision logs in PLAY-1 and TOOL-3 are a house convention, not a defect; TOOL-1 §5's "F2 … is unresolved" reads as the risk pending S8; TOOL-3 F1's authority argument and F2's "closed since AC1" both stand under M3; the out-of-order scope and AC numbering across three specs is cosmetic.

## Coverage

Four lenses, then one synthesis pass. Every finding was re-verified against the working tree before folding; several survivor citations were themselves stale (three quoted spec text that no longer exists at HEAD) and were corrected or dropped.

- **New-material** — read the `4b38e39..HEAD` diff as the unit of review and scored each new scope item and AC for observability. Contributed B1, B3, H1, H3, H7, M1, M2, M3, M4, and the L4/L5 measurement conflicts. Its strongest contribution was B3: the recognition that TOOL-1's AC9 quantifies over a build boundary its own unit precedes.
- **Factual** — re-derived every line anchor, count and quoted string in the delta against source. Contributed H8, H9, M8 (the fabricated SKILL.md follow-up), M9, M10, L1, L2, L3, L6, L7. This lens is also what caught that the reconcile commit updated anchors but not populations, which is the shared root of B1 and B2.
- **Cross-unit** — checked hand-offs, shared files and the seven fork resolutions across all seven specs plus the README. Contributed H4, H5, H6, M5, M6, M7, and the confirmation that the build order and coverage table join. This lens supplied the framing that H6 is round-2 blocker B1 recreated by the fold meant to close it.
- **Premise** — attacked the load-bearing claims rather than the citations: "an undeclared floor is its own refusal" (H2), "eleven kits" (B1), "the same four records" (H9), "13 of the 36" (H10). Highest yield per finding; H2 in particular invalidates the sole justification for a mandatory scope item.
- **Synthesis (this pass)** — merged 14 duplicate survivor entries into 10 findings, adjudicated the one contradiction between lenses (the v2.7 archive blob: resolved against measured precedent — `git show 5ed9b4b^:parallel-coding-governance.template.md` is byte-identical to the v-2-6 archive, so S8's "pre-bump" is right and AC7 is wrong), dropped survivor claims that did not reproduce at HEAD (TOOL-1 §4's "twelve tracked lines across eight files" no longer exists; only the L7 residue does), and added **B2**, which no lens found: the `govkit selfcheck` leg (`tools/govkit/govkit.py:516-519` over the `tools/*` surface at `registry.toml:22-32`) reds on all four new depth-1 files this build creates.

**Disposition — decisive.** Do **not** merge or push this spec set as it stands. Three blockers (B1, B2, B3) and H6 are **blocking-before-merge**: B1 and B2 each guarantee a red merge bar for a unit built from these specs, B3 makes an acceptance criterion unobservable by its owning unit, and H6 leaves a verified defect declared-routed-and-unreceived across three documents — the exact shape round 2 filed as blocker B1. The remaining seven highs are **strongly recommended before merge** but do not by themselves red a gate; H2 is the closest to blocking, since it lets half of TOOL-2 be skipped with every AC green. All mediums and lows are **safe-to-land** and can ride the next rev bump. Fold B1, B2, B3 and H6, re-run the delta review over the changed sections only, then merge.