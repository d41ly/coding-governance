# Review — aSiftedPlaybook spec set, round 4 (M4 spec audit)

**Serves:** spec-audit PLAY-aSiftedPlaybook-1 PLAY-aSiftedPlaybook-2 PLAY-aSiftedPlaybook-3 PLAY-aSiftedPlaybook-4 TOOL-aSiftedPlaybook-1 TOOL-aSiftedPlaybook-2 TOOL-aSiftedPlaybook-3  <!-- inferred: an M4 pass over this build's whole spec set; the other ids in the body are prior decisions this round CITES, not units it reviews -->

## Verdict: BLOCKED

*Synthesis pass over the `2c9d833..f43a48c` fold delta — the two commits that folded round 3. Four lenses -> batched skeptics -> one synthesis; **4/4 lenses live, 0 dead, 0 dead skeptic batches, 0 spurious / duplicate / conflicting verdicts**. **55 raw judged — 40 confirmed, 15 refuted, 0 unverified.** The 40 confirmed collapse to **14 findings** after merging (four lenses filed the same PLAY-3 AC7 defect independently, and three filed the same README count). Every anchor below was re-derived by this pass against the working tree at `f43a48c`; the corrections that re-verification forced are listed in Coverage.*

*Round 3 returned BLOCKED with three blockers and ten highs and ordered the folds re-reviewed. **Most of the fold landed**: B1, H2, H5, H7, H8, H10, M1, M5, M6, M7, M9, M10, L1, L2, L3, L7 and L8 all reproduce as fixed. What did not is a single pattern, and it is the pattern round 3 was filed about: **eleven of the fourteen findings below are a two-part remedy applied to one part**, and four of them are certified as complete by a revision log that describes an edit its own section does not carry.*

One blocker, reproduced empirically rather than argued: `govkit selfcheck` was declared in three units and the build creates depth-1 `tools/` paths in **four**.

| id | sev | file:line | finding | fix |
|---|---|---|---|---|
| B1 | blocker | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-2.md:131` | **Round-3 B2's fold enumerated four new depth-1 `tools/` paths and there are five; the fifth is TOOL-2's, which carries the obligation in NO section.** TOOL-2 S1:16 and §4:131 create `tools/check-template-size.test.sh`; `grep -rn 'govkit\|registry.toml'` over that spec returns nothing — no §4 registry row, no §7 gate line, no AC. Reproduced, not inferred: in a scratch clone of `f43a48c`, `python tools/govkit/govkit.py selfcheck` exits **0** with `surface 37 … 0 unclaimed`, and with that one path staged exits **1** with "tracked path 'tools/check-template-size.test.sh' is in the declared surface but is neither an entry member nor an exemption". Exemptions are exact-path (`govkit.py:501-519`), so `tools/check-template-size.sh`'s `[[exempt]]` row (`registry.toml:150`) does not cover the sibling — the `run-gates.sh` / `.test.sh` / `.evidence.test.sh` trio at `:134`/`:142`/`:146` is the precedent that each needs its own row. The leg carries **no `guard`** in `tools/gate-legs.json:524-531` (unlike `govkit selftest` at `:532-541`), so it runs on diff-scoped runs too. TOOL-2 AC4 ("`bash tools/run-gates.sh` … is green") and §7's push-boundary line both red the day unit 2 lands. The fold's own count sentence certifies this cannot happen: TOOL-1 AC12:272 says "three units in this build create one" — four do (TOOL-1 `:146`, TOOL-2 `:131`, PLAY-3 `:164`, TOOL-3 `:118-119`), and five paths, not four. | Add `tools/govkit/registry.toml` to TOOL-2 §4 Files touched, `python tools/govkit/govkit.py selfcheck` to TOOL-2 §7, and an AC observing the leg green with `tools/check-template-size.test.sh` declared (an `[[exempt]]` row beside `tools/run-gates.test.sh`'s is the shape). In TOOL-1 AC12:272 **delete the count** rather than correcting it to four — a hardcoded population inside the unit whose §2 S4 argues for deriving populations is the same defect, and this count is what let the fifth path be missed. |
| H1 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:144` | **Round-3 H6 was folded into prose only, and the fold introduced a false claim in a second carrier.** TOOL-1 S4:38-41 now names `README.md:33` and the stale "19-check", but the §4 Inventory row at `:144` still reads `README.md` \| `12` \| `rule statement` — `:33` is not in the inventory — and no AC in TOOL-1 (AC1-AC12 read in full at `:227-291`) observes it — AC3's alternation `32768\|32 ?KiB\|never raise the limit\|[Dd]o NOT raise\|raise the limit\|inflating` cannot match "19-check". PLAY-1 §3:84-85 now asserts the carrier "is named in that unit's scope item **with a §4 inventory row**" — a reader can check that and will not find it. The third carrier is untouched: the audit report `:80-83` still files root `README.md:33` under "What this audit did not cover". Measured live: `grep -nE '[0-9]+-check' README.md` returns one hit, `:33`, "a 19-check hygiene gate", against `tools/gate-legs.json:3` "memory hygiene (20 checks)". Every TOOL-1 AC goes green with the defect shipped — the declared-routed-and-unreceived shape round 2 filed as B1 and round 3 re-filed as H6, now with three documents giving three answers. | Add a `README.md` row carrying `12, 33` to TOOL-1 §4's Inventory table (or a second row for `:33` alone, classed as the stale check count), add the AC round 3 named (`grep -nE '[0-9]+-check' README.md` reads 20 and nothing else), and correct the audit report's bullet to say the carrier was pulled into TOOL-1 S4. Or drop "with a §4 inventory row" from PLAY-1 §3 — but not two answers. |
| H2 | high | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:231` | **Round-3 H4 was not folded, and the rev-6 log certifies that it was.** AC7 (`:228-232`) still requires the archive "byte-identical to the template as it stood **before this build's first template edit**", while S8:68 copies "the **pre-bump** template". `git diff 2c9d833..f43a48c` on this file has no hunk in the AC region — the delta touched §1, S9/S10, the population paragraph, two cost rows, three Files-touched rows, AC9/AC10/AC11 and the log. The log at `:279-280` nonetheless reads "**H4**: S8 and AC7 named two archive blobs ~2.3 KB apart; AC7 now matches S8 and the v-2-6 precedent." PLAY-3 is unit **6** (`README.md:78`), after PLAY-1 and PLAY-2 have both edited the template, so the two clauses name different blobs by construction. Under AC7 the builder archives the BASE blob as `-v-2-7` and the content actually labelled v2.7 is never archived — against the measured v-2-6 precedent round 3 cited. A later round reading the log will not re-check it. | Re-word AC7 to match S8 and precedent: byte-identical to the template at the parent of the bump commit, naming `git show <bump>^:parallel-coding-governance.template.md` as the observation. Then correct the rev-6 entry, or drop its H4 clause. |
| H3 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:249` | **H1's keying landed in S8 and in no acceptance criterion anywhere in the build.** S8:83-87 was rewritten to "**The record is KEYED BY MEASURED FILE**, one `<path>\t<bytes>` row per subject", and spells the failure a single value causes. AC7:249-253 is byte-unchanged in the delta and still reads "With `tools/template-size-highwater.txt` **holding a value H**" — one un-keyed number. TOOL-2's arms inherit it: A6 (`:30`) reads "`H` = the recorded high-water" with no subject binding, and no arm in A1-A8 proves a `--bump` on one subject leaves the other row untouched — the arm round-3 H1 asked for in terms. Measured: `skills/session-kickoff/SKILL.md` is 18215 bytes against a template of 32682, so a builder who satisfies AC7 and A6-A8 ships the single-value record S8 forbids, and a `--bump` on the kickoff leg's argv then makes the template leg print WARN forever. (H1's second half is closed: TOOL-2 S2:38-42 does cite the S8 path override as how the arms reach a scratch copy, so the H3 half of round 3 reproduces as fixed.) | Re-word AC7 against the keyed record — the row keyed by the measured file holds H — and add the second-consumer observation: a `--bump` on one subject leaves the other row byte-identical. Add the matching arm to TOOL-2 S2's table so AC1's "one line per arm in S2's table" picks it up. |
| H4 | high | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:432` | **H9's fold deleted one word and left the false premise standing.** The only §10 change in the delta is `The query returned the three records` -> `the records` (`:428`). The enumeration below it is still three — `PLAY-aCandidStub-1` §3, `TOOL-aGuardedTally-1`, `PLAY-aPrunedCeremony-1` RD7 — and `:432` still closes "**All three** remain accurate as of their dates". The `aCandidStub` review's refuted id 19 appears at `:55` and `:264` and nowhere in §10. AC6 (`:261-265`) still asserts "§10's recall result and this list are **the same four**". Neither of H9's two remedies was applied, and rev-6 `:363` lists H9 among the folded highs. AC6 governs a row appended to the append-only `memory/DECISIONS.md`: its identity clause is falsified by the section it names, so which records the row carries depends on which carrier the builder trusts. | Extend §10 to four records with `memory/builds/aCandidStub/reviews/2026-08-10-review-PLAY-aCandidStub-1-1.md:222-224` and change "All three remain accurate" to four; or delete AC6's identity clause and let §2 S6 + AC6 be the sole enumeration. Correct the rev-6 entry either way. |
| H5 | high | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:43` | **M2's fold pinned the waiver key grammar and converted the summary row, not the sentence a builder seeds from.** TOOL-3 §10:312-317 now pins it: "the BARE kit directory name exactly as the derivation yields it (`lib`, `hooks` — no trailing slash, no path prefix), **compared verbatim**", and states that a key that does not join "leaves its kit unexcused and reds AC1". PLAY-3's §4 Files-touched row WAS converted in the delta to "seeded with `lib` and `hooks` only" (`:164`). S7's bolded seeding instruction at `:43` was not: "**Seed with `lib/` and `hooks/` ONLY — not `workflows/`.**" Nor was §4's exemption prose at `:114-115` (`lib/`, `hooks/`). Round-3 M2's remedy said in terms "Make PLAY-3 S7 and its Files-touched row use that identical spelling"; one of two landed, and TOOL-3's own rev-8 log (`:266-270`) names the uncorrected carrier while leaving it uncorrected. PLAY-3 S7 is a hard prerequisite of TOOL-3 (`README.md:89-90`), so the mis-seeded file is exactly what unit 7's gate reads: both kits score unexcused, TOOL-3 AC1 reds, and the merge bar goes red — the vacuous-selector class the pin exists to prevent, produced by the fix for it. | Change S7:43 to "Seed with `lib` and `hooks` ONLY — not `workflows`", and §4:114-115 with it. Better: have S7 point at TOOL-3 §10 as the owner of the spelling instead of restating it, since §10 says the grammar is pinned "here and nowhere else". |
| M1 | medium | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-1.md:96` | **M3's fold hands off to arms that do not exist.** TOOL-1 S8:94-96 states the degenerate-case contract — "An absent file means no ratchet plus one explicit line saying so … never a `set -u` explosion on an empty operand at the numeric comparison. Non-numeric content is a named failure." — and closes "**Both are armed in `TOOL-aSiftedPlaybook-2` S2**". TOOL-2 S2's table (`:23-32`) is A1-A8; A3 is a missing TEMPLATE path (exit 2), matching the gate's `file not found` branch at `tools/check-template-size.sh:22`, not a missing RECORD. Grepping TOOL-2 for an absent/malformed high-water arm returns nothing, and rev-5/rev-6 do not mention adding one. Round-3 M3's remedy was explicitly two-part and only the statement landed. Compounding it, TOOL-2 AC1:168-170 anchors the expected set to "S2's table", so the omission is invisible to TOOL-2's own acceptance set — and `set -u` is live at `tools/check-template-size.sh:11` with the numeric comparison at `:28`, on a leg the bar runs twice. | Add two rows to TOOL-2 S2's table beside A3 — absent record (no ratchet, one explicit line, exit 0) and non-numeric record (named failure) — and extend AC8 to them. Or delete the "Both are armed in TOOL-2 S2" sentence and name a different owner. |
| M2 | medium | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:133` | **L4's fold added the two missing cost rows and left the total they change.** §4's table now carries six rows marked gated — S1 ~645, S2 ~527, S3 ~421, S4 ~450, plus the new "S8 the v2.8 header entry \| ~230" (`:130`) and "S9 govkit bullet \| measure at build time" (`:131`). Line `:133` still reads "The gated subtotal is roughly **2043 bytes** against 86 free at BASE" — exactly 645+527+421+450, the four pre-fold rows. The stated total excludes two of its own gated rows and understates by ≥230 plus an unmeasured item. This is the one number the build single-sources: TOOL-1 §3:123-126 defers to this table as "**its §4 cost table owns the figure** and this spec deliberately does not restate it", naming an earlier 1593 as the second-copy defect — and TOOL-1 S8:75's forecast "near 34963 bytes" is 32682 + 238 + 2043, derived from the stale subtotal. L4's second half is also still unanswered: S8 (`:63-75`) does not say whether the v2.6 header entry is dropped when v2.8 lands. | Measure S9 and re-derive the subtotal over every gated row, or delete the spelled subtotal and say the table IS the total — the de-numbering TOOL-1 §4:133-134 adopted in this same fold. Re-derive TOOL-1 S8:75's forecast from whatever the table then says. State in S8 whether the v2.6 entry is dropped. |
| M3 | medium | `memory/builds/aSiftedPlaybook/README.md:95` | **The B1/H10 folds added coverage rows B5 and B6 without moving the count that quantifies them.** `:94-95` still reads "`A*` are the eleven the audit confirmed; `B*` are **four** found after it closed and are NOT part of that count", and the table at `:110-115` carries six B-rows. A false count contradicting its own enumeration, one line apart, in the section the build declares authoritative for completeness ("Derived from the committed audit report, not from recollection") — and the same README warns at `:74-77` that two of its cells "drifted from their specs within minutes of being written … which is `two-answers-to-one-question` reproduced inside the build that exists to close it". Secondarily `:117`'s "`B5` and `B6` post-date the audit report" is wrong for B6: the report carries it as R2 (`build/…-playbook-audit.md:62`) with "**REFUTATION WITHDRAWN 2026-08-16** (round-3 H10)", so B6 is a re-adjudicated report row, not a post-audit find. It also breaks round 3's own certified invariant, "All eleven `A*` and four `B*` defects map to a scope item". | Delete the number — "`B*` were found or re-adjudicated after the audit closed and are NOT part of that count" — since the rows are the enumeration, matching TOOL-1 §4 and PLAY-3 §4 in this same fold. Correct `:117` to say B6 is the report's R2 re-adjudicated. |
| M4 | medium | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-4.md:7` | **H10's fold added a scope item that edits a third file, and neither §1 nor §4 was extended.** S4 (`:26-32`) edits `customize.md:15` **and** `WIRE-INTO-PROJECT.md:98`; AC5 (`:131-132`) greps both. §1:7 still reads "Correct **three** self-description defects in **the two companion files**" against four scope items across three files; §4 Files touched (`:77-80`) lists `customize.md` attributed to "S1 at `:20-21`, S2 at `:33-42` and `:47-48`" — not S4's `:15` — and `domain-rules.md`, with no `WIRE-INTO-PROJECT.md` row; `:82` then says "**Neither file** is under a size gate" quantifying over two where three are edited. §4 is the staging inventory and §1 the scope statement, and both under-describe the unit. **Adjudicated:** one lens filed this as shipping a wrong fix, a skeptic refuted that on the ground that AC5 greps both files by name so the file cannot silently be left out. Measured, the skeptic is right on consequence — AC5 catches it — and PLAY-2's own table at `:74-75` already routes both sentences to "**14** via `PLAY-4` S4". Records-level, not shipping-level, and kept at medium because a false count in the unit that fixes false counts is this build's subject. | Change §1 to four defects across the two companions and the runbook, add a `WIRE-INTO-PROJECT.md` row for S4 at `:98` to §4, add S4's `:15` to the `customize.md` row, and re-word `:82` over three files. |
| L1 | low | `build/2026-08-16-build-aSiftedPlaybook-1-playbook-audit.md:81` | **L6's fold made two of its three edits.** R6's range WAS tightened to `WIRE-INTO-PROJECT.md:469-472` (`:66`) and PLAY-1 §3:78 WAS moved to `:464`; the report's own follow-up bullet at `:81` still reads "`WIRE-INTO-PROJECT.md:463` calls `agent-cap` \"the review protocol's TWO rules\"". Measured: `grep -n "TWO rules" WIRE-INTO-PROJECT.md` returns exactly one hit, `:464`; `:463` is "rather than silently ignored. This is the". The report is the carrier a landing session mints the follow-up row from, and it now disagrees with the spec citing it. | Change `:463` to `:464` at `:81`. |
| L2 | low | `build/2026-08-16-build-aSiftedPlaybook-1-playbook-audit.md:87` | **M8's replacement bullet names the wrong commit, inside the paragraph whose stated purpose is showing the report's own corrections.** It says "Commit `0f4d308`, one of the 102 this branch was behind, replaced it with the right pattern". Measured: `git show --format="" 0f4d308 -- skills/session-kickoff/SKILL.md` is a single 4-insert/2-delete hunk at `:169` introducing the `<MEMORY_TREE_KIT>` disambiguation — nothing about the section count. `git log -S "states its own section count" -- skills/session-kickoff/SKILL.md` returns only `5fd7c7e`. The bullet's substance is right and re-verified: `git show 91ef1b05:skills/session-kickoff/SKILL.md` reads "status header + nine / canonical sections" at `:129-130`, and `5fd7c7e` is an ancestor of HEAD and not of BASE, so "one of the 102 this branch was behind" holds for it. Only the citation is wrong, and it was introduced by this fold. | Change `0f4d308` to `5fd7c7e` ("build(KICK-cKeyedLaunchpad-4): the sealed task region, and the duplication it removes"). |
| L3 | low | `spec/2026-08-16-spec-PLAY-aSiftedPlaybook-3.md:44` | **L5 was not folded and the rev-7 log says it was.** S7:44 still reads "`lib` scores 0 (all seven apparent hits are the substring inside \"deliberate\")" against TOOL-3 S1:25-26's "six inside \"deliberate\"/\"deliberately\" and one inside **`stdlib`**". Measured independently: `grep -on 'lib'` over the trio returns seven — template `:85`, `:140`, `:194` and domain-rules `:68`, `:72`, `:95` are `deliberate*`; template `:109` is "an offline **stdlib** CLI". TOOL-3 is right. The rev-7 entry at `:271-274` nonetheless states S7's evidence "now points at the owner". The seeding outcome is unaffected — `lib` needs a waiver either way — so the cost is records-level, but two specs still give different evidence for one measurement inside the build that exists to close that class, and a log certifying an edit that was never made tells the next round not to re-check. | Replace S7:44's parenthetical with a pointer to `TOOL-aSiftedPlaybook-3` S1's measurement rather than a second copy of it, and correct the rev-7 entry. |
| L4 | low | `spec/2026-08-16-spec-TOOL-aSiftedPlaybook-3.md:117` | **The B2 obligation is carried by a different subset of sections in each of the three units that received it.** TOOL-1 has all three (§4:147, §7:292-296, AC12:270-272). PLAY-3 has the §4 row (`:165`) and AC10 (`:219-220`) but no `govkit selfcheck` line in §7 (`:234-241`). TOOL-3 has the §7 line (`:214`) and AC10 (`:202-204`) but no `tools/govkit/registry.toml` row in §4 Files touched (`:116-124`). **Adjudicated by measurement, because the lenses split on it:** one lens filed the TOOL-3 half as a half-applied blocker remedy; three skeptic passes refuted the same class on the ground that each unit's AC10 names `tools/govkit/registry.toml` by path and cannot be satisfied without editing it. The skeptics are right — nothing ships wrong, unlike B1 where the file appears in no carrier of TOOL-2 at all — so this drops from blocker-class to a uniformity item that rides the rev bump. | Add the `govkit selfcheck` line to PLAY-3 §7 and the `tools/govkit/registry.toml` row to TOOL-3 §4, so all three receivers carry the obligation in the same three places and the next reader does not have to re-derive which subset is load-bearing. |

## Per-spec rev-bump fold list

- **`TOOL-aSiftedPlaybook-1` → rev-8** — H1 (§4 gains the `README.md` `:33` row; an AC greps `[0-9]+-check` over root `README.md`), H3 (AC7 re-worded against the keyed record + the second-consumer observation), H4 (§10 extended to four records and "All three" corrected, or AC6's identity clause deleted; the rev-6 entry corrected either way), M1 (either TOOL-2 S2 gains the two arms or S8's "Both are armed" claim names a different owner), B1's second half (AC12:272 — delete the population count, do not correct it), M2's knock-on (S8:75's `34963` re-derived once PLAY-3's subtotal moves).
- **`TOOL-aSiftedPlaybook-2` → rev-6** — **B1** (`tools/govkit/registry.toml` in §4 Files touched, `python tools/govkit/govkit.py selfcheck` in §7, an AC observing the leg green with `tools/check-template-size.test.sh` declared), M1 (two arms beside A3 for an absent and a non-numeric high-water record, with AC8 extended), H3 (A6-A8 bound to a subject row; one arm proving a `--bump` on one subject leaves the other untouched).
- **`TOOL-aSiftedPlaybook-3` → rev-9** — L4 (`tools/govkit/registry.toml` row in §4 Files touched).
- **`PLAY-aSiftedPlaybook-1` → rev-7** — H1 (either §3:85's "with a §4 inventory row" becomes true or the clause is dropped).
- **`PLAY-aSiftedPlaybook-3` → rev-8** — H2 (AC7 aligned to S8 and the `git show <bump>^` precedent; the rev-6 entry corrected), H5 (S7:43 and §4:114-115 re-spelled bare, or pointed at TOOL-3 §10), M2 (the gated subtotal re-derived or deleted; S9 measured; the v2.6-drop question answered in S8), L3 (S7:44 points at TOOL-3 S1; the rev-7 entry corrected), L4 (`govkit selfcheck` in §7).
- **`PLAY-aSiftedPlaybook-4` → rev-4** — M4 (§1 restated over four defects and three files; `WIRE-INTO-PROJECT.md` and S4's `customize.md:15` added to §4; `:82` re-worded).
- **`README.md`** — M3 (the `B*` count deleted; `:117` corrected for B6's provenance).
- **`build/2026-08-16-build-aSiftedPlaybook-1-playbook-audit.md`** — H1 (the `README.md:33` bullet says the carrier was pulled into TOOL-1 S4), L1 (`:463` → `:464`), L2 (`0f4d308` → `5fd7c7e`).

## Checked and clean

- **Sixteen of round 3's findings reproduce as genuinely folded**, verified individually against source rather than against their log entries: **B1** (`govkit` given a disposition as PLAY-3 S9; both spelled kit counts deleted; AC9 re-derives the population with `git ls-files -- 'tools/*/*' | awk …`), **H2** (the "an undeclared floor is its own refusal" premise deleted from TOOL-2 S6 and TOOL-3 AC8, replaced with the true behaviour and an AC reading `--report` — `check-arms.py`'s `floors.get()`/`continue` re-checked), **H5** (`memory/guides/SESSION-KICKOFF.md` added to the README's `PRODUCT_GLOBS` enumeration and the `memory/` sentence qualified; matches `tools/drift-audit/drift_signals.py:20-33` exactly), **H7** (PLAY-3 §4 now says **two** exempt kits and records `workflows`' hit at template `:150`), **H8** (AC11 re-cited at `drift_signals.py:104-136` / `:132-136` / `:113` and the uniqueness premise restated against the `AGENTS.md:97`+`:98` pair — all four anchors re-measured), **H10** (PLAY-2 S5's "the 13 stays 13" retracted, PLAY-4 S4/AC5 minted, the report's R2 withdrawn, `PLAY-aSealedCaravan-1` cited), **M1**, **M5** (no conditional survives in TOOL-2 §4/§7), **M6**, **M7**, **M9** (`memory/guides/SESSION-KICKOFF.md:52` is the default-branch line), **M10** (`.memory-tree.conf:57-71`), **L1**, **L2**, **L3** (nine dossiers at HEAD, `wc` confirms), **L7**, **L8**.
- **Round-3 H3 is fully closed even though H1 beside it is not.** TOOL-1 S8:88-92 specifies the record's path resolution as positional → env → tracked default, `--bump` writing to the same resolved path, and TOOL-2 S2:38-42 cites that override as how the arms reach a scratch copy. One lens filed the H3 half as unfolded; measured, it is folded, and only the AC7 keying (H3 above) is missing.
- **The blocker is a declaration gap, not a mechanism disagreement.** `govkit.py:97-120`'s `surface_paths` expands `tools/*` to depth-1 files as well as directories, exemptions are exact-string, and the other four new depth-1 paths ARE declared. Nothing about the fold's reasoning is wrong; it quantified over a four-item list instead of over the tree.
- **The delta introduced no new defect outside the eleven half-applied remedies.** The three genuinely new items are B1's fifth path (a population the fold inherited from round 3), L2's wrong commit citation, and M3's stale count — each a by-product of an edit, none a new design decision.
- **Refuted and not folded** (recorded so a later round does not re-spend them): TOOL-1 S8's "Seeding is NOT this unit's act" heading is resolved by its own next clause and by AC9, three carriers agree; PLAY-1 §3:77's "lines 465-475" is a containing range around the `:469-472` install and round 3 already scored it "contained, so not false" in a §3 non-goal ending "No edit."; PLAY-3 S3:27's "the highest-consequence omission of the four" ranks the S1-S4 scope items, not the kit population; PLAY-3 S10's "after S1-S4 and S8" is completed by its own purpose clause and observed by AC11 at DoD; TOOL-1 S8:98-99's "units 4-6" and `:102`'s "units 5 and 6" quantify over different predicates (edited vs grown past the record) and rest on a cost PLAY-1 §4 explicitly defers; PLAY-3 §4's missing S9 Items cell is covered by AC1 **and** AC9; the README fold list's two unmade README edits (the registry obligation and the seed/bump owner) are received, cross-cited and AC-observed in the specs themselves — TOOL-1 AC9 and PLAY-3 S10/AC11 are a reciprocal handoff, the opposite of the marker-bump precedent invoked against them.

## Coverage

Four lenses over the `2c9d833..f43a48c` fold delta, then one synthesis pass. All four lenses returned; no skeptic batch died; nothing came back unverified. **Every finding below was re-measured by this pass at `f43a48c` before it entered the table**, and the re-verification changed the set: one confirmed finding was **downgraded** (L4, where three skeptic passes had refuted the same class one lens confirmed), one was **halved** (round-3 H3's path-override limb does not reproduce as unfolded — only the AC7 keying does), and several anchors moved (`TOOL-1 §4` is `:144` not `:143`; TOOL-1's AC block runs `:227-291` not `:226-281`; PLAY-3 AC7 is `:228-232` with the offending clause at `:231`; TOOL-1 §10's "All three" is `:432`). One survivor claim was **corrected rather than dropped**: "B5 and B6 exist in no carrier but the README" is true of the report's `B*` table (B1-B4 only) but false of the report as a whole, since B6's subject is R2 with its refutation withdrawn — M3 states the corrected version.

- **New-material** — read the fold hunks as the unit of review and asked, per hunk, whether the edit the round-3 fold list ordered is the edit that landed. Contributed the H2/H3/H4/H5 "half a remedy" pattern and M1's unreceived hand-off. Its strongest contribution was noticing that four revision-log entries certify edits their sections do not carry, which is what makes these findings expensive rather than cosmetic.
- **Factual** — re-derived every anchor, count, quoted string and commit id the fold touched. Contributed M2's arithmetic (2043 = 645+527+421+450 exactly, and TOOL-1 S8:75's 34963 = 32682+238+2043), M3's six-vs-four, L1, L2 and L3's independent `grep -on 'lib'` measurement. This lens is what caught that the fold's own corrections carry new errors.
- **Cross-unit** — checked the hand-offs the fold created across the seven specs, the README and the audit report. Contributed H1 (three carriers, three answers), H5 (the pinned grammar converted in the summary row and not the seeding sentence), L4's section asymmetry, and the confirmation that the B2 declaration reaches four of five paths.
- **Premise** — attacked the fold's load-bearing claims rather than its citations: "three units in this build create one", "Both are armed in TOOL-2 S2", "§10's recall result and this list are the same four", "AC7 now matches S8". Highest yield per finding; every one of those four sentences is false as written.
- **Synthesis (this pass)** — merged 40 confirmed entries into 14 findings (four lenses filed PLAY-3 AC7 independently, four filed the cost subtotal, four filed the `lib/` spelling, three filed the README count, three filed the `:463` anchor); adjudicated the one contradiction between lenses by measuring (L4: confirmed by one lens, refuted by three skeptics — settled for the skeptics, since each unit's AC10 names the registry file by path and cannot be satisfied without editing it, so it is a uniformity item and not a blocker); and **escalated B1 from argument to reproduction** — cloned `f43a48c` to a scratch tree, staged the single path TOOL-2 creates, and measured `govkit selfcheck` going from exit 0 / "surface 37 … 0 unclaimed" to exit 1 / "1 unclaimed". No lens had run it.

**Disposition — decisive.** Do **not** build from this spec set and do **not** merge or push it as it stands. **B1 must be folded first**: it is a measured red merge bar, not a forecast, and it reds on diff-scoped runs as well as at the push boundary because the leg carries no guard. The five highs are all **blocking-before-merge** on round 3's own bar — H1 ships a verified wrong count with every AC green, H2 archives the wrong blob, H3 leaves the keyed record with no acceptance criterion anywhere in the build, H4 leaves AC6 falsified by the section it names, and H5 reds `TOOL-3` AC1 the day unit 7 lands. The four mediums and four lows are **safe to ride the rev bumps above**. One structural instruction beyond the folds: **four revision-log entries (TOOL-1 rev-6 for H9, PLAY-3 rev-6 for H4, PLAY-3 rev-7 for L5, and TOOL-1 rev-6's H1 clause) certify edits their sections do not carry.** Correct them in the same pass, because the cheapest way for round 5 to miss these is to read the logs and believe them — which is how three of round 3's findings survived into round 4. Fold B1 and the five highs, correct the four log entries, then re-review the changed sections only.
