## Verdict: BLOCKED

**Serves:** spec-audit TOOL-aLoosenedCeiling-1 TOOL-aLoosenedCeiling-2 TOOL-aLoosenedCeiling-3
TOOL-aLoosenedCeiling-4

Four lenses reported; this pass re-verified every claim it promotes against source in this worktree
and in `C:/projects/incms/main/vendor/nicocares-package`. What was checked: the four spec bodies
against `memory/TEMPLATE-SPEC.md` and `memory/guides/BUILD-METHOD.md` M2–M4; every arithmetic and
measurement claim re-run (`corpus_ids.py --measure`, `--report`, `check-arms.py --report`, file
sizes, awk binding semantics); the gate mechanisms the acceptance criteria name, read at their own
call sites (`check-verdict-epoch.sh`, `check-testsuite-counts.sh`, `check_citations.py`,
`check-memory-hygiene.test.sh`); and the landed unit-3 commit `540e0d2` against what units 1 and 3
each claim to own. `bash tools/memory-tree/check-memory-hygiene.sh` exits 0 silently today, so the
four spec files parse against check 12 and the corpus is green at base. What was NOT checked: the
full bar was not run (`GATE_FULL=1 bash tools/run-gates.sh`); the four growth-rate bases in unit 4
§4 were not re-derived from the adopter's history, only the arithmetic built on them; the §10 recall
and reuse probes were not re-executed; and no pass priced whether the set as specified stays inside
the owner's ask beyond confirming that no unit raises a per-class cap default.

| # | Sev | Spec · section | Finding | Fix |
|---|---|---|---|---|
| 1 | BLOCKER | unit 3 §6 AC2, against unit 3 §4 | AC2 requires `--measure`'s printed `READ_PATH_CEILING` to equal the conf's value, but §4 pins from the BASE measurement while `--measure` always re-measures live. Measured now: `--report` gives 86843 B; `.memory-tree.conf:90` holds `111994` (86394 base + 25600). After unit 1 lands, `--measure` prints 112443. The gap is 449 B today and §4 says it will widen. AC2 is false when the change is right. | Replace AC2 with the at-base identity it can actually observe: the conf's comment block records 86394 measured plus 25600 headroom and 86394 + 25600 equals the declared 111994. AC1 and AC3 already carry the unit. rev-2 with a §9 line. |
| 2 | HIGH | unit 1 §6 AC1 | AC1's precondition — "when this repo's conf declares no headroom" — is unreachable: `.memory-tree.conf:95` carries `READ_PATH_HEADROOM="25600"`, landed in `540e0d2`, and unit 3 S3 mandates it. Run as written it is also vacuous: because the conf declares 25600, `--measure` prints measured+25600 whether the module default is 20480 or 25600, so the one criterion guarding "the shipped default rose" cannot see it. | Move the absent-key case to AC2's fixture arm, and retarget AC1 at the declared path: `--measure` prints the measured total plus the value in `.memory-tree.conf`. Add one arm asserting the module constant is 25600 so the default is observed somewhere. |
| 3 | HIGH | unit 1 §2 S5, against unit 1 §3 | S5 orders `HYGIENE.template.md` and `memory/HYGIENE.md` to "describe check 16's budget as measured-plus-declared-headroom". §3 says check 16 "knows nothing about headroom", and source agrees: `corpus_ids.py:413-421` compares `total > int(conf["READ_PATH_CEILING"])` with no headroom term. `memory/HYGIENE.md:176-177` is correct today. Following S5 writes a false rule into the gate's own committed rule-set. | Reword S5: the docs describe `READ_PATH_HEADROOM` as what `--measure` ADDS when printing a pin to paste, and say in the same sentence that check 16 compares against `READ_PATH_CEILING` alone. |
| 4 | HIGH | unit 3 §2 S3 (landed), against unit 4 §3 | The key is declared and read by nothing. `corpus_ids.py:461` is still `headroom = 20480`; `load_conf`'s defaults dict at `:71-77` has no `READ_PATH_HEADROOM`; `--measure` prints `# measured 86843 B + 20480 B headroom`. Worse, the landed comment at `.memory-tree.conf:79` and `:92-94` asserts the key is "now DECLARED … rather than living as a constant" and that "ABSENT = the kit's shipped default (25,600 B)" — both false today. Unit 4 §3 refuses exactly this: "A declaration that does nothing is worse than an absent one." | Either land unit 1's S1/S2 before any further pass and record the window in unit 3 §9, or strike S3 and revert the two comment sentences to name 20480 as the current shipped default until unit 1 lands. Either way it is a rev bump on unit 3, not a silent conf edit. |
| 5 | HIGH | unit 2 §6 AC4 | AC4 asks `bash tools/check-testsuite-counts.sh` to observe that "the hygiene suite's printed assertion count has risen". That leg runs nothing — `tools/check-testsuite-counts.sh:21-22`: "IT RUNS NOTHING … the shape is asserted by reading the file". `compliant()` at `:47-58` is three `grep -qE` calls. The floor it checks is `-ge` (`check-memory-hygiene.test.sh:1040-1041`, `FLOOR_ASSERTIONS=136`), and §4 concedes added arms need no floor edit, so AC4 is green whether S6 adds six arms or zero — the format's banned unrelated green gate (`memory/TEMPLATE-SPEC.md:141`). | Point AC4 at `bash tools/memory-tree/check-memory-hygiene.test.sh` and state the expected count as at least 136 plus a named N, or raise `FLOOR_ASSERTIONS` in the same commit and say so. Keep `check-testsuite-counts.sh` in §7 as the shape gate only. |
| 6 | HIGH | unit 2 §2 S8, and unit 1 §2 S6 | S8 delegates the kit-version bump to unit 1 without naming which commit carries it. `check-verdict-epoch.sh:179` is topological: `git merge-base --is-ancestor "$W" "$S"`, where W is the newest behaviour-bearing commit across the engine OR its delegates (`corpus_ids.py` is one, `:69`) and S is the newest commit changing the constant's value. Unit 1 moves a delegate, unit 2 moves the engine, and `README.md:46` calls them independent. If unit 1 bumps and unit 2's engine commit lands after, the leg reds with "the bump is OLDER than the change it claims to date" — the exact leg AC5 and AC6 both claim green. | State the ordering in one place: either each unit carries its own bump, or the set declares that unit 2 lands before unit 1's bump commit. Give unit 1 S6 the reciprocal note that unit 2 shares the carriers. |
| 7 | MEDIUM | unit 1 §4 Files touched, against unit 3 §2 S3 | `.memory-tree.conf` is claimed by both units — unit 1 §4 line 97 ("the key, declared at this repo's chosen value") and unit 3 S3. It landed as unit 3 in `540e0d2`. Unit 1 §3 disclaims only the ceiling, not the headroom key, so nothing cedes it. M2's scope axis: nothing a sub-spec puts IN is OUT in a sibling. A closing diff cannot say which unit the line belongs to. | Strike `.memory-tree.conf` from unit 1's Files-touched list and add it to unit 1 §3: this repo's own declaration of the key is unit 3's S3. rev-3 on unit 1. |
| 8 | MEDIUM | unit 2 §1 Goal, against §3 and `README.md:26` | The Goal says the per-member ceiling is one "that check 16 rule 3 cross-references". §3 says the opposite and §3 is right: rule 3 is set membership, `corpus_ids.py:414` `capped = {…ask_shell("--print-index-set", root)…}` then `:422-425` `if p not in capped and p not in waived`. No cap value is read. The same wrong claim is mirrored in the build README. | Restate the Goal as what actually motivates the unit — the per-class cap governs how large one read-path member may grow, a separate constraint from the aggregate ceiling — and delete the cross-reference clause from the spec and from `README.md:26`. |
| 9 | MEDIUM | unit 2 §2 S3 and §6 AC2 | The abort's stream and status are undecided. S3 says "aborts … with a named message"; AC2 says "leaves with its distinct cannot-run status". Neither names a number or stdout vs stderr, and the engine's precedents disagree — the no-python abort prints to stdout and exits 2 (`check-memory-hygiene.sh:101`) while the inlined `resolve_python` prints to stderr (`:97`). The harness idiom discards both: `check-memory-hygiene.test.sh:438` is `out=$(bash "$SCRIPT" 2>/dev/null)`. A builder who picks stderr writes an arm that observes nothing and passes. | S3 names stdout and exit 2, citing `check-memory-hygiene.sh:101` as the precedent it copies. AC2 names the status literally and notes the arm needs its own capture, since the existing idiom drops both. |
| 10 | MEDIUM | unit 2 §6 AC3 and AC5 | Both are satisfied by an unchanged tree. `bash tools/memory-tree/check-memory-hygiene.sh` exits 0 printing nothing today, so AC3's "identical verdict" means identically empty, which any looser cap also produces. AC5's floors are one-sided upward — `.memory-tree.conf:111` pins `check-memory-hygiene.sh:19:19` and `check-arms.py --report` shows 19/19 — so a `fail`-based design scoring 20:20 would meet them too. | Drop AC3's green half and keep only AC1's per-class fixture arms. Restate AC5 as the risk it guards: no branch of `check-memory-hygiene.sh` becomes unarmed, and check 6's existing failure signature is unchanged (S9). |
| 11 | MEDIUM | unit 2 §4 Inventory | "check 6 has exactly one failure call site" is false. `grep -n "fail 6" tools/memory-tree/check-memory-hygiene.sh` returns lines 407 and 886, and `check-arms.py --report` lists both as `check 6 branch 1 line 407` and `check 6 branch 2 line 886`. The abort conclusion survives; the count the reasoning rests on does not. | Say two, or scope the sentence to the cap branch at `:407`. |
| 12 | MEDIUM | unit 2 §2 S4 and §4 Alternatives rejected | Both rest on a data dependency that does not exist: "a sibling gate would get an index set computed under a conf this script would have refused". `index_set()` at `check-memory-hygiene.sh:342-371` is built from `$M`, `$FILES` and `$MAP_SUB` alone; caps are applied afterwards in check 6's awk at `:391-405`. The set cannot vary with a cap value. | Restate the rationale as fail-fast propagation — the abort's non-zero exit is what `corpus_ids.py:181-183` converts into a named `Problem` for the sibling — and drop the differently-derived-set claim. |
| 13 | MEDIUM | unit 2 §2 S6 | S6 says "ONE fixture file" and stops. The harness runs the gate once per tree into `$out` (`:438`) from a conf written at `:65`, so declaring the six keys needs extra scratch trees or a re-run, and the malformed-cap arm cannot share a tree with any other assertion because the abort kills the run. S6 also exercises only the knob: no arm distinguishes the 25600 build-README default (`grep 25600 check-memory-hygiene.test.sh` returns nothing) or the `cl=0` semantics S5 generalises. | S6 states the invocation count and where the extra trees live, and adds two arms the Inventory's own gap requires: a build README over 25600 B with no key declared, and a zero-line-cap arm. |
| 14 | MEDIUM | unit 1 §2 S3, against unit 2 §2 S3 | The two siblings answer the same question differently. Unit 2 refuses a zero byte cap explicitly; unit 1 S3 says only "neither blank nor a whole number of bytes", which accepts `0` (the ceiling its own Alternatives entry calls wrong) and `-5000` (a pin permanently below the measured total) — verified, both `int()` calls succeed. `load_conf:84` strips outer quotes only, so `READ_PATH_HEADROOM="  "` reaches `int()` as whitespace: blank by S3's intent, `ValueError` by its letter. | S3 states the predicate as "a value that is neither empty-after-strip nor a positive decimal integer raises `Problem`", matching unit 2's zero ruling. |
| 15 | MEDIUM | unit 3 §2 S2 | S2 requires four things in the comment block; the landed block at `.memory-tree.conf:73-89` carries three. The measurement, the headroom and the convention change are all there. "That no read-path member was trimmed to get here" is not; the nearest sentence is the opposite framing about absorbing one more member of the largest class. | Add the missing sentence in unit 3's next commit — unit 3 §3 already says no member is trimmed, so it is worth keeping — or strike the clause from S2. |
| 16 | MEDIUM | unit 3 §2, against unit 1 §9 rev-2 | Unit 1's rev-2 line hands the stale ceiling-value carriers to unit 3 — "moved to unit 3, which owns that number" — and unit 3's §2 has no S-item for them. The live carrier is `memory/backlog/TOOL.md:62`, still reading "72642 B against a 86476 ceiling" against a live 86843 against 111994. | Add unit 3 S4: the backlog row and any other live carrier of 86476 move in the same commit as the conf. Dated build records under `memory/builds/` are immutable and stay. |
| 17 | MEDIUM | unit 4 §2 S2 | Two of S2's factual claims are wrong. The narrative ends at 116600 (`.memory-tree.conf:68`) against a live pin of 110000 (`:79`) — 6600 above, not fourteen thousand; 14000 is 124000 minus 110000, i.e. the pre-merge PIN. And the merge did not revert the prose: `git rev-list --parents -n1 3b78d8b` gives `e7ad913` (pin 124000) and `63f5f5c` (pin 123000), and both already carried the 116600 sentence at line 68. Only "a value neither parent held" survives. | Rewrite S2: the narrative ends at 116600, 6600 above the live pin, and both merge parents already carried that stale narrative; the merge separately wrote 110000, a pin neither parent held. |
| 18 | MEDIUM | unit 4 §6 AC3 and §7 Gates | Both spell `python scripts/check_citations.py --report`. The flag is `--report-only` (`scripts/check_citations.py:45` usage, `:268` `report = "--report-only" in sys.argv`). There is no argparse, so `--report` is silently ignored and the command runs gate mode — a different mode than the one written. M2's interface axis: a name spelled twice is spelled identically. | Spell `--report-only` if the report is wanted, or drop the flag if gate semantics are wanted, and say which. `python scripts/corpus_ids.py --check` in §7 was verified real. |
| 19 | MEDIUM | unit 4 §2 S3 and §6 AC3 | S3 says "its kickoff manifest carries exactly that citation", singular. There are two: `.claude/SESSION-KICKOFF.md:318` carries `READ_PATH_CEILING=110000` and `:320` carries "The live pin is 110000". Arm B's `CONST_CITE` (`scripts/check_citations.py:111`) requires an ALLCAPS name token adjacent to the number, so it sees `:318` only. Moving the gated site alone leaves a stale figure in a governing doc with AC3 green. | S3 names `.claude/SESSION-KICKOFF.md` by path, states that both occurrences move, and records that the gate covers only the first — so the second is a manual obligation. AC3 gains a grep witness for the literal 110000 in that file returning nothing. |
| 20 | MEDIUM | unit 4 §4 Files touched and §6 AC4 | The adopter repo's path appears nowhere in unit 4; the only pointer in the set is the rootless `incms/main/vendor/nicocares-package` in `README.md:35`. §4 says "two files, both in the adopter repo" and names neither. AC4 asserts "one commit, unpushed" with no branch stated; that tree is on `main`. | §4 names the absolute path and the two files. S5 states a path-scoped commit of exactly `.memory-tree.conf` and `.claude/SESSION-KICKOFF.md`, on `main`, no push — which is also what makes AC4 checkable. |
| 21 | MEDIUM | unit 1 §3 and unit 2 §3 | The invariant that the aggregate ceiling must bind before the per-member caps do is stated only in unit 4 §4, about another repo: "A ceiling above that sum can never fire before check 6 does, so it stops measuring anything." Unit 1 raises the shipped headroom for every adopter and unit 2 makes the caps declarations; together they let an adopter satisfy check 16 vacuously. Not violated in this repo today (4 x 61440 + 2 x 20480 = 286720 against 111994), which is why it surfaces at an adopter and not here. | State the invariant in unit 1 §3 and unit 2 §3, and add one observation: `--measure` names it when the printed ceiling exceeds the sum of the capped members' declared caps. |
| 22 | MEDIUM | build `README.md:46` and `:48` | Two structural statements are wrong. `:48` says "Unit 3 depends on unit 1" while unit 3 §4 sequences itself first and landed two commits before unit 1 exists — M2's ordering axis forbids a sub-spec depending on a unit sequenced after it. `:46` calls units 1 and 2 "independent mechanisms", but their Files-touched lists intersect on `check-memory-hygiene.sh`, `.memory-tree.conf.example`, the two hygiene doc carriers and the kit-version markers, which M6 keys parallelism on. | `:48` states the real relation: unit 3 consumes unit 1's chosen constant but lands first and therefore hard-codes a number unit 1 will later make derivable — a recorded inversion, not a dependency. `:46` adds that the two are independent in mechanism, not in write set, so they sequence. |
| 23 | MEDIUM | build-wide, unit 3 §4 | The ordering argument covers check 16 and misses check 6. `memory/backlog/TOOL.md` is 20345 B against the 20480 B cap at `check-memory-hygiene.sh:396` — 135 B, 85 of 250 lines — and `memory/backlog/TOOL.md:72` already records `TOOL-cSettledDocket-16` naming that wall. Any backlog row this build mints reds check 6. No spec in the set names it. Raising `INDEX_CAP_BYTES` would exceed the owner's ask, so the gap is in the record, not in the scope. | Unit 3 §4 or the build README names the second wall and states the disposition — rotate, shorten, or accept that the build mints no TOOL row — rather than leaving it to be discovered by a red gate. |
| 24 | LOW | unit 1 §4 Inventory | "would red the map's freshness leg until the map is re-rendered and the key claimed by a dossier" — the second clause is wrong. `map_extractors.py:170-171`: symbols feed `generated/symbols.json` only, "never the ratchet". The claimed-or-baselined ratchet runs over `INVENTORY_IDS` (`test_codebase_map.py:70,81`); the byte-compare at `:135-146` is the only thing a new symbol moves. | Drop the dossier clause. The design choice stands on its own. |
| 25 | LOW | unit 1 §2 S1 | "every consumer indexes `conf` directly" is false at `corpus_ids.py:198`, `conf.get("DEAD_PATH_EXCLUDE", "")` — the file already contains the alternative pattern the sentence says does not exist. Every other read is a direct index, so the conclusion holds. | "every consumer except `DEAD_PATH_EXCLUDE` indexes `conf` directly, so the new key must be in the defaults dict." |
| 26 | LOW | unit 1 §2 S5 and unit 2 §2 S7 | Both require declarations in `tools/memory-tree/.memory-tree.conf.example` (which exists, carrying `READ_PATH_CEILING=""` at `:56` and no cap keys). Nothing observes that file: unit 1's AC4 covers only the template/installed pair, and the only consumer repo-wide is `adopt-memory-tree.sh:40`, a copy. A builder can ship both units with the example half-done and every named gate green. | One acceptance bullet per unit naming the file and the keys, or accept it as ungated and say so under §5 user docs. |
| 27 | LOW | unit 2 §4 "Why validate" | The awk mechanism is described backwards, though both named outcomes are real. Measured on GNU Awk 5.4.0: unset and blank both compare numerically against 0 and red every file; a non-numeric binding compares as a string and reds nothing (`awk -v cb="abc" 'BEGIN{print (100+0>cb),(30000+0>cb)}'` prints `0 0`). The spec says an unset binding compares "as a string". | State which value produces which failure, and name the non-numeric case as the dangerous one, since it is the silent green. |
| 28 | LOW | unit 3 §6 AC1 | "headroom in the tens of kilobytes rather than the tens of bytes" is a human judgement; no command yields that verdict. Currently true at 86843 against 111994. | State the number: `--report`'s read-path total is under 111994 with at least 20000 B of margin. |
| 29 | LOW | unit 3 §4 | "the ceiling has to move before the first such append or check 16 reds" overstates what a commit can red on. `check-memory-hygiene.sh:847` guards the `corpus_ids.py --check` delegation behind `[ "$STAGED" = 0 ]`, and `.githooks/pre-commit:41` invokes only `bash "$g" --staged`. The binding deadline is the next full hygiene run. | Say "before the next full hygiene run". The sequencing conclusion is unchanged. |
| 30 | LOW | unit 4 §4 | "A ceiling above that sum can never fire before check 6 does" overstates its bound. `READ_PATH_WAIVER` names three of the seven members (11763 B) that count toward check 16's total and carry no cap, so a ceiling above 614400 can still fire on waived growth. | Qualify to "the capped members alone cannot reach it." Does not change the chosen number. |
| 31 | LOW | build `README.md:34` and unit 1 §4 | Both state 82 bytes of headroom in the present tense, and both were authored in or after `540e0d2`, which set the ceiling to 111994. Live headroom is 25151 B. Unit 3 §4 itself records the 82 B as already spent. | Past-tense both: at this build's base, 82 B remained. |
| 32 | LOW | unit 1 §2 S4 | S4 promises arms over `--measure` but names no way to observe it. `do_measure` prints and returns 0 (`corpus_ids.py:454-465`) while the selftest's `arm` helper compares a return value (`:537-547`), so a naive arm compares against `"0"` and fails; §4's Inventory constrains the fix by forbidding a new public name without resolving it. | S4 names the mechanism — capture stdout around `do_measure` and compare the printed line — and states that no public name is added. |

## Refuted

- **"check 6 has exactly one `fail` call site."** Claimed by the contradiction lens (as a verified-clean
  item) and by the prior-art lens. False: `grep -n "fail 6" tools/memory-tree/check-memory-hygiene.sh`
  returns 407 and 886, and `check-arms.py --report` lists both. The unstated-assumption lens had this
  right; it is finding 11 above, against the spec, not against the lens.
- **"`_scratch` has no parameter for extra conf keys, so the conf-varying seam does not exist"**
  (underspecification lens, finding 6, second bullet). Refuted at source: `_scratch` writes
  `.memory-tree.conf` first and only then runs `for rel, body in (extra or {}).items(): _w(tmp, rel,
  body)` before `return load_conf(tmp)`, so `extra={".memory-tree.conf": …}` overwrites it. The seam
  is already there. Only the stdout half of that finding survives, as finding 32.
- **"The adopter tree is currently dirty (` M README.md`), so a naive `git commit -a` sweeps an
  unrelated file"** (underspecification lens, finding 12). Not reproducible: `git status --porcelain`
  in `C:/projects/incms/main/vendor/nicocares-package` returns nothing. The branch half of that
  finding is real and survives as finding 20; the dirty-tree half is dropped.
- **"Unit 1 §4 and unit 2 §4 disagree on how `memory/HYGIENE.md` may be changed"** (contradiction lens,
  finding 11). Refuted: unit 1 S6 already states that the live copies move only through the parity
  harness's render mode, never by hand, which is the same rule unit 2 §4 states. Listing the rendered
  file under Files touched is not a claim that it is hand-edited.
- **"`TOOL-aDeclaredCeiling-2`'s premise is falsified by this build's own recall probe"** (prior-art
  lens, finding 11). Dropped as unverified: this pass did not re-run `tools/memory-recall/query.py`,
  and the row's claim is about what the recall corpus can reach, which was not exercised here.
- **The prior-art lens's remedy for the check-6 wall** — "unit 2 gains an S-item declaring
  `INDEX_CAP_BYTES` in this repo's conf, or a fifth unit does". Demoted. The owner's ask covers making
  the caps adjustable and raising the read-path headroom default; raising a per-class cap default in
  this repo is outside it, and unit 2 §3 correctly declines to turn the knob it adds. The underlying
  risk is real and survives as finding 23 with the fix reduced to naming the disposition.
- **"Unit 1's non-goal about `READ_PATH_CEILING` is contradicted by unit 3's landed edit."** Checked
  and dropped: unit 1's diff still moves no ceiling; the genuine overlap is the headroom key, which is
  finding 7.

## What no lens checked

- Nobody ran the full bar. `GATE_FULL=1 bash tools/run-gates.sh` was not executed at base, so the
  claim that the set's named gates are green before any of its code lands rests on the one leg this
  pass did run (`check-memory-hygiene.sh`, exit 0, silent) plus reading the others' sources.
- Nobody re-derived unit 4's growth rates. The four bases in its §4 table — 3513, 3791, 5255 and 4000
  B/day, and the 10998 B largest-landing figure — were taken on trust. The arithmetic built on them
  (109998 + 131072 = 241070, the 5.6x/2.5x/2.2x ratios, 614400 as 4 x 153600) was verified; the rates
  themselves were not.
- Nobody exercised the specs against check 12 by construction. The four files parse today because the
  live gate is green, but no pass built a deliberately malformed variant to confirm which of the
  format rules would actually catch a regression in them.
- Nobody asked whether unit 2's six keys are the right six. The class split is preserved by
  construction and the table matches `check-memory-hygiene.sh:396-401` value for value, but no lens
  tested whether an adopter needs a seventh knob — the `RUN.md` and `STATUS.md` members of the index
  set take the row-document tier with no separate key, and that choice is unexamined in the set.
- Nobody read `tools/memory-tree/HYGIENE.template.md` end to end against S5 and S7. The three
  row-document cap statements at `:66`, `:128` and `:312` were located and the build-README class
  confirmed absent, but the actual replacement wording those two scope items imply was not drafted or
  reviewed.
