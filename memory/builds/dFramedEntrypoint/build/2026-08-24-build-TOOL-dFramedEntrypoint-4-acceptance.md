**Serves:** journal TOOL-dFramedEntrypoint-4

# Acceptance ledger — TOOL-dFramedEntrypoint-4

*Node d, 2026-08-24. Every line carries the command that made the observation.*

**Evidences:** TOOL-dFramedEntrypoint-4

- AC1 — `_parse_order("x · order 0x2 · y")` and `_parse_order("x · order 2x · y")` both raise, naming
  the offending value. Probed against the SHIPPED regex first, where they returned 0 and 2
  respectively and would have rendered as plausible steps. Armed twice, and both arms were watched
  failing with the old unanchored regex staged back in before it was restored.
- AC2 — `python tools/memory-tree/gen_build_index.py --write`, then the `gen:build-units` region of
  `memory/builds/dFramedEntrypoint/README.md`: every unit row carries its declared value in the ORDER
  cell, 1 through 8.
- AC3 — `a unit with no order verb renders an em-dash in that cell` — the arm over a fixture whose
  unit declares no verb: the ORDER cell renders an em-dash and that build's `gen:build-order` region
  keeps its empty-case sentence.
- AC4 — `the tier cell always renders a value` — the arm, plus this build's own rendered rows, which
  show Tier 1 for unit 8 and Tier 2 for the rest, each matching that spec's `**Status:**` header.
- AC5 — `an ordered unit sorts ahead of an unordered one` — the arm, read from the rendered file
  after `--write` and NOT from `--check`, which reports only which artifacts are stale. The arm was
  rewritten once: its first spelling compared string offsets over the whole region and read False on
  a correct sort, because both fixture ids appear earlier in the `ids` line than in the table.
- AC6 — `_parse_order("x · order 2 · order 3 · y")` raises `the order verb more than once`. This
  refusal was UNREACHABLE when first written: it sat below the early return, so a header whose first
  occurrence was well-formed never reached it. Found by running it, not by reading it.
- AC7 — `memory/builds/dFramedEntrypoint/README.md` renders a `gen:build-order` step table rather
  than the empty-case sentence. This is the FIRST non-empty build-order region in the corpus: the
  verb shipped with zero adopters, and this build is its own first.
- AC8 — `BUILD_README_ENTRY_CAP_CHARS` — the widest generated unit row across all tracked build
  READMEs measures 236 characters, in `memory/builds/dUnstalledConvoy/README.md`, against that 350
  tier. It was 230 before this unit, so two cells cost 6 characters on the widest row rather than
  the 8 the spec estimated: the estimate assumed both cells were one ASCII character, and an
  em-dash is three bytes and one character.
- AC9 — `python tools/memory-tree/gen_build_index.py --selftest` — PASS, 95 `arm ok` lines against
  84 before this unit. The bare merge bar HOLDS that leg, so this unit's Definition of Done is
  `GATE_FULL=1 GATE_SELFTESTS=1 bash tools/run-gates/run-gates.sh` and the leg was additionally run
  standalone here.

## Spec amended during the build

S3 moved the order verb's validation from hygiene check 12 to the generator's own `_parse_order`, as
a rev bump with its §9 line before any code, per M2. Two reasons, both only visible with the code in
front of me: a second validator in shell would give one verb two definitions, and it would cost an
armed branch, a floor movement and a fixture in a 181-second self-test to restate a refusal the
generator already makes on an unguarded leg.

## What this unit cost another build

`memory/builds/aPacedTurnstile/README.md` went 25,579 to 25,603 bytes against a 25,600 cap — three
bytes over, from eight unit rows each gaining two cells. The round-2 spec audit named this exact file
as the one close enough to the cap that any byte reds it, so it is a recorded consequence rather than
a surprise. It is another build's record and no authoring choice inside it shortens a cell the
renderer emits, so it takes a curation-debt row whose blast radius is measured and whose discharge is
named: unit 5 removes the records table from every build README, and that is the larger half of this
file's generated bytes.

## Divergence found and parked

`--status` and `--plan` now disagree about which unit is next. `--status` reads the generated region,
which this unit renders in build order, and names unit 4; `--plan` sorts tracked specs by id and names
unit 2. Neither is wrong about STATUS. The fix belongs to `tools/unattended/`, a different kit with
its own contract that spec 4's non-goals do not reach, so it is parked with its three options.
