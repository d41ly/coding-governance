**Serves:** journal TOOL-dFramedEntrypoint-8

# Acceptance ledger — TOOL-dFramedEntrypoint-8

*Node d, 2026-08-24. Every line carries the command or file that made the observation. Measured on
the staged tree before the unit's commit, which is what the hygiene gate grades.*

**Evidences:** TOOL-dFramedEntrypoint-8

- AC1 — `memory/DECISIONS.md` — the row is present under the tooling section, names both reversed
  records, carries the word `knowingly` and the owner attribution, and carries the re-measurement.
- AC2 — `git ls-files 'memory/builds/*/README.md' | wc -l` — the corpus is 62 tracked build READMEs
  including this build's own, 61 before it. The ratio the reversal rests on is 20 of 61 authored
  halves carrying no heading, against the 17 of 25 the original refusal measured. Both figures and
  the corpus growth are in the decision row and in the census research record this build folded.
- AC3 — `grep -c "being RETIRED\|retires that pair's readers\|four build READMEs that carry one"
  memory/map/features/build-readme-surface.md` returns 0, and
  `git ls-files 'memory/builds/*/README.md' | xargs grep -lF '<!-- roster:units -->' | wc -l`
  returns 11, which is the count the corrected dossier now states.
- AC4 — `grep -c "owns its own prose" memory/project/curation-debt.txt` returns 0. The correction was
  reworded once for this reason: the first draft explained the miscitation by quoting the phrase,
  which left the criterion's own probe unable to distinguish a claim from its denial. An acceptance
  criterion that names a grep is a criterion that has to survive that grep.
- AC5 — `memory/backlog/TOOL.md` — the row is present and names both files that carry the sentence,
  `AGENTS.md` and `tools/hooks/README.md`, and the hook constant that contradicts them.
- AC6 — `git diff 470bb09b --stat -- memory/builds/aMouldedFolio/` returns empty. The census record
  is unchanged, which is the append-only rule for a ratified record.
- AC7 — `bash tools/memory-tree/check-memory-hygiene.sh` exits 0, check 16 included. Margins from
  `python tools/memory-tree/corpus_ids.py --report`: 132,708 B at the build's base against a 132,760
  ceiling, so 52 B of margin before the append; 132,985 B after it. `READ_PATH_CEILING` moved
  132,760 to 133,138, a minimal raise on the precedent the conf's own comments record, with its
  reason written beside it. The raise deliberately does not cover unit 1's growth of
  `memory/guides/BUILD-METHOD.md`; that unit carries its own item and prices its own charge.
- AC8 — `python tools/drift-audit/drift_report.py` — `readme_mechanism_drift` re-derived at 19 of 62,
  status ok. The value is recorded here rather than re-baselined, because the signal scans the
  authored region this build is about to shrink and the pin belongs against the corpus the contract
  produces, which is unit 7's landing.

## What moved that no criterion asked for

The decision row was written at 482 characters and rewritten at 274. Hygiene check 7 caps an entry
line in an index file at 300, and `memory/DECISIONS.md` is an index file. The first draft named the
registry mechanism as well as the reversal; the surviving row names the reversal, its two records,
the two ratios and nothing else. The read-path figure in `.memory-tree.conf` was corrected in the
same pass, because the comment had already been written against the longer row's measurement and a
raise justified by a number that is no longer true is the class this unit exists to repair.
