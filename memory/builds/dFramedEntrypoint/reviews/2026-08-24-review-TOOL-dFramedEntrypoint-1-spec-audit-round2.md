**Serves:** spec-audit TOOL-dFramedEntrypoint-1..8

# Spec audit round 2 — the folded spec set

*Node d, 2026-08-24, base 9ddcc5c9. Five lenses primed on the FOLD itself — did each fix land, is it correct, what did it break, and what did round 1 miss — then five batched skeptics prompted to REFUTE and told to verify a did-not-land claim by reading the spec body at HEAD rather than its revision log.*

## Verdict: BLOCKED

Converging. The qualifier sits here rather than on the verdict line, because that line's token is a closed set and a tally or a mood appended to it makes a structural check read as a semantic one.

70 raw findings. 62 survived, 8 refuted or dropped, 0 ungraded. Precision 0.89.

Surviving by final severity: 0 BLOCKER, 10 HIGH, 26 MEDIUM, 26 LOW.

**The loop's exit condition is a strictly falling confirmed-blocker count, and it fell: 1 to 0.** That is the only convergence claim this record makes. The surviving MEDIUM and LOW tiers are larger than a reader might expect and are not evidence against convergence — round 2 was aimed at freshly-written fold text, which had never been reviewed, so most of what it found is fold hygiene rather than design error.

By kind: 25 FOLD_BROKE (the fold created it), 17 FOLD_WRONG (the fold misread its finding), 20 ROUND1_MISSED.

## What was folded from this round

The ten HIGH findings collapse to THREE distinct defects, each raised independently by several lenses. All three are folded; every MEDIUM and LOW below is recorded and NOT folded.

1. **The kit-version carrier set.** The round-1 fold answered its blocker by pointing at the epoch gate's remedy text plus the dogfooded parity copies. That formula resolves to five carriers where seven exist — the two it cannot reach are kit SOURCES rather than dogfood copies — and following it reds the unguarded `kit version markers` leg. `TOOL-dSettledRoster-4` in the backlog records this exact procedure failing twice and says to derive the list from the markers. The fold reproduced a documented recurring defect by copying round 1's own fix text. All six S-EPOCH items now carry a DERIVATION.

2. **The read-path charge.** Round 1's fix named two specs; the fold priced one, and rev-3 then narrowed that raise so it cannot cover the other. Unit 1 grows a capped read-path member with a margin in the low tens of bytes and priced nothing. It now carries its own ceiling S-item and a criterion recording both margins.

3. **The adopter's unarmed state.** Unit 2's rev-2 promised an adopter copy with no ceilings and an announced unarmed state, and specified no mechanism for either while its own criteria declared unconditional refusals that left no unarmed path. The mechanism is now named, and the high-water file gained the absent-state semantics the rev-2 split created and did not define.

## Standing, unfolded

Everything below survived verification and is NOT fixed. It is the round-3 worklist.

## HIGH — 10

### R2-1 — 2026-08-24-spec-dFramedEntrypoint-1.md · §4 Files touched (estimate) · FOLDED IN THIS ROUND

**Status:** CORRECTED · raised HIGH, final HIGH · kind FOLD_WRONG

**Finding.** Same mechanism, graded HIGH rather than BLOCKER: the S-EPOCH formula resolves to five of seven carriers and reds the unguarded `kit version markers` leg, costing a full-bar cycle per engine-moving unit, but it does not stop the build. Duplicate of R2-05.

**Evidence.** grep -n gate-legs on the spec returns :39 (S6's correct refusal), :131 ("`tools/gate-legs.json` for the blind-spot\nparagraph"), :135 ("NOT `tools/gate-legs.json` — see S6.") — the second and third are both inside the same §4 Files touched paragraph. Round 1 GATE-1's fix reads verbatim: "drop `tools/gate-legs.json` from spec-1 §4 Files touched". The canary that reds is tools/run-gates/run-gates.test.sh:96 `KNOWN = {"name","argv","guard","impure","chunk","subject"}`; measured over tools/gate-legs.json the live key union is exactly ['argv','chunk','guard','impure','name','subject'] across 85 legs.

**How the skeptic settled it.** Every factual citation reproduces — see my R2-05 verdict for the full reproduction: seven live carriers at 2.30, remedy() naming three at check-verdict-epoch.sh:160-166, kit-dogfood-parity.test.sh:53 fixing the three pairs, check-kit-versions.sh:94-106 deriving its population from `git ls-files 'tools/memory-tree/*.template.md'`, that leg unguarded with subject 'repo', and memory/backlog/TOOL.md:21 recording the identical failure twice. The claim as stated is right on the mechanism and wrong on the grade. BLOCKER does not survive the test that a blocker must actually stop the build: the bump is now present in all six specs, so check-verdict-epoch.sh's topological rule is satisfied and the residual failure is check-kit-versions.sh redding on two template markers — a loud, self-announcing, one-commit repair whose recorded cost in this repo is a second full-bar cycle, which the backlog row itself calls a papercut. Round 1's skeptic graded the same mechanism HIGH at UNB-4 for the same reason. Two minor citation slips, neither material: remedy() is at :160-166 (the finding says :165-170), and the backlog row's own text says SIX markers while `git grep` returns seven. This is the same defect as R2-05, which states it more precisely and additionally catches spec-5 carrying both answers; one repair closes both.

**Fix.** As R2-05: replace 'every site that gate's remedy names PLUS the dogfooded parity copies' in all six S-EPOCH paragraphs with the derived instruction — bump every carrier `git grep -l 'gov:kit memory-tree@'` returns, then `bash tools/memory-tree/kit-dogfood-parity.test.sh --render` — citing TOOL-dSettledRoster-4.

### R2-READPATH-1 — 2026-08-24-spec-dFramedEntrypoint-1.md (against 2026-08-24-spec-dFramedEntrypoint-8.md) · spec-1 §2 S3b (:24-30), §4 Files touched (:132-133), §7 Gates (:185); against spec-8 §2 S4b (:34-39) and §5 risks (:97-101) · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised HIGH, final HIGH · kind FOLD_WRONG

**Finding.** Round 1's READPATH-2 fix named TWO specs by name and the fold landed it in only one. Spec 1 grows memory/guides/BUILD-METHOD.md — a capped read-path member with 52 B of margin — and carries no read-path S-item, no `.memory-tree.conf` in Files touched, and no mention of READ_PATH_CEILING. The fold made this worse, not neutral: spec 8's rev-3 narrowed its raise to a MINIMAL one sized against a single decision row, and unit 8 lands first, so unit 1 lands second into a ceiling deliberately sized to exclude it.

**Evidence.** `python tools/memory-tree/corpus_ids.py --report` prints 'read path : 6 files, 132708 B' with memory/guides/BUILD-METHOD.md at 23868 B a member; `.memory-tree.conf:188 READ_PATH_CEILING="132760"` gives a 52 B margin; the refusal is corpus_ids.py:456-458 `if total > ceiling`. Spec-1 S3b edits tools/memory-tree/BUILD-METHOD.template.md and re-renders memory/guides/BUILD-METHOD.md to route the M2 classification and the M4 promotion notice — an ADDITION to a member at 52 B of headroom. Spec-1 §7 names `memory hygiene`, which carries check 16, the read-path check, and that leg has no guard in tools/gate-legs.json. `grep -n 'READ_PATH\|read-path\|memory-tree.conf' memory/builds/dFramedEntrypoint/spec/*.md` returns hits in spec-8 ONLY (lines 34, 35, 84, 97, 122, 148, 149) and zero in spec-1. Round 1 READPATH-2's Fix reads verbatim: 'Add .memory-tree.conf to spec-8 §4 with an S-item raising READ_PATH_CEILING and its reason, or trim a member in the same commit; do the same for spec 1's BUILD-METHOD.md edit.' Spec-8 rev-3 then narrowed further: §2 S4b 'The raise follows the minimal-raise precedent the conf's own comments record, not the measure-plus-headroom jump the tool prints', and §5 risks 'Only the DECISION APPEND is on that path ... so the raise is sized against one file's growth.' Spec-8 §4 Migration puts unit 8 FIRST in the build order and the stated build order lands unit 1 next.

**How the skeptic settled it.** Reproduced. `python tools/memory-tree/corpus_ids.py --report` prints 'read path : 6 files, 132708 B' with memory/guides/BUILD-METHOD.md a member at 23868 B; .memory-tree.conf:188 READ_PATH_CEILING="132760", so the margin is 52 B, and the refusal is corpus_ids.py:456-458 `if total > ceiling`. `grep -n 'READ_PATH\|read-path\|read path\|memory-tree.conf' memory/builds/dFramedEntrypoint/spec/*.md` returns hits in spec-8 ONLY (lines 34, 35, 84, 97, 122, 148, 149) and zero in spec-1 — verified at HEAD, not inferred from a revision log. Spec-1 S3 and S3b both edit tools/memory-tree/BUILD-METHOD.template.md and re-render memory/guides/BUILD-METHOD.md, an addition to a capped member; spec-1 §4 Files touched does not name .memory-tree.conf and no AC prices the margin. Round-1 READPATH-2's Fix reads verbatim 'do the same for spec 1's BUILD-METHOD.md edit' (record line 99) and that half did not land. Spec-1 §7 names `memory hygiene`, whose leg is unguarded, so the leg that reds is one the spec already claims. Spec-8 S4b now narrows the raise ('sized against one file's growth', §5 :99-101) and spec 8 lands first, so unit 1's growth is priced by nothing. Held at HIGH, matching round 1's grade for the same class: the remedy is a one-line conf edit, so it stops a commit at the gate rather than the build.

**Fix.** Give spec 1 an S-item pricing the BUILD-METHOD.md growth against READ_PATH_CEILING, with .memory-tree.conf in §4 Files touched and an AC recording the pre- and post-edit margin (spec-8 AC7's shape), or make spec-8 S4b size its raise to cover both growths and say which two it prices.

### R2-2 — 2026-08-24-spec-dFramedEntrypoint-1.md (with -8) · spec 1 §2 S3/S3b and §4 Files touched; spec 8 §2 S4b · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised HIGH, final HIGH · kind FOLD_WRONG

**Finding.** Round 1's READPATH-2 fix had two halves — price spec 8's decision append AND spec 1's BUILD-METHOD.md edit. The fold landed the first and, in rev-3, actively NARROWED it so it cannot cover the second: S4b now sizes the raise "against one file's growth" and follows "the minimal-raise precedent". `memory/guides/BUILD-METHOD.md` is a read-path member with 52 B of margin, spec 1 grows it (S3 pointer + S3b routing), and spec 1 carries no ceiling item and no read-path mention at all. Unit 1 lands on a red `memory hygiene` leg.

**Evidence.** `python tools/memory-tree/corpus_ids.py --report` prints `read path : 6 files, 132708 B` with `23868 B memory/guides/BUILD-METHOD.md` among the six; `.memory-tree.conf:188 READ_PATH_CEILING="132760"` → 52 B margin. Refusal: tools/memory-tree/corpus_ids.py `if total > ceiling`, reached by hygiene check 16; the `memory hygiene` leg in tools/gate-legs.json carries no `guard` and `subject: repo`. `tools/memory-tree/BUILD-METHOD.template.md` is 23879 B and `memory/guides/BUILD-METHOD.md` 23868 B, so the template-first edit spec 1 §4 mandates lands in the capped member. `grep -n 'READ_PATH\|memory-tree.conf' memory/builds/dFramedEntrypoint/spec/2026-08-24-spec-dFramedEntrypoint-1.md` returns nothing. Spec 8 §2 S4b: "Only the DECISION APPEND is on that path … so the raise is sized against one file's growth" and "The raise follows the minimal-raise precedent" (.memory-tree.conf:180-187 records that precedent as "the smallest that is not a pin needing another movement on the next byte").

**How the skeptic settled it.** Reproduced by running the tool. `python tools/memory-tree/corpus_ids.py --report` prints 'read path : 6 files, 132708 B' and enumerates memory/guides/BUILD-METHOD.md at 23,868 B among the six; .memory-tree.conf:188 pins READ_PATH_CEILING="132760", so the live margin is 52 B. The `memory hygiene` leg in tools/gate-legs.json carries no `guard` and subject 'repo', so check 16 runs on every bar. Spec 1 grows that member twice — S3 (spec-1:21-23) updates the build method's pointer and S3b (:24-30) routes the classification and promotion writes, with §4 Files touched (:132-133) mandating `tools/memory-tree/BUILD-METHOD.template.md` edited FIRST and `memory/guides/BUILD-METHOD.md` re-rendered from it (template 23,879 B, live 23,868 B, so the render lands in the capped member). `grep -n 'READ_PATH\|ceiling\|memory-tree.conf'` over spec 1 returns exactly one hit, 'runaway-ceiling' at :26 — an unrelated term — so spec 1 prices nothing and carries no §4 conf edit. Meanwhile the fold's rev-3 actively narrowed the only unit that does price it: spec-8:99-101 now reads 'Only the DECISION APPEND is on that path — the dossier edit is not a member — so the raise is sized against one file's growth', and S4b (:37-39) binds the raise to 'the minimal-raise precedent the conf's own comments record', which .memory-tree.conf:175-182 defines as leaving 'the smallest [margin] that is not a pin needing another movement on the next byte' (153 B when last exercised). With unit 8 landing FIRST in the build order and unit 1 next, spec 1's growth lands against that minimal residue. Round 1 named this half explicitly ('Spec-1's BUILD-METHOD.md edit (23868 B, same budget) prices nothing either') and it was not folded. HIGH, matching round 1's grade for the same mechanism: it reds an unguarded merge-bar leg on unit 1's first commit and the repair is one conf line, so it stops the commit, not the build.

**Fix.** Either widen spec-8 S4b to price BOTH growths — naming spec 1's BUILD-METHOD.md edit as a second charge against the same ceiling — or give spec 1 its own S-item raising READ_PATH_CEILING with its reason, plus `.memory-tree.conf` in its §4 Files touched. Whichever unit owns it must state the post-edit margin, measured with `python tools/memory-tree/corpus_ids.py --report`.

### R2-READPATH-3 — 2026-08-24-spec-dFramedEntrypoint-1.md (with spec 8 §5) · spec-1 §2 S3/S3b, §4 Files touched, §5 risks, AC7; spec-8 §2 S4b · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised HIGH, final HIGH · kind FOLD_WRONG

**Finding.** Round 1's READPATH-2 fix said in as many words: "Add .memory-tree.conf to spec-8 §4 with an S-item raising READ_PATH_CEILING ... do the same for spec 1's BUILD-METHOD.md edit." The fold landed only the spec-8 half, and then NARROWED the raise so it cannot cover spec 1. memory/guides/BUILD-METHOD.md is a capped read-path member; spec 1 edits it in S3 and S3b, prices nothing, and AC7 asserts the whole bar is green.

**Evidence.** `python tools/memory-tree/corpus_ids.py --report` prints "read path : 6 files, 132708 B" over memory/DECISIONS.md 17544, memory/LIVE.md 1649, memory/guides/BUILD-METHOD.md 23868, memory/guides/REVIEW-PROTOCOL.md 15614, memory/guides/SESSION-KICKOFF.md 25195, memory/guides/UNATTENDED-PROTOCOL.md 48838. `.memory-tree.conf:188 READ_PATH_CEILING="132760"` — margin 52 B on this tree. Refusal at tools/memory-tree/corpus_ids.py:456-458 (`if total > ceiling`). Spec 8 §5 risks now says "Only the DECISION APPEND is on that path — the dossier edit is not a member — so the raise is sized against one file's growth", and S4b says the raise "follows the minimal-raise precedent the conf's own comments record" (.memory-tree.conf:175-182, +160 B for one manifest claim). Spec 1's §4 Files touched lists `tools/memory-tree/BUILD-METHOD.template.md` edited FIRST with `memory/guides/BUILD-METHOD.md` re-rendered, and does NOT list `.memory-tree.conf`; spec 1 §5 risks names only "a predicate that reds innocent files"; spec 1 §7 names `memory hygiene` and AC7 asserts `bash tools/run-gates/run-gates.sh` is green. Under the declared build order (8 first, then 1 and 4) unit 8 raises the ceiling to roughly the decision row and unit 1 immediately spends it on a read-path member nobody priced.

**How the skeptic settled it.** Reproduced by running the tool. `python tools/memory-tree/corpus_ids.py --report` prints 'read path : 6 files, 132708 B' and enumerates memory/guides/BUILD-METHOD.md at 23868 B as a member; .memory-tree.conf:188 `READ_PATH_CEILING="132760"`, so the margin is 52 B on this tree, and the refusal is corpus_ids.py `if total > ceiling`. Round 1's READPATH-2 fix text is verbatim 'Add .memory-tree.conf to spec-8 §4 with an S-item raising READ_PATH_CEILING and its reason, or trim a member in the same commit; do the same for spec 1's BUILD-METHOD.md edit.' The fold landed only the spec-8 half (S4b) and then narrowed it: spec 8 §5 risks now reads 'Only the DECISION APPEND is on that path — the dossier edit is not a member — so the raise is sized against one file's growth', and S4b sizes it on the +160 B minimal-raise precedent the conf records at :175-182. Spec 1 S3 and S3b edit tools/memory-tree/BUILD-METHOD.template.md with memory/guides/BUILD-METHOD.md re-rendered — a capped member — while spec 1 §4 Files touched lists no .memory-tree.conf and §5 risks names only 'a predicate that reds innocent files'. Under the declared order (8 first, then 1 and 4), unit 8 spends the raise on its own row and unit 1 grows a member nobody priced, redding hygiene check 16 on the unguarded `memory hygiene` leg that spec 1 §7 names. One overstatement in the claim: spec 1 AC7 asserts the `build README slot contract` LEG is green, not the whole bar; §7 Gates is what carries `memory hygiene`. HIGH, matching round 1's grade for the identical defect on spec 8.

**Fix.** Give spec 1 an S-item that raises READ_PATH_CEILING in the same commit as the BUILD-METHOD.md re-render (or trims a member by the added width), add `.memory-tree.conf` to spec 1 §4 Files touched, and record the pre/post margin from `corpus_ids.py --report` in an AC as spec 8 AC7 already does. Alternatively have spec 8 S4b size its raise against BOTH growths and say so, so one unit owns the budget.

### R2-1 — 2026-08-24-spec-dFramedEntrypoint-1.md, -2, -3, -4, -5, -6 · §2 S-EPOCH (identical paragraph in all six; spec 5 also §2 S6) · FOLDED IN THIS ROUND

**Status:** CORRECTED · raised BLOCKER, final HIGH · kind FOLD_WRONG

**Finding.** Same mechanism, graded HIGH rather than BLOCKER: the S-EPOCH formula resolves to five of seven carriers and reds the unguarded `kit version markers` leg, costing a full-bar cycle per engine-moving unit, but it does not stop the build. Duplicate of R2-05.

**Evidence.** `grep -rn 'gov:kit memory-tree@'` returns seven live carriers: tools/memory-tree/check-memory-hygiene.sh:13, tools/memory-tree/HYGIENE.template.md:1, tools/memory-tree/BUILD-METHOD.template.md:1, tools/memory-tree/SPEC-TEMPLATE.template.md:1, memory/HYGIENE.md:1, memory/guides/BUILD-METHOD.md:1, memory/TEMPLATE-SPEC.md:1 (all at 2.30). check-verdict-epoch.sh:165-170 `remedy()` names exactly three: the engine, HYGIENE.template.md, memory/HYGIENE.md. The dogfooded parity copies (the `kit/dogfood doc parity` leg's guard in tools/gate-legs.json) are memory/HYGIENE.md, memory/TEMPLATE-SPEC.md, memory/guides/BUILD-METHOD.md — so remedy ∪ dogfood = 5 files and both `tools/memory-tree/*.template.md` carriers other than HYGIENE are outside it. tools/check-kit-versions.sh:94-109 enumerates `git ls-files 'tools/memory-tree/*.template.md'` (3 files) and emits `kit-versions: $t marker != KIT_MEMORY_TREE_VERSION ($c)` + `fails++` for each mismatch; its leg `kit version markers` in tools/gate-legs.json carries no `guard` and `subject: repo`, so it runs on every bar. memory/backlog/TOOL.md:21 (TOOL-dSettledRoster-4, OPEN) records this exact failure verbatim — "there are SIX `gov:kit memory-tree@` markers … Following the message exactly leaves the bar RED on `kit version markers` — hit on 2026-08-20 … HIT AGAIN 2026-08-21 during dUnstalledConvoy … the fix cost a second full-bar cycle".

**How the skeptic settled it.** Every factual citation reproduces — see my R2-05 verdict for the full reproduction: seven live carriers at 2.30, remedy() naming three at check-verdict-epoch.sh:160-166, kit-dogfood-parity.test.sh:53 fixing the three pairs, check-kit-versions.sh:94-106 deriving its population from `git ls-files 'tools/memory-tree/*.template.md'`, that leg unguarded with subject 'repo', and memory/backlog/TOOL.md:21 recording the identical failure twice. The claim as stated is right on the mechanism and wrong on the grade. BLOCKER does not survive the test that a blocker must actually stop the build: the bump is now present in all six specs, so check-verdict-epoch.sh's topological rule is satisfied and the residual failure is check-kit-versions.sh redding on two template markers — a loud, self-announcing, one-commit repair whose recorded cost in this repo is a second full-bar cycle, which the backlog row itself calls a papercut. Round 1's skeptic graded the same mechanism HIGH at UNB-4 for the same reason. Two minor citation slips, neither material: remedy() is at :160-166 (the finding says :165-170), and the backlog row's own text says SIX markers while `git grep` returns seven. This is the same defect as R2-05, which states it more precisely and additionally catches spec-5 carrying both answers; one repair closes both.

**Fix.** As R2-05: replace 'every site that gate's remedy names PLUS the dogfooded parity copies' in all six S-EPOCH paragraphs with the derived instruction — bump every carrier `git grep -l 'gov:kit memory-tree@'` returns, then `bash tools/memory-tree/kit-dogfood-parity.test.sh --render` — citing TOOL-dSettledRoster-4.

### R2-04 — 2026-08-24-spec-dFramedEntrypoint-2.md · §2 S6b, against §6 AC9 and tools/memory-tree/kit.toml · FOLDED IN THIS ROUND

**Status:** CORRECTED · raised HIGH, final HIGH · kind FOLD_BROKE

**Finding.** S6b promises an adopter copy with no ceiling rows and an announced unarmed state but specs no mechanism for either: `[[files]] include = "**"` ships the gov file with its measured rows, and AC6 and AC9 declare unconditional refusals that leave no unarmed path. The mechanism exists in govkit (a literal-include rule with a non-engine role); it is simply not named.

**Evidence.** tools/memory-tree/kit.toml:10-12 '[[files]] include = "**" role = "engine"'. The precedent S6b invokes works the other way: the measured-pins hole at kit.toml:75-81 ships BLANK pins in `{kit}/.memory-tree.conf.example`, a file that is not gov's own measured `.memory-tree.conf`. spec-2 AC9 'When the limits file omits a row for a canonical slot, `--check-format` exits 1 naming the slot'. kit.toml gate_leg 'build README slot contract' carries subject 'repo' and guard [].

**How the skeptic settled it.** The packaging evidence reproduces and is the strongest thing in this batch. tools/memory-tree/kit.toml:10-12 declares '[[files]] include = "**" role = "engine"', and tools/govkit/govkit.py:2117 states that a `**` include means 'everything not otherwise claimed' over the tracked files under home — so a new tracked tools/memory-tree/build-readme-slot-limits.txt ships to every adopter carrying gov's measured ceilings unless another rule claims it. The measured-pins precedent S6b invokes works only because the file it blanks (`.memory-tree.conf.example`) is a distinct artefact and gov's own conf lives at the repo root, outside the kit home. The second horn reproduces too: kit.toml:142-145 declares the `build README slot contract` leg with subject 'repo' and guard [], so every adopter runs it, and spec-2 AC6 and AC9 make both an absent limits file and a missing canonical row unconditional refusals, which contradicts S6b's 'announced unarmed state' — unlike the pins hole, which works because corpus_ids.py:450 gives the engine a real off-switch (`if conf['READ_PATH_CEILING']:`). What is wrong is the word 'unsatisfiable'. The mechanism exists: govkit already supports a literal-include rule with a different role (tools/govkit/selftest.py:366-367 shows `{'include': '**', 'role': 'engine'}` beside `{'include': ['corpus_ids.py'], 'role': 'project-owned'}`), so S6b is unspecified, not impossible. Held at HIGH because it is the only finding in this batch that gov's own bar cannot catch — gov's limits file will have rows, so the adopter breakage is invisible here.

**Fix.** Name the packaging mechanism in S6b — a `[[files]]` rule claiming build-readme-slot-limits.txt with a non-engine role, or a `.txt.example` scaffolded by adopt-memory-tree.sh the way unit 3 S6c scaffolds the registry — and scope AC6 and AC9 to a non-empty declared population so the adopter's unarmed state is a declared pass rather than a red.

### R2-EPOCH-1 — 2026-08-24-spec-dFramedEntrypoint-{1,2,3,4,5,6}.md · §2 S-EPOCH (identical paragraph in all six specs); spec-5 §4 Migration · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised HIGH, final HIGH · kind FOLD_BROKE

**Finding.** The S-EPOCH paragraph pasted into six specs resolves to five carrier sites where seven exist; the two it cannot reach (SPEC-TEMPLATE.template.md, BUILD-METHOD.template.md) are kit sources rather than dogfood copies, and following it exactly reds the unguarded `kit version markers` leg. The formula was copied from round 1's SEQ-1 fix text (which did prescribe it); UNB-4's fix did not.

**Evidence.** check-verdict-epoch.sh remedy() prints exactly three: "$ENGINE (the constant AND the gov:kit marker on that same line)", "tools/memory-tree/HYGIENE.template.md (line 1)", "memory/HYGIENE.md (line 1)". `git grep -n "gov:kit memory-tree@"` outside memory/builds and memory/archive returns seven live carriers: tools/memory-tree/check-memory-hygiene.sh:13, tools/memory-tree/HYGIENE.template.md:1, tools/memory-tree/SPEC-TEMPLATE.template.md:1, tools/memory-tree/BUILD-METHOD.template.md:1, memory/HYGIENE.md:1, memory/TEMPLATE-SPEC.md:1, memory/guides/BUILD-METHOD.md:1 — all at 2.30. tools/check-kit-versions.sh:102-105 greps every shipped template for `gov:kit memory-tree@$V` and for the constant's value, and the `kit version markers` leg in tools/gate-legs.json carries no `guard`, so it runs on every bar. memory/backlog/TOOL.md:21 is OPEN and states this verbatim: "`check-verdict-epoch.sh`'s remediation message names THREE carriers to bump ... and there are SIX `gov:kit memory-tree@` markers: `BUILD-METHOD.template.md`, `SPEC-TEMPLATE.template.md`, `memory/TEMPLATE-SPEC.md` and `memory/guides/BUILD-METHOD.md` are absent from it. Following the message exactly leaves the bar RED on `kit version markers` — hit on 2026-08-20 ... HIT AGAIN 2026-08-21 during dUnstalledConvoy: following the message exactly bumped three carriers, the bar redded on the other three, and the fix cost a second full-bar cycle". The round-1 record's own fix text (SEQ-1, UNB-4) prescribed the same defective superset, and the fold copied it into six specs rather than checking it.

**How the skeptic settled it.** Reproduced end to end, and the fold's own provenance is worse than the finding states. check-verdict-epoch.sh:160-166 `remedy()` prints exactly three carriers: $ENGINE, tools/memory-tree/HYGIENE.template.md, memory/HYGIENE.md. kit-dogfood-parity.test.sh:53 defines exactly three parity pairs, so 'the dogfooded parity copies' resolves to memory/HYGIENE.md, memory/TEMPLATE-SPEC.md, memory/guides/BUILD-METHOD.md. Union = five. `git grep -n 'gov:kit memory-tree@'` outside memory/builds and memory/archive returns SEVEN live carriers, all at 2.30 — the two the formula cannot reach are tools/memory-tree/SPEC-TEMPLATE.template.md:1 and tools/memory-tree/BUILD-METHOD.template.md:1, which are kit SOURCES, not dogfood copies. check-kit-versions.sh:94-110 iterates every tracked `tools/memory-tree/*.template.md` and reds the elif branch on a marker != the constant; the `kit version markers` leg in tools/gate-legs.json carries NO guard, so it runs on every bar. memory/backlog/TOOL.md:21 (TOOL-dSettledRoster-4, OPEN) records this exact procedure redding the bar on 2026-08-20 and again on 2026-08-21 during dUnstalledConvoy, costing a second full-bar cycle, and instructs 'Derive the list from the markers.' The S-EPOCH paragraph is verbatim in specs 1, 2, 3, 4, 5 and 6. Spec 5 is internally inconsistent about it: its §4 Migration enumerates correctly ('the kit version at four gated sites with three dogfooded parity copies' = 7) while its own S-EPOCH carries the five-site formula. One evidence correction: SEQ-1's fix text DID prescribe this superset verbatim ('the three sites the gate's own remedy names, plus the dogfooded parity copies as spec-5 S6 already describes'), but UNB-4's fix did not — it prescribed placement plus a §7 listing. HIGH on round 1's own calibration: it kept SEQ-1 at BLOCKER for five specs whose landing plan reds an unguarded merge-bar leg in every ordering; this is six specs redding a different unguarded leg in every ordering, and the repo has an open row saying so twice.

**Fix.** Replace the 'remedy names PLUS the dogfooded parity copies' clause in all six S-EPOCH paragraphs with a DERIVATION — `git grep -l 'gov:kit memory-tree@'` over tracked paths outside memory/builds and memory/archive, plus the engine constant — and name `check-kit-versions.sh` (leg `kit version markers`) alongside `check-verdict-epoch.sh` in each §7. Cite TOOL-dSettledRoster-4 so the next reader does not rediscover it a fourth time. Spec 5's §4 Migration already carries the correct enumeration and is the wording to lift.

### R2-2 — CROSS — spec-8 §2 S4b/§5 risks vs spec-1 §2 S3/S3b and §4 Files touched · spec-8 S4b and §5 risks; spec-1 S3, S3b, §4 Files touched, §6 (no AC), §7 Gates · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised HIGH, final HIGH · kind FOLD_WRONG

**Finding.** Round 1's READPATH-2 fix said to price BOTH read-path growths. The fold priced only unit 8's, and rev-3 then actively NARROWED it — "Only the DECISION APPEND is on that path ... so the raise is sized against one file's growth". But unit 1 S3/S3b add prose to `memory/guides/BUILD-METHOD.md`, which IS a read-path member, and unit 1 lands in the wave immediately after unit 8. Unit 1 carries no read-path S-item, does not name `.memory-tree.conf` in §4, and has no AC for check 16 — while §7 Gates names `memory hygiene`, the leg that reds.

**Evidence.** `python tools/memory-tree/corpus_ids.py --report` on the staged tree: "read path : 6 files, 132708 B", members listed include `memory/guides/BUILD-METHOD.md 23868 B`. `.memory-tree.conf:188 READ_PATH_CEILING="132760"` → margin 52 B. Refusal is corpus_ids.py `if total > ceiling` (check 16; the conf comment at :192 reads "check 16 compares against READ_PATH_CEILING alone"). spec-8 rev-3 revision log: "The read-path claim is narrowed to the decision append". spec-1 S3: "The method's pointer is updated to name the slot"; S3b: "`tools/memory-tree/BUILD-METHOD.template.md` is edited to say so with `memory/guides/BUILD-METHOD.md` re-rendered from it" — both additive. Round 1 READPATH-2's fix, verbatim: "do the same for spec 1's BUILD-METHOD.md edit."

**How the skeptic settled it.** Reproduced by running the tool. `python tools/memory-tree/corpus_ids.py --report` prints 'read path : 6 files, 132708 B' and enumerates memory/guides/BUILD-METHOD.md at 23,868 B among the six; .memory-tree.conf:188 pins READ_PATH_CEILING="132760", so the live margin is 52 B. The `memory hygiene` leg in tools/gate-legs.json carries no `guard` and subject 'repo', so check 16 runs on every bar. Spec 1 grows that member twice — S3 (spec-1:21-23) updates the build method's pointer and S3b (:24-30) routes the classification and promotion writes, with §4 Files touched (:132-133) mandating `tools/memory-tree/BUILD-METHOD.template.md` edited FIRST and `memory/guides/BUILD-METHOD.md` re-rendered from it (template 23,879 B, live 23,868 B, so the render lands in the capped member). `grep -n 'READ_PATH\|ceiling\|memory-tree.conf'` over spec 1 returns exactly one hit, 'runaway-ceiling' at :26 — an unrelated term — so spec 1 prices nothing and carries no §4 conf edit. Meanwhile the fold's rev-3 actively narrowed the only unit that does price it: spec-8:99-101 now reads 'Only the DECISION APPEND is on that path — the dossier edit is not a member — so the raise is sized against one file's growth', and S4b (:37-39) binds the raise to 'the minimal-raise precedent the conf's own comments record', which .memory-tree.conf:175-182 defines as leaving 'the smallest [margin] that is not a pin needing another movement on the next byte' (153 B when last exercised). With unit 8 landing FIRST in the build order and unit 1 next, spec 1's growth lands against that minimal residue. Round 1 named this half explicitly ('Spec-1's BUILD-METHOD.md edit (23868 B, same budget) prices nothing either') and it was not folded. HIGH, matching round 1's grade for the same mechanism: it reds an unguarded merge-bar leg on unit 1's first commit and the repair is one conf line, so it stops the commit, not the build.

**Fix.** Either widen spec-8 S4b to price BOTH growths — naming spec 1's BUILD-METHOD.md edit as a second charge against the same ceiling — or give spec 1 its own S-item raising READ_PATH_CEILING with its reason, plus `.memory-tree.conf` in its §4 Files touched. Whichever unit owns it must state the post-edit margin, measured with `python tools/memory-tree/corpus_ids.py --report`.

### R2-EPOCH-1 — CROSS — specs 1, 2, 3, 4, 5, 6 (all six S-EPOCH items) · spec-1 §2 S-EPOCH (:49-54) · spec-2 (:47-52) · spec-3 (:41-46) · spec-4 (:32-37) · spec-5 (:50-55) · spec-6 (:40-45); each spec's §7 Gates · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised HIGH, final HIGH · kind FOLD_WRONG

**Finding.** The S-EPOCH paragraph pasted into six specs resolves to five carrier sites where seven exist; the two it cannot reach (SPEC-TEMPLATE.template.md, BUILD-METHOD.template.md) are kit sources rather than dogfood copies, and following it exactly reds the unguarded `kit version markers` leg. The formula was copied from round 1's SEQ-1 fix text (which did prescribe it); UNB-4's fix did not.

**Evidence.** check-verdict-epoch.sh remedy() at :163-169 names THREE files: tools/memory-tree/check-memory-hygiene.sh, tools/memory-tree/HYGIENE.template.md, memory/HYGIENE.md. kit-dogfood-parity.test.sh:57 declares PAIRS = memory/HYGIENE.md↔HYGIENE.template.md, memory/TEMPLATE-SPEC.md↔SPEC-TEMPLATE.template.md, memory/guides/BUILD-METHOD.md↔BUILD-METHOD.template.md, and its header pins DIRECTION: '--render writes TEMPLATE -> LIVE'. So 'the remedy's sites PLUS the parity copies' = {check-memory-hygiene.sh, HYGIENE.template.md, memory/HYGIENE.md, memory/TEMPLATE-SPEC.md, memory/guides/BUILD-METHOD.md} and OMITS tools/memory-tree/SPEC-TEMPLATE.template.md and tools/memory-tree/BUILD-METHOD.template.md. check-kit-versions.sh:94-108 derives its population as `git ls-files 'tools/memory-tree/*.template.md'` (verified: exactly those three templates) and emits '$t marker != KIT_MEMORY_TREE_VERSION' + fails++ for each stale one; its leg {"name":"kit version markers","chunk":"declarations","subject":"repo"} carries NO guard, so it runs on every bar. The parity leg {"name":"kit/dogfood doc parity", guard:[memory/HYGIENE.md, memory/TEMPLATE-SPEC.md, memory/guides/BUILD-METHOD.md, tools/lib/, tools/memory-tree/]} arms on every one of these six units, and because --render writes template->live, bumping memory/TEMPLATE-SPEC.md without SPEC-TEMPLATE.template.md is drift by construction — the prescribed action is not merely incomplete, it is inoperable. The repo's own record: memory/backlog/TOOL.md:21 (TOOL-dSettledRoster-4) — 'the remediation message names THREE carriers ... and there are SIX gov:kit memory-tree@ markers ... Following the message exactly leaves the bar RED on `kit version markers` — hit on 2026-08-20 ... HIT AGAIN 2026-08-21 ... the fix cost a second full-bar cycle'; and :151 (TOOL-aBoundedVerdict-29), same defect. Decisive internal contradiction: spec-5 §4 Migration (:88) already states the right enumeration — 'the kit version at four gated sites with three dogfooded parity copies' (constant + 3 templates, + 3 live copies) — two paragraphs above its own S-EPOCH.

**How the skeptic settled it.** Reproduced end to end, and the fold's own provenance is worse than the finding states. check-verdict-epoch.sh:160-166 `remedy()` prints exactly three carriers: $ENGINE, tools/memory-tree/HYGIENE.template.md, memory/HYGIENE.md. kit-dogfood-parity.test.sh:53 defines exactly three parity pairs, so 'the dogfooded parity copies' resolves to memory/HYGIENE.md, memory/TEMPLATE-SPEC.md, memory/guides/BUILD-METHOD.md. Union = five. `git grep -n 'gov:kit memory-tree@'` outside memory/builds and memory/archive returns SEVEN live carriers, all at 2.30 — the two the formula cannot reach are tools/memory-tree/SPEC-TEMPLATE.template.md:1 and tools/memory-tree/BUILD-METHOD.template.md:1, which are kit SOURCES, not dogfood copies. check-kit-versions.sh:94-110 iterates every tracked `tools/memory-tree/*.template.md` and reds the elif branch on a marker != the constant; the `kit version markers` leg in tools/gate-legs.json carries NO guard, so it runs on every bar. memory/backlog/TOOL.md:21 (TOOL-dSettledRoster-4, OPEN) records this exact procedure redding the bar on 2026-08-20 and again on 2026-08-21 during dUnstalledConvoy, costing a second full-bar cycle, and instructs 'Derive the list from the markers.' The S-EPOCH paragraph is verbatim in specs 1, 2, 3, 4, 5 and 6. Spec 5 is internally inconsistent about it: its §4 Migration enumerates correctly ('the kit version at four gated sites with three dogfooded parity copies' = 7) while its own S-EPOCH carries the five-site formula. One evidence correction: SEQ-1's fix text DID prescribe this superset verbatim ('the three sites the gate's own remedy names, plus the dogfooded parity copies as spec-5 S6 already describes'), but UNB-4's fix did not — it prescribed placement plus a §7 listing. HIGH on round 1's own calibration: it kept SEQ-1 at BLOCKER for five specs whose landing plan reds an unguarded merge-bar leg in every ordering; this is six specs redding a different unguarded leg in every ordering, and the repo has an open row saying so twice.

**Fix.** Replace the 'remedy names PLUS the dogfooded parity copies' clause in all six S-EPOCH paragraphs with a DERIVATION — `git grep -l 'gov:kit memory-tree@'` over tracked paths outside memory/builds and memory/archive, plus the engine constant — and name `check-kit-versions.sh` (leg `kit version markers`) alongside `check-verdict-epoch.sh` in each §7. Cite TOOL-dSettledRoster-4 so the next reader does not rediscover it a fourth time. Spec 5's §4 Migration already carries the correct enumeration and is the wording to lift.

### R2-05 — specs 1, 2, 3, 4, 5 and 6 — the shared S-EPOCH item · §2 S-EPOCH in each of the six specs ('at every site that gate's remedy names PLUS the dogfooded parity copies'), against spec-5 §4 Migration · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised HIGH, final HIGH · kind FOLD_WRONG

**Finding.** The fold's answer to round 1's UNB-4 names the bump sites by pointing at the epoch gate's remedy text plus the dogfood copies. That formula resolves to five sites and misses two. check-verdict-epoch.sh's remedy() prints exactly three paths, one of which is already a dogfood copy; adding the other two dogfood copies gives engine + HYGIENE.template.md + three live copies. But check-kit-versions.sh asserts the `gov:kit memory-tree@<version>` marker on ALL THREE tracked tools/memory-tree/*.template.md files. Following S-EPOCH literally leaves SPEC-TEMPLATE.template.md and BUILD-METHOD.template.md at the old version and reds `kit version markers` — a leg all six specs now list in §7 — and re-rendering the dogfood copies from those un-bumped templates reverts their markers too. Spec 5 states the correct arithmetic four paragraphs away, so one spec carries both answers.

**Evidence.** tools/memory-tree/check-verdict-epoch.sh:160-166 remedy() prints only: $ENGINE (tools/memory-tree/check-memory-hygiene.sh), tools/memory-tree/HYGIENE.template.md, memory/HYGIENE.md. tools/check-kit-versions.sh:94-109 iterates `git ls-files 'tools/memory-tree/*.template.md'` — which returns BUILD-METHOD.template.md, HYGIENE.template.md and SPEC-TEMPLATE.template.md — and fails any whose marker != the constant. All seven sites currently read 2.30 (verified by grep -oE 'gov:kit memory-tree@[0-9.]+' over the three templates and the three live copies, plus check-memory-hygiene.sh:13). spec-5:97 states it correctly: 'the kit version at four gated sites with three dogfooded parity copies'.

**How the skeptic settled it.** Reproduced end to end, independently. `git grep -n 'gov:kit memory-tree@[0-9]'` excluding memory/builds/ returns SEVEN live carriers, all at 2.30: tools/memory-tree/check-memory-hygiene.sh:13 (inline marker on the constant line), the three tracked tools/memory-tree/*.template.md files (BUILD-METHOD, HYGIENE, SPEC-TEMPLATE), and the three live copies memory/HYGIENE.md:1, memory/TEMPLATE-SPEC.md:1, memory/guides/BUILD-METHOD.md:1. check-verdict-epoch.sh remedy() is at :160-166 and prints exactly three paths: $ENGINE, tools/memory-tree/HYGIENE.template.md, memory/HYGIENE.md. kit-dogfood-parity.test.sh:53 fixes PAIRS as the three live/template pairs, so 'the dogfooded parity copies' = the three memory/ renders; remedy ∪ those = 5 files, and BUILD-METHOD.template.md and SPEC-TEMPLATE.template.md fall outside it. tools/check-kit-versions.sh:94 derives its population as `git ls-files 'tools/memory-tree/*.template.md'` (3 files) and emits a fail per marker != KIT_MEMORY_TREE_VERSION at :106; its leg `kit version markers` in tools/gate-legs.json carries no `guard` and subject 'repo', so it runs on every bar. The --render half also holds: --render writes TEMPLATE -> LIVE, so bumping memory/TEMPLATE-SPEC.md and memory/guides/BUILD-METHOD.md by hand and then re-rendering reverts them to the un-bumped template value. The intra-spec-5 contradiction reproduces verbatim: spec-5:88-89 §4 Migration says 'the kit version at four gated sites with three dogfooded parity copies' (= 7, correct) while spec-5:52 S-EPOCH gives the 5-site formula. All six specs list check-kit-versions.sh in §7 (spec-1:186, spec-2:167, spec-3:144, spec-4 §7, spec-5:143, spec-6:144). memory/backlog/TOOL.md:21 records the same defect hit twice (2026-08-20 and 2026-08-21). Not parked: no §3 non-goal or §8 fork in any of the six touches the bump site set. This and R2-1 are ONE defect with one repair; R2-05 is the better-stated version and additionally catches the spec-5 self-contradiction.

**Fix.** Replace the remedy-pointer in all six S-EPOCH blocks with a DERIVED instruction: bump the constant and its inline marker in check-memory-hygiene.sh, then every carrier `git grep -l 'gov:kit memory-tree@'` returns, then `bash tools/memory-tree/kit-dogfood-parity.test.sh --render` for the three live copies. Cite TOOL-dSettledRoster-4 as the reason the gate's own remedy message is not the list. Spec-5 §4 Migration already states the correct arithmetic and can be the one spelling the other five point at.

## MEDIUM — 26

### R2-01 — 2026-08-24-spec-dFramedEntrypoint-1.md · §4 Files touched (lines 128-135), against §2 S6 · FOLDED IN THIS ROUND

**Status:** CORRECTED · raised HIGH, final MEDIUM · kind FOLD_BROKE

**Finding.** Spec-1 §4 Files touched retains a stale entry naming tools/gate-legs.json that the same paragraph's closing sentence and S6 both negate — a residual half of round-1 GATE-1's fix, graded MEDIUM, and a duplicate of R2-GATELEGS-1.

**Evidence.** spec-1:131-132 '… inverts that · `tools/gate-legs.json` for the blind-spot paragraph · `tools/memory-tree/BUILD-METHOD.template.md` edited FIRST …' and spec-1:135 'refresh rather than a new claim. NOT `tools/gate-legs.json` — see S6.' Both sentences are in the same paragraph. The consequence is source-verified: tools/run-gates/run-gates.test.sh pins the leg key set (keyset_probe at :123-129) and the live union of keys over all 85 legs is exactly ['argv','chunk','guard','impure','name','subject']; the 'run-gates canary' leg carries guard ['tools/','tools/gate-legs.json','tools/lib/'], so editing the manifest is what arms it.

**How the skeptic settled it.** The factual half reproduces exactly and is identical to R2-GATELEGS-1: spec-1:131 names tools/gate-legs.json for the blind-spot paragraph and spec-1:135, in the same paragraph, says 'NOT `tools/gate-legs.json` — see S6.' The key-set evidence also reproduces — the live union over tools/gate-legs.json is ['argv','chunk','guard','impure','name','subject'] and the run-gates canary is guarded on ['tools/','tools/gate-legs.json','tools/lib/']. What does not survive is the HIGH grade and the reasoning behind it. 'An implementer reading the Files-touched list in order edits the manifest and reds the bar' requires the reader to stop four lines before the sentence that negates it, inside the same paragraph, with S6 (:37-41) giving the reason in full. The canary also reds immediately and unambiguously if they do not. This is a stale list entry, not a live instruction, and it is the same defect this round reports twice.

**Fix.** Delete the clause '· `tools/gate-legs.json` for the blind-spot paragraph ·' from §4 Files touched. Fix once; do not count it twice.

### R2-4 — 2026-08-24-spec-dFramedEntrypoint-1.md · §4 Files touched (one sentence-list, two clauses) · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised HIGH, final MEDIUM · kind FOLD_BROKE

**Finding.** The fold answered GATE-1/GATELEG-12/LEG-1 by APPENDING a negation instead of deleting the entry, so §4 Files touched now both requires and forbids the same edit. An implementer reading the list left-to-right edits `tools/gate-legs.json`, whose key set is closed and canary-enforced, and reds the bar — the exact failure the fold was folding.

**Evidence.** memory/builds/dFramedEntrypoint/spec/2026-08-24-spec-dFramedEntrypoint-1.md:131-132 "· `tools/gate-legs.json` for the blind-spot paragraph ·" and :135 "NOT `tools/gate-legs.json` — see S6." — both inside the same §4 Files touched list. Round 1 GATE-1's fix reads "drop `tools/gate-legs.json` from spec-1 §4 Files touched". The refusal is real: tools/run-gates/run-gates.test.sh pins `KNOWN = {"name","argv","guard","impure","chunk","subject"}` and exits 1 on a stray key; `python -c` over tools/gate-legs.json prints exactly those six keys across 85 legs; the `run-gates canary` leg guards `tools/gate-legs.json`.

**How the skeptic settled it.** Reproduced exactly. `grep -n 'gate-legs.json' memory/builds/dFramedEntrypoint/spec/2026-08-24-spec-dFramedEntrypoint-1.md` returns :39 (S6's refusal), :131 ("· `tools/gate-legs.json` for the blind-spot paragraph ·") and :135 ("NOT `tools/gate-legs.json` — see S6.") — :131 and :135 are inside the same §4 Files touched sentence-list, so the list both requires and forbids the same edit. The refusal is real and enforced: tools/run-gates/run-gates.test.sh:96 and :125 both pin `KNOWN = {"name","argv","guard","impure","chunk","subject"}` and exit 1 on a stray key, and a json.load over tools/gate-legs.json returns 85 legs carrying exactly name/argv/chunk/subject (85 each), guard (49) and impure (1) — no seventh key exists, and JSON has no comment form for a prose paragraph. Re-graded HIGH→MEDIUM rather than confirmed at HIGH: the negation sits three lines later in the same list and S6 in the binding scope section states the refusal with its full reason, so the worst outcome is one red canary run, not a wrong verdict. It is still a spec that says X and not-X in one section, and the fold is what left it there.

**Fix.** Delete the clause "`tools/gate-legs.json` for the blind-spot paragraph" from spec 1 §4 Files touched. Keep the trailing "NOT `tools/gate-legs.json` — see S6." as the only mention in that section.

### R2-CANON-FLAGS-1 — 2026-08-24-spec-dFramedEntrypoint-1.md · §2 S2 (:19-21) and §4 Data model table (:72-81), against §2 S1 (:15-18), §2 S8 (:47-48) and §6 AC1-AC5

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind ROUND1_MISSED

**Finding.** The canon declares two per-slot properties — `empty_ok` and `bullets` — that no trigger, no acceptance criterion, no selftest arm and no sibling unit reads. They are dead data in the one artifact this build exists to make binding, and one of them guards the goal bound itself.

**Evidence.** S2: 'the canon is a module-level tuple beside GEN_REGIONS, declaring for each slot its exact heading text, whether its body may be empty, and whether its body must be a bullet list.' The §4 table sets 'Body may be empty: no' for the description, expected improvements and detriments, and 'bullet list' for two of them. But S1's third trigger grades only headings, order, and the pre-heading line: 'must consist of exactly the canonical slot headings, in canonical order, with no heading outside the canon and no non-blank line between the title line and the first canonical heading.' AC1 grades a heading outside the canon, AC2 order, AC3 absent markers, AC4 the stray pre-heading line, AC5 the empty population — none grades a body. S8's arms cover 'every trigger, every refusal and the survey verb'; the flags are neither. §3 Non-goals routes per-slot BUDGETS to unit 2, and unit 2's S1-S8 grade bytes only (`build-readme-slot-limits.txt` carries 'the slot's canonical heading key, the hard ceiling in bytes, the recorded high-water in bytes'). So no unit in the eight reads either flag. Consequence: S3 declares 'the description slot IS the goal bound that the build method's rescope rule reads' — under this canon an EMPTY description passes, and the rescope rule reads nothing. This is the class AGENTS.md §7 names directly ('An unarmed predicate REDS rather than passing green', 'A gate's OWN header states what it does NOT check').

**How the skeptic settled it.** Reproduced. `grep -n 'empty_ok\|bullets\|bullet' memory/builds/dFramedEntrypoint/spec/*.md` returns the flags ONLY at spec-1:20 (S2), spec-1:73 and the §4 table rows :77-81; spec-2:56 is the per-bullet-cap non-goal and spec-8:41 is unrelated. No trigger reads them: S1's third trigger (:15-18) grades 'exactly the canonical slot headings, in canonical order, with no heading outside the canon and no non-blank line between the title line and the first canonical heading'. No AC reads them: AC1 heading outside canon, AC2 order, AC3 absent markers, AC4 stray pre-heading line, AC5 empty population, AC6 arm count, AC7 leg plus docstring, AC8 survey, AC9 the method writes rendering under check-format exit 0. S8's arms cover 'every trigger, every refusal and the survey verb' — the flags are neither. Unit 2 grades bytes only (build-readme-slot-limits.txt rows carry the heading key, ceiling and high-water); units 3-7 never touch a slot body. So both flags are declared data with no reader anywhere in the eight. The goal-bound consequence is real: S3 (:21-23) declares 'the description slot is DECLARED to be the goal bound that the build method's rescope rule reads', and under this canon an empty description passes. Not a re-raise — round 1's CANON-1, METHOD-1 and UNB-9 all concern method-mandated CONTENT classes, not the canon's per-slot flags, and §3 and §8 park neither.

**Fix.** Either give each flag a trigger, an AC and a selftest arm in unit 1 (an empty required body is a refusal; a prose body under a `bullets` slot is a refusal), or delete `empty_ok` and `bullets` from S2 and the §4 table and state in S6's blind-spot paragraph that the canon closes the heading set and grades no body.

### R2-GATELEGS-1 — 2026-08-24-spec-dFramedEntrypoint-1.md · §4 Files touched, lines 131 and 135 (one paragraph)

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind FOLD_BROKE

**Finding.** The rev-2 fold added the negation round 1 asked for and never deleted the entry it negates, so §4 Files touched now names tools/gate-legs.json as the blind-spot paragraph's home AND refuses it, in the same list.

**Evidence.** spec-1:131 '· `tools/gate-legs.json` for the blind-spot paragraph ·' and spec-1:135 'NOT `tools/gate-legs.json` — see S6.' Both sit inside the single §4 Files touched paragraph (:127-135). S6 (:37-41) and AC7 (:174-176) place the paragraph in gen_build_index.py's module docstring and do_check_format's docstring. Round 1 GATE-1's Fix reads verbatim: 'Restate spec-1 S6 and AC7 against gen_build_index.py's own header ... and drop `tools/gate-legs.json` from spec-1 §4 Files touched.' The first half landed; the second did not. The consequence is not cosmetic: the retained entry is an instruction to add a key to a JSON array whose key set is pinned to {name, argv, guard, impure, chunk, subject} by tools/run-gates/run-gates.test.sh:96, whose 'run-gates canary' leg is guarded on ['tools/','tools/gate-legs.json','tools/lib/'] and therefore arms on any tools/ change these units make.

**How the skeptic settled it.** Verified at HEAD, both sentences in the same §4 paragraph: spec-1:131 '· `tools/gate-legs.json` for the blind-spot paragraph ·' and spec-1:135 'NOT `tools/gate-legs.json` — see S6.' Round-1 GATE-1's Fix reads verbatim (record line 51) 'Restate spec-1 S6 and AC7 against gen_build_index.py's own header … and drop `tools/gate-legs.json` from spec-1 §4 Files touched'; the first half landed in S6 (:37-41) and AC7 (:174-176), the second did not. The key-set consequence reproduces: the live union of keys over tools/gate-legs.json is ['argv','chunk','guard','impure','name','subject'], and the `run-gates canary` leg is guarded on ['tools/','tools/gate-legs.json','tools/lib/']. Downgraded from the HIGH that R2-01 claims for the same defect: the negation sits four lines later in the SAME paragraph and S6 explains it, so an implementer who reads the paragraph through is corrected before acting, and the canary reds immediately if they do not. Note this finding and R2-01 are the same defect reported twice.

**Fix.** Delete the clause '· `tools/gate-legs.json` for the blind-spot paragraph ·' from spec-1 §4 Files touched, leaving the S6-consistent trailing sentence.

### R2-02 — 2026-08-24-spec-dFramedEntrypoint-2.md · §4 Data model (lines 71-72), against §2 S1 and §2 S6

**Status:** CONFIRMED · raised HIGH, final MEDIUM · kind FOLD_BROKE

**Finding.** rev-2 split the limits file in two (S1: build-readme-slot-limits.txt for the ceiling, build-readme-slot-highwater.txt for the high-water) but §4 Data model still declares the single-file schema the split exists to abolish: one row carrying the slot key, the ceiling AND the high-water. The data model is the section an implementer builds the file format from, so the fold left the authoritative schema statement describing the defect. It also contradicts the seam §10 claims to copy, which uses two files with a `<key>\t<bytes>` row each.

**Evidence.** spec-2:71-72 'One row per slot: the slot's canonical heading key, the hard ceiling in bytes, the recorded high-water in bytes.' against spec-2:18-22 S1 'TWO declared files, not one'. Source: tools/check-template-size.sh:54 resolves LIMITS to tools/template-size-limits.txt and :96 resolves HIGHWATER to tools/template-size-highwater.txt; the limits file's own header states 'This is NOT the high-water record — that one is `tools/template-size-highwater.txt`, which `--bump` WRITES as a side effect of measuring.'

**How the skeptic settled it.** Verified at HEAD. Spec-2 §4 Data model (:71-72) reads 'One row per slot: the slot's canonical heading key, the hard ceiling in bytes, the recorded high-water in bytes' against S1 (:15-19) 'TWO declared files, not one'. The seam §10 claims to copy does use two files: tools/check-template-size.sh:54 resolves LIMITS to tools/template-size-limits.txt and :96 resolves HIGHWATER to tools/template-size-highwater.txt, and tools/template-size-limits.txt's own header declares the grammar '`<repo-relative-path>\t<bytes>` per row'. So the data model describes the exact shape the rev-2 split exists to abolish. Downgraded from HIGH: two other places in the same spec pin the two-file design — S1 states it in capitals and AC10 (:157-158) asserts `git diff --stat` shows the limits file unchanged and only the high-water file modified — so an implementer building the one-file schema is contradicted by an acceptance criterion before landing.

**Fix.** Rewrite §4 Data model as two schemas: the limits file is one `<slot-key>\t<ceiling-bytes>` row per canonical slot; the high-water file is one `<slot-key>\t<measured-bytes>` row, written only by --bump. Keep the unknown-slot refusal on both.

### R2-03 — 2026-08-24-spec-dFramedEntrypoint-2.md · §4 Migration (lines 88-90), against §2 S7 and §6 AC8

**Status:** CONFIRMED · raised HIGH, final MEDIUM · kind FOLD_BROKE

**Finding.** The fold gave the seeding event a single owner in S7 ('It does NOT perform the seeding: unit 3 lands with zero bound rows by its own design') and declared this unit's green vacuous in AC8 — but left §4 Migration asserting the opposite verbatim. Round 1's SEQ-3 fix was 'Delete the seeding claim from spec 2 §4 Migration' and it was not done, so the one-fact-one-owner repair the rev-2 log claims is only two thirds landed and the third answer is still in the spec.

**Evidence.** spec-2:88-89 'The ceilings are seeded from the unit-3 population so the leg lands green over the tree it was written against, which is how every existing cap in this repo was seeded.' against spec-2:56-60 S7 'It does NOT perform the seeding … Unit 7 owns the one real seeding event, and this unit's green is VACUOUS until it lands' and spec-2 AC8 'the leg states that the declared population is empty, so the green is recorded as VACUOUS'. Round 1 record, SEQ-3 Fix: 'Delete the seeding claim from spec 2 §4 Migration.'

**How the skeptic settled it.** Verified at HEAD. Spec-2 §4 Migration (:90-92) opens 'The ceilings are seeded from the unit-3 population so the leg lands green over the tree it was written against', against S7 (:37-42) 'It does NOT perform the seeding: unit 3 lands with zero bound rows by its own design … Unit 7 owns the one real seeding event, and this unit's green is VACUOUS until it lands' and AC8 (:152-154), which records the green as vacuous. Round-1 SEQ-3's Fix reads verbatim (record line 461) 'Delete the seeding claim from spec 2 §4 Migration' and it was not done, so the one-fact-one-owner repair landed in S7 and AC8 and left the third answer standing. Downgraded from HIGH for the same reason as R2-02: S7 and AC8 are explicit and adjacent, and the residue is a stale sentence rather than a live instruction that survives a full read of the spec.

**Fix.** Replace §4 Migration's first sentence with the S7 statement: this unit writes provisional declared ceilings with their provenance, seeds nothing, and unit 7 performs the one seeding event against the then-bound population.

### R2-12 — 2026-08-24-spec-dFramedEntrypoint-2.md · §2 S8 arm list, §5 error/empty states and §6 (no AC), for the new high-water file · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind ROUND1_MISSED

**Finding.** The rev-2 split created a second declared data file and gave it no absent-state behaviour. §5 error states name only 'a missing limits file'; S8's six arms cover the limits file's three data-shape refusals and nothing about the high-water file; no AC mentions it. So the fold reproduced round 1's GATE-6 defect (a declared refusal with no arm) one file over — except here the branch is not even declared. The seam §10 claims to copy 'in both halves' has an explicit answer for this branch that the spec does not carry.

**Evidence.** spec-2 S8 lists 'over-ceiling, over-high-water-under-ceiling, under both, a missing limits file, a limits row naming a slot the canon does not declare, and a limits file MISSING a row for a canonical slot'. spec-2 §5 error/empty: 'a missing limits file is a refusal naming the expected path, never a skip. A slot with no row is a refusal for the same reason.' Neither names build-readme-slot-highwater.txt. Source: tools/check-template-size.sh:175-178 handles it explicitly — 'TEMPLATE-SIZE no-ratchet — no high-water record at $HIGHWATER; growth is unpriced.' and, per-subject, '$key has no row in $HIGHWATER; growth is unpriced.'

**How the skeptic settled it.** Reproduced against the spec at HEAD and against the seam it declares it copies. spec-2:15-19 S1 creates a SECOND declared file, tools/memory-tree/build-readme-slot-highwater.txt. spec-2:43-46 S8 enumerates six arms — 'over-ceiling, over-high-water-under-ceiling, under both, a missing limits file, a limits row naming a slot the canon does not declare, and a limits file MISSING a row for a canonical slot' — every one of them about the LIMITS file. spec-2:121-122 §5 error/empty states names only 'a missing limits file' and 'a slot with no row'. No AC (AC1-AC11) mentions the high-water file's absence; AC6 covers the limits file only. So the high-water file's absent state has no declared verdict, no arm and no criterion, while the file's presence is what the entire advisory tier reads. The seam spec-2 §10 says it copies 'in both halves' has an explicit answer for exactly this branch: tools/check-template-size.sh:175-178 prints 'TEMPLATE-SIZE no-ratchet — no high-water record at $HIGHWATER; growth is unpriced.' and, per subject, '$key has no row in $HIGHWATER; growth is unpriced.' Not parked: §3 non-goals and §8 (F1, F2) say nothing about it. Held at MEDIUM because it leaves a branch of the shipped mechanism undefined — the implementer must invent a verdict, and a silent skip there makes the whole advisory tier dark without announcing itself, which is the class AGENTS.md §7 names ('A skip must announce itself').

**Fix.** Declare the absent-high-water verdict in §5 (advisory 'growth is unpriced', exit code untouched, matching check-template-size.sh:175-178), add the arm to S8, and add an AC observing it. Do the same for a high-water file present but missing a row for a graded slot.

### R2-13 — 2026-08-24-spec-dFramedEntrypoint-2.md · §6 AC3 (and AC4, AC5, AC10), against §6 AC8

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind ROUND1_MISSED

**Finding.** AC3 is the hard case — impossible to observe at this unit's landing. AC4, AC5 and AC10 are not impossible but are satisfied vacuously over the empty population, which proves nothing.

**Evidence.** spec-2 AC8 'the leg states that the declared population is empty, so the green is recorded as VACUOUS. The real seeding observation belongs to unit 7 AC5, not here.' against spec-2 AC3 'the text is present in that leg's persisted log under the gate-log directory, demonstrated by grepping that file after a `bash tools/run-gates/run-gates.sh` run', AC4, AC5 and AC10. The population is empty at that point by spec-3 §4 Inventory ('Before that, the bound set is empty') and spec-1 S5. The persisted-log mechanism itself is real (run-gates.sh:96-106 builds `<git-dir>/gate-logs/<leg>.log`), so the defect is the population, not the channel.

**How the skeptic settled it.** The population claim reproduces: spec-2:152-154 AC8 states that at this unit's landing 'the declared population is empty, so the green is recorded as VACUOUS', spec-3:71-73 §4 Inventory says 'Before that, the bound set is empty', spec-3:28-29 S5 says the two verdict tiers 'grade only rows in the bound set', and spec-1:34-36 S5 wires the predicate to an empty declared population. AC3 (spec-2:142-143) then requires the advisory text to be present in the leg's persisted gate-log 'demonstrated by grepping that file after a `bash tools/run-gates/run-gates.sh` run' — with zero graded files no slot is measured, so no advisory can fire, and that observation cannot be made at this unit's landing. Under TEMPLATE-SPEC §6 each criterion is answered by a line in that unit's acceptance ledger, so AC3 is unanswerable. The channel itself is real and not the defect — run-gates.sh:95-106 resolves `<git-dir>/gate-logs` and writes one redacted log per leg. AC4, AC5 and AC10 are weaker than claimed: they are satisfiable, but only VACUOUSLY (printing nothing, changing nothing), which is the green-over-an-empty-population class this repo already carries a gotcha for. Held at MEDIUM on AC3 alone.

**Fix.** Re-point AC3 at a scratch fixture whose registry declares one file (the shape spec-2 AC9 and spec-3 AC7 already use, 'observed RED against a staged break first'), or move it beside unit 7 AC5's seeding observation. Give AC4, AC5 and AC10 a non-empty fixture population so their observation is not vacuous.

### R2-3 — 2026-08-24-spec-dFramedEntrypoint-2.md · §2 S6b, against S1/S7 and §4 Files touched · FOLDED IN THIS ROUND

**Status:** CORRECTED · raised HIGH, final MEDIUM · kind FOLD_BROKE

**Finding.** Spec 2 S6b names an adopter state with neither a mechanism nor a legal semantics. It has no mechanism, because tools/memory-tree/kit.toml:11-13 ships `include = "**"` verbatim and no [[files]] exclusion or rendered-variant rule is in scope or in §4 Files touched — so the adopter inherits gov's measured ceilings. And it has no semantics, because if the rows genuinely are absent, §5 and AC9 make that a refusal, while nothing in S1-S8 defines the exit-0 announced-unarmed branch S6b asserts; the kit.toml [[hole]] it cites as precedent is a declaration linted by govkit, not a runtime switch.

**Evidence.** tools/memory-tree/kit.toml: `home = "tools/memory-tree"` with `[[files]] include = "**"` `role = "engine"` — every tracked file in that directory is copy-installed. Round 1's ADOPT-1 skeptic established exactly this: "every tracked file in that directory — gen_build_index.py AND any new build-readme-slot-limits.txt — is copy-installed together". spec-2 S1 pins both paths under `tools/memory-tree/`; S7: "this unit ships DECLARED PROVISIONAL ceilings, written into the limits file at this unit's landing". spec-2 §4 Files touched names kit.toml, gen_build_index.py, gate-legs.json, .gitattributes and the dossier — not adopt-memory-tree.sh. Contrast the kit's existing precedent for a measured pin: `[[hole]] measured-pins` whose values live in `.memory-tree.conf` (blank by default), and the taxonomy hole whose discharge diffs against `{kit}/.memory-tree.conf.example`.

**How the skeptic settled it.** The core defect reproduces. Spec 2 S6b ("the ADOPTER copy ships with no ceiling rows and an announced unarmed state") has no legal semantics anywhere else in the spec: §5 error/empty states says "a missing limits file is a refusal naming the expected path, never a skip. A slot with no row is a refusal for the same reason", AC6 makes an absent file exit 1 and AC9 makes an omitted canonical-slot row exit 1 — a file with zero rows omits a row for every canonical slot, so AC9 fires. No S-item, AC or branch defines an exit-0 announced-unarmed state. The kit.toml precedent S6b invokes runs the other way and I reproduced it: tools/memory-tree/kit.toml [[hole]] id="measured-pins" states "Blank means checks 13-16 are simply OFF, so the hygiene leg exits 0 with the hole wide open", i.e. blank is a PASS there. And a [[hole]] is inert at runtime — the only reader is tools/govkit/govkit.py:697-707, which merely lints that each declared hole carries a discharge probe and a reason (plus blocks_adopt at :1272-1278); nothing in tools/memory-tree/ reads the word. But the finding's stated consequence does not follow as written. tools/memory-tree/kit.toml:11-13 is `[[files]] include = "**" role = "engine"`, so every tracked file in the kit home ships verbatim: absent an exclusion or a rendered variant that S6b does not name and §4 Files touched does not list, the adopter receives gov's limits file WITH gov's provisional rows and does not red on missing rows at all — it inherits ceilings measured against a corpus it never had, which is the exact 'vacuous or permanently red' class S6b was written to prevent. So S6b is broken on both horns rather than on the one the finding names. Re-graded HIGH→MEDIUM: gov's own bar is unaffected (S7 writes provisional rows at this unit's landing), the damage is adopter-facing, and round 1 settled the structurally identical ADOPT-1 at MEDIUM.

**Fix.** Give the unarmed state a code path and a shape: a ceiling row present with a blank VALUE means the slot is UNMEASURED — the leg prints an announced-unarmed line and exits 0 — while an absent row and an absent file stay the AC6/AC9 refusals. Add the selftest arm and an acceptance criterion for the blank-value branch, say in S6b which of the two shapes the adopter copy carries, and name the kit.toml [[files]] rule (exclusion or rendered) that actually produces an adopter copy differing from gov's in §4 Files touched.

### R2-3 — 2026-08-24-spec-dFramedEntrypoint-2.md · §2 S6b against §5 error/empty states and §6 AC9 (and AC6) · FOLDED IN THIS ROUND

**Status:** CORRECTED · raised HIGH, final MEDIUM · kind FOLD_BROKE

**Finding.** Spec 2 S6b names an adopter state with neither a mechanism nor a legal semantics. It has no mechanism, because tools/memory-tree/kit.toml:11-13 ships `include = "**"` verbatim and no [[files]] exclusion or rendered-variant rule is in scope or in §4 Files touched — so the adopter inherits gov's measured ceilings. And it has no semantics, because if the rows genuinely are absent, §5 and AC9 make that a refusal, while nothing in S1-S8 defines the exit-0 announced-unarmed branch S6b asserts; the kit.toml [[hole]] it cites as precedent is a declaration linted by govkit, not a runtime switch.

**Evidence.** Spec 2 §2 S6b: "the ADOPTER copy ships with no ceiling rows and an announced unarmed state". Spec 2 §5 error/empty: "a missing limits file is a refusal naming the expected path, never a skip. A slot with no row is a refusal for the same reason." AC9: "When the limits file omits a row for a canonical slot, `--check-format` exits 1 naming the slot, observed RED against a staged deletion". The leg ships to adopters: tools/memory-tree/kit.toml:142 `name = "build README slot contract"`. The precedent S6b invokes runs the other way — tools/memory-tree/kit.toml:75-81 `[[hole]] id = "measured-pins"` states "Blank means checks 13-16 are simply OFF, so the hygiene leg exits 0 with the hole wide open", i.e. the unarmed state is a PASS there, the opposite of AC9.

**How the skeptic settled it.** The core defect reproduces. Spec 2 S6b ("the ADOPTER copy ships with no ceiling rows and an announced unarmed state") has no legal semantics anywhere else in the spec: §5 error/empty states says "a missing limits file is a refusal naming the expected path, never a skip. A slot with no row is a refusal for the same reason", AC6 makes an absent file exit 1 and AC9 makes an omitted canonical-slot row exit 1 — a file with zero rows omits a row for every canonical slot, so AC9 fires. No S-item, AC or branch defines an exit-0 announced-unarmed state. The kit.toml precedent S6b invokes runs the other way and I reproduced it: tools/memory-tree/kit.toml [[hole]] id="measured-pins" states "Blank means checks 13-16 are simply OFF, so the hygiene leg exits 0 with the hole wide open", i.e. blank is a PASS there. And a [[hole]] is inert at runtime — the only reader is tools/govkit/govkit.py:697-707, which merely lints that each declared hole carries a discharge probe and a reason (plus blocks_adopt at :1272-1278); nothing in tools/memory-tree/ reads the word. But the finding's stated consequence does not follow as written. tools/memory-tree/kit.toml:11-13 is `[[files]] include = "**" role = "engine"`, so every tracked file in the kit home ships verbatim: absent an exclusion or a rendered variant that S6b does not name and §4 Files touched does not list, the adopter receives gov's limits file WITH gov's provisional rows and does not red on missing rows at all — it inherits ceilings measured against a corpus it never had, which is the exact 'vacuous or permanently red' class S6b was written to prevent. So S6b is broken on both horns rather than on the one the finding names. Re-graded HIGH→MEDIUM: gov's own bar is unaffected (S7 writes provisional rows at this unit's landing), the damage is adopter-facing, and round 1 settled the structurally identical ADOPT-1 at MEDIUM.

**Fix.** Give the unarmed state a code path and a shape: a ceiling row present with a blank VALUE means the slot is UNMEASURED — the leg prints an announced-unarmed line and exits 0 — while an absent row and an absent file stay the AC6/AC9 refusals. Add the selftest arm and an acceptance criterion for the blank-value branch, say in S6b which of the two shapes the adopter copy carries, and name the kit.toml [[files]] rule (exclusion or rendered) that actually produces an adopter copy differing from gov's in §4 Files touched.

### R2-ADOPT-2 — 2026-08-24-spec-dFramedEntrypoint-2.md · §2 S6b against §2 S7, §5 error/empty states, AC6 and AC9 · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised HIGH, final MEDIUM · kind FOLD_WRONG

**Finding.** S6b names an adopter outcome with no mechanism and contradicts S7 plus §5/AC9 over the single tracked limits path. The S7-versus-unit-7 pairing is not itself a contradiction.

**Evidence.** tools/memory-tree/kit.toml:10-12 declares `[[files]] include = "**"` with `role = "engine"` — every file under the kit home ships to the adopter; the only escape the kit uses for a data file is a separate `.example` sibling (tools/memory-tree/.memory-tree.conf.example, rendered to a path outside the kit home), which spec 2 does not spec. tools/memory-tree/kit.toml:141-145 declares `[[gate_leg]] name = "build README slot contract"` with `guard = []`, so the leg runs on every bar in every adopting tree. Spec 2 S7: "this unit ships DECLARED PROVISIONAL ceilings, written into the limits file at this unit's landing". Spec 2 §5 error/empty states: "A slot with no row is a refusal for the same reason." Spec 2 AC9: "When the limits file omits a row for a canonical slot, `--check-format` exits 1 naming the slot." A file with NO ceiling rows omits a row for every canonical slot, so S6b's "announced unarmed state" and AC9's exit 1 are opposite verdicts over the same bytes.

**How the skeptic settled it.** Reproduced. tools/memory-tree/kit.toml:10-12 is `[[files]] include = "**"  role = "engine"`, so every tracked file under the kit home copy-installs verbatim; kit.toml:141-145 declares `[[gate_leg]] name = "build README slot contract"` with `guard = []`, so every adopter RUNS it. There is exactly one tracked path for the ceilings and it sits inside the kit home. Spec 2 S7 writes provisional ceiling rows into it at landing; S6b says the adopter copy ships with no ceiling rows; §5 error/empty states says 'A slot with no row is a refusal for the same reason'; AC9 says a limits file omitting a row for a canonical slot exits 1. A row-less file omits a row for every canonical slot, so S6b's 'announced unarmed state' and AC9's exit 1 are opposite verdicts over the same bytes, and no S-item ships a mechanism that could produce the row-less adopter copy. The escape the kit already uses for a data file — a `.example` sibling rendered to a path outside the kit home, as with .memory-tree.conf.example — is not specced here, and spec 3 S6c specs exactly such an adopter scaffold for its own registry while spec 2 has no analogue. One sub-clause of the claim does not hold: S7's provisional ceilings and unit 7 S5's seeded ceilings are sequential states of one file, not a contradiction. Severity MEDIUM rather than HIGH, matching round 1's grading of the adopter-surface pair (SEED-1 MEDIUM, ADOPT-1 corrected from BLOCKER to MEDIUM) — the damage lands on the next adopter of this kit version, not on gov's bar. Note the fold implemented round 1's SEED-1 fix text literally ('ship the adopter copy with no ceiling rows plus an announced unarmed state'); the fix as written had no mechanism either.

**Fix.** Pick one and say it: (a) site the DECLARED ceilings outside the kit home on the .memory-tree.conf precedent and ship only an `.example` inside the kit, or (b) keep the file in the kit home and make the row-less state a DECLARED unarmed mode with its own announced line, with AC9's refusal firing only once at least one ceiling row exists. Either way name the discharge probe against the state that actually ships, and add tools/memory-tree/adopt-memory-tree.sh to §4 if the adopter copy is to be scaffolded rather than copied.

### R2-SPLIT-6 — 2026-08-24-spec-dFramedEntrypoint-2.md · §2 S1 (the fold's file split) against §2 S8, §5 error/empty states and AC6/AC9

**Status:** CORRECTED · raised MEDIUM, final MEDIUM · kind FOLD_BROKE

**Finding.** Spec 2 adds no arm, criterion or S-item for the high-water file's absent/missing-row/extra-row shapes, and omits the precedent's announce-the-skip line for an absent record — so the advisory tier can be silently off. The semantics themselves are not undefined: §10's named precedent, tools/check-template-size.sh, specifies absent record = no ratchet plus an explicit line, and a non-numeric row = a named failure.

**Evidence.** Spec 2 S1: "TWO declared files, not one: `tools/memory-tree/build-readme-slot-limits.txt` ... and `tools/memory-tree/build-readme-slot-highwater.txt`". S8's arm list, quoted in full: "over-ceiling, over-high-water-under-ceiling, under both, a missing limits file, a limits row naming a slot the canon does not declare, and a limits file MISSING a row for a canonical slot" — six arms, every one about the limits file. §5 error/empty states: "a missing limits file is a refusal naming the expected path, never a skip. A slot with no row is a refusal for the same reason" — limits file only. AC6 and AC9 name `build-readme-slot-limits.txt` explicitly; AC10 only asserts the high-water file is the one `--bump` writes. This is the same unarmed-declared-refusal class the fold itself corrected for the limits file at AC9 ("observed RED against a staged deletion before the arm is written"), one file over.

**How the skeptic settled it.** The asymmetry reproduces exactly: S1 declares two files, and every refusal, arm and criterion the fold added is about the limits file — S8's six arms name it, §5 error/empty states names it, AC6, AC7 and AC9 name it, and AC10 says only that `--bump` writes the high-water file. But 'silently unarmed' overstates the gap, because spec 2 §10 names tools/check-template-size.sh as the shape copied 'in both halves' and that source DOES define the semantics: its header at the ratchet block reads 'Absent record = no ratchet plus an explicit line; a non-numeric row is a NAMED failure, never a `set -u` explosion', and `recorded=""` for a subject with no row simply skips the ratchet. So the behaviour is inherited rather than undefined. What is genuinely missing is any arm, criterion or S-item for the high-water file's shapes, and — the load-bearing half — the 'explicit line' that makes an absent record announce itself, which is exactly what S5 exists to guarantee ('the advisory must REACH a human'). Without it the advisory tier can be entirely off and look identical to a clean run. MEDIUM, matching round 1's grade for the identical class one file over (GATE-6).

**Fix.** Extend §5, S8 and the AC set to the high-water file symmetrically, inheriting the precedent explicitly: an absent high-water file is a declared 'no high-water recorded' state that PRINTS that line rather than passing silently, a non-numeric row is a named failure, and a row naming a slot the canon does not declare is a refusal. Add the matching arms, each observed RED first.

### R2-13 — 2026-08-24-spec-dFramedEntrypoint-3.md · §2 S6b and §4 Files touched

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind ROUND1_MISSED

**Finding.** AC3 is the hard case — impossible to observe at this unit's landing. AC4, AC5 and AC10 are not impossible but are satisfied vacuously over the empty population, which proves nothing.

**Evidence.** AGENTS.md:52 — "`project/` (the gate's `*.txt` waiver registries and nothing else)". tools/memory-tree/check-memory-hygiene.sh:287 — "# project/ holds the gate's OWN waiver registries and nothing else (aMendedLedger U3)" directly above the closed `case` allowlist S6b edits. spec-3 §3: "It is deliberately not the shape of `memory/project/curation-debt.txt`, whose rows silence three checks on a whole file to buy one row." spec-3 §4 Files touched names readme-contract.txt, gen_build_index.py, check-memory-hygiene.sh (for the case arm), check-memory-hygiene.test.sh, adopt-memory-tree.sh, .gitattributes, HYGIENE.template.md and the dossier — AGENTS.md is absent from every spec in the set. Separately, memory/HYGIENE.md:93 already reads "Six plain lists in `memory/project/` — the whole of what that directory holds" while `git ls-files memory/project/` returns 8 names, so the prose S6b amends is already a stale count of a derived population (AGENTS.md §7: "NO count of a derived population is written in prose").

**How the skeptic settled it.** The population claim reproduces: spec-2:152-154 AC8 states that at this unit's landing 'the declared population is empty, so the green is recorded as VACUOUS', spec-3:71-73 §4 Inventory says 'Before that, the bound set is empty', spec-3:28-29 S5 says the two verdict tiers 'grade only rows in the bound set', and spec-1:34-36 S5 wires the predicate to an empty declared population. AC3 (spec-2:142-143) then requires the advisory text to be present in the leg's persisted gate-log 'demonstrated by grepping that file after a `bash tools/run-gates/run-gates.sh` run' — with zero graded files no slot is measured, so no advisory can fire, and that observation cannot be made at this unit's landing. Under TEMPLATE-SPEC §6 each criterion is answered by a line in that unit's acceptance ledger, so AC3 is unanswerable. The channel itself is real and not the defect — run-gates.sh:95-106 resolves `<git-dir>/gate-logs` and writes one redacted log per leg. AC4, AC5 and AC10 are weaker than claimed: they are satisfiable, but only VACUOUSLY (printing nothing, changing nothing), which is the green-over-an-empty-population class this repo already carries a gotcha for. Held at MEDIUM on AC3 alone.

**Fix.** Re-point AC3 at a scratch fixture whose registry declares one file (the shape spec-2 AC9 and spec-3 AC7 already use, 'observed RED against a staged break first'), or move it beside unit 7 AC5's seeding observation. Give AC4, AC5 and AC10 a non-empty fixture population so their observation is not vacuous.

### R2-9 — 2026-08-24-spec-dFramedEntrypoint-3.md · §2 S2, §2 S4 and §4 Data model, against spec-1 §8 F2

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind ROUND1_MISSED

**Finding.** No unit owns the path a NEW build takes after the contract lands, and the fold's equality pin makes the gap bite. S2 reds any tracked build README named by no row, so a new build's first commit reds the bar. The two legal responses are a bound row (conform) or an exempt row — and an exempt row raises the measured exempt count, which under the equality pin forces a pin RAISE in the same commit, which §4 Data model's "shrink-only" forbids. Unit 1 F2 declines the `--write` scaffold that would have made conforming the cheap path, so the cheap path is the one the registry's own rule forbids.

**Evidence.** spec-3 S2: "every tracked `memory/builds/*/README.md` that is not named by a row and not named by an explicit exemption row reds". spec-3 S4 (rev-2 fold): "The pin is an EQUALITY, not a ceiling: a pin above the measured exempt count reds and names both numbers." spec-3 AC4: "When the exempt count DIFFERS from the declared pin in either direction, `--check-format` exits 1". spec-3 §4 Data model: "A pin line declares the exempt count, and the count is shrink-only." spec-3 §3 Non-goals covers only EXISTING READMEs ("No AUTOMATIC ENROLMENT of existing READMEs"). spec-1 F2 recommendation: "no scaffold in this unit". `grep -n -i 'new build|future build|born'` over all eight specs returns only spec-1 F2's two prose mentions and spec-3 S2's clause — no S-item, no AC, no non-goal states the rule.

**How the skeptic settled it.** Reproduced end to end. tools/memory-tree/gen_build_index.py:668-671 computes `own = [u["id"] for u in build["units"]]` then `gap = [i for i in own if i not in named]` and `agap` the same way, so both gaps are [] whenever the build declares no unit. Seven tracked builds are in that state — aDeployScout, aFerriedDossier, aKitHardener, aLeanRework, aPortableWarden, aRatchetForge, bThriftyBellows each carry exactly one "No spec under this build carries a status header" line — and four of them (aFerriedDossier, aKitHardener, aRatchetForge, bThriftyBellows) already render a `| Record | Kind | Serves |` table, so they hit the fold's new unconditional branch with a populated `recs` and an empty `own`. Spec 5 S3 makes the joins render "UNCONDITIONALLY wherever the region renders" and mandates an explicit zero-gap phrasing ("every unit id is named by a record"), which turns a vacuously-true statement over an empty population into an affirmative coverage claim in four to seven generated files. AC5 enumerates the full-coverage and no-records states and not the no-units state, so nothing grades it. This is fresh surface the fold created while closing round-1 JOINS-22 (also MEDIUM), and it is the class AGENTS.md §7 names outright: "A probe that cannot move says so. A signal with no liveness assertion reports a reassuring zero when it is broken."

**Fix.** Add a third phrasing for the empty-unit-population case — "this build declares no unit, so there is nothing to cover" — distinct from the zero-gap phrasing, and extend AC5 to require it, demonstrated against one of the four unit-less builds that already carry a records table (aKitHardener is the smallest).

### R2-PIN-7 — 2026-08-24-spec-dFramedEntrypoint-3.md · §2 S4 (the fold's equality pin) against §4 Data model, §5 risks and §8 F1

**Status:** CORRECTED · raised MEDIUM, final MEDIUM · kind FOLD_BROKE

**Finding.** The equality pin and the stated shrink-only property are not jointly unsatisfiable — nothing gates shrink-only, so a new exempt row simply raises both numbers at exit 0. The defects are that §4 and §5 state a property no mechanism enforces, and that the spec never says whether a build README created after unit 3 joins bound or exempt, while S2 reds the unguarded leg for any README named by no row.

**Evidence.** Spec 3 S4: "The pin is an EQUALITY, not a ceiling: a pin above the measured exempt count reds and names both numbers." Spec 3 AC4: "When the exempt count DIFFERS from the declared pin in either direction, `--check-format` exits 1 naming the pin and the measured count." Spec 3 §4 Data model: "A pin line declares the exempt count, and the count is shrink-only." Spec 3 §5 risks: "Mitigated by the shrink-only pin, which turns a stalled drain into a visible number". Spec 3 S2: "every tracked `memory/builds/*/README.md` that is not named by a row and not named by an explicit exemption row reds". The three statements are jointly unsatisfiable for any build README created after unit 3 lands and before it conforms.

**How the skeptic settled it.** Every quotation verifies — S4 'The pin is an EQUALITY, not a ceiling', AC4 'DIFFERS from the declared pin in either direction', §4 Data model 'the count is shrink-only', §5 risks 'Mitigated by the shrink-only pin', S2's forward assertion — but 'jointly unsatisfiable' does not hold. Nothing in the spec gates shrink-only: AC4 gates equality alone, and no criterion refuses a pin that rises. So a new build README that cannot conform simply takes an exempt row and both numbers rise; the machine says exit 0 and only the prose property is violated. The real residue is smaller and different: the spec asserts a shrink-only property its own mechanism does not enforce and that the first new exempt row breaks, and it never states whether a build README created after unit 3 joins as bound or exempt — while S2 reds the unguarded `build README slot contract` leg for any tracked build README named by no row at all, which makes registry-plus-pin editing a mandatory step of every future build in this repo that no spec mentions. Kept at MEDIUM for that operational consequence; round 1's GATE-8 (whose fix produced the equality) parked only the pin's starting value at F1, not this.

**Fix.** State the new-build rule in S4: either a build README created after unit 3 must join as a BOUND row (conformance mandatory from here, so shrink-only holds), or a pin raise is legal with a recorded reason and §4/§5 become 'shrink-only absent a declared new-build exemption'. Say it once and make §4 Data model and §5 risks point at that sentence rather than restating it.

### R2-CHARTER-9 — 2026-08-24-spec-dFramedEntrypoint-3.md (with spec 8 §2) · spec-3 §2 S6b and §4 Files touched; spec-8 S3/S4 (the record-repair unit)

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind ROUND1_MISSED

**Finding.** Round 1 did surface both items inside REG-1's evidence; its fix text omitted them, so this is unrepaired residue of a HIGH rather than something round 1 missed. The HYGIENE 'six' is pre-existing debt (wrong at eight today), not created by this build.

**Evidence.** AGENTS.md:52 — "`project/` (the gate's `*.txt` waiver registries and nothing else)". Spec 3 §3 Non-goals — "It is deliberately not the shape of `memory/project/curation-debt.txt`, whose rows silence three checks on a whole file to buy one row" — and §1 calls it "a tracked registry asserted in BOTH directions", not a waiver. `grep -n "AGENTS.md" memory/builds/dFramedEntrypoint/spec/*.md` returns nothing: no spec touches it. Separately, the count claim is ALREADY stale before this build adds to it: memory/HYGIENE.md:29 "the gate's own waiver registries (`*.txt`, six of them) and nothing else" and :94 "Six plain lists in `memory/project/` — the whole of what that directory holds", while `git ls-files memory/project/` returns EIGHT files. Spec 3 S6b commits to "the matching `HYGIENE.template.md` prose" but names no count correction, and tools/memory-tree/HYGIENE.template.md:94 carries the same "Six".

**How the skeptic settled it.** Both halves reproduce. AGENTS.md:52 reads '`project/` (the gate's `*.txt` waiver registries and nothing else)'; that sentence exists only in AGENTS.md — `git grep 'waiver registries'` finds no counterpart in coding-governance-agents.template.md — so the repair is a single-file edit with no render step. Spec 3 §3 insists the registry is deliberately not a waiver ('deliberately not the shape of `memory/project/curation-debt.txt`') and §1 calls it 'a tracked registry asserted in BOTH directions', so the charter sentence is false the moment unit 3 lands. `grep -n 'AGENTS.md' memory/builds/dFramedEntrypoint/spec/*.md` returns nothing — no spec touches it. The count claim is already stale: `git ls-files memory/project/` returns EIGHT files while memory/HYGIENE.md says 'six of them' at :29, 'Six plain lists ... the whole of what that directory holds' at :94 and 'All six are scaffolded' at :112, with the same three lines in tools/memory-tree/HYGIENE.template.md and the same error in check-memory-hygiene.sh:289's own comment ('a directory defined as six named files'), while check 3's allowlist at :300-305 already carries eight names. Spec 3 S6b commits to editing that HYGIENE prose and names no count correction, so the file would land at nine with the prose still saying six. Round 1's REG-1 evidence did surface both, but its FIX prescribed only the allowlist, the arm and the HYGIENE prose — so this is a residue of a HIGH's incomplete fix, not a round-1 miss and not a triaged LOW. MEDIUM: no gate reds, but AGENTS.md is the always-loaded governing doc, no unit owns the repair, and unit 8 exists in this very build to repair three instances of exactly this class.

**Fix.** Add AGENTS.md's layout line to spec 8's record-repair scope (or spec 3 §4 Files touched) with the corrected characterisation — waiver registries AND declared populations — and have spec 3 S6b state that HYGIENE's three 'six' statements become a derived phrasing or the correct count, since check 3's allowlist already enumerates eight and the registry makes nine. The stale comment at check-memory-hygiene.sh:289 rides along.

### R2-12 — 2026-08-24-spec-dFramedEntrypoint-4.md · §6 AC5 · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind ROUND1_MISSED

**Finding.** AC5 observes the new sort order through `--check`, a verb that reports only which artifacts are missing or stale — it never emits rows, an order, or a residual line. The criterion names an output the program does not have, so it is satisfiable by a green `build-index: clean (N artifact(s))` line that says nothing about sorting. S5 flags the sort change as a behaviour change precisely because it alters which unit the status verb names as next, and AC5 is the only criterion binding it.

**Evidence.** spec-4 AC5: "When the roster renders for a build whose units carry order values, `python tools/memory-tree/gen_build_index.py --check` reports the rows sorted by order then id, with unordered units last." `do_check` at gen_build_index.py:1115-1134 builds `bad` from two conditions only — `f"{rel} (missing — never rendered)"` and `f"{rel} (stale — differs from a fresh render)"` — plus orphan/unmanaged ledger paths, then prints either the DRIFT list of paths or `f"build-index: clean ({len(artifacts)} artifact(s))"`. Row content is never printed. Contrast AC2 and AC3 in the same spec, which correctly observe the rendered cells after `--write`.

**How the skeptic settled it.** Reproduced against the spec at HEAD and against the seam it declares it copies. spec-2:15-19 S1 creates a SECOND declared file, tools/memory-tree/build-readme-slot-highwater.txt. spec-2:43-46 S8 enumerates six arms — 'over-ceiling, over-high-water-under-ceiling, under both, a missing limits file, a limits row naming a slot the canon does not declare, and a limits file MISSING a row for a canonical slot' — every one of them about the LIMITS file. spec-2:121-122 §5 error/empty states names only 'a missing limits file' and 'a slot with no row'. No AC (AC1-AC11) mentions the high-water file's absence; AC6 covers the limits file only. So the high-water file's absent state has no declared verdict, no arm and no criterion, while the file's presence is what the entire advisory tier reads. The seam spec-2 §10 says it copies 'in both halves' has an explicit answer for exactly this branch: tools/check-template-size.sh:175-178 prints 'TEMPLATE-SIZE no-ratchet — no high-water record at $HIGHWATER; growth is unpriced.' and, per subject, '$key has no row in $HIGHWATER; growth is unpriced.' Not parked: §3 non-goals and §8 (F1, F2) say nothing about it. Held at MEDIUM because it leaves a branch of the shipped mechanism undefined — the implementer must invent a verdict, and a silent skip there makes the whole advisory tier dark without announcing itself, which is the class AGENTS.md §7 names ('A skip must announce itself').

**Fix.** Declare the absent-high-water verdict in §5 (advisory 'growth is unpriced', exit code untouched, matching check-template-size.sh:175-178), add the arm to S8, and add an AC observing it. Do the same for a high-water file present but missing a row for a graded slot.

### R2-13 — 2026-08-24-spec-dFramedEntrypoint-4.md · §6 AC5 (contrast AC2)

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind ROUND1_MISSED

**Finding.** AC3 is the hard case — impossible to observe at this unit's landing. AC4, AC5 and AC10 are not impossible but are satisfied vacuously over the empty population, which proves nothing.

**Evidence.** tools/memory-tree/gen_build_index.py:1115-1136 `do_check` — on drift it prints `build-index DRIFT — run: …` plus one `rel (stale …)` line per artifact; otherwise `print(f"build-index: clean ({len(artifacts)} artifact(s))")`. No row, order value or sort key is emitted on either path. Spec 4 AC5: "When the roster renders for a build whose units carry order values, `python tools/memory-tree/gen_build_index.py --check` reports the rows sorted by order then id, with unordered units last." Spec 4 AC2 gets this right — it names `--write` and reads the rendered ORDER cell.

**How the skeptic settled it.** The population claim reproduces: spec-2:152-154 AC8 states that at this unit's landing 'the declared population is empty, so the green is recorded as VACUOUS', spec-3:71-73 §4 Inventory says 'Before that, the bound set is empty', spec-3:28-29 S5 says the two verdict tiers 'grade only rows in the bound set', and spec-1:34-36 S5 wires the predicate to an empty declared population. AC3 (spec-2:142-143) then requires the advisory text to be present in the leg's persisted gate-log 'demonstrated by grepping that file after a `bash tools/run-gates/run-gates.sh` run' — with zero graded files no slot is measured, so no advisory can fire, and that observation cannot be made at this unit's landing. Under TEMPLATE-SPEC §6 each criterion is answered by a line in that unit's acceptance ledger, so AC3 is unanswerable. The channel itself is real and not the defect — run-gates.sh:95-106 resolves `<git-dir>/gate-logs` and writes one redacted log per leg. AC4, AC5 and AC10 are weaker than claimed: they are satisfiable, but only VACUOUSLY (printing nothing, changing nothing), which is the green-over-an-empty-population class this repo already carries a gotcha for. Held at MEDIUM on AC3 alone.

**Fix.** Re-point AC3 at a scratch fixture whose registry declares one file (the shape spec-2 AC9 and spec-3 AC7 already use, 'observed RED against a staged break first'), or move it beside unit 7 AC5's seeding observation. Give AC4, AC5 and AC10 a non-empty fixture population so their observation is not vacuous.

### R2-07 — 2026-08-24-spec-dFramedEntrypoint-5.md · §2 S4b and §2 S4c, against §6 (AC1-AC8) and §5 testing

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind FOLD_BROKE

**Finding.** Both scope items added by rev-2 land with no acceptance criterion and no line in the readiness checklist. S4b replaces the liveness assertion that nine selftest arms read; S4c retires strip_records_sentence and its anchors. Neither is observable from any of the eight ACs, and §5's testing line names only the index-to-name arm move and the coverage-join arm. AGENTS.md §7 requires a new gate's failing case to be observed, and TEMPLATE-SPEC §6 requires each criterion to be an observation that proves THIS change works — so the fold's own headline repair (the folder sentence 'turns out to be the liveness assertion nine arms read, so its replacement is now an S-item') ships unverifiable.

**Evidence.** spec-5:34-37 S4b 'This unit therefore REPLACES it with an explicit non-empty-population assertion over the record selector' and spec-5:38-41 S4c. spec-5 §6 carries exactly eight ACs; grep over the spec shows 'liveness', 'non-empty-population' and 'strip_records_sentence' appear only in S4b, S4c, §4 Files touched and the §9 log — never in §5 testing or any AC. AC6, the nearest, asserts only that --selftest 'completes without an index error and every region-addressing arm names its region rather than its tuple position'. Source confirms the stake: gen_build_index.py:654-656 records that 'several arms detect a mis-segmented record selector by noticing the sentence went missing'.

**How the skeptic settled it.** Reproduced from the spec body at HEAD and from source. spec-5:34-37 S4b REPLACES the derived folder sentence with 'an explicit non-empty-population assertion over the record selector'; spec-5:38-41 S4c retires strip_records_sentence and its anchors. Neither appears in §5's testing line (spec-5:135-137, which names only the index-to-name arm move and the coverage-join arm) nor in any of AC1-AC8. AC6 asserts only that --selftest 'completes without an index error and every region-addressing arm names its region rather than its tuple position' — which a vacuous replacement assertion satisfies. The stake reproduces in source: gen_build_index.py:654-656 states in its own comment that 'strip_records_sentence manages it and several arms detect a mis-segmented record selector by noticing the sentence went missing, so replacing it would remove the thing they watch'; strip_records_sentence is defined at :788 and called at :1078. Not parked: spec-5 §3 non-goals and §8 (F1, F2) say nothing about either item. This is the exact standard the spec set applies elsewhere — spec-2 AC9 and spec-3 AC7 both say 'observed RED against a staged break first' — and AGENTS.md §7 requires it ('A new gate is not landed until its failing case has been observed'). The fold's own headline repair therefore ships with no observation.

**Fix.** Add an AC that stages a mis-segmented record selector and observes the new non-empty-population assertion RED, and an AC observing `git grep -n strip_records_sentence tools/memory-tree/gen_build_index.py` empty at HEAD with --selftest still green. Name both in §5 testing.

### R2-9 — 2026-08-24-spec-dFramedEntrypoint-5.md · §2 S3 and §6 AC5

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind FOLD_BROKE

**Finding.** The fold made the two coverage joins render unconditionally "wherever the region renders" to close an absence-reads-as-coverage hole, and thereby opened a green-by-absence hole one level up: seven tracked builds carry no unit at all, so `own` is empty, both gaps are empty, and the new zero-gap phrasing prints "every unit id is named by a record" and "every unit id has a spec-audit record" as affirmative claims over an empty population. AC5 enumerates the full-coverage and no-records states and not the no-units state.

**Evidence.** `grep -c 'No spec under this build carries a status header' memory/builds/*/README.md` returns 1 for seven files: aDeployScout, aFerriedDossier, aKitHardener, aLeanRework, aPortableWarden, aRatchetForge, bThriftyBellows. tools/memory-tree/gen_build_index.py:669-671 computes `own = [u["id"] for u in build["units"]]` then `gap = [i for i in own if i not in named]`, so both gaps are `[]` when `units` is empty. Spec 5 §2 S3: "they render UNCONDITIONALLY wherever the region renders … The zero-gap case gets its own explicit phrasing: every unit id is named by a record". AC5 names only "records with FULL coverage" and "no records at all". AGENTS.md §7: "A probe that cannot move says so. A signal with no liveness assertion reports a reassuring zero when it is broken."

**How the skeptic settled it.** Reproduced end to end. tools/memory-tree/gen_build_index.py:668-671 computes `own = [u["id"] for u in build["units"]]` then `gap = [i for i in own if i not in named]` and `agap` the same way, so both gaps are [] whenever the build declares no unit. Seven tracked builds are in that state — aDeployScout, aFerriedDossier, aKitHardener, aLeanRework, aPortableWarden, aRatchetForge, bThriftyBellows each carry exactly one "No spec under this build carries a status header" line — and four of them (aFerriedDossier, aKitHardener, aRatchetForge, bThriftyBellows) already render a `| Record | Kind | Serves |` table, so they hit the fold's new unconditional branch with a populated `recs` and an empty `own`. Spec 5 S3 makes the joins render "UNCONDITIONALLY wherever the region renders" and mandates an explicit zero-gap phrasing ("every unit id is named by a record"), which turns a vacuously-true statement over an empty population into an affirmative coverage claim in four to seven generated files. AC5 enumerates the full-coverage and no-records states and not the no-units state, so nothing grades it. This is fresh surface the fold created while closing round-1 JOINS-22 (also MEDIUM), and it is the class AGENTS.md §7 names outright: "A probe that cannot move says so. A signal with no liveness assertion reports a reassuring zero when it is broken."

**Fix.** Add a third phrasing for the empty-unit-population case — "this build declares no unit, so there is nothing to cover" — distinct from the zero-gap phrasing, and extend AC5 to require it, demonstrated against one of the four unit-less builds that already carry a records table (aKitHardener is the smallest).

### R2-ARMS-1 — 2026-08-24-spec-dFramedEntrypoint-5.md · §2 S4 clause (c) (:30-32) against §2 S4c (:38-41)

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind FOLD_BROKE

**Finding.** The fold added two S-items that give the SAME selftest arms two different dispositions — repair versus retire — and S4(c)'s stated failure mode does not occur once S4c lands. An implementer following S4(c) re-points arms that S4c deletes; one following S4c deletes arms S4(c) says to fix.

**Evidence.** The only two arms asserting `.count("Records live under")` are gen_build_index.py:1307-1308 ('the authored sentence leaves the body') and :1364-1365 ('a claim BELOW the marker pair is removed too, not ignored'), and BOTH sit inside the strip_records_sentence fixture block at :1303-1371; :1341-1342 ('the derived sentence names only the folders that exist') reuses that block's own fixture `ta`. S4c retires 'strip_records_sentence and its anchors ... with their arms' — which is exactly that block. S4(c) instead classes those arms as ones that 'go to zero and fail rather than raise', i.e. arms to be repaired. Worse, the 'go to zero' arithmetic holds only while strip_records_sentence still runs: fixture ca (:1304) authors the sentence into the body, strip removes it (called at :1078), the derived sentence renders at :653, count == 1. Delete the derived sentence AND retire strip and the count is 1 again — the authored copy survives unstripped — so the arm passes for the opposite reason and asserts nothing. The 'go to zero' claim was true of the rev-1 spec round 1 audited; it stopped being true the moment the same fold added S4c.

**How the skeptic settled it.** Reproduced. The only two `.count("Records live under")` arms are gen_build_index.py:1307-1308 ('the authored sentence leaves the body', fixture ca) and :1364-1365 ('a claim BELOW the marker pair is removed too, not ignored', fixture cf), and both sit inside the strip_records_sentence fixture block that runs from the `_rec` helper through the recwrap arm at :1368-1371; :1341-1342 reuses that block's fixture ta. S4c retires 'strip_records_sentence and its anchors … with their arms', which is that block. S4(c)'s stated arithmetic — 'those go to zero and fail rather than raise' — holds only while strip_records_sentence still runs: fixture ca authors the sentence into the body, strip removes it (called at :1078), the derived sentence renders at :653, count == 1. Delete the derived sentence AND retire strip and the authored copy survives unstripped, so the count is 1 again and the arm passes for the opposite reason, asserting nothing. Same for cf. So the same fold that added S4c falsified S4(c)'s failure mode, and the two arms become vacuous passes rather than the loud failures S4(c) promises — the could-not-fail class this tree names. One nuance the claim overstates: S4(c) does not literally prescribe 'repair'; it states a failure mode. But S4's own stem says 'each is enumerated with its post-deletion disposition', so reading (c) as disposition-bearing is fair, and the conflict with S4c is real either way.

**Fix.** State ONE disposition. Either S4c retires the whole strip_records_sentence block including its two count arms and the derived-sentence literal arm at :1341, and S4(c) narrows to the surviving render arm at :1629; or drop S4c and re-point the arms. Not both.

### R2-JOINS-1 — 2026-08-24-spec-dFramedEntrypoint-5.md · §6 AC5 (:153-155), against §2 S3 (:19-26) and §5 error/empty states (:125-127)

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind FOLD_WRONG

**Finding.** The fold collapsed the two cases round 1's fix explicitly said must stay distinct, and the resulting AC demands the check emit a false coverage claim — re-introducing, in the acceptance criterion, the absence-reads-as-coverage class the unit exists to remove.

**Evidence.** Round 1 JOINS-22's Fix reads verbatim: 'Widen S3 and AC5 to require both lines unconditionally whenever the region renders, with explicit zero-gap phrasings ("every unit id is named by a record", "every unit id has a spec-audit record") DISTINCT FROM the no-records-at-all case.' AC5 as folded reads: 'When a build holds records with FULL coverage, and again when it holds no records at all, both join lines rendered by `render_region` render with their explicit zero-gap phrasing rather than being omitted.' The two states are not the same answer. gen_build_index.py:668-672 computes `named = {i for r in recs for i in r.get('ids', [])}` and `gap = [i for i in own if i not in named]`; for a build holding units and zero records, `named` is empty so `gap` is EVERY unit id, and the truthful render is 'Ids no record names: <all of them>'. AC5 demands 'every unit id is named by a record' for that same build. Spec-5's own §5 error/empty states gets it right — 'in all three states: gaps present, full coverage, no records at all' — and S3 distinguishes them too, so AC5 is the outlier, and AC5 is the thing that gets graded. Round 1's SENT-1 skeptic already stated the correct render for this case: 'a selector matching nothing renders `Ids no record names: <every id>` — byte-identical to an honest build holding no records.'

**How the skeptic settled it.** Reproduced from source. gen_build_index.py:667-678: `named = {i for r in recs for i in r.get('ids', [])}`, `own = [u['id'] for u in build['units']]`, `gap = [i for i in own if i not in named]`. For a build holding units and zero records, `named` is empty so `gap` is every unit id, and the truthful unconditional render is 'Ids no record names: <all of them>'. AC5 (:153-155) attaches 'with their explicit zero-gap phrasing' to BOTH antecedents, including 'when it holds no records at all', so as graded it demands the generator assert full coverage over a build with none. Round-1 JOINS-22's Fix reads verbatim (record line 293) 'with explicit zero-gap phrasings … DISTINCT FROM the no-records-at-all case', and round 1's own SENT-1 skeptic states the correct render for that case: 'a selector matching nothing renders `Ids no record names: <every id>` — byte-identical to an honest build holding no records.' Spec-5 §5 (:125-127) and S3 (:19-26) both keep the states distinct, so AC5 is the outlier, and AC5 is what gets graded. Not parked in §3 or §8.

**Fix.** Split AC5 in two: records present with full coverage renders the zero-gap phrasing; no records at all renders the gap phrasing naming every unit id. Keep §5's three-state wording as the source.

### R2-VACUOUS-JOIN-1 — 2026-08-24-spec-dFramedEntrypoint-5.md · §2 S3 (:19-26), §5 error/empty states (:125-127), §6 AC5

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind ROUND1_MISSED

**Finding.** Making the coverage joins unconditional was the right fix, but it creates a new state the spec does not enumerate: a build whose specs carry no status header has an EMPTY unit population, so the new zero-gap line asserts full coverage over zero ids. Seven tracked build READMEs are in that state today.

**Evidence.** gen_build_index.py:669 `own = [u["id"] for u in build["units"]]`; :671-672 `gap = [i for i in own if i not in named]` and `agap = [i for i in own if i not in audited]`. With `own` empty both are empty, so under S3's unconditional rendering the region prints the zero-gap phrasing — 'every unit id is named by a record' — over a population of zero. The generator has an explicit branch for that population at :646-648 ('*No spec under this build carries a status header; the status above is declared in the front matter.*'). Measured: `git ls-files 'memory/builds/*/README.md' | xargs grep -lF 'No spec under this build carries a status header' | wc -l` = 7. Spec-5 §5 enumerates exactly three states — 'gaps present, full coverage, no records at all' — and no unit-population state; S4b's replacement liveness assertion is declared over the RECORD selector ('an explicit non-empty-population assertion over the record selector'), not the unit selector, so it does not cover this. Spec-5's own §2 S3 justification is that these joins are 'the only spec-to-record coverage signal this repo has' — a signal that reports clean over an empty population is the vacuous-selector-empty-population class this tree names and gates against elsewhere (check-kit-versions.sh:95-100 refuses an empty template population for exactly this reason).

**How the skeptic settled it.** Reproduced and measured. gen_build_index.py:669 `own = [u['id'] for u in build['units']]` and :671-672 compute gap/agap over `own`; with `own` empty both are empty, so S3's unconditional rendering prints the zero-gap phrasing over a population of zero. The generator has an explicit branch for that population at :646-648 ('*No spec under this build carries a status header; the status above is declared in the front matter.*'), and `git ls-files 'memory/builds/*/README.md' | xargs grep -lF 'No spec under this build carries a status header' | wc -l` = 7 of 62 — four of the seven (aFerriedDossier, aKitHardener, aRatchetForge, bThriftyBellows) already carry a records table, so they reach the join code today. Spec-5 §5 (:125-127) enumerates exactly three states, none of them the empty unit population, and S4b's replacement liveness assertion is declared over the RECORD selector, not the unit selector. Held at MEDIUM rather than LOW because the claim is vacuous rather than false, but the build's own standard forbids it: spec-1 S5/AC5 make an empty declared population announce itself for exactly this reason, and check-kit-versions.sh:95-100 refuses an empty template population by name. Distinct from round 1's SENT-1, which addresses builds with no RECORDS.

**Fix.** Add a fourth state to S3 and §5 with an AC and an arm: a build with no units states that it has no units rather than claiming every unit id is covered.

### R2-12 — 2026-08-24-spec-dFramedEntrypoint-6.md · §2 S6b, §4 Migration and §8 F3 · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind FOLD_BROKE

**Finding.** The fold converted round 1's UNB-5/SEQ-5 into a scope item that does not decide anything: S6b reads "This unit ships a path-scoping argument with stated semantics, OR the incremental claim is dropped". §2 is the closed IN scope, and an implementer cannot build a disjunction. §4 Migration compounds it ("Whether that is one reviewable mechanical commit or an incremental convergence depends on F3"), F3's recommendation is one-commit, and no acceptance criterion grades either branch.

**Evidence.** Spec 6 §2 S6b verbatim; §4 Migration "depends on F3"; §8 F3 "Recommendation: one commit … and the incremental language deleted". AC1-AC8 contain no criterion naming a path-scoping argument and none naming a one-shot corpus write. Contrast spec 5 §2 S5, which states the corpus write as a decided S-item with its measured split cost. memory/TEMPLATE-SPEC.md §2/§8 separate scope from open questions precisely so a fork does not sit inside an S-item.

**How the skeptic settled it.** Reproduced against the spec at HEAD and against the seam it declares it copies. spec-2:15-19 S1 creates a SECOND declared file, tools/memory-tree/build-readme-slot-highwater.txt. spec-2:43-46 S8 enumerates six arms — 'over-ceiling, over-high-water-under-ceiling, under both, a missing limits file, a limits row naming a slot the canon does not declare, and a limits file MISSING a row for a canonical slot' — every one of them about the LIMITS file. spec-2:121-122 §5 error/empty states names only 'a missing limits file' and 'a slot with no row'. No AC (AC1-AC11) mentions the high-water file's absence; AC6 covers the limits file only. So the high-water file's absent state has no declared verdict, no arm and no criterion, while the file's presence is what the entire advisory tier reads. The seam spec-2 §10 says it copies 'in both halves' has an explicit answer for exactly this branch: tools/check-template-size.sh:175-178 prints 'TEMPLATE-SIZE no-ratchet — no high-water record at $HIGHWATER; growth is unpriced.' and, per subject, '$key has no row in $HIGHWATER; growth is unpriced.' Not parked: §3 non-goals and §8 (F1, F2) say nothing about it. Held at MEDIUM because it leaves a branch of the shipped mechanism undefined — the implementer must invent a verdict, and a silent skip there makes the whole advisory tier dark without announcing itself, which is the class AGENTS.md §7 names ('A skip must announce itself').

**Fix.** Declare the absent-high-water verdict in §5 (advisory 'growth is unpriced', exit code untouched, matching check-template-size.sh:175-178), add the arm to S8, and add an AC observing it. Do the same for a high-water file present but missing a row for a graded slot.

### R2-SPEC8-S3-1 — 2026-08-24-spec-dFramedEntrypoint-8.md · §2 S3 (:22-29) against §4 Files touched (:84-86) and §6 AC1-AC8

**Status:** CONFIRMED · raised MEDIUM, final MEDIUM · kind FOLD_BROKE

**Finding.** rev-3 split S3 into two independent corrections and added an edit to tools/memory-tree/gen_build_index.py, but neither §4 Files touched nor any acceptance criterion was updated. The unit now carries a scope item with no declared file and no observation — the record says it is handled and nothing checks that it was.

**Evidence.** S3 (:24-28): 'Separately and NOT as part of the same claim, `gen_build_index.py`'s own comment above the plan-pair constants carries a stale RATIONALE ... It is corrected here because a true rule resting on a dead reason is how the next reader deletes the rule.' §4 Files touched (:84-86) lists exactly: memory/DECISIONS.md, .memory-tree.conf, memory/map/features/build-readme-surface.md, memory/project/curation-debt.txt, memory/backlog/TOOL.md. tools/memory-tree/gen_build_index.py is absent. AC1 grades the decision row, AC2 the re-measurement, AC3 the dossier, AC4 curation-debt, AC5 the backlog row, AC6 the census, AC7 hygiene, AC8 the drift pin — none names the generator comment. The S3 claim itself is CORRECT and I verified it: the comment at gen_build_index.py:48-51 says 'the unattended kit's check_authorization byte-compares that slice across a run's pinned BASE', while check_authorization at tools/unattended/unattended.sh:1119-1160 reads only front-matter keys (slug, authorized-by, playbook, pieces) and never compares the roster slice — and tools/unattended/check-unattended.sh:1669 records the move ('That report named the AUTHORED `roster:units` pair, and S5's answer was to make the GENERATED...'). So the defect is bookkeeping, not substance. (No epoch consequence: check-verdict-epoch.sh's behav_in at :94-96 filters lines whose first non-space character is `#`, so a comment-only edit is not behaviour-bearing and owes no bump — which is also why spec 8 correctly carries no S-EPOCH.)

**How the skeptic settled it.** Verified at HEAD. Spec-8 S3 (:24-28) adds a correction to gen_build_index.py's plan-pair comment; §4 Files touched (:84-86) lists exactly memory/DECISIONS.md, .memory-tree.conf, memory/map/features/build-readme-surface.md, memory/project/curation-debt.txt, memory/backlog/TOOL.md — tools/memory-tree/gen_build_index.py is absent — and AC1-AC8 name the decision row, the re-measurement, the dossier, curation-debt, the backlog row, the census, hygiene and the drift pin, none of them the generator comment. The S3 claim itself is correct and I reproduced it: gen_build_index.py:49-52 says 'the unattended kit's check_authorization byte-compares that slice across a run's pinned BASE', while check_authorization at tools/unattended/unattended.sh:1119-1160 parses only the front-matter keys slug, authorized-by, playbook and pieces from one blob, and the comment immediately above it at :1115 says 'no whole-file equality is assertable'. TEMPLATE-SPEC.md:72 and :153 make numbered ACs the answerable record for each unit, so a scope item with no file and no criterion is unverifiable by the house's own convention. Held at MEDIUM: the substance is right, only the bookkeeping is missing, and §4 is explicitly an estimate.

**Fix.** Add tools/memory-tree/gen_build_index.py to spec-8 §4 Files touched and add an AC observing that the plan-pair comment at HEAD no longer rests on the byte-compare rationale, cited against check_authorization in tools/unattended/unattended.sh.

### R2-4 — CROSS — spec-5 §2 S1/S2/S3 and §4 Inventory vs spec-6 §8 F2 · spec-5 S1, S2, S3, §4 Inventory ("Exactly five files are reachable only through it"); spec-6 F2 · FOLDED IN THIS ROUND

**Status:** CONFIRMED · raised HIGH, final MEDIUM · kind ROUND1_MISSED

**Finding.** UNBOUND records — those whose `**Serves:**` line binds no unit id — are rendered TODAY only in the records table and the document inventory. Unit 5 deletes both. Unit 6 renders a record only into the specs whose ids its binding names, so an unbound record renders nowhere, and spec-6 F2 declines to change that on the ground that "the build-level answer stays with the build" — a build-level answer unit 5 has just deleted. Spec-5's orphan accounting names five headerless legacy specs and stops; it was computed against the document-inventory region alone and never re-run against the records table it also removes.

**Evidence.** `python tools/memory-tree/gen_build_index.py --print-bindings` terminates with the line `N\t10` — ten unbound records. `git ls-files 'memory/builds/*/README.md' | xargs grep -l '| — | \*none'` returns 9 files (aBoundedVerdict, aBranchedMandate, aFerriedDossier, aMouldedFolio, aSiftedPlaybook, aTetheredRecord, aWrittenMethod, bThriftyBellows, cBriefedPilot), 10 rows total. The renderer is gen_build_index.py:664-665 — `if r["state"] == "unbound": out.append(f"| [{label}]({rel}) | — | *none — {r['reason']}* |")` — inside the records table S2 deletes. spec-5 S3 re-emits only the two COVERAGE JOINS, which run id→record, the opposite direction. spec-5 §4 Inventory: "Exactly five files are reachable only through it, and all five are headerless legacy specs".

**How the skeptic settled it.** Reproduced exactly. `grep -n 'gate-legs.json' memory/builds/dFramedEntrypoint/spec/2026-08-24-spec-dFramedEntrypoint-1.md` returns :39 (S6's refusal), :131 ("· `tools/gate-legs.json` for the blind-spot paragraph ·") and :135 ("NOT `tools/gate-legs.json` — see S6.") — :131 and :135 are inside the same §4 Files touched sentence-list, so the list both requires and forbids the same edit. The refusal is real and enforced: tools/run-gates/run-gates.test.sh:96 and :125 both pin `KNOWN = {"name","argv","guard","impure","chunk","subject"}` and exit 1 on a stray key, and a json.load over tools/gate-legs.json returns 85 legs carrying exactly name/argv/chunk/subject (85 each), guard (49) and impure (1) — no seventh key exists, and JSON has no comment form for a prose paragraph. Re-graded HIGH→MEDIUM rather than confirmed at HIGH: the negation sits three lines later in the same list and S6 in the binding scope section states the refusal with its full reason, so the worst outcome is one red canary run, not a wrong verdict. It is still a spec that says X and not-X in one section, and the fold is what left it there.

**Fix.** Delete the clause "`tools/gate-legs.json` for the blind-spot paragraph" from spec 1 §4 Files touched. Keep the trailing "NOT `tools/gate-legs.json` — see S6." as the only mention in that section.

## LOW — 26

### R2-10 — 2026-08-24-spec-dFramedEntrypoint-1.md · §4 Inventory ('unit 3 supplies only data'), against spec-3 §2 S5, S6b and S6c

**Status:** CORRECTED · raised MEDIUM, final LOW · kind FOLD_BROKE

**Finding.** spec-1 §4 Inventory's closing clause 'and unit 3 supplies only data' is false at HEAD — unit 3 also edits gen_build_index.py, check-memory-hygiene.sh and adopt-memory-tree.sh. The contradiction round 1 raised is resolved, in spec 3, by S5's explicit replacement statement.

**Evidence.** spec-1:66-68 §4 Inventory 'This unit ships the reader as a function returning an empty set when the registry file is absent, so the predicate is complete before its population exists and unit 3 supplies only data.' against spec-3:25-29 S5 'this unit REPLACES unit 1's absent-file behaviour … The corresponding unit-1 selftest arm is inverted in this unit rather than left contradicting AC5', spec-3 S6b (check-3 allowlist plus its arm and HYGIENE.template.md prose) and spec-3 S6c (adopt-memory-tree.sh scaffold).

**How the skeptic settled it.** The trailing clause reproduces and is now false: spec-1:96-98 §4 Inventory ends 'so the predicate is complete before its population exists and unit 3 supplies only data', while spec-3 at HEAD has unit 3 shipping code in three places — S5 (spec-3:25-29) inverts unit 1's absent-file behaviour and its selftest arm, S6b (:31-35) edits check-memory-hygiene.sh check 3 plus its arm and HYGIENE.template.md prose, S6c (:36-37) edits adopt-memory-tree.sh. But the finding's framing that the round-1 fix went unserved is wrong: round 1's ask was that the replacement be STATED, and spec-3 S5 states it explicitly and by name ('this unit REPLACES unit 1's absent-file behaviour ... The corresponding unit-1 selftest arm is inverted in this unit rather than left contradicting AC5'), so the two specs no longer contradict — one of them now owns the transition. The second sub-claim is also wrong: spec-1 S8 (:47-48) says 'new --selftest arms covering every trigger, every refusal and the survey verb' and enumerates no absent-registry arm, and unit 3 INVERTS that arm rather than deleting it, so S8 claims nothing unit 3 removes. Residue is one stale five-word clause in a §4 Inventory paragraph.

**Fix.** Drop 'and unit 3 supplies only data' from spec-1:98 and add one clause: the empty-set-on-absent branch is provisional and unit 3 replaces it with a refusal, inverting this unit's arm.

### R2-11 — 2026-08-24-spec-dFramedEntrypoint-1.md · §2 S7 (the stated justification for the --survey verb)

**Status:** CONFIRMED · raised MEDIUM, final LOW · kind FOLD_WRONG

**Finding.** An unknown FIRST argument exits 2 with a usage line; what main() silently discards is argv[2] and beyond. The S-item stands and only its stated justification is wrong.

**Evidence.** tools/memory-tree/gen_build_index.py:1661-1666: `mode = argv[1] if len(argv) > 1 else "--check"` … `if mode not in ("--check", "--write", "--check-format", "--print-bindings"): print("usage: …"); return 2`. Reproduce: `python tools/memory-tree/gen_build_index.py --survey` prints the usage line and exits 2. Against spec-1:60-62 S7 '… because `main()` silently ignores an unknown argument, so an acceptance criterion naming a flag that does not exist would pass by printing the ordinary clean line.'

**How the skeptic settled it.** Reproduced by running the tool, not by reading it. `python tools/memory-tree/gen_build_index.py --survey` prints 'usage: gen_build_index.py [--check|--write|--check-format|--print-bindings|--selftest]' and exits 2. Source: gen_build_index.py:1661-1666 — `mode = argv[1] if len(argv) > 1 else "--check"` then `if mode not in (...): print("usage: ..."); return 2`; argv[2:] is never read. So spec-1:44-46 S7's second clause ('because `main()` silently ignores an unknown argument, so an acceptance criterion naming a flag that does not exist would pass by printing the ordinary clean line') is false for the first argument position, and the same false claim is carried into spec-1:207-209's rev-2 revision log. Under the true behaviour, an AC8 naming --survey before the verb exists would have EXITED 2 loudly, not passed quietly. Severity re-graded from MEDIUM to LOW: the S-item stands on the independent house rule (AGENTS.md §7, 'Run a candidate gate predicate over the real tree before wiring it'), §4 Files touched and AC8 are unaffected, and no deliverable changes — this is a rationale clause that mis-describes a mechanism the S-item next to it gets right, the class round 1 graded LOW repeatedly.

**Fix.** Correct S7's second clause to name argv[2] as the silently-discarded position, or drop the clause and rest S7 on the run-the-predicate-over-the-real-tree rule alone. Correct the same sentence in the rev-2 revision log entry.

### R2-14 — 2026-08-24-spec-dFramedEntrypoint-1.md · §2 S7 (the justification clause)

**Status:** CONFIRMED · raised LOW, final LOW · kind FOLD_WRONG

**Finding.** The fold shipped the `--survey` verb on a stated reason that the source contradicts. S7 says the verb exists "because `main()` silently ignores an unknown argument, so an acceptance criterion naming a flag that does not exist would pass by printing the ordinary clean line". `main()` refuses an unknown MODE with a usage line and exit 2; only a trailing argv[2:] is discarded, which is what round 1 actually measured against the old `--check-format --dry-run`. Under the new AC8 shape — `--survey` as argv[1] — the failure the rationale describes cannot occur, and spec-8 S3 in this same build argues that a true rule resting on a dead reason is how the next reader deletes the rule.

**Evidence.** gen_build_index.py:1662-1666 — `mode = argv[1] if len(argv) > 1 else "--check"` then `if mode not in ("--check", "--write", "--check-format", "--print-bindings"): print("usage: gen_build_index.py [--check|--write|--check-format|--print-bindings|--selftest]"); return 2`. Round 1 DRYRUN-11 measured the argv[2] case: "`python tools/memory-tree/gen_build_index.py --check-format --dry-run` -> `build-index: slot contract clean (62 build README(s))`, rc=0". spec-1 AC8 names `python tools/memory-tree/gen_build_index.py --survey` — a mode, not a trailing flag.

**How the skeptic settled it.** Reproduced. spec-2:166-167 and spec-3:143-144 both name 'the LF discipline check' in §7, which TEMPLATE-SPEC:161-163 defines as 'The named gate legs this unit must keep green, plus any new gate it adds.' tools/gate-legs.json carries 85 legs and none of them checks line endings: a word-boundary regex for lf/crlf/eol/gitattributes/line-ending over every leg name returns nothing (a naive substring search hits only 'selftest'), and grepping the manifest for gitattributes/crlf/eol returns nothing at all. No script named or describable as an LF discipline check exists either — `git grep -n 'LF discipline'` returns one hit repo-wide outside this build, AGENTS.md:56, where it labels `.gitattributes` itself. The nearest real machinery is govkit's lf_pin plumbing (govkit.py:1805 lf_pins, :2315-2345 writing the .gitattributes block on apply), which is a DEPLOYER path into a target tree, not a gov-side assertion; selfcheck reads no .gitattributes. Severity re-graded from MEDIUM to LOW: no leg reds, no deliverable changes, and both specs already name the real protection in §4 — spec-2:110-112 correctly adds `.gitattributes` plus `tools/memory-tree/kit.toml`'s own `[[lf_pin]]` block. The defect is a §7 line asserting a merge-bar witness that does not exist.

**Fix.** Drop the phantom leg from both §7 lists. If a witness is wanted, say plainly that the LF pin is declarative (.gitattributes plus the kit's [[lf_pin]] rows) and verified by staged-byte inspection per AGENTS.md §11. Spec 3 §4 should also gain the kit.toml [[lf_pin]] row that spec 2 §4 already carries.

### R2-16 — 2026-08-24-spec-dFramedEntrypoint-1.md · §2 S4 and §6 AC3, against §2 S5

**Status:** CORRECTED · raised MEDIUM, final LOW · kind ROUND1_MISSED

**Finding.** The predicate is population-scoped by the plain reading of S5, so no contradictory verdicts follow; what is left is that §1's total-exemption-hole sentence reads corpus-wide against a scoped S5, and AC3 does not name where the staged break lands.

**Evidence.** spec-1:44-46 S5 'the canon applies only to files the registry of unit 3 declares. Until that unit lands, the predicate is written and exercised but wired to an empty declared population'. spec-1 S4 'It must instead report that absence as a violation naming the file.' spec-1 AC3 'When a README carries no generated marker pair at all, `--check-format` exits 1 naming that file; the RED is observed first by staging the break, because this arm passes today.' Source: do_check_format currently selects every tracked `{MEMORY_ROOT}/builds/*/README.md` with no registry filter, and slot_violations returns [] early on `if not spans`. Measured: all 62 tracked build READMEs carry the `<!-- gen:build-index -->` pair, so no live file exercises the arm either way — which is why spec-1 S8 already calls for a fixture.

**How the skeptic settled it.** The source citations reproduce — do_check_format (gen_build_index.py:1137-1157) selects every tracked `{MEMORY_ROOT}/builds/*/README.md` with no registry filter today, slot_violations returns [] early at :991-992 on `if not spans`, and I measured all 62 tracked build READMEs carrying the `<!-- gen:build-index -->` pair, so no live file exercises the arm. But the 'two opposite verdicts' framing overstates. spec-1:34-36 S5 scopes the check, not one trigger: 'the canon applies only to files the registry of unit 3 declares ... the predicate is written and exercised but wired to an empty declared population'. slot_violations is called per graded file from do_check_format, so scoping the population scopes all three triggers by construction; the plain reading is unambiguous and spec-1 §5 observability, S8 ('a fixture for the no-markers case that the live corpus cannot exercise') and §5 testing all sit consistently under it. AC3's 'staging the break' is also satisfiable on the live tree — gates read tracked/staged files, so staging a marker deletion is a legitimate RED observation — which is why this is not a could-not-fire defect. The residue is genuine but small: §1's 'closes the total-exemption hole that lets a README with no generated marker pair skip the check entirely' reads corpus-wide while S5 scopes it, so at landing the hole is closed for nobody, and AC3 does not say whether the staged break lands on a bound file or on the S8 fixture.

**Fix.** Add one clause to S4 stating that the no-markers refusal is population-scoped like the other two triggers, reconcile §1's sentence with it, and re-word AC3 to name the S8 fixture (or a staged break on a bound file) as the RED observation.

### R2-16 — 2026-08-24-spec-dFramedEntrypoint-1.md · §1 Goal and §2 S4 against §2 S5

**Status:** CORRECTED · raised MEDIUM, final LOW · kind ROUND1_MISSED

**Finding.** The predicate is population-scoped by the plain reading of S5, so no contradictory verdicts follow; what is left is that §1's total-exemption-hole sentence reads corpus-wide against a scoped S5, and AC3 does not name where the staged break lands.

**Evidence.** Spec 1 §1 Goal and §2 S4 ("It must instead report that absence as a violation naming the file") against §2 S5 ("the canon applies only to files the registry of unit 3 declares. Until that unit lands, the predicate is written and exercised but wired to an empty declared population"). Measured: all 62 tracked build READMEs carry all four registered pairs, so the no-markers arm has no live instance (`slot_violations` returns `[]` only when `spans` is empty — gen_build_index.py:986-991). The two shipped triggers grade all 62 today (do_check_format iterates every tracked README, :1145-1152) and must keep doing so, or the bar's current coverage shrinks to zero on this unit's commit. S8 correctly notes a fixture is needed; AC3 does not name it.

**How the skeptic settled it.** The source citations reproduce — do_check_format (gen_build_index.py:1137-1157) selects every tracked `{MEMORY_ROOT}/builds/*/README.md` with no registry filter today, slot_violations returns [] early at :991-992 on `if not spans`, and I measured all 62 tracked build READMEs carrying the `<!-- gen:build-index -->` pair, so no live file exercises the arm. But the 'two opposite verdicts' framing overstates. spec-1:34-36 S5 scopes the check, not one trigger: 'the canon applies only to files the registry of unit 3 declares ... the predicate is written and exercised but wired to an empty declared population'. slot_violations is called per graded file from do_check_format, so scoping the population scopes all three triggers by construction; the plain reading is unambiguous and spec-1 §5 observability, S8 ('a fixture for the no-markers case that the live corpus cannot exercise') and §5 testing all sit consistently under it. AC3's 'staging the break' is also satisfiable on the live tree — gates read tracked/staged files, so staging a marker deletion is a legitimate RED observation — which is why this is not a could-not-fire defect. The residue is genuine but small: §1's 'closes the total-exemption hole that lets a README with no generated marker pair skip the check entirely' reads corpus-wide while S5 scopes it, so at landing the hole is closed for nobody, and AC3 does not say whether the staged break lands on a bound file or on the S8 fixture.

**Fix.** Add one clause to S4 stating that the no-markers refusal is population-scoped like the other two triggers, reconcile §1's sentence with it, and re-word AC3 to name the S8 fixture (or a staged break on a bound file) as the RED observation.

### R2-NOMARK-8 — 2026-08-24-spec-dFramedEntrypoint-1.md · §2 S4 and S5; AC3 against AC5

**Status:** CORRECTED · raised MEDIUM, final LOW · kind ROUND1_MISSED

**Finding.** S4 leaves the no-markers trigger's population scope unstated, and AC5's unqualified 'empty population => exit 0' needs a carve-out for AC3's staged no-markers break. The ACs are not contradictory over one tree at landing, and the arm IS exercisable on the live tree by staging the break, as AC3 says.

**Evidence.** Spec 1 S4: "`slot_violations` returns an empty list whenever it finds no registered region pair ... It must instead report that absence as a violation naming the file" — no population clause. Spec 1 S5: "the canon applies only to files the registry of unit 3 declares." AC1, AC2 and AC4 each say "a README in the declared population"; AC3 does not. AC5: "When the declared population is empty, `--check-format` exits 0 AND prints a line stating the population is empty." The shipped code confirms the trigger is corpus-wide today: tools/memory-tree/gen_build_index.py:978-1004 `slot_violations` starts `if not spans: return []`, and do_check_format at :1137ff iterates every tracked `memory/builds/*/README.md` with no registry filter. Every one of the 62 tracked build READMEs carries `<!-- gen:build-index -->` today (verified by loop), so the arm cannot be exercised on the live tree either way.

**How the skeptic settled it.** Half the claim is wrong and half is a real wording gap. Wrong: the ACs do not demand opposite exit codes 'for the same tree at this unit's landing', because AC3 explicitly scopes its RED to a staged break ('the RED is observed first by staging the break, because this arm passes today') — and the finding's own line that 'the arm cannot be exercised on the live tree either way' contradicts that, since staging the break is exactly what exercises it. I verified 62 of 62 tracked build READMEs carry `<!-- gen:build-index -->`, and do_check_format at :1144-1150 iterates every tracked build README with no registry filter today. Real: S4 does not state its population scope, and AC5's unqualified 'When the declared population is empty, --check-format exits 0' collides with AC3's staged-break exit 1 on a tree where the population is also empty; one of the two needs a qualifier. Graded LOW rather than MEDIUM because §1 Goal settles the intent in as many words ('closes the total-exemption hole that lets a README with no generated marker pair skip the check entirely' — a corpus-wide framing) and S8 already ships the fixture, so an implementer does not build the wrong mechanism; the residue is one missing clause on AC5.

**Fix.** Say in S4 that the no-markers trigger is corpus-wide (matching §1 Goal) while the canon of S1 is population-scoped, and qualify AC5 to 'exits 0 absent a no-markers violation AND prints a line stating the population is empty', so an empty declared population is never read as an unarmed leg.

### R2-BYTES-11 — 2026-08-24-spec-dFramedEntrypoint-2.md · §4 Inventory ("Bytes, not characters"), folded at rev-3

**Status:** CONFIRMED · raised LOW, final LOW · kind FOLD_WRONG

**Finding.** The fold correctly replaced round 1's identified false reason with an accurate description of the source, then attached a new non-sequitur to it. The awk facts as now stated are right; the inference that bytes therefore make the verdict node-independent is not, because a character measure in Python is equally node-independent. The spec's stated justification for choosing bytes still does not support choosing bytes.

**Evidence.** Spec 2 §4 Inventory now reads: "What its own comment refuses to pin is the awk `length()` primitive, which counts bytes or characters depending on the awk build and the ambient locale ... This check is written in Python, where the choice is explicit, and it picks bytes so the verdict is node-independent by construction rather than by luck." The awk half verifies exactly — tools/memory-tree/check-memory-hygiene.sh:51-53 ("Characters, not bytes: `length()` decides this verdict and whether it counts characters or bytes is a property of the awk build and the ambient locale, which check 7 deliberately does not pin") and :502-506 ("NO `LC_ALL=` prefix and no `xargs` wrapper that sets one, deliberately"). But `len(text)` over decoded text in Python is as deterministic as `len(text.encode())`; neither is locale-dependent at this measurement site, so the node-independence clause distinguishes nothing. Round 1's BYTES-20 named the real reason available (the sibling declared ceiling in tools/template-size-limits.txt is in bytes and is compared against on-disk size) and the fold did not use it.

**How the skeptic settled it.** Reproduced. The corrected awk paragraph in spec 2 §4 is now accurate against check-memory-hygiene.sh:51-53 ('Characters, not bytes: `length()` decides this verdict and whether it counts characters or bytes is a property of the awk build and the ambient locale') — that half of the fold landed. The trailing clause does not follow: gen_build_index.py:124-128 `read_text` opens 'rb', decodes UTF-8 and normalises CRLF, so `len(text)` over decoded text is as node-independent as `len(text.encode())`; nothing at this measurement site distinguishes the units on that ground. This is not a wording preference — round 1's BYTES-20 fix said in as many words 'not because a character measure would differ across nodes, which it cannot at this measurement site', and named the reason that is actually available (tools/template-size-limits.txt declares '`<repo-relative-path>\t<bytes>` per row' and compares against on-disk size). The fold replaced the false reason with a softer restatement of the same false inference, so the record says the finding was folded while the defect it named survives. LOW: no mechanism, AC or deliverable turns on it.

**Fix.** Keep the corrected awk paragraph as a note about the SIBLING check's declared unit, and give the bytes choice the reason round 1 named: parity with tools/template-size-limits.txt, whose ceilings are bytes compared against on-disk size, which is the shape §10 says this unit copies in both halves.

### R2-14 — 2026-08-24-spec-dFramedEntrypoint-2.md (with -3) · spec 2 §6 AC8 and spec 3 §6 AC3, against the S-EPOCH per-push-range rule in both

**Status:** CONFIRMED · raised MEDIUM, final LOW · kind FOLD_BROKE

**Finding.** The fold introduced a bump rule scoped to a PUSH RANGE and left two acceptance criteria scoped to a UNIT's landing. Units 2 and 3 are one wave of the declared order, both move the engine, and only one commit in the range can carry the bump; at the other unit's commit `check-verdict-epoch.sh` fails outright, so "the bar is green" is false for whichever lands first. Neither spec says whether "landing" means the commit or the push.

**Evidence.** S-EPOCH (both specs): "The rule binds per PUSH RANGE, not per commit: units landing in one push need one correctly-placed bump, placed on the LAST engine-moving commit in that range." Spec 2 AC8: "When `bash tools/run-gates/run-gates.sh` runs at this unit's landing, the bar is green". Spec 3 AC3: "… `bash tools/run-gates/run-gates.sh` is green". tools/memory-tree/check-verdict-epoch.sh:127-141 walks `git rev-list "$BASE"..HEAD -- $SCAN` newest-first for W and fails with `FAILED — $moved behaviour-bearing line(s) of the engine moved in $W, and NO commit in BASE..HEAD changes KIT_MEMORY_TREE_VERSION` when S is empty; BASE defaults to `git merge-base origin/main HEAD` (:74-78), so every intermediate commit on the branch is inside the judged range. The leg carries no `guard` and `subject: repo` in tools/gate-legs.json.

**How the skeptic settled it.** Reproduced. spec-2:166-167 and spec-3:143-144 both name 'the LF discipline check' in §7, which TEMPLATE-SPEC:161-163 defines as 'The named gate legs this unit must keep green, plus any new gate it adds.' tools/gate-legs.json carries 85 legs and none of them checks line endings: a word-boundary regex for lf/crlf/eol/gitattributes/line-ending over every leg name returns nothing (a naive substring search hits only 'selftest'), and grepping the manifest for gitattributes/crlf/eol returns nothing at all. No script named or describable as an LF discipline check exists either — `git grep -n 'LF discipline'` returns one hit repo-wide outside this build, AGENTS.md:56, where it labels `.gitattributes` itself. The nearest real machinery is govkit's lf_pin plumbing (govkit.py:1805 lf_pins, :2315-2345 writing the .gitattributes block on apply), which is a DEPLOYER path into a target tree, not a gov-side assertion; selfcheck reads no .gitattributes. Severity re-graded from MEDIUM to LOW: no leg reds, no deliverable changes, and both specs already name the real protection in §4 — spec-2:110-112 correctly adds `.gitattributes` plus `tools/memory-tree/kit.toml`'s own `[[lf_pin]]` block. The defect is a §7 line asserting a merge-bar witness that does not exist.

**Fix.** Drop the phantom leg from both §7 lists. If a witness is wanted, say plainly that the LF pin is declarative (.gitattributes plus the kit's [[lf_pin]] rows) and verified by staged-byte inspection per AGENTS.md §11. Spec 3 §4 should also gain the kit.toml [[lf_pin]] row that spec 2 §4 already carries.

### R2-14 — 2026-08-24-spec-dFramedEntrypoint-2.md and 2026-08-24-spec-dFramedEntrypoint-3.md · spec-2 §7 (line 166) and spec-3 §7 (line 143)

**Status:** CONFIRMED · raised MEDIUM, final LOW · kind ROUND1_MISSED

**Finding.** Both specs list 'the LF discipline check' among the gate legs the unit must keep green. No such leg exists. tools/gate-legs.json carries 85 legs and none of them checks LF/CRLF or .gitattributes. §7 is defined by TEMPLATE-SPEC as 'the named gate legs this unit must keep green', and round 1 already required §7 accuracy for the epoch leg (UNB-4), so an invented leg name here is the same class left standing. The real protection for these files is declarative — .gitattributes plus the kit's [[lf_pin]] rows, which spec 2 rev-3 correctly added to §4 — and it is not a leg anyone can run.

**Evidence.** spec-2:166 '`build README slot contract` · `build-index selftest` · `memory hygiene` · the LF discipline check over the new data file …'; spec-3:143 '… · the LF discipline check ·'. Reproduce: `python -c "import json;print([l['name'] for l in json.load(open('tools/gate-legs.json'))])"` lists 85 names, none matching LF/CRLF/eol/gitattributes; `grep -in 'lf\b|gitattributes|crlf|line.ending' tools/gate-legs.json` returns only 'self-test' substring hits.

**How the skeptic settled it.** Reproduced. spec-2:166-167 and spec-3:143-144 both name 'the LF discipline check' in §7, which TEMPLATE-SPEC:161-163 defines as 'The named gate legs this unit must keep green, plus any new gate it adds.' tools/gate-legs.json carries 85 legs and none of them checks line endings: a word-boundary regex for lf/crlf/eol/gitattributes/line-ending over every leg name returns nothing (a naive substring search hits only 'selftest'), and grepping the manifest for gitattributes/crlf/eol returns nothing at all. No script named or describable as an LF discipline check exists either — `git grep -n 'LF discipline'` returns one hit repo-wide outside this build, AGENTS.md:56, where it labels `.gitattributes` itself. The nearest real machinery is govkit's lf_pin plumbing (govkit.py:1805 lf_pins, :2315-2345 writing the .gitattributes block on apply), which is a DEPLOYER path into a target tree, not a gov-side assertion; selfcheck reads no .gitattributes. Severity re-graded from MEDIUM to LOW: no leg reds, no deliverable changes, and both specs already name the real protection in §4 — spec-2:110-112 correctly adds `.gitattributes` plus `tools/memory-tree/kit.toml`'s own `[[lf_pin]]` block. The defect is a §7 line asserting a merge-bar witness that does not exist.

**Fix.** Drop the phantom leg from both §7 lists. If a witness is wanted, say plainly that the LF pin is declarative (.gitattributes plus the kit's [[lf_pin]] rows) and verified by staged-byte inspection per AGENTS.md §11. Spec 3 §4 should also gain the kit.toml [[lf_pin]] row that spec 2 §4 already carries.

### R2-06 — 2026-08-24-spec-dFramedEntrypoint-3.md · §4 Data model (line 67), §5 risks (line 113) and §6 AC3, against §2 S4 and §6 AC4

**Status:** CORRECTED · raised MEDIUM, final LOW · kind FOLD_BROKE

**Finding.** AC3 omits the pin decrement from its precondition, so it contradicts AC4 as literally written and describes the same act differently from spec-7 S4. §4 Data model and §5 risks are NOT contradictory — a shrink-only exempt count is compatible with an equality pin that tracks it — so that half of the claim does not stand.

**Evidence.** spec-3:21-22 S4 'The pin is an EQUALITY, not a ceiling: a pin above the measured exempt count reds and names both numbers'; spec-3 AC4 'When the exempt count DIFFERS from the declared pin in either direction, `--check-format` exits 1'. Against spec-3:67 '§4 Data model … A pin line declares the exempt count, and the count is shrink-only' and spec-3:113 '§5 risks … Mitigated by the shrink-only pin'. And spec-3 AC3 'When a build README moves from an exempt row to a bound row and conforms, `bash tools/run-gates/run-gates.sh` is green and the exempt count printed by the `build README slot contract` leg has fallen by one' — no pin edit named. Compare spec-7 S4: 'the exempt pin falls by the same number, in the same commit as the file they describe.'

**How the skeptic settled it.** Half reproduces, half overstates. Reproduced: spec-3:21-22 S4 makes the pin an EQUALITY and AC4 (spec-3:129-130) reds in either direction, while AC3 (spec-3:127-128) asserts a green `bash tools/run-gates/run-gates.sh` after one README moves exempt->bound with no pin edit named — under an equality that run reds, so AC3 as literally written cannot be observed. spec-7:22-23 S4 does state the opposite discipline ('the exempt pin falls by the same number, in the same commit as the file they describe'). But the §4/§5 'shrink-only' half does not contradict the equality: spec-3:67 says 'A pin line declares the exempt count, and the count is shrink-only' — a count that only travels downward is fully compatible with a pin asserted equal to it, and spec-3:112-113's 'shrink-only pin' is loose wording for the same policy, not a second mechanism. Nothing here changes what gets built: AC4 forces the implementer to lower the pin, so the defect is one missing clause in AC3's precondition, self-announcing on the first bar.

**Fix.** Add the pin decrement to AC3's precondition so it matches spec-7 S4 ('...moves from an exempt row to a bound row, the pin falls by one in the same commit, and conforms'). Optionally tighten §4/§5 to say the pin equals the measured exempt count and is lowered by hand as rows drain.

### R2-11 — 2026-08-24-spec-dFramedEntrypoint-3.md · §2 S4 and §6 AC4 against §4 Data model and §5 risks

**Status:** CONFIRMED · raised MEDIUM, final LOW · kind FOLD_BROKE

**Finding.** An unknown FIRST argument exits 2 with a usage line; what main() silently discards is argv[2] and beyond. The S-item stands and only its stated justification is wrong.

**Evidence.** Spec 3 §2 S4: "The pin is an EQUALITY, not a ceiling: a pin above the measured exempt count reds and names both numbers." AC4: "When the exempt count DIFFERS from the declared pin in either direction, `--check-format` exits 1". §4 Data model: "A pin line declares the exempt count, and the count is shrink-only." §5 risks: "Mitigated by the shrink-only pin, which turns a stalled drain into a visible number rather than a silent one." S2: "every tracked `memory/builds/*/README.md` that is not named by a row and not named by an explicit exemption row reds". The rev-2 log records only the equality change.

**How the skeptic settled it.** Reproduced by running the tool, not by reading it. `python tools/memory-tree/gen_build_index.py --survey` prints 'usage: gen_build_index.py [--check|--write|--check-format|--print-bindings|--selftest]' and exits 2. Source: gen_build_index.py:1661-1666 — `mode = argv[1] if len(argv) > 1 else "--check"` then `if mode not in (...): print("usage: ..."); return 2`; argv[2:] is never read. So spec-1:44-46 S7's second clause ('because `main()` silently ignores an unknown argument, so an acceptance criterion naming a flag that does not exist would pass by printing the ordinary clean line') is false for the first argument position, and the same false claim is carried into spec-1:207-209's rev-2 revision log. Under the true behaviour, an AC8 naming --survey before the verb exists would have EXITED 2 loudly, not passed quietly. Severity re-graded from MEDIUM to LOW: the S-item stands on the independent house rule (AGENTS.md §7, 'Run a candidate gate predicate over the real tree before wiring it'), §4 Files touched and AC8 are unaffected, and no deliverable changes — this is a rationale clause that mis-describes a mechanism the S-item next to it gets right, the class round 1 graded LOW repeatedly.

**Fix.** Correct S7's second clause to name argv[2] as the silently-discarded position, or drop the clause and rest S7 on the run-the-predicate-over-the-real-tree rule alone. Correct the same sentence in the rev-2 revision log entry.

### R2-15 — 2026-08-24-spec-dFramedEntrypoint-3.md · §2 S6b (the HYGIENE.template.md prose half)

**Status:** CORRECTED · raised MEDIUM, final LOW · kind ROUND1_MISSED

**Finding.** The S3 generator-comment half ships with no acceptance criterion. The §4 Files-touched omission is an estimate gap rather than a defect, and the epoch exemption is correct as-is — a comment-only edit is filtered by check-verdict-epoch.sh's behav_in, so no bump is owed and no statement is required.

**Evidence.** `git ls-files memory/project/` returns 8 files. memory/HYGIENE.md:29 "├── project/ the gate's own waiver registries (`*.txt`, six of them) and nothing else" and :94 "Six plain lists in `memory/project/` — the whole of what that directory holds". Spec 3 §2 S6b: "this unit ALSO adds the registry's name to that case list, with its selftest arm and the matching `HYGIENE.template.md` prose". AGENTS.md §7: "NO count of a derived population is written in prose. The checker derives every figure it reports; a number typed beside the thing it counts is wrong on the next commit and nobody notices."

**How the skeptic settled it.** The premise reproduces: spec-8:22-26 S3 now carries a code-file edit ('`gen_build_index.py`'s own comment above the plan-pair constants carries a stale RATIONALE ... It is corrected here'), added at rev-3 per spec-8:149, and spec-8:84-86 §4 Files touched lists only DECISIONS.md, .memory-tree.conf, the dossier, curation-debt.txt and backlog/TOOL.md. AC3 (spec-8:113-115) covers the dossier only, and §7 (:129-130) omits both legs the edit arms. But two of the three sub-claims are weak. §4's own sub-head is 'Files touched (estimate)' per TEMPLATE-SPEC:127, so an omitted single-comment edit is an incomplete estimate, not a broken contract. The epoch half is a suggestion, not a defect: check-verdict-epoch.sh:95-98 behav_in filters `^[[:space:]]*(#|$)`, so a comment-only edit is genuinely non-behaviour-bearing and no bump is owed — the spec is CORRECT to carry no S-EPOCH block, and asking it to say so is preference. The §7 half costs nothing either: 'build-index selftest' is a held selftest leg and 'kit/dogfood doc parity' compares live copies against templates, so neither reds on a Python comment change. What survives is the absent acceptance criterion for the S3 code-comment half — real, and the same class the fold fixed elsewhere in this set.

**Fix.** Add an AC observing the corrected rationale at HEAD above gen_build_index.py's plan-pair constants, and add tools/memory-tree/gen_build_index.py to §4 Files touched with the comment-only scope.

### R2-8 — 2026-08-24-spec-dFramedEntrypoint-3.md · §2 S6c against §6 AC5

**Status:** CONFIRMED · raised MEDIUM, final LOW · kind FOLD_WRONG

**Finding.** The fold answered ADOPT-1 with an adopter scaffold that only a NEW adopter ever runs. `adopt-memory-tree.sh` is an idempotent converge that exits 0 as a no-op the moment a scaffolded `memory/` exists, so an existing adopter who copy-installs a newer memory-tree kit gets AC5's hard refusal on an absent `memory/project/readme-contract.txt` and re-running the adopter will not create it. The adopter hazard the finding named survives the fix.

**Evidence.** tools/memory-tree/adopt-memory-tree.sh:48-53 — `if [ -d "$M" ]; then if [ -f "$M/HYGIENE.md" ] && grep -q 'gov:kit memory-tree@' "$M/HYGIENE.md"; then echo "$M/ already scaffolded by memory-tree — nothing to do."; exit 0; fi` — and :37 `[ "${1:-}" = "--scaffold" ] || { echo "usage: $0 --scaffold"; exit 2; }`, so there is no upgrade verb. Spec 3 AC5: "When `memory/project/readme-contract.txt` is absent, `--check-format` exits 1 naming the expected path rather than skipping." The leg ships to adopters: tools/memory-tree/kit.toml:142.

**How the skeptic settled it.** The mechanism reproduces from source. tools/memory-tree/adopt-memory-tree.sh:37 accepts only `--scaffold` (`[ "${1:-}" = "--scaffold" ] || { echo "usage: $0 --scaffold"; exit 2; }`), and :47-53 exits 0 as a no-op whenever `$M/HYGIENE.md` carries the `gov:kit memory-tree@` marker — the marker match is version-agnostic, so no upgrade path exists for a tree already scaffolded by any earlier kit version. tools/memory-tree/README.md documents only "Adopt — new project (scaffold)" and a one-time "Adopt — existing tree (migrate)"; no re-converge verb is documented. So spec 3 S6c's scaffold reaches new adopters only, while AC5's hard refusal on an absent `memory/project/readme-contract.txt` reaches every adopter, and the `build README slot contract` leg that enforces it ships to all of them (tools/memory-tree/kit.toml:142-146, subject repo, guard []). Round 1's ADOPT-1 offered exactly two remedies and the fold took the scaffold one, so this is the residue of that choice rather than a re-raise. Re-graded MEDIUM→LOW: the damage is adopter-only, the general defect (the kit adds a file, the adopter script only scaffolds) predates this build and already affects two gov-only registries round 1 noted as unscaffolded, and the manual remedy for an upgrading adopter is creating one empty file.

**Fix.** Say in S6c which shape an UPGRADING adopter gets. Either make the scaffold step an idempotent converge that creates a missing `readme-contract.txt` on an already-scaffolded tree (which needs a verb the adopter does not have today, so it is its own scope item), or let an absent registry resolve forward to the legal present-but-empty state with a printed announced line, keeping the hard refusal for a malformed one.

### R2-ARMS-12 — 2026-08-24-spec-dFramedEntrypoint-4.md · §4 Files touched ("memory/project/unarmed-branches.txt if branch ordinals shift"); rev-3 log

**Status:** CORRECTED · raised LOW, final LOW · kind FOLD_WRONG

**Finding.** memory/project/unarmed-branches.txt carries three data rows, all naming tools/unattended/unattended.sh; none names check-memory-hygiene.sh. Spec 4 §4 still points at that file instead of ARMS_FLOORS, and its rev-3 log over-claims by saying it folded the round-1 LOW factual corrections when it folded only ROWCOST-17 — GATE-9 and SEQ-2 are both still open.

**Evidence.** memory/project/unarmed-branches.txt contains exactly one data row, `tools/unattended/unattended.sh<TAB>9<TAB>1<TAB>cannot stage the run-state file...` — no row names tools/memory-tree/check-memory-hygiene.sh. The relevant pin is `.memory-tree.conf:269 ARMS_FLOORS="... tools/memory-tree/check-memory-hygiene.sh:20:20 ..."`, and tools/memory-tree/check-arms.py:207 documents it as "`ARMS_FLOORS="<gate>:<branches>:<armed> …"` — per gate, both one-sided upward". Spec 4 S3 adds check-12 validation branches to check-memory-hygiene.sh, so the floors are the artefact at risk, and only downward (a removed branch), which spec 4 §7 already words correctly ("check-arms.py floors if check-12 branches move") — §4 is the half left wrong.

**How the skeptic settled it.** The load-bearing half reproduces and the evidence detail does not. memory/project/unarmed-branches.txt carries THREE data rows, not one — all three name tools/unattended/unattended.sh (checks 9, 27, 29) — and none names tools/memory-tree/check-memory-hygiene.sh. The pin that would move is .memory-tree.conf:269 `ARMS_FLOORS="... tools/memory-tree/check-memory-hygiene.sh:20:20 ..."`, and check-arms.py compares one-sidedly upward, so only a REMOVED fail branch reds. Spec 4 §4 Files touched still reads 'memory/project/unarmed-branches.txt if branch ordinals shift', so round 1's GATE-9 fix did not land. The aggravating fact the finding is right about is the rev-3 log: it says 'folded the factual corrections from round 1's LOW tier' while folding only ROWCOST-17 — GATE-9's swap is absent, and so is SEQ-2's ('add every tracked memory/builds/*/README.md to §4', still missing). LOW as claimed: one wrong path in a section the spec itself labels an estimate, self-announcing on the first bar.

**Fix.** Swap `memory/project/unarmed-branches.txt` for `.memory-tree.conf`'s `ARMS_FLOORS` in spec 4 §4, note that the floors are one-sided upward so only a REMOVED check-12 fail branch forces an edit, narrow §7's line to match, add `every tracked memory/builds/*/README.md` to §4 per SEQ-2, and correct the rev-3 log to name what it actually folded.

### R2-11 — 2026-08-24-spec-dFramedEntrypoint-5.md · §3 Non-goals (4th bullet) and §4 Files touched, against the staged memory/project/curation-debt.txt

**Status:** CONFIRMED · raised MEDIUM, final LOW · kind ROUND1_MISSED

**Finding.** An unknown FIRST argument exits 2 with a usage line; what main() silently discards is argv[2] and beyond. The S-item stands and only its stated justification is wrong.

**Evidence.** `git diff --cached memory/project/curation-debt.txt` adds the row `memory/builds/dFramedEntrypoint/README.md` with the header comment "DRAINS AT TOOL-dFramedEntrypoint-5, which deletes the Records table outright - this row is a dated debt with a named discharge, not an open-ended waiver." spec-5 §3: "No drain of curation-debt rows as a scope item. Rows drain or they do not; the measurement is recorded as an acceptance observation rather than promised as a deliverable." spec-5 §4 Files touched: "`memory/project/curation-debt.txt` if rows drain". The only guard is check-memory-hygiene.sh:1084-1085 — `fail 6 "curation-debt.txt lists paths that no longer exist (stale-line guard)"` — which grades path existence, never whether the file now complies.

**How the skeptic settled it.** Reproduced by running the tool, not by reading it. `python tools/memory-tree/gen_build_index.py --survey` prints 'usage: gen_build_index.py [--check|--write|--check-format|--print-bindings|--selftest]' and exits 2. Source: gen_build_index.py:1661-1666 — `mode = argv[1] if len(argv) > 1 else "--check"` then `if mode not in (...): print("usage: ..."); return 2`; argv[2:] is never read. So spec-1:44-46 S7's second clause ('because `main()` silently ignores an unknown argument, so an acceptance criterion naming a flag that does not exist would pass by printing the ordinary clean line') is false for the first argument position, and the same false claim is carried into spec-1:207-209's rev-2 revision log. Under the true behaviour, an AC8 naming --survey before the verb exists would have EXITED 2 loudly, not passed quietly. Severity re-graded from MEDIUM to LOW: the S-item stands on the independent house rule (AGENTS.md §7, 'Run a candidate gate predicate over the real tree before wiring it'), §4 Files touched and AC8 are unaffected, and no deliverable changes — this is a rationale clause that mis-describes a mechanism the S-item next to it gets right, the class round 1 graded LOW repeatedly.

**Fix.** Correct S7's second clause to name argv[2] as the silently-discarded position, or drop the clause and rest S7 on the run-the-predicate-over-the-real-tree rule alone. Correct the same sentence in the rev-2 revision log entry.

### R2-JOINS-5 — 2026-08-24-spec-dFramedEntrypoint-5.md · §2 S4 (the "THREE classes, not one" enumeration) against §2 S3 and AC5

**Status:** CORRECTED · raised MEDIUM, final LOW · kind FOLD_BROKE

**Finding.** S4's 'THREE classes' enumeration omits the two arms at gen_build_index.py:1633 and :1645, whose LABELS assert the negation of AC5 once S3's joins render unconditionally. The arms do not red — S3's pinned zero-gap phrasing shares no prefix with `Ids no record names:` — so this is a stale-label class, not a fourth breakage class.

**Evidence.** tools/memory-tree/gen_build_index.py:1633-1635 `arm("a fully-covered build renders no coverage line", "True", lambda: str("Ids no record names:" not in plan(t13, conf13)[0][...]))`; :1645-1647 `arm("...and does NOT claim the id is unnamed, which a journal did name", "True", ... "Ids no record names:" not in ...)`; :1650-1652 `arm("a build with NO records renders neither the table nor a coverage line", ...)`. Spec 5 S3 now requires both joins to "render UNCONDITIONALLY wherever the region renders" and AC5 requires the zero-gap phrasing for the full-coverage case AND the no-records case — the exact two fixtures those arms use (t13 full coverage, t15 no records). Depending on the phrasing chosen, each arm either REDS (if the zero-gap line reuses the `Ids no record names:` prefix) or keeps passing while its LABEL asserts the negation of AC5 — the "arm reporting ok on a path it never took" class the charter §7 names. Spec 5 §5 testing mentions only the index→name move plus one new joins arm.

**How the skeptic settled it.** The arms are real and two of them are genuinely outside S4's enumeration, but the breakage described does not occur. gen_build_index.py:1633-1635 ('a fully-covered build renders no coverage line') and :1645-1647 ('...and does NOT claim the id is unnamed, which a journal did name') both assert `"Ids no record names:" not in <output>`; :1650-1652 ('a build with NO records renders neither the table nor a coverage line') asserts only `"| Record | Kind | Serves |" not in <output>`, which is class (b) and which round 1 already settled at UNB-2 ('it passes and contradicts nothing — only its LABEL goes stale'). The first disjunct of the claim is foreclosed by the fold itself: S3 pins the zero-gap phrasing as 'every unit id is named by a record, and every unit id has a spec-audit record', which shares no prefix with 'Ids no record names:', so none of the three arms reds. What actually happens is that :1633's and :1645's LABELS become false — a fully-covered build DOES render a coverage line under AC5 — and neither is in S4's three enumerated classes, which S4 claims are exhaustive. That is a naming defect in a passing arm, not a red suite. LOW, consistent with round 1's own handling of :1650.

**Fix.** Add the label class to S4: the arms at gen_build_index.py:1633, :1645 and :1650 keep passing and must be re-labelled to what they now assert, and add an arm asserting the zero-gap line IS present for t13 and t15. Keep S3's pinned wording explicit so the `Ids no record names:` prefix is unambiguously retired from the zero-gap case.

### R2-COUNTS-13 — 2026-08-24-spec-dFramedEntrypoint-5.md and -6.md · spec-5 AC1 ("ten tracked non-README files carry the string"); spec-6 §2 S4 ("789 id-to-record edges over 248 ids")

**Status:** CORRECTED · raised LOW, final LOW · kind FOLD_WRONG

**Finding.** Spec 5 AC1's 'ten' is wrong against its own probe (seven full-marker carriers, eleven bare-token). Spec 6 S4's figures are stale (804 edges over 256 ids, 17 cross-build, over 221 records), but its rev-2 log is accurate as written — the 248 figure was dropped from §4 Migration where it stood in for a spec count, and S4's use of it as an id count is the legitimate one.

**Evidence.** Spec 5 AC1's probe is `grep -lF -- '<!-- gen:build-docs -->'`; measured over tracked non-README files that returns SEVEN (memory/builds/aRuledFrontispiece/spec/2026-08-16-spec-TOOL-aRuledFrontispiece-{1,4}.md, three dFramedEntrypoint records, spec-5 itself, tools/memory-tree/gen_build_index.py). The count TEN comes from the bare token `gen:build-docs`, which measures ELEVEN today — so the sentence is right in kind and wrong in number for the pattern the AC uses. Spec 6 S4: "Measured: 789 id-to-record edges over 248 ids, 17 of them crossing a build boundary"; re-measured from `python tools/memory-tree/gen_build_index.py --print-bindings` over 221 records: 804 edges, 256 distinct ids, 17 cross-build edges. Spec 6's rev-2 log states "The 248 figure is dropped: it was an id count standing in for a spec count" — it was dropped from §4 Migration only.

**How the skeptic settled it.** Both measurements reproduce; one of the two readings of the specs does not. Spec 5 AC1: its probe is `grep -lF -- '<!-- gen:build-docs -->'`, and over tracked non-README files that returns SEVEN (two aRuledFrontispiece specs, three dFramedEntrypoint records, spec 5 itself, gen_build_index.py); the bare token `gen:build-docs` returns ELEVEN. Either way 'ten' is wrong, and it is wrong for the pattern the AC itself uses — confirmed. Spec 6 S4: re-measured from `--print-bindings` over 221 records I get 804 edges, 256 distinct ids and 17 cross-build edges, so '789 id-to-record edges over 248 ids' is stale by 15 and 8 respectively (the build's own records are what moved it). But the rev-2 log is not contradicting itself: it says the 248 figure was dropped because 'it was an id count standing in for a spec count', and §4 Migration confirms that is exactly where it was dropped from ('smaller than the 248 figure an earlier draft used'). S4 uses it as an id count, which is its legitimate use. LOW: AC8 already requires the figures re-derived from `--print-bindings` at implementation time, so nothing is built wrong.

**Fix.** Restate spec 5 AC1's justification against the pattern the probe actually uses — seven non-README carriers of the full marker, eleven of the bare token — or drop the number and keep the reason. In spec 6 S4, mark the figures as measured-at-drafting and point at AC8, which already requires the command.

### R2-09 — 2026-08-24-spec-dFramedEntrypoint-7.md · §4 Inventory (line 59), against §2 S6b and §6 AC8

**Status:** CONFIRMED · raised MEDIUM, final LOW · kind FOLD_BROKE

**Finding.** §4 Inventory's split is stale by exactly this build's own README (13 live / 49 terminal over 62, not 12 / 49 over 61), and the two derived figures move with it. The ordering half of the proposed fix is already carried by S6b itself.

**Evidence.** spec-7:59 'The corpus splits 12 live against 49 terminal. The live half carries roughly 52.6 KB of authored prose, 15% of the total, and 8 of the 12 are already under 5 KB.' against spec-7 S6b 'this build's OWN README is conformed by this unit and joins the bound set here'. Measured now: `git ls-files 'memory/builds/*/README.md' | wc -l` = 62; a sweep of the `**Build status:**` line over those 62 yields 49 CLOSED, 7 SPECCED, 3 INPROGRESS, 2 OPEN, 1 DEFERRED — 13 non-terminal. Round 1 SELF-1 proposed fix: 'Name this build's own README explicitly in unit 7's population and sequence it first … re-state the Inventory split against the corpus that will exist when the pass runs.'

**How the skeptic settled it.** The arithmetic reproduces. `git ls-files 'memory/builds/*/README.md' | wc -l` = 62. Sweeping the '**Build status:**' line over those 62 gives 49 CLOSED, 7 SPECCED, 3 INPROGRESS, 2 OPEN, 1 DEFERRED, and gen_build_index.py:97 sets TERMINAL = ('CLOSED', 'WONTDO'), so DEFERRED is live and the split is 13 live / 49 terminal over 62. spec-7:59 still says '12 live against 49 terminal', which sums to 61 and is exactly the corpus minus this build's own README — the file spec-7:29-31 S6b now brings into the bound set (memory/builds/dFramedEntrypoint/README.md, 7,475 B, '**Build status:** SPECCED'). The two dependent figures ('52.6 KB of authored prose', '8 of the 12 are already under 5 KB') are taken over the stale 12. So the fold's S6b addition did leave §4 Inventory contradicting it. Severity re-graded down from MEDIUM: S1 requires the population to be DERIVED by a script that reports per file ('measured by a script that reports, per file, which canonical slots it can already fill'), so no implementer reads the plan off §4's numbers, and S6b already states the AC8 dependency the finding's fix asks for ('this build's OWN README ... is what creates the parked-decisions slot that AC8 parks into'). What is left is a stale measurement paragraph, no unanswerable criterion and no changed deliverable.

**Fix.** Re-state §4 Inventory as 13 live against 49 terminal over 62 tracked READMEs and re-measure the authored-byte and under-5-KB figures with memory/builds/dFramedEntrypoint/README.md included.

### R2-10 — 2026-08-24-spec-dFramedEntrypoint-7.md · §2 S6b against §4 Inventory

**Status:** CORRECTED · raised MEDIUM, final LOW · kind FOLD_BROKE

**Finding.** spec-1 §4 Inventory's closing clause 'and unit 3 supplies only data' is false at HEAD — unit 3 also edits gen_build_index.py, check-memory-hygiene.sh and adopt-memory-tree.sh. The contradiction round 1 raised is resolved, in spec 3, by S5's explicit replacement statement.

**Evidence.** `git ls-files memory/builds/ | grep -c README.md` → 62. `grep -h '^\*\*Build status:\*\*' memory/builds/*/README.md | sed …| sort | uniq -c` → 49 CLOSED, 1 DEFERRED, 3 INPROGRESS, 2 OPEN, 7 SPECCED, i.e. 13 non-terminal (TERMINAL is `("CLOSED","WONTDO")` at gen_build_index.py:96). `wc -c memory/builds/dFramedEntrypoint/README.md` → 7475. Spec 7 §4 Inventory: "The corpus splits 12 live against 49 terminal. The live half carries roughly 52.6 KB of authored prose … 8 of the 12 are already under 5 KB."

**How the skeptic settled it.** The trailing clause reproduces and is now false: spec-1:96-98 §4 Inventory ends 'so the predicate is complete before its population exists and unit 3 supplies only data', while spec-3 at HEAD has unit 3 shipping code in three places — S5 (spec-3:25-29) inverts unit 1's absent-file behaviour and its selftest arm, S6b (:31-35) edits check-memory-hygiene.sh check 3 plus its arm and HYGIENE.template.md prose, S6c (:36-37) edits adopt-memory-tree.sh. But the finding's framing that the round-1 fix went unserved is wrong: round 1's ask was that the replacement be STATED, and spec-3 S5 states it explicitly and by name ('this unit REPLACES unit 1's absent-file behaviour ... The corresponding unit-1 selftest arm is inverted in this unit rather than left contradicting AC5'), so the two specs no longer contradict — one of them now owns the transition. The second sub-claim is also wrong: spec-1 S8 (:47-48) says 'new --selftest arms covering every trigger, every refusal and the survey verb' and enumerates no absent-registry arm, and unit 3 INVERTS that arm rather than deleting it, so S8 claims nothing unit 3 removes. Residue is one stale five-word clause in a §4 Inventory paragraph.

**Fix.** Drop 'and unit 3 supplies only data' from spec-1:98 and add one clause: the empty-set-on-absent branch is provisional and unit 3 replaces it with a refusal, inverting this unit's arm.

### R2-8 — 2026-08-24-spec-dFramedEntrypoint-7.md · §4 Inventory and §8 F1, against §2 S6b (added rev-2)

**Status:** CONFIRMED · raised MEDIUM, final LOW · kind FOLD_BROKE

**Finding.** The fold added S6b putting this build's OWN README into unit 7's population, but left the three places that state the population size unchanged. §4 Inventory still reads "The corpus splits 12 live against 49 terminal" (=61, the corpus without this build's README) and "8 of the 12 are already under 5 KB"; F1 still asks whether "all 12 live builds" are in the seed. The spec now declares a population of 13 in S6b and 12 in the two sections an implementer sizes the work from.

**Evidence.** spec-7 S6b: "this build's OWN README is conformed by this unit and joins the bound set". spec-7 §4 Inventory: "The corpus splits 12 live against 49 terminal. The live half carries roughly 52.6 KB of authored prose ... 8 of the 12 are already under 5 KB." spec-7 F1: "are all 12 live builds in the seed". Measured: `git ls-files 'memory/builds/*/README.md' | wc -l` = 62; sweeping `**Build status:**` over them gives 13 non-terminal, the thirteenth being memory/builds/dFramedEntrypoint/README.md (SPECCED); `grep -c '^| \[' memory/LIVE.md` = 13. memory/builds/dFramedEntrypoint/README.md is 7,475 B, so the "8 of the 12 under 5 KB" sub-count does not carry over either.

**How the skeptic settled it.** The mechanism reproduces from source. tools/memory-tree/adopt-memory-tree.sh:37 accepts only `--scaffold` (`[ "${1:-}" = "--scaffold" ] || { echo "usage: $0 --scaffold"; exit 2; }`), and :47-53 exits 0 as a no-op whenever `$M/HYGIENE.md` carries the `gov:kit memory-tree@` marker — the marker match is version-agnostic, so no upgrade path exists for a tree already scaffolded by any earlier kit version. tools/memory-tree/README.md documents only "Adopt — new project (scaffold)" and a one-time "Adopt — existing tree (migrate)"; no re-converge verb is documented. So spec 3 S6c's scaffold reaches new adopters only, while AC5's hard refusal on an absent `memory/project/readme-contract.txt` reaches every adopter, and the `build README slot contract` leg that enforces it ships to all of them (tools/memory-tree/kit.toml:142-146, subject repo, guard []). Round 1's ADOPT-1 offered exactly two remedies and the fold took the scaffold one, so this is the residue of that choice rather than a re-raise. Re-graded MEDIUM→LOW: the damage is adopter-only, the general defect (the kit adds a file, the adopter script only scaffolds) predates this build and already affects two gov-only registries round 1 noted as unscaffolded, and the manual remedy for an upgrading adopter is creating one empty file.

**Fix.** Say in S6c which shape an UPGRADING adopter gets. Either make the scaffold step an idempotent converge that creates a missing `readme-contract.txt` on an already-scaffolded tree (which needs a verb the adopter does not have today, so it is its own scope item), or let an absent registry resolve forward to the legal present-but-empty state with a printed announced line, keeping the hard refusal for a malformed one.

### R2-POP-1 — 2026-08-24-spec-dFramedEntrypoint-7.md · §2 S6b (:29-31) against §4 Inventory (:59-61) and §8 F1 (:142-145)

**Status:** CONFIRMED · raised LOW, final LOW · kind FOLD_BROKE

**Finding.** rev-2 added this build's own README to the conformance population and left every population figure in the spec describing the corpus without it, so the unit's inventory, its byte estimate and its open fork all understate the work by one build.

**Evidence.** §4 Inventory: 'The corpus splits 12 live against 49 terminal. The live half carries roughly 52.6 KB of authored prose, 15% of the total, and 8 of the 12 are already under 5 KB.' §8 F1: 'are all 12 live builds in the seed'. 12 + 49 = 61, the census total that excludes this build's own README (the established census figure is 61 tracked, 62 including this build's). S6b, added at rev-2, reads 'this build's OWN README is conformed by this unit and joins the bound set' — making the live population 13. Measured now: `git ls-files 'memory/builds/*/README.md' | wc -l` = 62, and memory/builds/dFramedEntrypoint/README.md is tracked. Its authored half is also not in the 52.6 KB figure. The same off-by-one reaches spec-3 §8 F1's 'start at the full size' pin recommendation only indirectly, since that pin is measured rather than authored.

**How the skeptic settled it.** Verified. Spec-7 §4 Inventory (:59-61) reads '12 live against 49 terminal' — 61, the census total that excludes this build's own README — and §8 F1 (:142) asks 'are all 12 live builds in the seed'. S6b (:29-31), added at rev-2 per the revision log (:157), puts this build's own README into the bound set, making the live population 13. Measured now: `git ls-files 'memory/builds/*/README.md' | wc -l` = 62, and memory/builds/dFramedEntrypoint/README.md is tracked. The 52.6 KB authored figure likewise excludes it. A genuine fold-introduced off-by-one, correctly graded LOW: it misstates the size of the pass by one build and changes no design.

**Fix.** State explicitly that the 12/49 split is the pre-build census and S6b adds a thirteenth live build, or re-derive §4 Inventory with this build included; update F1 to match.

### R2-SELFTEST-LEG-1 — 2026-08-24-spec-dFramedEntrypoint-7.md · §7 Gates (:137-138)

**Status:** CONFIRMED · raised LOW, final LOW · kind FOLD_WRONG

**Finding.** Round 1's GATE-4 fix had three clauses; the fold landed two and dropped the third. Spec 7 still names a held kit self-test leg it contributes no arm to and cannot exercise with the invocation its own AC7 uses.

**Evidence.** Round 1 GATE-4's Fix reads verbatim: 'Add a direct `python tools/memory-tree/gen_build_index.py --selftest` AC to specs 2 and 4 (spec 7 adds no arms, so drop the leg from its §7)...'. Spec-2 gained AC11 and spec-4 gained AC9, both naming `GATE_FULL=1 GATE_SELFTESTS=1`. Spec-7 §7 still reads '`build README slot contract` · `memory hygiene` · `build-index selftest` · the full bar at the push boundary', while spec-7 §2 S1-S7 add no selftest arm and §3 states its conservation check 'is a build-time script and an acceptance observation, not a merge-bar leg'. tools/gate-legs.json carries {"name":"build-index selftest", "chunk":"selftests", "subject":"kit"}, and spec-7 AC7's `bash tools/run-gates/run-gates.sh` holds every kit-subject leg unless GATE_SELFTESTS=1 — so the leg spec 7 names is one its own acceptance run never executes.

**How the skeptic settled it.** Verified. Round-1 GATE-4's Fix reads verbatim (record line 425) 'Add a direct `python tools/memory-tree/gen_build_index.py --selftest` AC to specs 2 and 4 (spec 7 adds no arms, so drop the leg from its §7), or spell the DoD command as `GATE_FULL=1 GATE_SELFTESTS=1 …`'. The fold took the first branch for specs 2 and 4 — spec-2 AC11 (:159-162) and spec-4 AC9 (:135) both exist and spec-2 AC11 names GATE_FULL=1 GATE_SELFTESTS=1 — and did neither half for spec 7. Spec-7 §7 (:137-138) still reads '… `build-index selftest` · the full bar at the push boundary'; S1-S7 add no selftest arm and §3 confirms its conservation check is 'not a merge-bar leg'; AC7 (:130-131) names the bare `bash tools/run-gates/run-gates.sh`, and tools/gate-legs.json carries {'name':'build-index selftest','chunk':'selftests','subject':'kit'}, which run-gates holds unless GATE_SELFTESTS=1. Both halves of the claim reproduce. One note on the fix: dropping the leg is not the only remedy and is arguably the worse one — spec 7 does touch tools/memory-tree/ (§4 names build-readme-slot-limits.txt and a possible kit-resident script), so the leg's guard arms for this unit and it will run under a DoD invocation.

**Fix.** Spell spec-7 AC7's invocation as `GATE_FULL=1 GATE_SELFTESTS=1 bash tools/run-gates/run-gates.sh`, matching spec-2 AC11, rather than dropping the leg from §7.

### R2-15 — 2026-08-24-spec-dFramedEntrypoint-8.md · §2 S3 (the generator-comment half, added at rev-3), against §4 Files touched, §6 and §7

**Status:** CORRECTED · raised MEDIUM, final LOW · kind ROUND1_MISSED

**Finding.** The S3 generator-comment half ships with no acceptance criterion. The §4 Files-touched omission is an estimate gap rather than a defect, and the epoch exemption is correct as-is — a comment-only edit is filtered by check-verdict-epoch.sh's behav_in, so no bump is owed and no statement is required.

**Evidence.** spec-8:19-24 S3 '… `gen_build_index.py`'s own comment above the plan-pair constants carries a stale RATIONALE … It is corrected here'. spec-8 §4 Files touched: 'memory/DECISIONS.md · .memory-tree.conf for the S4b ceiling raise · memory/map/features/build-readme-surface.md · memory/project/curation-debt.txt · memory/backlog/TOOL.md.' spec-8 §7 Gates: 'memory hygiene · the codebase-map coverage and freshness legs … · the row-grammar merge driver over the backlog shard.' Source: tools/memory-tree/check-verdict-epoch.sh:95-98 behav_in pipes through `grep -vE '^[[:space:]]*(#|$)'`, so a comment-only change is not behaviour-bearing; tools/gate-legs.json guards 'build-index selftest' and 'kit/dogfood doc parity' on 'tools/memory-tree/'.

**How the skeptic settled it.** The premise reproduces: spec-8:22-26 S3 now carries a code-file edit ('`gen_build_index.py`'s own comment above the plan-pair constants carries a stale RATIONALE ... It is corrected here'), added at rev-3 per spec-8:149, and spec-8:84-86 §4 Files touched lists only DECISIONS.md, .memory-tree.conf, the dossier, curation-debt.txt and backlog/TOOL.md. AC3 (spec-8:113-115) covers the dossier only, and §7 (:129-130) omits both legs the edit arms. But two of the three sub-claims are weak. §4's own sub-head is 'Files touched (estimate)' per TEMPLATE-SPEC:127, so an omitted single-comment edit is an incomplete estimate, not a broken contract. The epoch half is a suggestion, not a defect: check-verdict-epoch.sh:95-98 behav_in filters `^[[:space:]]*(#|$)`, so a comment-only edit is genuinely non-behaviour-bearing and no bump is owed — the spec is CORRECT to carry no S-EPOCH block, and asking it to say so is preference. The §7 half costs nothing either: 'build-index selftest' is a held selftest leg and 'kit/dogfood doc parity' compares live copies against templates, so neither reds on a Python comment change. What survives is the absent acceptance criterion for the S3 code-comment half — real, and the same class the fold fixed elsewhere in this set.

**Fix.** Add an AC observing the corrected rationale at HEAD above gen_build_index.py's plan-pair constants, and add tools/memory-tree/gen_build_index.py to §4 Files touched with the comment-only scope.

### R2-17 — 2026-08-24-spec-dFramedEntrypoint-8.md · §2 S5 and §6 AC5

**Status:** CONFIRMED · raised LOW, final LOW · kind ROUND1_MISSED

**Finding.** S5 and AC5 say the fan-out guard's documentation defect lives in "two files" and require the backlog row to name "both files that state it". Three tracked files carry the sentence. A row naming two of three leaves the third to be rediscovered, which is the propagating-error class this whole unit exists to close.

**Evidence.** `grep -rn 'array LITERAL' AGENTS.md tools/hooks/README.md coding-governance-agents.template.md` returns three hits: AGENTS.md:308, tools/hooks/README.md:31 ("An array LITERAL of ≤5 elements — the finder-lens fan — passes unmarked and needs no helper."), coding-governance-agents.template.md:240. AGENTS.md is the rendered dogfood of the template, so the repair is template-first plus a re-render, plus the hooks README — three paths, not two.

**How the skeptic settled it.** Reproduced. `git grep -n 'array LITERAL'` over tracked non-archive, non-build-record files returns exactly three live carriers of the mis-positioned exemption: AGENTS.md:308, coding-governance-agents.template.md:240, tools/hooks/README.md:31 ('An array LITERAL of <=5 elements — the finder-lens fan — passes unmarked and needs no helper.', sitting after the bullet list whose first bullet is 'A raw `parallel(` / `pipeline(` primitive'). memory/guides/REVIEW-PROTOCOL.md:115 and its template also state the exemption but scope it correctly to the receiver ('An `agent(` call reached through an iteration construct is allowed only when its receiver is:'), so they are NOT carriers of the defect — the finding is right to exclude them. The template/AGENTS pair being a render pair also checks out: tools/gate-legs.json carries an unguarded 'playbook render wiring' leg (`adopt-playbook.sh --target . --check`), and tools/check-playbook-parity.sh:113 pins the template's number to `MAX_LENSES`; no gate binds tools/hooks/README.md to either. Spec 8 S5 says 'two files' and AC5 requires the row to name 'both files that state it', so a conforming row can legitimately omit the kit README. Kept at LOW, not raised: the deliverable is one backlog row, spec 8 §3 makes the repair itself an explicit non-goal, and the residue is a count pinned in prose where a derivation is available.

**Fix.** Reword S5 and AC5 to name the carriers as DERIVED (`git grep -l 'array LITERAL'` over tracked paths outside memory/archive and memory/builds) rather than as a count, and note in the row that two of the three are a template/render pair gated by `playbook render wiring` while tools/hooks/README.md is gated by nothing, which is why it is the one that gets dropped.

### R2-HOOKDOC-10 — 2026-08-24-spec-dFramedEntrypoint-8.md · §2 S5 and §6 AC5

**Status:** CORRECTED · raised MEDIUM, final LOW · kind ROUND1_MISSED

**Finding.** Three files carry it and the count in S5/AC5 is wrong, but the severity is LOW: the deliverable is a backlog row whose repair spec 8 §3 explicitly parks, and the fix should DERIVE the carrier list rather than pin it at three.

**Evidence.** `git grep -n "array LITERAL"` over tracked non-archive files returns AGENTS.md:308, coding-governance-agents.template.md:240, and tools/hooks/README.md:31 ("An array LITERAL of ≤5 elements — the finder-lens fan — passes unmarked and needs no helper.", sitting directly under the bullet "A raw `parallel(` / `pipeline(` primitive"). The substantive claim verifies: tools/hooks/agent-cap.js:102-112 lists the exemption under "ALLOWED receivers ... (b) an identifier assigned from an ARRAY LITERAL with <= MAX_LENSES elements", and the raw-primitive deny is separate (agent-cap.js:17 "Any other raw primitive call = deny"). Spec 8 S5: "two files state that an array literal of five or fewer elements passes the guard unmarked". Spec 8 AC5: "it carries a row describing the fan-out guard's documentation defect, naming both files that state it." Note also that tools/check-playbook-parity.sh:113 machine-compares the template's `array LITERAL of ≤N elements` value against `MAX_LENSES`, so the charter pair moves together and the hooks README does not — which is exactly why it needs naming.

**How the skeptic settled it.** Substantively the same finding as R2-17 and correct on the count: three tracked files carry the mis-positioned exemption (AGENTS.md:308, coding-governance-agents.template.md:240, tools/hooks/README.md:31), and the parity citation checks out — tools/check-playbook-parity.sh:113 machine-compares the template's `array LITERAL of <=N elements` value against `MAX_LENSES` in tools/hooks/agent-cap.js, while nothing binds the hooks README. The substantive half also verifies: agent-cap.js:102-112 lists the array-literal exemption under ALLOWED RECEIVERS and the raw-primitive deny is separate. Two things bring it down from MEDIUM. The repair is an explicit non-goal of spec 8 §3 ('No fix for the fan-out guard's documentation. S5 records it; the repair belongs to whoever owns that kit's docs'), so the whole deliverable is one backlog row's completeness. And the proposed fix re-pins a count — 'Change S5 to three files' — which is the same defect one number over; R2-17's derived formulation is the correct repair.

**Fix.** Take R2-17's fix: derive the carriers in S5/AC5 (`git grep -l 'array LITERAL'` over tracked paths outside memory/archive and memory/builds), and note in the row that the charter pair is parity-pinned to `MAX_LENSES` by tools/check-playbook-parity.sh:113 while the kit README is pinned by nothing.

### R2-10 — CROSS — owner rulings "What was NOT ruled, and is parked" against all eight specs and memory/builds/dFramedEntrypoint/README.md · spec-1 §3 (5th bullet), spec-7 §2 S6b / §6 AC8, spec-8 §3 (2nd bullet); the build README's authored half

**Status:** CORRECTED · raised MEDIUM, final LOW · kind ROUND1_MISSED

**Finding.** spec-1 §4 Inventory's closing clause 'and unit 3 supplies only data' is false at HEAD — unit 3 also edits gen_build_index.py, check-memory-hygiene.sh and adopt-memory-tree.sh. The contradiction round 1 raised is resolved, in spec 3, by S5's explicit replacement statement.

**Evidence.** Owner rulings, final section: "Whether to make the pair mandatory and turn the term into a real check, or to delete the reader, the report and the term together, is an owner decision this build does not take. It is carried as this build's park." spec-1 §3: "No change to the authored roster pair or to any reader of it. That is this build's park." spec-8 §3 and spec-7 §4 repeat the reference. spec-7 AC8: "When a block cannot be routed, the pass parks it in this build's parked-decisions slot ... with the question and the reason" — scoped to unroutable blocks. `grep -n -i 'park|roster' memory/builds/dFramedEntrypoint/README.md` returns only the front-matter `roster: TOOL` key, one prose mention of "hand-authored rosters", and unit 4's generated row title. No park content exists.

**How the skeptic settled it.** The trailing clause reproduces and is now false: spec-1:96-98 §4 Inventory ends 'so the predicate is complete before its population exists and unit 3 supplies only data', while spec-3 at HEAD has unit 3 shipping code in three places — S5 (spec-3:25-29) inverts unit 1's absent-file behaviour and its selftest arm, S6b (:31-35) edits check-memory-hygiene.sh check 3 plus its arm and HYGIENE.template.md prose, S6c (:36-37) edits adopt-memory-tree.sh. But the finding's framing that the round-1 fix went unserved is wrong: round 1's ask was that the replacement be STATED, and spec-3 S5 states it explicitly and by name ('this unit REPLACES unit 1's absent-file behaviour ... The corresponding unit-1 selftest arm is inverted in this unit rather than left contradicting AC5'), so the two specs no longer contradict — one of them now owns the transition. The second sub-claim is also wrong: spec-1 S8 (:47-48) says 'new --selftest arms covering every trigger, every refusal and the survey verb' and enumerates no absent-registry arm, and unit 3 INVERTS that arm rather than deleting it, so S8 claims nothing unit 3 removes. Residue is one stale five-word clause in a §4 Inventory paragraph.

**Fix.** Drop 'and unit 3 supplies only data' from spec-1:98 and add one clause: the empty-set-on-absent branch is provisional and unit 3 replaces it with a refusal, inverting this unit's arm.

## Refuted and dropped

- **R2-08** (2026-08-24-spec-dFramedEntrypoint-6.md · §2 S6b, §4 Migration and §8 F3) — REFUTED/DROP. The spec parks this in §8, which under the grading rule is the spec doing its job. spec-6:151-155 F3 is a stated fork with a recommendation ('one commit, verified by re-running the verb ... and the incremental language deleted'), and S6b names it explicitly as the deciding fork. The two claimed contradictions do not hold on the source. S6 (spec-6:30-32) says '`--write` creates the pair where it is missing and `--check` never demands one ... so this region ships without demanding a corpus-wide render' — that is a statement about what the CHECK demands, and it stays true under the one-commit branch, where the corpus-wide write is a chosen migration step rather than a demand. §4 Rollout's 'a partial corpus is a legal state at every point' (spec-6:82-83) is true for the same reason and is confirmed by AC6, which pins the create-on-write asymmetry. On the missing-AC half: under the recommended branch no new argument ships, so AC1-AC8 already cover what lands; an AC is owed only if the owner picks scoping, which is a conditional, not a present defect. The argv facts cited are correct (gen_build_index.py:1661 reads argv[1] only) but they support F3's recommendation rather than a finding. Every other spec in this set also carries unresolved §8 forks with recommendations, so this is the house shape, not spec 6's defect.
- **R2-MAP-4** (2026-08-24-spec-dFramedEntrypoint-{1,2}.md (also 3, 4, 6 conditionally) · spec-1 §4 Files touched and §7 Gates; spec-2 §4 Files touched and §7 Gates) — REFUTED/DROP. This is a verbatim re-raise of round 1's MAP-1 (CORRECTED, raised MEDIUM, final LOW), on the same evidence and with nothing new. Every citation reproduces — tools/gate-legs.json carries 'codebase-map coverage + freshness' with no guard, test_codebase_map.py byte-compares memory/map/generated/symbols.json against a live re-render, and specs 1 and 2 name neither the artifacts nor the leg — but round 1 already established all of that and the fold triaged it deliberately, folding only the LOW's factual half (spec 1 §4 now says 'the new module-level functions need a dossier prose refresh rather than a new claim', which is correct: Python symbols are not claimable inventory keys). The severity argument does not survive: the REG-1 analogy fails because REG-1 required new code in a second script (check 3's allowlist, its arm, and the HYGIENE prose) while this needs one regen command already in the tree plus two lines of §4/§7 text. The bar catches it loudly on the first run and nothing lands wrong. The residue stands as recorded — round-1 MAP-1 at LOW — and does not become HIGH by restatement.
- **R2-5** (CROSS — owner ruling 4 against all eight specs · spec-1 §3 Non-goals (4th bullet) and §5 user docs; no §2 S-item and no §6 AC anywhere in the set) — REFUTED/DROP. This re-raises round-1 SEQ-4, which was refuted and recorded in the round-1 record's "Refuted and dropped" section under the explicit heading "Recorded so a later round does not re-raise them" — and the refutation still holds on the sources. AGENTS.md §7 mandates the staged-break discipline for every new gate ("Stage the break, confirm RED, unstage"), and nothing in AC3 forbids exercising the advisory that way: temporarily lowering one high-water row, or adding bytes to one bound slot, puts a file over its recorded high-water and under its ceiling, which is precisely AC3's precondition. Spec 2 S8 independently requires a selftest arm for that exact branch ("over-high-water-under-ceiling"), and AC2 grades the advisory's four values directly, so the mechanism is not shipped unexercised. The finding's own proposed fix — "stage a slot body over its recorded high-water in a scratch tree" — is the very demonstration round 1 already ruled available, so it asks for a wording change to an AC whose named observation is already performable. The vacuity claim is also weaker than stated: unit 2's landing green is declared VACUOUS by AC8 by design, and unit 7 S5/AC5 seed the ceilings, not the high-waters, from the measured population.
- **R2-6** (2026-08-24-spec-dFramedEntrypoint-5.md · §2 S3, §5 error/empty/loading states, §6 AC5) — REFUTED/DROP. The finding's load-bearing premise is false. Round 1's MAP-1 is not a MEDIUM: its own header reads "**Status:** CORRECTED · raised at MEDIUM, final LOW" and it is filed under "## LOW — 34" at line 813 of the round-1 record. It therefore belongs to the triaged LOW set the fold deliberately did not act on, and this re-raise carries no new severity argument. The mechanism is real and already on record at LOW — I re-verified it independently (memory/map/generated/symbols.json carries 35 gen_build_index.py entries including slot_violations and render_docs; tools/codebase-map/test_codebase_map.py byte-compares the committed artifact against a live render on an unguarded repo-subject leg) — and round 1 recorded the identical fix, including the units-3-and-4 conditionality this finding repeats almost verbatim. Adding unit 6 to the list is a one-unit extension of the same LOW class, not a promotion: spec 6 §7 already names the map-freshness leg, and AGENTS.md §1's Definition of Done already binds every commit to regenerate generated artifacts, so the omission costs a loud, immediately-remedied red rather than a wrong verdict.
- **R2-7** (2026-08-24-spec-dFramedEntrypoint-2.md · §6 AC3, against S7 (rev-2) and AC8) — REFUTED/DROP. Refuted on two independent grounds. First, this re-raises round-1 RECALL-1, whose header reads "**Status:** CONFIRMED · raised at MEDIUM, final LOW" and which sits under "## LOW — 34" at line 825 — a deliberately triaged LOW, not one of the 30 MEDIUMs. Second, the substance is wrong against the cited source: memory/guides/BUILD-METHOD.md M5 says verbatim "The obligation is `memory/TEMPLATE-SPEC.md` §10 and is machine-checked there. Satisfy it once for the SET, not per spec", so the terms recorded in spec 1 §10 discharge the obligation for the whole set, and the fold did exactly that. memory/TEMPLATE-SPEC.md §10 itself demands only the reuse-discovery result — "the existing seam this unit wires through ... or an explicit 'no existing seam fits' with the evidence" — and says nothing about recall terms, so nothing machine-checked is missing from specs 2-8; I confirmed all eight carry a seam or an explicit no-seam finding. M7 step 5 remains performable: a regrounding session reads the set's recorded terms, which exist. Repeating the line in seven more §10 sections is a convenience preference, which is what round 1's skeptic said when it graded this LOW.
- **R2-5** (2026-08-24-spec-dFramedEntrypoint-2.md · §6 AC3 against §2 S5, §6 AC8, and spec 7 §2 S5 / §6 AC5) — REFUTED/DROP. This re-raises round-1 SEQ-4, which was refuted and recorded in the round-1 record's "Refuted and dropped" section under the explicit heading "Recorded so a later round does not re-raise them" — and the refutation still holds on the sources. AGENTS.md §7 mandates the staged-break discipline for every new gate ("Stage the break, confirm RED, unstage"), and nothing in AC3 forbids exercising the advisory that way: temporarily lowering one high-water row, or adding bytes to one bound slot, puts a file over its recorded high-water and under its ceiling, which is precisely AC3's precondition. Spec 2 S8 independently requires a selftest arm for that exact branch ("over-high-water-under-ceiling"), and AC2 grades the advisory's four values directly, so the mechanism is not shipped unexercised. The finding's own proposed fix — "stage a slot body over its recorded high-water in a scratch tree" — is the very demonstration round 1 already ruled available, so it asks for a wording change to an AC whose named observation is already performable. The vacuity claim is also weaker than stated: unit 2's landing green is declared VACUOUS by AC8 by design, and unit 7 S5/AC5 seed the ceilings, not the high-waters, from the measured population.
- **R2-6** (2026-08-24-spec-dFramedEntrypoint-1.md, -2, -3, -4, -6 · §4 Files touched and §7 Gates of each) — REFUTED/DROP. The finding's load-bearing premise is false. Round 1's MAP-1 is not a MEDIUM: its own header reads "**Status:** CORRECTED · raised at MEDIUM, final LOW" and it is filed under "## LOW — 34" at line 813 of the round-1 record. It therefore belongs to the triaged LOW set the fold deliberately did not act on, and this re-raise carries no new severity argument. The mechanism is real and already on record at LOW — I re-verified it independently (memory/map/generated/symbols.json carries 35 gen_build_index.py entries including slot_violations and render_docs; tools/codebase-map/test_codebase_map.py byte-compares the committed artifact against a live render on an unguarded repo-subject leg) — and round 1 recorded the identical fix, including the units-3-and-4 conditionality this finding repeats almost verbatim. Adding unit 6 to the list is a one-unit extension of the same LOW class, not a promotion: spec 6 §7 already names the map-freshness leg, and AGENTS.md §1's Definition of Done already binds every commit to regenerate generated artifacts, so the omission costs a loud, immediately-remedied red rather than a wrong verdict.
- **R2-7** (2026-08-24-spec-dFramedEntrypoint-2.md, -3, -4, -5, -6, -7, -8 · §10 Reuse audit of each) — REFUTED/DROP. Refuted on two independent grounds. First, this re-raises round-1 RECALL-1, whose header reads "**Status:** CONFIRMED · raised at MEDIUM, final LOW" and which sits under "## LOW — 34" at line 825 — a deliberately triaged LOW, not one of the 30 MEDIUMs. Second, the substance is wrong against the cited source: memory/guides/BUILD-METHOD.md M5 says verbatim "The obligation is `memory/TEMPLATE-SPEC.md` §10 and is machine-checked there. Satisfy it once for the SET, not per spec", so the terms recorded in spec 1 §10 discharge the obligation for the whole set, and the fold did exactly that. memory/TEMPLATE-SPEC.md §10 itself demands only the reuse-discovery result — "the existing seam this unit wires through ... or an explicit 'no existing seam fits' with the evidence" — and says nothing about recall terms, so nothing machine-checked is missing from specs 2-8; I confirmed all eight carry a seam or an explicit no-seam finding. M7 step 5 remains performable: a regrounding session reads the set's recorded terms, which exist. Repeating the line in seven more §10 sections is a convenience preference, which is what round 1's skeptic said when it graded this LOW.