**Serves:** spec-audit TOOL-dFramedEntrypoint-1..8

# Spec audit round 1 — the eight dFramedEntrypoint specs

*Node d, 2026-08-24, base 9ddcc5c9. Five primed finder lenses over the whole spec set, then five batched skeptics each prompted to REFUTE and to default to refuted under uncertainty. Concurrency and verifier total both at the charter's cap.*

## Verdict: BLOCKED

82 raw findings. 72 survived verification, 9 refuted or dropped, 1 returned no verdict and is carried below as UNVERIFIED. Precision 0.88.

Surviving by final severity: 1 BLOCKER, 7 HIGH, 30 MEDIUM, 34 LOW. 32 of the survivors were CORRECTED by a skeptic, meaning something real was there but the finding as raised was wrong. The fold acts on the correction, never on the original claim.

| Spec | Surviving findings |
|---|---|
| CROSS | 22 |
| 2026-08-24-spec-dFramedEntrypoint-1.md | 13 |
| 2026-08-24-spec-dFramedEntrypoint-5.md | 11 |
| 2026-08-24-spec-dFramedEntrypoint-2.md | 6 |
| 2026-08-24-spec-dFramedEntrypoint-3.md | 5 |
| 2026-08-24-spec-dFramedEntrypoint-4.md | 5 |
| 2026-08-24-spec-dFramedEntrypoint-6.md | 4 |
| 2026-08-24-spec-dFramedEntrypoint-8.md | 3 |
| 2026-08-24-spec-dFramedEntrypoint-7.md | 3 |

## BLOCKER — 1

### SEQ-1 — CROSS · spec-5 S6 vs spec-6 S1/S3/S4 and §7 Gates

**Status:** CONFIRMED · raised at BLOCKER, final BLOCKER

**Finding.** The kit-version bump is scoped to unit 5 alone, but units 1, 2, 3, 4 and 6 all move `gen_build_index.py`, which is inside the verdict-epoch gate's scan set. Unit 6 lands AFTER unit 5's bump and moves the engine again, so the gate's topological rule is violated no matter how the earlier units are batched.

**Evidence.** tools/memory-tree/check-verdict-epoch.sh:69 — `DELEGATES="… tools/memory-tree/gen_build_index.py …"`; the rule at :16-19 — "Let W be the NEWEST commit in `<base>..HEAD` that moves a behaviour-bearing line of the engine, and S the NEWEST commit … that actually CHANGES the value of `KIT_MEMORY_TREE_VERSION`. Then W must be an ancestor of, or equal to, S". The leg `verdict epoch (kit version dates the engine)` in tools/gate-legs.json carries NO guard, so it runs on every bar. Only spec 5 S6 ("the memory-tree kit version moves at every gated site") bumps; specs 1, 2, 3, 4 and 6 have no bump S-item and none names this leg in §7. Unit 6's S1/S3/S4 (a new region, its renderer, its arms) are behaviour-bearing lines of that same file, landed two waves after unit 5's bump. The gate over-counts by design (:38-40: "A rename or a whitespace-only refactor changes no verdict and still demands a bump"), so there is no exemption to reach for.

**How the skeptic settled it.** Reproduced end to end. tools/memory-tree/check-verdict-epoch.sh:69 lists tools/memory-tree/gen_build_index.py in DELEGATES; the rule at :16-19 requires W (newest behaviour-bearing engine commit in <base>..HEAD) to be an ancestor of or equal to S (newest commit changing KIT_MEMORY_TREE_VERSION); and I read the decision path — when W exists and S is empty the script prints 'FAILED — $moved behaviour-bearing line(s) of the engine moved in $W, and NO commit in BASE..HEAD changes KIT_MEMORY_TREE_VERSION'. The leg's manifest row carries no `guard`, so it runs on every bar. `grep -n 'check-verdict-epoch' memory/builds/dFramedEntrypoint/spec/*.md` returns a single hit, spec-5:134; specs 1, 2, 3, 4 and 6 all edit gen_build_index.py per their §4 Files touched and none carries a bump S-item or names this leg. The gate over-counts by design (:38-40), so no exemption is reachable. Both batchings fail: landed unit-by-unit, each of units 1-4 and 6 pushes a range with W and no S; landed as one cumulative range, W is unit 6's engine change and S is unit 5's earlier bump, so W is not an ancestor of S. Kept at BLOCKER — this is not one AC in one spec, it is five specs whose landing plan reds an unguarded merge-bar leg in every ordering, and correcting it requires editing five specs plus a decision about bump churn.

**Fix.** Give every unit that edits gen_build_index.py its own KIT_MEMORY_TREE_VERSION bump S-item (the three sites the gate's own remedy names, plus the dogfooded parity copies as spec-5 S6 already describes) and list check-verdict-epoch.sh in each §7; or land the whole engine-touching set as one range with the bump in the LAST engine-touching commit and say so explicitly.

## HIGH — 7

### GATE-1 — 2026-08-24-spec-dFramedEntrypoint-1.md · S6 and AC7

**Status:** CONFIRMED · raised at BLOCKER, final HIGH

**Finding.** S6 and AC7 require the gate leg's entry in tools/gate-legs.json to "gain the paragraph stating what it does NOT check". That file is a JSON array of leg objects whose key set is PINNED to exactly six names by the run-gates canary, which exits 1 on any stray key. A prose key cannot be added, and JSON carries no comments — so AC7 can never pass, and the attempt reds the bar.

**Evidence.** tools/run-gates/run-gates.test.sh:96 `KNOWN = {"name", "argv", "guard", "impure", "chunk", "subject"}` and the stray-key branch below it printing `canary: leg row(s) carry a key outside the pinned set` then `sys.exit(1)`; the same set is re-pinned at line 125 as the arming control. Live keys confirmed: `python -c "import json;..."` over tools/gate-legs.json prints `keys: ['argv','chunk','guard','impure','name','subject']` across 85 legs. The `build README slot contract` entry is tools/gate-legs.json:298-307 and carries name/argv/chunk/subject only.

**How the skeptic settled it.** Reproduced independently. `python -c` over tools/gate-legs.json prints `keys: ['argv','chunk','guard','impure','name','subject']` across 85 legs, and the `build README slot contract` row carries only name/argv/chunk/subject. tools/run-gates/run-gates.test.sh pins `KNOWN = {"name","argv","guard","impure","chunk","subject"}` and exits 1 on any stray key; that canary leg's own manifest row is `{"name":"run-gates canary","guard":["tools/","tools/gate-legs.json","tools/lib/"],"subject":"repo"}`, so it runs on any tools/ change. JSON carries no comments and no free-text key exists, so spec-1 S6 and AC7 as written cannot be satisfied and the attempt reds the bar. Downgraded from BLOCKER: the failure is mechanical and immediate, the charter §7 rule the spec is invoking says the gate's OWN header, and the correct home (gen_build_index.py's module docstring plus do_check_format's docstring at line 1137, the shape tools/check-testsuite-counts.sh:1-23 already uses) is one file away — a one-line spec correction, not a design fault.

**Fix.** Restate spec-1 S6 and AC7 against gen_build_index.py's own header — the module docstring plus do_check_format's docstring — and drop `tools/gate-legs.json` from spec-1 §4 Files touched.

### REG-1 — 2026-08-24-spec-dFramedEntrypoint-3.md · §2 S1 · §4 Files touched · §7 Gates

**Status:** CONFIRMED · raised at BLOCKER, final HIGH

**Finding.** The registry is sited at `memory/project/readme-contract.txt`, but hygiene check 3 grades that directory against a CLOSED filename allowlist that the spec never edits — so the file reds the `memory hygiene` leg the spec itself names as a gate.

**Evidence.** tools/memory-tree/check-memory-hygiene.sh:300-305 — `case "$e" in F:legacy-files.txt|F:curation-debt.txt) ;; F:id-orphan-waiver.txt|F:corpus-path-unresolved.txt|F:unarmed-branches.txt) ;; F:method-carriers.txt|F:testsuite-count-waivers.txt) ;; F:trace-waiver.txt) ;; *) echo "$M/project/${e#*:}";; esac` then `fail 3 "unexpected entries (structure)"`. `readme-contract.txt` hits the `*)` arm. memory/HYGIENE.md:94 states the directory is "Six plain lists ... the whole of what that directory holds"; AGENTS.md layout says `memory/project/` is "the gate's `*.txt` waiver registries and nothing else", and this registry is not a waiver. Spec 3 §4 Files touched names gen_build_index.py, .gitattributes, HYGIENE.md and the dossier — never check-memory-hygiene.sh.

**How the skeptic settled it.** Reproduced exactly. `tools/memory-tree/check-memory-hygiene.sh:300-305` is a closed `case` allowlist — `F:legacy-files.txt|F:curation-debt.txt`, `F:id-orphan-waiver.txt|F:corpus-path-unresolved.txt|F:unarmed-branches.txt`, `F:method-carriers.txt|F:testsuite-count-waivers.txt`, `F:trace-waiver.txt` — with `*) echo "$M/project/${e#*:}"` feeding `fail 3 "unexpected entries (structure)"` at :307-308. `memory/project/readme-contract.txt` hits the `*)` arm and reds check 3. The `memory hygiene` leg in tools/gate-legs.json carries NO guard, so it runs on every bar, and spec 3 §7 names it as one of its own gates. Spec 3 §4 Files touched lists gen_build_index.py, .gitattributes, memory/HYGIENE.md via its template and the dossier — `check-memory-hygiene.sh` is absent. The siting question is real too: AGENTS.md:52 defines `memory/project/` as "the gate's `*.txt` waiver registries and nothing else", memory/HYGIENE.md:93 says "Six plain lists in `memory/project/` — the whole of what that directory holds", and this registry is a declared population, not a waiver. Held at HIGH rather than BLOCKER: it reds a named leg loudly on the first bar and the mechanical repair is one allowlist line, so it stops the commit, not the build.

**Fix.** Bring check 3's allowlist plus its selftest arm and the HYGIENE.template prose into unit 3's scope and Files touched, or site the registry under `tools/memory-tree/` beside unit 2's limits file, which is outside check 3's population — and which also fixes ADOPT-1.

### UNB-1 — 2026-08-24-spec-dFramedEntrypoint-3.md · S1 / AC5 / AC8 (and §4 Files touched)

**Status:** CORRECTED · raised at BLOCKER, final HIGH

**Finding.** Unit 3 omits the check-3 allowlist edit from its scope, so the first bar after the registry lands reds on the unguarded `memory hygiene` leg. The AC it blocks is AC3 ('bash tools/run-gates/run-gates.sh is green'), not AC5 or AC8; spec 7 AC7 is blocked for the same reason. Not a BLOCKER: the fix is one case arm in a file the same kit owns, the failure is self-announcing on the first run, and the placement itself is precedented — F:method-carriers.txt is allowlisted for a registry owned by a different script, which is the very shape spec 3 §10 says it copies.

**Evidence.** tools/memory-tree/check-memory-hygiene.sh:300-305 — `bp=$(printf '%s\n' "$p1" | grep . | while IFS= read -r e; do case "$e" in` then arms `F:legacy-files.txt|F:curation-debt.txt)`, `F:id-orphan-waiver.txt|F:corpus-path-unresolved.txt|F:unarmed-branches.txt)`, `F:method-carriers.txt|F:testsuite-count-waivers.txt)`, `F:trace-waiver.txt)` and the catch-all `*) echo "$M/project/${e#*:}"`; the comment at :287 states "project/ holds the gate's OWN waiver registries and nothing else". `bad3` feeds `fail 3 "unexpected entries (structure)"` at :313. The `memory hygiene` leg (tools/gate-legs.json) has no guard, so it runs on every bar.

**How the skeptic settled it.** Mechanism reproduced. tools/memory-tree/check-memory-hygiene.sh:300-305 is a CLOSED case list (F:legacy-files.txt|F:curation-debt.txt / F:id-orphan-waiver.txt|F:corpus-path-unresolved.txt|F:unarmed-branches.txt / F:method-carriers.txt|F:testsuite-count-waivers.txt / F:trace-waiver.txt) with a `*)` catch-all feeding bad3 -> `fail 3 "unexpected entries (structure)"` at :313; `git ls-files memory/project/` returns exactly those eight names. The `memory hygiene` leg carries guard=None in tools/gate-legs.json, so it runs on every bar. Spec 3 §4 Files touched lists only readme-contract.txt, gen_build_index.py, .gitattributes, memory/HYGIENE.md-via-template and the dossier — check-memory-hygiene.sh is absent, and HYGIENE.template.md is a document, not the allowlist. BUT the two ACs the finding names are wrong: spec 3 AC5 is stated for the file being ABSENT ('When memory/project/readme-contract.txt is absent, --check-format exits 1') and is testable with no green bar at all, and AC8 grades the slot-contract leg's output, not the hygiene leg.

**Fix.** Add `F:readme-contract.txt` to the check-3 case list as an S-item of unit 3, put tools/memory-tree/check-memory-hygiene.sh into §4 Files touched, and add a check-3 arm to check-memory-hygiene.test.sh.

### UNB-2 — 2026-08-24-spec-dFramedEntrypoint-5.md · S2 / S4 / §5 risks / AC6

**Status:** CORRECTED · raised at BLOCKER, final HIGH

**Finding.** S2 breaks a second class of selftest arms the spec does not name — the two records-table arms at :1625-1630 and the three folder-sentence count/literal arms at :1307, :1341, :1364 — so `build-index selftest` reds. The load-bearing part is the arm labelled '(nine arms depend on it)': the derived sentence is the liveness detector nine arms use to notice a mis-segmented record selector, and S2 deletes the detector without naming a replacement. The AC5 contradiction claim is dropped; that arm still passes.

**Evidence.** tools/memory-tree/gen_build_index.py:1625-1627 arm("a build with a record renders it in the Records table", "| [2026-08-01-review-tOne-1.md]&#40;reviews/2026-08-01-review-tOne-1.md&#41; | spec-audit | ARCH-tOne-1 |", ...); :1628-1630 arm("the derived folder sentence SURVIVES the table (nine arms depend on it)", "Records live under", ...); :1307-1308 arm("the authored sentence leaves the body", "1", ... .count("Records live under")); :1341-1342 arm("the derived sentence names only the folders that exist", "Records live under `spec/`.", ...); :1364-1365 arm("a claim BELOW the marker pair is removed too, not ignored", "1", ... .count(...)); :1649-1651 arm("a build with NO records renders neither the table nor a coverage line") — which after S3's re-emitted joins also contradicts this unit's own AC5. The render site is :651-676 (`Records live under {joined}.` at :653, table header at :657).

**How the skeptic settled it.** Five arms verified to break, and none is covered by S4's tuple-index class. gen_build_index.py:1625-1627 asserts the literal table row; :1628-1630 asserts 'Records live under' with the label '(nine arms depend on it)'; :1307-1308 and :1364-1365 assert count('Records live under') == '1', which goes to 0 once the derived sentence at :653 stops rendering (arm() at :1213 tests `want in str(got)`, so '1' in '0' is False); :1341-1342 asserts the literal 'Records live under `spec/`.'. Spec 5 §5 risks names only 'the tuple-index selftest arms' as the second hazard and §5 testing mentions only the index->name move plus one new joins arm, so this class is nowhere in the spec. The `build-index selftest` leg is in spec 5's own §7. One sub-claim is wrong: the arm at :1649-1651 asserts only `'| Record | Kind | Serves |' not in output`, which still holds after the table is deleted, so it passes and contradicts nothing — only its LABEL ('neither the table nor a coverage line') goes stale.

**Fix.** Add an S-item enumerating the records-table and folder-sentence arms with each one's post-deletion disposition, and state what replaces the derived sentence as the mis-segmentation detector for the nine arms that read it.

### READPATH-2 — 2026-08-24-spec-dFramedEntrypoint-8.md · §2 S1, §4 files touched, §5 risks, AC7

**Status:** CONFIRMED · raised at BLOCKER, final HIGH

**Finding.** The DECISIONS.md append cannot land green. memory/DECISIONS.md is one of six read-path members capped by hygiene check 16, and the budget has 153 B of margin at this build's pinned base (52 B on the current staged tree). Rows in that file measure 263–295 B. AC7 asserts the hygiene gate is green including the read-path check, and §4 does not list `.memory-tree.conf`, so nothing raises READ_PATH_CEILING.

**Evidence.** `python tools/memory-tree/corpus_ids.py --measure` -> 'READ_PATH_CEILING="158308"   # measured 132708 B + 25600 B headroom'; the live pin is `.memory-tree.conf:188 READ_PATH_CEILING="132760"`, so margin = 132760 - 132708 = 52 B (153 B at base 9ddcc5c9, where memory/LIVE.md is 1548 B not 1649 B). Members derived by corpus_ids.read_set: memory/DECISIONS.md 17544, memory/LIVE.md 1649, memory/guides/BUILD-METHOD.md 23868, memory/guides/REVIEW-PROTOCOL.md 15614, memory/guides/SESSION-KICKOFF.md 25195, memory/guides/UNATTENDED-PROTOCOL.md 48838. Refusal at tools/memory-tree/corpus_ids.py:455-458 `if total > ceiling`. Existing row widths: memory/DECISIONS.md:19 = 290 B, :78 = 281 B, neighbours 263–295 B.

**How the skeptic settled it.** Reproduced to the byte. `python tools/memory-tree/corpus_ids.py --report` gives 'read path : 6 files, 132708 B' over exactly the six members named — memory/DECISIONS.md 17544, memory/LIVE.md 1649, BUILD-METHOD.md 23868, REVIEW-PROTOCOL.md 15614, SESSION-KICKOFF.md 25195, UNATTENDED-PROTOCOL.md 48838 — against the live pin .memory-tree.conf:188 READ_PATH_CEILING="132760", so the margin is 52 B on the staged tree. At base 9ddcc5c9, memory/LIVE.md is 1548 B (git cat-file -s), so the margin was 153 B. The refusal is corpus_ids.py:456-458 `if total > ceiling`. Row widths in memory/DECISIONS.md measured by awk: the widest is 300 B and the recent tooling rows run 237-296 B, so any conforming append blows both margins. Spec-8 S1 appends such a row, AC7 asserts the hygiene gate is green 'including the read-path check', and §4 Files touched does not list .memory-tree.conf. Spec-8 §5 names the hazard but ships no S-item resolving it, and its §5 line is also slightly wrong: memory/map/features/build-readme-surface.md is NOT a read-path member. Spec-1's BUILD-METHOD.md edit (23868 B, same budget) prices nothing either. Downgraded from BLOCKER: the remedy is the checker's own sanctioned one ('trim it or raise the ceiling in a commit that says why'), a one-line conf edit that changes no design — it stops the first commit's gate, not the build.

**Fix.** Add .memory-tree.conf to spec-8 §4 with an S-item raising READ_PATH_CEILING and its reason, or trim a member in the same commit; do the same for spec 1's BUILD-METHOD.md edit.

### UNB-4 — CROSS · specs 1, 2, 3, 4 and 6 — §7 Gates and §2 Scope

**Status:** CONFIRMED · raised at BLOCKER, final HIGH

**Finding.** The rule binds per push range, not per commit — several units landing in ONE push need only one correctly-placed bump. But with per-unit landings, every push range containing an engine-moving unit needs its own bump, and only unit 5 has one.

**Evidence.** tools/memory-tree/check-verdict-epoch.sh:68-69 — `ENGINE=tools/memory-tree/check-memory-hygiene.sh` and `DELEGATES="... tools/memory-tree/gen_build_index.py ..."`; the header at :16-19 states "Let W be the NEWEST commit in `<base>..HEAD` that moves a behaviour-bearing line of the engine, and S the NEWEST commit ... Then W must be an ancestor of, or equal to, S". The leg `verdict epoch (kit version dates the engine)` in tools/gate-legs.json has `guard: None`, so it runs on every bar. Current value: tools/memory-tree/check-memory-hygiene.sh:13 `KIT_MEMORY_TREE_VERSION=2.30`.

**How the skeptic settled it.** Every citation reproduces. check-verdict-epoch.sh:68-69 lists gen_build_index.py in DELEGATES with check-memory-hygiene.sh as ENGINE; the header at :16-19 states the topological rule; the leg carries guard=None so it runs on every bar; check-memory-hygiene.sh:13 is at 2.30. Crucially the code at :137-141 fails outright when W exists and S is empty in BASE..HEAD, and BASE defaults to the merge-base with origin/main — so the rule is per PUSH RANGE. Under these specs' landing model (unit 5 = 'one commit', unit 7 = 'one commit per build folder', no declared build order beyond spec 8's 'lands FIRST'), any push boundary crossed by an engine-moving unit with no bump in its range reds. Only spec 5 S6 carries a bump; specs 1, 2, 3, 4 and 6 all edit gen_build_index.py (spec 4 edits check-memory-hygiene.sh too) and none lists check-verdict-epoch.sh in §7 — they list check-kit-versions.sh, which grades marker parity, not the epoch. Unit 5's bump also cannot cover unit 6 landing after it, because that later range has its own base. Not a BLOCKER: the gate's own remedy() prints the three sites and the --render command.

**Fix.** State the bump's placement against the build order (at or after the last engine-moving commit in each push range), and list check-verdict-epoch.sh in §7 of units 1, 2, 3, 4 and 6.

### UNB-9 — CROSS · spec 1 §4 Data model (the five-slot canon) against memory/guides/BUILD-METHOD.md M1/M2/M4

**Status:** CONFIRMED · raised at HIGH, final HIGH

**Finding.** The build method mandates authored build-README content that the closed canon has no slot for, beyond the parked decisions the owner's fork-2 ruling already accounted for: the per-unit classification (M2 "Write it into the build README before acting on it") and the runaway-ceiling promotion note (M4). The method also names the authored Units table as the primary roster source. A closed canon that omits them makes the method and the gate contradict each other the first time a run follows the method on a bound README.

**Evidence.** memory/guides/BUILD-METHOD.md:38 "**Classify, first match wins.** Write it into the build README before acting on it."; :33 "The roster is the build README's authored Units table where one exists"; :133 "...reaching it is itself a defect, so the run promotes and lands anyway and says so in its output AND the build README." Spec 1 §4 Data model declares exactly five slots (description/goal bound, expected improvements, detriments, build-level rules, parked decisions) and spec 1 S1 forbids any heading outside the canon.

**How the skeptic settled it.** Every quote verifies verbatim in memory/guides/BUILD-METHOD.md: :33 'The roster is the build README's authored Units table where one exists'; :38 '**Classify, first match wins.** Write it into the build README before acting on it.'; :133 '...the run promotes and lands anyway and says so in its output AND the build README.' Spec 1 §4 Data model declares exactly five slots and S1 forbids any heading outside the canon, so a method-following run on a bound README must write content the canon has no home for. Not a relitigation of the owner's ruling — the ruling itself reasons from this exact source ('the build method mandates authored README content that the owner's four cannot hold') and accounted for only ONE of the mandates, parked decisions; the classification and the promotion note were not considered. Note the finding's first fix option is closed off by that ruling (the slot set is the owner's), so the amendment has to land on the method side. Supporting detail the finding missed: tools/memory-tree/BUILD-METHOD.template.md exists and the guide is rendered from it, yet spec 1 §4 Files touched names only memory/guides/BUILD-METHOD.md for the S3 pointer — the same template-first inversion spec 1 explicitly avoids for HYGIENE.

**Fix.** Amend tools/memory-tree/BUILD-METHOD.template.md (and re-render memory/guides/BUILD-METHOD.md) to route the M2 classification and the M4 promotion note somewhere the canon admits, in the same build, and name the TEMPLATE in the owning unit's §4 Files touched rather than the rendered guide.

## MEDIUM — 30

### DRYRUN-11 — 2026-08-24-spec-dFramedEntrypoint-1.md · AC8

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** AC8 requires the output of `--check-format --dry-run` listing hits and near-misses, but no such flag exists, no S-item adds one, and the extra argument is silently ignored — so the AC passes by printing the ordinary clean line and proving nothing. That defeats the charter §7 rule it exists to satisfy.

**Evidence.** tools/memory-tree/gen_build_index.py:1662-1665 — `mode = argv[1] if len(argv) > 1 else "--check"` then `if mode not in ("--check", "--write", "--check-format", "--print-bindings")`; argv[2] is never read. `grep -rn 'dry-run|dry_run' tools/memory-tree/*.py tools/memory-tree/*.sh` returns nothing. Ran `python tools/memory-tree/gen_build_index.py --check-format --dry-run` -> `build-index: slot contract clean (62 build README(s))`, rc=0 — no hits, no near-misses, no error.

**How the skeptic settled it.** Reproduced by execution, not by reading. gen_build_index.py:1661-1666 sets `mode = argv[1] if len(argv) > 1 else "--check"` and gates on `mode not in ("--check", "--write", "--check-format", "--print-bindings")`; argv[2] is never read anywhere in the module. `grep -rniE 'dry-run|dry_run'` over tools/memory-tree/ returns no source hit. I ran `python tools/memory-tree/gen_build_index.py --check-format --dry-run` in this worktree: it printed `build-index: slot contract clean (62 build README(s))` and exited rc=0 — the extra argument is silently swallowed and the ordinary clean line is emitted. Spec-1 AC8 demands 'its output of `--check-format --dry-run` lists hits AND near-misses'; no S-item (S1-S7) ships such a verb, and §3 non-goals and §8 forks park nothing about it. So AC8 is satisfiable today by a command that proves nothing, which is exactly the self-satisfying-gate class AGENTS.md §7 forbids and the same §7 rule ('Run a candidate gate predicate over the real tree before wiring it, and print hits AND near-misses') that AC8 exists to discharge. MEDIUM: the silent-ignore makes it a real trap rather than an obvious typo, but it costs one spec revision, not the build.

**Fix.** Add the survey mode as its own S-item in §2 (a `--survey` verb or an env-gated report), name it in S7's arms, and re-word AC8 to that verb.

### GATELEG-12 — 2026-08-24-spec-dFramedEntrypoint-1.md · §2 S6 and AC7

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** The blind-spot paragraph is put in the wrong file and needs an unspecced schema change. `tools/gate-legs.json` is a JSON array whose 85 leg objects carry only name/argv/chunk/subject/guard/impure and no prose field, and JSON has no comments. Meanwhile the gap the verification actually found is in the module's own header — which the charter §7 rule names — and spec 1 leaves it untouched.

**Evidence.** `python -c` over tools/gate-legs.json: 85 legs, key set = ['argv','chunk','guard','impure','name','subject']; the slot-contract leg object is {name, argv, chunk, subject} with no free-text field. The real gap: tools/memory-tree/gen_build_index.py:3-5 lists only `--check`, `--write`, `--selftest` — `--check-format`, the verb this merge-bar leg invokes, appears nowhere in the module docstring — and do_check_format's docstring at :1137-1143 states only why the verb is unreachable from plan(), naming no non-coverage. Confirmed by reviews/…claim-verification.md:198-199.

**How the skeptic settled it.** Reproduced and then strengthened. tools/gate-legs.json parses as a JSON array of 85 leg objects whose union of keys is exactly ['argv','chunk','guard','impure','name','subject']; the slot-contract entry is {"name": "build README slot contract", "argv": [...], "chunk": "records", "subject": "repo"} with no free-text field, and JSON carries no comments — so spec-1 S6/AC7 ('the gate leg's own entry gains the paragraph', 'its tools/gate-legs.json entry carries a paragraph') needs a schema change no S-item specs. Harder evidence the finding did not have: tools/run-gates/run-gates.test.sh:95-114 pins the key set — `KNOWN = {"name", "argv", "guard", "impure", "chunk", "subject"}` — and reds on any stray key; that harness is the leg named 'run-gates canary' in gate-legs.json with subject 'repo' (so it runs on an ordinary bar, not only under GATE_SELFTESTS) and guard ['tools/', 'tools/gate-legs.json'] (so editing the manifest is exactly what arms it). Implementing S6 literally therefore reds the bar, and spec-1 §7 does not list the run-gates canary among its gates nor §4 among its files touched. The finding's second half also reproduces: gen_build_index.py:3-5 lists only --check, --write and --selftest, omitting --check-format and --print-bindings, and do_check_format's docstring (:1137-1143) explains only why the verb is unreachable from plan(), naming no non-coverage — so the charter §7 rule 'A gate's OWN header states what it does NOT check' is unmet where the reader actually is. MEDIUM: it reds loudly at the first bar rather than landing silently wrong.

**Fix.** Re-point S6 at the module: add --check-format and --print-bindings to the gen_build_index.py docstring and give do_check_format's docstring the not-checked paragraph. If a manifest note is still wanted, S6 must also amend KNOWN in tools/run-gates/run-gates.test.sh and add that file to §4 and §7.

### LEG-1 — 2026-08-24-spec-dFramedEntrypoint-1.md · §2 S6 and §6 AC7

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** S6/AC7 put the blind-spot paragraph inside `tools/gate-legs.json`, a machine manifest with a closed six-key schema and no prose field; no S-item extends the schema or any reader, and the repo's stated home for that paragraph is the gate's own header.

**Evidence.** Measured over all 85 entries of tools/gate-legs.json, the key set is exactly `['argv','chunk','guard','impure','name','subject']` (no description/prose key anywhere); the `build README slot contract` entry is tools/gate-legs.json:299-307. The manifest is read by tools/run-gates/run-gates.sh, tools/check-testsuite-counts.sh, tools/codebase-map/map_extractors.py, tools/drift-audit/drift_signals.py and tools/govkit/govkit.py:859-925, which compares it against kit descriptors by name and subject. AGENTS.md §7 states the rule as "A gate's OWN header states what it does NOT check", and this leg's argv is `gen_build_index.py --check-format`, whose header is its module docstring.

**How the skeptic settled it.** Reproduced, and it is stronger than the finding states: implementing S6 as written REDS a bar leg, it does not merely sit in the wrong place. Measured over all 85 entries of tools/gate-legs.json the key set is exactly ['argv','chunk','guard','impure','name','subject'] — no prose field — and the slot-contract entry (tools/gate-legs.json:298-307) carries only name/argv/chunk/subject. The set is PINNED by a canary in tools/run-gates/run-gates.test.sh:96 `KNOWN = {"name","argv","guard","impure","chunk","subject"}`, which exits 1 on any stray key ('canary: leg row(s) carry a key outside the pinned set'), and its own control at :121-134 proves the predicate is armed by failing a near-miss fixture. No S-item in spec 1 extends that schema or the canary, and spec-1 §7 does not list the run-gates canary among its gates. There is no alternative home in a descriptor either: tools/memory-tree/kit.toml:141-145's [[gate_leg]] for the same leg carries only name/subject/argv/guard. AGENTS.md §7's rule is 'A gate's OWN header states what it does NOT check', and this leg's argv is gen_build_index.py --check-format, whose header is the module docstring.

**Fix.** Move the blind-spot paragraph to gen_build_index.py's --check-format header (mirrored in the build-readme-surface dossier) and drop tools/gate-legs.json from AC7.

### UNB-10 — 2026-08-24-spec-dFramedEntrypoint-1.md · AC8

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** AC8 requires the output of `--check-format --dry-run` listing hits AND near-misses. No such flag exists, `main()` silently ignores every argument after the mode, and no S-item (S1-S7) ships one. Run as written the command performs an ordinary `--check-format` and prints only `build-index: slot contract clean (N build README(s))` — the AC cannot be observed and cannot fail.

**Evidence.** tools/memory-tree/gen_build_index.py:1661-1666 — `mode = argv[1] ...`; `if mode not in ("--check", "--write", "--check-format", "--print-bindings"): ... return 2`; `argv[2:]` is never read. `do_check_format` at :1137-1158 has no dry-run branch and prints only the clean/FORMAT lines.

**How the skeptic settled it.** Reproduced. gen_build_index.py:1662-1666 accepts only --check, --write, --check-format, --print-bindings and --selftest, and argv[2:] is never read; do_check_format at :1137-1158 has no dry-run branch and prints only the clean line or the FORMAT block. No S-item in spec 1 (S1-S7) ships such a verb. Run literally, `--check-format --dry-run` performs an ordinary --check-format and prints 'build-index: slot contract clean (N build README(s))' — so AC8 looks satisfied while proving nothing about hits or near-misses, which is the could-not-fail shape §7 of the charter names. This matters because §5 risks leans on that same run-the-candidate-over-the-tree-and-print-near-misses mitigation, so the AC is the only thing binding it.

**Fix.** Ship the report/dry-run verb as an explicit S-item with its output shape, or restate AC8 against a one-off probe script whose output is recorded in the acceptance ledger.

### UNB-8 — 2026-08-24-spec-dFramedEntrypoint-1.md · S1 and §3 Non-goals (with spec 7 S2 and spec 2 S2)

**Status:** CORRECTED · raised at HIGH, final MEDIUM

**Finding.** The five-slot canon gives the parked roster:units pair no declared position, leaving an implementer to guess whether it belongs inside a slot body or after the canon, and spec 2 S2's last-slot byte slice silently charges its table to that slot's ceiling. 11 READMEs carry the pair, 2 of them live and therefore inside unit 7's population — not 12.

**Evidence.** `git ls-files "memory/builds/*/README.md" | xargs grep -l "roster:units"` returns 12 paths; of those `aDeclaredBound` renders `**Build status:** OPEN` and `aTetheredScratch` renders `INPROGRESS`. memory/builds/dSettledRoster/README.md: title at line 10, `<!-- roster:units -->` at 77, `<!-- /roster:units -->` at 81, `<!-- gen:build-index -->` at 83 — the pair and its table sit between title and first generated marker. memory/map/features/build-readme-surface.md:28-30 states the sequence as "Front matter, title, ONE authored prose block, the authored `roster:units` plan, then the generated regions". `slot_violations` already treats the pair as a distinct landmark (gen_build_index.py:998-1006, PLAN_CLOSE trigger).

**How the skeptic settled it.** The gap is real but the claim overstates twice. Measured: 12 tracked build READMEs contain the string `roster:units`, but only 11 carry a complete pair — aRuledFrontispiece has 0 open and 0 close markers, it only names the construct in prose. The two live carriers verify (aDeclaredBound OPEN, aTetheredScratch INPROGRESS), and in dSettledRoster the pair sits at lines 77/81 between the title at 10 and gen:build-index at 83, so it IS inside the region S1 governs; memory/map/features/build-readme-surface.md:28-30 states the sequence verbatim and slot_violations already treats PLAN_CLOSE as a landmark (gen_build_index.py:1001, PLAN_OPEN/PLAN_CLOSE at :53-54). But S1 as written bans a HEADING outside the canon and a non-blank line BEFORE the first canonical heading; an HTML comment pair sitting after the last slot heading trips neither clause, so 'unit 7 must red it' is not supported. The surviving defect is that the canon assigns the pair no position at all, and spec 2 S2's slice ('to the line before the next canonical heading or the first generated marker, whichever comes first') therefore charges the whole roster table against the last authored slot's byte ceiling.

**Fix.** Give the roster pair an explicit stated position relative to the canon (a named optional slot, or an allowance between the last slot and the first generated marker), and exclude its lines from the byte slice in spec 2 S2.

### BUDGET-ROSTER-15 — 2026-08-24-spec-dFramedEntrypoint-2.md · §2 S2 and §4 Inventory (the measured slice)

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** The slice for the LAST canonical slot runs 'to the first generated marker', which on a roster-carrying README swallows the authored `roster:units` pair and bills its table to the parked-decisions slot — a block spec 1 §3 forbids this build from touching and the owner explicitly parked.

**Evidence.** spec-2:20-22 S2 — 'from a slot's heading line to the line before the next canonical heading or the first generated marker, whichever comes first'. The pair sits between the authored prose and the first `gen:` marker on all eleven carriers: memory/builds/aDeclaredBound/README.md:111, aTetheredScratch/README.md:75, aBranchedMandate:234, aFusedCharter:169, aMeteredTurnstile:44, aPromptedMandate:107, aStandingWrit:191, cBriefedPilot:288, cSettledDocket:73, dSettledRoster:77, dUnstalledConvoy:203. tools/memory-tree/gen_build_index.py:53-54 defines it as PLAN_OPEN/PLAN_CLOSE, and the shipped contract already treats it as structure (slot_violations trigger 2, :1001-1005).

**How the skeptic settled it.** Reproduced end to end. spec-2 S2 (:19-20) measures a slot 'from a slot's heading line to the line before the next canonical heading or the first generated marker, whichever comes first', and §4 Inventory repeats 'or before the first generated marker for the last slot'. I measured the marker order on every carrier: for all eleven files carrying `<!-- roster:units -->`, the pair opens BEFORE the first `<!-- gen:` marker — aBranchedMandate 234/245, aDeclaredBound 111/123, aFusedCharter 169/181, aMeteredTurnstile 44/52, aPromptedMandate 107/120, aStandingWrit 191/208, aTetheredScratch 75/82, cBriefedPilot 288/317, cSettledDocket 73/85, dSettledRoster 77/83, dUnstalledConvoy 203/228. The roster pair is not a generated marker: gen_build_index.py:47-54 declares PLAN_OPEN/PLAN_CLOSE as 'The AUTHORED plan region. This generator NEVER writes between these two markers', and slot_violations trigger 2 (:1000-1005) already treats the PLAN_CLOSE→first_open span as its own structural case. So the last canonical slot's slice swallows the roster pair and bills its table to parked-decisions — bytes spec-1:43 forbids this build from touching. Reachable, not hypothetical: memory/LIVE.md lists aDeclaredBound (OPEN) and aTetheredScratch (INPROGRESS) among the 13 live builds, and both are roster carriers, so both are inside unit 7's conformable population. Nothing in spec-2 §3 or §8 parks it. MEDIUM: it forces either an inflated parked-decisions ceiling or a red on bytes the build may not remove.

**Fix.** Terminate the last slot's slice at PLAN_OPEN when the pair is present, and state in S2 that the plan pair's span is measured by no budget until the parked roster question is ruled.

### GATE-5 — 2026-08-24-spec-dFramedEntrypoint-2.md · S1 and S6 (and §10 reuse audit)

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** S1 puts the hard ceilings and the advisory high-waters in ONE file and S6 gives `--bump` a verb that rewrites that file — so the ceiling declarations sit inside the write path of the advisory that must never move them. The shape §10 claims to copy "in both halves" deliberately keeps them in two files precisely so `--bump` cannot reach the ceilings. AC5 catches a moved ceiling only as a one-time `git diff` eyeball, never as a check.

**Evidence.** tools/check-template-size.sh:54 `LIMITS=${4:-${LIMITS:-"$ROOT/tools/template-size-limits.txt"}}` (the declared ceilings) versus :96 `HIGHWATER=${3:-${HIGHWATER:-"$ROOT/tools/template-size-highwater.txt"}}` (the ratchet), with the bump block at :140-168 rewriting `$HIGHWATER` only and never opening `$LIMITS`. Its header at :14-15 states the split: "The ceiling for each subject is DECLARED in tools/template-size-limits.txt ... this script carries only a hard default". spec-2 §2 S1 declares one file `tools/memory-tree/build-readme-slot-limits.txt` carrying "its hard ceiling in bytes and its recorded high-water"; §6 AC5 is the only guard.

**How the skeptic settled it.** Reproduced against the source. tools/check-template-size.sh:54 resolves `LIMITS` to tools/template-size-limits.txt, :96 resolves `HIGHWATER` to tools/template-size-highwater.txt, and the `--bump` block at :140-168 opens, rewrites, sorts and `mv`s `$HIGHWATER` alone, never touching `$LIMITS`; the header at :13-15 confirms the ceilings live in the separate declared file. Spec-2 S1 collapses both into one file and S6 gives `--bump` a verb that rewrites it, so the declared policy sits inside a write path, and AC5 ('`git diff` shows only high-water rows changed and no ceiling changed') is a one-time eyeball rather than a standing check. Spec-2 §10 is internally inconsistent with its own S1 on this point: it describes the copied shape as declaring ceilings 'in tools/template-size-limits.txt' with 'a separate high-water ratchet'. One over-read noted and not held against the finding's substance: the source nowhere states that the split exists 'precisely so --bump cannot reach the ceilings' — that rationale is the reviewer's inference.

**Fix.** Split the two files as tools/check-template-size.sh does, or keep one file and add a standing assertion that the ceiling column is byte-unchanged across a `--bump`, rather than leaving AC5 as a diff review.

### GATE-6 — 2026-08-24-spec-dFramedEntrypoint-2.md · S8 against §5 error/empty states

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** A declared refusal ships with no arm and no acceptance criterion. §5 states "A slot with no row is a refusal for the same reason", but S8's arm list is over-ceiling, over-high-water-under-ceiling, under both, a missing limits file, and an extra row — the missing-ROW branch is in neither the arm list nor any AC. The charter's rule is that a gate is not landed until its failing case has been observed.

**Evidence.** spec-2 §2 S8 (five named arms) and §5 "error / empty / loading states" ("a missing limits file is a refusal naming the expected path, never a skip. A slot with no row is a refusal for the same reason"); §6 AC6 covers the missing FILE and AC7 the extra ROW, nothing covers a canon slot with no row. AGENTS.md §7: "A new gate is not landed until its failing case has been observed."

**How the skeptic settled it.** Reproduced by reading spec-2 alone. §5 declares two data-shape refusals — 'a missing limits file is a refusal naming the expected path, never a skip. A slot with no row is a refusal for the same reason' — but S8's arm list holds five items (over-ceiling, over-high-water-under-ceiling, under both, a missing limits file, a limits row naming a slot the canon does not declare) and the missing-ROW branch is in none of them. §6 covers the missing FILE at AC6 and the extra ROW at AC7 and covers a missing row nowhere. The spec's own §5 testing line compounds it: 'five selftest arms, one per branch, including the two data-shape refusals', when its own §5 error states enumerate six branches. AGENTS.md §7 is explicit: 'A new gate is not landed until its failing case has been observed.'

**Fix.** Add the sixth arm to S8 and an AC: a limits file missing a row for a canonical slot exits 1 naming the slot, observed RED against a staged deletion before the arm is written; and correct §5's 'five selftest arms' to six.

### SEED-1 — 2026-08-24-spec-dFramedEntrypoint-2.md · §2 S7 and §4 Files touched

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** The ceilings are seeded from gov's own corpus into a file that ships verbatim to every adopter of the memory-tree kit, which is the inherited-pin class this kit already declares a hole against.

**Evidence.** tools/memory-tree/kit.toml `[[files]] include = "**" role = "engine"` copies everything under the kit home into an adopter's tree, and the same descriptor's `[[hole]] id = "measured-pins"` states the rule: "every pin is measured against the target corpus; an inherited number is vacuous or permanently red". Spec 2 S7: "the ceilings are SEEDED from the declared population of unit 3" — gov's population. Spec 2 §4 Files touched names the limits file, gen_build_index.py, gate-legs.json, .gitattributes and the dossier; it never names kit.toml.

**How the skeptic settled it.** Reproduced verbatim. tools/memory-tree/kit.toml:10-12 is `[[files]] include = "**" role = "engine"`, so everything under the kit home copies into an adopter's tree, and the same descriptor's `[[hole]] id = "measured-pins"` (:76-81) states the rule the finding quotes: 'every pin is measured against the target corpus; an inherited number is vacuous or permanently red.' Spec-2 S7 seeds the ceilings from gov's own declared population and spec-2 §4 Files touched names the limits file, gen_build_index.py, gate-legs.json, .gitattributes and the dossier — never kit.toml. The precedent it copies is NOT shipped: tools/govkit/registry.toml:211-216 exempts tools/template-size-limits.txt and its high-water with the reason 'gov's own charter template … prescribed for copy nowhere', so spec 2 is the first time this shape lands INSIDE a copy-installed kit. No spec in the set mentions adopters at all except spec-4, and there only about the order verb.

**Fix.** Declare a [[hole]] for the ceilings in tools/memory-tree/kit.toml with a discharge probe, name that file in spec-2 §4, and ship the adopter copy with no ceiling rows plus an announced unarmed state.

### GATE-8 — 2026-08-24-spec-dFramedEntrypoint-3.md · S4 and AC4

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** The exemption pin is asserted in one direction only. AC4 reds when the exempt count RISES above the pin; nothing reds when the pin sits ABOVE the measured count. A pin left un-lowered after a drain is permanent slack that silently re-permits exemptions, which is the same "a stale row silently widens the surface it was written to narrow" class S3 invokes for the path rows — applied to the paths but not to the pin.

**Evidence.** spec-3 §4 Data model ("A pin line declares the exempt count, and the count is shrink-only") and §6 AC4 ("When the exempt count rises above the declared pin, --check-format exits 1"); AC3 only reports the fall, it does not gate it. Both precedent registries in this tree red on the stale direction: tools/check-testsuite-counts.sh:78 ("a testsuite-count waiver names a suite that now complies, so the list has stopped shrinking and the row hides nothing") and tools/memory-tree/check-arms.py:246-249 (a pin naming a branch "which IS armed now — delete the row (the pin is shrink-only)").

**How the skeptic settled it.** Reproduced. Spec-3 AC4 gates only the rise ('When the exempt count rises above the declared pin, --check-format exits 1'); AC3 merely observes the fall; S4's 'shrink-only' constrains the pin VALUE from rising without an owner edit, not the pin from sitting above the measured count. So a pin left at 61 while the measured exempt count is 40 permits 21 new exemptions with a green bar. Both precedents red on the stale direction: tools/check-testsuite-counts.sh:78 fails a waiver 'naming a suite that now complies, so the list has stopped shrinking and the row hides nothing', and check-arms.py:239-243 fails a pin naming a branch 'which IS armed now — delete the row (the pin is shrink-only)'. Note for the fixer: those precedents red per-ROW staleness rather than on a count pin, and spec-3's REVERSE assertion (S3) covers only path existence, so the exempt row that no longer needs its exemption is the same hole one level down. Spec-3 §8 F1 parks only the pin's STARTING value, not the equality question, so this is not a parked fork.

**Fix.** Make the pin an equality — a pin above the measured exempt count reds and names both numbers — and consider the per-row form the precedents use: an exempt row whose file now conforms reds as stale.

### ORDER33-13 — 2026-08-24-spec-dFramedEntrypoint-4.md · §4 Alternatives rejected ("Authoring order in the README instead")

**Status:** CORRECTED · raised at MEDIUM, final MEDIUM

**Finding.** spec-4 §4's 'Authoring order in the README instead … in 33 of 61 files' cites census class D (the hand-maintained roster duplicate, 50 980 B) for a paragraph about class E (order rationale, 10 sections in 9 READMEs, 15 541 B — independently re-measured at 9 files). The paragraph's claim that the build removes that duplication conflicts with spec-1:43 only for the 11 pair-carrying READMEs; the other 22 loose authored rosters are within unit 7's reach.

**Evidence.** memory/builds/dFramedEntrypoint/build/2026-08-24-build-TOOL-dFramedEntrypoint-1-corpus-census.md:190 — 'the single largest class is **D, 50 980 B across 33 of 61 READMEs** … a **hand-maintained duplicate of a table the generator already renders** into `gen:build-units`'. Class E at census:176 — '**E — order rationale** | 10 | 9 | 15 541 | 4.5%'. Spec 1:43 — 'No change to the authored roster pair or to any reader of it. That is this build's park.'

**How the skeptic settled it.** Primary claim reproduced and independently re-measured; the second half is imprecise. Census:190 reads '**D, 50 980 B across 33 of 61 READMEs** … a **hand-maintained duplicate of a table the generator already renders** into `gen:build-units`' — class D is the roster duplicate, not authored build ORDER. Class E, 'order rationale', is census:176 at 10 sections / 9 READMEs / 15 541 B / 4.5%. I measured it myself rather than trusting the census: `git grep -l -iE '^#{2,3} .*\border\b' -- 'memory/builds/*/README.md'` returns 9 files (aBoundedVerdict, aDeclaredBound, aDrainedSluice, aFoldedQuarry, aFusedCharter, aPacedTurnstile, aRuledFrontispiece, aShardedFloor, dUnstalledConvoy) out of 62 tracked build READMEs — matching class E, not 33. So spec-4:72-73's '33 of 61 files' overstates the alternative's population 3.7x and cites the wrong class. CORRECTED on the second half: the finding says spec-1 §3 parks that duplication outright, but spec-1:43 parks the authored roster PAIR, and `git grep -F '<!-- roster:units -->'` shows only 11 of the 33 authored rosters sit inside a pair — the other 22 are loose prose that unit 7's conformance would evict from any bound README. The conflict with the park is partial, not total.

**Fix.** Replace '33 of 61 files' with '10 sections across 9 READMEs, 15 541 B' and cite census:176, or drop the numeric claim and rest the paragraph on the owner's ask, which spec-4 already quotes.

### TIERCELL-16 — 2026-08-24-spec-dFramedEntrypoint-4.md · §4 Migration and §5 error/empty states, against S4 and AC4

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** 'Both columns as em-dashes' cannot happen. Tier is a mandatory named group of the status-header regex and only header-carrying specs produce a roster row, so the TIER cell always has a value; only the ORDER cell can be an em-dash. AC4 asserts the opposite of §5.

**Evidence.** tools/memory-tree/gen_build_index.py:104 — `r" · Tier-(?P<tier>[12]) · base (?P<base>[0-9a-f]{8,})"`, non-optional; parse_spec returns None when HDR_RE does not match (:265-268), so a row exists only where the tier matched. spec-4:63-64 Migration 'two additional columns carrying an em-dash for every unit with no verb'; spec-4:91-92 'a build with no adopter renders both columns as em-dashes'; against spec-4 S4 'Tier is already captured by the header regex and discarded one line later' and AC4.

**How the skeptic settled it.** Reproduced from source. HDR_RE (gen_build_index.py:99-103) requires ' · Tier-(?P<tier>[12]) · base (?P<base>[0-9a-f]{8,})' with no optionality; parse_spec (:260-268) returns None when no line in the first five matches; and collect stores `"units": [u for u in units if u]` (:603), so a roster row exists only where a header matched and therefore only where a tier was captured. The TIER cell can never render an em-dash. spec-4 §4 Migration (:63-64, 'two additional columns carrying an em-dash for every unit with no verb') and §5 (:91-92, 'a build with no adopter renders both columns as em-dashes') both assert the impossible, and spec-4 AC4 (:112-113, 'each roster row shows that spec's own tier, matching the value in its **Status:** header') asserts the opposite of §5 inside the same spec. spec-4 S4 (:26-27) itself says tier is 'already captured by the header regex and discarded one line later', which confirms the value is always present. Not parked in §3 or §8. MEDIUM: the error/empty-states section is one of the ten canon sections and a contract, and it specifies a branch that cannot fire while contradicting its own AC.

**Fix.** Correct §4 Migration and §5 to say the ORDER cell renders an em-dash for a unit with no verb and the TIER cell always renders 1 or 2, because the header regex makes Tier mandatory.

### AC-GREP-10 — 2026-08-24-spec-dFramedEntrypoint-5.md · AC1 and AC2

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** Both acceptance criteria are unsatisfiable as written. A repo-wide grep for `gen:build-docs` cannot return zero — ten tracked non-README files carry the string, including spec 5 itself. `git grep -c 'Record | Kind | Serves'` cannot return zero either — eleven tracked non-README files match, including spec 5's own AC2 line. And a no-match `git grep -c` prints nothing and exits 1; it never returns zero, which is the charter §14 trap.

**Evidence.** `git grep -l -F 'gen:build-docs' -- . ':!memory/builds/*/README.md' | wc -l` = 10, including memory/builds/dFramedEntrypoint/spec/2026-08-24-spec-dFramedEntrypoint-5.md and tools/memory-tree/gen_build_index.py. `git grep -l 'Record | Kind | Serves' -- . ':!memory/builds/*/README.md'` = 11 files, including spec-5 itself, aSealedCaravan/RUN.md, aSiftedPlaybook/RUN.md and tools/unattended/unattended.test.sh. `git grep -c 'ZZZ_NO_SUCH_STRING_ZZZ'` prints nothing, rc=1.

**How the skeptic settled it.** Reproduced all three legs. (1) `git grep -l -F 'gen:build-docs' -- . ':!memory/builds/*/README.md'` returns 10 tracked files, including tools/memory-tree/gen_build_index.py and spec-5 itself, so AC1's stated verification — 'a repo-wide grep returning zero' — can never be satisfied even after a perfect implementation. (2) `git grep -l 'Record | Kind | Serves' -- . ':!memory/builds/*/README.md'` returns 11 files (aBoundedVerdict/spec/…-11.md, aSealedCaravan/RUN.md, aSiftedPlaybook/RUN.md, three dScrubbedConduit reviews, two dFramedEntrypoint build records, spec-5 itself, gen_build_index.py, tools/unattended/unattended.test.sh), so AC2's literal command cannot return zero either. (3) `git grep -c 'ZZZ_NO_SUCH_STRING_ZZZ'` printed nothing and exited rc=1 — a no-match `git grep -c` never 'returns zero', which is the trap AGENTS.md §14 names by name ('A no-match grep exits non-zero and fails && chains — a PASSING zero-count check reads as failure'). Not parked: spec-5 §3 and §8 say nothing about the verification method. MEDIUM, not BLOCKER: the intent is unambiguous and the recipe is one edit away, but as written both ACs are unsatisfiable and the DoD cannot be shown met by the stated method.

**Fix.** Scope both probes to the graded class and assert a derived count, terminating the probe per §14: `test "$(git ls-files 'memory/builds/*/README.md' | xargs grep -lF '<!-- gen:build-docs -->' | wc -l)" -eq 0`, and the same shape for the records-table header.

### JOINS-22 — 2026-08-24-spec-dFramedEntrypoint-5.md · §2 S3, §5 error/empty states, AC5

**Status:** CONFIRMED · raised at LOW, final MEDIUM

**Finding.** The empty-case requirement covers the rarer branch and misses the common one. The join lines vanish under `if gap:` / `if agap:`, so a build that HOLDS records with full coverage also renders nothing — the same 'absence reads as coverage' failure the spec names — yet AC5 only covers a build holding no records at all.

**Evidence.** tools/memory-tree/gen_build_index.py:668-678 — the two emitters sit under `if gap:` and `if agap:` inside `if recs:` opened at :660. spec-5:99-100 — 'a build holding no records still renders both coverage joins, and the joins must state the empty case rather than vanishing, or their absence reads as coverage'; AC5 scopes the requirement to 'a build holds no records at all'.

**How the skeptic settled it.** Reproduced from source and then measured, which raises rather than lowers it. gen_build_index.py:660-678: `recs = build.get("records") or []`, `if recs:` opens at :660, and both emitters sit under their own guards inside it — `if gap:` at :673 and `if agap:` at :676. So a build that HOLDS records with complete coverage renders neither line. I measured the corpus over `git ls-files 'memory/builds/*/README.md'` (62 files): 8 carry no records table at all — the only case spec-5 AC5 (:121-122, 'When a build holds no records at all') covers — while 49 carry a records table and no 'Ids no record names' line, and 35 carry no 'Ids no spec-audit record has ever named' line. The uncovered branch is six times more common than the covered one, and it is precisely the failure spec-5 §5 (:99-100) names in its own words: 'or their absence reads as coverage'. Not parked — spec-5 §3 and §8 say nothing about it. Re-graded up from LOW to MEDIUM: the whole justification for re-emitting the joins in S3 is that they are the repo's only spec-to-record coverage signal (verification confirms hygiene check 21 grades the opposite direction and nothing gates these lines), and as specced that signal stays silent on 49 of 62 builds.

**Fix.** Widen S3 and AC5 to require both lines unconditionally whenever the region renders, with explicit zero-gap phrasings ('every unit id is named by a record', 'every unit id has a spec-audit record') distinct from the no-records-at-all case.

### SENT-1 — 2026-08-24-spec-dFramedEntrypoint-5.md · §2 S2 and §2 S4

**Status:** CONFIRMED · raised at HIGH, final MEDIUM

**Finding.** S2 deletes the `Records live under …` sentence, which is the record selector's only liveness assertion and the anchor of roughly ten selftest arms; S4 rewrites only the TUPLE-INDEX arms, so the sentence's arms and its live migration helper are left unaddressed and a mis-segmented record selector becomes indistinguishable from a build with no records.

**Evidence.** tools/memory-tree/gen_build_index.py:650-656 — the renderer's own comment: "`strip_records_sentence` manages it and several arms detect a mis-segmented record selector by noticing the sentence went missing, so replacing it would remove the thing they watch." gen_build_index.py:1629 — an arm literally named `the derived folder sentence SURVIVES the table (nine arms depend on it)`. gen_build_index.py:784 RECORDS_SENTENCE, :788 `strip_records_sentence`, :1078 called on EVERY write. Spec 5 S4 names only "the SELFTEST arms that address the three trailing regions by TUPLE INDEX"; §4 Files touched lists no arm work for the sentence. With spec 5 AC5 rendering an explicit empty case for both joins, a selector that matches nothing renders the same bytes as an honest empty build.

**How the skeptic settled it.** This is the half of SEQ-6 that no gate catches, and it reproduces. `gen_build_index.py:650-653` renders the sentence only `if build["kinds"]`, so its ABSENCE is what a mis-segmented record selector looks like — which is exactly what the source comment at :654-656 says the arms watch: "`strip_records_sentence` manages it and several arms detect a mis-segmented record selector by noticing the sentence went missing, so replacing it would remove the thing they watch", with the arm at :1629 named "the derived folder sentence SURVIVES the table (nine arms depend on it)". `RECORDS_SENTENCE` is :784, `strip_records_sentence` :788, called on every write at :1078. Spec 5 S2 deletes the sentence with the table; S4 addresses only the tuple-index arms and §4 Files touched schedules no arm work for the sentence. The liveness loss is real and gate-invisible: after unit 5 the surviving signal is spec 5 AC5's two coverage joins, and a selector matching nothing renders `Ids no record names: <every id>` — byte-identical to an honest build holding no records. That is the class AGENTS.md §7 names directly ("A probe that cannot move says so. A signal with no liveness assertion reports a reassuring zero when it is broken"), and spec 5's own §5 risk list names only two hazards, neither of them this. Held at MEDIUM rather than HIGH: it degrades a diagnostic, it does not stop the build or produce a wrong merge verdict.

**Fix.** Extend spec 5 S4 to name the sentence-keyed arms and the fate of `strip_records_sentence`, and replace the deleted liveness assertion with an explicit non-empty-population assertion over the record selector, so the two joins cannot report clean by matching nothing.

### UNB-3 — 2026-08-24-spec-dFramedEntrypoint-5.md · S2 (and §4 Files touched)

**Status:** CORRECTED · raised at BLOCKER, final MEDIUM

**Finding.** S2 leaves `strip_records_sentence` in the render path with nothing re-rendering what it removes, turning it from a de-duplicator into a silent prose deleter for any future author. Latent, not immediate: no tracked build README carries an authored copy today. The pre-existing wrapped/duplicated-anchor refusals are unchanged by S2.

**Evidence.** tools/memory-tree/gen_build_index.py:654-656, the code's own warning: "The sentence above is KEPT rather than replaced: `strip_records_sentence` manages it and several arms detect a mis-segmented record selector by noticing the sentence went missing, so replacing it would remove the thing they watch." The remover runs unconditionally on every build README inside `plan()` at :1078 (`text = strip_records_sentence(read_text(path), b["readme"])`); its own docstring at :788-806 says the whole-document scan is deliberate and that leaving a claim un-removed is a refusal. It also raises `Problem` on a wrapped or duplicated anchor (:834-852), so a future author's prose can hard-fail `--write` for a sentence the generator no longer answers.

**How the skeptic settled it.** The code facts hold: `strip_records_sentence` runs unconditionally in the render path at gen_build_index.py:1078 inside plan(); its docstring at :788-806 makes the whole-document scan and the two refusals deliberate; the comment at :654-656 states the derived sentence is KEPT because the remover manages it. No spec in the set mentions strip_records_sentence or RECORDS_SENTENCE (grepped all eight). So S2 leaves a remover whose replacement no longer renders. Two corrections. First, the Problem raise on a wrapped or duplicated anchor (:834-852) is PRESENT behaviour, not something S2 introduces, so 'can hard-fail --write' is not a new consequence. Second, the live blast radius today is zero: all 62 tracked build READMEs carry the sentence only inside the generated region, so the remover currently has no authored subject and the harm is latent — it lands the first time an author writes the sentence after S2.

**Fix.** Add an S-item choosing one: retire strip_records_sentence with RECORDS_SENTENCE/RECORDS_ANCHOR and their arms in the same commit as S2, or keep the derived sentence and delete only the table.

### SPECREC-6 — 2026-08-24-spec-dFramedEntrypoint-6.md · §2 S1 against §2 S5, AC4 and §4 Migration

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** S1 and S5 specify opposite populations for the same region. S1 renders `gen:spec-records` only into specs 'named by at least one record binding'; S5 and AC4 require a spec named by NO record to render the region with an explicit empty-case line. Under S1 that spec gets no region at all — the exact 'absent region reads as coverage' failure S5 is written against.

**Evidence.** spec-6:17-18 'rendered into every tracked spec that carries a status header and is named by at least one record binding'; spec-6:31-32 'a spec named by no record renders the region with an explicit empty-case line, never an absent region'; AC4 'When a spec is named by no record, its gen:spec-records region renders the empty-case line.' The §4 Migration figure ('creating 248 regions') is the S1 population; the S5 population is every header-carrying spec (268 at HEAD, 260 at base 9ddcc5c9, measured by matching HDR_RE over `git ls-files 'memory/builds/*/spec/*.md' 'memory/builds/*/spec/units/*.md'`).

**How the skeptic settled it.** Reproduced. The two populations are stated in the same section: spec-6:15-16 S1 renders the region 'into every tracked spec that carries a status header and is named by at least one record binding', while spec-6:26-27 S5 requires that 'a spec named by no record renders the region with an explicit empty-case line, never an absent region', restated as AC4. Under S1 such a spec receives no pair at all, which is exactly the 'absent region reads as coverage' state S5 is written against, and S6's create-on-write asymmetry does not reconcile them because --write is the thing S1 scopes. The figures check out: 268 of 273 tracked spec files carry a conforming status header (measured by matching the header regex over `git ls-files 'memory/builds/*/spec/*.md' 'memory/builds/*/spec/units/*.md'`), against the 248 record-named ids spec-6 S4 states and the '248 regions' its §4 Migration prices — so roughly 20 header-carrying specs fall in the gap.

**Fix.** Delete the 'and is named by at least one record binding' clause from S1, restate the population as every header-carrying spec, and correct the Migration figure from 248 to the header-carrying count.

### UNB-14 — 2026-08-24-spec-dFramedEntrypoint-6.md · S1 against S5 / AC4

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** The unit declares two incompatible populations for the same region. S1 renders it into "every tracked spec that carries a status header and is named by at least one record binding"; S5 and AC4 require a spec named by NO record to render the region with an explicit empty-case line, "never an absent region". Under S1 such a spec never gets a pair, so under S6's create-on-write asymmetry it never gets the empty-case line either — the exact state S5 forbids. A builder cannot tell which population to implement.

**Evidence.** spec 6 S1 "...and is named by at least one record binding"; S5 "a spec named by no record renders the region with an explicit empty-case line, never an absent region"; AC4 "When a spec is named by no record, its `gen:spec-records` region renders the empty-case line"; S6 "`--write` creates the pair where it is missing and `--check` never demands one". The corresponding build-README behaviour is unconditional per region (gen_build_index.py:1082-1086), i.e. binding count is not a creation predicate there.

**How the skeptic settled it.** The contradiction is in the spec's own words. S1 renders the region into 'every tracked spec that carries a status header and is named by at least one record binding'; S5 requires 'a spec named by no record renders the region with an explicit empty-case line, never an absent region'; AC4 restates it. Under S1 such a spec never gets a pair, and under S6's create-on-write asymmetry (which the shipped behaviour at gen_build_index.py:1082-1086 and the S1c skip at :1093-1095 confirm — creation is unconditional per region, binding count is not a creation predicate) it never gets the empty-case line either. S5's own rationale — 'an absent region is indistinguishable from a spec nobody has recorded against' — is the exact state S1 produces, so this is not a reading that can be reconciled. A builder cannot tell which population to implement, and the two choices differ by roughly the whole unbound-spec corpus.

**Fix.** Pick one: drop 'and is named by at least one record binding' from S1 so the population is every tracked spec with a status header, or drop S5/AC4's empty-case requirement and say plainly that an absent region means 'not yet written'.

### UNB-5 — 2026-08-24-spec-dFramedEntrypoint-6.md · S6 and §4 Migration

**Status:** CONFIRMED · raised at BLOCKER, final MEDIUM

**Finding.** S6 and §4 Migration promise "the corpus converges as specs are written rather than in one surgery" because "creating 248 regions in one commit is a diff nobody can review". `--write` has no per-file or per-path scoping: it is a whole-tree verb, and `create_missing` inserts every registered region into every member of the artifact set in one run. Implemented as specced, the first `--write` anyone runs after this unit lands creates all ~248 `gen:spec-records` pairs at once — exactly the commit the spec says it avoids. No S-item ships a filter.

**Evidence.** tools/memory-tree/gen_build_index.py:1661-1666 `main()` reads only `argv[1]` as mode, rejects anything outside the four verbs, and ignores `argv[2:]` entirely — there is no path argument. `do_write` at :1160 calls `plan(root, conf, create_missing=True)` and writes every artifact; `plan`'s create loop at :1082-1086 iterates `GEN_REGIONS` over every member unconditionally. Any build-README edit already forces a `--write` because hygiene check 9 (check-memory-hygiene.sh:590-595) delegates to `--check` and reds on staleness.

**How the skeptic settled it.** Reproduced exactly. main() at gen_build_index.py:1662-1666 reads only argv[1] as mode and never touches argv[2:]; do_write at :1160 calls plan(root, conf, create_missing=True) over the whole tree; the create loop at :1082-1086 walks GEN_REGIONS for every build unconditionally, and the S1c skip at :1093-1095 is a RENDER skip, not a CREATE skip. Hygiene check 9 at check-memory-hygiene.sh:589-595 delegates to `--check` and fails on drift, so any build-README edit forces a --write. Nothing in spec 6 ships a path filter (S1-S7 checked). So §4 Migration's 'the corpus converges as specs are written rather than in one surgery' and its stated reason ('creating 248 regions in one commit is a diff nobody can review') are both unachievable as specced. Severity is MEDIUM, not BLOCKER: the unit still works, the resulting diff is mechanical and re-verifiable by the write verb exactly as unit 5's is. The real operational cost is that the 248-region commit lands on whoever next runs --write for an unrelated reason.

**Fix.** Either spec a path-scoping argument to --write as an S-item with its semantics, or drop the incremental-convergence claim and state the one-shot corpus write with unit 5's atomicity reasoning.

### SURGERY-5 — 2026-08-24-spec-dFramedEntrypoint-7.md · §4 Alternatives rejected ("One corpus-wide surgery commit"), and §10

**Status:** CORRECTED · raised at HIGH, final MEDIUM

**Finding.** Spec-7's Alternatives-rejected and §10 paragraphs conflate two plans — the roster-wrapping half that never fired and the six-file slot-contract surgery that landed — and lean on the retirement cause (readers removed) that spec-8 S3 and the build's own verification declare false.

**Evidence.** memory/builds/aRuledFrontispiece/build/2026-08-17-build-TOOL-aRuledFrontispiece-11-corpus-surgery.md:1-30 — 'A 6-file authored diff is not reviewable from the diff alone, so this is the record spec 11's S6 requires', then the What-moved table listing aRuledFrontispiece, aSiftedPlaybook, aStandingWrit, aTimedTurnstile, cKeyedLaunchpad, cTracedPromise. The only 'retired in favour of removing the readers' claim is memory/map/features/build-readme-surface.md:99-102, which spec-8:22-24 S3 declares false ('one reader is live and deliberate') and which the verification confirms at reviews/…claim-verification.md:53-54. Spec 7 §10 then contradicts itself in one paragraph: 'planned, specced, and then abandoned' followed by 'the SHAPE recorded by the surgery unit that did land'.

**How the skeptic settled it.** The record reproduces and the paragraph is wrong, but both the spec and the finding conflate two different plans. Verified: memory/builds/aRuledFrontispiece/build/2026-08-17-build-TOOL-aRuledFrontispiece-11-corpus-surgery.md:1-30 opens 'A 6-file authored diff is not reviewable from the diff alone, so this is the record spec 11's S6 requires', derives its population from --check-format, and tables six files including two node-c builds — that surgery LANDED, with the per-file record as the remedy, not abandonment. Separately, a DIFFERENT corpus-wide plan genuinely never fired: memory/map/features/build-readme-surface.md:99-102 records that 'the roster-wrapping half of the corpus surgery never fired, and it is now MOOT' because 'TOOL-aBoundedVerdict-11 retires that pair's readers instead of wrapping the corpus'. That stated CAUSE is the false one — this build's own verification records 'TOOL-aBoundedVerdict-11 did NOT remove the readers … the spec was corrected at rev-8 to admit this', and spec-8 S3 exists to correct that very sentence. So spec-7's Alternatives-rejected paragraph and §10 rest a rejection on a dossier claim unit 8 of the same build declares false, while citing in the same breath the surgery that did land. Downgraded from HIGH: the decision to reject one corpus-wide commit still stands on the honest ground the finding names (49 terminal builds, 45 node-a builds); only the rationale is false.

**Fix.** Rewrite the paragraph to reject the single corpus-wide commit on the 49-terminal / 45-node-a ground, cite aRuledFrontispiece-11 as the landed shape it reuses, and drop the readers-removed premise unit 8 is repairing.

### UNB-16 — 2026-08-24-spec-dFramedEntrypoint-7.md · AC6

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** AC6 requires the pass to name "the terminal status token from its `**Status:**` header" when it declines a terminal BUILD. A build README carries no `**Status:**` header — that is the SPEC header grammar. A build's status is DERIVED from its units and rendered as `**Build status:**` inside the generated index region, with an optional front-matter `status:` key as the fallback. The AC names a field that does not exist on the object it grades.

**Evidence.** Of 62 tracked build READMEs, 1 contains a line matching `^\*\*Status:\*\*`, 62 contain `^\*\*Build status:\*\*`, and 7 carry a front-matter `status:` key. The renderer emits `**Build status:** {build['status']} · ...` at tools/memory-tree/gen_build_index.py:618-620; the value comes from `derive_status(units, fm, readme)` at :477, which falls back to the front-matter `status:` key when no unit carries a parseable header.

**How the skeptic settled it.** The measurements reproduce exactly. Of 62 tracked build READMEs, `git grep -c '^\*\*Status:\*\*'` matches exactly one (bConvergentLodestar), 62 carry '^**Build status:**', and 7 carry a front-matter `status:` key. The renderer emits '**Build status:** {build['status']}' inside the generated index region and derive_status() falls back to the front-matter key, so a build README has no '**Status:**' header — that is the SPEC header grammar. AC6 therefore grades a field the object does not have, and an implementer following it literally builds a decline branch that can never fire on 61 of 62 READMEs — which would silently admit terminal builds into a population spec 7 §3 forbids 'ever'. Kept at MEDIUM rather than LOW for that could-not-fire consequence against a hard non-goal.

**Fix.** Restate AC6 against the derived value and its source — the build's front-matter `status:` key when present, else the terminal status of every unit's own `**Status:**` header — and say which one the pass quotes as its reason.

### ABSENT-8 — CROSS · spec-1 §4 Inventory against spec-3 AC5

**Status:** CONFIRMED · raised at MEDIUM, final MEDIUM

**Finding.** The two specs give the registry reader opposite behaviour for the same condition. Unit 1 ships it 'returning an empty set when the registry file is absent' (a pass); unit 3 AC5 requires an absent registry to exit 1. Unit 3 lists only 'the registry reader shipped by unit 1 is pointed at this file' (S5) and never names the flip, so unit 1's S7 arm for that branch silently goes stale.

**Evidence.** spec-1:66-68 'This unit ships the reader as a function returning an empty set when the registry file is absent, so the predicate is complete before its population exists'; spec-3 AC5 'When memory/project/readme-contract.txt is absent, --check-format exits 1 naming the expected path rather than skipping.' Spec 1 S7 arms 'every trigger and every refusal'. The precedent unit 3 §10 cites already refuses: tools/memory-tree/check-method-carriers.sh:36-41 'no registry at $REG, and an absent registry is not an empty one'.

**How the skeptic settled it.** Reproduced. spec-1:66-68 ships the reader 'as a function returning an empty set when the registry file is absent, so the predicate is complete before its population exists', and spec-3 AC5 requires 'When memory/project/readme-contract.txt is absent, --check-format exits 1 naming the expected path rather than skipping' — opposite verdicts for the same branch of the same function. Spec-3 S5 says only that 'the registry reader shipped by unit 1 is pointed at this file' and never names the flip, and spec-3 S7's arm list (both directions, the exemption path, the pin, a malformed row, an out-of-class path) has no arm for the absent-file refusal AC5 asserts — so unit 3 carries an acceptance criterion with no declared arm while unit 1's opposite arm survives under S7's 'every trigger and every refusal'. That is a red selftest leg, not a theoretical mismatch. The cited precedent is verbatim: tools/memory-tree/check-method-carriers.sh:35-39 — 'An absent registry is the violation, not the exemption' / 'no registry at $REG, and an absent registry is not an empty one'.

**Fix.** State in spec-3 S5 that it REPLACES unit 1's absent-file arm with a refusal and deletes or inverts the corresponding selftest arm, and add that arm to spec-3 S7.

### ADOPT-1 — CROSS · spec-3 §6 AC5 and spec-2 §6 AC6

**Status:** CORRECTED · raised at BLOCKER, final MEDIUM

**Finding.** Only ONE of the two files is an adopter hazard. `tools/memory-tree/build-readme-slot-limits.txt` lives inside the kit home and ships to every adopter, so spec 2 AC6 is safe. `memory/project/readme-contract.txt` is outside the kit and unscaffolded, so spec 3 AC5's hard refusal does red a copy-installed adopter's `build README slot contract` leg with no repair path.

**Evidence.** tools/memory-tree/kit.toml `[[files]] include = "**" role = "engine"` ships gen_build_index.py to every adopter, and `[[gate_leg]] name = "build README slot contract" subject = "repo"` makes them RUN it. tools/memory-tree/adopt-memory-tree.sh:116-165 writes exactly six registries (`legacy-files.txt` … `method-carriers.txt`) and nothing else; its own comment reads "ALL SIX are written here". Spec 3 AC5: "When `memory/project/readme-contract.txt` is absent, `--check-format` exits 1 naming the expected path rather than skipping"; spec 2 AC6 does the same for `build-readme-slot-limits.txt`. Neither spec's §4 Files touched names adopt-memory-tree.sh.

**How the skeptic settled it.** kit.toml declares `home = "tools/memory-tree"` and `[[files]] include = "**" role = "engine"`, so every tracked file in that directory — gen_build_index.py AND any new `build-readme-slot-limits.txt` — is copy-installed together; the finding's premise that the limits file is unscaffolded is wrong. Confirmed `[[gate_leg]] name = "build README slot contract" subject = "repo"` at kit.toml:142-146 with `guard = []`, so adopters do run it. `adopt-memory-tree.sh:116-165` writes exactly legacy-files, curation-debt, id-orphan-waiver, corpus-path-unresolved, unarmed-branches and method-carriers under `$M/project/`, with the comment "ALL SIX are written here" — and neither spec's §4 Files touched names that script. Note the adopter surface already tolerates two gov-only registries (testsuite-count-waivers.txt, trace-waiver.txt are allowlisted but unscaffolded); what makes this one different is spec 3 AC5's REFUSAL on absence. Also worth flagging: spec 3 AC5 contradicts spec 1 §4 Inventory, which ships the reader "as a function returning an empty set when the registry file is absent". Not a BLOCKER — this repo lands green; the damage is to the next adopter of this kit version.

**Fix.** Either scaffold `readme-contract.txt` in `adopt-memory-tree.sh` inside unit 3, or reconcile with spec 1 §4 by keeping absent-registry = empty declared population ANNOUNCED on every run, with the hard refusal behind a declared opt-in key in `.memory-tree.conf`. Siting the registry under `tools/memory-tree/` resolves this and REG-1 at once.

### EPOCH-1 — CROSS · spec-5 §2 S6 and §7 vs spec-6 §2 S1/S4 and §7

**Status:** CONFIRMED · raised at HIGH, final MEDIUM

**Finding.** Same defect, with the ordering premise stated as an inference: the set declares one kit-version bump (spec-5 S6) and no spec declares an order value, so if unit 6 lands after unit 5 — which its own Goal sentence implies — the unguarded verdict-epoch leg reds until a second bump lands on unit 6's commit.

**Evidence.** tools/memory-tree/check-verdict-epoch.sh:15-19 — "Let W be the NEWEST commit in `<base>..HEAD` that moves a behaviour-bearing line of the engine, and S the NEWEST commit ... that actually CHANGES the value of `KIT_MEMORY_TREE_VERSION`. Then W must be an ancestor of, or equal to, S"; :69 `DELEGATES="… tools/memory-tree/gen_build_index.py …"`. Spec 5 S6: "the memory-tree kit version moves at every gated site"; spec 5 §7 lists `check-verdict-epoch.sh`. Spec 6 S1/S4 add a region, a renderer and an inversion to gen_build_index.py; spec 6 §7 does not list `check-verdict-epoch.sh`, and spec 5 §3 ("No SPEC-SIDE rendering of the records. That is unit 6") orders 6 after 5.

**How the skeptic settled it.** Reproduced. tools/memory-tree/check-verdict-epoch.sh:16-19 states the topological rule verbatim ('W must be an ancestor of, or equal to, S'), and its DELEGATES line lists tools/memory-tree/gen_build_index.py, so a change there demands the bump be at-or-after it. The leg is UNGUARDED in the manifest — {"name": "verdict epoch (kit version dates the engine)", "chunk": "declarations", "subject": "repo"} with no guard key — so it runs on every bar, and its default base is the mainline merge-base, meaning the whole build range is judged at once. Grep over the spec set for kit-version language returns exactly one declared bump: spec-5:29 S6 ('the memory-tree kit version moves at every gated site'); specs 1-4 and 6 name only check-kit-versions.sh, a different gate. Spec 6 S1/S4 add a region, an inversion and a renderer to gen_build_index.py and its §7 omits check-verdict-epoch.sh. One caveat the finding does not state: the build order is INFERRED, not declared — no spec carries an order value, and only spec-8's Migration fixes a position ('lands FIRST'). The inference is nonetheless well supported by spec-6:8 ('Unit 5 removes both listings from the build README; this unit puts each record where its reader is'). Severity re-graded from HIGH to MEDIUM: it reds the push boundary rather than stopping the build, and the remedy is one constant edit inside unit 6's own commit.

**Fix.** Either give every engine-touching unit's DoD the re-placed bump, or declare the order values spec-4 S6 promises and put the bump on the last engine-touching unit in that order.

### GATE-4 — CROSS · spec-2 AC8, spec-3 AC3 and AC7, spec-7 AC7 (each against §7 Gates)

**Status:** CORRECTED · raised at HIGH, final MEDIUM

**Finding.** Seven of eight specs list `build-index selftest` in §7 and none names GATE_SELFTESTS=1, so the bare `bash tools/run-gates/run-gates.sh` their ACs name holds that leg. Specs 1, 3, 5 and 6 are unaffected because each carries a direct `--selftest` AC; specs 2, 4 and 7, whose only claimed coverage of their new arms is that held leg, have no AC that actually executes them.

**Evidence.** tools/gate-legs.json:284-296 — `"name": "build-index selftest"`, `"chunk": "selftests"`, `"subject": "kit"`. tools/run-gates/run-gates.sh:741 `if [ "${subjects[$i]}" = kit ] && [ -z "${GATE_SELFTESTS:-}" ]; then` and :939 `printf 'GATE held  %s  (kit self-test, set GATE_SELFTESTS=1 to run)\n'`. AGENTS.md's own bar section: `GATE_FULL=1 GATE_SELFTESTS=1 ... # the complete bar, and what a DoD needs`. None of the eight specs names GATE_SELFTESTS anywhere.

**How the skeptic settled it.** The mechanism reproduces exactly. tools/gate-legs.json carries `{"name":"build-index selftest", "chunk":"selftests", "subject":"kit"}`; run-gates.sh:741 is `if [ "${subjects[$i]}" = kit ] && [ -z "${GATE_SELFTESTS:-}" ]; then printf 'ondemand'`, and :939 prints `GATE held ... (kit self-test, set GATE_SELFTESTS=1 to run)`. `grep -c GATE_SELFTESTS memory/builds/dFramedEntrypoint/spec/*.md` returns 0 for all eight, while the build's own research record 2026-08-24-build-...-enforcement.md:847 already recorded the standing rule — so the specs dropped an established fact from their evidence base. But 'every spec' and 'green over a bar that never ran a single new arm' both overstate. Spec 8 lists no selftest leg at all, and specs 1, 3, 5 and 6 each carry a direct AC invoking `python tools/memory-tree/gen_build_index.py --selftest` (AC6, AC7, AC6, AC7 respectively), which executes their arms regardless of the held leg. AGENTS.md's bar section also already binds `GATE_FULL=1 GATE_SELFTESTS=1` as what a DoD needs.

**Fix.** Add a direct `python tools/memory-tree/gen_build_index.py --selftest` AC to specs 2 and 4 (spec 7 adds no arms, so drop the leg from its §7), or spell the DoD command as `GATE_FULL=1 GATE_SELFTESTS=1 bash tools/run-gates/run-gates.sh` wherever an AC claims the bar is green.

### METHOD-1 — CROSS · spec-1 §4 Data model (the five-slot canon); no S-item in any of the eight specs

**Status:** CORRECTED · raised at BLOCKER, final MEDIUM

**Finding.** Both method-mandated writes lack a NAMED slot and are unmentioned in the entire spec set, but the method does not become un-obeyable: spec 1 S1 closes the heading set, not slot bodies, so both can land as body text inside `build-level rules` or `parked decisions`. The gap is that nobody has said so, and unit 2's budget then applies silently.

**Evidence.** memory/guides/BUILD-METHOD.md:38 — "**Classify, first match wins.** Write it into the build README before acting on it" (MISSING / THIN / FORKED / READY per unit). BUILD-METHOD.md:133 — "the run promotes and lands anyway and says so in its output AND the build README". The canon in spec 1 §4 is description · expected improvements · detriments · build-level rules · parked decisions, and `grep -n 'classif|MISSING|THIN|promot|runaway|BUILD-METHOD' memory/builds/dFramedEntrypoint/spec/*.md` returns only spec 1's S3 pointer line — neither obligation is named, housed or routed anywhere in the set. The owner's fork-2 ruling justified the two additions as covering "authored README content that the owner's four cannot hold" and enumerated only parks.

**How the skeptic settled it.** Both citations reproduced verbatim: `memory/guides/BUILD-METHOD.md:38` — "**Classify, first match wins.** Write it into the build README before acting on it" followed by the MISSING/THIN/FORKED/READY list; and :133 — "the run promotes and lands anyway and says so in its output AND the build README". The spec-set grep reproduces too: `grep -nE 'classif|MISSING|THIN|FORKED|promot|runaway|BUILD-METHOD' memory/builds/dFramedEntrypoint/spec/*.md` returns only spec 1's S3 line and its Files touched line. The canon in spec 1 §4 is the five owner slots and holds no home for either. The corpus already carries live instances — `memory/builds/aBoundedVerdict/README.md:275` is a `### Classification, per the build method's four states` heading with rows at :281-284 — so unit 7 S3's routing rule does reach the EXISTING ones case by case; what is unrouted is the RECURRING future obligation. Downgraded from BLOCKER because S1's own wording ("no HEADING outside the canon") leaves bodies open, so a home exists even unnamed, and because spec 1 S3 already opens BUILD-METHOD.md for editing in this build.

**Fix.** Either name the two homes in spec 1 §4 (classification and promotion notices are legal body content in `build-level rules`, budgeted), or amend `memory/guides/BUILD-METHOD.md` in unit 1 — which already edits it — to point both writes at a home the canon admits.

### SEED-4 — CROSS · spec-2 §2 S7 and §4 Migration against spec-3 §4 Migration and spec-7 §2 S5

**Status:** CONFIRMED · raised at HIGH, final MEDIUM

**Finding.** Three specs give three different answers to where unit 2's ceilings come from, and under the specced landing order the source is empty. Spec 2 S7 seeds from 'the declared population of unit 3'; spec 3's Migration lands that registry 'empty of bound rows and full of exempt rows'; spec 7 S5 says unit 7 seeds them. Unit 2 cannot ship numbers seeded from an empty set, yet its AC6 makes an absent limits file a hard refusal, so it must ship the file with values.

**Evidence.** spec-2:31-32 'the ceilings are SEEDED from the declared population of unit 3, not from the whole corpus'; spec-2:68-69 'The ceilings are seeded from the unit-3 population so the leg lands green over the tree it was written against'; spec-3:58-59 'The registry file lands empty of bound rows and full of exempt rows'; spec-7:26-27 'the ceilings of unit 2 are SEEDED from this measured population'; spec-2 AC6 'When tools/memory-tree/build-readme-slot-limits.txt is absent, --check-format exits 1 naming the expected path, rather than skipping.'

**How the skeptic settled it.** All five quotations reproduce verbatim: spec-2:31-32 ('the ceilings are SEEDED from the declared population of unit 3'), spec-2:67-69 ('seeded from the unit-3 population so the leg lands green over the tree it was written against'), spec-3:58-59 ('The registry file lands empty of bound rows and full of exempt rows'), spec-7:26-27 ('the ceilings of unit 2 are SEEDED from this measured population'), spec-2 AC6 (absent limits file is a hard refusal). Spec-1 S5 independently confirms the emptiness ('Until that unit lands, the predicate is written and exercised but wired to an empty declared population'). So at unit 2's landing the named source is empty under every ordering that keeps unit 7's S4/S5 downstream of unit 2, and AC6 forces the file to exist with rows anyway. The consequence bites the spec's own stated mitigation: spec-2 §5 names 'a budget that reds a third of the corpus on landing' as the dominant hazard and says 'S7 removes it by seeding from the declared population' — a mitigation that is inert at the moment it is claimed, and AC8's 'the bar is green over the declared population' is vacuously green until unit 7 lands, which is the green-by-absence class AGENTS.md §7 bans. Downgraded from HIGH: the remedy is a spec edit naming one owner for the seeding act; nothing is unbuildable.

**Fix.** Name unit 7 as the sole seeding owner, have spec-2 S7 ship declared placeholder ceilings, and make spec-2 AC8 state that its green is vacuous until unit 7 lands.

### SEQ-3 — CROSS · spec-2 S7 and §4 Migration vs spec-3 S5 and §4 Migration

**Status:** CORRECTED · raised at BLOCKER, final MEDIUM

**Finding.** There is no dependency CYCLE and no unsatisfiable ordering. What is real is narrower: one fact — who performs the ceiling seeding and from which population — has two answers, in spec 2 S7/§4 Migration and in spec 7 S5/AC5, and spec 2's named source (unit 3's declared population) is empty at unit 2's landing by spec 3's own design.

**Evidence.** spec 2 S7: "the ceilings are SEEDED from the declared population of unit 3, not from the whole corpus"; spec 2 §4 Migration: "The ceilings are seeded from the unit-3 population so the leg lands green over the tree it was written against". spec 3 S5: "the registry reader shipped by unit 1 is pointed at this file; the two verdict tiers of unit 2 grade only rows in the bound set" — a scope item that edits code unit 2 has not shipped. spec 3 §4 Migration: "The registry file lands empty of bound rows and full of exempt rows, one per existing README", and §4 Inventory: "Before that, the bound set is empty". So unit 2's seeding source is a zero-file population. The actual seeding is then claimed a THIRD time by spec 7 S5 ("The ceilings of unit 2 are SEEDED from this measured population") and AC5.

**How the skeptic settled it.** Quotes verified verbatim in all three specs. The cycle claim fails on spec 3 S5's second clause: "the registry reader shipped by unit 1 is pointed at this file; the two verdict tiers of unit 2 grade only rows in the bound set." The edit in that S-item is pointing UNIT 1's reader at the registry file; the second clause states the consequence for unit 2's tiers, which grade whatever population that shared reader returns. It is not an edit to code unit 2 has not shipped, so 3 does not depend on 2 and the plain numbered order 1 -> 2 -> 3 -> ... -> 7 is satisfiable: unit 1 ships the reader returning an empty set when the registry is absent (spec 1 §4 Inventory), unit 2 rides it, unit 3 supplies the data. The premise "Units 2 and 3 are specced as one parallel wave" is unsourced — NO spec in the set declares a wave or an order; `grep -nE 'wave' memory/builds/dFramedEntrypoint/spec/*.md` returns nothing and the build README's `gen:build-order` region reads "No spec under this build declares an `order` verb". The residue that survives is the duplicated seeding claim, which is this repo's own two-answers-to-one-question class; note also that spec 2 §2's ceilings are researched values ("32 of 61 READMEs exceed the sum of the researched ceilings today"), so unit 2 does not land with numbers derived from nothing.

**Fix.** Pick one owner for the seeding event. Recommend: spec 2 S7 states the RULE (seed from the declared population, never the whole corpus) and ships researched placeholder ceilings; spec 7 S5 owns the one seeding EVENT. Delete the seeding claim from spec 2 §4 Migration.

### SEQ-5 — CROSS · spec-5 S1/S2 vs spec-6 S6 and §4 Migration

**Status:** CORRECTED · raised at HIGH, final MEDIUM

**Finding.** The first half (218 records with no listing anywhere) is not established. What reproduces is spec 6's internal contradiction: S6 says `--write` creates the pair wherever it is missing, while §4 Migration claims the corpus "converges as specs are written rather than in one surgery" and that "creating 248 regions in one commit is a diff nobody can review" — but `--write` is corpus-wide and the spec names no scoped write verb, so the first `--write` after unit 6 lands produces exactly the 248-region commit it says it avoids.

**Evidence.** spec 5 S1/S2 delete `build-docs` and the records table (54 READMEs carry one today: `git grep -lF 'Record | Kind | Serves' -- "memory/builds/*/README.md" | wc -l` = 54). spec 6 S6: "`--write` creates the pair where it is missing and `--check` never demands one … so this region ships without re-rendering the spec corpus inside its own commit". But `do_write` calls `plan(root, conf, create_missing=True)` over EVERY build in one pass (gen_build_index.py:1160-1161, and the create loop at :1080-1084), so there is no per-file write verb. spec 6 §4 Migration therefore contradicts S6: "the corpus converges as specs are written rather than in one surgery … creating 248 regions in one commit is a diff nobody can review". Between unit 5 and that first `--write`, records are reachable only by directory listing.

**How the skeptic settled it.** Reproduced the engine behaviour: `do_write` at gen_build_index.py:1160-1161 is `artifacts, orphans, unmanaged = plan(root, conf, create_missing=True)` followed by an unconditional loop over every artifact; the create loop is at :1080-1084 and is per-build with no path filter. There is no `--write <build>` — `main()` at :1662 reads only `argv[1]` and the mode set is `(--check, --write, --check-format, --print-bindings)`, so no scoping argument is even parseable. Confirmed `git grep -lF 'Record | Kind | Serves' -- "memory/builds/*/README.md" | wc -l` = 54. The interval half fails because no build order is declared anywhere in the set (see SEQ-3), so 6-before-5 is as available as 5-before-6, and spec 5 §3 explicitly names the separation as a non-goal with its reason ("this unit is a deletion whose blast radius is the corpus, while that one is an addition whose blast radius is the spec canon") — a spec doing its job.

**Fix.** Give unit 6 a scoped write (`--write <build>` or a per-spec create predicate) so the stated convergence has a mechanism, or delete the incremental-convergence claim from §4 Migration and accept the one-time creation commit.

### UNB-6 — CROSS · spec 2 S7 / §4 Migration / AC8 against spec 3 §4 Migration and spec 7 S5

**Status:** CONFIRMED · raised at HIGH, final MEDIUM

**Finding.** Unit 2's ceilings are to be "SEEDED from the declared population of unit 3", but unit 3 lands with ZERO bound rows by its own design, and unit 7 is what seeds the population. So at unit 2's landing the seeding source is the empty set — there is no measured data to derive a ceiling from — while AC6 makes an absent limits file a hard refusal and AC1/AC7 require rows to exist. Unit 2 as specced cannot produce the numbers it must ship, and AC8 ("the bar is green over the declared population") is vacuous over an empty set.

**Evidence.** spec 2 S7 "the ceilings are SEEDED from the declared population of unit 3" and §4 Migration "seeded from the unit-3 population so the leg lands green over the tree it was written against"; spec 3 §4 Migration "The registry file lands empty of bound rows and full of exempt rows" and §4 Inventory "Before that, the bound set is empty"; spec 7 S5 "The ceilings of unit 2 are SEEDED from this measured population" — i.e. seeding happens in unit 7, not unit 2.

**How the skeptic settled it.** All three quotes verify verbatim: spec 2 S7 'the ceilings are SEEDED from the declared population of unit 3'; spec 2 §4 Migration 'seeded from the unit-3 population so the leg lands green over the tree it was written against'; spec 3 §4 Migration 'The registry file lands empty of bound rows and full of exempt rows' and §4 Inventory 'Before that, the bound set is empty'; spec 7 S5 'The ceilings of unit 2 are SEEDED from this measured population'. So two units claim the same seeding act and unit 2's declared source is the empty set at its own landing. AC8's 'the bar is green over the declared population' is then vacuous — the green-by-absence shape §7 of the charter names, only partly mitigated by unit 1 S5 making the emptiness announce itself. Downgraded from HIGH: unit 2 can still ship declared placeholder ceilings (S1 calls them 'declared data'), the mechanism lands and binds nothing until unit 7, and no AC is unsatisfiable — AC1/AC7 are demonstrable on the S8 selftest arms regardless of the bound population. The defect is one question with two answers in two specs.

**Fix.** Either declare interim ceilings in unit 2 from a stated provisional source and make unit 7 S5 an explicit re-seed with its own diff, or move the limits file's creation to unit 7 and have unit 2 ship only the reader plus the missing-file refusal.

## LOW — 34

### DRY-1 — 2026-08-24-spec-dFramedEntrypoint-1.md · §6 AC8 (against §2 S1-S7)

**Status:** CORRECTED · raised at MEDIUM, final LOW

**Finding.** AC8 names a flag (--dry-run) that no S-item builds and that the CLI swallows without a diagnostic, so the acceptance criterion depends on reporting affordances the scope never declares. It is not silently satisfiable — the near-misses requirement still forces new output — so the cost is an unbuildable-as-written AC, not a false green.

**Evidence.** tools/memory-tree/gen_build_index.py:1662 `mode = argv[1] if len(argv) > 1 else "--check"` and :1665 `if mode not in ("--check", "--write", "--check-format", "--print-bindings")` — argv[2] is never read, so a trailing `--dry-run` is discarded without a diagnostic. Spec 1 §2 S1-S7 add a canon tuple, a third trigger, a no-markers arm, a registry reader and selftest arms; no S-item adds a verb or flag.

**How the skeptic settled it.** Half reproduces, the consequence does not. Verified at tools/memory-tree/gen_build_index.py:1662 `mode = argv[1] if len(argv) > 1 else "--check"` and :1665 the four-mode membership test; argv[2] is never read, so a trailing --dry-run is discarded with no diagnostic. And no S-item in spec 1 §2 adds a verb or flag. But the stated failure mode — 'recorded as met by a run that observed nothing it claims' — does not follow. AC8's observable is that the output 'lists hits AND near-misses', and today's --check-format prints violations only, never near-misses, so the literal command produces output that visibly fails the criterion rather than silently satisfying it. The defect is a scope/AC mismatch, not a silent pass.

**Fix.** Add the hits-and-near-misses reporting arm to §2 as its own S-item (AGENTS.md §7 requires the pass anyway), or reword AC8 onto a one-off probe recorded in the acceptance ledger.

### GATE-2 — 2026-08-24-spec-dFramedEntrypoint-1.md · AC6

**Status:** CORRECTED · raised at HIGH, final LOW

**Finding.** `--selftest` prints no summary count line but does print one `arm ok` line per executed arm, so AC6 is verifiable by counting those lines; the real gap is the missing FLOOR_ASSERTIONS pin and the `*.test.sh`-only selector in check-testsuite-counts.sh, which leaves the stranded-arm class unratcheted for this file — a standing repo gap, not a defect introduced here.

**Evidence.** tools/memory-tree/gen_build_index.py:1655 `print(f"FAIL — {len(fails)} arm(s) failed")` and :1657 `print("PASS — gen_build_index: all arms held")` — the pass line carries no count; `grep -n "assertions\|FLOOR_ASSERTIONS" tools/memory-tree/gen_build_index.py` returns nothing. tools/check-testsuite-counts.sh:36 derives its population as `grep -oE '"[^"]*\.test\.sh"' "$MANIFEST"`, so a `.py` selftest named in tools/gate-legs.json:284-296 is never in it; that leg's own header (lines 7-10) records that the floor exists because one suite "printed a hardcoded PASS (130 assertions) for its whole life with no counter behind it" and shipped nine dead arms.

**How the skeptic settled it.** Half reproduces, half does not. Confirmed: gen_build_index.py:1657 prints `PASS — gen_build_index: all arms held` with no count, `grep -n 'FLOOR_ASSERTIONS' tools/memory-tree/gen_build_index.py` returns nothing, and tools/check-testsuite-counts.sh:36 derives its population as `grep -oE '"[^"]*\.test\.sh"' "$MANIFEST"`, so a `.py --selftest` leg is never in it. Refuted: 'reports no arm count at all' overstates it. The `arm()` helper at gen_build_index.py:1213 prints `arm ok    {label}` for every arm that runs, one line each, so the executed-arm count is directly observable (`--selftest | grep -c '^arm ok'`) and AC6 is verifiable as written. What is genuinely absent is a machine RATCHET — no FLOOR_ASSERTIONS pin and no membership in check-testsuite-counts' population — and that is a pre-existing repo condition these eight specs neither create nor claim to fix.

**Fix.** Optional: reword spec-1 AC6 to name the observable (`the count of `arm ok` lines rises by the number of arms added`). The FLOOR_ASSERTIONS/selector work is its own backlog row against tools/check-testsuite-counts.sh, not this build.

### GATE-3 — 2026-08-24-spec-dFramedEntrypoint-1.md · §4 Alternatives rejected — "Delegating the check to the hygiene shell gate"

**Status:** CORRECTED · raised at HIGH, final LOW

**Finding.** The spec's 'armable' claim is true and its deviation from check 21 is named and recorded; what it does not spell out is that the Python home forfeits the STANDING machine requirement that those branches stay armed, since check-arms.py reaches only tracked shell.

**Evidence.** tools/memory-tree/check-arms.py:236-256 (`do_check`: an unarmed, unpinned branch appends "has no POSITIVE assertion naming its own failure text ... and is not pinned") and :283-291 (per-gate ARMS_FLOORS comparison), with the floors declared at .memory-tree.conf:269 including `tools/memory-tree/check-memory-hygiene.sh:20:20`. tools/memory-tree/check-memory-hygiene.sh:655-658 states the precedent verbatim: check 21 "Delegates the PARSE to the sibling generator ... the shell owns the four fail branches, because check-arms.py discovers its population from tracked shell and cannot see a Python raise." The population predicate is check-arms.py's docstring: a tracked `*.sh` defining `fail() {`.

**How the skeptic settled it.** The cited sources reproduce — tools/memory-tree/check-arms.py's docstring scopes the population to a tracked `*.sh` defining `fail() {`, do_check at :239-256 requires a positive assertion per unarmed branch, :283-291 compares per-gate floors, and check-memory-hygiene.sh:655-658 states check 21's precedent verbatim — but they do not show the spec asserting anything false. Spec-1 §4 says 'the branches ARE armable', which is true: the file carries its own arm harness and its own selftest leg, and S7 plus AC1-AC4 mandate an arm for every new trigger and refusal. The spec also names the precedent it is deviating from and records the deviation explicitly ('The deviation from check 21's precedent is deliberate and recorded here rather than left to be rediscovered'), which is exactly the disclosure the finding's fix asks for. No false equivalence is stated; 'armable' and 'machine-required-to-stay-armed' are the finding's distinction, not the spec's claim.

**Fix.** One clause in spec-1 §4's rejection: the branches are armed by S7 but not machine-required to stay armed, because check-arms.py's population is tracked shell.

### MARKERS-18 — 2026-08-24-spec-dFramedEntrypoint-1.md · §4 Alternatives rejected ("Per-slot marker pairs")

**Status:** CONFIRMED · raised at LOW, final LOW

**Finding.** The cited record's two sentences reject two DIFFERENT alternatives, and spec 1 reads them as two reasons against one. The 'a heading is not a delimiter' sentence rejects heading-based slot detection — the very mechanism this unit adopts — not marker pairs. The paragraph also calls it 'Refused by the owner' when the text is a spec's own alternatives-rejected prose, with no owner ruling attached.

**Evidence.** memory/builds/aRuledFrontispiece/spec/2026-08-16-spec-TOOL-aRuledFrontispiece-1.md:156-158 — 'Bounding the prose by a marker pair of its own was rejected: it makes every README carry two more lines…'; :160-162 — 'Detecting the plan slot by heading text rather than by a marker was rejected because `check_authorization` … byte-compares a marker-delimited region across a base commit, and a heading is not a delimiter it can address.' Two separate paragraphs, two separate subjects.

**How the skeptic settled it.** Both halves reproduced. memory/builds/aRuledFrontispiece/spec/2026-08-16-spec-TOOL-aRuledFrontispiece-1.md carries two SEPARATE paragraphs under '### Alternatives rejected': :156-158 'Bounding the prose by a marker pair of its own was rejected: it makes every README carry two more lines to solve a problem that position already solves…' and :160-162 'Detecting the plan slot by heading text rather than by a marker was rejected because `check_authorization` … byte-compares a marker-delimited region across a base commit, and a heading is not a delimiter it can address.' Different subjects: the first rejects marker pairs, the second rejects heading-based detection — the very mechanism spec-1 adopts. spec-1:79-83 folds them into one paragraph as 'the second reason that record gives' against marker pairs. Second half also reproduces: `grep -n -i 'owner'` over that entire spec returns ZERO hits, and a repo-wide grep for the rejection's own wording finds only that one line, so 'Refused by the owner' (spec-1:79) attributes to the owner a rejection that is the cited spec's own prose with no ruling attached — a provenance defect under AGENTS.md §6 ('Non-obvious rules carry provenance inline'). LOW: it misreads a rationale, not a mechanism, and changes no deliverable.

**Fix.** Split the paragraph — keep 'two more lines' as the rejection of marker pairs, and move the expired heading-is-not-a-delimiter finding into its own sentence explaining why the heading canon is now legal. Drop 'by the owner' unless a ruling id can be cited.

### SEQ-11 — 2026-08-24-spec-dFramedEntrypoint-1.md · §4 Files touched, for S3

**Status:** CONFIRMED · raised at MEDIUM, final LOW

**Finding.** S3's pointer edit is scheduled against `memory/guides/BUILD-METHOD.md`, which is a rendered dogfood copy, not a source. Editing it without its template reds the `kit/dogfood doc parity` leg on unit 1's commit — the exact trap the same sentence avoids for HYGIENE.

**Evidence.** tools/memory-tree/kit-dogfood-parity.test.sh:53 — `PAIRS="$M/HYGIENE.md:$KITREL/HYGIENE.template.md $M/TEMPLATE-SPEC.md:$KITREL/SPEC-TEMPLATE.template.md $M/guides/BUILD-METHOD.md:$KITREL/BUILD-METHOD.template.md"`, with :30-32 "`--render` writes TEMPLATE -> LIVE … Edit the template, then re-render — never hand-edit the live copy" and :102-105 redding on drift. spec 1 §4 Files touched gets it right for one pair ("`tools/memory-tree/HYGIENE.template.md` edited FIRST with `memory/HYGIENE.md` re-rendered from it, because the parity harness renders the live copy from the template") and wrong for the next ("`memory/guides/BUILD-METHOD.md` for the S3 pointer"). §7 Gates lists "the hygiene parity harness" but not the doc-parity leg. The target sentence is real — BUILD-METHOD.md:73 "Two bounds: the README's GOAL…" — so the edit will be attempted.

**How the skeptic settled it.** Reproduced exactly. `tools/memory-tree/kit-dogfood-parity.test.sh:53` is `PAIRS="$M/HYGIENE.md:$KITREL/HYGIENE.template.md $M/TEMPLATE-SPEC.md:$KITREL/SPEC-TEMPLATE.template.md $M/guides/BUILD-METHOD.md:$KITREL/BUILD-METHOD.template.md"`, its header at :30-32 reads "`--render` writes TEMPLATE -> LIVE ... Edit the template, then re-render — never hand-edit the live copy", and :102-105 reds on drift with a `fix:` line. `tools/memory-tree/BUILD-METHOD.template.md` exists. The `kit/dogfood doc parity` leg in tools/gate-legs.json guards on `memory/guides/BUILD-METHOD.md` among others, so an edit to the live copy arms it. Spec 1 §4 Files touched does state the rule correctly for the HYGIENE pair ("edited FIRST ... because the parity harness renders the live copy from the template") and then names `memory/guides/BUILD-METHOD.md` — the rendered copy — for S3. The target sentence is real (BUILD-METHOD.md:73, "Two bounds: the README's GOAL statement is what a run may not amend"). Severity re-graded to LOW: the second half of the finding is wrong — spec 1 §7 Gates lists "the hygiene parity harness", and kit-dogfood-parity.test.sh IS that harness (it is the script that renders HYGIENE.md from HYGIENE.template.md and covers all three pairs), so the leg is named, just loosely. The residue is one wrong path in an estimate section, self-announcing on the first bar.

**Fix.** Change spec 1 §4 Files touched to `tools/memory-tree/BUILD-METHOD.template.md` edited FIRST with `memory/guides/BUILD-METHOD.md` re-rendered from it.

### SEQ-13 — 2026-08-24-spec-dFramedEntrypoint-1.md · AC8

**Status:** CONFIRMED · raised at LOW, final LOW

**Finding.** AC8 is written against a verb no scope item ships, and the engine will silently ignore it — so the acceptance run would print neither hits nor near-misses and still look like it passed.

**Evidence.** spec 1 AC8: "its output of `--check-format --dry-run` lists hits AND near-misses". `gen_build_index.py:1662-1666` — `mode = argv[1] if len(argv) > 1 else "--check"` then `if mode not in ("--check", "--write", "--check-format", "--print-bindings")`: extra arguments after argv[1] are never read, so `--dry-run` is discarded and the plain slot-contract verb runs. No S-item (S1-S7) adds a dry-run mode or a near-miss report.

**How the skeptic settled it.** Reproduced at the source. `gen_build_index.py:1662` is `mode = argv[1] if len(argv) > 1 else "--check"` and :1665 is `if mode not in ("--check", "--write", "--check-format", "--print-bindings")` — nothing reads `argv[2]` anywhere in `main`, so `--check-format --dry-run` runs the plain slot-contract verb and silently discards the flag, printing neither hits nor near-misses while exiting 0. Spec 1 AC8 names that invocation, and no S-item (S1-S7) ships a dry-run mode or a near-miss report. AGENTS.md §7 does require "Run a candidate gate predicate over the real tree before wiring it, and print hits AND near-misses", so the AC encodes the right obligation with a mechanism that does not exist and fails silently.

**Fix.** Either add the dry-run/near-miss verb to spec 1's scope, or re-word AC8 to name a scratch candidate script over `git ls-files` that records hits and near-misses.

### UNB-11 — 2026-08-24-spec-dFramedEntrypoint-1.md · AC6

**Status:** CONFIRMED · raised at MEDIUM, final LOW

**Finding.** AC6 says "the arm count the run reports has risen by the number of branches added". `--selftest` reports no arm count — it prints one `arm ok` line per arm and a single terminal `PASS` line with no number — so there is no count to compare, and no S-item adds one.

**Evidence.** tools/memory-tree/gen_build_index.py:1216 `print(f"arm ok    {label}")`; :1653-1657 `if fails: print(f"FAIL — {len(fails)} arm(s) failed"); return 1` / `print("PASS — gen_build_index: all arms held")`. `tools/check-testsuite-counts.sh` derives its population from `*.test.sh` argv strings in tools/gate-legs.json only, so it does not govern this Python selftest either.

**How the skeptic settled it.** Factually right: gen_build_index.py:1216 prints 'arm ok    {label}' per arm, :1655-1658 prints 'FAIL — N arm(s) failed' or the bare 'PASS — gen_build_index: all arms held' with no number, so no aggregate count is reported on the pass path. And no floor governs it — check-arms.py's population is discovered as tracked `*.sh` files that define `fail() {` (its own docstring), so this Python selftest is outside it, as is check-testsuite-counts.sh's `*.test.sh` population. Graded LOW rather than MEDIUM because the observable is one derivation away: `--selftest | grep -c '^arm ok'` yields exactly the count AC6 asks for, so unlike UNB-10 nothing looks green while proving nothing. The defect is that AC6 names a reported number that is not reported.

**Fix.** Restate AC6 against the derived count (`--selftest | grep -c '^arm ok'`) or against the new arm LABELS that must appear in the run's output, or add a counted floor as an S-item.

### BYTES-20 — 2026-08-24-spec-dFramedEntrypoint-2.md · §4 Inventory ("Bytes, not characters")

**Status:** CONFIRMED · raised at LOW, final LOW

**Finding.** The stated reason inverts its source. The hygiene entry cap is DECLARED in characters; what the source calls unpinnable is awk's `length()`, not the concept of a character. Unit 2 measures in Python over decoded text, where a character count is deterministic on every node — so the cited hazard does not reach it. Bytes may still be the right unit; the justification given is false.

**Evidence.** tools/memory-tree/check-memory-hygiene.sh:51-54 — 'Check 7's ENTRY budget, per class … **Characters, not bytes**: `length()` decides this verdict and whether it counts characters or bytes is a property of the awk build and the ambient locale, which check 7 deliberately does not pin.' Unit 2's measurement site is tools/memory-tree/gen_build_index.py, which reads decoded text (`read_text`), so `len()` is stable.

**How the skeptic settled it.** The cited source says the opposite of the spec's paraphrase, reproduced verbatim. tools/memory-tree/check-memory-hygiene.sh:51-54 reads 'Check 7's ENTRY budget, per class, on the same footing (TOOL-aDeclaredBound-1). Characters, not bytes: `length()` decides this verdict and whether it counts characters or bytes is a property of the awk build and the ambient locale, which check 7 deliberately does not pin', with `ENTRY_CAP_CHARS=300 ; BUILD_README_ENTRY_CAP_CHARS=350`. So the cap IS declared in characters, and what is unpinnable is awk's `length()` implementation — not the concept of a character. spec-2:61-64 inverts this into 'the per-line entry check in the hygiene gate already refuses to pin its locale … so a character measure would pass on one node and red on another'. That hazard cannot reach unit 2's measurement site: gen_build_index.py:124-128 `read_text` opens 'rb', decodes UTF-8 and normalises CRLF, so a Python `len()` over decoded text is deterministic on every node. A supporting reason the spec does NOT give is true — tools/template-size-limits.txt declares its grammar as '`<repo-relative-path>\t<bytes>` per row' — so the CHOICE of bytes is defensible; only the stated justification is false. LOW: no mechanism changes.

**Fix.** Rewrite the reason: bytes because the sibling declared ceiling in tools/template-size-limits.txt is in bytes and the budget is compared against on-disk size — not because a character measure would differ across nodes, which it cannot at this measurement site.

### LF-1 — 2026-08-24-spec-dFramedEntrypoint-2.md · §4 Files touched

**Status:** CONFIRMED · raised at LOW, final LOW

**Finding.** The new parsed data file gets an LF pin in gov's `.gitattributes` only; the kit's own `[[lf_pin]]` block, which is what emits pins into an adopter's tree, is not touched, so the file smudges to CRLF in every adopting checkout on a Windows fleet.

**Evidence.** tools/memory-tree/kit.toml's `[[lf_pin]]` entries cover `{memory_root}/**/*.md`, `**/*.toml`, `**/*.txt`, `.memory-tree.conf` and `{kit}/.memory-tree.conf.example` — nothing under `{kit}` matching `*.txt`. The precedent is in .gitattributes for the file unit 2 copies: `tools/template-size-limits.txt text eol=lf` — "the gate parses this file, and an unpinned path smudges to CRLF on this fleet". Spec 2 §4 Files touched names `.gitattributes` and not `tools/memory-tree/kit.toml`.

**How the skeptic settled it.** Reproduced. tools/memory-tree/kit.toml:192-205 carries exactly five [[lf_pin]] rows — {memory_root}/**/*.md, **/*.toml, {memory_root}/**/*.txt, .memory-tree.conf, {kit}/.memory-tree.conf.example — and nothing under {kit} matching *.txt, so the new tools/memory-tree/build-readme-slot-limits.txt gets no pin emitted into an adopting tree. The gov-side precedent is verbatim at .gitattributes:20-22: 'The DECLARED ceilings, same reason: the gate parses this file, and an unpinned path smudges to CRLF on this fleet. A person edits it, so it never gets a rewrite to normalise it back.' / `tools/template-size-limits.txt text eol=lf`. Spec-2 §4 names .gitattributes and not kit.toml. Worth noting the finding does not: unit 3's registry at memory/project/readme-contract.txt IS covered by the {memory_root}/**/*.txt pin, so the gap is spec 2's file alone.

**Fix.** Add a [[lf_pin]] row for {kit}/build-readme-slot-limits.txt to tools/memory-tree/kit.toml in unit 2, and name that file in §4.

### LOW-2 — 2026-08-24-spec-dFramedEntrypoint-3.md · §10 Reuse audit

**Status:** CONFIRMED · raised at LOW, final LOW

**Finding.** The reuse audit's load-bearing claim is false: method-carriers is called "the only registry in this tree that can red for a stale row as well as for a missing one". At least three others do, and one of them is a closer shape (a shrink-only pinned waiver list, which is exactly what S4 is building). A wrong uniqueness claim is why the closer precedent goes unread.

**Evidence.** tools/check-testsuite-counts.sh:78 reds on a waiver naming a now-compliant suite, and its comment names a third: "the same rule install-prefix-waivers.txt already carries". tools/memory-tree/check-arms.py:246-249 reds on a pin naming a branch that is now armed. memory/project/testsuite-count-waivers.txt:1-3 declares the same shrink-only-plus-stale-red contract spec-3 S4 wants.

**How the skeptic settled it.** Reproduced. Spec-3 §10 calls memory/project/method-carriers.txt 'the only registry in this tree that can red for a stale row as well as for a missing one'. memory/project/ holds eight registries, and at least two others red on the stale direction: memory/project/testsuite-count-waivers.txt's own header declares 'SHRINK-ONLY: a row leaves when its suite complies, and a row naming a compliant suite REDS as stale', enforced at tools/check-testsuite-counts.sh:78, whose comment names a third ('the same rule install-prefix-waivers.txt already carries'); and check-arms.py:239-243 reds a pin naming a branch that is armed now. testsuite-count-waivers is also the nearer shape for spec-3 S4, being a shrink-only exemption list with a stale-row refusal rather than a bidirectional population declaration.

**Fix.** Drop the uniqueness claim from spec-3 §10 and audit S4's exemption half against memory/project/testsuite-count-waivers.txt plus tools/check-testsuite-counts.sh, which is the closer precedent and supplies the stale-direction assertion GATE-8 is missing.

### SEQ-9 — 2026-08-24-spec-dFramedEntrypoint-3.md · AC3 against §3 Non-goals

**Status:** CORRECTED · raised at MEDIUM, final LOW

**Finding.** Spec 3 AC3 names the live bar over a conformed file while spec 3 §3 assigns conformance to unit 7, so the AC as worded is demonstrable only once one README conforms. It does not make unit 3 unacceptable — one conformance is available inside unit 3's own commit, and the other seven ACs are independently demonstrable.

**Evidence.** spec 3 AC3: "When a build README moves from an exempt row to a bound row and conforms, `bash tools/run-gates/run-gates.sh` is green and the exempt count printed by the `build README slot contract` leg has fallen by one." spec 3 §3: "No AUTOMATIC ENROLMENT of existing READMEs. A path joins the bound set when its build's owner conforms it, which is unit 7's work for the seed". spec 3 §4 Migration confirms the landing state carries zero bound rows. Unlike AC7, which explicitly says "each having been observed RED against a staged break first", AC3 names the full bar over the live tree.

**How the skeptic settled it.** Quotes verified: AC3, §3 ("A path joins the bound set when its build's owner conforms it, which is unit 7's work for the seed and the individual builds' work afterwards"), §4 Migration ("lands empty of bound rows"), and AC7's contrasting staged-break wording. But the finding overstates the consequence. Nothing in spec 3 forbids conforming ONE README as its own acceptance demonstration — §3 forbids AUTOMATIC ENROLMENT, which is a different act — and spec 3 S1 itself says the registry is "seeded from the conforming subset", which contradicts §4 Migration's empty-bound-rows landing and leaves the door open. Downgraded from MEDIUM because the repair is a wording change to one AC and no gate, mechanism or other unit is affected.

**Fix.** Re-word AC3 to AC7's shape (a staged conformance in a scratch tree, unstaged after), or name one specific README unit 3 conforms in its own commit to make the AC self-contained.

### GATE-9 — 2026-08-24-spec-dFramedEntrypoint-4.md · §7 Gates and §4 Files touched

**Status:** CORRECTED · raised at MEDIUM, final LOW

**Finding.** Spec-4's arm coverage for the order verb is already specced at §5 and AC6. The single real error is that §4 Files touched names memory/project/unarmed-branches.txt as the file to re-pin, when that file carries no row for check-memory-hygiene.sh; the pin that would need moving is ARMS_FLOORS in .memory-tree.conf:269 (`tools/memory-tree/check-memory-hygiene.sh:20:20`), and only if a fail branch is removed.

**Evidence.** `grep -n 'fail 12 "' tools/memory-tree/check-memory-hygiene.sh` returns exactly one hit, line 1038; the per-spec findings are awk prints into `bad12_raw` (lines 766-830). memory/project/unarmed-branches.txt holds three rows, all `tools/unattended/unattended.sh` (lines 12, 33, 34). .memory-tree.conf:269 `ARMS_FLOORS="... tools/memory-tree/check-memory-hygiene.sh:20:20 ..."`, and tools/memory-tree/check-arms.py:283-291 compares with `if got[i] < want[i]`, so a rise is never reported. tools/memory-tree/check-arms.py docstring: the population is tracked `*.sh` defining `fail() {`.

**How the skeptic settled it.** The mechanics reproduce: `grep -n 'fail 12 "' tools/memory-tree/check-memory-hygiene.sh` returns exactly one hit (line 1038), check 12's per-spec findings are awk prints aggregated into `bad12_raw`, memory/project/unarmed-branches.txt holds three rows all naming tools/unattended/unattended.sh, and check-arms.py:283-291 compares `if got[i] < want[i]` so a rise never reds. But the conclusion overstates. Spec-4 already mandates the compensating mechanism the finding asks for: §5 testing says 'a check-12 arm needs a fixture because no tracked spec carries the verb until S6 lands', and AC6 is that arm ('a spec whose header carries the verb twice, check 12 exits non-zero naming that header'). So the order-verb refusal is not left uncovered. Also, 'check-arms.py floors if check-12 branches move' is not false — a REMOVED branch does red the 20:20 floor — it is merely the wrong half of the mechanism for an addition.

**Fix.** Swap memory/project/unarmed-branches.txt for .memory-tree.conf's ARMS_FLOORS in spec-4 §4, and narrow §7's gate line to 'check-arms.py floors if a check-12 fail branch is removed'.

### ROWCOST-17 — 2026-08-24-spec-dFramedEntrypoint-4.md · §4 Inventory ("Measured cost is eight bytes") and AC8

**Status:** CONFIRMED · raised at LOW, final LOW

**Finding.** The row-cost arithmetic is wrong and mixes units. Eight bytes assumes both new cells are one ASCII character; the em-dash the spec itself specifies for the ORDER cell is three bytes, so the pair costs 10 bytes (12 if both render em-dashes as §5 claims). The cap it is measured against is BUILD_README_ENTRY_CAP_CHARS = 350 CHARACTERS, not bytes, and the source says so in as many words.

**Evidence.** Measured widest generated unit row over `git ls-files 'memory/builds/*/README.md'` inside the gen:build-units regions: 230 bytes / 228 characters, memory/builds/dUnstalledConvoy/README.md. Added-cell cost in UTF-8: ' — |' = 6 B, ' 2 |' = 4 B, so 230 -> 240 B and 228 -> 236 chars, not 238. Cap declaration: tools/memory-tree/check-memory-hygiene.sh:51-54 — 'Check 7's ENTRY budget … **Characters, not bytes**: `length()` decides this verdict'. The row format is gen_build_index.py:645.

**How the skeptic settled it.** Re-measured independently. Walking every `<!-- gen:build-units -->` region across `git ls-files 'memory/builds/*/README.md'`, the widest generated unit row is 230 bytes / 228 characters, in memory/builds/dUnstalledConvoy/README.md — so spec-4:57-59's base of 230 is the BYTE figure. The row format is `| link | status | rev-N | date |` (gen_build_index.py:645); inserting two cells adds `{order} | {tier} | `, which is 8 characters but 10 bytes when the ORDER cell carries the em-dash spec-4 itself specifies (U+2014 is 3 bytes in UTF-8). So the true pair is 228→236 characters and 230→240 bytes; the spec's '230 to 238' adds a character delta to a byte base. The cap it is compared against is characters, not bytes, and the source says so verbatim: tools/memory-tree/check-memory-hygiene.sh:51-54 — 'Check 7's ENTRY budget, per class … Characters, not bytes: `length()` decides this verdict …' with `ENTRY_CAP_CHARS=300 ; BUILD_README_ENTRY_CAP_CHARS=350`. Headroom is 114 characters, not '112 bytes'. LOW as claimed: the margin is ample under every reading, so no verdict changes — only the arithmetic and the unit label are wrong.

**Fix.** Restate in characters against the character cap: 228 → 236 of BUILD_README_ENTRY_CAP_CHARS=350, 114 characters of headroom, and name the measuring command in §4 as AC8 already requires.

### SEQ-2 — 2026-08-24-spec-dFramedEntrypoint-4.md · §4 Migration and §4 Files touched, against S4/S5

**Status:** CORRECTED · raised at BLOCKER, final LOW

**Finding.** Spec 4's `§4 Files touched (estimate)` omits `memory/builds/*/README.md`, which S4/S5 re-render across 55 files. It does NOT claim the corpus is unaffected, and the re-render IS carried by its acceptance criteria.

**Evidence.** `gen_build_index.py:640-645` renders the header row `| Unit | Status | Rev | Last change |` and sorts `key=lambda x: x["path"]`; `do_check` byte-compares every artifact — `elif read_text(path) != want: bad.append(f"{rel} (stale …)")`. That verb is a merge-bar leg: `tools/memory-tree/check-memory-hygiene.sh:595` — `if ! drift=$("$_PY" "$HERE/gen_build_index.py" --check 2>&1); then fail 9 …`, inside the unguarded `memory hygiene` leg. Measured: `git grep -lF '| Unit | Status | Rev | Last change |' -- "memory/builds/*/README.md" | wc -l` = 55. Contrast spec 5, which does carry `every tracked memory/builds/*/README.md` in its Files touched and makes atomicity an explicit S-item.

**How the skeptic settled it.** Reproduced the mechanics: `gen_build_index.py:640` emits `| Unit | Status | Rev | Last change |`, `:641` sorts `key=lambda x: x["path"]`; `do_check` at `:1122-1123` byte-compares (`elif read_text(path) != want: bad.append(f"{rel} (stale ...)")`); `check-memory-hygiene.sh:595` is check 9 and the `memory hygiene` leg in tools/gate-legs.json carries NO guard. `git grep -lF '| Unit | Status | Rev | Last change |' -- "memory/builds/*/README.md" | wc -l` = 55. But the finding quotes §4 Migration selectively: the full text reads "None for landed specs. The corpus renders exactly as it does today EXCEPT FOR TWO ADDITIONAL COLUMNS carrying an em-dash for every unit with no verb" — the corpus re-render is stated, not denied; "None" scopes to the landed SPECS (the §3 no-back-fill non-goal), not to the READMEs. And "no acceptance criterion carries it" is false: AC2 requires `gen_build_index.py --write` (corpus-wide by construction — `do_write` at :1160-1161 calls `plan(create_missing=True)` over every build), AC7 requires this build's own README to render a step table (again only after `--write`), and §7 Gates names `memory hygiene`, whose check 9 IS the byte-compare that would red. `memory/LIVE.md` is not affected at all — the sort and columns are unit-table only. Residue: one missing path in an estimate section, where spec 5 by contrast does list `every tracked memory/builds/*/README.md`.

**Fix.** Add `every tracked memory/builds/*/README.md` to spec 4's §4 Files touched. Drop `memory/LIVE.md` from the proposed fix; nothing in S4/S5 moves it.

### DEBTROWS-19 — 2026-08-24-spec-dFramedEntrypoint-5.md · §4 Alternatives rejected ("Keep the records table and delete only the inventory")

**Status:** CONFIRMED · raised at LOW, final LOW

**Finding.** 'The 17 over-cap generated lines … the population three curation-debt rows exist to waive' conflates lines with rows. Those 17 lines are waived by five rows; three rows DRAIN, and only when both regions go.

**Evidence.** reviews/…claim-verification.md:266 — the corpus carries 23 offending lines across 6 READMEs (aBoundedVerdict 10, dUnstalledConvoy 6, aRuledFrontispiece 3, cBriefedPilot 2, aDrainedSluice 1, aUnmannedHelm 1); records-table removal drops it to 6, so the 17 span five waived files. :263-264 — 'Three curation-debt rows drain — but only under BOTH removals … Under records-table-only, two drain.' Row count verified: `grep -vc '^\s*#\|^\s*$' memory/project/curation-debt.txt` = 7.

**How the skeptic settled it.** Reproduced against the authoritative verification and the registry itself. `grep -vc '^\s*#\|^\s*$' memory/project/curation-debt.txt` returns 7, and the rows are cBriefedPilot, aUnmannedHelm, aBoundedVerdict, aDrainedSluice, aRuledFrontispiece, memory/backlog/TOOL.md and dUnstalledConvoy. The verification (…claim-verification.md:266) measures 23 offending lines across 6 READMEs at HEAD (aBoundedVerdict 10, dUnstalledConvoy 6, aRuledFrontispiece 3, cBriefedPilot 2, aDrainedSluice 1, aUnmannedHelm 1), dropping to 6 after records-table removal — so the 17 lines that go are spread across FIVE waived files (aBoundedVerdict 5 of its 10, dUnstalledConvoy 6, aRuledFrontispiece 3, cBriefedPilot 2, aDrainedSluice 1), each holding its own debt row. The verification is equally explicit at :263-264 that 'Three curation-debt rows drain — but only under BOTH removals … Under records-table-only, two drain.' spec-5:82-83's 'the 17 over-cap generated lines … which is the population three curation-debt rows exist to waive' therefore equates a line count with a row count and attaches the both-removals drain figure to a records-table-only alternative. LOW as claimed: it is a precision error inside an alternatives-rejected paragraph and the alternative is rejected either way.

**Fix.** Say 'the 17 over-cap generated lines, spread across five waived files, of which three rows drain only when both regions go', citing the verification record.

### GATE-10 — 2026-08-24-spec-dFramedEntrypoint-5.md · AC2 (and AC1)

**Status:** CORRECTED · raised at MEDIUM, final LOW

**Finding.** AC2's command produces the passing observable (empty output) but exits 1 on that path, so the phrase 'returning zero' is ambiguous between exit code and match count; AC1 names no command at all.

**Evidence.** Measured in this worktree: `git grep -c 'Record | Kind | Serves' -- 'memory/builds/*/README.md'` prints one `path:1` row per carrier and exits 0; `git grep -c 'ZZZ-no-such-string-ZZZ' -- 'memory/builds/*/README.md'` prints nothing and exits 1. AGENTS.md §14 names the class: "A no-match grep exits non-zero and fails && chains — a PASSING zero-count check reads as failure; use a purpose-built check or terminate the probe with ; / || true."

**How the skeptic settled it.** The command behaviour reproduces exactly as claimed: in this worktree `git grep -c 'Record | Kind | Serves' -- 'memory/builds/*/README.md'` prints one `path:1` row per carrier and exits 0, while `git grep -c 'ZZZ-no-such-string-ZZZ' -- ...` prints nothing and exits 1. But 'AC2's verification command cannot produce the result it claims' does not follow. AC2 says the command returns 'zero across the corpus', which reads naturally as zero matches — and zero matches is precisely the passing state's output. The AC is loose about the exit code, not wrong about the observable. Nothing in spec-5 chains the probe into an `&&`, so the AGENTS.md §14 class the finding cites is not triggered as written. AC1's 'a repo-wide grep returning zero' naming no command at all is the weaker half and is a real verifiability gap.

**Fix.** Name the observable rather than the return: `git grep -lF 'Record | Kind | Serves' -- 'memory/builds/*/README.md' || true` producing empty output, and give AC1 the same shape with an explicit command.

### SEQ-6 — 2026-08-24-spec-dFramedEntrypoint-5.md · S2, with S4 and §5 risks

**Status:** CORRECTED · raised at HIGH, final LOW

**Finding.** Spec 5 S2 does delete a sentence several selftest arms assert on, and S4/§5 name only the tuple-index arms — but this is a gate-caught, grep-findable omission in an estimate, not the hidden hazard the tuple-index class was. The finding's substantive point (the lost liveness assertion) is SENT-1's, graded there.

**Evidence.** `gen_build_index.py:653` renders it; the comment immediately above at :654-656 says "The sentence above is KEPT rather than replaced: `strip_records_sentence` manages it and several arms detect a mis-segmented record selector by noticing the sentence went missing, so replacing it would remove the thing they watch." There is an explicit arm at :1629 — `arm("the derived folder sentence SURVIVES the table (nine arms depend on it)", "Records live under", …)` — plus counting arms at :1307 and :1365 whose expected value of `1` is satisfied by the derived sentence alone (stated at :1311), and :1341 `arm("the derived sentence names only the folders that exist", "Records live under `spec/`.", …)`. spec 5 S4 covers only "the SELFTEST arms that address the three trailing regions by TUPLE INDEX"; §5 risks names "the tuple-index selftest arms" as "the second hazard" and stops there.

**How the skeptic settled it.** Reproduced every citation: `gen_build_index.py:653` renders `Records live under {joined}.`; the comment at :654-656 says "`strip_records_sentence` manages it and several arms detect a mis-segmented record selector by noticing the sentence went missing"; the named arm is at :1629-1630; the two count-`1` arms are at :1307-1308 and :1364-1365, and the "satisfied by the DERIVED sentence alone" comment is at :1311; the folder-naming arm is at :1341. Spec 5 S4 does say only "the SELFTEST arms that address the three trailing regions by TUPLE INDEX", and §5 names "the tuple-index selftest arms" as "the second hazard". Severity re-graded down because spec 5 §7 explicitly lists `build-index selftest` as a gate and every one of these arms is findable by `git grep 'Records live under' tools/memory-tree/gen_build_index.py` — the exact opposite of the tuple-index arms, which S4 singled out precisely because "no marker grep can find" them. An implementer deleting :653 gets four red arms on the first selftest run.

**Fix.** Extend spec 5 S4 to "the selftest arms addressed by tuple index AND the arms keyed on the derived folder sentence", and state the fate of `strip_records_sentence` (:788, called on every write at :1078).

### TUPLE-9 — 2026-08-24-spec-dFramedEntrypoint-5.md · §3 Non-goals, third bullet

**Status:** CORRECTED · raised at MEDIUM, final LOW

**Finding.** spec-5 §3 third bullet states a mechanism that cannot occur — build-docs is the LAST GEN_REGIONS entry, so no surviving region's tuple position moves. The real breakage S4 absorbs is six literal `3`s addressing GEN_REGIONS inside do_selftest (gen_build_index.py:1453, 1460, 1463, 1465, 1467-1468, 1481). The renderers need no sweep: render_order (:702) and render_edges (:733) address indices 1 and 2, which are unaffected, and render_docs (:749) is deleted by S1.

**Evidence.** tools/memory-tree/gen_build_index.py:69-74 — GEN_REGIONS = (build-index, build-order, build-edges, build-docs); build-docs is index 3. The index-addressed sites are tools/memory-tree/gen_build_index.py:1452 `for _i in (3, 2, 1)` and :1459-1467 `for i in (1, 2, 3)`, all of which name 3 explicitly. The verification reproduced it: reviews/…claim-verification.md:251-252 'IndexError: tuple index out of range at line 1452'. Note the renderers are index-addressed too (render_order :702 `GEN_REGIONS[1]`, render_edges :727 `GEN_REGIONS[2]`), which S4 does not mention.

**How the skeptic settled it.** Half reproduced, half wrong. Reproduced: gen_build_index.py:69-74 declares GEN_REGIONS = (build-index, build-order, build-edges, build-docs), so build-docs IS index 3 and IS last — deleting it leaves indices 0/1/2 untouched, and spec-5:43-44 'only their tuple positions move' is therefore a false mechanism. Reproduced the real breakage: `grep -n GEN_REGIONS` finds hard-coded 3s at :1453 (`for _i in (3, 2, 1)`), :1460, :1463, :1465, :1467-1468 and :1481 — all in do_selftest, exactly what S4 names. The finding's own appended claim is wrong and self-defeating: it says the renderers 'are index-addressed too … which S4 does not mention', but render_order uses GEN_REGIONS[1] (:702) and render_edges uses GEN_REGIONS[2] (:733, not :727 as cited) — indices that do not move for precisely the reason the finding gives, so no renderer sweep is owed. Only render_docs addresses [3] (:749) and S1 deletes that renderer outright. So the fix as proposed would add scope that does not exist. Downgraded to LOW: the defect is one false sentence in a §3 non-goals rationale; S4 already carries the correct remedy, so no deliverable changes.

**Fix.** Replace the bullet with: 'Both survive and neither tuple position moves — build-docs is last. What breaks is every literal 3 addressing GEN_REGIONS inside the selftest harness, which S4 rewrites to address by name.' Do not extend S4 to the renderers.

### UNB-12 — 2026-08-24-spec-dFramedEntrypoint-5.md · AC2

**Status:** CONFIRMED · raised at MEDIUM, final LOW

**Finding.** AC2 verifies the records table's removal with `git grep -c 'Record | Kind | Serves'` "returning zero across the corpus". Eleven tracked non-README files carry that literal today — including this very spec — and unit 5 touches none of them, so the command can never return zero. It also exits 1, not 0, when it finds nothing, so "returning zero" is not the observable either.

**Evidence.** `git ls-files | grep -v "builds/[^/]*/README.md" | xargs grep -l "Record | Kind | Serves"` returns 11 paths, among them memory/builds/aBoundedVerdict/spec/2026-08-19-spec-TOOL-aBoundedVerdict-11.md:111, memory/builds/aSealedCaravan/RUN.md, tools/unattended/unattended.test.sh, tools/memory-tree/gen_build_index.py, and memory/builds/dFramedEntrypoint/spec/2026-08-24-spec-dFramedEntrypoint-5.md itself (1 match).

**How the skeptic settled it.** Both halves reproduce exactly. `git grep -lF -- 'Record | Kind | Serves' | grep -v 'builds/[^/]*/README.md'` returns exactly 11 paths, and all four the finding names are among them — aBoundedVerdict/spec/2026-08-19-spec-TOOL-aBoundedVerdict-11.md, aSealedCaravan/RUN.md, tools/unattended/unattended.test.sh, tools/memory-tree/gen_build_index.py — plus spec 5 itself, which unit 5 does not touch. And `git grep -c` on a no-match pattern prints nothing and exits 1 (verified against a nonsense string), so 'returning zero' is never the observable either way. Graded LOW rather than MEDIUM: the AC's intent is unmistakable and the error is self-announcing — a builder running it sees unrelated hits immediately, unlike UNB-10's silent clean line.

**Fix.** Scope the AC to the graded class and to the exit code, e.g. `git grep -lF -- '| Record | Kind | Serves |' -- 'memory/builds/*/README.md' ; test $? -eq 1`, and note the §14 no-match-exits-non-zero trap.

### UNB-13 — 2026-08-24-spec-dFramedEntrypoint-5.md · §3 Non-goals, third bullet

**Status:** CONFIRMED · raised at MEDIUM, final LOW

**Finding.** "No change to `gen:build-order` or `gen:build-edges`. Both survive; only their tuple positions move, which is what S4 exists to absorb." Their tuple positions do NOT move: `build-docs` is the LAST entry in `GEN_REGIONS`, so removing it leaves indices 0-2 untouched. The real hazard S4 is written for is an IndexError on `GEN_REGIONS[3]`, which the spec states correctly in S4 and then mis-explains here — and the mis-explanation invites a reviewer to look for shifted positions that do not exist while missing the production renderers that also index the tuple.

**Evidence.** tools/memory-tree/gen_build_index.py:69-74 — `GEN_REGIONS = (("build-index", ...), ("build-order", ...), ("build-edges", ...), ("build-docs", ...))`, so build-docs is index 3. `render_order` reads `GEN_REGIONS[1]` at :702 and `render_edges` reads `GEN_REGIONS[2]` at :733 — both unaffected by removing index 3. The selftest arms that DO break are `GEN_REGIONS[3]` / `for i in (1, 2, 3)` at :1460, :1463, :1465, :1467-1468, :1481.

**How the skeptic settled it.** The sentence is factually wrong. gen_build_index.py:69-74 puts build-docs LAST in GEN_REGIONS (index 3), so removing it moves no other entry's index, and it is also the last region in file order, so no file position moves either — the non-goal's 'only their tuple positions move' is false on both readings. render_order reads GEN_REGIONS[1] at :700 and render_edges reads GEN_REGIONS[2] at :726, both untouched by the removal; the arms that actually break are the trailing-index ones at :1459-1468 and :1481 (`GEN_REGIONS[i] for i in (1, 2, 3)` and `GEN_REGIONS[3][1]`), which is what S4 states correctly. LOW rather than MEDIUM: this is an explanatory clause in §3 that mis-describes a mechanism the S-item next to it states right, so it misleads a reviewer without changing what gets built.

**Fix.** Replace the sentence with the actual mechanism (an IndexError from the trailing GEN_REGIONS[3] address), and note that render_order/render_edges index the tuple in production code so removing a NON-terminal entry would have a wider radius.

### GATE-11 — 2026-08-24-spec-dFramedEntrypoint-6.md · S1, S6 and §4 Files touched

**Status:** CORRECTED · raised at MEDIUM, final LOW

**Finding.** Spec-6 does not name GEN_REGIONS and its §10 already points at the GEN_REGIONS-free seam; the real gap is that S6's create-on-write asymmetry for a SPEC-class region has no described mechanism, because the only existing one (plan()'s create_missing over GEN_REGIONS) is build-README-scoped.

**Evidence.** tools/memory-tree/gen_build_index.py:1146-1150 — `if create_missing: for _name, mo, mc in GEN_REGIONS:` inserts a missing pair into every build README; :1019 `here = names.index(mark_open)` over GEN_REGIONS, which raises ValueError for an unregistered marker; :985 `for _name, mo, mc in GEN_REGIONS:` is how slot_violations computes `first_open` and `inside`. The precedent is at :63-67: `gen:build-units` "is deliberately not in GEN_REGIONS: that tuple drives region CREATION and the canonical order check, and registering this one there would place the units table outside the region every existing reader brackets." spec-6 §2 S1/S6 and §4 Files touched say only "the region constants".

**How the skeptic settled it.** The GEN_REGIONS mechanics reproduce — gen_build_index.py:1080-1084 splices every GEN_REGIONS pair into every build README under create_missing, insert_region at :1018 does `names.index(mark_open)` and raises for an unregistered marker, slot_violations at :987 computes its span set from GEN_REGIONS, and the non-registration precedent with its reason sits at :63-67. But the hazard is one the reviewer supplies, not one the spec creates. Spec-6 never says GEN_REGIONS; §4 Files touched says 'the region constants', which is exactly the shape of the UNITS_OPEN/UNITS_CLOSE precedent, and §10 Reuse audit names `apply_region` as the seam — whose signature (`apply_region(text, region, readme, mark_open=..., mark_close=...)`) is GEN_REGIONS-independent by construction. The spec is silent on registration, not wrong about it. What is genuinely unspecified is the CREATION path S6 promises ('--write creates the pair where it is missing'), since create_missing today loops over builds and GEN_REGIONS and no spec-scoped equivalent exists.

**Fix.** Add one sentence to spec-6 §4 naming a spec-scoped region table and its own inserter, and cite the `gen:build-units` non-registration precedent at gen_build_index.py:63-67 so the next reader does not repeat the experiment.

### SEQ-10 — 2026-08-24-spec-dFramedEntrypoint-7.md · AC8 and §4 Inventory

**Status:** CORRECTED · raised at MEDIUM, final LOW

**Finding.** Spec 7 AC8 parks unroutable blocks in a parked-decisions slot of `memory/builds/dFramedEntrypoint/README.md` that will not exist until that README is itself conformed, and no S-item orders its own conformance first. The stale-base half of the claim is not a defect.

**Evidence.** `memory/builds/dFramedEntrypoint/README.md` carries its title at line 10 and its first generated marker at line 32, with no `##` heading in between — it is one of the 20 READMEs with no authored heading, so it has no parked-decisions slot. `git ls-tree HEAD -- memory/builds/dFramedEntrypoint/README.md` returns nothing while `git status --short` shows it staged as `A`, so the base 9ddcc5c9 that spec 7 §4 Inventory measured ("The corpus splits 12 live against 49 terminal") excludes it; once this build's folder commits, the split is 13 live of 62, and `memory/LIVE.md` already renders 13 rows in the staged tree. spec 7 S2 conforms "one commit per build folder" from the S1 derived candidate set and never names its own.

**How the skeptic settled it.** Reproduced: `grep -n '^#|^<!-- gen' memory/builds/dFramedEntrypoint/README.md` shows the title at line 10 and the first generated marker at line 32 with no `##` heading between, so the file today has no authored slot of any kind — it is one of the READMEs whose whole authored region is bare prose, which under spec 1 S1 ("no non-blank line between the title line and the first canonical heading") does not conform. `git ls-tree HEAD -- memory/builds/dFramedEntrypoint/README.md` is empty while `git status --short` shows `A`, and `git ls-files "memory/builds/*/README.md" | wc -l` = 62 with `memory/LIVE.md` carrying 13 rows — so the 61/12-live figures are the base figures, as the spec's own `base 9ddcc5c9` header declares. That is not a defect: a spec measures at its declared base, the delta is +1 file, and spec 7 S1's candidate report is explicitly "generated, never hand-kept", so it re-derives the population at run time and this build's own README enters the candidate set automatically. Only the AC8 chicken-and-egg survives.

**Fix.** Add an S-item (or one clause to S2) making `memory/builds/dFramedEntrypoint/README.md` the first conformance commit of the pass, so AC8's destination exists before any other build is touched.

### LOW-1 — 2026-08-24-spec-dFramedEntrypoint-8.md · S3 (and §2 scope)

**Status:** CORRECTED · raised at LOW, final LOW

**Finding.** gen_build_index.py:49-52 carries a stale RATIONALE, not the dossier's retirement claim: its rule ('this generator NEVER writes between these two markers') is still correct and still needed, but the reason it gives — check_authorization byte-comparing that slice — moved to the generated units region, which is the same expired fact spec-1 §4 relies on to declare its rejected alternative expired.

**Evidence.** tools/memory-tree/gen_build_index.py:48-51: "This generator NEVER writes between these two markers: the unattended kit's check_authorization byte-compares that slice across a run's pinned BASE". tools/unattended/unattended.sh:443-448: "NARROWED, not retired. Four of the authored pair's five readers moved to the GENERATED units region: the authorization scope comparison, --plan's malformed-pair refusal, and build-complete's presence and non-empty terms. The FIFTH stays - roster_ids". spec-8 §2 S3 names only memory/map/features/build-readme-surface.md; §3 non-goals do not exclude source comments.

**How the skeptic settled it.** Both citations reproduce. gen_build_index.py:49-52 asserts in the present tense that 'the unattended kit's check_authorization byte-compares that slice across a run's pinned BASE', while tools/unattended/unattended.sh:443-448 records that the authorization scope comparison moved to the generated units region and only `roster_ids` still reads the authored pair. Spec-8 S3 names only memory/map/features/build-readme-surface.md, and spec-8 §3 does not exclude source comments. But 'the same false claim' is inaccurate: the dossier's defect (line 55, 'it is being RETIRED' and its readers were removed) and the generator comment's defect (a stale RATIONALE for a rule that is still correct — the generator must still never write there, because roster_ids reads it) are two different sentences sharing one expired fact.

**Fix.** Add the gen_build_index.py:49-52 comment to spec-8 S3's list of sites with the correct characterisation (stale rationale, live rule), or state in §3 that source comments are out of scope and why.

### READPATH-DOSSIER-21 — 2026-08-24-spec-dFramedEntrypoint-8.md · §5 Production-readiness checklist, risks

**Status:** CONFIRMED · raised at LOW, final LOW

**Finding.** 'The decision append and the dossier edit both sit on the path this tree caps' — the dossier does not. Check 16's read path is derived from AGENTS.md's citations under the memory root and contains exactly six files; no codebase-map dossier is among them.

**Evidence.** corpus_ids.read_set over this tree returns 6 members: memory/DECISIONS.md, memory/LIVE.md, memory/guides/BUILD-METHOD.md, memory/guides/REVIEW-PROTOCOL.md, memory/guides/SESSION-KICKOFF.md, memory/guides/UNATTENDED-PROTOCOL.md. AGENTS.md cites `memory/map/features/` as a directory, which read_set discards because it is not a tracked file (tools/memory-tree/corpus_ids.py:352-357).

**How the skeptic settled it.** Reproduced by running the tool. `python tools/memory-tree/corpus_ids.py --report` in this worktree prints 'read path : 6 files, 132708 B (tracked-but-absent: 0)' and enumerates them: memory/DECISIONS.md (17 544 B), memory/LIVE.md (1 649 B), memory/guides/BUILD-METHOD.md (23 868 B), memory/guides/REVIEW-PROTOCOL.md (15 614 B), memory/guides/SESSION-KICKOFF.md (25 195 B), memory/guides/UNATTENDED-PROTOCOL.md (48 838 B). No codebase-map dossier is a member. The mechanism the finding names also holds: read_set (corpus_ids.py:352-362) admits a candidate only when `cand in tracked` AND `os.path.isfile(...)`, so AGENTS.md's citation of `memory/map/features/` as a directory is discarded. Dossiers are governed by a different budget — DOSSIER_CAP_BYTES=20480 under check 6 (check-memory-hygiene.sh:48-50). spec-8:86-88's 'The decision append and the dossier edit both sit on the path this tree caps' is therefore half true: the DECISIONS.md append is on it, the dossier edit is not. LOW: the real risk is correctly identified, the second file is not, and no acceptance criterion depends on the wrong half.

**Fix.** Name memory/DECISIONS.md as the one member at risk and state the measured margin from `corpus_ids.py --report`; note separately that the dossier is governed by check 6's DOSSIER_CAP_BYTES.

### CANON-1 — CROSS · spec-1 §2 S1 and §4 data model, against spec-7 §2 S3

**Status:** CORRECTED · raised at BLOCKER, final LOW

**Finding.** The canon closes headings, not classes, so M2:38 and M4:133 have a legal home in a canonical slot's body and the only genuinely homeless class (`## Units`) is the build's declared park. The residual defect is that spec 1 does not state where the roster pair may legally sit relative to the canon, given trigger 2's PLAN_CLOSE rule.

**Evidence.** memory/guides/BUILD-METHOD.md:33 'The roster is the build README's authored Units table where one exists'; :38 '**Classify, first match wins.** Write it into the build README before acting on it.'; :133 'the run promotes and lands anyway and says so in its output AND the build README.' Spec 1 S1 admits only the five canonical headings. `grep -n "BUILD-METHOD|M2|M4|classif" memory/builds/dFramedEntrypoint/spec/*.md` returns only spec-1:21/98 (the §S3 goal-bound pointer) — no spec mentions M2:38, M4:133 or the authored Units table. The verification record already flagged it: memory/builds/dFramedEntrypoint/reviews/2026-08-24-review-TOOL-dFramedEntrypoint-1-claim-verification.md:114-115 'UNDERSTATED — the proposed slot set drops at least five further mandated authored README classes beyond parks, none of which fits slots 3-6.'

**How the skeptic settled it.** The BLOCKER premise does not hold. Spec-1 S1 closes the HEADING set, not the content classes: 'the authored region … must consist of exactly the canonical slot headings, in canonical order, with no heading outside the canon and no non-blank line between the title line and the first canonical heading.' Nothing constrains what sits inside a canonical slot's body except unit 2's byte budgets. So M2's classification (BUILD-METHOD.md:38 'Classify, first match wins. Write it into the build README before acting on it') and M4's ceiling disclosure (:133) can both be written into the build-level-rules body without a heading of their own, and M3:73's goal statement is explicitly folded into the description by spec-1 S3. The one class that genuinely needs its own heading is the roster's `## Units` (verified live in memory/builds/aDeclaredBound/README.md:109 and memory/builds/aTetheredScratch/README.md:70) — and that is exactly what spec-1 §3 and spec-8 §3 name as this build's park, which under the grading rule is the spec doing its job. The cited verification line reproduces (reviews/…claim-verification.md, 'UNDERSTATED — the proposed slot set drops at least five further mandated authored README classes beyond parks, none of which fits slots 3-6') but it judges the EARLIER four-slot proposal and asserts fit rather than demonstrating a mechanical refusal. What does survive is smaller and different from the claim: spec 1 never states how the closed canon interacts with slot_violations' existing trigger 2, which fires on any authored line between PLAN_CLOSE and the first generated marker (gen_build_index.py:1002-1006) — so a conformed README carrying the roster pair must place that pair at the end of the LAST canonical slot, and no spec says so.

**Fix.** Add one sentence to spec-1 §4 fixing the roster pair's legal position (last authored bytes before the first generated marker) so the closed canon and trigger 2 cannot disagree.

### CROSS-2 — CROSS · spec-2 S7 + §4 Migration vs spec-3 §4 Migration vs spec-7 S5

**Status:** CORRECTED · raised at HIGH, final LOW

**Finding.** Spec 2 does not state what ceiling values tools/memory-tree/build-readme-slot-limits.txt carries at unit 2's own landing, when the population it names as the seed source (spec-3 §4 Migration: 'lands empty of bound rows and full of exempt rows') is empty until unit 7 seeds it via spec-7 S5.

**Evidence.** spec-2 §2 S7 and §4 Migration; spec-3 §4 Migration and §4 Inventory ("Before that, the bound set is empty"); spec-7 §2 S5 ("The ceilings of unit 2 are SEEDED from this measured population") and §6 AC5. Nothing in the eight specs declares a build order that could reconcile them: no spec status header carries the `order` verb yet (spec-4 S6 is the deliverable that would add it).

**How the skeptic settled it.** The 'three answers' half is wrong. Spec-3 §4 Inventory states 'The seed is the conforming subset measured at unit 7's landing', so 'the declared population of unit 3' (spec-2 S7) and 'this measured population' (spec-7 S5) name the SAME set from two sides — one by the registry that declares it, one by the pass that measures it. That is one answer, not two. The 'no build order could reconcile them' claim is also wrong: spec-4 S6 puts the order verb on this build's own eight specs and spec-8 §4 Migration already asserts a position in it ('The decision row lands FIRST in the build order'), so the ordering mechanism is in scope. What survives is narrow and is a documentation gap, not a contradiction.

**Fix.** Add one sentence to spec-2 §4 Migration saying the limits file lands with researched provisional ceilings over an empty bound set, and that unit 7 S5 re-seeds them from the measured population — which is what spec-7 AC5 already verifies.

### EXEMPT-7 — CROSS · spec-3 §2 S4 and §4 data model against spec-7 §2 S6

**Status:** CORRECTED · raised at MEDIUM, final LOW

**Finding.** Spec-7 S6 hard-codes as scope the header-reason form that spec-3 F2 leaves open for the owner, so a parked fork is resolved by another unit without saying so; the S4-vs-F2 tension inside spec 3 is parked and is not itself a defect.

**Evidence.** spec-3:20-22 S4 'each exemption row carrying its reason inline'; spec-3:47-48 'an exempt row carries its reason on the same line, because an exemption whose reason lives elsewhere is an exemption nobody can drain'; spec-7:28-29 S6 'builds at a terminal status are OUT of the population by declaration, with the reason recorded once in the registry header rather than per row'; spec-3 F2 makes the same header recommendation. Split measured at base: 12 live / 49 terminal (`grep -c '^| \[' memory/LIVE.md` = 12 against 61 tracked READMEs).

**How the skeptic settled it.** The quotations reproduce (spec-3:20-22 S4 'each exemption row carrying its reason inline'; spec-3:47-48 'an exempt row carries its reason on the same line, because an exemption whose reason lives elsewhere is an exemption nobody can drain'; spec-7:28-29 S6 'the reason recorded once in the registry header rather than per row'), and the 49/61 terminal split reproduces from the status sweep. But the header-reason form is spec 3's OWN F2 recommendation, sitting in its §8 Open questions and marked 'Owner decision' — a parked question, which under the grading rule is the spec doing its job, not a defect. What is left is narrower and real: spec-7 S6 states that parked answer as SCOPE, so unit 7 silently resolves unit 3's open fork in the direction opposite to unit 3's own S4, and spec-3 AC6 machine-grades row shape, so an implementer has no single grammar to build against.

**Fix.** Have spec-7 S6 cite spec-3 F2 as the decision it depends on rather than restating it, and record the resolution in spec-3 F2 if the owner has ruled.

### MAP-1 — CROSS · spec-1, -2, -3 and -4 §4 Files touched and §7 Gates

**Status:** CORRECTED · raised at MEDIUM, final LOW

**Finding.** Units 1 and 2 each ship a new module-level function in tools/memory-tree/gen_build_index.py and neither names memory/map/generated/ in §4 nor the codebase-map coverage+freshness leg in §7, so each lands with a byte-stale symbols.json and reds that leg. Units 3 and 4 are only at risk if their implementation adds a top-level symbol; the coverage/claim half of the map gate is not engaged at all.

**Evidence.** AGENTS.md §1 Definition of Done: "claim edits regen the generated artifacts in the same commit". tools/codebase-map/kit.toml:50-55 declares `[[gate_leg]] name = "codebase-map coverage + freshness" subject = "repo"`. tools/codebase-map/map_extractors.py:225 `"kit-py": lambda: _live_py("kit-py")` indexes every Python symbol under `tools/`, and .gitattributes pins `memory/map/generated/*` with the comment "The gate's freshness leg BYTE-COMPARES the generated artifacts against a fresh LF render". Spec 5 §2 S7 declares the regen; specs 1, 2, 3 and 4 name only "the `build-readme-surface` dossier" and omit the leg from §7.

**How the skeptic settled it.** The mechanism reproduces but the claim overreaches. Verified: memory/map/generated/symbols.json carries 35 top-level symbols from tools/memory-tree/gen_build_index.py (Problem, apply_region, slot_violations, render_docs, …), and tools/codebase-map/test_codebase_map.py:126-146 byte-compares the committed artifact against a fresh render ('assert committed == expected, f"STALE {path.name}"'), on an UNGUARDED repo-subject leg. So a unit adding a module-level function does stale symbols.json. But the AGENTS.md citation the finding leans on ('claim edits regen the generated artifacts') names the wrong mechanism: the inventories are coarse (gate-legs, kits, git-hooks, workflow-scripts, skill-engines, rendered-skills, gotcha-classes, guides, backlog-shards, lexicon-verbs — verified from inventories.json), Python symbols are NOT claimable inventory keys, so nothing about coverage or claiming is engaged. And the four-unit scope is not established: unit 1 (Inventory: 'ships the reader as a function') and unit 2 (S6's --bump plus --report, which follow the do_* verb pattern) certainly add top-level symbols; units 3 and 4 may add none — unit 3's assertions can live inside do_check_format and unit 4's change is 'one dict key, two rendered cells and one sort key' by its own §10. Neither unit 3's new file (memory/project/readme-contract.txt) nor unit 4's memory/TEMPLATE-SPEC.md edit touches any inventory.

**Fix.** Add memory/map/generated/ to §4 and the codebase-map coverage+freshness leg to §7 in units 1 and 2, and make the same addition conditional on a new top-level symbol in units 3 and 4.

### RECALL-1 — CROSS · §10 Reuse audit of all eight specs

**Status:** CONFIRMED · raised at MEDIUM, final LOW

**Finding.** No spec records the memory-recall terms the build method mandates in §10, so M7's regrounding step 5 cannot be performed for any unit in this build.

**Evidence.** memory/guides/BUILD-METHOD.md:145-147 — "Write into the spec's §10 the seam you will extend, cited by path, or an explicit 'no existing seam fits' with the evidence — **and the recall terms you used**, because composing them is the expensive half and M7 re-runs the query"; BUILD-METHOD.md:205 — reground step 5 is "Re-run the recall probe with the terms recorded in that spec's §10". `grep -n 'terms|memory-recall|reuse_lookup' memory/builds/dFramedEntrypoint/spec/*.md` returns exactly one hit — spec 1 line 167, a `reuse_lookup.py` phrase — and no `--terms` anywhere in the set.

**How the skeptic settled it.** Reproduced. memory/guides/BUILD-METHOD.md M5 says verbatim: 'Write into the spec's §10 the seam you will extend, cited by path, or an explicit "no existing seam fits" with the evidence — and the recall terms you used, because composing them is the expensive half and M7 re-runs the query'; M7's regrounding step 5 is 'Re-run the recall probe with the terms recorded in that spec's §10'. `grep -n 'terms' memory/builds/dFramedEntrypoint/spec/*.md` returns ZERO hits across all eight files, and the only probe citation anywhere is spec-1:167, a reuse_lookup phrase with no --terms. So M7 step 5 is unperformable for every unit. Two things reduce the severity from the claimed MEDIUM: M5 says the obligation is satisfied 'once for the SET, not per spec', and the substantive half — the seam cited by path, or an explicit 'no existing seam fits' with evidence — IS present in all eight §10 sections (spec-7 §10 and spec-8 §10 both verified). Nothing that gets built changes; the cost is that a regrounding session must recompose the query.

**Fix.** Add the 8-14 recall terms to §10 once for the set and repeat the line in each spec, since M7 reads the spec in front of it.

### ROSTER-1 — CROSS · spec-1 §2 S1 vs spec-1 §3 Non-goals vs spec-7 §2 S1/S3

**Status:** CORRECTED · raised at BLOCKER, final LOW

**Finding.** Conformance evicts the `## Units` HEADING, not the roster pair and not its reader, so spec 1 §3's park and spec 8 §3's DoD-term park both hold. The real residue is that no spec names the heading's destination or the position trigger 2 forces the pair into, and spec 7 already ships the escape for a carrier that cannot be conformed.

**Evidence.** Carriers measured: memory/builds/aDeclaredBound/README.md:109-121 (`## Units` + the pair, **Build status:** OPEN) and memory/builds/aTetheredScratch/README.md:70-80 (`## Units — the authored roster (M2)`, INPROGRESS). Spec 1 S1 requires the authored region to "consist of exactly the canonical slot headings ... with no heading outside the canon"; `## Units` is outside it. Spec 1 §3: "No change to the authored roster pair or to any reader of it." The reader is live: tools/unattended/unattended.sh:449 ROSTER_OPEN, :1487 `roster_ids()` ("This reads the AUTHORED pair, deliberately: it is the only carrier of a unit that is PLANNED and has no spec yet"), :1511 `missing_units()`, feeding `build-complete` term 3 at :2674ff. tools/unattended/PROTOCOL.template.md:423-425 — `--plan` "joins the build README's roster region against the tracked specs, so a planned unit nobody has specced is reported as MISSING". memory/guides/BUILD-METHOD.md:33 makes that table the M2 roster. Spec 7 S3 requires every evicted class to be routed; the roster pair appears in no spec's scope, non-goals or routing table.

**How the skeptic settled it.** Carriers and readers all reproduced: aDeclaredBound/README.md:109-121 (OPEN) and aTetheredScratch/README.md:70-80 (INPROGRESS), both in memory/LIVE.md; unattended.sh:449 `ROSTER_OPEN='<!-- roster:units -->'`, :1487-1492 `roster_ids()` gating on `grep -qF -- "$ROSTER_OPEN"`, :1511-1512 `missing_units()`; BUILD-METHOD.md:33 names the authored Units table as the M2 roster. But the reader keys on the MARKER, and spec 1 S1 closes the heading set ("no heading outside the canon") while leaving slot bodies free — so the pair moves into a slot body with its bytes untouched and every reader keeps working. BUILD-METHOD.md:33 also makes the table optional by construction: "The roster is the build README's authored Units table WHERE ONE EXISTS, else the conforming specs under `memory/builds/<slug>/spec/`". And spec 7 §5 ("a build whose README cannot fill a required slot from its own text stays exempt and is reported") plus F1 ("seed with the conformable subset and report the remainder as still-exempt") already cover a carrier that cannot be conformed — the spec doing its job. One thing the finding did not surface and which is worth the fix: `slot_violations` trigger 2 (gen_build_index.py:1001-1005) forbids authored lines between `PLAN_CLOSE` and the first generated marker, so a conformed carrier must place the pair as the very last authored content.

**Fix.** Name the pair's destination in spec 1 §4 — legal as body content in the FINAL canonical slot, heading dropped, marker bytes untouched, and last before the first generated marker per trigger 2 — and add the `## Units` heading to spec 7 S3's routed-class list.

### ROSTER-3 — CROSS · spec-7 §2 S1-S2 against spec-1 §3 and spec-8 §3

**Status:** CORRECTED · raised at HIGH, final LOW

**Finding.** The two live roster carriers are real and their `## Units` heading is outside the canon, but they are not stranded: they stay exempt rows under unit 3 and are reported by unit 7 §5. The residual is only that spec-7 §4's '12 live' figure implies a conformable population it does not name the exclusions from.

**Evidence.** memory/builds/aDeclaredBound/README.md:109 `## Units` with the pair at :111; memory/builds/aTetheredScratch/README.md:70 `## Units — the authored roster (M2)` with the pair at :75. Both appear in the twelve rows of memory/LIVE.md that spec 7 §4 calls the conformable population (`grep -c '^| \[' memory/LIVE.md` = 12). Spec 1 §3: 'No change to the authored roster pair or to any reader of it. That is this build's park.' The pair is load-bearing: tools/memory-tree/gen_build_index.py:53-54 defines it as PLAN_OPEN/PLAN_CLOSE and slot_violations trigger 2 keys on it (:1001-1005), and tools/unattended/unattended.sh:1487-1493 reads it.

**How the skeptic settled it.** The measurements reproduce; the 'nothing in the set says which' conclusion does not. Verified: 12 tracked build READMEs carry the roster pair (not 11), of which exactly two are non-terminal — memory/builds/aDeclaredBound/README.md (OPEN, `## Units` at :109, pair at :111) and memory/builds/aTetheredScratch/README.md (INPROGRESS, `## Units — the authored roster (M2)` at :70, pair at :75) — and both appear in memory/LIVE.md. The pair is load-bearing exactly as claimed (gen_build_index.py:53-54 PLAN_OPEN/PLAN_CLOSE, trigger 2 at :1002-1006; tools/unattended/unattended.sh roster_ids). But the set DOES carry a disposition, in three places: unit 3's whole point is an exemption section for files the contract cannot bind; spec-7 §5 states 'a build whose README cannot fill a required slot from its own text stays exempt and is reported'; and spec-7 F1 explicitly parks the seed-population question ('seed with the conformable subset and report the remainder as still-exempt with the missing slot named'). Spec-7 AC8 additionally covers an unroutable block by parking it rather than deleting it. A parked question is not a defect under the grading rule.

**Fix.** Add one line to spec-7 §4 naming the two roster carriers as structurally non-conformable while the pair is parked, so the seed figure is not read as a promise.

### SEQ-12 — CROSS · spec-1 S1 and §3 Non-goals vs spec-7 S2

**Status:** CORRECTED · raised at MEDIUM, final LOW

**Finding.** The canon closes HEADINGS, not body content and not marker pairs, so the `roster:units` PAIR — which is what every reader keys on — survives conformance untouched and spec 1 §3's park holds. What genuinely has no named destination is the `## Units` HEADING above it, plus the position the shipped trigger 2 forces the pair into. Duplicates ROSTER-1.

**Evidence.** spec 1 S1: the authored region "must consist of exactly the canonical slot headings, in canonical order, with no heading outside the canon"; the five slots in §4 Data model contain no roster slot. spec 1 §3: "No change to the authored roster pair or to any reader of it. That is this build's park." Measured carriers that are LIVE builds, hence in unit 7's population: `memory/builds/aDeclaredBound/README.md:109` `## Units` with the pair at :111-121, and `memory/builds/aTetheredScratch/README.md:70` `## Units — the authored roster (M2)` with the pair at :75-80. Both appear in `memory/LIVE.md`. spec 7 S2 conforms "one commit per build folder, each restructuring only that build's own README into the canon", and its §3 forbids only rewriting prose meaning, not moving the pair.

**How the skeptic settled it.** Carriers reproduced: `memory/builds/aDeclaredBound/README.md:109` `## Units` with the pair at :111-121 (Build status OPEN), `memory/builds/aTetheredScratch/README.md:70` `## Units — the authored roster (M2)` with the pair at :75-80 (INPROGRESS); both appear in memory/LIVE.md, and `git grep -lF '<!-- roster:units -->' -- "memory/builds/*/README.md"` returns 11. Spec 1 S1 and §3 quoted correctly. But S1's text is "must consist of exactly the canonical slot headings, in canonical order, with no HEADING outside the canon" — it closes the heading set, leaving slot BODIES free, and the roster pair is markers plus a table, not a heading. The reader keys on the marker: `tools/unattended/unattended.sh:1487-1492`, `grep -qF -- "$ROSTER_OPEN"` where `ROSTER_OPEN='<!-- roster:units -->'` (:449). So conformance drops a heading and keeps the pair, and neither the park nor the DoD term breaks. Additionally the shipped contract already knows the pair — `slot_violations` trigger 2 at gen_build_index.py:1001-1005 keys on `PLAN_CLOSE` and forbids authored lines between it and the first generated marker — which means the pair must be the LAST authored content, a constraint no spec states. Spec 7 §5 and F1 provide the escape for anything unconformable ("stays exempt and is reported").

**Fix.** State in spec 1 §4 that the roster pair is legal as body content inside the final canonical slot, heading dropped and marker bytes untouched, and add the `## Units` heading class to spec 7 S3's routed-class list.

### SEQ-7 — CROSS · spec-7 §4 Rollout vs spec-2 S7/S3 and spec-7 S5

**Status:** CORRECTED · raised at HIGH, final LOW

**Finding.** Spec 7 does not PIN when S5's seeding happens relative to the S2/S4 conformance commits. The Rollout's "every intermediate state is legal" holds if the seeding precedes them and is unproven if it follows. That is an unstated ordering, not a demonstrated falsehood.

**Evidence.** spec 7 §4 Rollout: "Every intermediate state is legal: the contract binds exactly the bound rows, so a half-conformed corpus is a smaller bound set, not a red bar." But spec 7 S4 moves rows to bound "in the same commit as the file they describe", while S5 seeds the ceilings from "this measured population" — i.e. after the pass. spec 2 S3: "A slot over its hard ceiling exits 1", and spec 2 AC6: an absent limits file "exits 1 naming the expected path, rather than skipping" — so unit 2 must have landed SOME ceiling values, seeded from the empty population of SEQ-3. The first bound file is therefore graded against a number derived from nothing.

**How the skeptic settled it.** Quotes verified: spec 7 §4 Rollout, S4 ("in the same commit as the file they describe"), S5 ("SEEDED from this measured population"), spec 2 S3 and AC6. But S5 says "this MEASURED population", and the population measured by this unit is S1's derived candidate report and §4 Inventory's 12-live split — both of which exist BEFORE any conformance commit. The S-item numbering is not asserted to be a temporal sequence anywhere in the spec, and spec 7's own §4 Migration lists what each per-build commit carries without placing the seeding after them. The claim that unit 2 lands with ceilings "derived from nothing" is also refuted: spec 2 §2 and §4 reference researched ceiling values ("32 of 61 READMEs exceed the sum of the researched ceilings today"), so the numbers exist independently of unit 3's row count.

**Fix.** Add one clause to spec 7 S5 or §4 Rollout pinning the seeding to the FIRST commit of the pass, derived from S1's candidate report, before any registry row moves to bound.

### UNB-15 — CROSS · spec 3 AC3 and AC8; spec 7 AC7

**Status:** CORRECTED · raised at MEDIUM, final LOW

**Finding.** Spec 3 AC3 and spec 7 AC7 name a run-gates run as the observation vehicle without naming where the count is read, while spec 2 correctly identified the problem (S5) and pointed its own AC3 at the persisted log. The counts are observable in <git-dir>/gate-logs/, so the ACs are imprecise rather than unobservable.

**Evidence.** tools/run-gates/run-gates.sh:946 `elif [ "$rc" = 0 ]; then c_ran=$((c_ran+1)); printf 'GATE ok    %s\n' "${names[$i]}"` — no output echo; :955 `printf 'GATE FAIL  %s  %s\n' ...; sed 's/^/    /' "$WORK/$i.out"` — the only echo site. Output IS persisted for every leg at :881-885 (`leg_log`, redacted, mode 600). Note also the `reuse` verb at :941/:850: a leg proven green with unchanged inputs is not re-run at all, so no fresh log exists for that run.

**How the skeptic settled it.** The runner facts are right — run-gates.sh:946 prints only 'GATE ok    %s' for a passing leg and :955 is the sole echo site, on failure — but 'cannot be observed' is wrong, and the finding itself supplies the refutation: every leg's output is persisted, redacted, mode 600, at :880-885 via leg_log(), so the bound/exempt/pin counts ARE readable after a green run. The reuse caveat is narrower than stated: the reuse branch only arms when GATE_REUSE is set (:839), so a default run always produces a fresh log. Also only two of the three cited ACs actually name run-gates — spec 3 AC3 and spec 7 AC7; spec 3 AC8 says 'When the leg runs', which a direct invocation satisfies.

**Fix.** Point those two ACs at a direct `python tools/memory-tree/gen_build_index.py --check-format` invocation or at the persisted per-leg log, as spec 2 AC3 already does.

## UNVERIFIED — no skeptic returned a verdict

### SELF-1 — 2026-08-24-spec-dFramedEntrypoint-7.md · §4 Inventory and §6 AC8

Raised at LOW. Carried as unverified rather than folded or dropped: a finding no skeptic graded is not a confirmed finding, and silently promoting it would make this record's precision figure a lie.

**Claim.** Unit 7's stated live population excludes this build's own README, and AC8 parks into a parked-decisions slot in that same README which no S-item creates — so the acceptance criterion depends on a slot whose existence is unordered with respect to the pass that makes it.

**Evidence.** Measured on the index today: `git ls-files 'memory/builds/*/README.md'` returns 62, of which 13 carry a non-terminal `**Build status:**` — the thirteenth is memory/builds/dFramedEntrypoint/README.md (SPECCED), whose authored region carries no `##` heading at all and therefore satisfies none of the canon. Spec 7 §4 Inventory: "The corpus splits 12 live against 49 terminal" (= 61, the corpus without this build's own README). Spec 7 AC8 names "this build's parked-decisions slot in `memory/builds/dFramedEntrypoint/README.md`".

**Proposed fix.** Name this build's own README explicitly in unit 7's population and sequence it first, so the park destination exists before AC8 can be exercised; re-state the Inventory split against the corpus that will exist when the pass runs.

## Refuted and dropped

Recorded so a later round does not re-raise them.

- **SEQ-4** (2026-08-24-spec-dFramedEntrypoint-2.md · AC3 and AC8) — REFUTED/DROP. The green-by-absence mitigation the finding says is missing already exists on the same leg and the same verb. Unit 2's budget check rides `slot_violations`' caller in `gen_build_index.py --check-format` (spec 2 S2) — the identical entry point unit 1 instruments, and spec 2 §7 lists the identical `build README slot contract` leg. Spec 1 S5 ("the leg says so on every run rather than reporting a clean count") and AC5 ("exits 0 AND prints a line stating the population is empty, so a green run is never mistaken for coverage") therefore cover unit 2's tiers too; a second announcement would be one fact in two places. AC8 is not vacuous either: it opens "When the ceilings are SEEDED and `run-gates.sh` runs", and per spec 7 S5 the seeding happens over a measured, non-empty population. AC3 is demonstrable by the staged-break discipline AGENTS.md §7 already mandates for every new gate ("Stage the break, confirm RED, unstage") — temporarily binding one over-high-water row exercises the advisory path; nothing in AC3 forbids it, and spec 2 S8 additionally requires a selftest arm for that exact branch. Not reproducing an independent defect here.
- **SEQ-8** (2026-08-24-spec-dFramedEntrypoint-4.md · S6 against §3 Non-goals) — REFUTED/DROP. Two independent refutations. (1) Spec 4 §3's non-goal reads "No back-fill of the verb onto landed specs. A ratified record is not rewritten." Its subject is the pre-existing corpus — the 260 tracked units the spec's §1 measures as carrying zero order verbs — not this build's own eight, which S6 names explicitly as the first adopters. A spec is not a ratified append-only record: every spec in this tree carries a `rev-N` and a §9 Revision log, its `**Status:**` header is rewritten on every status transition by design, and spec 8 S3 itself cites a spec that "was itself corrected". Adding a header field to a spec of the same build is a rev bump, not a rewrite of a ratified record. (2) The claimed build order does not exist. Nothing in the set declares waves or an order; spec 8 §4's "The decision row lands FIRST in the build order" is about the row appended to `memory/DECISIONS.md`, which is unit 8's product — it says nothing about spec 8 the document becoming immutable, and the build README's own `gen:build-order` region reads "No spec under this build declares an `order` verb". Also confirmed the header regex at check-memory-hygiene.sh:798 is prefix-anchored (no `$`), so a trailing order verb passes today — the mechanical concern behind the finding is already absent.
- **CROSS-1** (CROSS · spec-1 S5 vs spec-3 AC5) — REFUTED/DROP. The finding's load-bearing sentence — 'neither declares the change' — is false against the cited source. Spec-3 declares it twice: §5 error/empty states reads 'an absent registry file is a refusal naming the expected path. An empty bound set is legal and announced, which is the state this unit lands in', and AC5 is the binding form of the same statement. The two semantics are sequential stages of one design, not a contradiction: at unit 1's landing memory/project/readme-contract.txt does not exist yet, so an empty-set reader is the only shape that can land, and spec-1 S5 plus AC5 supply exactly the green-by-absence guard the finding asks for ('exits 0 AND prints a line stating the population is empty, so a green run is never mistaken for coverage'). Unit 3 then creates the file and makes absence a refusal. The finding's own proposed fix — 'have unit 1's empty-population announcement cover the file-present-but-no-bound-rows case instead' — is verbatim what spec-3 §4 Inventory and §5 already specify. Residual, not a defect: unit 3's absence-refusal is stated in §5 and AC5 rather than as its own S-item.
- **CROSS-3** (CROSS · spec-1 S1/S2 and §3 non-goals vs spec-7 S1/S2) — REFUTED/DROP. The evidence reproduces but the conclusion does not follow. `git grep -lF '<!-- roster:units -->' -- 'memory/builds/*/README.md'` returns 11 files; aDeclaredBound is OPEN and aTetheredScratch is INPROGRESS; and the pair does sit in the authored region (aDeclaredBound: `## Units` at 109, roster pair 111-121, first `<!-- gen:build-index -->` at 123). But the claim 'unit 7 must conform files it cannot conform' is false. Unit 7 conforms a DERIVED subset, not the live set: S1 is 'the build READMEs whose authored half can be conformed without inventing content, measured by a script that reports, per file, which canonical slots it can already fill ... and which it cannot'; §4 Data model has that report list 'the classes it carries that have no slot'; AC8 says 'When a block cannot be routed, the pass parks it ... and does not delete it'; and §8 F1 recommends seeding 'with the conformable subset and report[ing] the remainder as still-exempt with the missing slot named'. A roster carrier whose `## Units` block cannot be routed therefore stays exempt and is reported — a legal state under spec-3 S4. Spec-1 §3 also names the roster pair as this build's explicit park, which is the spec doing its job.
- **GATE-7** (2026-08-24-spec-dFramedEntrypoint-2.md · §5 error/empty states and §6 (all ACs)) — REFUTED/DROP. The announcement the finding asks for already exists and already covers unit 2, because unit 2 rides the same leg and the same run. Spec-2 S2 puts the budget check on 'slot_violations' caller in gen_build_index.py --check-format' — the single argv of the `build README slot contract` leg. Spec-1 S5 makes that leg announce the empty population 'on every run rather than reporting a clean count', §5 observability has it 'report the declared count and the graded count separately', and spec-3 §5 observability adds 'the leg prints the bound count, the exempt count and the pin on every run'. A green run of that leg over an empty bound set therefore prints the emptiness once, at the leg level, in the same stdout the budget check writes to. Adding a second per-check announcement would be the one-fact-in-two-places shape this spec set avoids by design.
- **UNB-7** (CROSS · spec 1 §4 Inventory against spec 3 AC5) — REFUTED/DROP. The two ACs do not collide. Spec 1 AC5 is conditioned on 'When the declared population is EMPTY', not on the registry file being absent — and that state is directly realisable with the file PRESENT, which is precisely the state spec 3 §4 Migration lands ('empty of bound rows and full of exempt rows'). So spec 1 AC5 remains satisfiable after unit 3 and nothing fails. What the finding actually found is a design-note sentence in spec 1 §4 Inventory (absent file -> empty set) that unit 3 supersedes, and spec 1 S5 already frames it as the interim: 'Until that unit lands, the predicate is written and exercised but wired to an empty declared population.' An interim disposition replaced by the unit that supplies the data is ordinary sequencing, and asking spec 3 S5 to say so is a wording preference.
- **UNB-17** (2026-08-24-spec-dFramedEntrypoint-6.md · AC8) — REFUTED/DROP. The AC does not assert the verb prints those aggregates. It says the counts 'are recorded FROM `--print-bindings` ... with the command that produced them' — provenance, and the trailing clause explicitly anticipates a pipeline. do_print_bindings at gen_build_index.py:443-474 emits S rows carrying the resolved id set per record plus the terminal N line, from which both figures are ordinary post-processing (sum the id fields; compare each id's slug against the record path's build folder). Asking the AC to spell that derivation out is a wording preference, which this review does not report.
- **PARK-1** (CROSS · spec-2 §2 S1/S3 vs spec-7 §6 AC8) — REFUTED/DROP. No collision is reproduced — one is predicted, and the spec set already carries three sanctioned remedies. (1) The ceilings are seeded from the measured population at unit 7's landing (spec-2 S7, spec-7 S5), so any park present at that time is under its ceiling by construction; the finding measures no park against any ceiling because no ceiling exists yet. (2) A ceiling is a declared row an owner edits with a reason — that is not a failure mode, it is precisely the tools/check-template-size.sh shape the owner ruled for in ruling 4, and spec-2 §3 names it ('it ships the mechanism that makes tightening a one-row diff') with F1 leaving the ratchet to owner edits. (3) Spec-7 §3 already gives the overflow shape for a block that will not fit a budget: 'Where a block must be summarised to fit a budget, it moves whole to its destination and the slot carries a pointer.' The premise that AC8 pushes whole blocks into the slot is also weak: AC8 says the pass parks 'the question and the reason', which is BUILD-METHOD M6's triple, not the block. Calling the owner's own ruled budget shape a defect is relitigating ruling 4.
- **SCAFFOLD-14** (CROSS · spec-1 §8 F1/F2 against spec-3 §2 S2 and spec-7 §2) — REFUTED/DROP. Refuted on the grading rule and on the text. (1) The scaffold question is not omitted — it is PARKED as spec-1 §8 F2 (:154-158) with both sides argued and a recommendation, which is a §8 fork doing its job, not a defect. (2) The claim 'no spec states the standing obligation unit 3 creates' is false on the spec's own words: spec-3 S2 (:16-18) reads 'every tracked memory/builds/*/README.md that is not named by a row and not named by an explicit exemption row reds, **so a new build cannot silently escape the contract**' — that clause states exactly the obligation the finding says is unstated, and spec-3 §3 second bullet adds 'a path joins the bound set when its build's owner conforms it, which is unit 7's work for the seed and **the individual builds' work afterwards**'. (3) The claim that unit 7 'can never measure' authoring friction is wrong on unit 7's own S1: its derived candidate report measures, per file, 'which canonical slots it can already fill from its own text and which it cannot' — a direct fillability measurement of the canon, which is the input F2's revisit condition asks for. (4) The ~2.9 builds/day figure reproduces (migration.md:187, :628, :648) but supports the spec rather than the finding: migration.md:648 already states 'every new member is born conforming', so the obligation is on record in this build's own evidence base. Nothing here survives independent reproduction as a defect.