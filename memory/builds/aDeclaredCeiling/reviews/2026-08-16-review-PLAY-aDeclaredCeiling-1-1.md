# Review — aDeclaredCeiling, round 1 (M4 spec-set audit, UNBUILT)

**Serves:** spec-audit PLAY-aDeclaredCeiling-1 TOOL-aDeclaredCeiling-1..3  <!-- inferred: an M4 pass over the four UNBUILT specs, per its own subtitle -->

## Verdict: BLOCKED

*Synthesis pass over the four UNBUILT specs and the build README at HEAD `8aa4a26`, BASE `96141ae`.
Four lenses -> batched skeptics -> this pass; **4/4 lenses live, 0 dead, 0 dead skeptic batches,
0 spurious / duplicate / conflicting verdicts, 0 unverified findings.** **48 raw judged — 28
confirmed, 20 refuted, 0 unverified.** The 28 confirmed collapse to **19 findings** after merging
six duplicate clusters (four lenses filed the `:464` anchor, two the recall corpus's second
enumerator, two the unreachable phase-less AC, two the missing `.memory-tree.conf` row, two the
wrong protocol-template path, two the "other three checks" undercount, two the misattributed leg
key). **Two findings below are this pass's own** and were not filed by any lens. Every row was
re-executed here against the working tree or an isolated clone before it entered the table —
nothing is inherited on a lens's word, and three lens impact claims were narrowed in the process.*

*The tree is green and stays green: `bash tools/unattended/check-unattended.sh` exits 0 silently,
`python tools/memory-tree/gen_build_index.py --check` reports clean over 42 artifacts. **Not one
finding below reds a leg today** — these are specs, and the whole point of an M4 pass is that the
cost lands at the end of a code pass instead. Two of the three blockers are late-discovery by
construction: they surface only when the unit's own headline acceptance criterion is run, after
everything else is written.*

**The refutation at the heart of `PLAY-aDeclaredCeiling-1` is CORRECT, and its anchor is not.**
Measured here: `WIRE-INTO-PROJECT.md:465` reads "the mechanical enforcement of the review protocol's
TWO rules: route fan-out through the cap-5 helpers, AND a review's verify stage spawns at most 5
agents TOTAL"; `memory/guides/REVIEW-PROTOCOL.md` binds exactly those two (`## The hard cap — ≤5
verify-stage agents TOTAL` at `:7`, `## Concurrency — ≤5 agents at once, always` at `:106`), and
`tools/hooks/agent-cap.js` numbers four IMPLEMENTATION rules (`// RULE 2` `:90`, `// RULE 3` `:346`,
`// RULE 4` `:549`, "Rule 1 above" `:92`) — a different population. The build's premise holds. What
does not hold is the line number the unit exists to publish: it is `:465`, not `:464`, at BASE and
at HEAD. That is `B1`, and it is first because the measurement IS the deliverable.

**Three blockers.** `B1` would write an off-by-one measurement into a CLOSED spec and a landed audit
report, in the exact drift class the lineage is about. `B2` aims the recall unit's entire scope at
the wrong one of two corpus enumerators, so the unit's central AC fails after everything is built.
`B3` states as current behaviour something the gate's source contradicts in its own comment, and
ties an AC to it that can only be ticked on the wrong gate failure.

## Findings

| id | sev | file:line | finding | fix |
|---|---|---|---|---|
| B1 | blocker | `spec/2026-08-16-spec-PLAY-aDeclaredCeiling-1.md:102` | **AC3 asserts an anchor that does not reproduce, and the anchor is the whole deliverable.** AC3 requires `grep -n "TWO rules" WIRE-INTO-PROJECT.md` to return "exactly one hit at `:464`" while §3:26 forbids editing the file. Measured at HEAD **and at BASE `96141ae`**: the single hit is `:465`; `:464` is the tail "rather than silently ignored. This is the". Walked the history — the line sat at 464 through `ac3d091` and moved to 465 at `d3bd21b` ("PLAY-aSiftedPlaybook-3"), i.e. one commit AFTER round 4's L1 corrected the same anchor `:463` -> `:464`. The stale `:464` is carried by six lines of this spec (`:7`, `:13`, `:26`, `:40`, `:99`, `:103`), by `README.md:20` and `:75`, and by the freshly-minted OPEN row `memory/backlog/PLAY.md:5`. A builder trusting the spec rather than re-measuring publishes an off-by-one into a CLOSED spec and a landed audit report, where the next reader finds it authoritative. This anchor has now gone stale twice in six days. | Re-measure at build time and write `:465` in all six spec lines, `README.md:20`, `README.md:75` and the backlog row. Then make the citation drift-proof: restate AC3 as "returns exactly one hit, and the refutation cites the line number that grep prints", and have S1/S2 quote the SENTENCE — which is stable — with the number as a convenience. §4:40's `464-466` span survives as written (the sentence does start at the tail of 464); say so explicitly so the next fold does not "correct" it. |
| B2 | blocker | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-2.md:79` | **§4 hooks the change to the one corpus enumerator none of the unit's ACs exercise.** "Where it hooks" calls `corpus_files()` at `extract.py:166` "the single place the corpus is enumerated". There are TWO: `extract.corpus_files(repo, rev)` (tracked-only, `rev`-pinnable, called once at `extract.py:497`) and `query.corpus_files(repo)` (`query.py:218`, tracked AND untracked-not-ignored), whose own docstring names the split — "``extract.corpus_files`` is tracked-only on purpose -- it is the MEASUREMENT path". The CLI indexes from the second: `files = corpus_files(repo)` at `:589` inside `ensure_cache`, `live_files = len(corpus_files(repo))` at `:1179`, then `_docs()` (`:291-301`) re-extracts locally with `E.extract_records`/`E.extract_chunks` under a comment stating "this CLI never runs that entry point and never reads its output dir -- it re-extracts here and indexes the result … the dead-plumbing class arriving through the door marked 'the gate already covers it'." `tools/memory-recall/query.py` appears nowhere in §4's Files-touched (`:87-93`). AC1, AC2, AC4 and AC5 all shell out to `query.py`. A unit built exactly to the table widens only the measurement/bench corpus; AC1 — the criterion the unit says it exists for — fails at the end of the build, and `corpus_digest` is computed from the query-side list so the cache never invalidates either. | Add `tools/memory-recall/query.py` to Files-touched and name the SECOND enumerator in §4 with the same both-branches reasoning. Better: put the declared-source resolution in ONE helper in `extract.py` that both enumerators call, and say so, so S2's extraction is reachable from `extract.main()` and `query.build_cache` alike. Also state how a declared non-`.md` path bypasses the `p.endswith(".md")` filter — present in BOTH enumerators (`extract.py:172`, `query.py:228`) and satisfied by no `.conf`. Add an AC observing the declared source in `query.py --stats`' `n_files`/digest, not only in the selftest. |
| B3 | blocker | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-3.md:128` | **AC4 is false against source, unreachable under S1's design, and has no scope item.** AC4 requires that "when a run-state file carries no readable `phase`, the region IS compared and a drift reds", and §5:107-109 rests on it. `check-unattended.sh:203-206` fails check 4 and `continue`s — `fail()` at `:40` sets status and RETURNS — so a phase-less file leaves the per-file loop long before the check-8 block at `:245-254`. Its own comment says it: "a file with no phase is outside every check keyed on one". S1 only adds a terminal-phase scope INSIDE check 8; it does not move the comparison above that `continue`. The consequence is a false green: the gate does red on a phase-less drifted file, but with `UNATTENDED check 4`, so an arm asserting only that the gate reds passes for the wrong reason, and an arm obeying this repo's discipline ("assert the SPECIFIC message, never the exit code alone") cannot be written at all. §5 also says "Armed in S4" while S4:27-29 names exactly two arms, neither of them this one. | Pick one and make S4's arm count and §5 agree with it. (a) Drop the claim: state in §5 that check 4's `continue` already excludes a phase-less file from check 8, and rewrite AC4 to observe THAT — `fail 4` fires, check 8 is silent. (b) Add a scope item moving the region comparison above the `continue` (or replacing it with a per-check guard) plus a third arm in S4, and keep AC4. Either way the arm asserts the check-8 message string. |
| H1 | high | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-1.md:22` | **S3 silently removes an env insulation that a landed unit built, verified and recorded as load-bearing, and no AC observes it.** S2 reasons about LAYERS ("this inserts one layer and removes none") — true of the script, false of the SUBJECT. Measured both directions today: `MAX_BYTES=999999 bash tools/check-template-size.sh skills/session-kickoff/SKILL.md 18432` gates at 18432; the same command with the positional dropped gates at 999999. After S3, one ambient `MAX_BYTES` sets the cap for BOTH gate subjects at once — `run-gates.sh` execs argv but inherits the caller's environment, and `.githooks/pre-push` runs the bar from whatever shell pushes. This falsifies `TOOL-aSiftedPlaybook-1` S1:19-24 ("insulated by construction … the alternative reading would have silently raised the kickoff engine's cap from 18 KiB to 48 KiB") and `memory/map/features/playbook.md:50-52`, which states it as a Constraint. AC3 asserts the behaviour that causes it. | Name the exposure in §4's blast radius and decide it deliberately. If the insulation matters, make the env layer per-subject (`MAX_BYTES_<key>`, or refuse a bare `MAX_BYTES` when a declared row exists for the subject) or rank the declared row above the bare env for non-default subjects. Add an AC: `MAX_BYTES=999999 bash tools/check-template-size.sh skills/session-kickoff/SKILL.md` still gates at 18432. |
| H2 | high | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-1.md:39` | **The limits file has no positional or env slot, so four of S6's arms and three ACs can only be exercised by mutating the tracked tree during a concurrent gate run.** S2 specifies resolution for the VALUE and none for the FILE. The gate already solved this for its sibling record and says why at `:55-58`: "The high-water record, resolved the same three ways `MAX_BYTES` is — positional `$3`, then the environment, then the tracked default — because a gate leg cannot set an environment variable and the self-test must point the gate at a scratch copy without writing the tracked one." Every ratchet arm uses it (`bash "$GATE" "$TMP/subj" 49152 "$HW"`, `:121-183`), the harness's stated discipline is "Nothing writes into the real tree" (`:26-27`), and A13 at `:193-197` exists to catch an arm that broke it. `run-gates.sh` schedules legs concurrently at width up to 8, so a leg that deletes or corrupts a tracked limits file mid-run corrupts siblings. AC4 (file absent) and AC5 (non-numeric row) have no other mechanism, and §5:136-138's headline-risk mitigation — "one of them uses a value deliberately unequal to the default" — has nothing to hang on. | Give the limits file the same resolution the record already has: a positional slot plus a `LIMITS` env, spelled in S2 and in the header usage block (an env-only override avoids a fourth positional). S6's four arms and AC1/AC4/AC5 then become scratch-file arms with no tracked-tree write. |
| H3 | high | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-2.md:121` | **§5 asserts as READ the one thing it did not read.** It states "the rendered recall Skill is unaffected because it documents the CLI's arguments, not the corpus membership. Verified by reading `SKILL.template.md`, not assumed." `tools/memory-recall/SKILL.template.md:31` reads "The corpus is the tracked `{{MEMORY_ROOT}}/` of `git rev-parse --show-toplevel` from the cwd" — a direct statement of corpus membership, and the exact sentence S1 falsifies; `:52` adds the `Grep` fallback "over `{{MEMORY_ROOT}}/`". The render carries both at `.claude/skills/memory-recall/SKILL.md:31` and `:52`. Neither file is in §4's Files-touched (`:87-93`), and AC8 cannot catch it: `adopt-memory-recall.sh --check` compares render to template, so both stay stale together and exit 0. This is the provenance class the build's own rule at `README.md:95-98` was written for, reproduced inside the sentence that promises it was not. | Add the template and the render to Files-touched; amend `:31` to say the corpus is `{{MEMORY_ROOT}}/` plus the files declared in `RECALL_EXTRA_SOURCES`; add an AC that the rendered Skill names the widened membership and that `--check` is green after re-rendering. Correct the §5 sentence to say what was actually read. |
| H4 | high | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-1.md:43` | **S7 leaves the map dossier's positional constraint bullet standing, and after S3 it is the last live carrier of a claim the unit falsifies.** S7 scopes three edits to `memory/map/features/playbook.md`: `[paths].globs`, an ADDITION to "Constraints & why", and moving the Gaps sentence out. It never names `:47-52`, the bullet in that same section which states as live fact that `tools/gate-legs.json` "runs the same script over `skills/session-kickoff/SKILL.md` at 18432, passed POSITIONALLY … Raising the default therefore cannot move the kickoff engine's cap — insulated by construction". S3 falsifies every clause. Repo-wide after S3/S4, `18432` survives outside `memory/builds/` only in `tools/gate-legs.json` and that bullet. No gate sees it: `grep -n "49152\|18432" tools/check-playbook-parity.sh` is empty, and `test_codebase_map.py` pins headings and `[claims]` keys, not prose. AC6 greps only `tools/gate-legs.json AGENTS.md`. (§10:204-207 does bind the Reuse affordance at `:94-98` by quoting it, so that carrier is covered — this bullet is not.) | Name `memory/map/features/playbook.md:47-52` explicitly in S7 as a CORRECTION, not an addition, and add an AC that greps the dossier for `18432` and for `positionally` and asserts each surviving occurrence describes the declaration rather than an argv. |
| H5 | high | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-3.md:136` | **S3's protocol edit has no observing AC, and AC7's inference is invalid.** AC7 reads "…it exits 0, and the shipped protocol template equals the installed protocol, **so S3's edit landed in both**". `adopt-unattended.sh:142-143` (and `:155`) is a plain `diff -q` between the installed copy and the shipped half — symmetric, so it exits 0 for two unedited files exactly as for two equally-edited ones. Read all nine ACs: AC1-AC5 observe the gate and its fixtures, AC6 the arms floor, AC7/AC9 parity and the bar, AC8 the reproduction. None reads a byte of `memory/guides/UNATTENDED-PROTOCOL.md` §2 — the sentence at `:56`, "The gate byte-compares it against a fresh render." A builder who implements S1/S2/S4 and skips S3 passes every AC and ships a gate whose behaviour contradicts the BINDING document governing it. | Add an AC that reads the text: `grep -n` the terminal-phase qualification in §2 and assert it names the terminal set and the reason, THEN assert parity via AC7 so the shipped half carries the same bytes. Parity is the second half of the criterion, never the whole of it. |
| H6 | high | `README.md:67` | **Unit 4's stated reason for being last is empty in both halves, and the real gap it hides is that the ceiling's new home is outside the corpus unit 4 widens.** README:67-69 says unit 4 "changes what the recall index READS, and unit 3 creates the first declaration that the change is supposed to make reachable … sequenced the other way, unit 4's acceptance would have to quantify over a declaration that does not exist yet". Measured: unit 4's acceptance quantifies over `READ_PATH_CEILING`, which exists at BASE at `.memory-tree.conf:71` (`git diff 96141ae..HEAD -- .memory-tree.conf` is empty), and TOOL-2 AC1/AC2 and §4:65-67 name that exact line. What unit 3 creates is `tools/template-size-limits.txt`, a `<path>\t<bytes>` TSV (TOOL-1 S1:15-21), which TOOL-2's design cannot admit: S1:19 seeds `RECALL_EXTRA_SOURCES` with `.memory-tree.conf` and `.unattended.conf`, S2:20-24 extracts `KEY=value` assignments, and a TSV has none. TOOL-1 §3:58-61 also refuses to declare the ceiling in `.memory-tree.conf`, so no conf declaration is created by unit 3 at all. After all four units land, the ceiling's justification sits in a file the build's own retrieval widening still cannot reach — the same miss TOOL-2 exists to close, aimed at the ceiling's NEW home, and neither TOOL-1 §3 nor TOOL-2 §3 records it. | Correct README:67-69 to state the real reason unit 4 is last (diff hygiene) and move the ordering claim to the constraint `H7` measures. Then close the substantive gap in exactly one place: either extend TOOL-2 S1's seed and S2's extraction to admit a `<path>\t<bytes>` declaration file with its comment block, with an AC naming `tools/template-size-limits.txt` by path — or record in TOOL-1 §3 as an explicit non-goal that the new declaration is deliberately outside the corpus, with the reason and a backlog row. |
| H7 | high | `README.md:64` | **The total order's one real dependency is the one the README declares absent.** README:64 calls unit 2 (`PLAY-aDeclaredCeiling-1`) "a records-only correction with no dependency", and README:57-62 justifies unit 1 going first by pointing at units 3 and 4. Reproduced in an isolated clone at HEAD: bumping only `PLAY-aSiftedPlaybook-1`'s status rev-8 -> rev-9 (exactly PLAY-1 S2, which AC2 requires) makes `gen_build_index.py --check` red naming `memory/builds/aSiftedPlaybook/README.md`; after `--write`, `bash tools/unattended/check-unattended.sh` exits 1 with "`UNATTENDED check 8 FAILED` … `memory/builds/aSiftedPlaybook/RUN.md`". Baseline in the same clone: exit 0, silent. `RUN.md` carries `phase: LANDED` and a frozen `rev-8` row, and the driver's check 26 refuses to re-splice a finished record — no legitimate exit. Units 3 and 4 touch nothing that feeds that README. So unit 2 is the ONE unit that cannot land before unit 1, PLAY-1 carries no `check-unattended.sh` line in §7 and no AC over it, and §5:87-89 names "editing a CLOSED spec" as the one real risk without this consequence. The order as written happens to be safe; the recorded reason for it is not, so a resumed or reordered run walks into the exact dead end unit 1 exists to remove. | Rewrite README:57-69: unit 1's reason is "unit 2 cannot land before it, measured"; delete "with no dependency" at `:64`. Add `bash tools/unattended/check-unattended.sh` to PLAY-1 §7 and an AC — after S2's rev bump and `gen_build_index --write`, the gate exits 0 because aSiftedPlaybook's run is terminal and check 8 skips it, which is also the second live observation of unit 1's fix. |
| H8 | high | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-3.md:20` | **S2's skip line falsifies the gate's own shipped output contract, and no scope item or AC reconciles them.** `check-unattended.sh:7` declares "Exit 0 + no output = clean. **Anything printed is a violation.** Exit 2 = misconfigured", and `:165` repeats it; the self-test's green control asserts `same "a conforming tree prints nothing" "$out" ""`. Both real run-state files are terminal (`memory/builds/aSiftedPlaybook/RUN.md:24` and `memory/builds/aSealedCaravan/RUN.md:19`, `phase: LANDED`) and the gate prints nothing today — verified, exit 0, silent. Once S1/S2 land the leg prints two informational lines on every green bar while exiting 0, and AC2/AC3 REQUIRE exit 0 with output. The header is not in Files-touched, no scope item touches it, and this ships to adopters. It also means check 8 compares NOTHING in this tree from day one: AC1 cannot be run against the real tree at all. | Add the header line to Files-touched and to a scope item: the contract becomes "Exit 0 + no violation lines = clean; skip lines are informational and prefixed as such". Give the skip line its own prefix (e.g. `UNATTENDED skip 8 —`) so the two classes are separable by grep, and update the self-test's green-control assertion with it. Say in AC1 that it is observed on a fixture, since this tree has no non-terminal run. |
| M1 | medium | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-1.md:106` | **`.memory-tree.conf` is owed by an AC in two units and named in neither Files-touched table, and the gate that would catch it is one-sided.** `ARMS_FLOORS` at `.memory-tree.conf:87` pins `tools/check-template-size.sh:5:5` and `tools/unattended/check-unattended.sh:39:39`, and `check-arms.py --report` measures both at exactly floor. TOOL-1 AC5 adds a NEW named `fail` branch (5 -> 6) and AC10 demands the pair EQUAL the measured counts; TOOL-3 AC6 says the same and even names the file. Neither §4 table lists it (TOOL-1 `:106-115`, TOOL-3 `:87-92`). `check-arms.py:281-284` only appends when `got[i] < want[i]` — `parse_floors` documents the floors as "both one-sided upward" — so a floor left behind a risen measurement is SILENT: the bar is green while the AC is false and the ratchet quietly stops tracking the gate. TOOL-3 pays twice: `.memory-tree.conf` is a watched pathspec (`memory/guides/SESSION-KICKOFF.md:6`), so it owes a `last-audit` re-stamp and a `manifest-check.sh` gate line, and its §7 lists neither — an unlisted red at the push boundary. Consequently `README.md:59-60`'s "unit 3 edits `.memory-tree.conf`" is true only for a reason no carrier states, and `README.md:65`'s "the two conf-touching units" is three. | Add a `.memory-tree.conf` row ("`ARMS_FLOORS` re-measure") to TOOL-1's and TOOL-3's Files-touched, add `memory/guides/SESSION-KICKOFF.md` + a `manifest-check.sh` §7 line to TOOL-3, and note in both AC10/AC6 that the floor rises in the same commit because the gate is one-sided and nothing else would notice. Correct README:59-60 and `:65`. |
| M2 | medium | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-3.md:92` | **A Files-touched row names a file that does not exist.** The table and S5:30 both spell `tools/unattended/UNATTENDED-PROTOCOL.template.md`. `git ls-files tools/unattended/` shows `PROTOCOL.template.md`; the `UNATTENDED-` prefix is the INSTALLED name only. `adopt-unattended.sh:77-78` holds the real pair (`PROTO_SHIP="$KIT_DIR/PROTOCOL.template.md"`, `PROTO_REL="$MEMORY_ROOT/guides/UNATTENDED-PROTOCOL.md"`) and `check-unattended.sh:373` reads `SHIP="$HERE/PROTOCOL.template.md"`. A builder working the table either leaves the real shipped half unedited or creates an orphan file inside a govkit-declared kit dir; AC7 catches the resulting parity failure, but at the end of the unit and from the wrong artifact. | Change both occurrences to `tools/unattended/PROTOCOL.template.md`, and state the pair — `PROTOCOL.template.md` ships, `memory/guides/UNATTENDED-PROTOCOL.md` is installed — so the two names are not conflated again. |
| M3 | medium | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-3.md:43` | **The non-goal counts three where there are seven, and S1's phrasing invites an implementation that disables all of them.** There is exactly ONE per-run-state-file loop, `while IFS= read -r f` at `:193` closing at `:362`, and the distinct `fail` ordinals inside it are 4, 5, 6, 8, 9, 11, 13, 15 — seven besides check 8 — plus check 7 at `:368`, which consumes the `nlive` counter the same loop accumulates at `:210`. So "The other three checks that iterate run-state files … none of them compares against a moving source" is asserted over a population the author never enumerated. The live hazard is S1:16's "check-8 loop" plus S2's "a skipped FILE prints one line": the natural implementation is a terminal-phase `continue` at the top of the loop, which silently disables checks 4, 5, 6, 9, 11, 13 and 15 for terminal runs while AC1-AC5 all still pass. | Derive the phrase rather than counting it ("every other check in the per-file loop"), or state seven and name them. Restate S1 as "inside the shared run-state loop, the check-8 comparison is gated on the `ph` the loop already read at `:199`" — `ph=$(phase_of "$f")` and the `PHASES_TERMINAL` case at `:210` are already exactly what S1 needs, which strengthens the design as well as fencing the misreading. |
| M4 | medium | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-1.md:33` | **S4 misidentifies the dossier that owns the leg key it is reasoning about.** S4 states the leg NAME `<=18KiB` "is an inventory key claimed by `memory/map/features/playbook.md`". Measured: `memory/map/features/session-kickoff.md:11` holds `gate-legs = ["kickoff engine size <=18KiB"]`; `playbook.md:11` holds `["template size gate selftest", "playbook parity", "playbook parity selftest"]`, and the sibling `template size <=48KiB` sits in `memory/map/baseline.toml:42`. The generated artifacts agree (`MAP.md:33` maps the key to `session-kickoff`). S4's conclusion — do not rename the leg — survives, its reason does not, and the same unit sends the builder into `playbook.md` at S7 to edit `[claims]`/`[paths]`. A builder who believes the leg key lives there can move it and red the coverage gate in both directions, which is the failure S4 was written to avoid. S7's dossier edits are correctly aimed (playbook.md's `[paths].globs` really does claim the gate's path set); only the ownership sentence is wrong. | Cite `memory/map/features/session-kickoff.md:11` in S4 (and `baseline.toml:42` for the template leg), and say explicitly that S7 edits `playbook.md` because that is where the PATHS live while the leg key's owner is a different dossier this unit deliberately does not touch — verified: `session-kickoff.md` carries no `18432` and no positional prose, so nothing there goes stale. |
| M5 | medium | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-1.md:147` | **AC1's "two observations" is one observation and a no-op.** S1 seeds the playbook template's declared row at 49152 and `check-template-size.sh:51` is `MAX_BYTES=${2:-${MAX_BYTES:-49152}}`, so "deleting the row changes the reported limit to the hard default" changes 49152 to 49152. The clause the AC says "proves the declaration is read at all" cannot fail — an implementation that never opens the limits file passes AC1 in both states. §4:86-91 names the trap ("they will be equal on the day this lands") and AC1 walks into it one section later. AC2 (`SKILL.md` at 18432 against the 49152 default) is in fact the only shipped-subject observation of the new layer, and AC1 reads as if it contributed. | Delete AC1's second half or make it distinguishable: point the gate at a scratch limits file whose row for the template is deliberately NOT 49152 (e.g. 40960) and assert the OK line reports it, then assert 49152 with the row removed — which needs `H2`'s file-resolution slot. State plainly that AC2 is the shipped-subject observation. |
| M6 | medium | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-1.md:15` | **This pass's own.** **S1 creates a tracked data file the gate PARSES and does not pin its line endings, on a fleet where every unpinned path smudges.** `git check-attr -a tools/template-size-limits.txt` resolves to `text: auto` — no `eol=lf`. Its sibling, the file S1 explicitly models the format on, IS pinned, with the reason written into `.gitattributes`: "The size gate's high-water record: a data file the gate PARSES and `--bump` REWRITES … the key side has no such tolerance to spare." The high-water reader survives a CRLF checkout only because it pipes through `tr -d '[:space:]'` (`check-template-size.sh:94`); a limits reader written any other way (`read -r k v`, `IFS=$'\t'`) lands a trailing CR on the VALUE — the last field — and `[ "$bytes" -gt "49152$CR" ]` is an "integer expression expected" error, not AC5's named failure. `.gitattributes` is in no unit's Files-touched. | Add a `.gitattributes` row to TOOL-1's Files-touched and an S1 clause pinning `tools/template-size-limits.txt text eol=lf` beside the sibling record's pin, with the same reason. Add an arm that a CRLF-checkout row still resolves, or state in §5 that the reader strips CR the way the ratchet reader does. |
| L1 | low | `spec/2026-08-16-spec-PLAY-aDeclaredCeiling-1.md:29` | **A false provenance citation in the spec whose subject is provenance drift.** §3 says of the audit report "Round 4 already re-adjudicated one of its rows and round 6 corrected a commit citation in another". Both numbers are wrong. The R2 re-adjudication is round 3 (review-3 H10; the report's own row at `:62` reads "REFUTATION WITHDRAWN 2026-08-16 (round-3 H10)"). The commit-citation correction `0f4d308` -> `5fd7c7e` is round 4 (review-4 L2). Round 6 files nothing against the audit report — `grep -c playbook-audit` over review-6 returns 0, and review-5 records "No fold owed" for that file. It is the sentence a reader uses to accept the non-goal. | "Round 3 re-adjudicated one of its rows (H10, `R2`) and round 4 corrected a commit citation in another (L2, `0f4d308` -> `5fd7c7e`)." |
| L2 | low | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-1.md:18` | **S1 overstates the gap the new file closes and understates the carrier problem.** It says the ceiling reversal's history "today survives only in `memory/DECISIONS.md` and a spec nobody reads at the gate". It also survives in two places a reader AT the gate does see: the gate's own header, `tools/check-template-size.sh:5-8` ("The ceiling moved 32 KiB -> 48 KiB on owner order (recorded in memory/DECISIONS.md)") plus its inline `:51-53`, and `memory/map/features/playbook.md:42-46` ("**The size ceiling is 49152 bytes, and that is a REVERSAL.**"). After S1 there are four narrations and the spec does not say which is canonical — while the same unit edits both of the unnamed carriers (Files-touched: "S2's layer, and its header's usage block"; S7). | Name the four existing carriers, and say what happens to `check-template-size.sh:5-8` and `playbook.md:42-46` when the limits file becomes the declared home: retired, pointed at the new file, or deliberately duplicated with a reason. |
| L3 | low | `spec/2026-08-16-spec-PLAY-aDeclaredCeiling-1.md:105` | **AC4's rationale claims a protection its mechanism cannot deliver.** `grep -c "## The hard cap\|## Concurrency" memory/guides/REVIEW-PROTOCOL.md` returns 2 — verified — but it is pinned to two literal headings, and the protocol carries five `##` sections. A third rule added under any other heading leaves the count at 2, so AC4's second sentence ("If a later build adds a third protocol rule this AC is what makes the refutation re-checkable rather than merely archived") is false. The measurement itself is fine and the pin does red if either heading is reworded. | Keep the AC, drop the overclaim: state it as a pin on the two heading strings the refutation quotes, which reds if either is reworded. §3:31-34 already declines the machine-checked rule count with a reason; AC4 should not quietly promise it. |
| L4 | low | `spec/2026-08-16-spec-TOOL-aDeclaredCeiling-2.md:126` | **This pass's own, derived from two refuted findings.** AC1's wording — "The §4 query, run unchanged, returns `READ_PATH_CEILING` among its hits" — was read by two independent lenses as already green at BASE, and both were refuted here: I re-ran the §4 query and it returns 40 hits, every one keyed to a `memory/`-rooted path, with the `.memory-tree.conf:71` DECLARATION absent exactly as §4 states. But hit `[1]` prints the token `READ_PATH_CEILING` inside a backlog row's TEXT, so the criterion as worded is satisfiable by reading. Two lenses misreading one AC is a defect in the AC, not in the lenses. | Name the hit's key form: "returns a hit keyed `.memory-tree.conf:READ_PATH_CEILING` — the DECLARATION, not a `memory/` record that mentions the token". AC2 already discriminates by construction; say so beside AC1. |

## Per-spec rev-bump fold list

- **`spec/2026-08-16-spec-PLAY-aDeclaredCeiling-1.md` -> rev-2** — `B1` (`:464` -> `:465` at `:7`,
  `:13`, `:26`, `:40` frame, `:99`, `:103`, `:128`; AC3 restated content-addressed), `H7` (§7 gains
  `check-unattended.sh`; a new AC over the terminal-run skip; §5's risk names the check-8
  consequence), `L1` (round 3 / round 4), `L3` (AC4's rationale).
- **`spec/2026-08-16-spec-TOOL-aDeclaredCeiling-1.md` -> rev-2** — `H1` (the env exposure in §4's
  blast radius + an AC), `H2` (a file-resolution slot for the limits file in S2 and the header
  usage block), `H4` (S7 names `playbook.md:47-52` as a correction + an AC), `M1` (`.memory-tree.conf`
  row), `M4` (S4 cites `session-kickoff.md:11`), `M5` (AC1's second observation), `M6`
  (`.gitattributes` row + the `eol=lf` pin), `L2` (S1's carrier count), and `H6`'s half if the
  corpus gap is recorded here as a non-goal rather than built into unit 4.
- **`spec/2026-08-16-spec-TOOL-aDeclaredCeiling-2.md` -> rev-2** — `B2` (`query.py` into
  Files-touched, the two-enumerator statement in §4, the shared-helper design, the `.md` filter, and
  an AC over `--stats`), `H3` (`SKILL.template.md` + the render into Files-touched, `:31` amended, a
  new AC, §5's sentence corrected), `L4` (AC1's hit form), and `H6`'s half if the TSV is admitted
  here instead.
- **`spec/2026-08-16-spec-TOOL-aDeclaredCeiling-3.md` -> rev-2** — `B3` (AC4 and §5:107-109
  reconciled with the source; S4's arm count and §5's "Armed in S4" agree), `H5` (an AC that reads
  the protocol text), `H8` (the header contract + a prefixed skip line + the self-test's green
  control), `M1` (`.memory-tree.conf` and `memory/guides/SESSION-KICKOFF.md` rows, a
  `manifest-check.sh` §7 line), `M2` (`PROTOCOL.template.md`), `M3` (the loop population and S1's
  phrasing).
- **`README.md`** — `B1` (`:20`, `:75`), `H6` (`:67-69`), `H7` (`:57-64`), `M1` (`:59-60`, `:65`).
- **`memory/backlog/PLAY.md:5`** — `B1` (the row's own anchor).

## Checked and clean

- **The refutation's SUBSTANCE.** Re-measured independently: the runbook sentence names exactly the
  two rules `REVIEW-PROTOCOL.md` binds, and `agent-cap.js`'s four are numbered implementation rules.
  The build's premise is sound; only its anchor is not.
- **The §4 recall measurement in `TOOL-aDeclaredCeiling-2`.** Ran the query verbatim: 40 hits, every
  path inside `memory/`, and no hit is the `.memory-tree.conf` declaration. Two lenses read it as
  already-green and both were refuted here — see `L4` for the residual wording item.
- **AC4's arithmetic in `PLAY-aDeclaredCeiling-1`.** `grep -c "## The hard cap\|## Concurrency"`
  returns 2, as claimed. Only the rationale sentence overclaims (`L3`).
- **`TOOL-aDeclaredCeiling-1` §4's read of the drift probe.** `_charter_mentions_every_leg`
  (`drift_signals.py:104`) matches argv PATHS, so dropping `18432` really does not move the signal.
  The spec says it confirmed this by reading the probe; it did.
- **The govkit obligation (S5/AC8).** Reproduced: adding a tracked `tools/template-size-limits.txt`
  makes `govkit.py selfcheck` report "tracked path … is neither an entry member nor an exemption".
  The scope item and the AC are correctly sized.
- **S3's routing in `TOOL-aDeclaredCeiling-2`** (declarations join `chunks`, not `records`) and the
  `recall_conf.resolve()` seam it names — `MEMORY_ROOT`, `FAMILIES` and `RECALL_CACHE_BUDGET_MB` are
  all resolved there today, so `RECALL_EXTRA_SOURCES` really is one field.
- **The three options weighed in `TOOL-aDeclaredCeiling-3` §4**, including the omitted fourth
  (compare a terminal run at its own recorded sha). The omitted option keys on facts the run itself
  writes, and §5 does state the vacuity cost. Refuted, and I agree.
- **`memory/DECISIONS.md` append-only discipline.** No unit mints or rewrites a decision row; TOOL-1
  §3 is explicit that the VALUE does not move.
- **`gen_build_index.py --check`** is clean over 42 artifacts at HEAD, and `check-unattended.sh`
  exits 0 silently — the tree the specs describe is the tree that exists, except where a row above
  says otherwise.

## Coverage

Four lenses ran; all four came back live, none produced a dead batch, and the skeptic pass returned
no unverified verdicts. Lens identities were not carried into this pass, so the ground below is
reconstructed from the finding set each produced.

- **Spec-claim-vs-source lens** — every factual assertion the four specs make about a named
  artifact, opened against the artifact. Produced `B2`, `H3`, `M2`, `M4`, `L1`, `L2`, plus the two
  refuted claims about `check_recall.py` and the `extract_chunks` shatter. Strongest yield of the
  four; its misses were the `.gitattributes` pin (`M6`) and it never priced the CRLF fleet at all.
- **Acceptance-criteria falsifiability lens** — can each AC fail, and does it observe the scope item
  it is attached to. Produced `B1`, `B3`, `H5`, `M5`, `L3`, `L4`'s subject, and the refuted pair on
  AC1's vacuity. Its blind spot was the gate's own OUTPUT contract, which `H8` reaches from the
  opposite side.
- **Gate/harness mechanics lens** — what the merge bar actually observes, one-sidedness, and where
  an arm can be written. Produced `H1`, `H2`, `H8`, `M1`, `M3`, and the refuted A0 mutation claim
  (which I reproduced and confirmed refuted). This is the lens whose findings cost the most to fix
  and the least to have caught.
- **Build-level / sequencing lens** — the README, the total order, the coverage table and the
  build-level rules. Produced `H6`, `H7`, `M1`'s README half, and the refuted account of the
  predecessor's four follow-ups. `H7` is the only finding in the set that was reproduced end to end
  in an isolated clone rather than read.

**Limits of this report.** No lens was dead and no batch was lost, so there is no unreviewed ground
of that kind — but four lenses over four specs is not exhaustive, and three areas were touched by no
finding and are therefore UNGRADED rather than clean: the `.unattended.conf` half of
`RECALL_EXTRA_SOURCES`' seed (refuted as a coverage complaint, never positively verified), the
precision cost of widening the recall corpus (which `TOOL-aDeclaredCeiling-2` §3 correctly records
as unmeasurable today), and every unit's `§5` sections for perf, a11y and i18n, all of which are
`N/A` and were read but not probed. Nothing here was executed against BUILT code, because none
exists: every behavioural claim was checked against the CURRENT source and, where a change's
consequence was at issue, against a scratch clone carrying the minimal edit. Zero findings were
carried on a lens's word.

**Disposition — the build may NOT proceed to its first code pass.** Three blockers stand, and each
one lands its cost at the END of the unit it belongs to: `B1` would publish an off-by-one
measurement into a CLOSED spec and a landed audit report — the exact drift class the build was
convened to close, in the correction itself; `B2` aims the recall unit's whole scope at the
enumerator none of its acceptance criteria exercise, so `AC1` fails after the extractor, the conf
key, the arms and the README are written; `B3` ties an acceptance criterion to behaviour the gate's
own source contradicts, so it can only be ticked on the wrong failure. All three are spec edits, not
design reversals — the four mechanisms are sound and `H6`/`H7` are the only rows that touch the
roster's shape. Fold `B1`-`B3` and the eight `H` rows, bump the four specs and the README to rev-2,
and this set is buildable; the `M` and `L` rows can ride the same fold at no extra cost.
