# DEPL-dRetiredFork-9 — acceptance ledger

**Serves:** journal DEPL-dRetiredFork-9

**Evidences:** DEPL-dRetiredFork-9
- AC1 — `memory/builds/dRetiredFork/spec/2026-09-02-spec-DEPL-dRetiredFork-3.md` — AC10 names the escape it depends on: NO escape at NicoCares, `--allow-ungraded` at inCMS when its deferred half is attempted, and the pinned argv carries neither flag nor a preceding `govkit adopt --re-adopt --write`
- AC2 — `python3 -c "sa==sb"` — the shared sentence is byte-identical in both criteria, 470 characters, compared programmatically rather than read separately
- AC3 — `DEPL-dRetiredFork-1` — S7 drives the NicoCares count to zero; the id is named in this spec's §9 and the unit is a roster row in the build README
- AC4 — `grep -n 'allow-ungraded\|re-adopt'` — both escape names appear in BOTH criteria, never in one, because the shared sentence carries both
- AC5 — `bash tools/memory-tree/check-memory-hygiene.sh` — exits 0 after the edits
