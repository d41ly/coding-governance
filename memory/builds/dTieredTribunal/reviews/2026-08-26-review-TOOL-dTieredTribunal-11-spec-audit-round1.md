**Serves:** spec-audit TOOL-dTieredTribunal-11 TOOL-dTieredTribunal-12 TOOL-dTieredTribunal-13 TOOL-dTieredTribunal-14 TOOL-dTieredTribunal-15

# Spec audit round 1 — the five-unit spec set of the second run

*Node a, 2026-08-26, round 1, over the reviewed range `cd971285..136cdb70`. Five finder lenses over the five specs and the build's own record, then five batched skeptics prompted to REFUTE each finding by re-deriving it against the source at the pinned base.*

## Verdict: BLOCKED

The qualifier sits below the verdict line rather than on it, because that line's token is a closed set and a tally appended to it makes a structural check read as a semantic one.

One confirmed blocker: `TOOL-dTieredTribunal-13` §2 S8 directs an edit to `memory/guides/REVIEW-PROTOCOL.md` and its shipped template with no owner grant, which this build's own committed rules and M3 veto 2 reserve to the owner. It is not a wrong design — it is an act the run is not authorized to perform, and the fix is a park, not a rewrite. Round 2's exit condition is a strictly falling confirmed-blocker count; this round sets the ceiling at 1.

## Review shape

- raw 41 · confirmed 28 · refuted 13 · unverified 0 · precision 0.68
- lenses 5/5 live · skeptic batches 5/5 live
- spurious 0 · conflicts 0 · duplicates 0
- confirmed by severity: 1 BLOCKER · 6 HIGH · 12 MEDIUM · 9 LOW
- confirmed blockers: 1

Precision is measured over the whole raw population of 41, not over a survivor subset: no lens and no skeptic batch died, and nothing was demoted or merged away before grading.

## Confirmed findings

### BLOCKER — 1

| # | Where | Claim | Fix |
|---|-------|-------|-----|
| 28 | `2026-08-26-spec-dTieredTribunal-13.md` §2 S8, §4 Migration, §4 Files touched, AC10 | S8 edits `memory/guides/REVIEW-PROTOCOL.md` and `tools/workflows/REVIEW-PROTOCOL.template.md` — a governance carrier and its shipped source — with no owner grant. The build README's Build-level rules read verbatim that `BUILD-METHOD.md` M4/M8 and `REVIEW-PROTOCOL.md` are in scope as PROPOSALS, not as edits, and that bullet is unamended and not session-scoped. `RUN.LANDED.3584b586.md:29` records the same rule as the reason the FIRST run parked its proposals, and `memory/guides/BUILD-METHOD.md:84` veto 2 plus `:76` confirm the delegation does not reach a governance-carrier clause. `memory/guides/REVIEW-PROTOCOL.md:1` declares itself BINDING, part of the charter. The two owner rulings this run holds, `TOOL-dTieredTribunal-7` and `TOOL-dTieredTribunal-8`, name neither protocol document. Unit 12 grounds its own M4 edit in `-7` and states outright that any other M4 edit is back under veto 2; unit 13 makes the identical class of edit citing nothing, so the run would ship an unauthorized carrier change with every leg green. | Split S8 in two. Keep the `tools/hooks/README.md` bullet inside the unit — a kit doc, not a carrier. Move the `REVIEW-PROTOCOL.md` / `REVIEW-PROTOCOL.template.md` half out as a parked fork in §8 with its options and the veto-2 ground stated, exactly as the first run parked P3/P5/P11. AC10 and the `review-protocol parity (kit vs dogfood)` leg then drop to must-stay-green-untouched. If the owner rules it in, cite that ruling by id in S8 the way unit 12 cites `TOOL-dTieredTribunal-7` at `memory/DECISIONS.md:107`. |

### HIGH — 6

| # | Where | Claim | Fix |
|---|-------|-------|-----|
| 1 | `2026-08-26-spec-dTieredTribunal-14.md` §6 AC11, against §2 S10 | AC11 is the only witness for S10's four-carrier version bump, and it passes before the change. It greps `agent-cap@1\.6` in one file, but `TOOL-dTieredTribunal-13` at order 2 has already moved that carrier to 1.7, so the grep returns 0 before unit 14 edits anything. `tools/check-kit-versions.sh:46-49` reconciles only the constant/marker pair inside `tools/hooks/agent-cap.js`, so a partial bump satisfies AC11 too: `tools/hooks/scratch-guard.js` and `.claude/hooks/scratch-guard.js` can sit at 1.7 while `tools/hooks/agent-cap.js` reads 1.8 and every leg stays green. AC10's `diff` of the two `agent-cap.js` copies does bind the second agent-cap carrier, so the silently-laggable set is the two scratch-guard files rather than three. The adopter-visible contract move S10 exists to signal ships half-applied. | Rewrite AC11 on the population unit 13's AC9 already uses: `grep -rn 'agent-cap@1\.7' tools/hooks/ .claude/hooks/` returns no hits, `grep -rc 'agent-cap@1\.8' tools/hooks/ .claude/hooks/` returns four carriers, and `bash tools/check-kit-versions.sh` exits 0. Both halves then fail at unit 14's own base. |
| 11 | `2026-08-26-spec-dTieredTribunal-14.md` §6 AC11, against §2 S10 | The same criterion read as a contradiction: S10 says the kit version moves from 1.7 to 1.8 across all four carriers, and the only AC covering it cites a string unit 13 has already removed. `tools/check-kit-versions.sh:46-47` asserts the constant and its same-line marker are EQUAL, not that either MOVED — unit 13's own §8 F2 states this in as many words, that a behavioural change carrying no bump leaves every carrier equal and the leg green. A builder who ships unit 14 with the version untouched at 1.7 passes AC11, passes the whole AC set, and reds nothing on the bar. An adopter's scripts start denying with no version signal, which is the exact contract move S10 exists for. This is the repo's own could-not-fail class landing inside the acceptance set of a unit whose subject is a guard. | Rewrite AC11 to grep the version this unit actually leaves behind: `grep -rn 'agent-cap@1\.7' tools/hooks/ .claude/hooks/` returns no hits and `grep -c "KIT_AGENT_CAP_VERSION = '1.8'" tools/hooks/agent-cap.js` returns 1, mirroring unit 13's AC9 shape which does cover all four carriers. Keep the `check-kit-versions.sh` half, and state that it grades consistency and not movement. |
| 21 | `2026-08-26-spec-dTieredTribunal-14.md` §2 S3, and §4 Rollout's one-commit-unit claim | S3 places the join rule after the `offendingLines` block that begins at `tools/hooks/agent-cap.js:821`, and asserts that `main()` exits on the first rule that fires. That block is the one rule block written as an early exit-0: `:821` is `const bad = offendingLines(script)` and `:822` is `if (bad.length === 0) process.exit(0)`. The two rules above it, `fan` at `:786` and `caps` at `:806`, use the additive `if (findings.length) { …exit 2 }` shape, so appending is safe there and is not safe here. A join rule appended after `:822` is unreachable for every script with no raw `parallel(`/`pipeline(` primitive — which is every script the join ban exists to judge. Followed literally, the unit ships inert, and the spec's stated premise about control flow is false for exactly the block it names. | State in S3 that the `offendingLines` block is INVERTED as part of this unit — `if (bad.length) { <existing message>; process.exit(2) }` — with a single `process.exit(0)` moved to the end of `main()` after `scanJoinFindings`. Add an arm asserting that a script with no raw primitive and a `verdicts[v.ref] = v` exits 2, which is the arm that fails if the rule is merely appended. |
| 22 | `2026-08-26-spec-dTieredTribunal-14.md` §4 Migration, §4 Files touched, and AC6 | S5's new missing-hook refusal reds three pre-existing arms of `tools/workflows/check-review-join.test.sh`, because those arms run the gate inside scratch repos holding no `tools/hooks/agent-cap.js`. `:121-133` builds `$TMP/discover` with only `tools/workflows/` and copies just the gate; `:160-163` builds `$TMP/emptyrepo` with only `README.md` and copies just the gate. The arms at `:141`, `:153` and `:165` then run the gate there, and S5's `[ -f "$HOOK" ] || { …; exit 2; }` makes all three print the missing-predicate refusal instead of their expected strings. The sibling already solved this: `tools/workflows/check-verifier-fanout.test.sh:63` copies the hook into its scratch repo, and keeps a separate `nohook` repo at `:74-79` purely to exercise the refusal — an arm spec 14's table does not include, so the new refusal's own failing case is unobserved too. §4 Migration's no-live-verdict-moves claim and the every-existing-arm-unedited line are both wrong. | Add to S8 and Files touched: copy `tools/hooks/agent-cap.js` into both scratch repos, after `:133` and after `:163`, copying the sibling's line 63 verbatim; and add a `nohook` scratch-repo arm asserting the refusal text. Reword §4 Migration and AC6 to say the arms' EXPECTED STRINGS are unchanged while the scratch-repo setup gains a hook copy. |
| 29 | `2026-08-26-spec-dTieredTribunal-11.md` §4 Inventory table (declared authoritative by S9), §2 S3 and S8 | The Inventory omits two diff-shaped prompt sites S9 normatively requires be made per-kind. `tools/workflows/tier2-review.js:195` is the finder's output-shape instruction, `Return JSON {lens:"${L.key}", findings:[{file,line,severity,claim,impact,fix}]}.`; S3 makes `SPEC_FINDING_SCHEMA` require `where` in `line`'s place, so on the spec arm every lens is told to emit `line` while the schema it is graded against requires `where`, and every finder return fails validation — the forced-regeneration class the charter's structured-output rule exists for. `:376` reads `followed by every unit id in the diff, space-separated.`, telling the synth to enumerate ids in a diff a spec-audit run does not have. Neither line appears in the Inventory's changed rows nor in its untouched-on-purpose list, and AC9 grades only against that table, so nothing catches the gap before AC14's live run. | Add both rows to the Inventory: `:195`, where the finder's field list becomes per-kind with `line` on the diff arm and `where` on the spec arm, and `:376`, where the id clause names the diff on one arm and the audited unit set on the other. Extend AC4 to assert the finder prompt's field list matches the schema selected for the same kind, so the pair cannot drift. |
| 36 | `2026-08-26-spec-dTieredTribunal-14.md` §6 AC11 and §2 S10 | AC11's grep half — the half the spec calls the one that binds — greps for `agent-cap@1.6`, which unit 13 at order 2 eliminates before unit 14 at order 3 starts, so it returns 0 whether or not the bump happens. The version bump this unit owes therefore has no failing observation anywhere. `tools/check-kit-versions.sh` reconciles only the pair inside `tools/hooks/agent-cap.js`, so three of S10's four carriers are observable ONLY by grep, and the grep as written passes on an empty diff. A run that bumps nothing, or bumps `agent-cap.js` and forgets both scratch-guard markers, satisfies AC11 in full. This is the residue of the already-caught same-version collision between the two hook units: the version numbers were split, the criterion was not. | Rewrite AC11's second half to the state unit 14 actually leaves behind, over the same two directories its sibling scans: `grep -rn 'agent-cap@1\.7' tools/hooks/ .claude/hooks/` returns no hits, and `grep -rln 'agent-cap@1\.8' tools/hooks/ .claude/hooks/` returns all four carriers by name. Keep the `check-kit-versions.sh` half as-is. |

### MEDIUM — 12

| # | Where | Claim | Fix |
|---|-------|-------|-----|
| 3 | `2026-08-26-spec-dTieredTribunal-11.md` §2 S4, S7, S9, against §6 | Three scope items carry no acceptance criterion of their own: S4 (the display `ref` becomes kind-aware at `:210`), S7 (the `context` default becomes per-kind at `:68`) and S9 (six diff-shaped prompt strings at `:103`, `:184`, `:194`, `:245`, `:345`, `:353`). Searching §6 for `context` and `ref` returns nothing; the six prompt sites appear only inside AC9, whose subject is a provenance COMMENT. Their only witness is that a comment naming the unit id sits nearby — satisfiable by typing a comment beside unchanged code. A build leaving `const context = a.context || 'the cumulative diff landing on main'` intact, or leaving `:353`'s range line spelling `${base}...${head}` on the spec arm, passes every criterion in §6 while a spec-audit run primes its lenses with diff-shaped instructions, which is the failure S7 itself names. | Add one criterion per item with a failing observation. S7: a synthetic invocation with `kind: 'spec-audit'` and no `context` logs a default containing no `diff`. S4: a spec-kind finding's `ref` matches `path#section` and contains no `:undefined`. S9: the spec-arm prompt region contains no `diff`, or each of the six sites is asserted to interpolate a kind-selected string. |
| 13 | `2026-08-26-spec-dTieredTribunal-11.md` §5 Production-readiness, observability bullet, against `2026-08-26-spec-dTieredTribunal-15.md` §4 | Unit 11 claims the kind token, the review-shape line, the subject anchor and the trust counters all reach a spec-audit record by the same path they reach a diff-review one. Three of those four hold. The fourth does not: the synthesis prompt runs `tools/workflows/tier2-review.js:328-386`, its shape line at `:346` interpolates raw, confirmed, refuted, unverified and precision — finding counts — and no liveness counter is interpolated anywhere in that range. `lensesDead`, `skepticsDead`, `conflicts`, `duplicates` and `spurious` reach `log()` and the RETURN objects only. Unit 15 §4 measures exactly this and calls it D2's shape in the reference harness; `memory/backlog/TOOL.md:220` says the seven counters were returned to the caller and interpolated into no synthesis prompt; and unit 11's own S10 preserves only the RETURN fields. The headline unit overstates its shipped observability in the direction its sibling unit exists to record. | Reword §5 to say the counters reach the CALLER on every exit path, which S10 preserves, and explicitly NOT the written record, citing `TOOL-dTieredTribunal-15`'s record as the disclosure. If the counters really should reach a spec-audit record, that is an extra scope item and belongs in §2 with its own AC over the synthesis prompt. |
| 14 | `2026-08-26-spec-dTieredTribunal-14.md` §2 S5, against §6 AC5 | S5 deletes only the awk block at `tools/workflows/check-review-join.sh:72-92` and says everything else about that gate is KEPT, but `awk` also appears at `:53`, so `grep -c awk` returns 2 today and AC5's demand that it return 0 fails on a literal implementation. The substantive half is worse: `:53-59` describes one awk over the whole population and its `//`-tail rule, and `:67-71` explains at length why `https://` inside a STRING survives as code — precisely the behaviour S2's switch to `blankLiterals` removes, as spec 14's own Migration measures. `:67-71` contains no `awk` token, so even AC5 does not force its removal, and the gate would ship a comment describing a stripper it no longer has. | Extend S5 to delete or rewrite `:53-71` together with `:72-92`, and say what replaces them: a short comment naming `blankLiterals` as the stripper and stating the deliberate narrowing S2's Migration justifies. Move the three ban descriptions at `:57-59` into the hook's own rule, where S1 puts the table. AC5 is then satisfiable. |
| 15 | `2026-08-26-spec-dTieredTribunal-11.md` §2 S11 and §6 AC11 | S11 closes `TOOL-aDeclaredBound-6`, but its carrier inventory misses a third live carrier. `git grep -n -- "6-wide"` returns `tools/workflows/tier2-review.js:154`, `memory/backlog/TOOL.md:148`, and `tools/check-agent-cap-restatement.sh:35`, whose surrounding bullet at `:33-36` reads that `tier2-review.js` says one thing on one line and another on another while its code fans at 5, and closes `Filed as TOOL-aDeclaredBound-6`. That file is tracked, sits under no frozen prefix, ships with the hooks/gates surface, and is not graded by its own gate, which scans markdown only. After unit 11 the sentence is false and the id it names reads CLOSED, and nothing on the bar notices — the `dead-path carriers` leg only sees deleted files. This is the stated-in-prose-beside-the-source class, left behind by the unit that closes the row. | Add the file to S11 and to §4's Files touched: rewrite `:33-36` so the example reads as a past observation resolved by `TOOL-dTieredTribunal-11`, or replace it with an in-source example that is still live — the bullet's own point, that executable files are out of scope, does not need this particular instance. Extend AC11 to `git grep -n -- "6-wide"` returning only frozen `memory/builds/` and `memory/archive/` paths. |
| 23 | `2026-08-26-spec-dTieredTribunal-13.md` §2 S9 and §6 AC12 | Both say `memory/map/features/agent-cap.md`'s Gaps section lists three gaps; it lists six. `## Gaps` is at `:132` and its bullets are at `:134`, `:137`, `:139`, `:143`, `:146` and `:148` — the sidechain-uncounted gap, the `Workflow({name:…})` gap, the runtime-count gap, the enclosing-opener walk, the dynamic-array gap and the block-comment gap. AC12 is therefore unsatisfiable as written: a builder either records it failed or, worse, prunes the section to make the number true. It is also the charter's own no-count-of-a-derived-population-in-prose rule broken inside a unit whose subject is a guard that trusted an unchecked count. | Drop the number: its Gaps section gains no bullet and loses none, because this unit closes rather than discloses. Observe it as a bullet count over the Gaps span before and after, rather than against a literal three. |
| 24 | `2026-08-26-spec-dTieredTribunal-14.md` §6 AC11 | AC11 is already satisfied before this unit runs, so it observes nothing about unit 14's own bump. `TOOL-dTieredTribunal-13` is order 2 and moves the pair 1.6 to 1.7; this unit at order 3 moves 1.7 to 1.8. The moment 13 lands, `agent-cap@1.6` is absent and AC11 passes forever, including on a build of unit 14 that forgets S10 entirely. `tools/check-kit-versions.sh:46-47` asserts only that the constant and its same-line marker AGREE, which spec 13 §8 F2 itself records as internal-consistency-only, so a no-bump leaves the leg green too. A criterion that cannot fail, in a build whose sibling unit 15 exists because a check that cannot fail is this corpus's named defect class. | Assert the positive at the version this unit ships, in the shape spec 13's AC9 already uses: `agent-cap@1.8` present once in each of `tools/hooks/agent-cap.js`, `.claude/hooks/agent-cap.js`, `tools/hooks/scratch-guard.js` and `.claude/hooks/scratch-guard.js`, and `grep -rn 'agent-cap@1\.7' tools/hooks/ .claude/hooks/` returning nothing. |
| 25 | `2026-08-26-spec-dTieredTribunal-11.md` §2 S11, §4 Files touched, §6 AC11 | The same carrier miss seen as an unstated assumption: `tools/check-agent-cap-restatement.sh:33-38` is a merge-bar gate whose header quotes the stale text and records the row as still filed, and spec 11's Files touched names two files plus the backlog row. After this unit, that gate's own what-this-cannot-see section cites a live example that no longer exists and an id that reads CLOSED, and nothing reds because the gate's population stops at markdown and it cannot see itself. One overstatement in the finding as filed, which does not change the disposition: the standing-argument-for-widening sentence attaches to the remedy-string carrier, not to this example. | Add `tools/check-agent-cap-restatement.sh` to Files touched and to S11: rewrite the example in the executable-files-are-out-of-scope bullet to a carrier that is still live, or restate it as the instance this unit removed with the id marked closed. Alternatively state in §3 that the header is deliberately left and why — but not silently. |
| 30 | `2026-08-26-spec-dTieredTribunal-11.md` §2 S11, §4 Inventory, AC11 | The carrier miss again from the prior-art lens, plus a second half the other filings do not carry: `memory/builds/dTieredTribunal/README.md:58` says `TOOL-aDeclaredBound-6` and `TOOL-aBoundedVerdict-20` are OPEN and untouched here, which S11 falsifies, and no unit amends that slot. It is the Parked-decisions slot rather than the M3-reserved description slot, so it is not one of the adjudicated states and the run may amend it. The stale-carrier half is the class this corpus already tracks as `TOOL-dUnstalledConvoy-16`. | Add `tools/check-agent-cap-restatement.sh:34-36` to §4's Files touched and to AC11 — the paragraph keeps its widening argument but re-points its example, or names the row as closed. Amend the README's Parked-decisions bullet in the same commit to say the row is closed by this unit. |
| 31 | `2026-08-26-spec-dTieredTribunal-13.md` AC12 and §2 S9 | Counted at the pinned base rather than at the tip: `git show cd971285:memory/map/features/agent-cap.md` sliced from `## Gaps` to `## Reuse affordance` yields six bullets, and `git diff --stat cd971285 HEAD` is empty for that file, so base and tip agree. AC12's `still lists the same three gaps it lists at the pinned base` and S9's `the three gaps it lists are untouched` are both false about the tree before any work is done, in an acceptance criterion, inside a unit about a guard that accepts without examining the rest. | Restate AC12 without the count: the Gaps bullet set is unchanged from the pinned base, and no bullet describing the marked-derivation hole was added. Fix the same figure in S9. |
| 32 | `2026-08-26-spec-dTieredTribunal-12.md` §2 S3, AC3, §10 | S3 grounds the ban on running `--render` in the build rule pinned for this run, and no such rule exists in any carrier a run reads: the build README's Build-level rules slot names none, `memory/builds/dTieredTribunal/RUN.md` carries none, and `--render` appears nowhere in the build folder outside specs 12 and 13 themselves. Meanwhile `tools/memory-tree/kit-dogfood-parity.test.sh:30-32` states the opposite direction verbatim, that the template is the authored source and the live copy is never hand-edited, and the cited precedent, F3 of `memory/builds/aBoundedVerdict/reviews/2026-08-21-review-TOOL-aBoundedVerdict-1-round3.md`, refuses `--render` only as a REMEDY for live-only drift, where the fix would be deleted. Neither reaches a unit about to write both sides. The outcome is harmless because line 116 carries no placeholder, which is exactly why nothing would catch the fabricated ground. | Replace S3's ground: either follow the kit's stated direction — edit `tools/memory-tree/BUILD-METHOD.template.md`, re-render, then `--check` — or keep the hand-edit and justify it on what is true, that line 116 has no placeholder so the two edits are byte-identical, with F3 cited as the reason not to reach for `--render` as a remedy. Drop the reference to a build rule that is not written down. |
| 33 | `2026-08-26-spec-dTieredTribunal-14.md` §2 S10, §4 Files touched, AC11 | S10 says the 1.7 to 1.8 bump carries the same four sites, and it is right — `tools/hooks/scratch-guard.js:41` does carry the hooks-kit marker, and all four carriers spell `agent-cap@1.6` today. But §4's Files touched lists only the two `agent-cap.js` copies, omitting both scratch-guard files, which the sibling spec 13 §4 does list; and `tools/check-kit-versions.sh:46-50` reconciles only the pair inside `tools/hooks/agent-cap.js`, so a missed scratch-guard bump is invisible on the bar. That is the exact residual unit 13's S7 flags when it says three of the four sites are moved by reading. | Add `tools/hooks/scratch-guard.js` and `.claude/hooks/scratch-guard.js` to §4 Files touched, and re-point AC11 at 1.7: no hits for `agent-cap@1\.7` under both hook directories, all four for `agent-cap@1\.8`. That is the only observation that binds for the three ungated carriers. |
| 37 | `2026-08-26-spec-dTieredTribunal-13.md` §6 AC12 and §2 S9 | The same wrong count seen as a could-not-fail criterion: a criterion unsatisfiable as written is either abandoned or read charitably as the Gaps being unchanged, and in both cases it observes nothing. The thing AC12 exists to catch — a builder disclosing this hole as a new gap instead of closing it — then has no check at all. Verified by counting `^- \*\*` between `## Gaps` at `:132` and `## Reuse affordance` at `:151`: six at HEAD and six at `cd971285`, with an empty diff between them. | Drop the number and make it a diff observation: `git diff cd971285 -- memory/map/features/agent-cap.md` shows no added or removed line between the `## Gaps` heading and the `## Reuse affordance` heading. That fails exactly when a gap is added or removed, and needs no count. |

### LOW — 9

| # | Where | Claim | Fix |
|---|-------|-------|-----|
| 8 | `2026-08-26-spec-dTieredTribunal-12.md` §4, The replacement | The candidate wording is labelled measured at 274 bytes; extracting those three lines and running `wc -c` gives 275. The rest of the arithmetic is right and depends on 275: the old line 116 is 114 bytes in both carriers, 275 minus 114 is the stated +161, and 24385 + 161 = 24546 with 24396 + 161 = 24557, matching §4's table against the 24576 cap. So only the label is inconsistent, under the same newline-inclusive convention as the delta beside it. Cosmetic here, but §4's byte table is the unit's whole risk argument and this is the figure a later reader re-uses to price a further M4 edit against the remaining 19 bytes of template headroom. | Change 274 to 275, or state the figure as +161 over the 114-byte line it replaces, so only one number carries the arithmetic. |
| 9 | `2026-08-26-spec-dTieredTribunal-11.md` §7 Gates | §7 says two of the named legs carry `subject: kit` and then names three, and one of the three, `agent-cap restatement self-test`, does not appear in the named-legs paragraph above — that paragraph carries `agent-cap restatement`, a different, unguarded `subject: repo` leg. The section also opens by saying no count of them is written here, two sentences before writing one. A builder reconciling the DoD run against `tools/gate-legs.json` gets a count that does not match the list, in the one section that says which run satisfies Definition of Done. The DoD conclusion is right either way, so this is bookkeeping. | Drop the count and write that the `subject: kit` legs among those named are `verifier fan-out self-test` and `review-join self-test`, and either add `agent-cap restatement self-test` to the named list or remove it from the sentence. |
| 16 | `2026-08-26-spec-dTieredTribunal-11.md` §7 Gates, the `subject: kit` paragraph | The count-versus-list contradiction seen by the contradiction lens: §7's named set is ten legs, of which exactly two carry `subject: kit` in `tools/gate-legs.json`, and the next sentence lists those two plus a third that exists but is not among the ten. A builder cannot tell whether the count or the list is authoritative, and the third name implies a leg this unit's diff does not name is part of its evidence. One wobble in the finding's own impact text, which does not affect the disposition: unit 11 does edit under `tools/`, at `tools/workflows/tier2-review.js`. | Either drop the third leg and keep two, or add it to the named set and state the legs without a digit — the section already refuses to write a leg count two paragraphs down. |
| 19 | `2026-08-26-spec-dTieredTribunal-12.md` §2 S3 and §6 AC3 | S3 attributes the ban on running `--render` to the build rule pinned for this run, and no such rule exists in either carrier a run reads: the README's Build-level rules slot holds seven bullets naming none, and `memory/builds/dTieredTribunal/RUN.md` is run facts with an empty Parked section. The substantive reason S3 gives is correct — the round-3 F3 record does say not to run it, and `tools/memory-tree/kit-dogfood-parity.test.sh:93` renders template over live — so the instruction is right and the authority cited for it is invented. AC3's second sentence, that `--render` is not invoked at any point, compounds it: an assertion with no observation, which AC4 openly concedes it cannot prove. | Replace the build-rule citation with the actual provenance — F3 of the `aBoundedVerdict` round-3 record — plus the one-line reason. Drop AC3's unobservable second sentence and let the observation be what it can be: both files hand-edited, the parity test exits 0, and AC4's single-hunk diff. |
| 27 | `2026-08-26-spec-dTieredTribunal-11.md` §7 Gates, the `subject: kit` paragraph | The same sentence from the unstated-assumption lens, with the additional fact that separates the three: `verifier fan-out self-test` and `review-join self-test` are `subject: kit` guarded on `['tools/lib/','tools/workflows/']`, while `agent-cap restatement self-test` carries no `guard` at all — so the surrounding argument, that a guarded leg arming is not the same as a leg running, does not describe the one leg the count is wrong by. | Delete the count: some of the named legs carry `subject: kit` and are HELD unless the self-tests are enabled, then list them; and either add the third leg to §7's opening list or drop it from the sentence. |
| 34 | `2026-08-26-spec-dTieredTribunal-11.md` §7 Gates, final paragraph | The count is wrong rather than the name: read from `tools/gate-legs.json`, all three legs the sentence lists are `subject: kit`, so `Two` is simply false, and the third is absent from §7's own named list. Spec 14 §7 gets the same arithmetic right, four of twelve, which is the control that shows this is a slip and not a convention. It is the charter's no-count-of-a-derived-population rule inside the section that says no count of them is written here. | Delete the numeral and either add `agent-cap restatement self-test` to §7's named list or drop it from the sentence. |
| 35 | `2026-08-26-spec-dTieredTribunal-13.md` §4, the candidate run over the real tree | The spec attributes the two `agent-cap.js` self-denials to block comments; the denial actually fires on rule 2 over the remediation TEMPLATE LITERALS. Reproduced at the pinned base: feeding `tools/hooks/agent-cap.js` to itself exits 2 with the bounded-fan-out message at `L796` and `L816`, both remediation strings. The block-comment behaviour is a different mechanism, rule 1, and is the gap `memory/map/features/agent-cap.md` records separately. The verdict the spec reports is right; the stated cause is not, in a section whose whole claim is that the sweep was RUN rather than reasoned about — and unit 14 §4 depends on the correct cause when it rejects letting the gate run the whole hook. | Restate the row as the two copies of `agent-cap.js`, which spell a bounded fan-out inside their own remediation strings — rule 2, not the block-comment case. Unit 14's alternatives-rejected entry already has it right; make the two agree. |
| 40 | `2026-08-26-spec-dTieredTribunal-11.md` §7, the `subject: kit` paragraph | The fifth filing of the same sentence, and the one that states the manifest fact most usefully: `agent-cap restatement` is `subject: repo` with no guard and `agent-cap restatement self-test` is `subject: kit` with no guard, so the third leg runs on any self-tests-enabled bar regardless — which makes the count wrong rather than the name, in the section a builder reads to derive the DoD run. | Add `agent-cap restatement self-test` to the named-legs list in §7's first paragraph, then delete the count entirely — §7's own next sentence already says the manifest owns the population and a number beside it is wrong on the next commit. |
| 41 | `2026-08-26-spec-dTieredTribunal-14.md` §6 AC5 against §2 S5 | AC5 requires `grep -c awk tools/workflows/check-review-join.sh` to return 0, but the file spells `awk` twice, at `:72` inside the block S5 deletes and at `:53` in the comment paragraph S5 explicitly preserves when it says everything else about that gate is KEPT. A builder executing S5 exactly reds their own DoD for a reason no scope item authorizes. The neighbouring stripper note at `:67-71` is likewise a description of the deleted awk that S5 never mentions, and §4 Migration explicitly changes what the stripper does, so that comment becomes false as well as orphaned. | Extend S5 to say the comment block at `:53-59` and the stripper note at `:67-71` are rewritten to describe the delegation, not just that the awk block at `:72-92` is deleted. AC5 then measures what S5 actually asks for. |

## Unverified — zero, and that is positive evidence

**There are no unverified findings, and none is carried forward as outstanding.** Every one of the 41 raw findings reached a skeptic verdict of confirmed or refuted; none was filed with no usable verdict, and none is deferred to round 2 for adjudication.

That zero is positive evidence rather than an absence, and it is asserted against the liveness counters rather than inferred from a quiet log. **All 5 finder lenses returned and all 5 skeptic batches returned** — `lenses 5/5 live`, `skeptic batches 5/5 live`. Had a batch died, its findings would have arrived with no verdict and appeared here as outstanding; had a lens died, the raw population would have been narrower than the fan and the precision figure would have been measured over a surface nobody looked at. Neither happened, so the 41/28/13 split covers the whole population the fan produced, and `precision 0.68` is measured over all 41 rather than over a survivor subset. `spurious 0 · conflicts 0 · duplicates 0` completes the assertion: nothing was discarded as unparseable, no two verdicts disagreed about one finding, and no finding was counted twice.

Round 2 therefore inherits no unadjudicated work. Its confirmed-blocker count is comparable against this round's ceiling of 1 without a correction term.

## Refuted — 13

- **2** [medium] spec-11 §6 AC9 — the premise that only a line address identifies a site is false: §4's Inventory carries two prose columns per row and `grep -n` prints the matched LINE TEXT, so a hit maps to its row by content with no arithmetic.
- **4** [medium] spec-11 §6 AC5 — reads only the `Observation:` clause: AC5's first sentence states the empty-array refusal as a criterion in the same read-based form AC4, AC6 and AC12 use, and the grep is already labelled the anti-drift guard the fix asks for.
- **5** [medium] spec-11 §6 AC14 — the spec names the window itself in §4 Rollout and calls it real and deliberate; nothing mechanical breaks inside it, AC14 pins no commit, and a disclosed one-commit sequencing nit is not medium.
- **6** [low] spec-12 §2 S4 — the mechanical half holds but the impact does not: the commit message is not the one durable record, since `wc -c` re-derives the figures at any commit and §4's table already carries the pair, and AC5 grades the outcome the measurement protects.
- **7** [low] spec-12 §6 AC3 — the proposed discriminator does not discriminate: a `--render` from a correctly edited template yields the same byte pair offered as proof it was never run, and the destructive case already fails AC1 and AC2 outright.
- **10** [low] spec-13 §2 S2 — the gap does not exist: S2 records the `--suggest` observation, the `lexicon naming predicates` leg is named in §7, armed by this diff, and required green by the DoD; asking §6 to restate a §7 gate obligation is the duplication the one-fact-in-one-place rule refuses.
- **12** [medium] spec-14 §2 S10 — the contingency the fix asks for is already in the text S10 quotes, M3 sweeps §8 forks before any code so F2 is closed at the roster stage, and unit 14 names the four carriers regardless of the starting digit.
- **17** [low] spec-11 §1 Goal — no defect follows: §1's digit matches its own enumeration of six per-kind axes, the seventeen Inventory entries are SITES and several are not per-kind work, and S9 designates the table authoritative rather than the goal paragraph.
- **18** [low] spec-11 §3 Non-goals — the added risk is inverted: unit 14 at order 3 narrows the join predicate by blanking template-literal contents, so unit 11's new prompt strings at order 4 face the NARROWER predicate, and §8's one-fork sentence is accurate as written.
- **20** [low] spec-15 §2 S3 — S3's third sentence already defers to whatever the finished body contains, `nothing wider` is about WIDTH rather than exhaustiveness, and AC8 blocks the postulated harm by requiring the citation to resolve.
- **26** [low] spec-13 §4, last paragraph — the counter-reproduction differs only in working directory: the arm resolves `ROOT` from the CWD, so it passes inside the repo and skips outside one, which is the run the spec describes; the finding also misreads 64/64 as the candidate's result.
- **38** [medium] spec-11 §6 AC9 — reads AC9 as line-number-keyed when it is site-keyed: each Inventory row identifies its site by content and S13 pins the mechanism, so post-edit drift is irrelevant to the match, and pre-edit addresses in a `today` column are correct by design.
- **39** [medium] spec-11 §6 AC5 — two errors: the grep's failing state is a POST-EDIT duplicate, which is the drift AC5 names, and the finding treats the `Observation:` line as the whole criterion when AC5's binding clause is its first sentence, in this corpus's standard read-clause-plus-observation shape.

## What a fold should take, and what is recorded and not folded

The count round 2 measures is the CONFIRMED BLOCKER count, and it stands at **1**.

The 28 confirmed findings are **14 distinct defects**. Five lenses landing on one sentence is one edit, not five, and the grouping below is what a fold should schedule against.

**Fold now — the blocker, as one defect (finding 28).** Unauthorized governance-carrier edit in spec 13 S8. The fold takes the PARK arm: split S8, keep the `tools/hooks/README.md` bullet in the unit, move the `REVIEW-PROTOCOL.md` and `REVIEW-PROTOCOL.template.md` half into §8 as a fork with its options and the veto-2 ground named, and drop AC10 plus the parity leg to must-stay-green-untouched. That is spec text and this run may write it. The other arm — the owner ruling S8 in — is an owner turn under M3 veto 2 and is recorded below as not folded. Folding the park arm drops the blocker count to 0; leaving AC10 pointed at an edited carrier keeps the finding alive.

**Fold now — the HIGH tier, which collapses to four distinct defects.**

1. **The unfailable version bump — 1, 11, 24, 36, and the Files-touched half of 33.** One defect, five lenses, one fix surface: spec 14 §6 AC11 plus §4 Files touched. Re-point the grep at 1.7 over `tools/hooks/` and `.claude/hooks/`, assert 1.8 present in all four carriers, add both scratch-guard files to Files touched, and state that `check-kit-versions.sh` grades consistency and not movement. Five findings close on one edit.
2. **The join rule placed after an early exit — 21.** Spec 14 S3 must say the `offendingLines` block is inverted, with one `process.exit(0)` moved to the end of `main()`, and add the no-raw-primitive arm that fails if the rule is merely appended.
3. **The self-test arms the new refusal reds — 22.** Spec 14 S8 and Files touched gain the hook copy into both scratch repos, plus a `nohook` arm for the refusal's own failing case; §4 Migration and AC6 are reworded to say the setup changes and the expected strings do not.
4. **The two omitted prompt sites — 29.** Spec 11 §4's Inventory gains `:195` and `:376` as changed rows, and AC4 gains the finder-prompt-matches-schema assertion so the pair cannot drift.

**Fold now — the MEDIUM tier, which collapses to six distinct defects.**

5. **The three unwitnessed scope items — 3.** One falsifiable observation each for spec 11 S4, S7 and S9.
6. **The observability overclaim — 13.** Reword spec 11 §5 to counters-reach-the-caller-not-the-record, citing unit 15's record as the disclosure.
7. **The awk comment blocks — 14, and its low-tier twin 41.** One edit to spec 14 S5 widening the deletion to `:53-71` with a replacement comment; AC5 becomes satisfiable and both findings close.
8. **The missed `6-wide` carrier — 15, 25, 30.** Add `tools/check-agent-cap-restatement.sh` to spec 11 S11 and §4 Files touched, re-point or retire its example, and widen AC11 to a tree-wide grep. Finding 30 carries a second half the others do not: the build README's Parked-decisions bullet at `:58` is falsified by S11 and should be amended in the same commit.
9. **The three-gaps-that-are-six — 23, 31, 37.** One edit: spec 13 AC12 and S9 drop the numeral and become a diff observation over the Gaps span.
10. **The fabricated `--render` authority — 19, 32.** One edit to spec 12 S3 replacing the invented build rule with the F3 provenance and the real reason, plus dropping AC3's unobservable second sentence.

**Fold now — the LOW tier, which collapses to three distinct defects.**

11. **The `subject: kit` count — 9, 16, 27, 34, 40.** Five findings, one sentence in spec 11 §7. Delete the numeral and reconcile the list against `tools/gate-legs.json`. Round 2 should count these as closed by that single edit rather than as five items.
12. **The 274/275 byte label — 8.** One digit in spec 12 §4.
13. **The wrong cause for the self-denials — 35.** One row in spec 13 §4, restated as rule 2 over the remediation template literals, matching what spec 14 already says.

**Recorded and NOT folded — one arm of one defect.** The OWNER arm of finding 28: ruling the `REVIEW-PROTOCOL.md` edit in scope. M3 veto 2 reserves a governance-carrier change to the owner, the build's own committed rules say those documents are in scope as proposals and not as edits, and the first run parked exactly this class for exactly this reason. A run cannot ratify its own authorization, so the fold parks it in spec 13 §8 with the options stated and leaves the decision on the owner's desk. If round 2 reports the carrier edit as folded IN without a ruling cited by id, that report is itself the defect.

Nothing else is held back. Every other fix above is spec text or build-record text this run is authorized to write, and none of the 28 requires a new gate, a new mechanism, or a budget the run cannot raise.

**What round 2 measures.** Confirmed blockers must fall below 1. Every folded finding should show either a CORRECTED disposition or an explicit refusal in the spec's own revision log; a folded finding that reappears unchanged is a FOLD_WRONG, and a new finding created by the fold text is a FOLD_BROKE. The five `subject: kit` filings and the five AC11 filings should reappear as at most one finding each, or not at all — if either group returns at full count, the fold edited prose without editing the sentence the lenses were pointing at.
