**Serves:** spec-audit TOOL-dTieredTribunal-1 TOOL-dTieredTribunal-2 TOOL-dTieredTribunal-3

# Spec audit round 1 — the three-unit spec set

*Node a, 2026-08-26, base bb41f748. Four finder lenses over the three specs and the build's own record, then five batched skeptics prompted to REFUTE each finding by re-deriving it against the source at the pinned base.*

## Verdict: BLOCKED

The qualifier sits here rather than on the verdict line, because that line's token is a closed set and a tally or a mood appended to it makes a structural check read as a semantic one.

Three confirmed blockers, and all three are the SAME defect seen by three lenses: spec-2's chosen anchors cannot select the new gotcha class on a fold diff, which is the only diff class §1 says the unit exists to serve, and the `universal: true` escape is already at its declared budget. Round 2's exit condition is a strictly falling confirmed-blocker count; this round sets the ceiling at 3.

## Review shape

- raw 53 · confirmed 38 · refuted 15 · unverified 0 · precision 0.72
- lenses 4/4 live · skeptic batches 5/5 live
- spurious 0 · conflicts 0 · duplicates 0
- confirmed by severity: 3 BLOCKER · 15 HIGH · 13 MEDIUM · 7 LOW

Precision is measured over the whole raw population, not over a survivor subset, because no batch died and nothing was demoted.

## Confirmed findings

### BLOCKER — 3

| # | Where | Claim | Fix |
|---|-------|-------|-----|
| 1 | spec-2 §2 S2 (and §1 Goal, §5 AC3) | The record's two anchors cannot select on the diff the class exists for. A fold round's diff is spec files under `memory/builds/<slug>/spec/`, and neither `memory/guides/BUILD-METHOD.md` nor `tools/workflows/tier2-review.js` matches one under `gotchas.py`'s selection predicate. Verified by running the shipped tool: `--for-paths memory/builds/dFramedEntrypoint/spec/2026-08-24-spec-dFramedEntrypoint-1.md` prints `0 class(es) selected by an anchor + 4 universal`. | Add a third anchor a fold diff actually touches (a backticked `memory/builds/` token — `selectable()` is substring-both-ways plus basename equality, so a directory token selects everything beneath it), or declare the record `universal: true`. The universal branch additionally needs an S-item raising `UNIVERSAL_BUDGET`, which `--report` shows at `universal : 4 (budget 4)`. Then replace AC3's path with one a fold round actually produces. |
| 13 | spec-2 §1 Goal vs §2 S2 (and §7 Open questions) | The record as scoped can never be selected on a fold diff, so the unit does not deliver its own goal, and the only mechanism that would deliver it is blocked by a full `UNIVERSAL_BUDGET` the spec never mentions. AC3 tests the anchor the spec declared rather than the population the goal names, so it passes by construction — the ships-inert class already in this repo's catalogue. §7 closes the question as `none` without a probe, though the research it derives from flagged it unchecked at P12. | Add a §7 fork naming the two arms and their evidence: anchor on the fold's write surface rather than the fold's rule, or `universal: true` plus a same-commit budget raise, which is an owner call under M3 veto 2 and therefore a park. Either way replace AC3 with an observation over a real fold range so the criterion can fail. |
| 31 | spec-2 §2 S2 (the two anchors) and §1 Goal | Same mechanism from the unstated-assumption lens: the record fires only for sessions editing the fold RULE or the harness — governance work, which is not where the 42-of-62 measurement came from — and `.memory-tree.conf:160` declares `UNIVERSAL_BUDGET="4"` with exactly four records already carrying the flag, so marking this one universal reds checks 17-19. §7 records `none` open questions when this is the unit's central unresolved fork. | Anchor on a path a fold diff touches and verify with `--for-paths memory/builds/dFramedEntrypoint/spec/`, or raise `UNIVERSAL_BUDGET` to 5 with the reason recorded beside it as the conf's existing raises are. Mark the chosen fork `RESOLVED (agent, <date>, delegated)` in §7. |

The proposed `memory/builds/` anchor works but buys near-universal noise, so the fork is real rather than a formality.

### HIGH — 15

| # | Where | Claim | Fix |
|---|-------|-------|-----|
| 2 | spec-2 §5 AC4 | AC4 names `python tools/codebase-map/reuse_lookup.py` as the observation that the new `gotcha-classes` key is claimed and absent from `baseline.toml`. That command cannot show either fact: it takes a required `query` positional, exits on an argparse usage error run bare, returns 0 unconditionally by its own docstring, and prints a ranked behaviour-to-seam shortlist that reports neither claim coverage nor baseline membership. | Point AC4 at the leg that adjudicates it — `codebase-map coverage + freshness` (`tools/codebase-map/test_codebase_map.py:79`) — or at a direct read of the dossier and `baseline.toml`. AGENTS.md's claim that this script "prints the live pair" is the stale source the AC inherited. |
| 3 | spec-1 §2 S5 | S5 — the prompt sentence defining `blockers` as the count of CONFIRMED blocker-severity findings — has no acceptance criterion at all. AC1-AC3 cover the schema and the return plumbing, AC4 the verdict line, AC5-AC6 are stay-green gates. A build that ships the plumbing and skips the sentence passes every criterion and returns an integer with no defined population. | Add an AC in the AC4 shape: when the synthesis prompt is read, it states that `blockers` is the count of CONFIRMED findings at blocker severity, and that severity is the value the synthesis pass adjudicated. |
| 6 | spec-1 §2 S2 / §6 AC3 | The synth-death path is unspecified. `if (!synth)` at `tier2-review.js:381` only logs; execution falls through to the SUCCESS return at `:390`. S3's never-zero rule names only the three early returns and AC3 scopes itself to them, so no criterion observes the fourth exit. | Add the synth-death case to S3: on the success return `blockers` and `highs` are `synth?.blockers ?? null`, never zero. Extend AC3 to a fourth clause covering a falsy `synth`. |
| 7 | spec-3 §4 Data model vs §2 S3 | Two incompatible shapes for `lensesRun`: S3 says the return carries the surviving lens SLUGS, §4 says it gains `lensesRun` as the SURVIVING count. §4's "no field a reader relies on today changes meaning" is false under the count reading, since `drift-audit-state.js:378` returns an array today. AC2 only requires the value be "derived from the surviving results", which both readings satisfy. | Pick one and state it in both places. `tier2-review.js:400` is `lensesRun: liveResults.length`, so the integer is the consistent choice; if the slugs are wanted, name a second field rather than overloading one. |
| 14 | spec-1 §2 S2 and §4 Data model | The synthesis-death path is the fourth degraded exit and the spec never says what `blockers`/`highs` carry there. §4's table has exactly two columns, success and early. A builder writing `synth.blockers` throws, `synth?.blockers` emits `undefined`, and `synth?.blockers ?? 0` emits the exact false-clean zero §4 calls a clean bill nothing earned — all three pass acceptance. | Add S3b: on the success return both fields are `null` whenever `synth` is falsy, never zero. Give §4's table a third column and add an AC reading the success block for the null coalescing. |
| 15 | spec-1 §6 AC1 against spec-3 §3 Non-goals | AC1's unscoped grep for the old two-key spelling can never return no match. `grep -n "required: ['path', 'summary']" tools/workflows/*.js` returns three byte-identical hits, and spec-3 §3 forbids changing the two siblings that carry the other two. Read repo-wide the criterion is unsatisfiable at every point in the build; read file-scoped it is satisfiable but the spec never says which. | Scope AC1 to the file, and add one clause noting the two siblings keep that spelling by design under `TOOL-dTieredTribunal-3` §3. |
| 16 | spec-3 §2 S3 against §4 Design, Data model | Same `lensesRun` type collision from the contradiction lens, on a unit whose whole subject is a return contract. `drift-audit-state.js:378` IS the current value of `lensesRun`, and S7's "in the shape tier2-review.js already uses" is a third implicit vote for the integer. Whichever a builder picks, the other document records the unit as not built to spec. | Rewrite S3 as "returns the surviving lens COUNT plus `lensesDead`, matching `tier2-review.js:400`", and if the slug list is wanted add it under a distinct key named in both S3 and §4. |
| 17 | spec-3 §4 Design, "Files touched (estimate)" | "No third file" is false. `memory/map/features/review-harnesses.md:73-79` states this unit's delta as live fact, claims all three files under `workflow-scripts`, and §10 says `reuse_lookup` returned them under that key, so the author read it. AGENTS.md §1 DoD requires dossier prose refreshed on touch and the map's legs are merge-bar legs. Sibling spec-2 S4 handles its map obligation, so the build treats one rule two ways. | Add the dossier to Files touched and a scope item requiring the first Gaps bullet rewritten in the same commit; the "no review-KIND parameter" and "parses no args" bullets stay. Add an AC reading that bullet at HEAD. |
| 20 | build README roster note under `<!-- roster:units -->`, against RUN.md "## Parked" | The narrowing describes itself in terms the record contradicts in both halves. P5 and P11 need no governance-carrier edit — both are changes to `tools/hooks/agent-cap.js` — and neither was built; P3 is likewise carrier-free and unbuilt. RUN.md's single park entry names only P1 and P9, so P3, P4, P5, P10 and P11 appear in no park at all. M9 derives the owner's parked rows from those entries, so the build's one owner turn silently omits five of twelve proposals. | Either restate the README sentence to what happened, or add RUN.md park entries for P3, P4, P5, P10 and P11 with the one-line reason each was not taken. Cross-reference spec-3 §8 F1 from the P5/P11 entry so the owner sees one disposition per proposal. |
| 32 | README §Build-level rules (line 36) against the roster note (lines 58-60) | The BUILD-LEVEL RULES slot still reads "This session delivers RESEARCH ONLY. No spec... Settled at kickoff, all three forks", unamended, twenty lines above the note explaining that this run specced three units anyway. `gen:build-index` renders three SPECCED rows under a rule saying no spec exists. M2 names that slot as where a run records a deviation. | Amend the rules bullet in place: mark the research-only rule superseded, state that the owner narrowing was delegated to the run under the standing mandate on 2026-08-26, and name the criterion actually applied. Keep the parked half as an explicit owner turn with its refused alternatives named in the README. |
| 33 | spec-3 §2 S3 and §6 AC2 | "Return the surviving lens slugs" is underivable from what S3 and AC2 point at. `r.lens` is an agent-typed string with no enum that the prompt never supplies (`COMMON` invites the agent to invent one at `:89`), and `filter(Boolean)` at `:234` has already destroyed the index alignment that would let the harness answer the question itself. AC2's "derived from the surviving results" is satisfied most cheaply by `lensOut.map(r => r.lens)` — the echo-drift class the integer join at `:237` exists to kill, one field over. | Derive survival from position: compute the survivor set from `finderResults` BEFORE the `filter(Boolean)`, return `survivors.map(L => L.slug)` and `lensesDead = LENSES.length - survivors.length`. State in §4 that `r.lens` is display-only, and rewrite AC2 to name index alignment. |
| 34 | spec-1 §1 Goal against `tools/workflows/tier2-review.js:357` | §1 tells the synthesis agent to open its record with the literal verdict line, but `:357` already mandates the binding line in that position, provenanced to `TOOL-aBoundedVerdict-14` S7. S4 states the token set and says nothing about position, so the builder must either demote the existing hardening or emit two contradictory sentences and let the model choose. | Pick the order in S4: `**Serves:**` stays line 1 and `## Verdict: <token>` is the next non-blank line, which satisfies check 21's 12-line window and check 22. Reword §1 to match, and make AC4 assert the ORDER rather than only presence. |
| 42 | README "Build-level rules" bullet 1 and "Expected improvements" | The committed rule forbidding a spec is left verbatim while the same run committed three specs, and "Expected improvements" still promises P1's outcomes — one hardened engine driving every review, the majority review kind getting mechanical enforcement — that none of the three specced units delivers. At M8's re-read-the-README step this build cannot close honestly, and the derived LIVE.md row advertises SPECCED against a goal no unit serves. | Amend the rules block with a dated, sourced `RESOLVED (agent, 2026-08-26, delegated)` mark naming the mandate clause relied on, and rewrite Expected improvements/Detriments to what units 1-3 actually buy, moving the harness-generalisation claims into the parked fork's text. |
| 43 | spec-1 §2 S2 and §6 AC3 (the synthesis-death exit) | There are four degraded exits, not three. With `synth` null the function falls through to the success return, so fields sourced "from the synthesis result" resolve to `undefined` — absent, not null — which is the false-clean shape `TOOL-aBoundedVerdict-14` S6 was written to kill, and it breaches prior-art item 1's price on any change to this engine: every counter on EVERY exit path. | Spell the source as `blockers: synth ? synth.blockers : null` (same for `highs`), state in §4 that synth-death is a FOURTH null-carrying exit, and add an AC that no path yields `undefined`. |
| 44 | spec-1 §1 Goal and §2 S4 (the verdict-line instruction) | Two mutually exclusive first-line instructions in one prompt, resolved by the agent. The binding line is the guard `TOOL-aBoundedVerdict-14` S7 added to close `TOOL-aLoosenedCeiling-6`; if the agent drops or displaces it, check 21 reds and the record binds to no spec. The collision mirrors an unamended M4-versus-harness disagreement the spec imports without noticing. | State the order in S4 — binding line first, `## Verdict:` next non-blank — and add it to AC4 as an ordering assertion, not a substring assertion. |

### MEDIUM — 13

| # | Where | Claim | Fix |
|---|-------|-------|-----|
| 8 | spec-3 §6 AC1 (and S5, S7) | AC1's grep enumerates four tokens — `lensesDead\|skepticsDead\|spurious\|conflicts` — omitting `duplicates` and `note`, and it is file-scoped, so it cannot see whether any counter reaches a return block. S5's duplicate counter and S7's every-counter-on-both-returns rule have no failing observation, leaving unenforced the prior-art refusal §4 quotes verbatim. | Add `duplicates` and `note` to AC1's pattern, and add a return-block-scoped criterion asserting both the success return and the new early return name all seven fields. |
| 9 | spec-3 §2 S4 | S4 defines the early return as "when every lens failed to return", but `drift-audit-state.js:224` builds `LENSES` from a caller filter, so a typo'd slug yields an empty array and the ported `lensesDead === LENSES.length` reads `0 === 0`. The run then reports "all lenses died, nothing was reviewed" with `lensesDead: 0` — a degraded-run message for a misconfiguration. This is the repo's own `vacuous-selector-empty-population` class. | Split the condition: refuse or return a distinct note when `LENSES.length === 0`, and take the all-dead path only when `LENSES.length > 0 && lensesDead === LENSES.length`. Add the zero-configured case to AC4. |
| 10 | spec-2 §4 / §2 (no S-item) — the codebase-map artifacts | S4 requires the new inventory key claimed by a dossier in the same commit but names no scope item for re-rendering the committed generated map artifacts. `memory/map/generated/inventories.json` currently enumerates 32 `gotcha-classes` keys; the record makes it 33. A builder following §2 literally leaves the freshness leg red, and AC5 catches it only afterwards as a failed bar. | Add an S-item naming the map regeneration command, and list `memory/map/generated/inventories.json`, `memory/map/generated/MAP.md` and the chosen dossier as touched files. `memory/map/features/review-harnesses.md` carries `gotcha-classes = []` and is the natural home. |
| 11 | spec-2 §6 Gates and §5 AC5 | Both name a gate leg called `memory-tree hygiene`; `tools/gate-legs.json` has no such leg. The manifest's names are `memory hygiene` and `memory-hygiene self-test`. A builder greps the manifest for the spec's spelling, finds nothing, and AC5 cannot be answered or recorded as written. | Use `memory hygiene`. Resolve §6's hedge while there: `gotchas index freshness` is not a leg either — INDEX freshness is check 17 inside `gotchas.py --check`, reached through `memory hygiene`. |
| 24 | spec-3 §7 Gates against spec-1 §7 Gates | Two specs editing the same directory name different leg sets. `drift-audit selftest` is guarded on `tools/drift-audit/` and `tools/lib/` and cannot arm for a diff confined to `tools/workflows/`; `drift-audit wiring` touches `workflows` only to render a path string. Meanwhile `review-protocol parity (kit vs dogfood)` is guarded on `tools/workflows/` and WILL run, and `agent-cap restatement` is unguarded — spec-1 names both, spec-3 names neither. | Replace spec-3's two drift-audit legs with `agent-cap restatement` and `review-protocol parity (kit vs dogfood)` so both specs name the same set for the same directory. If the drift-audit legs are wanted as a smoke check, say they are held by default and name the `GATE_SELFTESTS=1` invocation. |
| 36 | spec-3 §2 S5 and §6 AC3 | The demote-to-unverified rule is imported from a binary verdict vocabulary into a ternary one — both siblings enum `confirmed\|refuted\|partial` — and the spec never says whether `partial` against `confirmed` is agreement or contradiction. Worse, the demotion mechanism renders `reason: 'no verdict returned'`, which is false when two verdicts did return, and that false reason is serialized into the synth prompt and the report. `severityCorrection` on the dropped verdict is lost silently. | State the rule over three tokens in §4: `confirmed` vs `refuted` is a conflict, anything involving `partial` collapses to `partial` and increments `duplicates`. Give the demotion its own reason string and extend AC3 to observe that reason in the judged record. |
| 39 | spec-3 §7 Gates and §4 Files touched | The gate list names a leg the diff cannot select and omits every leg actually guarded on `tools/workflows/`. Separately, `kit version markers` is unguarded and `tools/check-kit-versions.sh:179-194` asserts each sibling's `meta.version` equals `KIT_DRIFT_AUDIT_VERSION` and that its inline marker agrees; the spec says nothing about whether a behavioural change moves that version, and `TOOL-dSettledRoster-4` records this exact class redding the bar twice at a full-bar cycle each. | Replace `drift-audit selftest` with the three `tools/workflows/`-guarded legs, keep `drift-audit wiring`, and add one line to §4 stating the version decision explicitly — no bump, and therefore no edit to the version string, the `gov:kit drift-audit@1.6` markers, `KIT_DRIFT_AUDIT_VERSION`, or the kit README marker. |
| 40 | spec-1 §2 (no S covering the synthesis-death path) | The spec enumerates a success path and three early paths and never names the fourth exit. A builder following S2 writes `blockers: synth.blockers` and throws, or copies the local `\|\| null` idiom, which also maps a genuine `0` to `null` and collides with S3's semantics where `null` means no synthesis ran. §5's "a synthesis that dies is already logged and returned by the existing guard" is the spec touching this path and still not giving the new fields a value on it. | Add an S covering it: on synth death the return carries `blockers: null` and `highs: null` explicitly, not via falsy coalescing — spell it `synth ? synth.blockers ?? null : null` so a returned zero survives — and add it to AC2 so the observation covers all four exits. |
| 45 | spec-1 §2 S4 and §7 Gates | S4 makes `tier2-review.js` a new carrier of the closed verdict set, already spelled in `BUILD-METHOD.md` M4, `HYGIENE.md` check 22, the hygiene engine's awk, `unattended.sh`, and the unattended Skill — and §7 explicitly adds no gate joining them. This is the class `TOOL-dUnstalledConvoy-16` is OPEN for, and the repo already GATES one of those pairs, which is proof the class is treated as gateable here. S4 also names the wrong owner: for a `diff-review` record the enforcing carrier is check 22, not M4. | Add the prompt's token list to `tools/check-playbook-parity.sh`'s machine-compared pairs, or add a sub-second `subject: repo` scanner asserting the prompt's tokens match check 22's set, and cite check 22 rather than the build method as the owner in S4. |
| 47 | spec-3 §2 S3 versus §4 "Data model" and "Migration" | Third view of the `lensesRun` collision, and it adds the migration half: §4 asserts "every field is additive" and "no field a reader relies on today changes meaning", both false of `lensesRun`, whose current value is `LENSES.map((L) => L.slug)`. The false claim sits beside the genuine safety argument, so a reviewer scanning §4 is told there is no breaking change when there is exactly one. | Pick the integer and say so in both S3 and §4; name any slug list as a separate additive field. Correct §4 Migration to state that `lensesRun` changes type and meaning and that no tracked caller reads it, which is the actual safety argument. |
| 49 | spec-3 §2 (scope) and §3 (non-goals) | The `args`-must-be-an-object guard (`tier2-review.js:32-60`, `TOOL-aGuardedTally-1` S5) is not ported and not excluded: both siblings are `const a = args \|\| {}` with no `JSON.parse`, so a caller handing a string silently audits the process cwd. §1 claims the siblings are "missing every hardening the third learned from a shipped defect" and §4's Inventory claims every row, so the omission falsifies a completeness claim. The recorded consequence is worse than a false clean: twice auditing a DIFFERENT repository and returning confident findings about it. | Either add an S-item porting the parse-then-validate guard into both siblings, or add an explicit §3 non-goal naming it and open a `TOOL-` backlog row so it survives this build. Do not leave §1's "every hardening" claim standing against a scope that ports only the accounting. |
| 50 | spec-3 §4 "Files touched (estimate)" | "No third file" forecloses refreshing the map dossier whose Gaps section this unit falsifies in every clause — the trust-accounting bullet, the CONFIGURED-lens-set claim, the no-lens-information claim, and the first-verdict-wins claim. The unit lands leaving a machine-verified-looking record making false statements about the files it just changed, inside the layer sessions are told to trust over prose. | Name `memory/map/features/review-harnesses.md` in §4 and add an AC: after the unit lands the Gaps section no longer claims either ported gap, and the remaining gaps — the args parse, the kind parameter, the inline-script blind spot — are left standing with their evidence. |
| 51 | spec-2 §2 S3/S4 and §5 AC4 | S4 requires the new key claimed by a map dossier but no S-item re-renders the map's generated artifacts, and no dossier is named. `test_codebase_map.py:128` byte-compares the committed artifacts against a live re-render and runs unguarded on `subject: repo`. A builder following S1-S5 literally adds the claim, skips the render, and reds the bar AC5 asserts is green — the same one-step-missing shape the dFramedEntrypoint fold was measured on. | Add an S-item running `python tools/codebase-map/gen_map.py --write` in the same commit as the claim edit, and name the dossier that takes the claim. Note only one of the record's two anchors sits inside that dossier's `tools/workflows/*` glob. |

### LOW — 7

| # | Where | Claim | Fix |
|---|-------|-------|-----|
| 12 | spec-1 §2 S6 | S6 — a comment on each edit naming this unit id — has no acceptance criterion. The provenance convention is the mechanism the build README says keeps a hardening from being re-lost, and spec-3 S8 restates it as load-bearing for the port, so it is the scope item most likely to be silently dropped. | Add: when `grep -n 'dTieredTribunal-1' tools/workflows/tier2-review.js` runs, it returns a hit at each edited site — the schema, the success return, and the three early returns. |
| 26 | spec-1 §3 Non-goals, "A new gate leg" bullet (repeated in §7) | "The three existing scanners over the workflow scripts already select this file with no marker filter" is false for two of the three: `check-verifier-fanout.sh:44-45` and `check-workflow-syntax.js` both select on `export const meta =`. It also contradicts the map dossier this same run committed, which calls that "a real selection seam and not a formality". The conclusion survives because the file carries the marker. | Rewrite the bullet: `check-review-join.sh` selects with no marker filter; the other two select on `export const meta =`, which this file carries and must keep. |
| 27 | all three spec status headers, `node a`, against the build README front matter `node: d` | The three specs claim node `a` while the build, its ids and its run state are node `d`, and nothing in the build record explains the difference. The corpus convention is the other way. A reader cannot tell a typo from a continuation, and §2 makes node identity the thing that keeps minted ids uncontested. | Set all three headers to the node whose session authored them, and if that is genuinely `a`, add one line to RUN.md recording that the run continued on node `a` under the same slug — otherwise the id ownership reads as contested. |
| 28 | spec-3 status header against §8 F1's RESOLVED mark | A fork was resolved under the mandate but the header carries no `ratified <date>` pointer, which TEMPLATE-SPEC's resolved-forks rule requires explicitly. Hygiene check 12 grades §8 only at a terminal status, so this stays green until the spec closes — at which point the header still will not say the fork was ratified. | Append `· ratified 2026-08-26` to spec-3's status header tail. |
| 29 | spec-3 §4 Design, Inventory table citations, against spec-1 §2 S3 | Three of eight citations are one line off — the dead-lens count is `:206`, the all-lenses-dead return opens at `:213` and returns at `:215`, and the surviving lens set on the return is `:400`, not `:401`. Spec-1 S3 cites `:215` for the same construct spec-3 cites as `:214`, so two documents name one construct differently. Every other citation in both specs reproduces exactly. | Correct the three rows to `:206`, `:213` and `:400`, and align spec-1 S3 and spec-3's inventory on one line number for the all-lenses-dead return. |
| 52 | spec-2 §6 Gates and §5 AC5 | Second view of the leg-name error: `memory-tree hygiene` matches nothing in `tools/gate-legs.json`. A §6 Gates list is what a builder greps to run the named legs before claiming done, in a repo whose §16 rule is that leg names are read from the manifest and never typed from memory. | Replace both occurrences with `memory hygiene`; the neighbouring names are `memory-hygiene self-test` and `gotchas selftest`, and index freshness lives inside the hygiene leg. |
| 53 | spec-1 §3 Non-goals, "A new gate leg" bullet | Second view of the marker-filter error. The research record already states the corrected version verbatim and the new dossier calls the marker selection a real seam — a spec restating a premise its own research corrected is how the premise gets re-inherited by the next unit that reasons from it. | Reword to: `check-review-join.sh` selects every `tools/**/*.js` with no marker filter, and the other two select on the `export const meta =` marker this file already carries, so all three grade it today and no new leg is owed. |

## Unverified — 0, and that is positive evidence

**Zero findings are outstanding for want of a verdict.** Every one of the 53 raw findings received a usable skeptic verdict, so nothing sits in the "raised but never adjudicated" bucket that a reader must treat as neither confirmed nor cleared.

That number is positive evidence rather than an absence, because it is asserted against a liveness fact recorded in the same shape: 5/5 skeptic batches returned and 4/4 lenses returned. A zero unverified count produced by dead batches would be indistinguishable from this one on the count alone — the difference is that here the denominator moved. Precision 0.72 is therefore measured over the whole raw population, and the confirmed set is 38 of 53 rather than 38 of some smaller number nobody wrote down.

## Refuted — 15

- **4** [high] spec-1 §1 Goal vs §2 S4/§6 AC4 — the impact chain fails: `BIND_HEAD_LINES = 12`, so check 21 admits a record opening with `## Verdict:` and carrying the binding line beneath it, and the corpus already shows the settled coexistence.
- **5** [high] spec-1 §6 AC4 — the factual half holds but the defect does not follow: §3 names `BUILD-METHOD.md` by path and S4 points at the set it declares, which is the charter's own point-at-the-source rule; the proposed fix would create a third carrier.
- **18** [high] spec-3 §2 S1 — the ambiguity dissolves on reading the spec beside the file: S3 pins "configured" to the identifier `LENSES`, which IS the post-filter dispatched set, and the file establishes the same denominator at `:239`. Nothing points a builder at `ALL_LENSES`.
- **19** [high] spec-1 §3 Non-goals against §2 S4 — a reading artifact: §3's bullet is about not EDITING the governance carriers, and the file already instructs project-specific record grammar in the same prompt with no parity gate, landed deliberately.
- **21** [medium] spec-2 §3 Non-goals against spec-1 §2 S4/S5 — spec-2's bullet defers the two FOLD instructions specifically, verbatim from the research's own disposition; the cut-line the finding calls unwritten is written in spec-1 §3's first bullet.
- **22** [medium] spec-3 §5 against §8 F1 — the two sections decline two DIFFERENT gates: §5 declines a field-presence assertion, F1 defers a source-pattern predicate for its own stated reason. A rewrite preference on a rationale sentence.
- **23** [medium] spec-3 §4 Inventory preamble against `order 2` — the staleness is already closed three ways: §3 names `tier2-review.js` as OUT, §3 excludes sibling prompt changes, and §2/§4 enumerate a closed field set. The base is pinned in the header.
- **25** [medium] spec-2 §2 S2 against S5 — the mechanism claim is right but both failure modes are caught or sanctioned: an unanchored record reds check 19, which AC2 already asserts against, and the third derived anchor is over-selection the module documents as the accepted trade.
- **30** [blocker] spec-1 §2 S3 and §4 — the load-bearing claim is false at source: the success path is reached whenever surviving findings exist, so any round whose findings are all high or below returns `blockers: 0`. §4 also weighs and rejects the proposed alternative in writing.
- **35** [high] spec-3 §3 Non-goals against §1 Goal — §1's own sentence names the caller, not the markdown report, as the audience; total lens death calls no synth agent at all under S4, and the Workflow return is what the invoking agent reads.
- **37** [medium] spec-1 §4 Alternatives rejected — the verified facts do not carry it: §4's word is "allowed", not "instructed", nothing forbids the agent re-grading, and §4 carries a second independently sufficient reason the finding does not touch.
- **38** [medium] spec-1 §2 S4 against §3 — uncharitable reading of a non-goals block about not editing carriers; and the copy is not ungated in EFFECT, since check 22 reds any drifted token at the record.
- **41** [medium] spec-3 §2 S4 — fact confirmed, impact refuted: post-port behaviour on an empty lens set is strictly better than today's false clean, and the residual is a message-quality nit. (The underlying zero-configured gap survives as confirmed finding 9, filed against the predicate rather than the guard.)
- **46** [medium] spec-1 §4 Alternatives rejected, first bullet — attributes to the spec a claim it does not make; permission is a negative, and the rejection rests on a second independent ground the finding does not touch.
- **48** [medium] spec-3 §6 AC1 — the acceptance SET is not satisfiable by comment prose: AC4 demands a RETURNED field and AC2 demands derivation from surviving results, both of which the finding's own scenario fails. AC1 also carries its own liveness assertion.

## What a fold should take, and what is recorded and not folded

The count round 2 measures is the CONFIRMED BLOCKER count, and it stands at **3**.

**Fold now — the blocker, as one defect.** Findings 1, 13 and 31 are one defect with one fix surface: spec-2 §2 S2, §5 AC3 and §7. The fold takes the ANCHOR arm — a `memory/builds/` anchor plus an AC3 that observes selection over a real fold range — and records the `universal: true` plus `UNIVERSAL_BUDGET` raise as an owner arm in §7 under M3 veto 2, because a budget raise is not a text fix and this run cannot ratify it. Folding the anchor arm alone drops the blocker count to 0 only if AC3 is rewritten with it; leaving AC3 pointed at the declared anchor keeps the could-not-fail shape and keeps the finding alive.

**Fold now — the HIGH tier, which collapses to seven distinct defects.**

1. The synth-death exit — 6, 14, 40, 43. One rule added to spec-1 S3 and §4's table, one clause added to AC3, `null` never zero and never `undefined`.
2. The verdict-line collision — 34, 44. State the order in S4, assert the order in AC4.
3. The `lensesRun` type collision — 7, 16, and its migration half 47. Pick the integer, say it in S3 and §4, correct §4 Migration.
4. The map dossier — 17, 50. Add `memory/map/features/review-harnesses.md` to spec-3 §4 and an AC over the rewritten Gaps bullet.
5. The build record's unamended rules slot — 20, 32, 42. Amend the rules bullet with a dated delegated-resolution mark, rewrite Expected improvements to what units 1-3 buy, and add the five missing park entries.
6. The two unobserved scope items — 3 (S5) and 15 (AC1's unscoped grep). One AC added, one AC scoped to its file.
7. The undelivered observations — 2 (AC4 names a command that cannot answer it) and 33 (survivor derivation from agent-typed text).

**Fold now — the MEDIUM tier, except one.** 8, 9, 10, 11, 24, 36, 39, 47, 49, 50 and 51 are corrections to named identifiers, missing scope items, or predicates with a stated replacement; every one is spec text. 49 folds as its cheaper arm — an explicit §3 non-goal plus a `TOOL-` backlog row — and the port-it arm is named as a fork rather than decided here.

**Fold now — the LOW tier.** 12, 26, 27, 28, 29, 52 and 53 are one AC, two rewordings, two header tokens and three line numbers. 52 folds with 11 and 53 folds with 26; they are the same two edits seen twice, and the next round should count them as closed by those edits rather than as separate work.

**Recorded and NOT folded — 1 finding.** Finding **45**, the sixth carrier of the closed verdict vocabulary with no parity gate joining it. Its fix is a new gate leg or a new machine-compared pair in `tools/check-playbook-parity.sh` — mechanism, not spec text — and the class is already OPEN as `TOOL-dUnstalledConvoy-16`. Folding a new gate into a spec-text round would smuggle a mechanism past the round that is supposed to price it. The owner-slip half of 45 IS folded: S4 should cite check 22 rather than M4 as the enforcing carrier for a `diff-review` record.

**What round 2 measures.** Confirmed blockers must fall below 3. Every folded finding above should show either a CORRECTED disposition or an explicit refusal in the spec's own revision log; a folded finding that reappears unchanged is a FOLD_WRONG, and a new finding created by the fold text is a FOLD_BROKE. Finding 45 should reappear as standing, not as fixed — if round 2 reports it closed without a gate or a parity pair, that report is the defect.
