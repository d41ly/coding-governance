**Serves:** spec-audit TOOL-dTieredTribunal-1 TOOL-dTieredTribunal-2 TOOL-dTieredTribunal-3

# Spec audit round 2 — the three-unit spec set, after the round-1 fold

*Node a, 2026-08-26, round 2, reviewed range bb41f748..93312e05. Five finder lenses over the folded specs and the build's own record, then five batched skeptics prompted to REFUTE each finding by re-deriving it against the source at the pinned range.*

## Verdict: BLOCKED

The qualifier sits here rather than on the verdict line, because that line's token is a closed set and a tally or a mood appended to it makes a structural check read as a semantic one. BLOCKED because two confirmed blockers stand — and they are one defect, seen twice: spec-2's AC3 names a DIRECTORY argument that `normalise_paths` strips to a form the `/spec/` anchor cannot select, so the criterion's pass state is byte-identical to the failure signature the criterion itself quotes.

## Review shape

- raw 38, confirmed 29, refuted 9, unverified 0, precision 0.76, lenses 5/5 live, skeptic batches 5/5 live, spurious 0, conflicts 0, duplicates 0.
- confirmed by severity: 2 BLOCKER · 5 HIGH · 16 MEDIUM · 6 LOW

Precision is measured over the whole raw population of 38, not over a survivor subset, because no lens and no skeptic batch died and nothing was demoted.

## Convergence — the count fell STRICTLY

**Round 1 confirmed 3 blockers. Round 2 confirms 2. Three is greater than two, so the count fell STRICTLY, and that — and nothing else — is the loop's exit condition.** The loop therefore RE-ARMS: a round 3 is owed, and it must confirm fewer than 2 blockers.

The fall is real rather than arithmetic. Round 1's three blockers (1, 13, 31) were one defect — the record's declared anchors could not select on a fold diff — and the fold resolved the mechanism half of it: the `/spec/` anchor lands, and re-measured at HEAD it selects 316 of 1025 tracked paths with zero hits outside a spec directory. What survives is the OBSERVATION half: AC3 still cannot be answered. Two of three blockers closed, one carried forward in a new form.

## By kind — where this round's findings came from

- **FOLD_BROKE 20** (the fold created it) · **FOLD_WRONG 6** (the fold misread its own round-1 finding) · **ROUND1_MISSED 3** (round 1 never saw it).

That ratio is the round's headline measurement, and it says three things.

**The fold read round 1 well.** Only 6 of 29 confirmed findings are the fold misreading an instruction it was given, and only 3 are defects round 1 failed to raise at all. Coverage and comprehension are not the problem.

**The fold's own new text is the problem.** 20 of 29 confirmed findings — 69% — live in prose the fold wrote. For comparison, the fold round measured at `memory/builds/dFramedEntrypoint/reviews/2026-08-24-review-TOOL-dFramedEntrypoint-1-spec-audit-round2.md` split 25 FOLD_BROKE / 17 FOLD_WRONG / 20 ROUND1_MISSED of 62, where FOLD_BROKE was 40% of the population. Here it is the dominant source, not a residual. Both surviving blockers are FOLD_BROKE, and both are the same fold-written sentence.

**Net-productive on the bounded metric, expensive everywhere else.** The blocker count still fell strictly, so the fold moved the number the loop is bounded by. It did so while generating more new surface than it closed. That is the exact proposition unit 2 exists to record as a bug class, and this round is its own evidence for it.

## Confirmed findings

### BLOCKER — 2

| # | Where | Kind | Claim | Fix |
|---|-------|------|-------|-----|
| 1 | spec-2 §5 AC3 (answers round-1 blockers 1/13/31) | FOLD_BROKE | AC3's rewritten command can never pass. `gotchas.py --for-paths memory/builds/dFramedEntrypoint/spec/` normalises to `memory/builds/dFramedEntrypoint/spec` (`normalise_paths` at `gotchas.py:355` does `q.rstrip("/")`), and none of `selectable()`'s three arms matches the `/spec/` anchor against it: `anchor in p` fails on the missing trailing slash, `p in anchor` fails, and `os.path.basename('/spec/')` is the empty string. Verified by importing the shipped module — `selectable('/spec/', ['memory/builds/dFramedEntrypoint/spec'], 'memory')` returns `set()`, while the same anchor against the FILE `memory/builds/dFramedEntrypoint/spec/2026-08-24-spec-dFramedEntrypoint-1.md` returns that path. The anchor arm S2 took is correct; it is the observation that is broken. The one criterion the whole blocker trio was folded to produce is unsatisfiable, and the obvious recovery is to re-add the `memory/builds/` anchor F1 refused as noise. | Point AC3 at a FILE under that directory: `python tools/memory-tree/gotchas.py --for-paths memory/builds/dFramedEntrypoint/spec/2026-08-24-spec-dFramedEntrypoint-1.md`. That form is what round-1 finding 1 actually ran, and it selects once `/spec/` is a derived anchor. Keep the `0 class(es) selected by an anchor + 4 universal` baseline sentence — it is true of the file form too. |
| 10 | spec-2 §5 AC3 (the fold's replacement for the round-1 blocker trio) | FOLD_BROKE | Same defect from the underspecification lens, and it names the consequence: after a CORRECT build, AC3's literal command still prints `0 class(es) selected by an anchor + 4 universal` — byte-identical to the string AC3 itself cites as "how the criterion can fail". So the pass state and the failure signature are indistinguishable, and a builder who runs it concludes the `/spec/` anchor does not work and reverts the one fix round 1's three blockers bought. The mechanism itself is sound: `/spec/` harvests through `ANCHOR_RE` and selects 316 of 1025 tracked paths with zero non-spec hits, and `--for-diff` derives FILE paths from git, so a real fold diff does select. Only AC3's invocation is broken. | Point AC3 at file paths, not a directory (or at `--for-diff` over a real fold range), and re-state the today-line against that same invocation so the failing and passing observations differ. |

**Findings 1 and 10 are one defect with one fix surface: the argument on AC3's command.** Two lenses reached it independently and both re-derived it by running the shipped module rather than reading the spec's prose. Fixing the argument closes both. Leaving it and adding the `memory/builds/` anchor instead would undo F1's own measured decision, which is why the fix is the invocation and not the anchor set.

### HIGH — 5

| # | Where | Kind | Claim | Fix |
|---|-------|------|-------|-----|
| 3 | spec-3 §2 S4 vs S7, §4 Data model and §6 AC8 (answers round-1 9) | FOLD_BROKE | Folding round-1 finding 9 gave S4 TWO new exits — the guarded all-lenses-dead branch and a separate zero-length-`LENSES` branch with its own `note` — but every section that enumerates exit paths still speaks of one. S7 binds "the success path AND the new early path", §4 Data model says "The new early return carries the same fields", and AC8 reads "the success return block and the new early return block of each sibling". Nothing requires the zero-configured branch to carry `lensesRun`, `lensesDead`, `skepticsDead`, `conflicts`, `duplicates` or `spurious`. A builder can ship it as `return { wave, note: 'no lenses configured' }`, pass AC4 and AC8, and violate §4's own load-bearing sentence — "every counter on every exit path, or the port is the same refusal on a different filename". | Make the plural explicit: S7 reads "the success path AND EACH new early path"; §4 Data model says both new returns carry the seven fields with report and summary null; AC8 enumerates three blocks per sibling (success, all-lenses-dead, zero-configured) and asserts all seven in each. |
| 11 | spec-1 §6 AC5, against §2 S4 and the fold-added S4b | FOLD_BROKE | The fold rewrote AC4 into AC5 as an ORDER assertion and DELETED the previous revision's clause observing that the prompt states the closed verdict set. rev-1's AC4 read "it contains the literal string `## Verdict:` and states the closed verdict set"; rev-2's AC5 stops at the literal heading. No criterion in AC1-AC10 now mentions the verdict tokens, so S4's "the prompt states the set and states that it is closed" and all of fold-added S4b — which makes the SOURCE load-bearing, read from hygiene check 22 / `REVIEW_VERDICT_CUTOFF` — have no acceptance criterion. Round-1 finding 44 asked to ADD ordering to AC4, not to replace what was there. A builder can now satisfy every criterion with a prompt naming no verdict token at all, or naming a set that disagrees with check 22. Distinct from the parity-GATE class §7 correctly parks: this is a build-time observation the fold removed. | Restore the deleted clause as a second sentence on AC5, or add AC5b: when the synthesis prompt is read, it enumerates the verdict tokens, states the set is closed, and the enumerated tokens are byte-equal to the set hygiene check 22 enforces under `REVIEW_VERDICT_CUTOFF`. |
| 12 | spec-3 §6 AC2, against §2 S3 and §4 Data model | FOLD_WRONG | AC2 is scoped to `drift-audit-state.js` only, so `drift-audit-code.js` gains `lensesRun` with no criterion that it is the SURVIVING count rather than the configured one — the exact false-clean the unit exists to kill, one file over. S3 requires both siblings to carry the surviving count and §4 says "`lensesRun` is an integer in both siblings after this unit", but AC8 is presence-only. `drift-audit-code.js` returns no lens field today and `LENSES.length` is already in scope at `drift-audit-code.js:226`, so `lensesRun: LENSES.length` is the cheapest thing a builder writes — and it is precisely the defect §1 charges to `drift-audit-state.js:378`. It passes AC1, AC8 and every other criterion. | Extend AC2 to both files: when each sibling is read, `lensesRun` is an integer derived from the length of the post-`filter(Boolean)` survivor array (`lensOut` at `drift-audit-state.js:234` and `drift-audit-code.js:218`) and not from `LENSES.length` or from any `r.lens` string. |
| 18 | build README `## Expected improvements` bullet 1 and `## Detriments` bullet 3, against `memory/guides/BUILD-METHOD.md` M4 and spec-1 §1/§5 | FOLD_BROKE | Both fold-rewritten slots credit unit 1 with taking the M4 convergence loop's blocker count off hand-typing. M4 is the SPEC AUDIT, it is where the count-bounded rule lives, and its own text says "Not the harness. `tier2-review.js` reviews DIFFS; a spec is not code." The enabling change is P1, which is parked. So unit 1's returned `blockers` reaches only M8's closing diff-review, and the M4 loop keeps its hand-typed number — exactly what Detriments bullet 3 still asserts, so the two fold-written slots contradict each other. This build's own park row `blockers 3` in RUN.md was produced by a hand-written spec-audit driver unit 1 never touches, so M8's "re-read the build README against the code before closing" cannot close honestly. spec-1 §1 and §5 carry the same overclaim. | Rewrite the Expected-improvements bullet to what unit 1 actually buys — M8's closing diff-review returns its own blocker/high counts instead of the caller reading them out of the report — and either drop the M4 Detriments bullet or restate it as a detriment this build does NOT remove because P1 is parked. Make spec-1 §1 and §5 say "the closing diff-review's count" rather than "the convergence loop". |
| 20 | spec-3 §2 S4 against §2 S7, §4 Design/Data model and §6 AC8 | FOLD_BROKE | Second view of finding 3, from the contradiction lens, and it adds the §5 half: S4's fold-new split creates two new return branches, but S7, §4 Data model and AC8 each speak of exactly one, while §5 correctly counts three empty states. AC4 reaches the second branch but demands only that it exist with "a different `note`" and binds no counters to it. So a builder can ship the zero-configured branch returning a bare `note` and pass AC1 through AC10 in full, recreating the asymmetry AC8 exists to forbid one branch further along. §2/§4/§6 govern two exit paths while §5 counts three, in a unit whose entire subject is exit-path accounting. | Rewrite S7 as "the success return AND both new early returns carry the same seven fields", extend AC8 to name all three blocks per sibling, and add one clause to §4 Data model stating what the zero-configured branch reports for `lensesRun`, `lensesDead` and `skepticsDead` (all zero, with the `note` naming the misconfiguration) so the counters are not confused with lens deaths. |

Findings 3 and 20 are one defect reached by two lenses — the field rules did not follow S4's fold-new branch split. One fix closes both.

### MEDIUM — 16

| # | Where | Kind | Claim | Fix |
|---|-------|------|-------|-----|
| 4 | spec-3 §2 S8 (answers round-1 12), against spec-1 §6 AC7 | FOLD_WRONG | Round-1 finding 12 was folded into spec-1 as AC7, but spec-3's S8 — the identical provenance-comment obligation, and the one the finding's own text calls "load-bearing for the port" — still has no acceptance criterion. spec-3's ten ACs cover S1-S7, S9 and S10; S8 is the only scope item nobody observes. The fold fixed the instance the finding's `Where` named and left the sibling the finding's body named. | Add an AC in AC7's shape: `grep -n 'dTieredTribunal-3' tools/workflows/drift-audit-state.js tools/workflows/drift-audit-code.js` returns a hit beside each ported guard in both files — the dead-lens count, the all-lenses-dead return, the dead-skeptic count, the verdict-join counters and the synthesis-death log. |
| 5 | build README `## Expected improvements` bullet 1 and `## Detriments if this is not built` bullet 3 (answers round-1 42) | FOLD_WRONG | Same slot as finding 18, from the fold-landed lens: round-1 finding 42 asked for the slot to be rewritten to what units 1-3 actually buy, and the rewrite narrowed the overclaim without removing it. The bullet now contradicts the README's own "The headline goal is NOT delivered" rule eight lines below. | Restate bullet 1 as what unit 1 buys where it applies — a diff-review's blocker and high counts become returned values instead of prose a reader re-counts — and add one clause noting the M4 spec-audit loop keeps hand-typing its count until P1 is ratified. Rewrite the matching Detriments bullet the same way. |
| 6 | spec-3 §9 Revision log, the finding-39 paragraph (against `tools/check-kit-versions.sh:169-194`) | FOLD_BROKE | The justification for refusing finding 39's version half rests on a false statement about the gate: "the unguarded `kit version markers` leg reds on a contract change that leaves the version behind". It does not. That checker asserts only internal CONSISTENCY — `KIT_DRIFT_AUDIT_VERSION` == the README marker == each sibling's `meta.version` == each sibling's inline `gov:kit` marker. A behavioural change with no bump at all leaves all five sites equal and the leg green. S9's own sentence ("a partial bump reds") is correct; the §9 sentence generalises it into something the checker cannot see — the structural-read-as-semantic false-confidence class, recorded in the one document a resumed session reads to learn what was decided and why. | Correct the sentence: the checker asserts consistency across the five sites, so a PARTIAL bump reds and a missing bump is invisible to it; the bump is owed by kit-contract discipline, and AC9 is the only observation of it. Leave the decision (bump to 1.7) standing — it is defensible on its own ground. |
| 7 | spec-3 §6 AC10 and §2 S10 (answers round-1 17/50) | FOLD_BROKE | AC10 requires the refreshed dossier's remaining gaps to still name "the absent `args` parse … with the evidence it carries today". That bullet, at `memory/map/features/review-harnesses.md:89-91`, names only `drift-audit-state.js`. spec-3 §3 and backlog row `TOOL-dTieredTribunal-4` both record the defect as live in BOTH siblings, at `drift-audit-state.js:47` and `drift-audit-code.js:48`, and both are byte-identical `const a = args \|\| {}`. The criterion freezes a false-by-omission claim in the layer sessions are told to trust over prose, in the same commit where the spec and the backlog state it correctly for two files. | Add the correction to S10 and to AC10: the args-parse gap bullet names both siblings with both line citations and cites `TOOL-dTieredTribunal-4` as where it survives. Prose-only, so §4's "no re-render of `memory/map/generated/` is owed" still holds — the generated artifacts carry claims and symbols, not dossier prose. |
| 13 | spec-1 §2 S5 and §6 AC6 | ROUND1_MISSED | S1 makes `highs` a REQUIRED schema field and S2 returns it, but S5's prompt sentence and AC6's observation both define only `blockers` — so `highs` ships as a required integer with no stated population, which is the defect the unit exists to fix, on the second field. §4 Data model does say "Both fields mean the same thing", but no scope item puts that in the prompt and no criterion reads it, so a blockers-only sentence passes every AC while `highs` is whatever the agent decides. Round-1 finding 3 named `blockers` only and the fold reproduced the asymmetry verbatim. | Extend S5 and AC6 to both fields: the prompt states that `blockers` and `highs` are the counts of CONFIRMED findings at blocker and high severity respectively, as adjudicated by the synthesis pass. |
| 14 | spec-2 §7 F1 against the §5 acceptance set | FOLD_BROKE | F1's whole argument is that `/spec/` buys the fold surface "and no noise" while `memory/builds/` buys 374 non-spec hits, yet no acceptance criterion can fail if the finished record's DERIVED anchor set is wider than that — S2 itself says the set is "whatever the finished body contains". Anchors come from every backtick-quoted path-like token in the body. AC3 is a single positive selection; `--check` carries only index freshness, the declares-a-gate arm, the inert-anchor arm and the universal budget, and none bounds anchor WIDTH. A body citing a backticked `memory/builds/` while explaining where fold diffs live silently converts the record from 316 selectable paths to 690, of which 374 sit outside any spec directory — the near-universal selection F1 refused by name, arriving with every gate green. | Add a negative and a width observation: `--for-paths` over a non-fold, non-governance file (e.g. `tools/run-gates/run-gates.sh`) does NOT list the class, and `python tools/memory-tree/gotchas.py --report` shows the new record with exactly the anchor count S2 accounts for. |
| 15 | spec-3 §2 S8 (provenance comments) | FOLD_WRONG | Second view of finding 4, from the underspecification lens, and it prices the surface: S8 covers roughly eight ported guard sites across two files, §10 Reuse audit rests the entire port on provenance travelling with the copied code, and S8 produces no behaviour so no gate reds without it. One build now grades one rule two ways — spec-1 got AC7, spec-3 got nothing. Without it the guards land in both siblings stripped of the defect ids that earned them, which is the re-loss §1 says the unit exists to stop. | Mirror spec-1's AC7: `grep -nE 'aGuardedTally\|aFoldedQuarry\|aBoundedVerdict' tools/workflows/drift-audit-state.js tools/workflows/drift-audit-code.js` returns a hit at each ported guard site in each file, naming the unit that originally earned it. |
| 21 | spec-3 §6 AC10 against §2 S10 and `memory/map/features/review-harnesses.md` Gaps bullet 1 | FOLD_BROKE | S10 says landing the unit "falsifies every clause" of the dossier's first Gaps bullet, but AC10 enumerates only six clauses and omits two the unit also falsifies: "Both resolve a disagreeing repeat verdict by keeping whichever arrived first" (killed by S5) and "`drift-audit-code.js` returns no lens information at all" (killed by S3). A builder who satisfies AC10 literally leaves two false sentences standing in the dossier, and the codebase-map legs cannot catch it — spec-3 §4 itself states S10 edits dossier PROSE only, so `codebase-map coverage + freshness` has nothing new to compare. | Extend AC10's negative list to all eight clauses, or replace the enumeration with the derived form S10 already uses: after this unit no clause of that bullet describes live behaviour, and the criterion is that the bullet is rewritten rather than edited. |
| 24 | spec-1 §2 S6 and §6 AC7, against §2 S4, S5 and S7 | FOLD_BROKE | S6's fold-new sentence "The edited sites are the schema, the success return, and the three early returns" is false of this unit's own scope: S4 and S5 both edit the synthesis prompt and S7 edits line 3 — seven edited sites, not five. S6 excludes only the version line and gives a reason for it; the two prompt edits are neither enumerated nor excluded, so S6's opening clause ("a comment on each edit") contradicts its own list, and AC7 hard-codes "the five edited code sites". The convention does cover prompt text — `tier2-review.js:349` already carries a provenance comment on exactly the prompt block S4 edits. The two scope items round-1 finding 12 called likeliest to be silently dropped are the two left with no provenance and no stated reason for having none. | Either extend S6 and AC7 to a sixth site — the synthesis prompt block around `tools/workflows/tier2-review.js:357` — or state in S6 why prompt text is excluded, as it already does for the version line. Either way, make AC7 count the sites S6 names rather than repeating a literal "five". |
| 25 | spec-2 §5 AC4 against §2 S4, and `tools/codebase-map/test_codebase_map.py:79` | FOLD_BROKE | AC4 says the test "adjudicates both halves: that the new `gotcha-classes` key is claimed by a dossier, and that it no longer sits unclaimed in `memory/map/baseline.toml`". The test asserts the DISJUNCTION — `compute_coverage`'s rule 1 is `inv - claimed - base` — so a builder who adds the new key to `baseline.toml` instead of claiming it passes AC4 and AC5 with nothing red. S4's actual content, that `memory/map/features/build-method.md` takes the claim, is observed by nothing, and the baseline AGENTS.md says shrinks would silently grow by one. | Split AC4: keep the leg run as the no-regression half, and add a direct read — `grep -n 'fold-text-is-unreviewed-surface' memory/map/features/build-method.md` returns a hit AND the same grep over `memory/map/baseline.toml` returns none. That is the only pair that distinguishes claiming from baselining. |
| 28 | spec-3 §4 Design "Inventory" and §5 error/empty/loading states | FOLD_BROKE | The Inventory's fold-added completeness disclosure names exactly one further absence (the `args` guard) when there are two. `tier2-review.js:220-225` is a nothing-raised early return — distinguishing `clean: 0 findings` from `partial: N/M lenses died, survivors found nothing` — and neither sibling has it; spec-1 S3 enumerates the same exit, so the build's two specs disagree about what the sibling delta is. §5 then counts "the three empty states" where there are four. After this unit a sibling whose lenses partly died and whose survivors found nothing still spawns a synthesis over an empty finding set and returns a written report. | Add a row to the Inventory for the `:220` guard / `:225` return, and either port it as an S-item (both siblings can reach `indexed.length === 0` with `lensesDead < LENSES.length`) or name it in §3 with the reason it is excluded. Correct §5 to four empty states, and make §4's disclosure sentence enumerate both further absences rather than one. |
| 30 | spec-1 §2 S4 and §6 AC5 | FOLD_WRONG | The fold's new opening-ORDER rule settles two of the three sentences in the prompt that claim the record's opening and never places the third: `tier2-review.js:349` reads "Open the report with a line naming the reviewed range … and state the ROUND". S4 names three elements — the `**Serves:**` binding line, then "the record's title and provenance", then the verdict heading — and never says whether `:349` IS the provenance element or a fourth claimant on position one. AC5 observes only `:357`, so `:349` is observed by nothing, and the builder ships a prompt carrying two imperatives about what opens the record for the agent to arbitrate — the resolution mechanism round-1 findings 34 and 44 were raised to remove. `BIND_HEAD_LINES = 12` means check 21 will not red it, so the spec's own order rule is unenforceable. | Name `:349` in S4. Either fold its content into the provenance element and reword it from "Open the report with" to a non-positional phrasing, or state the four-element order explicitly. Extend AC5 to read `:349` as well as `:357`. |
| 31 | spec-3 §2 S5 and §4 Design, Data model | FOLD_WRONG | The headline sentence is a misreading — S5's "no field is added to carry it" means this unit adds none — but the underlying defect holds. `drift-audit-code.js:309` derives `downgrades` from `partial` verdicts carrying a `severityCorrection`, interpolates it into the synthesis prompt at `:345` and returns it as `severityCorrections` at `:369`. S5's demotion rule moves a conflicting `partial` to `unverified`, silently decrementing that returned and printed count — so the loss S5 says it is recording is subtracted from a number the report prints. spec-3 names neither `severityCorrections` nor `downgrades` anywhere, and §4 Data model states which existing fields are untouched over a return the spec has not fully modelled. | Name `severityCorrections`/`downgrades` in §4 Data model, and state in S5 whether a demoted `partial` still counts as a severity correction (it should not — the verdict is gone). Reword S7 from the seven new fields to "every field the success return carries, plus the seven", so each sibling's early return is specified against that file's actual return rather than a shared list. |
| 32 | spec-3 §2 S9 and §4 Design, Files touched | FOLD_BROKE | S9 edits `tools/drift-audit/README.md` for the marker byte alone, in a README whose `:7` already carries `**Migrating 1.0 → 1.1 (breaking, \`args\` only).**` — the kit's established shape for announcing a breaking contract change. §4 Migration states plainly that `lensesRun` changes type AND meaning at `drift-audit-state.js:378`, the one breaking change in the unit. An adopter who pulls 1.7 reads a README whose only migration note describes a change six versions old, and nothing tells them `lensesRun` stopped being an array of slugs. The in-tree safety argument (no tracked caller reads it) is about THIS repo and says nothing about an adopter's callers. | Extend S9 to add a `**Migrating 1.6 → 1.7 (breaking, return shape).**` paragraph beside the existing one, naming `lensesRun` array→integer and the new early return, and extend AC9 to observe that the paragraph exists and names 1.7. |
| 35 | spec-1 §7 Gates (with §2 S7 and §6 AC8) | FOLD_BROKE | The `kit version markers` leg that §7 names as "which S7 is the reason to care about" cannot see S7's bump. `tools/check-kit-versions.sh:24` is a bare presence check against `[0-9]+\.[0-9]+`, and `grep -n tier2-review` over that gate returns that one line — no `gov:kit tier2-review@` pair assertion exists anywhere, and neither `marker-contract.test.sh` nor `govkit.py` covers it. So AC8's first clause is satisfied by the file as it stands at 1.2 and cannot fail on S7. `tools/workflows/kit.toml:6` derives the kit version from the `version:` field while the deployer greps the marker, so a half-bump ships two answers to adopters over a green bar — the class `TOOL-aBoundedVerdict-29` records costing a 1.4→1.5 bump three settling rounds. | Either add the pair assertion for the review-harness kit to `tools/check-kit-versions.sh` in this unit, copying the agent-cap block that already solves the same-line case, and name it in §4 Files touched; or rewrite §7 to state that the leg presence-checks the field only, drop the "which S7 is the reason to care about" clause, and make AC8's grep — not the leg — the whole observation for S7. |
| 36 | spec-3 §2 S10 and §6 AC10, against `memory/map/features/review-harnesses.md` Gaps bullet 4 | FOLD_BROKE | Prior-art view of finding 7, and it corrects the address: it is S10, not AC10, that directs the freeze. S10 says "The three remaining gaps stay standing with their evidence", which preserves a bullet naming one sibling in the same commit that lands the correct two-sibling fact in spec-3 §3 and in backlog row `TOOL-dTieredTribunal-4`. A later session reading the dossier for the reuse decision on `drift-audit-code.js` is told that file is clean on the one gap the build deliberately deferred. | Extend S10 to require the args bullet corrected to name both siblings with their line citations and to cite `TOOL-dTieredTribunal-4`, and have AC10 assert the corrected wording rather than "the evidence it carries today". The other two standing gaps can keep the freeze. |

### LOW — 6

| # | Where | Kind | Claim | Fix |
|---|-------|------|-------|-----|
| 8 | spec-1 §7 Gates, second paragraph | FOLD_BROKE | "Four of those are unguarded in `tools/gate-legs.json` and run on every bar" then names five: `workflow script syntax`, `review-join ban (no ref-keyed join)`, `verifier fan-out`, `agent-cap restatement` and `kit version markers`. All five carry no `guard` and `subject: repo`. Four plus the three guarded legs also fails to account for the eight legs the paragraph above names. A derived count typed in prose beside the manifest that owns it — the class AGENTS.md §7 bans by name, and the one sibling spec-3 §7 deliberately avoids by naming legs without counting them. | Drop the numeral: "These carry no guard and `subject: repo`, so they run on every bar:" followed by the five names. Do the same for the "Three are guarded" sentence. |
| 9 | spec-2 §7 F1, the anchor-arm measurement paragraph | FOLD_BROKE | All three absolute figures are stale at HEAD, staled by the commit that wrote them. F1 says "over the 1024 tracked paths" and that `memory/builds/` "selects 689 of 1024 with 373 of those outside any spec directory". Re-measured at HEAD with the shipped `selectable()`: 1025 tracked paths, `memory/builds/` selects 690 with 374 non-spec — all three off by exactly one. The fold measured at 3b5f9b11 and then added the 130-line round-1 review record under `memory/builds/` in the same commit as the text, which is +1 tracked, +1 `memory/builds/` hit, +1 non-spec, +0 `/spec/`. The `/spec/` figure (316) still reproduces, so the argument survives; the credibility of a paragraph whose whole job is to show the measurement was run does not. | Either pin the measurement to the sha it was taken at, or drop the absolute counts and keep the ratio and the qualitative result: `/spec/` selects only paths under a spec directory, `memory/builds/` selects more than half the tree with a majority outside one. |
| 16 | spec-2 §2 S5 (the tracked-evidence requirement) | ROUND1_MISSED | S5 requires the record's evidence to cite a specific tracked file, and no acceptance criterion reads the record's body for it. AC1 is an `ls`, AC2 is `gotchas.py --check` (index freshness, the declares-a-gate arm and the inert-anchor arm, none of which reads evidence), AC3 is selection, AC4/AC5 are the map and the bar. The 25/17/20-of-62 split in §4 is the record's entire justification and is verifiable only because that path is tracked. Nothing stops a builder writing the record with a paraphrase and no citation, at which point the class carries an untraceable measurement and the anchor set loses a token nobody counted. | Add an AC: when the new record is read, its evidence cites `memory/builds/dFramedEntrypoint/reviews/2026-08-24-review-TOOL-dFramedEntrypoint-1-spec-audit-round2.md` and `git ls-files` returns that path. |
| 17 | spec-1 §6 AC2 | FOLD_BROKE | The fold added the pin `:390` to AC2, which the unit's own S6 comments guarantee will be stale before the criterion is answered. rev-1's AC2 read "When the success return block is read" with no line number; `:390` is correct at base, but S6 requires provenance comments at the schema (`:366`) and the three early returns (`:215`, `:225`, `:313`) — all above 390 — so the unit's own edits move the success return before AC2 is verified. AC2 is spec-1's only line-pinned criterion. Sibling spec-3 S3 states the identical hazard outright ("a base pin, not a live one"), so one build treats one hazard two ways. | Either drop `:390` from AC2 and name the block by its comment (`// H1: the SUCCESS return carries the same trust counts`), or add spec-3's one-sentence base-pin disclosure to spec-1 §2 covering all six citations. The load-bearing half is AC2 alone. |
| 37 | spec-2 §5 AC4 | FOLD_BROKE | Prior-art view of finding 25, and it adds the second half: a brand-new key was never in `baseline.toml`, so nothing can "no longer sit" there, and `test_every_inventory_key_is_claimed_or_baselined` is satisfied by a baseline line INSTEAD of a dossier claim. The "baseline.toml is reserved for the initial backfill" rule exists only as prose — the gate docstring, the assertion message, and the baseline's own header, which states outright that nothing enforces it. So AC4 replaces round-1 finding 2's defect (an AC naming a command that cannot answer it) with an AC naming the right command and describing its semantics wrongly. | Make AC4 observe the artefacts directly: `memory/map/features/build-method.md`'s `gotcha-classes` list names the new record, `memory/map/baseline.toml` gains no line for it, and `python tools/codebase-map/test_codebase_map.py` exits zero. |
| 38 | spec-1 §3 Non-goals, the "Renaming the harness" bullet | ROUND1_MISSED | "Refused three times on record" transposes the wrong prior-art count onto the rename. The build's own research record makes prior-art item 1 the UPSTREAM-ADOPTION refusal, three-times-refused with three citations; item 2 records the rename refusal ONCE, in `memory/builds/aFoldedQuarry/spec/2026-08-08-spec-aFoldedQuarry-1.md` §3. The "three" that genuinely belongs to the rename is the three tracked documents the same sentence already names, and all three still name the file at HEAD. A fabricated refusal count sits in the section whose only job is carrying prior art forward, in the spec a later kind-parameter unit will reason from. | Reword to: "Refused once on record — `memory/builds/aFoldedQuarry/spec/2026-08-08-spec-aFoldedQuarry-1.md` §3 — and three tracked documents name the file: `AGENTS.md`, `README.md`, `WIRE-INTO-PROJECT.md`, all still naming it at HEAD." |

## Unverified — 0, and that is positive evidence

**Zero findings are outstanding for want of a verdict, and none is cleared by that fact.** Every one of the 38 raw findings received a usable skeptic verdict, so nothing sits in the "raised but never adjudicated" bucket a reader would have to treat as neither confirmed nor refuted.

The zero is positive evidence rather than an absence because it is asserted against liveness facts recorded in the same shape: 5/5 lenses returned and 5/5 skeptic batches returned. A zero unverified count produced by dead batches would be indistinguishable from this one on the count alone — the difference is that here the denominator moved. Precision 0.76 is therefore measured over the whole raw population, and the confirmed set is 29 of 38 rather than 29 of some smaller number nobody wrote down.

## Refuted — 9

- **2** [high] {FOLD_BROKE} spec-1 §6 AC5 — factual half true, defect does not follow on three grounds: round-1 findings 34 and 44 asked for the substring clause to be REPLACED by an order assertion, the residual is finding 45's standing half already recorded as OPEN under `TOOL-dUnstalledConvoy-16`, and the proposed fix creates the third carrier round 1 refuted at finding 5.
- **19** [high] {FOLD_BROKE} spec-3 §1 Goal — a misparse of S7: its first sentence enumerates a CLOSED seven-field set by name, and the "in the shape `tier2-review.js` already uses" clause governs the `note` string in the SECOND sentence, not the field list. `tier2-review.js`'s success return already carries fifteen fields at base. §3 already flatly forbids changing that file.
- **22** [medium] {FOLD_BROKE} spec-3 §6 AC10 — AC10's clause is a PRESERVATION floor, not a freeze: a builder who corrected the bullet to name both siblings would still satisfy it. The bullet is incomplete rather than false, on a file whose args defect §3 declares a non-goal, and `memory/backlog/TOOL.md:219` names both files with both citations so the picked-up row cannot be scoped to one.
- **23** [medium] {FOLD_BROKE} spec-1 §7 Gates — no spec claims what the finding refutes. spec-1 §7 says only that S7 is the reason to care about the leg, which is accurate; spec-3 S9's pair assertion is explicitly scoped to the drift-audit sites and true of them. AC8's second clause already binds marker and `meta.version` on one line, so a half-bump fails it.
- **26** [low] {FOLD_BROKE} spec-1 §7 and spec-2 §6 — the finding concedes no conclusion is currently false, and the manifest confirms it: all five legs spec-1 names are `guard: null, subject: repo`. spec-1 §7 already carries the arming-versus-running distinction two sentences later. A request that two specs restate spec-3's disclaimer.
- **27** [high] {ROUND1_MISSED} spec-1 §2 S1 — the load-bearing mechanism is conceded unverified by the filer; the class pre-exists S1 (`tier2-review.js:366` is already `required: ['path','summary']`); the protocol citation is about `additionalProperties` and stray keys, not `required`; and "S1 buys nothing S3b does not" is false, since without `required` an omitted field yields `undefined`, which S3b bans.
- **29** [medium] {FOLD_BROKE} spec-1 §2 S7 and §7 — the finding's own impact concedes AC8's grep binds the pair while claiming a half-bump lands green. AC8 requires the grep to return the single line 3 that also carries `version: '1.3'`, so the criterion it says cannot fail is not the criterion. Editorial ask to state what the gate does not check.
- **33** [low] {ROUND1_MISSED} spec-2 §2 S4 — S1 already fixes the claim string by naming the record file, and the inventory key IS that basename; sibling dossiers show the form, and `test_codebase_map.py` prints the exact UNCLAIMED key back, so a wrong guess is self-naming and one line to fix. The proposed remedy is a second copy of a filename the spec already carries.
- **34** [low] {FOLD_BROKE} spec-1 §7 Gates — spec-1 §7 says "the named legs this unit must keep green" and makes no completeness claim to disclaim. By spec-3's own rule (the legs whose SUBJECT this unit touches) spec-1's omissions are CORRECT: unit 1's diff is confined to `tools/workflows/tier2-review.js`, which is not the subject of the four legs named.

## What a fold should take, and what is recorded and NOT folded

The count round 3 measures is the CONFIRMED BLOCKER count, and it stands at **2**. It fell strictly from 3, so the loop re-arms rather than stopping, and no blocker is promoted to a unit of this build.

**Fold now — the blocker, as one edit.** Findings 1 and 10 are one defect on one line: AC3's `--for-paths` argument. Replace the directory with the file `memory/builds/dFramedEntrypoint/spec/2026-08-24-spec-dFramedEntrypoint-1.md` (or with `--for-diff` over a real fold range) and re-state the today-line against that same invocation, so the failing observation and the passing one differ. Do NOT reach for the `memory/builds/` anchor: F1 refused it on a measurement that still reproduces, and adding it would undo the fork the fold resolved rather than fix the criterion that observes it.

**Fold now — the HIGH tier, which collapses to four distinct defects.**

1. The unfollowed branch split — 3 and 20. S4 now has two new exits; S7, §4 Data model and AC8 still govern one, and §5 already counts three. One rewrite across those four sections.
2. The deleted token clause — 11. Restore the verdict-set observation to spec-1 AC5, or add AC5b binding it to check 22's set.
3. The one-sided criterion — 12. Extend spec-3 AC2 to `drift-audit-code.js` and pin `lensesRun` to the post-`filter(Boolean)` survivor array in both siblings.
4. The README's surviving overclaim — 18 with its medium twin 5. Rewrite both slots to what unit 1 buys at M8's diff-review, and say the M4 loop keeps hand-typing until P1 is ratified. Amend spec-1 §1 and §5 in the same pass.

**Fold now — the MEDIUM tier, all sixteen, as eleven edits.** 4 and 15 are one AC on spec-3 S8. 7 and 36 are one correction to the dossier's args bullet, addressed at S10 rather than AC10. 25 and 37 are one rewrite of spec-2 AC4 into a three-part direct observation. The rest are single edits: 6 (correct the §9 rationale sentence about what `check-kit-versions.sh` sees), 13 (define `highs` in S5 and AC6), 14 (an anchor-width negative on spec-2), 21 (AC10's enumeration widened to all eight clauses), 24 (S6's edit-site list and AC7's literal "five"), 28 (the fourth empty state and the completeness sentence), 30 (place `:349` in S4 and read it in AC5), 31 (name `severityCorrections` in §4 and rule on the demoted `partial`), 32 (a 1.6→1.7 migration paragraph in the kit README, with AC9 observing it), and 35 as its TEXT arm only — see below.

**Fold now — the LOW tier, all six.** 8 and 9 are two derived counts to delete from prose. 16 is one AC. 17 is one line pin to drop. 37 folds with 25. 38 is one reworded sentence with the correct citation.

**Recorded and NOT folded — two mechanism halves and one standing item.**

- **Finding 35's gate arm.** No `gov:kit tier2-review@` pair assertion exists in `tools/check-kit-versions.sh`, and adding one is a gate change, not spec text. Fold the TEXT arm — §7 stops calling that leg the reason to care about S7, and AC8's grep becomes the whole observation — and record the pair assertion as a fork for the owner, the same way round 1 handled finding 45.
- **Finding 37's underlying gap.** Nothing machine-enforces that `memory/map/baseline.toml` is reserved for the initial backfill; the rule lives in the gate docstring, the assertion message and the baseline's own header, which says so in as many words. The AC-level fix folds; a growth ratchet does not belong in a spec-text round.
- **Round 1's finding 45 stands, and round 2 confirms it correctly stands.** Refuted finding 2 tried to reopen it as a missing AC; the refutation records that the residual is 45's parity-gate half, still OPEN as `TOOL-dUnstalledConvoy-16`, and that spec-1 §7 declines the gate knowingly. If round 3 reports 45 closed without a gate or a parity pair, that report is the defect.

**What round 3 measures.** Confirmed blockers must fall below 2. Every folded finding above should show either a CORRECTED disposition or an explicit refusal in the spec's own revision log; a folded finding that reappears unchanged is a FOLD_WRONG, and a new finding created by this fold is a FOLD_BROKE. Watch the by-kind split in particular: at 20 FOLD_BROKE of 29 this round, the fold's own prose is now the dominant defect source, and a round 3 that does not move that share is telling you the fold loop is running out of return before the blocker count does.
