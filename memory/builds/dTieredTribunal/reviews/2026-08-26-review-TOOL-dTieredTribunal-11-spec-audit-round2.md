**Serves:** spec-audit TOOL-dTieredTribunal-11 TOOL-dTieredTribunal-12 TOOL-dTieredTribunal-13 TOOL-dTieredTribunal-14 TOOL-dTieredTribunal-15

# Spec audit round 2 — the five-unit spec set of the second run, after the fold

*Node a, 2026-08-26, round 2, over the reviewed range `136cdb70..a19e71b5`. Five finder lenses over the folded specs and the build's own record, then five batched skeptics prompted to REFUTE each finding by re-deriving it against the source at the pinned range.*

## Verdict: CLEAN WITH FIXES

The qualifier sits below the verdict line rather than on it, because that line's token is a closed set and a tally appended to it makes a structural check read as a semantic one.

CLEAN WITH FIXES, on two grounds stated separately. Zero blockers: round 1's single blocker was the unauthorized governance-carrier edit in `TOOL-dTieredTribunal-13` S8, the fold took the PARK arm, and no criterion in any of the five specs is now unsatisfiable or unauthorized — every spec is buildable as written. Not CLEAN: 18 confirmed findings stand, seven of them high, and in every high the scope item says the right thing in prose while the table or the criterion that OBSERVES it is short. A builder who reads §2 ships correctly; a builder who implements the authoritative table and then runs the acceptance set can ship wrong and red their own Definition of Done.

## Review shape

- raw 20, confirmed 18, refuted 2, unverified 0, precision 0.90, lenses 5/5 live, skeptic batches 5/5 live, spurious 0, conflicts 0, duplicates 0.
- confirmed by severity: 0 BLOCKER · 7 HIGH · 6 MEDIUM · 5 LOW
- confirmed blockers: 0

Precision is measured over the whole raw population of 20, not over a survivor subset: no lens and no skeptic batch died, and nothing was demoted or merged away before grading.

## Convergence — 1 → 0, CONVERGED

**Round 1 confirmed 1 blocker. Round 2 confirms 0. One is greater than zero, the count fell strictly, and it has reached zero. The loop's exit condition is met and the verdict is CONVERGED.**

The loop does not re-arm. No blocker stands, so nothing is promoted to a unit of this build, and no round 3 is owed by the count-bounded rule.

The fall is real rather than arithmetic. Round 1's blocker (finding 28) was an act the run was not authorized to perform: spec-13 S8 directed an edit to `memory/guides/REVIEW-PROTOCOL.md` and its shipped template, which M3 veto 2 and this build's own committed rules reserve to the owner. The fold took the park arm the round-1 record named — S8 was split, the `tools/hooks/README.md` bullet stayed in the unit, the protocol half moved to §8 as fork F3, and AC10 was dropped. Re-derived at HEAD: post-fold spec-13 S8 keeps only the README bullet, §6 enumerates AC1–AC9, AC11, AC12, AC13 with no AC10, and no criterion in any of the five specs reaches a protocol carrier. The authorization defect is closed, and it was closed by a park rather than by a rewrite, which is what round 1 asked for.

What the park left behind is weaker but real, and it is why the verdict is not CLEAN: dropping AC10 removed the only criterion in spec-13 asserting a bullet's PRESENCE, and the fold did not re-point that positive half at the carrier that stayed in scope. S8's surviving criterion, AC11, is true at the pinned base and stays true if the bullet is never written (finding 6). That is a could-not-fail criterion, not an unsatisfiable one — a builder can satisfy it, which is exactly the problem — so it is a high and not a blocker.

## By kind — the minimise-new-prose fold, and what it measured

- **FOLD_BROKE 11** (the fold created it) · **FOLD_WRONG 5** (the fold misread or half-landed its own round-1 finding) · **ROUND1_MISSED 2** (round 1 never saw it).

Fold-caused, on the wide reading that counts both fold kinds, is **16 of 18 — 88.9%**. On the narrow reading that counts only defects living in prose the fold wrote, it is **11 of 18 — 61.1%**.

**The instruction did not move the rate, in either direction.** This fold was told to minimise new prose. Against the band this build has already measured — roughly two thirds fold-caused across the earlier rounds — the narrow reading goes 69.0% (run-1 round 2, 20 of 29) → 62.5% (run-1 round 3, 20 of 32) → **61.1% here**, and the wide reading goes 89.7% (26 of 29) → 96.9% (31 of 32) → **88.9% here**. Both are flat. An eight-point drift on the narrow reading across three rounds of a shrinking population is not a measured effect, and the wide reading is where it started.

**What moved is the absolute count, and it moved in step with the diff rather than ahead of it.** FOLD_BROKE fell 20 → 11, a 45% fall. The confirmed population fell 32 → 18 over the same step, a 43.75% fall. Less new prose bought proportionally fewer new defects and no change whatever in the rate at which fold prose breaks. That is the plainest statement the data supports: minimising new prose is a way to make a fold smaller, not a way to make it safer.

**On DISTINCT DEFECTS the picture is the same.** The 18 confirmed findings are 10 distinct defects (below); 8 of those 10 are fold-caused, 80%. Two are not: findings 16 and 20, both pre-existing in text `136cdb70` already carried.

One measurement is worth carrying forward for whoever tunes this next, because it bears on the metric itself. The five lenses that reached the `:192` defect labelled it three FOLD_BROKE and two FOLD_WRONG, and the AC17 cluster split two-to-one the other way. The kind field is assigned per FINDING and the defects are per FIX SURFACE, so the split above is a census of filings and not of defects. It is reported as filed, because that is what the earlier rounds measured too and re-labelling one round's population silently would break the only comparison this build has.

## Confirmed findings

### HIGH — 7

| # | Where | Kind | Claim | Fix |
|---|-------|------|-------|-----|
| 1 | spec-11 §4 Inventory, against §2 S9 and §9 rev-3 | FOLD_WRONG | rev-3 states that the `:192` prompt site "is a genuine per-kind site and the Inventory now carries it"; §4's Inventory has no `:192` row. At HEAD the rows run `:184`, `:194`, `:195`, `:210` with nothing between `:184` and `:194`, and `grep -n 192` over the spec returns only S9 at `:72` and the rev-3 log at `:441` — the site was added to S9's prose alone, and S9 itself says "They are enumerated in §4's Inventory table, which is the authoritative list for this unit". AC9 grades provenance comments only at sites the table lists, so a builder implementing the Inventory exactly leaves `tools/workflows/tier2-review.js:192` reading `PRIOR ROUND'S FINDINGS: none - this is a first-round review of the whole diff.` and reds AC17 — the exact failure rev-3 claims to have repaired. The revision log now carries a false statement, which is the carrier that suppresses the next reviewer's check. | Add the row `` `:192` `` — the round-1 no-prior-findings sentence names the whole diff, per-kind — to §4's Inventory between the `:184` and `:194` rows. If S9 is instead meant to carry it alone, delete S9's "which is the authoritative list for this unit" clause and correct rev-3's sentence, so one list is authoritative and it is the complete one. |
| 5 | spec-11 §4 Inventory, against §2 S9 and §9 rev-3 | FOLD_BROKE | Underspecification view of the same defect, and it prices the acceptance gap. S9 declares the Inventory authoritative, S13 sources its provenance-comment sites from that table, and AC9 grades hits only against rows the table lists as changed or untouched — `:192` is in NEITHER list. A builder therefore leaves the site diff-shaped, gets no provenance comment there, passes AC9, and reds AC17 at their own Definition of Done. Same class as round-1 finding 29 (`:195`/`:376` omitted from the authoritative table), one site over, with the revision log asserting a body change the body does not carry — this build's own `fold-text-is-unreviewed-surface` class. | One row in §4's Inventory. Nothing else moves: S9 and AC17 already carry the site. |
| 10 | spec-11 §4 Inventory, against §2 S9 and §9 rev-3 | FOLD_WRONG | Contradiction view. Of the seven prompt-string `diff` hits in `tools/workflows/tier2-review.js` (`:175 :182 :184 :192 :194 :375 :376`), `:192` is the only one no Inventory row covers — `:175` rides the `:156-177` catalogue row and the rest each have their own. So the list S9 declares authoritative is the incomplete one, and the revision log certifies a repair that never landed. | The one row, then correct rev-3's sentence or delete the claim. The Inventory carrying it is the fix, not the assertion that it does. |
| 14 | spec-11 §4 Inventory, against §2 S9 and §9 rev-3 | FOLD_BROKE | Unstated-assumption view, and it names the cost to the loop rather than to the build: the next round reads the revision log and sees the finding as closed. Verified by reading the body and not the log — `:192` appears in the whole spec exactly twice, at S9 line 72 and inside the rev-3 entry at line 441. | Insert the row between `:184` and `:194`, leave S9's mention as it is, and correct rev-3's sentence only once the Inventory actually carries the site. |
| 18 | spec-11 §4 Inventory, against §2 S9 and §9 rev-3 | FOLD_BROKE | Could-not-fail view. `git diff 136cdb70..a19e71b5` shows the fold added `:195` and `:376` to the table and `:192` to S9 only. AC17's grep DOES return `:192` (hits: 29 63 68 71 97 103 175 182 184 192 194 359 375 376), so the unit reds its own DoD on a site no scope table names. | Insert the row so S9, AC9, AC17 and the rev-3 log all describe one set. Do NOT instead delete `:192` from S9 — AC17 binds it whatever S9 says. |
| 19 | spec-11 §6 AC17, against §2 S9 | FOLD_WRONG | AC17's population is `grep -n "diff" tools/workflows/tier2-review.js`, and three of S9's nine sites carry no `diff` token, so the criterion cannot reach them: `:245` reads `Read/Grep the cited file:line and callers`, `:345` reads `each with file:line + fix + a left-shift gate suggestion`, `:353` reads `Open the report with a line naming the reviewed range as ${base}...${head}`. AC17's own text names `:353` as the failing case "which is its state today" — false, the line is not a grep hit and never fails AC17. Those three are left with AC9's provenance-comment check as their only witness, which round-1 finding 3 already ruled satisfiable by typing a comment beside unchanged code. A run that ships the range line still spelling `${base}...${head}` with no kind selection — the exact defect S9 exists to remove — passes every criterion in §6. `:195` is also outside the population and is separately covered by AC4, which strengthens rather than weakens the finding. | Widen AC17's population to a `grep -nE` whose alternation covers every token the listed sites actually carry — `diff`, `file:line`, and the literal `${base}...${head}` — or add one criterion per uncovered site: the spec-arm range line names the subject set and its blobs and spells no `${base}...${head}`; the `:245` skeptic instruction and the `:345` report instruction name a path and a section rather than `file:line`. Delete the `:353` exemplar sentence either way, since it is false as written. |
| 6 | spec-13 §6 AC11, against §2 S8 | FOLD_WRONG | After the park fold cut S8 to the `tools/hooks/README.md` bullet and dropped AC10, S8's only surviving criterion is AC11 — `bash tools/check-agent-cap-restatement.sh` exits 0 — which is already true at the pinned base and stays true if the bullet is never written. Ran at HEAD: rc=0. AC10 was the spec's only presence assertion (a `gov:fixed-verifiers` count one higher than at base); the fold removed it and did not re-point its positive half at the carrier that stayed in scope. Enumerated §6 at HEAD — AC1, AC2, AC3, AC4, AC5, AC6, AC6b, AC7, AC8, AC9, AC11, AC12, AC13 — and no other criterion reads that README. A build that ships the predicate and never touches the README greens the whole bar with the marked-derivation branch undocumented, which is the half of the 2026-08-08 finding S8 exists to close. NOT a request to unpark F3: the protocol carrier stays parked and untouched. | Give AC11 a presence half over the carrier that remains: `grep -c 'gov:fixed-verifiers' tools/hooks/README.md` returns a count one higher than at `cd971285` (base count is 1, at `tools/hooks/README.md:25`), and the new bullet names the marked-derivation receiver. Keep the existing green-gate half as the no-digit-beside-a-noun check it already is. |

Findings 1, 5, 10, 14 and 18 are ONE defect reached by five lenses independently, every one re-derived against the body at HEAD rather than against the revision log. One row in §4's Inventory closes all five. Finding 19 folds with mediums 2 and 9 on the AC17 population. Finding 6 stands alone.

### MEDIUM — 6

| # | Where | Kind | Claim | Fix |
|---|-------|------|-------|-----|
| 2 | spec-11 §6 AC17 | FOLD_BROKE | AC17's predicate is `grep -n "diff" tools/workflows/tier2-review.js`, and the line it names as its failing case, `:353`, contains no `diff` and is never returned by that grep. Measured at HEAD the grep returns 29, 63, 68, 71, 97, 103, 175, 182, 184, 192, 194, 359, 375, 376 — never 353. Line 353 is an S9 site and an Inventory row whose only other criterion is AC9's provenance comment, so it has no criterion that can fail on it, and it stays unfailable after the change too, since a `kind === 'diff-review'` selector carries the token AC17 expressly allows. One correction to the filing that does not move the disposition: AC17 is not S9's ONLY substantive witness, since AC4's closing clause witnesses the S9 site `:195` by name. AC4 does not reach `:353`, so the subject stands. | Give `:353` its own observation: the range line interpolates `${base}...${head}` only on the diff arm and the subject paths with their pinned blobs on the spec arm. Then delete AC17's `:353` sentence or re-point it at a line the grep actually returns, such as `:192` or `:194`. |
| 3 | spec-14 §2 S8, the scratch-repo paragraph added by the rev-2 fold | FOLD_BROKE | S8 directs the builder to copy `tools/workflows/check-verifier-fanout.test.sh:63` "verbatim" into `check-review-join.test.sh`, and that line cannot run there: it resolves the hook through `$HERE`, which the target never defines, while the target runs under `set -u`. Verified — the source line is `mkdir -p "$E/tools/hooks" && cp "$(cd "$HERE/../hooks" && pwd)/agent-cap.js" "$E/tools/hooks/agent-cap.js"`, its `$HERE` comes from that file's own `:7`, and `grep -n 'HERE' tools/workflows/check-review-join.test.sh` exits 1 while `set -u` is set at `:14` and `ROOT` resolves the gate at `:15-17`. Worse than filed: at the `:133` insertion point `E` is not yet assigned either, so under `set -u` the copied line aborts the whole self-test in the main shell rather than merely failing the `cp`. The third scratch repo copied from the sibling's `:74-79` re-binds `D`, which `:121` already binds to `$TMP/discover`. The word `verbatim` is load-bearing because this spec's own S1 writes "ported verbatim in MEANING" when it means the loose sense; the fold lifted the word out of round-1 finding 22's proposed fix without adapting it, which is why 22's own failure returns. | Spell the adapted line instead of saying verbatim: resolve the hook from `$ROOT` the way `:17` resolves the gate, then `mkdir -p "$D/tools/hooks" && cp "$ROOT/tools/hooks/agent-cap.js" "$D/tools/hooks/agent-cap.js"`, the `$E` twin after `:163`, and a fresh name for the third scratch repo. |
| 7 | spec-11 §4 Inventory row `:194`, against §6 AC4 | FOLD_WRONG | `tools/workflows/tier2-review.js:194` carries TWO diff-shaped things — the address field list `each needs file, line, severity …` and the `nothing outside the diff` clause — and both the Inventory row and AC4's new schema-matching assertion name only one: the row says "the out-of-diff clause", AC4 is scoped to `:195`. Round-1 findings 2 and 38 established that Inventory rows are matched by CONTENT in the prose columns, which is precisely why the unnamed half is unbound. Neither grep reaches it: the file spells `file, line, severity` with spaces at `:194` and `file,line,severity` without at `:195`, so AC4's count stays 1 with the prose half untouched, and that half carries no `diff` token for AC17. A builder makes `:194` per-kind by swapping only the out-of-diff clause and leaves a spec-arm finder told in prose to emit `line` while `SPEC_FINDING_SCHEMA` requires `where` — the forced-regeneration failure round-1 finding 29 described. One correction to the filing: round-1 finding 29 named only `:195` and `:376`, so the kind is ROUND1_MISSED rather than FOLD_WRONG; both are in scope this round and the census above reports it as filed. | Restate the `:194` row's `today` column as "the address field list and the out-of-diff clause" with `per-kind on both halves` as the `after`, and widen AC4's second sentence from `:195` to every finder-prompt line naming the address field. |
| 15 | spec-14 §2 S3, against §3 Non-goals and §5 risks | FOLD_BROKE | Inverting the `offendingLines` early exit-0 makes the join rule reachable for every script the hook sees, which silently widens `check-verifier-fanout.sh`'s verdict too — and §3 forbids touching that file beyond its guard row, so its header keeps describing a gate it no longer is. Mechanism re-derived: `tools/hooks/agent-cap.js:821-822` is `const bad = offendingLines(script)` / `if (bad.length === 0) process.exit(0)`, and `tools/workflows/check-verifier-fanout.sh:72-77` pipes each harness to `node "$HOOK"` with NO `--only` flag. That gate starts enforcing the ref-keyed-join ban while its own header at `:2` still reads "the COMMITTED workflow harnesses obey the review protocol's verifier cap" and `:7` still reads "Exit 0 = clean · 1 = a per-item verify fan-out survives". §5 prices only the widening from seven files to every `Workflow` call and never mentions the sibling gate; `grep -n check-verifier-fanout` over the spec returns no line disclosing it. No wrong merge verdict follows — the join gate's population is a superset and §4 measured zero live hits — so the cost is a merge-bar leg whose header describes a gate it no longer is. | Say it and pay for it. Add one sentence to S3 disclosing that the inversion gives `check-verifier-fanout.sh` the join rule as well, carve its two header lines out of §3's Non-goal, and add an AC reading the corrected legend. |
| 16 | spec-13 §2 S8 and §6 AC11 | ROUND1_MISSED | S8 has `tools/hooks/README.md:24-26` gain a bullet for the third receiver but never amends the bullet already there, whose "must spell" is an absolute the new receiver contradicts. At HEAD that bullet says the marked assignment "carries a `gov:fixed-verifiers` marker and must spell `chunk(x, Math.ceil(x.length / K))` or `splitInto(x, K)`", while S1/S3 make a marked line admissible as a per-branch derivation — and the spec knows it, its last non-goal reading "this unit changes what a marked line must SPELL". An appended third bullet leaves two adjacent sentences in the kit's own grammar document disagreeing about what a marked line may spell, and an adopter reading the shipped README cannot tell which is live. This is the append-a-negation-beside-the-text-it-contradicts class spec-12 §4 refuses by name, citing `memory/gotchas/fold-text-is-unreviewed-surface.md`. Nothing catches it: `tools/check-agent-cap-restatement.sh` keys on a bound word adjacent to a digit over markdown outside the frozen prefixes and has no opinion on this sentence. Not covered by the `TOOL-dFramedEntrypoint-1` carve-out, which is a different sentence and a different row. | Make S8 REWRITE the `:24-26` bullet rather than only appending after it: the marked assignment spells a bounded split OR a derivation whose every top-level value branch qualifies, with the three qualifying forms named. Add an observation that binds — `grep -c 'must spell' tools/hooks/README.md` returns 0, or the receiver list names three forms. |
| 20 | spec-13 §2 S5 and §6 (AC1–AC6b), against §4's predicate block and §5 error/empty states | ROUND1_MISSED | §4 calls `branches.length` load-bearing because `Array.prototype.every` is TRUE over an empty array — "the vacuous-predicate shape this repo gates against, inside the guard being hardened" — and §5 adds the null-branch case for a ternary whose `:` the walk cannot find. Neither guard has an arm or a criterion. S5's four arms are the caller-supplied ternary, the caller-rooted `.filter`, the bounded-split ternary and the two-literal ALLOW, and AC1/AC2/AC3/AC6/AC6b are those same five fixtures; every one parses to at least one real branch and none has an unclosed ternary. Delete `branches.length &&` from the predicate and every arm and every criterion still passes — AC13's whole-tree sweep covers only the four marked lines that ship, all of which parse, and AC5 asks only for strictly more than the 64 arms the suite reports today. So the two guards that stop the tightening from vacuously admitting exactly what it was written to deny ship never having been seen to fail, inside the unit whose subject is a guard that accepted on one qualifying reference and never examined the rest. | Add two DENY arms to S5 and two criteria to §6: a marked assignment whose right-hand side `parseBranches` returns an empty list for exits 2, and a marked ternary whose `:` is not on the line exits 2. Restate S5 as six arms, five DENY and one ALLOW, and let AC5's "strictly more than 64" stand. |

### LOW — 5

| # | Where | Kind | Claim | Fix |
|---|-------|------|-------|-----|
| 8 | spec-11 §2 S11 and §4 Files touched, against §6 | FOLD_BROKE | The fold added an obligation to amend `memory/builds/dTieredTribunal/README.md:58` — the Parked-decisions bullet calling `TOOL-aDeclaredBound-6` OPEN and untouched — in the same commit, and no acceptance criterion reads that file. AC11 is a `git grep -- "6-wide"` sweep and the bullet names the id rather than the string, so the one new carrier the fold added is the one carrier AC11 cannot see; its surviving-hit clause excludes `memory/builds/` anyway. The bullet can be left asserting the row is OPEN while the same commit closes it — the stale-carrier class round-1 finding 30 raised, reintroduced by the edit that answered it. The amendment itself is permitted: this is the Parked-decisions slot, not the M3-reserved problem-statement slot. | Add a clause to AC11: `grep -n 'aDeclaredBound-6' memory/builds/dTieredTribunal/README.md` returns the Parked-decisions bullet naming the row as closed by this unit, where today it reads OPEN and untouched. |
| 9 | spec-11 §6 AC17, against §2 S9's `:103` site | FOLD_BROKE | AC17 is billed as "S9's witness" but bounds its population to hits "inside a prompt string", which excludes `:103` — one of S9's nine sites — because that line is a `log()` call: `log(\`review root (unverified — see note): ${repo} — diff ${base}...${head}\`)`. The Inventory (line 202) requires it become "names the diff or the subject set" and S9 (line 76) lists it, but no criterion measures the change; AC9 grades only that a provenance COMMENT sits at each changed site, the satisfiable-by-typing-a-comment witness round-1 finding 3 named verbatim, listing `:103` first among the six unwitnessed S9 sites. A spec-audit run then announces a diff range it does not have, in the one line emitted before any agent spawns. One correction to the filing: the kind is FOLD_WRONG, not FOLD_BROKE — AC17 is the fold's own answer to round-1 finding 3, delivered in part. | Widen AC17's population to "every hit inside a prompt OR log string", or add a sentence naming `:103` directly: a `spec-audit` invocation's pre-spawn log line names the subject set and contains no `diff`, where today the string is unconditional. |
| 12 | spec-11 §7 Gates, final paragraph | FOLD_BROKE | The fold's replacement sentence asserts that `verifier fan-out self-test` and `review-join self-test` are "guarded on `tools/lib/` and `tools/workflows/`", which `TOOL-dTieredTribunal-14` S12 falsifies one order before this unit lands. Read from `tools/gate-legs.json` at HEAD both rows carry that guard pair and `subject: kit`, so the sentence is true at base `cd971285`; unit 14 is order 3 and adds `tools/hooks/` to both guards in `tools/gate-legs.json` AND `tools/workflows/kit.toml`, graded by its own AC, and unit 11 is order 4. Unit 14's own §7 handles the split explicitly; unit 11 states only the pre-change half, in the present tense, in the section a builder reads to reconcile the DoD run against the manifest. This is the charter's stated-in-prose-beside-the-source class, two sentences after the same paragraph correctly refuses to write the leg COUNT for that exact reason. The DoD conclusion is unaffected, so the cost is one reconcile. | Drop the guard clause — the sentence's load is HELD-unless-`GATE_SELFTESTS` — or write it as unit 14 does: guarded on `tools/lib/` and `tools/workflows/` at base and on `tools/hooks/` as well once unit 14's S12 lands at order 3. |
| 13 | spec-11 §2 S11 and §4 Files touched, against §6 | FOLD_BROKE | Contradiction view of finding 8: the fold added a fifth edit target and gave it no criterion, in the same fold that added AC15, AC16 and AC17 precisely because three scope items had no falsifiable observation. An aggravator not in the filing and verified here: rev-2's own log (spec-11:434-435) claims "S11, §4's Files touched and AC11 gained … plus the build README's Parked-decisions bullet", and AC11's body never mentions it — the same half-landed-repair shape as the `:192` cluster. Room exists: the Parked-decisions slot measures 994 B by this slice against the 1800 B row in `tools/memory-tree/build-readme-slot-limits.txt` (the filing said 972 B; the 22 B gap is a slot-slice convention difference and is not load-bearing). One correction to the filing: the kind is FOLD_WRONG, not FOLD_BROKE — round-1 finding 30 answered in part, not a defect the fold invented. | Extend AC11 with a second observation: `grep -n 'TOOL-aDeclaredBound-6' memory/builds/dTieredTribunal/README.md` returns a Parked-decisions bullet that no longer reads OPEN and untouched, and names `TOOL-dTieredTribunal-11` as what closed it. |
| 17 | spec-14 §2 S8 | FOLD_BROKE | Second view of finding 3, and it fixes the failure mode in the builder's own idiom. Ran the literal construct under `set -u`: it prints `HERE: unbound variable`, `cp` then fails with rc=1, the outer shell continues, the copy silently never happens, and the arms at `:141`, `:153` and `:165` meet S5's own missing-hook refusal — the failure S8 was written to prevent. The companion point holds too: sibling `:74` is `D="$TMP/nohook"` while `check-review-join.test.sh:121` is `D="$TMP/discover"`. `git diff 136cdb70..a19e71b5` shows the whole S8 sentence is fold-added. Low rather than medium: the failure is loud and the fix is one line per site. | `mkdir -p "$D/tools/hooks" && cp "$ROOT/tools/hooks/agent-cap.js" "$D/tools/hooks/agent-cap.js"` after `:133`, the `$E` twin after `:163`, and a fresh variable name for the third scratch repo. |

## Unverified — zero, and that is positive evidence

**There are no unverified findings, and none is carried forward as outstanding.** Every one of the 20 raw findings reached a skeptic verdict of confirmed or refuted. None was filed with no usable verdict, none is deferred for adjudication, and nothing is held open pending a re-derivation somebody else owes.

That zero is positive evidence rather than an absence, and it is asserted against the liveness counters rather than inferred from a quiet log. **All 5 finder lenses returned and all 5 skeptic batches returned** — `lenses 5/5 live`, `skeptic batches 5/5 live`. Had a batch died, its findings would have arrived with no verdict and would appear here as outstanding; had a lens died, the raw population would have been narrower than the fan and `precision 0.90` would be measured over a surface nobody looked at. Neither happened, so the 20/18/2 split covers the whole population the fan produced. `spurious 0 · conflicts 0 · duplicates 0` completes the assertion: nothing was discarded as unparseable, no two verdicts disagreed about one finding, and no finding was counted twice.

The convergence verdict therefore rests on an adjudicated population and not on a partial one. A confirmed-blocker count of 0 measured over a fan that half-died would be a count of what survived, not a count of what is there.

## Refuted — 2

- **4** [low] spec-14 §2 S3 — the finding's own impact concedes "Both counts are correct against source", and that checks out (`fan` at `agent-cap.js:786`, `caps` at `:806`, `bad`/exit at `:821-822`): two rules sit above the `offendingLines` block, three above the new join rule, and every rule above the join rule does prevent a burst. The first sentence names its two members inline, which forecloses the postulated misreading — there is no reading under which three rules sit above the block. The analogy to round 1's five §7 filings fails: those were about a numeral that was FALSE, this is a true count twice with a shifted pronoun. What remains is a prose-style preference about pronoun hygiene.
- **11** [medium] spec-11 §2 S11 against §6 AC11 — the spec resolves the ambiguity in the sentence immediately after the one quoted: "The bullet keeps its point and its widening argument, which rest on the remedy-string carrier named beneath the example RATHER THAN ON THE EXAMPLE." That detaches the bullet's argument from the quoted instance, so dropping the `6-wide` string is inside S11's instruction, not against it. Scope and gate point at the same file and the same string. The comparison to round-1 finding 41 fails on the discriminating detail: there S5 EXPLICITLY preserved the paragraph containing the banned token, a hard contradiction with a grep-count-0 criterion; here S11 preserves the point and disclaims the example.

## What a fold should take, and what is recorded and not folded

The count round 2 measured is the CONFIRMED BLOCKER count, and it stands at **0**. The loop is closed; what follows is a fix list, not an exit condition.

The 18 confirmed findings are **10 distinct defects**. Five lenses landing on one missing table row is one edit, not five, and the grouping below is what a fold should schedule against.

**Fold now — the HIGH tier, which collapses to three distinct defects.**

1. **The missing `:192` Inventory row — 1, 5, 10, 14, 18.** One defect, five lenses, one fix surface: add the `:192` row to spec-11 §4's Inventory between `:184` and `:194`, then correct the rev-3 log sentence so it stops certifying a repair the body does not carry. Five findings close on one row. If a later round sees any of these five return, the fold edited the log and not the table.
2. **AC17's population and its false exemplar — 19, with mediums 2 and 9.** One edit surface in spec-11 §6: widen the population past `grep "diff"` so it reaches `:245`, `:345`, `:353` and the `:103` log string, and delete or re-point the `:353` failing-case sentence, which is false as written. Three findings close together.
3. **The unfailable AC11 after the park — 6.** Spec-13 AC11 gains a presence half over `tools/hooks/README.md` (a `gov:fixed-verifiers` count one higher than at `cd971285`, base 1), replacing the positive half AC10 took with it. This is bookkeeping the park owes, not an unpark of F3.

**Fold now — the MEDIUM tier, which collapses to four more distinct defects.**

4. **The verbatim hook copy — 3 and 17.** One edit to spec-14 S8: spell the adapted `cp` in `check-review-join.test.sh`'s own idiom (`$ROOT`, the right scratch variable at each insertion point), and rename the third scratch repo off `D`. Note when folding that the `$E` reference at the `:133` insertion aborts the whole self-test under `set -u`, which is worse than the finding as filed.
5. **The half-named `:194` row — 7.** Restate the `:194` Inventory row over BOTH diff-shaped halves and widen AC4 from `:195` to every finder-prompt line naming the address field.
6. **The undisclosed sibling-gate widening — 15.** One disclosure sentence in spec-14 S3, a carve-out in §3 for `check-verifier-fanout.sh`'s two header lines, and an AC over the corrected exit legend.
7. **The contradicted `must spell` bullet — 16.** Spec-13 S8 REWRITES `tools/hooks/README.md:24-26` rather than appending beside it, with an observation over the bullet's content.
8. **The unarmed vacuous-predicate guards — 20.** Two DENY arms and two criteria in spec-13, so `branches.length` and the null-branch test each have a failing case that has been seen.

**Fold now — the LOW tier, which collapses to two distinct defects.**

9. **The unwitnessed README bullet — 8 and 13.** One clause on spec-11 AC11 over `memory/builds/dTieredTribunal/README.md:58`. The slot has room (994 B against 1800 B), so nothing forces the omission.
10. **The stale guard sentence — 12.** One clause in spec-11 §7, either dropped or written as unit 14's §7 already writes it.

**Recorded and NOT folded — four items, none of them a fix.**

- **Three disputed kind attributions.** The skeptics re-labelled finding 7 as ROUND1_MISSED (round-1 finding 29 named only `:195` and `:376`), and findings 9 and 13 as FOLD_WRONG rather than FOLD_BROKE (both are round-1 fixes delivered in part). The census in this record reports all three AS FILED, because the earlier rounds' splits were measured the same way and silently re-labelling one round's population would break the only comparison this build has. The dispute is recorded here and changes no disposition, no severity and no fix.
- **Two filings that understate their own subject.** Finding 3's `$E`-at-`:133` abort and finding 19's omission of `:195` from its uncovered list both make the defect larger, not smaller. Recorded so a fold does not scope from the weaker text.
- **Two corrections to finding prose that do not move a verdict.** Finding 2's claim that AC17 is S9's ONLY substantive witness is wrong (AC4 witnesses `:195`), and finding 13's 972 B slot measurement is 994 B by this record's slice. Neither reaches the disposition.
- **Fork F3 stays parked.** No finding in this round asks for the `memory/guides/REVIEW-PROTOCOL.md` edit, and finding 6 explicitly does not: it asks for a presence criterion over the carrier that REMAINED in scope. If a later round reports F3 as folded in without an owner ruling cited by id, that report is itself the defect — the same sentence round 1 wrote, and it is still true.

Nothing else is held back. Every fix above is spec text or build-record text this run is authorized to write, and none of the 18 requires a new gate, a new mechanism, or a budget the run cannot raise. Finding 15's fix is the only one that amends a NON-GOAL; it should land as an explicit §3 carve-out logged in §9, never as a silent widening.
