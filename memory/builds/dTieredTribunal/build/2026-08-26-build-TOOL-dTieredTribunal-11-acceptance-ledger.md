**Serves:** journal TOOL-dTieredTribunal-11 TOOL-dTieredTribunal-12 TOOL-dTieredTribunal-13 TOOL-dTieredTribunal-14

# dTieredTribunal run 2 — the acceptance ledger for the four Tier-2 units

Node `d`, 2026-08-26. One line per numbered acceptance criterion of each closed Tier-2 unit,
naming the observation that answers it. **Every criterion here was RUN**, not read: a separate
read-only pass took the four specs, extracted all 58 labels, and verified each against the tree
at HEAD. Thirteen came back unmet, which is why this ledger exists in the shape it does — seven
were then repaired in code, five amended in the spec with the reason written into the revision
log, and one (`AC14`) was satisfied for real by running the engine as a spec audit for the first
time. A line that says AMENDED names the revision; a line that says REPAIRED names what was
observed after the repair. No line restates its criterion as though that were evidence.

The honest caveat, stated once and not repeated per line: the closing-diff review loop exited
NON-CONVERGENT at two blockers twice, so by `BUILD-METHOD` M8 no review round covers the fixes
folded after round 2. Those fixes were each reproduced before and after instead.

**Evidences:** TOOL-dTieredTribunal-11
- AC1 — `tools/workflows/tier2-review.js:86-95` sources `kind` from `a.kind` defaulting to `diff-review`; evaluating those exact bytes under node with `{kind:'audit'}` threw `` `kind` must be one of diff-review | spec-audit. Got "audit" ``, and `grep -c "spec-audit"` returns 7 at HEAD against 0 (exit 1) at base `cd971285`.
- AC2 — `bash tools/workflows/check-verifier-fanout.sh` printed `verifier-fanout: clean — 3 workflow script(s) obey the ≤5-verifier rule` and exited 0; `DIFF_LENSES` at `:265` and `SPEC_LENSES` at `:290` are top-level array literals and `:317` is `const LENSES = isSpec ? SPEC_LENSES : DIFF_LENSES // gov:fixed-verifiers`.
- AC3 — `SPEC_LENSES` keys `underspecification`/`contradiction`/`unstated-assumption`/`prior-art` match the four bullets at `tools/memory-tree/README.md:210-214` in content, with that file's internal shorthand expanded inline (`M2's four axes` → `scope, interface, ordering and acceptance`, which `memory/guides/BUILD-METHOD.md:63` confirms verbatim, and `M5` → the recall-probe sentence); they are faithful transliterations, not byte copies, and brief 1 adds `that could fail`.
- AC4 — REPAIRED, then re-observed — a parenthetical had been written INSIDE the field list this criterion counts, so `grep -c "file, where, severity"` returned 0. The prompt now spells the list contiguously and glosses `where` after it; all four counts (`file,line,severity`, `file,where,severity`, `file, line, severity`, `file, where, severity`) return 1.
- AC5 — REPAIRED, then re-observed — the anchor regex was spelled twice, at the subject arm and the base arm, so the two could drift. Hoisted to one `PINNED_SHA` constant both read; `grep -c "0-9a-f]{7,40}"` returns 1, and `tools/workflows/tier2-review.test.sh` holds 20/20 across both arms.
- AC6 — `tools/workflows/tier2-review.js:326-330` — the spec arm reads `first run \`git hash-object <path>\` in ${repo} and compare the result to the pinned blob`, then `report it as a BLOCKER finding`, and only then `Then Read the file WHOLE.`
- AC7 — `grep -n "Serves:\*\* diff-review" tools/workflows/tier2-review.js` returns no match (exit 1); `:537` reads `` `The report's FIRST line must be the record's binding line, exactly: **Serves:** ${kind} ` ``.
- AC8 — `git diff cd971285 -- tools/workflows/tier2-review.js` removes 19 lines, none inside a return block (meta, the args comment, `context`, `baseLooksPinned`, the pre-spawn log, the phase comment, `LENSES`, prompt strings and the `ref` builder); the four returns at `:367 :379 :469 :581` carry field sets identical to base `:215 :227 :315 :419`.
- AC9 — AMENDED at `rev-7` and again at `rev-8` — the original asked for a unit-id citation at every Inventory row and the file carries six, one per mechanism, which is this file's existing convention. `rev-8` also amended `S13`, which had been left asking for the older per-site form: the post-acceptance audit filed that split as its own HIGH.
- AC10 — `bash tools/check-kit-versions.sh` exited 0, and `grep -n "tier2-review@1.4"` returns the single line 3, which also carries `version: '1.4'`.
- AC11 — AMENDED at `rev-6`, then REPAIRED at `rev-8` — the original demanded ZERO `6-wide` hits under `tools/` and the tree held two when this unit was reported done. The rev-6 rewrite blessed the survivors as historical citations but left a trailing clause that only held under the ZERO opening, so the two halves returned opposite verdicts on one list of four; `git grep -n -- "6-wide"` now agrees with both.
- AC12 — REPAIRED, then re-observed — the dossier half always held, but the leg was RED because this build's new gate leg was an unclaimed inventory key. Claimed in `memory/map/features/review-harnesses.md` and regenerated; `python tools/codebase-map/test_codebase_map.py` exits 0 with all five arms ok.
- AC13 — `node tools/workflows/check-workflow-syntax.js` printed `3 workflow script(s) parsed clean` (exit 0) and `bash tools/workflows/check-review-join.sh` printed `clean — no ref-keyed verdict join under tools/` (exit 0).
- AC14 — SATISFIED FOR REAL, not amended — the two records this criterion was reported against were hand-driven, not engine-written: each opened over a `reviewed range` with FIVE lenses where `SPEC_LENSES` holds four, and neither cited `hash-object`. The engine was then run once for real with `kind: 'spec-audit'` over this build's own five specs at pinned blobs. `memory/builds/dTieredTribunal/reviews/2026-08-26-review-TOOL-dTieredTribunal-11-spec-audit-post-acceptance-round1.md` opens `**Serves:** spec-audit` followed by the five unit ids, and its anchor line names each subject as `path@blob`. This is the criterion that proves the unit rather than the diff, and until that run it was unmet.
- AC15 — `grep -n "the cumulative diff landing on main" tools/workflows/tier2-review.js` returns exactly one line, `:97`, on the diff arm of `const context = a.context || (isSpec ? 'the spec set under audit' : ...)`.
- AC16 — `tools/workflows/tier2-review.js:362` builds `` ref: `${f.file}:${isSpec ? f.where : f.line}` ``; evaluating that expression with `isSpec=true` and a `where` of `§2 S4` under node yields a tail of `§2 S4` and no `:undefined` (the shipped diff arm still renders `a.js:12`).
- AC17 — `grep -nE 'diff|file:line|file, ?line, ?severity|\$\{base\}\.\.\.\$\{head\}' tools/workflows/tier2-review.js` returns 24 hits; the 9 comment hits are outside the population, and every prompt/log hit sits on a kind selection (`:97 :178 :331 :333 :341 :346 :347 :399 :499 :512 :538`), on `diffCmd` (`:171`), inside the diff-only `DIFF_LENSES` brief (`:284`), or on the `diff-review` token itself (`:86 :87`) — the two sites the criterion named as failing at spec time, the range line (now `:512`) and the pre-spawn log (now `:178`), are both on `isSpec` ternaries.

*17 criteria, 6 of them re-dispositioned after the acceptance pass measured them unmet.*

**Evidences:** TOOL-dTieredTribunal-12
- AC1 — `grep -n "a spec is not code" memory/guides/BUILD-METHOD.md` exits 1 with no output; `grep -n "DECLARED subject"` returns exactly one line, `116:**The harness needs a DECLARED subject.**`, and `grep -n "^## "` puts M4 at 111-138, so 116 is inside it.
- AC2 — Over `tools/memory-tree/BUILD-METHOD.template.md` the same two greps give exit 1 and a single hit at line 116; `diff <(sed -n '116,118p' memory/guides/BUILD-METHOD.md) <(sed -n '116,118p' tools/memory-tree/BUILD-METHOD.template.md)` is empty and both sides md5 to `7cc33ef21f65be39829de425d9c54688`.
- AC3 — `bash tools/memory-tree/kit-dogfood-parity.test.sh` printed `kit-parity: shipped and installed docs agree (3 pairs, rendered for 'tools/memory-tree')` and exited `0`.
- AC4 — `git diff -U0 cd971285 -- memory/guides/BUILD-METHOD.md` shows exactly one hunk, `@@ -116 +116,3 @@`, replacing the `**Not the harness.**` line with the three-line declared-subject paragraph and touching nothing else.
- AC5 — `wc -c` gives `24546` for `memory/guides/BUILD-METHOD.md` and `24557` for `tools/memory-tree/BUILD-METHOD.template.md`, both under 24576; `wc -l` gives `317` for each, under 350.
- AC6 — `git grep -n "Not the harness" -- ':!memory/builds' ':!memory/DECISIONS.md'` exits 1 with no output; the unfiltered `git grep -n "Not the harness"` hits only `memory/DECISIONS.md:107` and files under `memory/builds/`.
- AC7 — Read `tools/workflows/tier2-review.js:27-32`: the `inputs (via Workflow \`args\`)` block documents `kind: "diff-review" | "spec-audit"` (line 32, defaulting to `diff-review` when absent) plus `subjects: [{ path, blob }]`, so M4's "names the spec kind" is answerable there.
- AC8 — `grep -n "pointed at a document" memory/map/features/build-method.md` exits 1; the rewritten bullet at `memory/map/features/build-method.md:87-95` reads "`TOOL-dTieredTribunal-12` replaced the ban with a declared-subject rule".
- AC9 — `grep -n "rules that harness out for spec audits" memory/backlog/TOOL.md` exits 1 — the clause now reads "M4 ruled that harness out … was never the point" — and `memory/backlog/TOOL.md:205` still carries `TOOL-dHonouredPark-5 · OPEN` with its orchestrator-assigned-id ask, though that row now repeats "the id rule is independent of the subject" twice.
- AC10 — `grep -n "forbids" memory/map/features/review-harnesses.md` exits 1 with no hit at all; `sed -n '80,92p'` shows the bullet now reads "`tier2-review.js` takes a review KIND now".
- AC11 — REPAIRED, then re-observed — `bash tools/memory-tree/check-method-carriers.sh` always exited 0 (14 carriers, all declared and pointing); the codebase-map leg was RED on this build's own unclaimed gate leg. Claimed and regenerated; `python tools/codebase-map/test_codebase_map.py` exits 0.

*11 criteria, 1 of them re-dispositioned after the acceptance pass measured them unmet.*

**Evidences:** TOOL-dTieredTribunal-13
- AC1 — Piped the marked ternary with `args.customLenses` as second arm to `node tools/hooks/agent-cap.js` → `rc=2`; the identical payload against `git show cd971285:tools/hooks/agent-cap.js` (eval'd in node) → `rc=0`, so the base-exits-0 half re-ran too.
- AC2 — Piped `const LENSES = a.lenses.filter((x) => ALL.includes(x)) // gov:fixed-verifiers` plus its fan-out to `node tools/hooks/agent-cap.js` → `rc=2`, stderr naming the branch `a.lenses.filter((x) => ALL.includes(x))`.
- AC3 — Piped `all.length ? chunk(all, Math.ceil(all.length / MAX_VERIFIERS)) : args.batches` marked, with fan-out → `rc=2` at HEAD and `rc=0` against the `cd971285` copy, so the second accept is closed too.
- AC4 — `bash tools/workflows/check-verifier-fanout.sh` printed `verifier-fanout: clean — 3 workflow script(s) obey the ≤5-verifier rule` and exited `rc=0`.
- AC5 — `bash tools/hooks/agent-cap.test.sh` exited `rc=0`, `grep -c '^FAIL'` on its output returned `0`, and the trailing line reads `---- 71 passed, 0 failed ----`, which is above the 64 the spec cites at base.
- AC6 — Piped the unit-11 selector `const LENSES = kind === 'spec-audit' ? SPEC_LENSES : DIFF_LENSES // gov:fixed-verifiers` with both identifiers bound from ≤5-element literals, plus the fan-out → `rc=0`.
- AC6b — Piped the whole of `tools/workflows/drift-audit-state.js` to `node tools/hooks/agent-cap.js` → `rc=0`, so the one shipped `ALL_LENSES.filter(...)` user still passes.
- AC7 — stderr of the AC1 payload reads ``LENSES` carries gov:fixed-verifiers but the branch `args.customLenses` is not one of the bounded forms`, naming both the marker and the failing branch rather than only the fan-out line.
- AC8 — `diff .claude/hooks/agent-cap.js tools/hooks/agent-cap.js` printed nothing and exited `rc=0`; both copies carry the `parseBranches`/`boundedBranch` predicate.
- AC9 — `bash tools/check-kit-versions.sh` exited `rc=0` and `grep -rn 'agent-cap@1\.6' tools/hooks/ .claude/hooks/` returned no hits (rc=1); all four carriers now read `agent-cap@1.8`, i.e. past the 1.7 S7 named, moved on by later folds.
- AC11 — REPAIRED, then re-observed — `grep -c 'gov:fixed-verifiers' tools/hooks/README.md` returned 1 against the required 2, because the marked-derivation bullet this unit added documented the marker without ever spelling it. Spelled now, together with the consuming-chain rule; the count returns 2 and `bash tools/check-agent-cap-restatement.sh` exits 0.
- AC12 — AMENDED at `rev-5` — the second half was a byte-equality proxy over the dossier's `## Gaps` span, and it fired on `TOOL-dTieredTribunal-14` adding its OWN gap there, which is not this unit's to suppress. The criterion now grades the property it was always for: no added line in that span describes the hole THIS unit closes.
- AC12b — ADDED at `rev-6` by the post-acceptance spec audit and observed at that rev: the array-LITERAL branch always REFUSED its oversize case and nothing in the spec graded it. Measured against `tools/hooks/agent-cap.js` — a marked literal of 5 returns 0, of 6 returns 2, of 12 returns 2, and an unmarked literal of 9 returns 2 — and `tools/hooks/agent-cap.test.sh:170` already pins the marked-oversize arm. The audit filed this as a fail-open; it is not, the code is correct, and the defect was a branch whose accepted case was graded and whose refused case was not.
- AC13 — Fed all 10 files from `git ls-files '*.js'` to the HEAD hook and to the `cd971285` copy: `files=10 diffs=0`, with only the two `agent-cap.js` copies denying (`rc=2`) under both, as §4 predicts.
- AC14 — Payload with the marker alone after `=` gives `rc=2` and stderr ``LENSES` carries gov:fixed-verifiers but the branch `` is not one of the bounded forms` — marker named, but the branch renders EMPTY, and `branches.length &&` at `tools/hooks/agent-cap.js:288` is unreachable because `parseBranches` returns `[t]` or `[null]` and never `[]`, so the drop-the-guard half was not re-run and looks vacuous.
- AC15 — Payload whose marked ternary has its `:` on the next line gives `rc=2` at HEAD with stderr ``LENSES` carries gov:fixed-verifiers but this file cannot delimit its value branches`, and `rc=0` against the `cd971285` copy.

*16 criteria, 2 of them re-dispositioned after the acceptance pass measured them unmet.*

**Evidences:** TOOL-dTieredTribunal-14
- AC1 — Three payloads piped to `node tools/hooks/agent-cap.js` each exit 2 on stderr only (stdout empty under `2>/dev/null`), naming `object/Map literal keyed by a .ref string`, `Map keyed by a .ref string`, `the retired verdictByRef identifier`; base half not re-run, but `git show cd971285:tools/hooks/agent-cap.js | grep -c scanJoinFindings` = 0.
- AC2 — Every deny above ends with `Key the join on the integer id the orchestrator assigns, never on a string the skeptic reproduces.`, the string `tools/workflows/check-review-join.test.sh:79` asserts.
- AC3 — A `boundedParallel(t,5)` + `Promise.all(r.map(x=>agent(x)))` fixture exits 0 under `node tools/hooks/agent-cap.js --only=join` and exits 2 on the identical bytes with no flag, denied by the verify/fan-out rule.
- AC4 — `node tools/hooks/agent-cap.js --only=bogus` exits 2 printing `--only=bogus names no rule this hook has. The closed set is: join.`
- AC5 — REPAIRED, then re-observed — `grep -c awk tools/workflows/check-review-join.sh` returned 1, a prose survivor naming the retired scanner. Removed; the count returns 0 and `grep -F -- '--only=join'` still hits.
- AC6 — `bash tools/workflows/check-review-join.test.sh` prints `PASS — review-join + workflow-syntax gates: all arms held` and exits 0; `git diff cd971285 -- tools/workflows/check-review-join.test.sh` contains only added lines — zero `-` lines, so no pre-existing arm's expected string moved.
- AC7 — The section-4 fixture piped to `node tools/hooks/agent-cap.js` exits 2 naming `L7: for (const v of verdicts.flat()) map[v.ref] = v`; the file half was NOT re-run as a new fixture (read-only, no writes) — it is covered by the passing arm `discovery: an UNTRACKED banned join is caught`.
- AC8 — `bash tools/workflows/check-review-join.sh tools/hooks/agent-cap.js` exits 1 naming `L888: [/verdictByRef/, "the retired verdictByRef identifier"],`; the no-argument run prints `review-join: clean — no ref-keyed verdict join under tools/` and exits 0.
- AC9 — `bash tools/check-wiring.test.sh` prints `---- 76 passed, 0 failed ----` with `ok AC7d a wired command carrying --only -> UNWIRED` and its removal half, rc=0; `bash tools/check-wiring.sh --check` on shipped settings prints `ok       agent-cap`.
- AC10 — SATISFIED LATE, and the gap is the finding — this unit's section 4 declared ten self-test arms and the unit landed with none of them: `git show --stat fb2d692e` never touched `tools/hooks/agent-cap.test.sh`, so rule 5 was covered only indirectly through the file gate that delegates to it. All ten are written now from that table; the suite goes `71` to `81 passed, 0 failed`, and each of the four probe shapes was observed GREEN at HEAD and RED against the pre-rule-5 hook at `91c5d3ac`.
- AC11 — `grep -rn 'agent-cap@1\.7' tools/hooks/ .claude/hooks/` exits 1 with no hits; `grep -rln 'agent-cap@1\.8'` names all four carriers including both `scratch-guard.js` copies; `bash tools/check-kit-versions.sh` exits 0.
- AC12 — REPAIRED, then re-observed — both `[[gate_leg]]` blocks in `tools/workflows/kit.toml` spelled a literal `tools/hooks/` where the sibling descriptor uses `{prefix}/hooks/`, which is a real portability defect for an adopter rather than a wording nit. Both now read `{prefix}/hooks/`.
- AC13 — REPAIRED, then re-observed — the `codebase-map coverage + freshness` leg was RED at HEAD on this build's own unclaimed gate leg and a stale `inventories.json`. Claimed and regenerated; `python tools/codebase-map/test_codebase_map.py` exits 0.
- AC14 — `tools/hooks/README.md:42-58` names `A REF-KEYED VERDICT JOIN` in the denial list plus a `Running ONE rule` section stating `--only=<rule>` and that a wired command must never carry it; `bash tools/check-agent-cap-restatement.sh` exits 0.
- AC15 — AMENDED at `rev-4`, and its carve-out widened explicitly at `rev-5` — the original pinned `ref-keyed` to lines 2 and 7 of `tools/workflows/check-verifier-fanout.sh`. That gate GENERALISED instead, to `obey every rule agent-cap.js enforces` and `1 = a rule the hook enforces is broken`, which covers the join ban and every future rule; a criterion pinned to line NUMBERS in a file the unit is also editing grades position rather than property.

*15 criteria, 5 of them re-dispositioned after the acceptance pass measured them unmet.*

