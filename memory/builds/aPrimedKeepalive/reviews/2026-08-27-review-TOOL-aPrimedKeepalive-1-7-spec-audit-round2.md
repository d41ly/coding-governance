**Serves:** spec-audit TOOL-aPrimedKeepalive-1 TOOL-aPrimedKeepalive-2 TOOL-aPrimedKeepalive-3 TOOL-aPrimedKeepalive-4 TOOL-aPrimedKeepalive-5 TOOL-aPrimedKeepalive-6 TOOL-aPrimedKeepalive-7

# Spec audit round 2 — the seven-unit spec set of aPrimedKeepalive, measuring the fold

*Node a, 2026-08-27, round 2. Round 1 returned BLOCKED with 4 blockers, 2 highs and 4 mediums over 9 distinct defects; every one was folded. This round grades the FOLD — does each fix hold, and did any fix introduce a new defect — over a subject set widened by one, because `TOOL-aPrimedKeepalive-7` was outside round 1's set and round 1 flagged it as inheriting findings 25 and 26 if those were fixed only in unit 4. A parallel fan of primed finder lenses, each handed the set's §3 non-goals and named prior art up front, then batched skeptics prompted to REFUTE. Every surviving finding was re-derived a third time in this record against the tree at HEAD; the figures below came back from `wc -c`, `grep -n`, `git grep` and one live checker run, not from the specs' own prose.*

**Pinned subjects — seven spec blobs, each audited at the blob named beside it, ROUND 2:**

- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-1.md@9ca96ba2c5f07ef27f70c4cb1ac7af1d4ec995bf`
- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-2.md@a83028f3f05dd3ad1511068717a4b9bf1dec0af1`
- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-3.md@ab2237d1c40a395013cc46218b65f0fe63908032`
- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-4.md@2b82a09cff5172029b042b6354f69fa5bb42d82a`
- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-5.md@2f5669a8b60016b1e20e56f4f23becff15459405`
- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-6.md@a6dd59830343b5ce1ce9e109282aacd6d25fe9c2`
- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-7.md@9b9da3d2d89fe20c6f90fa0b5bbeb27ab0004b30`

## Verdict: BLOCKED

One blocker stands, and it is a leg that is RED in the tree right now rather than a defect in prospect. `bash tools/memory-tree/check-verdict-epoch.sh b4e1d5be` exits 1 at HEAD, naming two behaviour-bearing lines of the hygiene engine moved by this build and no commit bumping `KIT_MEMORY_TREE_VERSION`. That leg carries no `guard` key in `tools/gate-legs.json`, so it runs on every bar and blocks the push boundary, and no unit in the set scopes the bump or names the leg. The tally sits in this paragraph rather than on the verdict line because that line's token is a closed set.

The rest is fold residue, and the shape is consistent enough to be worth naming: **every high in this round is an amendment that left its other half standing.** Round 1's fixes landed in the section the finding addressed and not in the sibling section that repeated the same claim — spec-4 §4 was corrected and §10 still carries the disproved sentence verbatim, spec-6 rewrote S1, S4, §3, §4, AC2, AC4 and AC5 and left S3 and §5 quantified over the check the fold had just declared a non-goal. That is the class unit 7's own S3 names, occurring inside the fold that was written to close it. No design change is needed for any of them: four are a sentence rewrite, one is a number, two are a criterion.

The three round-1 defects with a byte or a value in the tree are FIXED and re-measured here, so the fold is real work, not paper. Nothing in this round overturns a round-1 verdict.

## Review shape

- raw 22, confirmed 9, refuted 13, unverified 0, precision 0.41, round 2.
- confirmed by severity, as adjudicated in THIS record: **1 BLOCKER · 3 HIGH · 2 MEDIUM · 1 LOW**.
- distinct defects: 7. Findings 8, 14 and 19 are ONE defect — the same three sentences of `spec-4` §10 — reached by three lenses from three directions, and they are reported as one row at that defect's severity rather than as three highs. Splitting them would triple-count a single sentence rewrite and make the fix order lie about how much work is left.

**Precision moved 0.30 to 0.41 and is still under the floor.** The priming change worked in the direction §8 predicts: handing each lens the §3 non-goals up front removed most of round 1's refuted population, which was lenses grading a spec against a rule that spec had already carved out. What replaced it is a different waste — the fan converging. Three of nine confirmed findings are one paragraph, which means the lenses are now duplicating coverage rather than extending it. **Round 3, if there is one, should not re-audit whole specs.** Scope it to the residue sites this record names plus whatever the blocker's fix touches, and drop the fan to two lenses; a third pass over seven specs will return the same paragraph a fourth time.

## BLOCKER — 1

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 18 | spec-6 §2 (S1/S4), §6, §7 Gates | The unit moves behaviour-bearing lines of the memory-tree engine and never scopes the `KIT_MEMORY_TREE_VERSION` bump that `tools/memory-tree/check-verdict-epoch.sh` demands; §7 names neither that leg nor `kit version markers`. **Verified live, not on paper:** the checker exits 1 at HEAD with `FAILED — 2 behaviour-bearing line(s) of the engine moved in b944720cb81d..., and NO commit in b4e1d5be..HEAD changes KIT_MEMORY_TREE_VERSION (still 2.47)`, naming `check-memory-hygiene.sh` — unit 6's own subject file. The leg `verdict epoch (kit version dates the engine)` has NO `guard` key in `tools/gate-legs.json`, so it runs on every bar and reds the push boundary. The spec does not merely omit the bump: §4 "Files touched" affirmatively states that `HYGIENE.template.md` and `memory/HYGIENE.md` "describe the checks, not their mode guards" — precisely the two files the checker demands move together — so the stated file set is wrong rather than silent. No §3 non-goal withholds it. Unit 6 is order 1, so its edit arms the trap for every later commit in the build. | Add an S-item and a matching AC: bump the constant to 2.48 in every carrier `git grep -l "gov:kit memory-tree@"` returns among tracked kit and guide sources — **derived from the markers, NOT the three the remediation message lists** — then `bash tools/memory-tree/kit-dogfood-parity.test.sh --render`. The AC observes `bash tools/memory-tree/check-verdict-epoch.sh` and `bash tools/check-kit-versions.sh` both exiting 0. Name both legs in §7 and cite `TOOL-dSettledRoster-4` as the reason the derived list is the instruction. Measured here, the non-record carrier set is eleven files across `tools/memory-tree/`, `tools/check-kit-versions.sh`, `memory/HYGIENE.md`, `memory/TEMPLATE-SPEC.md`, `memory/guides/BUILD-METHOD.md` and `memory/map/features/memory-tree-hygiene.md` — the message names three of them. | The gate exists and its REMEDIATION is the defect. `TOOL-dSettledRoster-4` is OPEN at `memory/backlog/TOOL.md`, records that following the message exactly leaves the bar RED on `kit version markers`, and records the same miss twice — 2026-08-20 and again 2026-08-21 during dUnstalledConvoy, at the cost of a second full-bar cycle. The left-shift is one line inside `check-verdict-epoch.sh`: derive the carrier list from `git grep -l "gov:kit memory-tree@"` at emission time and print THAT, instead of the three-name literal. A remediation message that hand-keeps a population is the same authored-count class the charter bans in prose, one file over, and it has now cost three cycles. |

## HIGH — 3

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 8 · 14 · 19 | spec-4 §10 Reuse audit, first paragraph (all three sentences), against §4 "The predicate, and where it actually goes" | **One defect, three lenses.** rev-3 corrected §4 to say `$b` is NOT in scope at the `nlive` accumulation point and that the delivered exclusion is a post-loop second pass over `$live` reading `ADV_HEAD` — and §10 still reads that check 15's idiom sits "inside the same per-record walk that computes `nlive`", that "Both the witness and the anchor are already in scope there", and that the unit "adds a phase guard to an existing expression rather than introducing a second way to ask the same question". That is round 1's finding 26 surviving verbatim in the one section whose job is the M5 reuse record. The code settles it against §10: the record walk runs `check-unattended.sh:678-1089` with `nlive` at `:695` and `b="$ADV_HEAD"` at `:816` inside check 9's conditional, while the shipped exclusion is a separate `for c7f in $live` pass at `:1122-1145` with its own `c7anchor`, its own `phase_of`/`fact_of` re-derivation and its own `GIT merge-base --is-ancestor`. Nothing was added to the `:909` expression. §4 defends that re-derivation at length as one grammar evaluated twice; §10 denies a second evaluation exists. The rev-3 §9 line lists §4 as fixed and never mentions §10, so the fold is half applied — and a later reader sent to `:909` will look for a one-line phase guard and find a 25-line second pass. | Rewrite the paragraph to the delivered shape: the IDIOM (`GIT merge-base --is-ancestor`) and the `ADV_HEAD`/`ADV_TIPS`/`ADV_NREM_RC` observation are reused from check 15, the SITE is not; `$b` is unset on the first record and loop-carried afterwards, so the exclusion is a post-loop pass over `$live` reading `ADV_HEAD` directly and re-deriving the two accessors. Name the two-spelling maintenance cost the way `TOOL-aPrimedKeepalive-7` §3 does — there are now two ancestry tests in one file and they must move together. Add a rev-4 line recording that the fold updated §4 and left §10 standing. | A hygiene arm over `memory/builds/*/spec/*.md`: when a §9 revision line claims a section was corrected, every other section quoting the corrected section's replaced sentence reds. That is expensive to spell. The cheap version that catches this exact class: red a spec whose §10 cites a `file:line` that no §4 in the same document cites, because a reuse audit naming a seam its own design section does not name is the fold-half-applied signature. Both are worth less than the documented check, which is the one round 1 already prescribed for §4 and which §10 also needs — paste the `grep -n` output for the claimed scope, so an assertion about what is in scope is a citation rather than a memory. |
| 9 | spec-6 §2 S3 (`:38`) and §5 observability (`:98`) | rev-3 established that the guarded block is check 23 ALONE, made check 22 an explicit non-goal (§3, and the `:610` row of §4's table), and rewrote S1, S4, §3, §4, AC2, AC4 and AC5 accordingly — but S3 still reads "The full (non-`--staged`) run must still execute checks 22 and 23", and §5's observability bullet still reads "the full run still names checks 22 and 23; S3 observes that", a claim rev-2 itself recorded as FALSE because both checks print nothing when green. The §9 rev-3 line lists neither S3 nor §5, so the fold is partial on the exact axis rev-3 called a blocker. S3 is the unit's only liveness assertion against disarming the check in both modes, and it is quantified over a two-check population whose second member this unit does not touch — satisfying it literally means observing a check that was never guarded and would grade green for the unrelated reason round 1 identified. §5 compounds it by naming as the observable the exact thing rev-2 discarded as unfalsifiable. A builder who follows S3 and §5 instead of AC2 and AC4 reproduces the defect the fold removed. | S3: "the full (non-`--staged`) run must still execute check 23", with the observable pointed at AC4's staged break rather than at execution. §5 observability: replace the sentence with the fact rev-2 established — neither check prints when green, so liveness is observed only over an inserted break, per AC2 and AC4. While in the file, decide whether the §3 non-goal at `:49` ("Any change to what checks 22 and 23 ASSERT") keeps its two-check phrasing deliberately; it is defensible as a scope disclaimer but it is the third site of the same population error and a reader will not know which sites are intentional. | The project's own rule, unapplied: a new gate arm is not landed until its failing case has been OBSERVED. Make that an AC SHAPE for any unit touching a leg — the AC names the stage, the expected RED and the unstage — so a liveness claim resting on "the run names the check" is visibly not an observation. A spec-format lint can enforce the shape: a unit whose files-touched includes a `check-*.sh` must carry at least one AC containing a staged-break observation, and reds without one. |
| 20 | spec-1 §2 S3 (`:32`) and §6 AC9 (`:147`), the Resume path added by rev-3 | S3 and AC9 rest on "a resumed session's keepalive is dead by construction" — a premise an OPEN backlog row records as MEASURED FALSE. `TOOL-aPromptedMandate-11` at `memory/backlog/TOOL.md` reads: "MEASURED here — this run asserted twice that two keepalives died with their processes because the store is session-scoped, and `CronList` showed both still firing." The rendered text AC9 grades goes further than the spec: `.claude/skills/unattended/SKILL.md:589` tells the agent the run-state file "still names the old id, which no `CronDelete` you can issue will reach" — an instruction not to attempt the reap at all. If the measurement holds, every resume leaves a keepalive firing forever while `keepalive-reaped` attests green about a different job: the orphan leak S2 was added to close, reintroduced on the one path the fold added. No §3 non-goal covers it — the non-goals bar DRIVER scheduling and reaping, and this is an agent-side read-back. A grep for `aPromptedMandate-11` across `memory/builds/aPrimedKeepalive/` returns nothing, so no unit in the set cites the row that measures its premise false. | In S3 and AC9, oblige the resumed session to issue a delete against the id the run-state file already names — no new conf key is needed, the id is already recorded — before scheduling the replacement, and to report the result. Downgrade "dead by construction" to "expected dead, verified by the delete and its read-back". Correct `SKILL.template.md`'s Resume prose so the render stops instructing the opposite. Cite `TOOL-aPromptedMandate-11` as the measurement that forbids the assertion. | The row's own stated remedy IS the gate: `keepalive-reaped` is attestable but not checkable, so make the attestation carry evidence. Require the close path to record the delete's return alongside the id, and have `check-unattended.sh` red a terminal record whose `keepalive-reaped` fact carries no read-back token. That converts an honest-but-wrong attestation into a failing one, which is the only thing that would have caught the measured case. Until then this is the documented manual check, and it belongs in the protocol's close section rather than in a review nobody re-reads. |

## MEDIUM — 2

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 2 | spec-7 §6 (AC1–AC5), against §2 S3 | S3 names four fail-closed causes for the driver's exclusion — no observed anchor, an anchor naming an unfetched commit, an unresolvable witness, and any phase other than `LANDING` — and §6 grades two of them (AC3, on an empty or unresolvable `ASHA`) plus the non-ancestor case (AC2). **No criterion grades the phase guard**, and none grades the unresolvable witness. The phase guard is the single predicate keeping a live `BUILDING` record from being excluded from `check_single_live` — the thing standing between this unit and admitting a genuinely concurrent run — and it is the one clause with no observation behind it. AC1 cannot stand in: check 5 fires only on n greater than 1, so a driver copy with no phase guard yields n of 0 or 1 and AC1 still passes. The kit test named in §5 does not reach it either — the check-5 fixture at `tools/unattended/unattended.test.sh:473` builds RUN.md files through `runmd()`, which writes no `witness` fact, so the exclusion takes the empty-witness branch whether or not the phase guard exists. Sibling spec-4 grades exactly this with its AC4, but by-design item 8 of this spec says the driver and the leg each carry their OWN transcribed copy of the predicate, so unit 4's AC provably does not reach unit 7's code. A driver copy transcribed without `[ "$p" = LANDING ]` passes every criterion in §6. | Add the mirror of spec-4's AC4: "When a record in any phase other than `LANDING` carries a witness that is an ancestor of the observed anchor, `check_single_live` still counts it — verified by reading the delivered predicate in `tools/unattended/unattended.sh`." Extend AC3's cause list to include an unresolvable witness, or give it its own criterion. | The transcription is the hazard, so gate the transcription: a leg asserting that the driver's `check_single_live` predicate and `check-unattended.sh`'s check-7 exclusion agree token-for-token on the phase name and the ancestry call, derived from both files rather than from a copy. Two deliberate spellings of one grammar is a decision this build made with its eyes open (spec-4 §4 defends it), and the price of that decision is a parity gate — otherwise the second copy drifts and only the first one is ever graded. Cheaper interim: extend the check-5 fixture so `runmd()` can write a `witness` fact, which is the one-line change that makes every exclusion branch reachable by a test at all. |
| 10 | spec-2 §4, Migration paragraph | The paragraph prices the M10 edit against "`BUILD-METHOD.md` sits 30 B under M1's 24 576 B budget at BASE" — the RENDER — while AC6 (added at rev-2) and unit 1's §4 both establish that the TEMPLATE is the tighter half by 11 B and is the binding one. Measured at BASE b4e1d5be: `memory/guides/BUILD-METHOD.md` 24546 (30 B under), `tools/memory-tree/BUILD-METHOD.template.md` 24557 (19 B under); the 11 B delta is the render substitution and is structural, not slack. rev-2's §9 line records fixing AC6 and says nothing about §4, so the fold left the number the builder actually sizes the edit against 11 B too generous. The "Re-measure with `wc -c`" sentence does not save it — it names `BUILD-METHOD.md`, so a builder obeying it literally re-measures the wrong half. Unit 1 §4 records this exact breach already happening once in this build (guide 24 573, template 24 584, 8 B over), and AC6 catches a breach only after the bytes are spent, at which point the donor paragraph this migration nominates gets cut for a reason nobody wrote down. | State the headroom of the BINDING half: `tools/memory-tree/BUILD-METHOD.template.md` sits 19 B under 24 576 at BASE, 11 B tighter than the render. Keep the re-measure sentence but make it name the template FIRST, matching AC6. | Round 1's finding 12 already named it and the owner's call is still open: one leg reading M1's stated budget and applying it to every carrier, sourced from `memory/project/method-carriers.txt`, which already enumerates them. The generalisation this round adds: where a document has a template and a render, **the constraint belongs to the tighter one and prose must never quote the looser**. A spec-format lint can catch the class — a byte figure in §4 that names a render whose template is also in the files-touched list reds, because the pair is exactly the situation where quoting the wrong half is free and wrong. |

## LOW — 1

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 6 | spec-1 §6 (AC1–AC9), against §2 S4 | S4's second half puts an explicit verification act in scope: the two paths that reach preflight through "exactly as the slug path does" are checked to make sure that phrase now carries what it claims. §6 grades the slug path's own 0–4 numbering (AC3, plus a zero-count grep for the removed sentence) and the hoisted section's placement above the routing table (AC2), and **no criterion observes the PROMPT or PLAYBOOK path bodies at all**. The renumber moves step numbers those paths cite by number: the delivered render carries "at the slug path's step 4" at `.claude/skills/unattended/SKILL.md:271` and `:331`, and the slug path's preflight moved from step 4 to step 3 in this very renumber. A stale citation would have sent a prompt-path run to the wrong step and satisfied AC1 through AC9 unchanged. It happens to be correct in the delivered render, which means the class shipped ungraded rather than unbroken. This build's own fold precedent is scope-item to criterion — rev-3 added AC8 for S7 and AC9 for the Resume path in this same file — so an in-scope check with no observable is a real acceptance gap, low value but genuine. | Add a criterion: "When `.claude/skills/unattended/SKILL.md` is read, every cross-reference from the PROMPT and PLAYBOOK paths to a slug-path step number resolves to the step it names under the new 0–4 numbering." | A rendered-Skill lint, sibling to the one round 1's finding 24 proposed for path enumeration: every "at the slug path's step N" reference resolves to an existing numbered step in the slug path's list, and a reference past the end reds. One pass over a file the kit already renders, and it fires on the next renumber rather than in the review after it. By-number cross-references between sections of one document are a rot class the moment either section is renumbered, and this document now has two of them. |

## What the fold fixed — re-measured here

- **Round 1 blocker 11 and 29** (`DIRECTIVES_FLOOR` 15 against a 16-member core) — FIXED. `tools/unattended/.unattended.conf.example:67` and this repo's `.unattended.conf:71` both read `16`, and `DIRECTIVES_CORE` at `unattended.sh:352` holds 16 members.
- **Round 1 blocker 12** (the BUILD-METHOD pair, 8 B over at HEAD) — FIXED and the breach is gone from the tree: template 24560, render 24549, both under 24 576, 16 B and 27 B of headroom. Finding 10 above is the residue: the criterion is right and the paragraph that prices the edit still is not.
- **Round 1 blocker 22** (unit 6 grading "checks 22 and 23") — MOSTLY FIXED. Title, S1, S4, §3, §4, AC2, AC4 and AC5 now name check 23 alone. Finding 9 above is the residue at S3 and §5.
- **Round 1 finding 28** (the unscoped manifest edit, 25 579 B against 25 600) — FIXED: `memory/guides/SESSION-KICKOFF.md` is 25417 B, 183 B of headroom, and unit 1 §9 records the fourth carrier being priced.
- **Round 1 finding 23** (the protocol's false "no byte budget") — the constraint is now real and measurable: `memory/guides/UNATTENDED-PROTOCOL.md` is 681 lines / 55700 B against check 6's `GUIDE_CAP_LINES=750` and `GUIDE_CAP_BYTES=61440`. 69 lines of headroom remain, unchanged by the fold, which is a fact for the next build rather than a finding against this one.
- **Round 1 findings 25 and 26** (spec-4's fail-closed causes and the in-scope claim) — the §4 half is fixed; the §10 half is the merged HIGH above, and it now also reaches `TOOL-aPrimedKeepalive-7`, which cites this reuse audit as its own seam.
- **Round 1 finding 24** (the Resume path outside "every path") — the path was added, and finding 20 above is a defect in what was added, not a survival of the original.

## Fix order

1. **Finding 18 first, and before any further commit in this build.** The leg is red now, unguarded, and every later commit inherits it. Derive the carrier list from the markers, not from the checker's message, and run `bash tools/check-kit-versions.sh` after — `TOOL-dSettledRoster-4` says following the message costs a second full-bar cycle, twice on record.
2. **Findings 8/14/19, 9 and 6 together** — three sentence rewrites and one criterion, all in the amendment-left-its-other-half class. Doing them in one pass and adding the rev lines that name BOTH halves is the cheapest way to stop the class recurring in round 3.
3. **Finding 20** — the only one needing a decision rather than an edit: whether the resumed session reaps by read-back or the protocol redefines what `keepalive-reaped` attests. Everything else follows from that answer.
4. **Findings 2 and 10** — one criterion and one number, no dependencies.
