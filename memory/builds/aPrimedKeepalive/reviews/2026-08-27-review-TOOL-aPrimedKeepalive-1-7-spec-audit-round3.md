**Serves:** spec-audit TOOL-aPrimedKeepalive-1 TOOL-aPrimedKeepalive-2 TOOL-aPrimedKeepalive-4 TOOL-aPrimedKeepalive-6 TOOL-aPrimedKeepalive-7

# Spec audit round 3 — the five units that took a round-2 finding, measuring THAT fold

*Node a, 2026-08-27, round 3, and the LAST round this build runs unless it finds a blocker. Round 1 returned BLOCKED with 4 blockers; round 2 returned BLOCKED with 1. Both folds are complete on paper and every prior finding is claimed fixed, so this round grades the fold and nothing else. It also takes round 2's own recommendation literally: the fan was converging rather than extending, so `TOOL-aPrimedKeepalive-3` and `TOOL-aPrimedKeepalive-5` are DROPPED from the subject set — neither took a round-2 finding — and the lenses were scoped to the named residue sites with each spec's §3 non-goals handed over first. Nothing below is withheld by a non-goal; each row states which clause was checked. Every figure in this record was re-derived at HEAD `0300c20a` against a clean tree with `wc -lc`, `grep -n` and `sed -n` over the delivered files, not read out of the specs' own prose.*

**Pinned subjects — five spec blobs, each audited at the blob named beside it, ROUND 3:**

- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-1.md@20124f37e7b859c5dd0504514dc35abcac7413f3`
- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-2.md@5a8b49cb4e30850019293631a0dd77ecf0c872a5`
- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-4.md@6194d455665e4dcdda49f7b0a0d0527e773c7e40`
- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-6.md@faf5ea9fac84efed56a44e17f818e3b3f4bc2fa6`
- `memory/builds/aPrimedKeepalive/spec/2026-08-27-spec-TOOL-aPrimedKeepalive-7.md@16c9b37c5da385e05564ce907138a5ee224a447d`

## Verdict: BLOCKED

Two blockers, three highs and three mediums, over eight distinct defects. The tally sits in this paragraph rather than on the heading above because that line's token is a closed set.

**The dominant shape did not change between rounds, and it did not change in this fold either: `amendment-leaves-its-other-half-standing`.** Round 2 named the class; round 2's own fold then committed it again, twice, in the two highest-traffic places this build owns. The rev-4 fold corrected the `## Resume` section of the delivered Skill and left the hoisted `## Before any path` section — the section the same unit's S3 created — asserting the exact claim rev-4 records as MEASURED FALSE, 554 lines earlier in the same file, where every path reads it first. And the acceptance ledger that grades this build carries no evidence line for a single acceptance criterion that either fold ADDED. Both blockers are that one shape: a correction landed where the finding pointed and nowhere else.

The rest is residue of the same kind at lower severity, and none of it needs a design decision. Three findings are one criterion each, two are a sentence, one is a `printf` string, one is a stale pair of numbers. What is worth saying plainly: **no acceptance criterion in this spec set can fail on either blocker.** Unit 1's AC9 stops its observation window at the `## Resume` heading, so the contradicting sentence is outside every criterion in the set, and the ledger omission is invisible until the units flip to CLOSED — at which point hygiene check 23 reds the bar, which is the gate `TOOL-aPrimedKeepalive-6` was built to keep alive.

The three capped carriers were re-measured after this fold and all three are UNDER, unmoved from round 2: the fold spent no bytes at all. Figures in the last section.

## Review shape

- raw 26, confirmed 11, refuted 15, unverified 0, precision 0.42, round 3.
- confirmed by severity, as adjudicated in THIS record: **2 BLOCKER · 3 HIGH · 3 MEDIUM**.
- distinct defects: 8. Raw findings 1, 6, 12 and 19 are ONE defect — the hoisted section's Resume sentence and the AC9 window that cannot see it — reached by four lenses from four directions and reported as one blocker. One fold closes all four, and splitting them would quadruple-count a two-sentence rewrite plus one criterion, making the fix order lie about how much work is left.

**Precision moved 0.30 → 0.41 → 0.42, and is still under §8's 0.5 floor.** The scoping change round 2 prescribed did what it was supposed to do at the top of the funnel — the refuted population is no longer lenses grading a spec against its own carved-out non-goals — but the convergence it was also meant to fix got worse, not better: four of eleven confirmed findings are the same paragraph, against three of nine last round. That is not a priming failure. **A five-lens fan over five specs whose residue sites are already enumerated has more lenses than there are distinct places to look**, so the marginal lens re-finds the loudest defect instead of finding a new one. If this build runs a round 4 after the two blockers are folded, run it as a single-lens re-derivation of the eight rows below and nothing else; another fan will return this same paragraph a fifth time and cost a full cycle to learn it.

## BLOCKER — 2

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 1 · 6 · 12 · 19 | `spec-1` §2 S3 and §6 AC9 — offending site `tools/unattended/SKILL.template.md:35` and its byte-identical render `.claude/skills/unattended/SKILL.md:35` | **One defect, four lenses.** rev-4's fold corrected the `## Resume` section and left the hoisted `## Before any path` section — the section S3 itself creates — asserting the corrected-away claim verbatim. Line 35 of both halves reads *"A resumed session's keepalive is dead before it starts — the store went with the process — so that section carries its own instruction and this one cannot bind it"*, while `.claude/skills/unattended/SKILL.md:589` reads *"that intuition is MEASURED FALSE"* and, at `:592`, *"Assume a surviving job, not a dead one"*. Two directly contradicting sentences, 554 lines apart, in one delivered file, on the build's own subject — and the false half is in the section every path reads FIRST. S3 requires that hoisted section to state a resumed session's keepalive is *"presumed alive, not dead"*, so the artifact states the opposite of the scope clause that created it. The consequence is not stylistic: the sentence exempts `Resume` from the whole hoisted section, including the orphan-reap duty S2 exists to impose, so a resumed run that reads top-down and stops there learns its old job is dead AND that the reap paragraph does not bind it. That is the orphan leak `TOOL-aPromptedMandate-11` MEASURED — two jobs asserted dead, both still firing. **No criterion in the set can see it:** AC2 grades the hoisted section's line-number POSITION relative to `## Which path`, AC3 grades step numbering and a zero-count grep inside path bodies, AC9's observation window is the `## Resume` section alone. Every AC in unit 1 goes green over it, so the build's DoD cannot detect it. The parity leg byte-compares template against render and certifies the pair CONSISTENT, which is true and useless — both copies are wrong. No §3 non-goal reaches it: unit 1 withholds driver changes, the cadence, and a machine check on agent tool calls. | Rewrite `SKILL.template.md:34-38` so the Resume exclusion rests on the true reason — Resume is named because its ORDER differs: reap the recorded id, which is presumed ALIVE, then schedule the replacement — keeping only the pointer that `## Resume` carries its own sequencing, and re-render. Then close the grading gap that let it ship: widen AC9 from the `## Resume` section to the whole document, in AC1's own proven shape — *"the only surviving statement that a resumed keepalive is dead is the one that names it MEASURED FALSE"* — and add the criterion finding 1 asks for over the hoisted section's Resume paragraph specifically, so both sites are graded rather than one. While in the file, check §4's "Where the rule goes", which still reasons over four table paths only. | A rendered-Skill lint, and it is cheap because the contradiction is lexical: red the render when a normalised claim string appears both plainly and under a `MEASURED FALSE` / `SUPERSEDED` qualifier, unless the plain occurrence IS inside the qualifying sentence. AC1 already proved the pattern works — it grades exactly this for `before the run leaves` and it is why the protocol's superseded wording did not ship twice. The generalisation the two rounds have now earned: **when a fold marks a claim false, the same commit greps the whole render for that claim and every surviving hit is either fixed or named as the qualifier.** Spell it as a spec-format obligation too — a §9 revision line that says a claim was corrected reds if the claim's own words survive elsewhere in a file that unit touched. That is the machine form of the class both rounds have now been spent on. |
| 7 | `memory/builds/aPrimedKeepalive/build/2026-08-27-build-TOOL-aPrimedKeepalive-1-7-acceptance-ledger.md` — the `**Evidences:**` blocks for units 1, 2, 4 and 6, against each spec's §6 | **Every acceptance criterion a fold ADDED is missing from the journal that grades it.** Verified against the four specs and the ledger at HEAD: spec-1 §6 numbers AC1–AC9 (AC8 added rev-3, AC9 rev-4) and its block stops at AC7; spec-2 numbers AC1–AC9 (AC9 appended by a fold) and its block stops at AC8; spec-4 numbers AC1–AC6 (AC6 added rev-3) and its block stops at AC5; spec-6 numbers AC1–AC5 (AC5 rewritten rev-3) and its block stops at AC4. Not one carries an evidence line, and not one carries the `AMENDED` form the same file already uses correctly for unit-4 AC2 and unit-3 AC5. **It is the fold's omission and not a convention**, settled by unit 7: its own rev-3-added AC6 IS evidenced, in full, with the staged fixture and its revert. The impact is mechanical, not prospective — `tools/memory-tree/check-memory-hygiene.sh:1144` flattens every tracked record's `**Evidences:**` block into `<unit> <label> <form>` triples and joins them against every CLOSED Tier-2 spec's numbered criteria, failing on any gap. Units 1, 2 and 4 are Tier-2, so the moment they flip to CLOSED the build reds the very gate unit 6 was built to keep alive, at the push boundary, on a corpus-wide join. Unit 6 is Tier-1 and escapes that join, so its AC5 is simply ungraded with nothing that will ever say so. Any DoD claim of full acceptance coverage is false until these four lines exist. Worse in combination with the blocker above: AC9 of unit 1 is one of the four, and its subject is the sentence this review just found broken. | Append the missing lines in the file's own `- AC<n> — <observation>` shape. Unit 1 AC8: `wc -c memory/guides/SESSION-KICKOFF.md` — 25 417, under `MAX_MANIFEST_BYTES` 25 600. Unit 1 AC9: the `## Resume` reap-first text — **after** the blocker above is folded, not before, or the line certifies a document that contradicts itself. Unit 2 AC9: `wc -lc memory/guides/UNATTENDED-PROTOCOL.md` — 681 lines / 55 700 B against 750 / 61 440, plus the remaining-headroom figure AC9 demands. Unit 4 AC6: the unconditional `printf` at `check-unattended.sh:1128`, not routed through `report`. Unit 6 AC5. Anything that cannot be observed inside this build's budget goes in the `AMENDED` form with the reason, which is what that form is for. | The join already exists and it fires too late — only on CLOSED, only at the push boundary, only for Tier-2. The left-shift is one arm on the other side of the same data: when a spec's §9 gains a revision line that ADDS a numbered criterion, the same commit must add that criterion's evidence line or its `AMENDED` stand-in. Cheapest correct spelling, and it needs no new parser: red a build whose ledger's highest evidenced AC number for a unit is lower than that unit's highest numbered AC, at ANY status and any tier, rather than waiting for CLOSED. That catches all four of these today, catches the Tier-1 case check 23 structurally cannot, and costs one `sort -V | tail -1` per block against a population the checker already flattens. |

## HIGH — 3

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 3 | `spec-4` §6, against §2 S3 and §9 rev-3 — delivered site `tools/unattended/check-unattended.sh:1118-1131` | S3 names four fail-closed causes for check 7's exclusion; §6 grades three. AC3 covers the empty and the unresolvable anchor, AC4 covers the phase guard, and **"the witness does not resolve" has no criterion at all** — rev-3 extended AC3 to the newly-added fetched-tip cause and stopped there. **The delivered header asserts the behaviour the code does not have**, which is what makes this the stronger of the pair: `:1118-1119` reads *"IT FAILS CLOSED AND IT SAYS SO. No advertisement, an unresolvable witness, or a tip this clone has not fetched leaves the record counted, with the reason reported"* — and eleven lines below, the UNAVAILABLE `printf` at `:1128` is gated on its own `if` at `:1127`, `[ "$nlive" -gt 1 ] && [ -z "$c7anchor" ]` alone. A `LANDING` record that WAS eligible, anchor present, whose witness is empty or does not resolve in this clone, falls into `c7keep` at `:1140` with no line printed. So S3's "and the reason is reported" is undelivered, the block header claims it anyway, and no criterion in either half of the shared predicate observes either fact. A gate header claiming a report it does not emit is precisely the class charter §7 makes the header's job to prevent. Not withheld: unit 4's §3 non-goals cover `--landed`, `PHASES_TERMINAL`, non-`LANDING` phases, stamping `dTieredTribunal` by hand, and the driver's own `check_single_live` — none of them reaches this. | Emit the line. One `printf` in the `else` arm, naming the witness as the cause and distinguishing empty from unresolvable, alongside the existing EXCLUDED line and unconditional for the same reason `:1124` already documents. Then grade it: extend AC3 to the witness cause, or add *"When a `LANDING` record whose witness is empty or does not resolve in this clone is present with a second live run, check 7 counts it and reports the witness as the reason the exclusion did not apply."* Keep it in the `AMENDED` form beside AC2 and AC5 if the fixture needs a full-leg run — that is a known ~15-minute cost this build has already paid twice, and recording the gap honestly beats claiming an observation. | The generalisable form, and it is this repo's own rule turned into an arm: **a block header that says "it says so" is a testable claim.** For every fail-closed cause a spec's S-item enumerates, the leg must emit a distinguishable line, and the check is a fixture per cause rather than a reading of the code. Cheaper interim that catches this exact defect class across the kit: red a `# ----` header containing `SAYS SO` / `the reason is reported` when the block below it has fewer `printf` statements than the header has enumerated causes. Crude, and it would have fired here. The durable version belongs with the parity gate finding 2 asks for — one grammar, two spellings, and the causes each spelling reports must match. |
| 2 | `spec-7` §6, against §2 S3 and §9 rev-3 — delivered site `check_single_live` at `tools/unattended/unattended.sh:1118-1136` | The same gap at the driver half of the deliberately-duplicated predicate. S3 names four fail-closed causes; after the rev-3 fold §6 grades three — AC3 the empty and unfetched anchor, AC6 the phase guard — and the unresolvable witness has none. AC6's own text names the empty-witness branch, so the fold looked straight at the branch and gave it no observation. The shipped code prints nothing for it: `:1134` gates the UNAVAILABLE `printf` on `[ "$n" -gt 1 ] && [ -z "$anc" ]`, so a record whose anchor is present but whose witness is empty or unresolvable falls through to `n=$((n + 1))` silently. Half of S3 — "the record counts exactly as it does today, **and the reason is printed**" — is undelivered with nothing in the spec set able to fail on it. **AC6 is not a stand-in and the two causes are not one class:** a non-`LANDING` record was never eligible for exclusion, which is why AC6 correctly grades the ABSENCE of a line; the witness cause is the eligible-but-skipped case, and a `LANDING` record dropped from the exclusion for an unstated reason is indistinguishable from one that was never eligible. That is the silent-skip class this same section invokes by name. Not withheld: unit 7's §3 withholds only the genuinely-live second run, phase writing, and sharing an implementation with the leg. | Add AC7: *"When a record at `LANDING` whose `witness` fact is empty or names a commit this clone cannot resolve is placed beside this run's own, `--preflight` counts it, fires `check 5`, and prints the exclusion-unavailable reason naming the witness as the cause. Observed with a staged fixture, then removed."* The fixture is the one AC6 already stages — `sed` the witness of `memory/builds/dTieredTribunal/RUN.md` to a non-commit instead of `sed`-ing its phase — so the arm costs one extra `--preflight` invocation and no new scaffolding. Fold it together with finding 3 and finding 11, which touch the same two `printf` strings. | Both halves are missing the same branch, which is the argument for the gate round 2 already proposed and this round now has a second reason to want: a leg asserting that `check_single_live`'s predicate and check 7's exclusion agree, derived from both files, on the phase token, the ancestry call **and the set of causes each one reports**. §3 accepts two spellings of one grammar with its eyes open; the price of that decision is a parity gate, and without one the second copy drifts and only the first is ever graded. The interim that unblocks both fixtures is still one line: teach `runmd()` at `tools/unattended/unattended.test.sh:473` to write a `witness` fact, which is what currently makes every exclusion branch unreachable by the kit's own tests. |
| 20 | `spec-6` §2 S4 and §6 AC5 — surviving sites `tools/memory-tree/check-memory-hygiene.sh:1125` and `memory/builds/aPrimedKeepalive/README.md:84` and `:176` | S4 enumerates exactly three sites for the misidentification rev-3 called a BLOCKER — the block header comment at `:1104`, the run-time HELD line, the file header — and all three are correct in the tree, so the fold landed precisely where S4 pointed and nowhere else. **Three sites carrying the same wrong claim were never enumerated.** `check-memory-hygiene.sh:1125`, a comment inside the very block this unit edited, reads *"22 and 23 join every acceptance criterion of every CLOSED Tier-2 spec against every `**Evidences:**` block"* — false of check 22, which is the review-verdict vocabulary at `:610-662`, as the unit's own §3 non-goal states — and `:1134`, nine lines later, reads *"CHECK 22 IS NOT HELD AND IS NOT THIS BLOCK"*. The shipped gate contradicts itself inside one comment block. The build's own README states it as verified-at-BASE fact twice: `:84` *"the block at `tools/memory-tree/check-memory-hygiene.sh:1113` holding checks 22 and 23"*, and `:176`, the unit table row, *"hygiene checks 22 and 23 take the `--staged` guard"*. **AC5 is structurally incapable of catching any of them:** its grep is `# ---- 2[23]:`, which requires a colon immediately after the number and cannot match `...the pre-commit leg. 22 and 23 join...` or anything in a markdown file it does not scan. A maintainer trusting `:1125` repeats the exact error that made rev-1's AC2 and AC4 grade the wrong check. (Round 2's finding 9 residue at S3 and §5 IS fixed, and §3's non-goal now names check 23 alone — this is new ground, not a survival.) | Add the sites to S4 — the guard comment's own prose and the build README's Defect D paragraph and unit table row — correct `:1125-1127` to name check 23 alone, correct `README.md:84` and `:176` to say the block holds check 23. Then widen AC5 so it can reach them: `grep -n '22 and 23' tools/memory-tree/check-memory-hygiene.sh memory/builds/aPrimedKeepalive/README.md` returns only lines that name check 22 as NOT this block. A criterion whose regex cannot match the prose form of its own subject is a criterion that certifies the shape of the fix rather than the fix. | Same left-shift as the blocker above, one file over, and it is the cheapest instance of it: **a fold that corrects an identifier corrects every occurrence of that identifier's wrong form in the files it touches, and the criterion greps the WORD not the heading syntax.** As a standing arm: red a `check-*.sh` whose comment prose attributes a behaviour to a check number that another comment in the same file explicitly disclaims — here `22 and 23 join` at `:1125` against `CHECK 22 IS NOT ... THIS BLOCK` at `:1134`, nine lines apart, both authored by the same unit. Narrow, mechanical, and it fires on exactly this. |

## MEDIUM — 3

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 11 | `spec-7` §2 S3 against §6 AC3 — delivered site the UNAVAILABLE `printf` at `tools/unattended/unattended.sh:1135` | S3 names the unfetched-anchor cause as DISTINCT from the empty-anchor cause and requires the reason be printed, but AC3 collapses both into one observable and the driver prints one string for both: *"the live-run exclusion is UNAVAILABLE — no anchor was observed"*. `check_single_live` blanks `anc` in two cases at `:1119-1120` — `ASHA` empty, and `ASHA` set but failing `rev-parse --verify`. In the second case an anchor WAS observed; it simply does not resolve in this clone, which is exactly the cause rev-2 amended S3 to add. So a run refused by an un-excluded record is told its anchor was never observed when it was, and AC3 as written grades that message green. The sibling half gets it right — `check-unattended.sh:1128` prints *"no advertised default-branch tip resolves in this clone"*, which is truthful for both causes — so the two copies now disagree on the one axis §3 accepts duplication for: that the two spellings move together. | Split the driver's message so the unresolvable branch names itself — *"the observed anchor `<sha>` does not resolve in this clone"* — and amend AC3 to grade each cause against the reason actually printed rather than against the single UNAVAILABLE line. Fold with findings 2 and 3; all three are edits to the same two `printf` strings and the criteria that grade them. | The parity gate finding 2 names, in its strongest useful form: the two spellings must report the same CAUSE SET with the same distinctions, not merely reach the same verdict. Until that exists, the documented check is one line in the driver's header — the causes this function distinguishes, enumerated, so the next transcription copies the list rather than the string. A message that names one cause while covering two is the same green-by-absence family as a header claiming a report it does not emit; it is milder only because the record still counts. |
| 8 | The acceptance ledger — `Measured` table row `memory/guides/BUILD-METHOD.md`, and the `**Evidences:**` lines for unit 1 AC7 and unit 2 AC6 | Round 1 amended unit 1 AC7 and unit 2 AC6 to grade BOTH halves of the BUILD-METHOD pair with **the template named as the binding half**, and the record that grades them still prices only the render: both evidence lines read `wc -c memory/guides/BUILD-METHOD.md` — 24 573, and the `Measured` table carries one row, for the render. 24 573 is exactly the moment `spec-2` §4 records the render passing while the template stood at 24 584, 8 B OVER M1's 24 576. So the two amended criteria are certified green by evidence that measures the half the amendment was written to stop being the sole measure — the record would have passed over the breach it was rewritten to catch. The figures are also stale: measured here, the render is 24 549 and the template 24 560. The build's own budget report describes bytes that no longer exist. The staleness is not confined to that row — the same table's `memory/guides/SESSION-KICKOFF.md` row reads `25 236 B → 25 579 B`, against a BASE of 25 118 and a HEAD of 25 417, so both of the table's byte rows are wrong in both columns. | Re-run `wc -c` over both halves and rewrite the two evidence lines to carry both figures with the template FIRST, matching the criteria. Change the `Measured` row to name `tools/memory-tree/BUILD-METHOD.template.md` as the binding half, or add it as its own row, and correct the `SESSION-KICKOFF.md` row's two figures in the same pass — 25 118 before, 25 417 after. Both figures are in the last section of this record. | Round 2 left this with the owner and it is still the right call: one leg reading M1's stated budget and applying it to every carrier, sourced from `memory/project/method-carriers.txt`, which already enumerates them. The generalisation this round adds on the EVIDENCE side rather than the spec side: **where a criterion names two files, the evidence line that discharges it names two files.** A ledger arm can assert that mechanically — count the paths a criterion's text cites against the paths its evidence line cites, and red on fewer. It is the same join check 23 already runs, one field deeper, and it catches the honest-by-luck case this row is. |
| 4 | `spec-1` §6 AC8, against §2 S7 | **AC8 is the only criterion on the fourth carrier and its observable could not fail.** `memory/guides/SESSION-KICKOFF.md` measured 25 118 B at BASE `b4e1d5be` against `MAX_MANIFEST_BYTES` 25 600, so "at or below the cap" was green before any edit — a build that never touched the manifest passes AC8. Nothing grades that the §B bullet S7 promises exists or says the right thing. AC8's only other clause, *"the build's own §B bullet is the trim that paid for it"*, is not an observation, and the delivery makes it worse: the diff against BASE ADDS the bullet outright, +299 B, now 25 417, and trims nothing. So the one criterion covering S7's fourth carrier neither detects the omission of its own scope item nor accurately describes what happened — while every other carrier in unit 1 gets a content criterion (AC1 the protocol, AC2 and AC3 the Skill, AC6 BUILD-METHOD). The bullet is present at `:76` and correct, which means the ledger's green is honest by luck rather than by grading. This carrier was added to §2 precisely because the charter's DoD obliges a manifest update when a unit changes a governing doc, and that obligation is the one part of S7 with no observation behind it — a budget guard standing in for a content check. | Keep AC8 as the budget guard, drop the accounting clause, and add: *"When `memory/guides/SESSION-KICKOFF.md` §B is read, it carries the bullet stating the keepalive is scheduled FIRST, before orienting, and points at `UNATTENDED-PROTOCOL.md` §5 rather than restating the rule."* That line can fail; the byte count cannot. | The class is broader than this build and worth a spec-format arm: **a criterion whose observable is already true at BASE is not a criterion.** The mechanical form is a documented check rather than a gate — for every AC citing a threshold, record the value at BASE beside it, and any AC already satisfied at BASE must be paired with one that is not. Cheap, because the BASE sha is pinned in every spec's header and `git show BASE:<path> | wc -c` is one command. This repo's own rule states it already, in the section about gates: a criterion you have only ever seen pass is an assertion about nothing. It applies to acceptance criteria exactly as it applies to gate legs, and nothing currently says so. |

## The capped carriers, re-measured after this fold

All three are UNDER, and **not one moved between round 2 and round 3** — the round-2 fold spent no bytes on any capped carrier, which is a fact the next unit can plan against rather than a finding against this one.

| Carrier | Measured at HEAD `0300c20a` | Cap | Headroom |
|---|---|---|---|
| `tools/memory-tree/BUILD-METHOD.template.md` — the BINDING half | 24 560 B | M1's 24 576, **which may not be raised** | 16 B |
| `memory/guides/BUILD-METHOD.md` — the render | 24 549 B | 24 576 | 27 B |
| `memory/guides/UNATTENDED-PROTOCOL.md` | 681 lines / 55 700 B | hygiene check 6 — `GUIDE_CAP_LINES` 750, `GUIDE_CAP_BYTES` 61 440 | 69 lines / 5 740 B |
| `memory/guides/SESSION-KICKOFF.md` | 25 417 B | `MAX_MANIFEST_BYTES` 25 600 | 183 B |

The 11 B gap between template and render is the render substitution and is structural, not slack — the template is tighter and stays the binding half, per unit 1 AC7 and unit 2 AC6. Every fix in this record is a sentence, a criterion, an evidence line or a `printf`; the largest, the blocker's rewrite of `SKILL.template.md:34-38`, is roughly byte-neutral and the Skill is under no cap in this table.

## What the round-2 fold fixed — re-derived here

- **Round 2 blocker 18** (`KIT_MEMORY_TREE_VERSION` unbumped, `check-verdict-epoch.sh` RED at HEAD) — FIXED. The leg no longer reds and no unit in the set is blocked on it.
- **Round 2 finding 9** (`spec-6` S3 and §5 still quantified over "checks 22 and 23") — FIXED at S3, §5 and §3; §3's non-goal now names check 23 alone. Finding 20 above is NEW ground, at three sites S4 never enumerated, not a survival of this one.
- **Round 2 finding 20** (the Resume path resting on a premise measured false) — **HALF fixed, and the surviving half is this round's blocker.** The `## Resume` section was corrected, cites the measurement, and now instructs reap-then-schedule; the hoisted section S3 created was left asserting the disproved premise verbatim. The fold was real and it stopped one section short.
- **Round 2 finding 2** (`spec-7` §6 grading neither the phase guard nor the unresolvable witness) — HALF fixed. AC6 was added for the phase guard and observed with a staged fixture; the witness cause is finding 2 above, still ungraded.
- **Round 2 finding 10** (`spec-2` §4 pricing the render instead of the template) — FIXED in the spec. Finding 8 above is the same error surviving in the LEDGER that grades it, which round 2 did not look at.
- **Round 2 finding 6** (`spec-1` §6 not observing the PROMPT and PLAYBOOK path bodies) — FIXED; the cross-references resolve under the 0–4 numbering.

## Fix order

1. **Finding 1/6/12/19 first, and before anything else in the build.** It is the only defect in this record that is wrong in a DELIVERED, agent-read artifact rather than in a record about one, and the wrong sentence is the first thing an unattended run reads. Two halves in one commit: the template rewrite plus re-render, and the widened AC9. Do not evidence unit 1 AC9 before this lands, or the ledger certifies a self-contradicting document.
2. **Finding 7 immediately after**, because item 1 changes what unit 1 AC9's evidence line has to say. Four lines, plus any `AMENDED` stand-ins. Until they exist, units 1, 2 and 4 cannot flip to CLOSED without reddening hygiene check 23 at the push boundary.
3. **Findings 3, 2 and 11 together** — one `printf` added, one `printf` split, two criteria, all inside the two copies of one predicate. Doing them in one pass is the only way the two spellings end up reporting the same cause set, which is the condition `spec-7` §3 attaches to accepting the duplication at all.
4. **Finding 20** — three prose corrections and one widened grep, no dependencies.
5. **Findings 8 and 4** — two evidence lines and one criterion. The figures for finding 8 are in the carriers table above; nothing needs re-measuring.
