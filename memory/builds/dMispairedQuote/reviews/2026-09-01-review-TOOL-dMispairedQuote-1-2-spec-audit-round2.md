**Serves:** spec-audit TOOL-dMispairedQuote-1 TOOL-dMispairedQuote-2

# Spec audit round 2 — the FOLD of round 1 into the `dMispairedQuote` spec set

*Node d, 2026-09-01, round 2. The subject of this round is the FOLD: rev-2 is fresh prose nobody has reviewed, written to close round 1's 2 blockers and 6 highs, and the two bug classes this repo's own checklist selects for this diff are `fold-text-is-unreviewed-surface` and `amendment-leaves-its-other-half-standing`. Both were hunted first and both landed. No code has been written — the specs are still the PLAN, and they are audited as designs. Every surviving finding was re-derived against `tools/hooks/agent-cap.js` at BASE `d65da7ab`, against a patched copy implementing §4's published `checkLiteralOpen` verbatim, or against a gate actually run.*

Reviewed subjects, each pinned at the blob actually read:

- `memory/builds/dMispairedQuote/spec/2026-09-01-spec-TOOL-dMispairedQuote-1.md@a28d089b69af5d4d30c7f58c8f5e4ace794792fe`
- `memory/builds/dMispairedQuote/spec/2026-09-01-spec-TOOL-dMispairedQuote-2.md@67b0958eb47b4d0524cff3da180f4f0d73f068fb` — read as context; no finding was filed against it this round.

Round 2.

## Verdict: BLOCKED

Four confirmed blockers, nine highs and one medium, presented as 14 rows over 17 confirmed findings. All four blockers are in `TOOL-dMispairedQuote-1`, all four are in text the FOLD introduced, and none is a re-report: rev-1's whole-line fail-closed is genuinely gone and round 1's `B26` and `B27` are genuinely closed as filed.

**Round 1's exit condition was a strictly falling confirmed-blocker count from a ceiling of 2. It is not met.** The count rose to 4. That is the headline and it should not be softened: the fold removed one class of defect and its replacement mechanism introduced another, in the same direction the old one ran.

The shape repeats across all four. Rev-1's mistake was that a fail-closed handing back a RAW line is a fail-OPEN with respect to a consumer that reads across the line boundary. Rev-2 deleted the raw line and kept the other half of that insight — and the other half is the one that bites. Refusing a quote as a literal OPENER un-hides the characters the old mispairing was accidentally neutralising. A backtick that was swallowed now reaches `blankLiterals`' mode machine (#17, and #1's first reproduction). A stray `)` inside a regex that was blanked now re-enters `joinCall`, which closes early and reads the helper's default of 5 instead of a declared 50 (#1, second reproduction — `B27`'s mechanism with the sign reversed). A `//` inside a quoted span inside a template, which HEAD's backtick-blind regexes blank, now survives into rule 1's view and truncates it (#8). §4's closing sentence — "delimiters keep the structure the consumer walks" — and §5's "a precision loss and not a structural one" are both false in exactly the direction that matters, and every rule-3 criterion in §6 asserts a DENIAL, so nothing in the acceptance set can fail on any of it.

The fourth blocker is different and worse in one respect, because it is not a regression at all. §4's new keyword clause admits `return 'x'` and `case 'y':` — and also `one of 'em`, `do 'em` and `in 'ere`. Five of the fourteen members of `LITERAL_OPENERS` are ordinary English words, only three are measured anywhere in the spec, and §4's rejection of the previous candidate rests on `W6` (`/* run 'em */`) failing purely because `run` happens not to be a keyword. The class the unit exists to close stays open on ordinary prose, §5 asserts the residual is something else entirely, and S9 arms no probe for it.

`TOOL-dMispairedQuote-2` carries no finding this round. Its round-1 defect — a carrier list authored from the reported defect rather than derived from unit 1's touched set — is closed properly: S3, S5 and the two extra dossier bullets are in, `gotcha-classes` is claimed, `memory/map/generated/` is in its files-touched, and §6 now pins one AC per S-item. The irony is recorded in #26: unit 1 did not apply to itself the lesson round 1 taught its sibling.

## Review shape

- raw 30 · confirmed 17 · refuted 13 · unverified 0 · precision 0.57
- confirmed by severity, as adjudicated in this report: 4 BLOCKER · 9 HIGH · 1 MEDIUM, over 14 rows
- confirmed blockers: 4 · confirmed highs: 9

Precision rose from 0.28 to 0.57, just over the ~0.5 floor §8 names, on a fan that was NOT widened — round 1's advice was to prime harder rather than add agents, and priming with §4's candidate table, the three view headers and the `aLexedStripper` records is what did it. The refuted 13 were, again, readings of the spec text alone; every survivor was re-derived against the binary or the gate.

**Two rows fold three findings each and two, and are marked in place.** `#10 (with #5, #20)` is one defect filed three times — AC8's `reports/` write target — with one fix; `#13 (with #28)` is one defect filed twice — the `agent-cap self-test` leg is held. Merging them is honest: three rows asserting one fix would inflate the count the exit condition is measured on. The counts above are over the 14 adjudicated rows.

Severities here are **this report's adjudication**, not the finders' filings. One moved: #15 was filed MEDIUM and is promoted to HIGH, because the residual §5 understates is not a documentation nit — the reproduced shape (one legal-opener apostrophe before the fan-out, any same-kind quote after it, a trailing `//` comment included) is the shape round 1 measured as DOMINANT in `tools/workflows/*.js`, six lines including `tier2-review.js:3`, `:79`, `:101`, `:536`, and the sentence propagates verbatim into unit 2's S6 dossier residual and S8 backlog row.

## The two selected bug classes, and where they landed

**`fold-text-is-unreviewed-surface`** — eight findings sit in sentences that did not exist at rev-1: #1, #8, #17 and #24 (all four blockers), plus #11, #15, #18 and #22. The fold was written under time pressure to close a BLOCKED verdict, and it is the least-reviewed prose in the build.

**`amendment-leaves-its-other-half-standing`** — six findings are an edit whose counterpart was left where it was: S9 grew ADMIT-direction arms and AC4 was not touched (#2); AC8's "each new arm ... FAILS" quantifier was left standing over arms that pass at the tip by construction (#9); F21's fix relocated the RED observations without checking the folder is legal (#10); S7 bumped the constant over half its carrier population and dropped the "carrier agreement" half of F39's text (#25); F31's lesson was applied to the sibling unit and not to this one (#26); and §5's testing bullet was edited from "S7" to "S9" while "already on the bar" was left standing beside it (#13).

## Confirmed findings

### BLOCKER — 4

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 1 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §2 S5, against §5 risks, §4's closing paragraph and §6 (no AC covers it) | The chosen mechanism produces two DENY-to-ADMIT moves, because refusing a quote as an OPENER un-hides the characters the old mispairing was accidentally neutralising. **Reproduction A** (widened `aLexedStripper-4` mode leak): a script with `const re = /it's` then a backtick then `don't/` above a multi-line `boundedParallel(LENSES.map(...), 50)` exits 2 at BASE, because HEAD's mispairing blanks the backtick — `blankLiterals` returns `const re = /it''t/`, verified by calling the function. Remove the apostrophes and the same script exits 0: the exposed backtick puts the scan in template mode, the out-of-scope `let mode` leak blanks every later line, and the cap-50 call disappears. Under S5 the first script BECOMES the second. **Reproduction B** (unmodelled regex parens, no mode leak needed, and the stronger half): `blankLiterals` on `labels.map((s) => () => agent(s.replace(/it's)don't/, 'y'))),` returns `.../it''t/, ''))),` at BASE and `.../it's)don't/, ''))),` after, so the stray regex `)` the old mispairing blanked is exposed, `joinCall` closes early, `topLevelArgs` yields one argument, the helper DEFAULT of 5 is read, and a declared cap of 50 exits 0 where BASE exits 2. This is `B27`'s mechanism with the sign reversed. It is not the accepted residual — that one is about quotes that STILL mispair; this is a fail-open the new mechanism CREATES. §5's "a precision loss and not a structural one" and §4's "delimiters keep the structure the consumer walks" are both false in the fail-open direction, and every rule-3 criterion in §6 asserts a denial, so nothing can fail on it. | Add an AC per reproduction — the backtick-between-apostrophes script above a multi-line cap-50 call exits 2, and a multi-line cap-50 call whose argument line carries a regex-borne `)` between two prose apostrophes exits 2 — each with its S9 arm staged RED. State in §5 that the mechanism moves delimiters and mode-openers INTO the views, not only out of them. Then close it in S1/S5: either apply the same opener decision to the BACKTICK branch (costing tagged templates, ``tag`x` ``), or require an unpaired quote's line to preserve the delimiter and mode balance the mispairing supplied, so `joinCall` and the mode machine see what they see today. | A table-driven rule-3 arm set in `agent-cap.test.sh`, one row per call-site SHAPE, each row carrying a variant and its control, plus a **delimiter-conservation** property arm: for every corpus line, assert that the count of unescaped `(`, `)`, backtick and `//` occurrences a view emits is ≤ what HEAD's view emits. That arm is the class, not the instance — it reds on any future change that un-hides a delimiter, whichever branch does it. |
| 8 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §2 S2, against §4's `B26` reproduction and §5 risks; no AC in §6 covers it | S2's new sentence — "It skips a same-line backtick span whole rather than scanning inside it", plus "It does NOT blank template contents" — REMOVES a blanking HEAD performs. HEAD's two regexes are backtick-blind and blank a quoted span wherever it sits, template interiors included. Under S2 that span is emitted verbatim, so a `//` inside it survives into `offendingLines`' `stripStrings(line).split('//')[0]` and truncates the view — the exact channel `B26` names, one construct over. Reproduced against the real hook at BASE: a line assigning a template containing `see 'http://x' now` followed by `await parallel(all.map(f))` exits 2 today (rule 1 prints the offending line); under S2 the view becomes the text up to `http:` with the primitive gone, i.e. exit 0. No other rule catches it — there is no `agent(` for rule 2 and no bound for rule 3. §3's and S3's ceiling "No view ever hands a consumer a raw line" is false inside a template span, and so is unit 2's S1 replacement text ("its line is still blanked"), which would ship a second untrue ceiling in the carrier written to fix the first one. AC2's backtick shape still denies under S2 because the pass resumes after the closing backtick, so §6 covers it nowhere. | Scope the backtick skip to OPENER DETECTION only: do not treat a quote inside the span as an opener, but still blank the contents of a quoted span found inside it, so the `//` cannot survive. Or state in S2 that a same-line backtick span is emitted with its inner quoted spans blanked. Add an AC pinning the template-with-a-quoted-URL script at exit 2 and an S9 arm staged RED, and correct unit 2's S1 replacement text to cover template-borne quoted spans, not only the unpairable quote. | The same conservation arm as #1 covers this one if it counts `//`: assert no `//` reaches any view from inside a quoted span that HEAD blanked. Add it as a per-view property test over the whole tracked corpus rather than a fixture, since the population is "every line with a quote inside a template" and a fixture set cannot enumerate it. |
| 17 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §2 S5, against §3's `TOOL-aLexedStripper-4` non-goal and §6 (no AC covers it) | S5 assumes `blankLiterals`' quote branch can be changed without changing what its cross-line `mode` machine SEES. The branch's `while (i < raw.length && raw[i] !== q)` currently SKIPS the whole mispaired span, so a backtick inside it never reaches the mode switch. Removing the swallow feeds it. Measured at BASE with the shipped functions: the two-line script `const re = /won't` + backtick + `x/; const s = 'y'` above `await boundedParallel(items, 50)` returns both lines with the fan-out intact and the hook exits 2; with the apostrophe removed — which is exactly the scan path S1's predicate forces, since `won` is not in `LITERAL_OPENERS` — the second line comes back EMPTY and the hook exits 0. A DENY-to-ADMIT move on the only rule that reads a declared cap, caused by this unit, which §3 also lists as out of scope ("Any change to what rules 1, 2 or 3 DECIDE"). §3 waves it off in one sentence — "S5 touches the quote branch and not the mode" — and that is the false assumption: the quote branch decides which characters the mode machine ever sees. Not a re-report of the by-design `-4`: the leak already exists, and the finding is that S5 newly ROUTES a currently-denied script into it. | Add a scope line to S5 stating that an unpairable quote re-admits every later character on its line to the mode machine, and choose a mechanism that survives it — cheapest is to bound the `-4` leak for this function inside this unit (reset `mode` per line, or refuse to enter `tmpl` from a line whose scan began in code mode) rather than declaring it out of scope. Add an AC pinning the backtick script at exit 2 with the apostrophe-removed control recorded as the ADMIT it is at BASE, and an S9 arm staged RED. | A `blankLiterals` liveness arm: a two-line fixture whose first line ends inside a template, asserting the SECOND line is still scanned. It reds today (that is `-4`) so it lands as an xfail with the record id, and flips to a pass when `-4` closes — which converts an OPEN backlog row into a gate that will tell someone when it is fixed, instead of a note that will not. Pair it with the direction-coverage canary from #2. |
| 24 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §2 S1 and §4 Data model (the `checkLiteralOpen` snippet), against §5 risks and §6 (no AC covers it) | The fold's keyword clause re-opens the exact fail-open the unit exists to close, for prose whose last word is a JavaScript keyword. `LITERAL_OPENERS` ships fourteen members and five are ordinary English words. Traced over the published snippet plus S2's ordered pass, `/* one of 'em */ await parallel(all.map((f) => agent('a')))` gives deny=FALSE: the whitespace skip lands on the `f` of `of`, `of` is in the set, the apostrophe is ruled a legal opener, `resolveLiteralEnd` pairs it with the quote opening `agent('a'`, the span between is blanked, and `parallel(` leaves rule 1's view exactly as it does at HEAD. `do 'em` and `in 'ere` admit too. `run 'em` — which is `W6`, the case §4 uses to REJECT the opener-alone candidate — denies only because `run` happens not to be a keyword, so the criterion that eliminated the previous candidate is satisfied by vocabulary accident. The 21-case corpus holds no keyword-final prose case, only `return`, `case` and `throw` are measured anywhere, §5's residual is scoped to a BALANCED set of loose quotes and does not reach this unbalanced single apostrophe, and S9 arms no probe for it. Not a regression against HEAD — the security surface the unit exists to close simply stays open for a class the spec asserts is closed. | Either narrow `LITERAL_OPENERS` to the members a probe actually exercised and state the exclusion of the English-word keywords, or require a second condition the prose case cannot meet (the keyword clause fires only outside a comment or regex span, or only where the keyword's preceding token is a statement boundary). Add the `/* one of 'em */` shape to §4's rule-1 corpus with its measured verdict, name whichever residual survives beside the balanced-quote one in §5, and add an AC pinning a keyword-final prose apostrophe above a raw `parallel(` at exit 2 with an S9 arm staged RED. | Parameterise the rule-1 fixture over the SET: one arm per member of `LITERAL_OPENERS`, each a `/* <keyword> 'em */` line above a raw `parallel(`, asserting the unit's chosen verdict. The population is then the declared set itself rather than a sample of it, and adding a keyword to the set adds its fixture — the derive-over-author rule §6 states, applied to a test corpus. §7's "gate the CLASS, not the instance" names exactly this failure: measuring three of fourteen members certifies coverage the unit does not have. |

### HIGH — 9

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 2 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §2 S9 ("in both directions") against §6 AC4 | S9 claims arms covering the class "in all three rules AND in both directions", but its own enumeration gives ADMIT-direction arms only for rule 1 and for the `-5` fallback fixture. Rule 3 has none, and AC4's control ("the control with the apostrophe removed") also exits 2, so every rule-3 observation in §6 is a denial — the could-not-fail shape §7 names, in the fixture group for the rule that carried `B27`. A real ADMIT-direction case exists and is unpinned: at BASE a LEGAL multi-line `boundedParallel(LENSES.map((s) => () => agent(s.replace(/won't/, 'x'))), 5)` exits 2 with "the boundedParallel() CALL SITE never closes its parens within 200 lines", because `blankLiterals`' run-to-end-of-line swallow eats the argument line. S5 removes that swallow, so the same script must exit 0 afterwards. Nothing in §6 pins it, so the swallow removal — the one behaviour change in this unit with no criterion at all — can silently not happen, or happen and break, with every AC still green. AC7's "pass count above 105" cannot observe it. | Add an AC pinning that legal cap-5 multi-line call at exit 2 before and exit 0 after, with its S9 arm written as a control/variant pair, and correct S9's "both directions" sentence to enumerate a rule-3 ADMIT arm rather than asserting a property its own list does not deliver. | A suite canary in `agent-cap.test.sh`: for each rule's arm group, assert the group contains at least one arm asserting exit 0 and at least one asserting exit 2. A one-directional group reds by that fact. This is the round-1 gate suggestion for `B26` generalised from a pair to a property, and it is the cheapest mechanical answer to "a fixture group that only ever asserts denials cannot catch a fail-closed that has become a fail-open". |
| 9 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §6 AC8, against §2 S9's ADMIT arms and controls, and against AC5 and AC6 | AC8 quantifies over every new arm — "When each new arm from S9 is staged against the tip WITHOUT the fix, it FAILS" — but the fold added arms to S9 that pass at the tip BY CONSTRUCTION. Run against the tip: `log('parallel(') // we don't allow it` exits 0 (round 1: HEAD yields the blanked form and admits); the `return`/`case`/`throw` string arm exits 0 because HEAD blanks the string; `-5`'s fixture exits 0, which AC6 states in as many words ("both before and after"); and S9's "control with the apostrophe removed" denies at BOTH sides, which §4's own `B27` paragraph states. §6 therefore contradicts itself within one section. AC8 is unsatisfiable as written for roughly half of S9's arms, and the builder's two exits are both bad: weaken or drop the ADMIT-direction arms and controls so every arm can be shown RED — deleting the both-directions coverage the fold was written to add — or record a fabricated RED. This is rev-1's AC left standing after rev-2 amended the scope item it grades. | Scope AC8 to the DENY-direction arms: "each new arm asserting exit 2 fails when staged against the tip without the fix". Add a second clause naming the ADMIT-direction arms and the controls explicitly and asserting they are GREEN on both sides — a stated skip, per §7's "a skip must announce itself" — so a green control is never misread as an observed failing case. | Make the staged-RED claim per-arm DATA rather than a blanket AC: each new arm carries a marker comment declaring its direction and, for a DENY arm, the sha it was observed RED at. A checker asserts every DENY-direction arm has one and every ADMIT-direction arm does not. That closes the class §7 names — a gate you have only ever seen pass is an assertion about nothing — without letting the reverse (a control that cannot go red) masquerade as a missing observation. |
| 10 (with 5, 20) | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §6 AC8's write target, against §4 "Files touched" and §7 Gates | AC8 sends the per-arm RED observations to `memory/builds/dMispairedQuote/reports/`, a folder the memory-tree gate FORBIDS. `tools/memory-tree/check-memory-hygiene.sh` check 4 whitelists a build folder's entries by string equality — `F:README.md`, `F:RUN.md`, `D:prompts`, `D:spec`, `D:build`, `D:reviews`, plus the archived `RUN.<PHASE>.<8hex>.md` grammar — and the awk flush at ~line 384 prints every other entry as a check-4 failure. Verified by staging a dated record there and running the gate: `HYGIENE check 4 FAILED — build-folder naming/shape: memory/builds/dMispairedQuote/reports` (staging reverted, tree clean). The leg carries NO `guard` in `tools/gate-legs.json` (`chunk: records`, `subject: repo`), so it runs on EVERY bar. It is underivable as well as illegal: check 5's selector is `builds/[^/]+/(prompts\|spec\|build\|reviews)/`, `gen_build_index.py:54` sets `RECORD_KINDS = ("spec", "build", "reviews", "prompts")`, and no build in the corpus has a fifth directory. §4's files-touched has no `memory/builds/` row at all and §7 names no leg that would explain the red. Round 1's F21 fix text offered this path as an option; the fold took it without checking the gate's closed set, so F21 is RELOCATED rather than closed. | Point AC8 at `memory/builds/dMispairedQuote/build/2026-09-01-build-TOOL-dMispairedQuote-1-<tail>.md` — the sanctioned kind for a dated evidence record, inside both check 4's whitelist and `RECORD_KINDS`, so the generated build index carries it — give it a `**Serves:** build TOOL-dMispairedQuote-1` head, add that `build/` row to §4's files-touched, and add `memory hygiene` to §7's gate list, since S8's `DECISIONS.md` row is graded by it too. | A `memory hygiene` arm asserting that no AC in a spec under `memory/builds/*/spec/` names a build-folder path whose first segment is outside check 4's whitelist. One regex, and it catches the whole class — round 1's F21 (a spec pointing an AC at `RUN.md`) and this round's relocation of it would both have been caught while the spec was still cheap to change. The gate's own closed set is the source; the arm derives from it rather than restating it. |
| 11 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §4 Inventory (rows 5 and 6), against §4's own published `checkLiteralOpen` block and §5 security | The row "tracked `.js` `.sh` `.py` fed line-by-line to both rule-1 predicates — 86217 lines, 4319 blank differently, **0 verdict flips**" is not reproducible from the mechanism §4 publishes. Implementing that code block verbatim plus S2/S3's stated pass over the same corpus gives the row's line count EXACTLY — 140 files, 86217 lines, so the corpus is identical — but **15537** lines blanking differently (15342 with the backtick skip disabled) and **5** verdict flips, not 0. The flips are `tools/hooks/agent-cap.test.sh:38,39,42,43,44`, all the shell shape `check "raw parallel(items.map) → deny" 2 '{json}'`, and they follow from the published predicate alone: the `"` after `check ` skips whitespace back to `k`, the word is `check`, `check` is not in `LITERAL_OPENERS`, so the quote is not an opener and the string is never blanked, leaving `parallel(` visible where HEAD blanks it. All five are ADMIT-to-DENY on a fixture file never fed to the hook as a script, so the safety direction is intact; the defect is that §5's security bullet cites this table as "the evidence" for the both-directions claim, and a reader cannot re-derive it from the design as written. Either the measured candidate is not the one §2 and §4 specify — in which case the whole table grades a different mechanism — or the row is wrong. | Re-run the line-by-line comparison against the exact predicate §4 publishes and restate both numbers, or state which additional rule the measured candidate carried that the block omits (a language filter, a shell exclusion, or an opener allowance after a bare command word). If shell files are outside the predicate's domain, say so in the row rather than counting their lines in it. | Commit the candidate harness itself under `memory/builds/dMispairedQuote/build/` and have §4's inventory rows name the command that produced each figure, so a later round RE-RUNS the measurement instead of re-implementing it from prose. This is §7's "NO count of a derived population is written in prose" applied to a spec: a number typed beside a mechanism it was not derived from is exactly the failure here, and the fix is the same one the charter prescribes for code — derive it, or point at what does. |
| 13 (with 28) | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §5 "testing + left-shift gates" and §7 Gates, against `tools/gate-legs.json` and `tools/run-gates/run-gates.sh:947` | §5 rests the left-shift on "the `agent-cap self-test` leg already on the bar" and §7 lists that leg above "`bash tools/run-gates/run-gates.sh` at the push boundary". The leg's manifest entry is `chunk = "selftests"`, `subject = "kit"`, and the runner holds every `subject = kit` OR `chunk = selftests` leg unless `GATE_SELFTESTS=1`, printing `GATE held <name> (self-test, set GATE_SELFTESTS=1 to run)`. `GATE_FULL` explicitly does NOT lift it — the comment at `:925-945` says so and `changed()` is the only thing `GATE_FULL` touches — and `.githooks/pre-push` sets `GATE_FULL=1` in its forcing case and never `GATE_SELFTESTS`. AGENTS.md records the owner ruling: a kit's self-tests are not on this bar, on demand only, set by no boundary. So every regression arm S9 adds — the unit's entire left-shift and §7's observed-failing-case evidence — lands in a leg the merge bar does not execute, and a later regression in `agent-cap.js` reaches `main` with a green bar. Of §7's legs, `verifier fan-out` and `agent-cap restatement` do run (both `subject = repo`, unguarded), but neither runs S9's arms. §7 currently reads as though its last bullet subsumes its first; it does the opposite. | State the invocation in §7: the leg is HELD, and the DoD run for kit work is `GATE_FULL=1 GATE_SELFTESTS=1 bash tools/run-gates/run-gates.sh`. Correct §5's "already on the bar" to "on the bar only under `GATE_SELFTESTS=1`". AC7 invokes the script directly and is fine as written; it is §5 and §7 that overstate. | A spec-lint arm (`agent-cap restatement` is the natural host, since it already greps specs): any spec §7 naming a leg whose manifest entry is `chunk = selftests` must also name `GATE_SELFTESTS=1`. The manifest is the source, so the arm derives the held set rather than carrying a list. It is a one-line predicate and it closes the green-by-absence class §7 documents — the belief that a green default bar covers arms it structurally cannot reach. |
| 15 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §5 risks (the stated residual), against §2 S1's `resolveLiteralEnd` contract and §8 F2 | §5 says the residual "needs an apostrophe in a legal opener position on BOTH sides of the fan-out", but S1's contract opener-tests only the OPENING quote — `resolveLiteralEnd` "returns the index of the unescaped partner on the same line", with no opener test on the partner. So the shape needs one legal-opener-position quote BEFORE the fan-out and ANY unescaped quote of the same kind after it, a possessive apostrophe in a trailing `//` comment included, which `stripStrings` reaches because it runs before the comment strip (`agent-cap.js:69`, and S3 says so). Reproduced: with S1/S2 implemented as specified, `/* ('nuff) */ await parallel(all.map(f)) // we don't` yields the view `/* (''t` and rule 1's predicate sees NO fan-out. Round 1 measured possessive apostrophes in `//` comments as the DOMINANT apostrophe shape in `tools/workflows/*.js`. The "does not occur in this corpus" clause is also unmeasured: the shape mispairs identically at HEAD and under the fix, so it produces no verdict flip in §4's inventory, which counts flips. Separately, §8's F2 prices its chosen option with this residual, but the residual is a property of the OPENER predicate and is present identically under all three F2 options — under a fail-closed the two quotes still pair, so the fallback branch is never reached. Unit 2's S6 dossier residual and S8 backlog row are both derived from this sentence, so the understatement propagates into the two records a future reader will actually find. | Restate the residual as: a quote in a legal opener position before the fan-out plus ANY later unescaped quote of the same kind on the line, comment text included. Either measure that shape's occurrence over `tools/workflows/*.js` and the suite fixtures and give the number, or drop the "does not occur in this corpus" clause. In F2, attribute the residual to the opener predicate rather than to the fallback choice. | A `verifier fan-out`-adjacent arm that counts the residual shape over `tools/workflows/*.js` and reds when the count RISES — a ratchet, not a threshold, in the shape `.lexicon.conf`'s `VERB_OFFENDER_PIN` already uses in this repo. An accepted residual with a shrink-only pin is an accepted residual somebody will notice growing; an accepted residual described in prose is one nobody will. Pair it with the trailing-comment fixture pinned at whichever verdict the unit chooses. |
| 18 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §2 S4 | S4 asserts "mode tracking, the `unterminated` flag and the fallback route are all untouched". The flag's CODE is untouched; its VALUE is not. `renderCodeView`'s quote branch does `i = e + 1`, skipping the span, so a backtick inside a mispaired span never reaches `stack.push('tmpl')`. Measured on both copies of the function: at BASE, `renderCodeView("const re = /won't` + backtick + `x/; const s = 'y'").unterminated` is **false**; under the S1/S4 implementation the same input returns **true**, because the apostrophe no longer opens a literal, the span-skip no longer runs, and the backtick reaches the stack. `fanoutFindings:356` then judges the WHOLE script with the per-line fallback view instead of `view.code`. So S4 changes WHICH OF TWO VIEWS rule 2 judges a script with, and that population move is measured nowhere: §4's candidate table scores rule 2 over three probes, the inventory has no row for view selection, and §5 names only the balanced-loose-quote residual. `TOOL-aLexedStripper-2` measured that fallback view DENYING a correct five-element fan on five prose spellings, so the direction is a known precision loss and is simply unknown in the other. The 1261-file sweep bounds the verdict effect on tracked files and says nothing about the view-selection population. | Delete the "untouched" clause and replace it with the true property: the flag is computed by untouched code over a scan that now walks characters it used to skip, so its value moves. Add an inventory row measuring how many corpus scripts change VIEW (primary vs fallback) under the candidate, and an AC pinning the verdict of a script whose flag flips — the backtick-in-a-regex line above a five-lens bounded fan — at the same exit code before and after. | A two-view AGREEMENT arm: for every script in the suite corpus, assert `view.code` and the per-line fallback reach the same verdict, with the known divergences declared as named exceptions. A second view whose only coverage is the arms of the record that created it is a population of one — round 1 said so about `-5`'s six arms, and this finding is what that population misses. The arm also makes the view-selection move observable, which is the thing S4 currently asserts away. |
| 25 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §2 S7, §4 Files touched and §6 AC10, against §7's `kit versions` gate | The `gov:kit agent-cap@` population is FOUR tracked carriers, not the one line S7 names. `git grep -lE "gov:kit agent-cap@" -- '*.js'` returns both `agent-cap.js` copies AND `tools/hooks/scratch-guard.js:41` and `.claude/hooks/scratch-guard.js:41`, each carrying `gov:kit agent-cap@1.9` on a line whose own constant is `KIT_SCRATCH_GUARD_VERSION`. `tools/check-kit-versions.sh:60-70` derives that exact population and compares every carrier to the constant, printing `carries a gov:kit agent-cap@ marker that is not <V>` per offender. S7 moves the constant and its same-line marker, §4's files-touched lists only the two `agent-cap.js` copies, and AC10 greps ONE file — so both `scratch-guard` copies stay at `1.9` while the constant reads `1.10`, and the checker exits 1 with two lines. The `kit version markers` leg is `chunk: declarations`, `subject: repo`, NO guard, so it runs on every bar, and §7 names it as a gate this unit must pass. Prior art decided this exactly: `dTieredTribunal` spec-audit round 1 findings 24, 33 and 36 are the same half-bumped population on this same kit, and the checker's own comment records the failure having passed "for the THIRD time" before the population was derived. The fold also dropped the half of round-1 F39 that matters — S7's rationale says the checker "grades its FORM only", where F39 said "FORM and carrier agreement", and it is the agreement half that reds here. | Restate S7 over the DERIVED population — all four tracked `*.js` carriers move to `1.10` — add both `scratch-guard.js` paths to §4's files-touched, and rewrite AC10 in the shape the prior build's audit prescribed: `grep -rn 'agent-cap@1\.9' tools/hooks/ .claude/hooks/` returns no hits, `grep -rln 'agent-cap@1\.10' tools/hooks/ .claude/hooks/` names all four, and `bash tools/check-kit-versions.sh` exits 0. Correct S7's rationale to say the checker grades form and carrier agreement but not MOVEMENT. | The bump rule the checker still lacks, which round 1 already proposed and this finding re-earns: a `check-kit-versions.sh` arm that reds when a kit's `version_from` FILE changes in a commit without its version constant changing, waivable by an explicit commit marker. Add the carrier half beside it — when the constant moves, every derived carrier must move in the same commit. The population is already derived by that script, so both arms are predicates over a set it computes rather than a list anybody maintains. |
| 26 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §4 Files touched, with §2 S1/S6 and §7's gate list | S1 adds two top-level function definitions to `tools/hooks/agent-cap.js` and S6 mirrors both into `.claude/hooks/agent-cap.js`. That file already contributes 23 rows to the committed `memory/map/generated/symbols.json` (including `stripStrings`), because `map_extractors._build_js_layer` scans top-level definitions under `tools/`, so `checkLiteralOpen` and `resolveLiteralEnd` become new rows. No scope item regenerates the artifact, §4's files-touched has no `memory/map/generated/` row, and §7 does not name the leg that catches it. `tools/codebase-map/test_codebase_map.py:128 test_generated_artifacts_are_fresh` byte-compares `symbols.json` against a live re-derivation, and its leg `codebase-map coverage + freshness` is `chunk: declarations`, `subject: repo`, unguarded — it runs on every bar. No git hook regenerates the map. §7's closing `run-gates.sh` bullet therefore cannot be green as the unit is scoped. This is the class round 1 adjudicated one unit over — F31 against `TOOL-dMispairedQuote-2`, whose fix was "regenerate `memory/map/generated/` in the same commit and list it in files-touched" — and the fold applied it to the SIBLING and not to itself, though the charter DoD states the rule for every claim edit. Unit 2's §4 carries the row; unit 1's does not. | Add `memory/map/generated/` to §4's files-touched with the regeneration command as its reason, add a scope line (or extend S1) requiring the regen in the same commit, and add `codebase-map coverage + freshness` to §7's gate list. | A DoR-level lint rather than a new gate, since the ratchet already reds: assert that a spec whose §2 adds a top-level definition to a file already present in `memory/map/generated/symbols.json` names `memory/map/generated/` in its §4. The artifact is the source of the file list, so the check derives its population. Cheaper still and worth doing anyway — a pre-commit regen for the map, which removes the whole class rather than reporting it, at the cost of one generated-artifact write per commit that touches a scanned tree. |

### MEDIUM — 1

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 22 | `2026-09-01-spec-TOOL-dMispairedQuote-1.md` §2 S2, against §3's `TOOL-aLexedStripper-3` non-goal | S2 adds "It skips a same-line backtick span whole rather than scanning inside it" and §3 says "S2 deliberately leaves the ceiling where it is". Those two sentences contradict each other. Today `stripStrings` has NO backtick awareness — its two regexes blank `'…'` and `"…"` INSIDE a template — so a quoted primitive in a prompt is invisible to rule 1. Verified at BASE: a script assigning a template that reads `never write 'parallel(' by hand` above a bounded fan exits **0**, while the unquoted variant exits 2, which is the `-3` ceiling. Under S2 as written, that quoted primitive survives into rule 1's view and the lookbehind passes on the preceding apostrophe, so it DENIES — old deny=false, new deny=true on exactly that line. A new rule-1 false-positive class: a lens prompt that QUOTES the banned spelling, which is the natural way to write a prompt teaching this rule. §4's candidate table scores nothing that would have caught it, and the whole-file sweep could not see it — its four flips were all markdown records. | State in S2 which of the two properties wins and measure it: either keep blanking quoted spans inside a backtick run (skipping only for OPENER purposes, not for pairing — which is also #8's fix, and one edit closes both), or record the WIDENED ceiling in §3 beside `TOOL-aLexedStripper-3`, with an AC in each direction: the quoted-primitive-in-a-prompt script pinned at whichever exit code the unit chooses, plus its unquoted control. | A control/variant arm for the CEILING itself: the quoted and unquoted spellings of a primitive inside a prompt, both pinned. A documented non-goal with no fixture is a ceiling nobody will notice moving — which is precisely what happened here, twice, since `-3` has now been silently widened by a spec that names it as out of scope. Pinning it makes the next move a red rather than a discovery. |

## What the fold closed, and what it only moved

Round 1 filed 11 confirmed findings. The audit below is per finding, against rev-2's text and against the tree.

| Round-1 finding | Status after the fold | Evidence |
|---|---|---|
| 26 — BLOCKER, S3's whole-line fail-closed (rule 1) | **Closed as filed**, class re-opened | Rev-1's fail-closed is deleted and §4 records `B26` reproduced. But #8 re-opens a rule-1 DENY-to-ADMIT through the exact `//`-truncation channel F26 named, one construct over. |
| 27 — BLOCKER, S5's fail-closed vs `joinCall` (rule 3) | **Closed as filed**, class re-opened | Same deletion, `B27` recorded. #1's second reproduction is `B27`'s mechanism with the sign reversed, and #17 reaches rule 3 by a second route. |
| 22 — HIGH, lexicon-refused names | **Closed** | `checkLiteralOpen` / `resolveLiteralEnd`; both verified live — `python tools/lexicon/lexicon.py --suggest` answers OK for each. AC11 pins them and §7 lists the leg. |
| 25 — HIGH, `stripStrings`' third consumer | **Closed** | S2 now names all three consumers in bold, and §4's view inventory says "rule 1's view and rule 2's fallback". |
| 29 — HIGH, S4's mid-template substitution | **Closed as filed**, replaced by a new untrue claim | The substitution is gone; S4 keeps the branch's existing behaviour. But #18 shows its replacement sentence is false of the flag's VALUE, and the view-selection population it moves is measured nowhere. |
| 32 — HIGH, `-5`'s cannot-regress clause | **Closed** | S8 records the supersession in `memory/DECISIONS.md` and keeps `-5`'s fixture green on both sides (AC6). |
| 33 — HIGH, unit 2 omits `blankLiterals`' header | **Closed** | Unit 2 S5, plus §1's authored carrier count dropped. |
| 34 — HIGH, unit 2 omits the second `-5` note and dossier bullets | **Closed** | Unit 2 S3 and S6; AC2 pins BOTH `aLexedStripper-5` hits and AC5 pins each dossier bullet. |
| 31 — MEDIUM, unclaimed `gotcha-classes` key | **Closed in unit 2, re-opened in unit 1** | Unit 2's S7 claims the key, AC6 pins the ratchet, and `memory/map/generated/` is in its files-touched. Unit 1's S1 adds two symbol rows and does none of that — #26. |
| 39 — MEDIUM, no `KIT_AGENT_CAP_VERSION` bump | **Partially closed** | S7 bumps the constant and its same-line marker over 2 of 4 derived carriers (#25), and its rationale drops F39's "carrier agreement" half, which is the half that reds. |
| 21 — LOW, RUN.md as the AC write target | **Relocated into a gate red** | AC8 now writes to `memory/builds/dMispairedQuote/reports/`, which check 4 forbids and no build in the corpus has. F21's actual complaint — the observations land where nothing indexes or derives them — is equally true of the new folder (#10). |

Six closed outright. Two closed as filed with the class re-opened by the fold's own replacement mechanism. One closed with a new unmeasured claim in its place. One partial, one relocated. **The fold's hit rate on the round-1 text is good; its hit rate on the text it wrote to achieve that is the problem**, which is the `fold-text-is-unreviewed-surface` class stated as a measurement rather than a warning.

## Two notes on what this round did NOT find

**No finding was filed against `TOOL-dMispairedQuote-2`, and that is the fold working.** Round 1's diagnosis was that unit 2's carrier list was AUTHORED from the reported defect rather than DERIVED from unit 1's touched set. Rev-2 says so in §1 in as many words and the derivation holds: S3, S5 and the two extra dossier bullets are exactly what deriving produces, and §6 now carries one AC per S-item. The one thing that would falsify a carrier — unit 1 touching a construct unit 2 does not describe — was checked against unit 1's rev-2 scope and did not turn up. Unit 2's residual exposure is inherited, not its own: its S1 replacement text repeats unit 1's "no view ever hands a consumer a raw line" ceiling, which #8 falsifies, so fixing #8 in unit 1 fixes a sentence in unit 2 as well.

**The mechanism itself was not refuted, and should not be re-litigated in round 3.** One shared opener decision applied in all three views is the right shape: F1 and F2 are both fact-questions, both were decided by probes that could have produced a negative, and §4's candidate table survives every finding above. Nothing here argues for a fallback rendering or for reverting to rev-1. What the findings say is narrower and fixable: the opener decision as specified UN-HIDES structural characters, and the spec asserts the opposite in three places (§4's closing sentence, §5's precision-loss framing, §3's "S5 touches the quote branch and not the mode"). Preserve delimiter and mode balance on an unpairable line — blank the CONTENTS, keep the structure — and #1, #8 and #17 close together with one decision.

## Exit condition for round 3

A strictly falling confirmed-blocker count, from 4. Round 1 set the ceiling at 2 and round 2 raised it, so round 3 is BLOCKED from landing at anything above 3 and should be aiming at 0.

Three of the four blockers close with ONE decision — what a view hands back for an unpairable quote, stated as a conservation property rather than as a rendering choice: no view emits a delimiter, backtick or `//` that HEAD's view blanked. Write that as S3's actual contract, add the four reproductions above as ACs with their S9 arms staged RED, and #1, #8 and #17 go with it. #24 is independent and needs a decision about `LITERAL_OPENERS`, not a mechanism change: narrow the set, or add a second condition the prose case cannot meet, and parameterise the fixture over whatever set survives.

Of the highs, #2, #9, #10, #13, #25 and #26 are one to three lines each in §4, §6 and §7 and should not consume round-3 fan. #11, #15 and #18 need a measurement re-run rather than a design change. Round 3 should be primed with §4's published `checkLiteralOpen` block, the three view functions' own headers, `tools/gate-legs.json`, and this report's conservation property, and at precision 0.57 it should hold the fan where it is — the survivors this round were all re-derived against the binary, and the refuted were all readings of the prose.
