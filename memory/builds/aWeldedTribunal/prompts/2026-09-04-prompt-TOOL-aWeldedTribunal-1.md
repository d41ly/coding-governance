# The owner's prompt — verbatim

**Serves:** research TOOL-aWeldedTribunal-1 TOOL-aWeldedTribunal-2 TOOL-aWeldedTribunal-3 TOOL-aWeldedTribunal-4 TOOL-aWeldedTribunal-5 TOOL-aWeldedTribunal-6 TOOL-aWeldedTribunal-7 TOOL-aWeldedTribunal-8
**Commissions:** TOOL-aWeldedTribunal-1 TOOL-aWeldedTribunal-2 TOOL-aWeldedTribunal-3 TOOL-aWeldedTribunal-4 TOOL-aWeldedTribunal-5 TOOL-aWeldedTribunal-6 TOOL-aWeldedTribunal-7 TOOL-aWeldedTribunal-8

The value arrived on the `/unattended` invocation line as `--prompt <value>`. It carried whitespace
and named no readable file, so per the kit's own routing table it IS the prompt and is taken
verbatim. There was no prompt FILE, so no source path is recorded below.

```
Build backlog items TOOL-aLexedStripper-4, TOOL-dScaffoldedMirror-22, TOOL-dFoldedVerdict-8,
TOOL-aFlaggedScaffold-4, TOOL-aGroundedOrientation-4, TOOL-dTieredTribunal-16, TOOL-aCandidStub-1,
TOOL-dUnstalledConvoy-37, TOOL-dScrubbedConduit-2, TOOL-aScouredKit-19, TOOL-aFlaggedScaffold-3.
```

*(The line wraps here for the 100-column discipline this tree keeps; the invocation was one line.)*

## What the run derived from it, and what it did not have to ask

Eleven ids, one family, no other instruction. Every field the kickoff checker's task skeleton names
was derivable from the rows themselves plus the code each names, so the ONE owner turn this path
allows was not spent. Nothing about acceptance or gates was missing, which is the only gap that
would have disqualified a unit.

## The one reading this prompt does NOT settle, and how the run read it

"Build backlog items" over a row whose defect is already fixed has two readings: rebuild it, or
close it. The run took CLOSE, on evidence, and the evidence is per-row in the README's
Build-level rules slot. The reasoning is not a preference — a row naming a defect that source no
longer has cannot be built, because there is nothing left to change, and the owner asking for
eleven rows to be worked is asking for eleven rows to stop being open.

Four of the eleven are in that state and the run says so rather than reporting eleven units built.
