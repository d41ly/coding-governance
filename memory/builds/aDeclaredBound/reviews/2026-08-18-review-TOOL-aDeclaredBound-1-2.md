## Verdict: BLOCKED

**Serves:** spec-audit TOOL-aDeclaredBound-1 TOOL-aDeclaredBound-2 TOOL-aDeclaredBound-3 TOOL-aDeclaredBound-4 TOOL-aDeclaredBound-5

I read all five specs at rev-2/rev-3/rev-4, the build README, the round-1 review record including its
Refuted section, and the folded gate measurement under `build/`. Every BLOCKER and HIGH below was
re-verified against source in this worktree at `5ffeab5`. What I checked mechanically: the hook's
declaration reads and its `gitCommonDir` walk executed from inside this linked worktree, the ratchet's
`_scalar_at` and `_justified` executed over a three-key fixture, the govkit surface predicate executed
over the declared globs, the `check-protocol-parity.test.sh` literal and its leg guard, the
`check-verdict-epoch.sh` topological rule and its unguarded leg row, the example-conf key population
against the engine's validation loop, the `check-arms.py` signature rule, `check-playbook-parity.sh`'s
pair table run live, the read-path budget, and the unit-5 scanner pattern re-run at both `5770f4a` and
HEAD. What I did NOT check: whether each acceptance set is collectively sufficient for Tier 2 rather
than merely correct, the `parallel-coding-governance.template.md` byte budget after unit 5's edits, an
adopter-tree run of anything, and whether any spec's prose is a fluent paraphrase that is subtly wrong.
The five specs pass `check-memory-hygiene.sh` as documents; I ran it and it is silent.

| # | Sev | Spec · section | Finding | Fix |
|---|---|---|---|---|
| 1 | BLOCKER | unit 5 · S2b, S4, AC4, §4 "Why this lands BEFORE unit 4" | The ratified replacement predicate cannot be satisfied at unit 5's own landing commit. S2b requires the protocol to name a conf key and a file and requires "the named file is one the hook actually reads"; the key and the read are both minted by unit 4, which lands AFTER (README "Order"), and unit 5 §3 forbids the escape — "`tools/hooks/agent-cap.js` is untouched." `grep -n "readFileSync\|\.conf" tools/hooks/agent-cap.js` returns only stdin at 59, the gitdir walk at 591/595, a slot token at 647 and the script payload at 720. No conf read exists, so AC4's third clause ("a fixture names a conf key the hook does not read, it reds too") is satisfied by every key including the correct one, while AC4's first clause demands green. S4 one line earlier gives a different pointer target for the same document class — "points at the hook" — and nothing says which target `memory/guides/REVIEW-PROTOCOL.md` gets. The leg fires on exactly this commit: `tools/gate-legs.json` guards `review-protocol parity (kit vs dogfood)` on `memory/guides/REVIEW-PROTOCOL.md`. | Reconcile S4 and S2b on ONE target, then split the predicate by landing. Either unit 5 asserts the pointer-shape half only (the section names the file that resolves the bound, which is `tools/hooks/agent-cap.js` today) and unit 4 adds the reads-it half in the same commit that makes the hook read it, or S2/S2b/S3 move into unit 4 and unit 5 lands only digit-free prose. Say which in both specs. |
| 2 | BLOCKER | unit 5 · S9, S10, AC1, §4 "What the gate can and cannot see"; `build/2026-08-18-build-TOOL-aDeclaredBound-5-gate-measurement.md` | AC1 pins the build to "18 sites across ten files" and no stated pattern reproduces it. S9 names exactly two constraints and calls them the whole tightening; the record gives the bound-word alternation in full. Running exactly that over `git ls-files '*.md'` minus the three frozen prefixes: 31 lines across 11 files at `5770f4a`, 32 across 12 at HEAD. A third constraint, the noun list, is load-bearing for 18 and is written down in neither S9 nor the record — S10 declares a blind spot for a list S9 never introduces. It is also what AC2 needs: `skills/session-kickoff/SKILL.md:47` reads `## Step 1 — Orient (ONE batched command; report ≤5 lines)`, matches the stated pattern, and AC2 requires it silent as the green control. The record's own table does not close either: pass 2 is `55 hits, 17 true positives, 30 false positives` (17+30=47), "about 64%" is 30/47 rather than 30/55, and pass 3 reports 18 true positives against pass 2's 17 while being a strict narrowing of it in both scope and pattern. §4 says "Full working, including the classification of all 55, is in `build/…`"; the record is 58 lines and classifies nothing item by item. | Write the complete pattern into S9 — bound-word alternation AND the noun list, verbatim — and relabel the record's pass-3 row to match. Re-run at the build's own base, publish the classified hit list in the record, make true positives plus false positives equal the hit count in every row, and restate AC1 as "reproduces the recorded list" rather than a bare integer. |
| 3 | HIGH | unit 4 · S8, S4b, §4 "Files touched" | The ratchet cannot enforce the keyed attribution S4 requires, so one line launders all three raises. `drift_report.py:162-165` builds the matcher from the two NUMBERS only — `re.compile(r"\b" + str(old) + r"\b\s*(?:->|→|to)\s*\b" + str(new) + r"\b")` — with no key. Executed over a fixture carrying all three keys at 8 and one comment naming `LENS_CAP RAISED 5 -> 8`: `CONCURRENCY_CAP justified(5->8)= True`, `VERIFIER_CAP justified(5->8)= True`, `LENS_CAP justified(5->8)= True`. S4's stated reason for requiring the key — "all three bounds default to 5 and an unkeyed line would attribute a raise of one to a raise of another" — is exactly the ratchet's defect, and S4b assigns arithmetic to the ratchet. `tools/drift-audit/drift_report.py` is absent from §4 Files touched, so nothing in the build can repair it. | Add `drift_report.py` to unit 4's write set and make `_justified` require the key token on the matched line. Add an arm where one key's justification does not excuse another key's raise. If the key-aware form is out of scope, delete S4's sentence claiming the ratchet distinguishes them and say plainly that the ratchet only sees "some justified move happened in this file". |
| 4 | HIGH | unit 4 · S4 vs AC4 | The acceptance criterion tests the exact line S4 forbids. S4 states the grammar as a keyed comment and says "the key must appear"; AC4 reads "When the same fixture adds `# RAISED 5 -> 8 (owner, …)` above the key, the eight-lens script is allowed" — no key. A builder who implements S4 fails AC4; one who makes AC4 pass ships the unkeyed acceptance rev-2 wrote S4 to remove. This is round-1 finding 6's fix applied to the scope item and not carried to its criterion. | Rewrite AC4 with `LENS_CAP` in the attribution line and a concrete date. Add the paired negative: the same line without the key, or carrying `VERIFIER_CAP` instead, leaves the lens raise unapplied at 5. |
| 5 | HIGH | unit 4 · S4; unit 3 · S1 | S4 names "the window must be stated" as one of the three defects it is fixing and then does not state it. `grep -nE "[0-9]+ lines\|window" spec-…-4.md` returns one hit, line 37, which is the sentence naming the gap. There are now two windows that must agree or be declared independent: the hook's, and the ratchet's `_RATCHET_LOOKBACK` at `drift_report.py:137`, consumed by `_justified` at line 166 — which unit 3 of this same build makes adopter-declarable. `grep -n LOOKBACK` over unit 4 returns nothing, and unit 3 names unit 4 only in its §10 boilerplate. An adopter who declares a narrow lookback gets attribution lines the hook accepts and the ratchet reds on. | State the hook's window as an integer in S4, and state in the same sentence whether it is independent of `RATCHET_LOOKBACK` or reads the same declaration. If independent, one of the two specs owes a sentence naming the other; if coupled, unit 4 gains a dependency on unit 3 that the README's Order section currently denies. |
| 6 | HIGH | unit 4 · S2 | The resolver S2 mandates reusing cannot produce what S2 requires. S2 promises three things at once: reuse `gitCommonDir`, "This unit does not write a second one", and read "from the REPO ROOT and not the common dir, so every linked worktree can carry its own". `gitCommonDir` (`agent-cap.js:577-608`) deliberately resolves a worktree through `commondir` to the shared dir — its own comment says "One budget per repo, not per worktree". Executed from this worktree with the function lifted verbatim: `common = C:\projects\coding-governance\.git`, `dirname = C:\projects\coding-governance`, against `git rev-parse --show-toplevel` = `…/.claude/worktrees/read-path-ceiling-adjustable-84d43b`. A builder obeying S2 reads the MAIN checkout's declaration in every linked worktree, silently. | Name the second return as a scope item: a walk that returns the directory holding the `.git` entry, beside `gitCommonDir` rather than replacing it, and state explicitly that `gitCommonDir` keeps its worktree-collapsing behaviour because the slot budget depends on it. Or drop the per-worktree claim and read from the common dir's parent. Either way S2 stops saying "does not write a second one" unqualified. |
| 7 | HIGH | unit 4 · S1b, AC1, AC2, AC3, AC4, AC5, AC10 | No fixture isolation exists on the Workflow path, and S1b closes the door AC1 needs. S2 anchors the walk on "the same payload cwd the existing code anchors on, which is what the hook's own harness relies on for test isolation" — true only of the Agent modality: `agent-cap.js:637` reads `data.cwd \|\| process.cwd()` and `apay()` at `agent-cap.test.sh:369-372` supplies `cwd` pointing at a scratch tree with a `.git` directory. Measured on the suite: `grep -c 'tool_name":"Workflow'` gives 13, `grep -c 'tool_name":"Workflow"[^}]*cwd'` gives 0, and the suite never `cd`s before `node "$HOOK"`. So every Workflow arm — the modality where `MAX_LENSES` and `MAX_VERIFIERS` are consumed — resolves against `process.cwd()`, which on the bar is the repo root where S1b commits `.agent-cap.conf`. AC1's premise "When no `.agent-cap.conf` exists" is unreachable, and AC2/AC3/AC4/AC5/AC10 say "a fixture declares …" with no stated mechanism. | Add a scope item naming the mechanism: every Workflow payload in the affected arms gains a `cwd` pointing at a scratch tree containing a `.git` entry, on the idiom already at `agent-cap.test.sh:363-372`, or those arms run from inside the scratch tree. State it in S2 beside the payload-cwd sentence, since S2 currently asserts the isolation already exists. |
| 8 | HIGH | unit 5 · S5 and §4 inventory; unit 4 · S12 | The "shipped default may keep the number" exemption is misapplied to the playbook, which states the ENFORCED rule. `parallel-coding-governance.template.md:24` reads "Never more than 5 agents at once, AND never more than 5 per verify stage"; line 188 reads "the ≤5 cap is enforced at the `Workflow` tool-call AND at the `Agent` one"; line 181 carries "at most 5 verify agents TOTAL" and "denies any `K` it cannot resolve to an integer ≤5". None states what the kit ships, so S5's rationale — "changes only with a kit release" — is false for them, and the inventory row says "two sites" where the pattern finds three. The protection S5 names is gov-local: `check-playbook-parity.sh:106` compares the template against `^const MAX_LENSES = ([0-9]+)` in this repo's own hook, runs in no adopter tree, and `bash tools/check-playbook-parity.sh` prints "pairs in agreement" today. Unit 4 S12 then keeps that literal alive expressly so the extraction resolves while the deciding value moves to the declaration — which is the shape `memory/map/features/agent-cap.md:56-61` records as "a constant to the guard, a knob to the runtime". | Reclassify template lines 24 and 188, and 181's enforcement clauses, as S4 carriers that point at the declaration. Keep S5 only for a sentence literally phrased as the shipped default, and have the pair's owning extraction read that constant with an in-file comment saying it is the default and not the enforced bound. State in S5 that the declared-pair guarantee is gov-local and buys an adopter nothing. |
| 9 | HIGH | README · Order; unit 1 · S9; unit 2 · S7 | The independence claim is false four ways and the order the specs defer to is never named. "Units 1, 2 and 3 are independent of each other and of 4/5 — three different files, three different kits" is followed two sentences later by "Units 1 and 2 touch the same engine" (round-1 finding 26, unfolded). Measured against the specs' own Files touched: units 3 and 4 both write `tools/drift-audit/drift_signals.py` (unit 3 line 83, unit 4 line 165) and both need arms in `tools/drift-audit/selftest.py` (unit 3 S5; unit 4 AC7, which is not in unit 4's write set); units 3 and 5 both write `tools/drift-audit/README.md` (unit 3 line 86; unit 5 §4 inventory row); and units 1, 2 and 3 span two kits, not three. Separately, unit 1 S9 and unit 2 S7 carry the same sentence — "the LATER of the two carries the single bump and the earlier carries none" — and nothing anywhere names which is later, while both §7 sections list `check-verdict-epoch.sh`, whose leg is unguarded in `tools/gate-legs.json` and whose rule is topological over `merge-base..HEAD`. The earlier unit reds that leg from its own commit until the other lands. | Rewrite the paragraph from the measured write sets. Name the order in both specs on `aLoosenedCeiling-2` S8's wording, say which commit carries `KIT_MEMORY_TREE_VERSION`, and add one clause to the earlier unit's §7 saying the verdict-epoch leg is satisfied at the pair's later commit. Say which unit's edit to `drift_signals.py` lands first. |
| 10 | MEDIUM | unit 4 · S1 | The conf grammar is still unwritten and the ratchet reads exactly one shape. Round-1 finding 24 asked for it; rev-3's log does not mention it and S1 still names only three key spellings. `_scalar_at` (`drift_report.py:148-151`) matches `^\s*KEY\s*=\s*"?(\d+)"?\s*$` and skips comment lines. Executed: `_scalar_at("LENS_CAP=8  # raised for the audit sweep", "LENS_CAP")` returns `(None, None)`, which makes `ratchet_findings` `continue` at line 189 and emit nothing. A trailing comment on the key line silently disarms S8, which is unit 4's only left-shift for an unattributed raise. | State in S1: one bare key-equals-integer per line with nothing else on the line, attribution on its own comment line above. Add an arm for the trailing-comment shape so the blind spot reds rather than passing. |
| 11 | MEDIUM | unit 4 · S13, AC11 | The govkit half is green today and after, because a repo-root dotfile is outside the declared surface. `tools/govkit/registry.toml`'s `[surface] globs` are `tools/*`, `.githooks/**`, `skills/session-kickoff/**` and the two playbook root patterns. Executing `surface_paths` over them: 48 paths, and the only root-level members are the three `parallel-coding-governance.*` files. `python tools/govkit/govkit.py selfcheck` prints "0 unclaimed" and exits 0. So S13's "A new shipped file that is none of those reds `govkit selfcheck`" is false for the ratified root location, and AC11's second clause passes whether or not the declaration is made. `agent-cap` is already an entry (`registry.toml:82`), and `tools/hooks/kit.toml` has `home = "tools/hooks"` with per-file `to` destinations that already reach outside the home (`.claude/hooks/agent-cap.js`). | Ship the example from `tools/hooks/.agent-cap.conf.example` with an unprefixed `to`, which the per-file-claim arm does cover, and restate AC11 on that arm rather than on `selfcheck`'s exit code. If the source stays at the root, add a surface glob as its own scope item and say so. Cite `memory/guides/SESSION-KICKOFF.md:175` — "A new tool at the REPO ROOT rather than under `tools/` silently leaves the enforced surface" — as the objection §4's prefix-independence argument has to beat. |
| 12 | MEDIUM | unit 4 · §4 Files touched, AC11 | The unit's central artifact is missing from its own write set, and one listed file does not exist. S1b and Rollout both commit `.agent-cap.conf`; Files touched lists only the `.example`. Files touched also names "the kit's README" — `ls tools/hooks/` returns `agent-cap.js`, `agent-cap.test.sh`, `kit.toml` and nothing else, so that is a third NEW file needing its own `kit.toml` rule, while S13 and AC11 both say "the two new files". `tools/drift-audit/selftest.py` is absent too, though AC7 observes it. | Add `.agent-cap.conf` at 5/5/5, name the README as new, add `selftest.py`, and make S13 and AC11 count three files. Say whether the per-repo declaration is kit payload at all — the example usually is and the instance usually is not. |
| 13 | MEDIUM | unit 4 · §7 | Unit 4 lands one commit after unit 5 puts a cap-restatement scanner on the bar, then writes two new sections about the cap into `memory/guides/REVIEW-PROTOCOL.md` (S9, S10) — live markdown squarely in the scanner's population — and §7's thirteen gates do not include it. `grep -n "check-agent-cap-restatement" memory/builds/aDeclaredBound/spec/*.md` hits only unit 5. Round-1 finding 27 asked for this and rev-3's log does not mention it. S9's markdown-only decision does dispose of the `.agent-cap.conf.example` half of that finding, which is worth saying rather than leaving inferred. | Add `bash tools/check-agent-cap-restatement.sh` and its self-test to unit 4 §7, and state in S9/S10 that the new prose points at the declaration rather than restating a number, or name the waiver row it needs. |
| 14 | MEDIUM | unit 4 · §4 "What this buys, and what it does not" | The spec attributes to the unattended protocol §9 a control §9 does not name and, in the same paragraph, forbids documents from implying. Unit 4 says "The control that actually binds is the same one §9 names — review of the diff, by a human, on the remote." `memory/guides/UNATTENDED-PROTOCOL.md:350-355` says the opposite: "The same leg, re-run in a clone the run never touched by a party the run cannot execute code as … and no document here may imply otherwise." Human diff review appears nowhere in §9, and unit 5 §4 separately rejects relying on review. Round-1 finding 22 filed this; `git diff 1d45f8c 5770f4a` on this spec touches no line matching it. S10 would commit the misstatement into the BINDING review protocol. | Quote §9's actual sentence and name the control that applies here. With finding 3 fixed that is the ratchet leg re-run where the run cannot reach; without it, say there is none. |
| 15 | MEDIUM | unit 5 · §4 inventory and its count | "The 18 land in the ten carriers this section's table names" cannot be true as counted. The table's tenth row is `memory/gotchas/concurrency-is-not-a-budget.md`, a frozen tree pass 3 excludes by S6, and it carries no bound-word site at all — its 5s are `boundedParallel(thunks, 5)`, `chunk(items, 5)` and `const MAX_VERIFIERS = 5`. The `tools/drift-audit/SKILL.template.md` row names two files, so ten rows are eleven carriers. Two live files that match the stated pattern are absent from the table: `skills/session-kickoff/SKILL.md:47`, which AC2 needs silent, and `memory/backlog/TOOL.md:103`, whose backlog row quotes `≤5 concurrent` and was added by the fold commit itself — `git log --oneline -S 'TOOL-aDeclaredBound-6' -- memory/backlog/TOOL.md` returns `75a664f`, while the measurement ran at `5770f4a`. `memory/backlog/` is not one of S6's three prefixes. | Drop the gotchas row from the count and keep it as a documented S6 exclusion. Re-measure at the build's real base. Give backlog rows and decision rows a stated disposition — they quote carriers rather than assert rules, so a fourth excluded prefix stated as "append-only records of past state" is likelier correct than a waiver row, and AC3's stale-prefix arm then covers it. |
| 16 | MEDIUM | unit 5 · S2b vs §3 | S2b is singular where the protocol and unit 4 are both plural. It speaks of "the section that states the fan-out rule" and "the conf KEY"; the protocol has two rule sections — `memory/guides/REVIEW-PROTOCOL.md:7` "The hard cap — ≤5 verify-stage agents TOTAL" and `:108` "Concurrency — ≤5 agents at once, always" — and unit 4 S1 mints two matching keys, with unit 5 §3 insisting they stay two because `memory/gotchas/concurrency-is-not-a-budget.md` exists from conflating them. One predicate over "the section" cannot bind both. Round-1 finding 25 filed the singular; rev-3 rewrote S2 and left it. | Make S2b per-rule: each section that states a bound names its own key. AC4's negative cases then need one instance per rule. |
| 17 | MEDIUM | unit 5 · S2 | S2 says the protocol "loses the digit" and gives no disposition for the sites where the digit is executable. `memory/guides/REVIEW-PROTOCOL.md:109` instructs an agent to inline `boundedParallel(thunks, 5)` and `boundedPipeline(items, 5, …stages)`; those are the literal code the hook then resolves at the call site. The S9 scanner will not flag them, because no bound word is adjacent, so the gate cannot arbitrate what S2's prose demands. Round-1 finding 25's second half; rev-3/rev-4 folded the first half only. | Give S2 an explicit keep-with-reason disposition for the two code-shaped sites, and say whether they need a waiver row or are simply outside the pattern. |
| 18 | MEDIUM | unit 5 · §4 "Why this lands BEFORE unit 4" vs unit 4 · S7 | The two specs disagree about whether unit 5 leaves anything false. Unit 5 says "Strip-then-adjust keeps every carrier true throughout"; unit 4 S7 says "after unit 5 the protocol no longer states it" and scopes the repair. Unit 4 is right: `tools/hooks/agent-cap.js:693-698` tells an operator "change it in tools/hooks/agent-cap.js and in memory/guides/REVIEW-PROTOCOL.md, where the rule is stated." That string is live today, is invisible to a markdown-only gate, and unit 5 §3 forbids touching the file. The interval between the two units ships a refusal that misdirects the operator. | Either unit 5 fixes that string itself, narrowing §3 to "its constants are untouched", or unit 5's ordering paragraph says plainly that one non-markdown carrier is left stale until unit 4 and names it. |
| 19 | MEDIUM | unit 2 · §3 | S7 is filed under Non-goals (OUT). The section header `## 3. Non-goals (OUT)` is line 43 and S7 is line 49, below three genuine non-goal bullets. S7 is the fold's answer to round-1 finding 10, an in-scope obligation, and unit 1 put the identical item at S9 inside §2. A builder reading §3 as authoritative drops it, which reproduces the defect the fold was closing. | Move S7 into §2 after S6 and leave §3's three bullets alone. |
| 20 | MEDIUM | unit 1 · S7; unit 2 · S4 | Both units edit `tools/memory-tree/.memory-tree.conf.example` and neither has an acceptance criterion observing it. Round-1 finding 23 filed this and neither rev-2 log mentions it. It is not hypothetical: the engine's cap loop at `check-memory-hygiene.sh:48` names eight keys, the example declares `ROW_DOC_CAP_BYTES` which `grep -c` shows the engine never reads, omits `DOSSIER_CAP_LINES` entirely, and the hand-kept assertion loop at `check-memory-hygiene.test.sh:1234` covers seven keys and neither `DOSSIER_CAP_*`. Two more keys added to that loop by hand is the same failure a third time. | One acceptance criterion per unit greping the example for the declared keys. Derive the test loop's key list from the engine's own `for _k in …` list rather than restating it, which drains the existing gap in the same commit. |
| 21 | MEDIUM | unit 3 · §4 Alternatives rejected | Per-ratchet windows are rejected on a premise the corpus contradicts. §4 says "nothing in this corpus has two pins whose justifications compete for one window". Measured: `.memory-tree.conf:54` is `ORPHAN_ID_PIN="0"` and `.memory-tree.conf:57` is `DEAD_PATH_PIN="0"` — three lines apart, identical values, both declared `weakens: "up"` at `drift_signals.py:240-241`. A justification for either sits inside the other's fourteen-line window. Unit 4 then makes the competing-window case the default shape with three keys at one value in one small file. | Delete the sentence or replace it with the measured pair, which argues the other way. The rejection can still stand on scope; it cannot stand on that premise. |
| 22 | MEDIUM | `build/2026-08-18-build-TOOL-aDeclaredBound-5-gate-measurement.md` · "Markdown only" | The record misattributes a false-positive class to the tightening it is offered as evidence for. It lists "the node registry row naming the machine `agent-0`" among "the remaining fifteen false positives … all code" removed by scanning markdown only. `git grep -ln "agent-0"` returns `AGENTS.md`, `memory/archive/ledger/README.md`, `memory/archive/ledger/c.md` and this build's own record. The two ledger files were already excluded at pass 2 by S6, and the surviving carrier is `AGENTS.md:63`, which is markdown and stays in the pass-3 population. Markdown-only cannot have removed it. This is the only itemised evidence offered for the decision S9 calls measured rather than guessed. | Re-derive the fifteen and correct the class list, or delete the enumeration and rest the decision on the classes that survive. |
| 23 | LOW | unit 1 · S4 and §4 Inventory | The replacement message is not named and the inventory is short. S4 says only "the message becomes cap-free"; the constraints are non-obvious and mechanical — `check-arms.py`'s `signature()` (lines 104-113) drops interpolations and takes the longest surviving literal run, and `branches()` raises `Problem` when that run is under twelve characters (line 153). The arm it must match is a literal-text `hit` at `check-memory-hygiene.test.sh:640` against `index entry lines over 300 chars` at `check-memory-hygiene.sh:525`. §4's "Three sites hold the number today" is right about the applying sites (497, 498, 525) and short as an inventory: comments at 393, 428 and 468 restate 300 or 350 and become wrong for a declaring adopter, which is the argument S4 uses to justify rewording the message. Line 496's "between 300 and 331" is a corpus measurement and correctly stays. Round-1 finding 33, unfolded. | Name the replacement string in S4 and state the twelve-character longest-literal-run constraint. Say "three applying sites plus three in-file comments" in §4 and put the comments in Files touched. |
| 24 | LOW | unit 4 · §4 Alternatives rejected | Two ratified rows that extend the agent-cap argument past the channel are still uncited. `grep -n "cBriefedPilot\|aBoundedVerdict\|CORE_FLOOR"` over the spec returns nothing. `memory/DECISIONS.md:52` refuses a conf key because "A key lets a project declare zero directives, which is a global waiver carrying no name, no reason and no record"; `memory/DECISIONS.md:63` keeps a second cap as a driver file constant on the reused agent-cap argument, and it is the cap governing this review. The shape they imply is already built at `UNATTENDED-PROTOCOL.md:362-367` — kit-owned, project may only EXTEND. Round-1 finding 21, unfolded. | Add both rows by name and add the kit-owned extend-only shape as a fourth rejected alternative, with the reason that actually defeats it: it forecloses the raise the owner asked for. Note in the same breath that it needs no attribution grammar, no ratchet rows and no second parser, so the reader can price what the chosen design costs. |
| 25 | LOW | unit 4 · §4 Alternatives, §5 perf | Round-1 finding 32 is unfolded and the two sections now contradict each other. §4 says "the hook runs on every tool call"; §5 says "one small file read per `Workflow`/`Agent` tool call". `.claude/settings.json:5` is `"matcher": "Workflow|Agent"`, so §5 is right. §5's "cached within a single hook invocation" buys nothing either: the command is `node "${CLAUDE_PROJECT_DIR}/.claude/hooks/agent-cap.js"`, a fresh process per call, and the invocation reads once regardless. | Correct §4's sentence to the matcher and delete the caching clause. |
| 26 | LOW | unit 4 · S4b | S4b fixes the old value precisely — the hook accepts any old value in a correctly keyed and shaped line, the ratchet enforces arithmetic — and says nothing about the new one. If the hook does not require the new value to equal what it is applying, an attribution line left from a previous raise authorises a further move for the whole session, and only the bar catches it, only under `--check`, only with a readable base blob. | One sentence in S4b: the hook requires the stated new value to equal the declared value it is applying. Add an arm for the stale-attribution case. |
| 27 | LOW | unit 4 · §6 | The acceptance criteria run AC1 through AC7, then AC10, AC11, AC8, AC9. AC10 and AC11 were appended by rev-2 and inserted mid-list. | Renumber, or move the two appended criteria to the end. |
| 28 | LOW | unit 5 · AC5 | Green before a line is written. `bash tools/check-playbook-parity.sh` prints "pairs in agreement" today, the `MAX_LENSES` pair already exists at `check-playbook-parity.sh:106` and already resolves, and the anti-vacuity behaviour AC5's second clause asserts is already the gate's (line 129). Same class as round-1 findings 17 and 30. | State AC5 differentially: the pair S5 ADDS resolves, and deleting its source line reds it. |

## Refuted

- **Unit 3 S4/AC3's named refusal is unreachable from where S1 puts the read.** One lens argued that
  `load_project_layer`'s `DriftError` handler wraps only three calls at `drift_report.py:833-838`,
  while the value's consumers sit sixty-five lines later inside `if args.check:`, so a builder gets a
  traceback. Dropped. S1b already says the value is "resolved once at the layer-load site and threaded
  down", and that site is inside the try block. The spec constrains the placement adequately; the
  residual ambiguity is not worth a finding at this round's cost.
- **Unit 4 AC9's delegation premise is broken because the gate sends no `cwd`.** Dropped.
  `tools/workflows/check-verifier-fanout.sh:21` runs `cd "$ROOT"` before building the payload, so the
  hook's `process.cwd()` fallback lands on the repo root, which is where S1b commits the declaration.
  AC9 holds as written. The narrower point — that the file is absent from unit 4's Files touched —
  did not survive either, because the gate needs no edit.
- **Unit 4 S12 alone reintroduces the recorded "constant to the guard, knob to the runtime" class.**
  Not filed on its own. `memory/map/features/agent-cap.md:56-61` records that class about a guard that
  read a constant while harnesses bound their own caps from a fallback expression. S12 keeps the
  constant as the SHIPPED DEFAULT, which is coherent with S5. The defect only materialises through
  S5's misclassification of the playbook, and that is finding 8.
- **The drift-audit tier rows are inconsistently classified, so "zero false positives" is not
  established.** `tools/drift-audit/README.md:136` and `tools/drift-audit/SKILL.template.md:57` state
  a tier's own cost as `≤5 agents`, in the same column as the `~22 agents` and `0 agents` the record
  calls false positives, and neither becomes wrong if the cap moves. I could not confirm whether those
  rows are among the 18, because the 18 are not reproducible from anything stated. Folded into finding
  2's evidence rather than filed as its own defect.
- **Unit 3 §3's "No change to which keys are ratcheted" contradicts unit 4 S8.** Not a defect. It is a
  non-goal about unit 3's own change, in the same house pattern as unit 1 §3 and unit 4 §3. The
  file-sharing between the two units is real and is finding 9.
- **Unit 5 §4 Files touched omits the govkit registry rows a new leg needs.** Round-1 finding 19's
  remainder. Dropped: §4 now names "the gate-leg descriptor rows a new leg needs" and §7 lists both
  govkit legs, which is enough for a builder.
- Nothing above re-litigates the two ratified forks, and nothing re-files an item the round-1 record
  refuted: the lookback margin, the three lens carrier totals, the protocol line list, the
  byte-identical-copies arm, the install-prefix dossier, unit 4 §3's "No value changes", or unit 2's
  already-conf-settable premise.

## What no lens checked

Four gaps. First, **whether each acceptance set closes its unit's scope** — every lens graded criteria
one at a time and none asked whether a unit's set is sufficient for Tier 2, which is the same gap
round 1 declared and nobody has since closed. Second, **the playbook's byte budget after unit 5's
edits**: unit 5 rewrites three sites in `parallel-coding-governance.template.md`, the file is gated at
48 KiB against a recorded high-water, and nobody computed the delta. The read-path side IS measured
and is not a risk — `corpus_ids.py --report` prints `read path : 6 files, 92184 B` against
`READ_PATH_CEILING="112987"`, so unit 4's two new protocol sections have about 20 KiB of headroom.
Third, **any adopter-tree behaviour**: every claim here was checked in gov's own tree, and the two
places the design ships outward — the scanner's noun list and the playbook's cap sentences — are
exactly where gov's tree cannot answer for an adopter. Fourth, **whether unit 5's rebuilt parity
predicate is writable at all** once finding 1 is resolved: no lens attempted the predicate, only its
ordering, so the cost that displaced the cheaper option is still an estimate.
