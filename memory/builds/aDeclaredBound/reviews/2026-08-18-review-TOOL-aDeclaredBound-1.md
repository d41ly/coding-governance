## Verdict: BLOCKED

**Serves:** spec-audit TOOL-aDeclaredBound-1 TOOL-aDeclaredBound-2 TOOL-aDeclaredBound-3 TOOL-aDeclaredBound-4 TOOL-aDeclaredBound-5

I re-read all five specs, the build README, `memory/TEMPLATE-SPEC.md`, `memory/guides/BUILD-METHOD.md`
M2 through M4, and the prior build's specs where a claim cited them. Every BLOCKER and HIGH below was
re-verified against source in this worktree at `1d45f8c` (the specs' base `497d25d0` is its parent and
neither engine file moved between them). What I checked mechanically: the two gate literals the build
would break, the ratchet's control flow, the eight cap keys and their validation loop, the conf source
ordering in the hygiene engine, the `gitCommonDir` walk and the environment refusal string, the
lookback window arithmetic against the real `.memory-tree.conf` blob, the govkit surface predicate, and
the carrier population for the number 5. What I did NOT check: whether any spec's prose is a fluent
paraphrase that is subtly wrong, whether the acceptance criteria are collectively sufficient for the
tier, the review-protocol and template byte budgets after the proposed edits, and whether unit 5's gate
is buildable at acceptable false-positive cost, which no lens attempted and which is the largest open
risk after the two blockers.

| # | Sev | Spec · section | Finding | Fix |
|---|---|---|---|---|
| 1 | BLOCKER | unit 5 · S2, §4, §7 | Stripping the number from `memory/guides/REVIEW-PROTOCOL.md` reds a bar leg. `check-protocol-parity.test.sh:70` runs `grep -qF '≤5 verify-stage agents TOTAL'` on that file; the file carries it at line 7. Leg row at `gate-legs.json:315`. §7 omits the leg; §4 omits `tools/workflows/REVIEW-PROTOCOL.template.md`, which parity diffs against. | Decide the assertion's fate as a scope item before touching either document. Add the template as a paired row and the leg to §7. Any digit-free replacement needs a new anti-vacuity predicate, because the leg's comment at lines 66-69 argues the digit-free phrase is the failure mode. |
| 2 | BLOCKER | unit 4 · S8, §4, AC7 | The ratchet cannot fire for `.agent-cap.conf`. `drift_report.py:179-184` skips a file absent at the base ref, so the first raise is never compared; §4 Rollout says gov declares nothing, so `head_txt` is empty and `_scalar_at` returns `None` forever. Liveness does not cover it: `drift_report.py:909` computes `dead` over `out` only, never over `ratchets`. §4's "the drift ratchet reds if the justification is missing" is false as designed. | Either commit `.agent-cap.conf` at the shipped values so a base blob exists, or add a ratchet branch for absent-at-base and present-at-head above the shipped constant. Say which, extend Files touched to `drift_report.py`, and give the three ratchet rows a liveness statement. |
| 3 | HIGH | unit 4 · S1, S11, §7 | Restructuring `MAX_LENSES` breaks an existing declared pair through its anti-vacuity arm. `check-playbook-parity.sh:106` extracts `^const MAX_LENSES = ([0-9]+)` from `agent-cap.js:119`. Any call-shaped assignment empties the extraction and reds with "an extraction matched NOTHING". `bash tools/check-playbook-parity.sh` is green today. §7 omits the gate. | Keep a line-anchored bare literal for the shipped default and resolve from it, or move the pair's extraction in the same commit. Add the gate to §7 and the constraint to Files touched. |
| 4 | HIGH | unit 5 · §4 inventory | The measured table is wrong in both directions. `skills/session-kickoff/SKILL.md` carries no cap site at all: a grep for agent-cap, fan-out, verifier, concurrency and lens returns nothing, and its only `5` is line 47's "report ≤5 lines", which is AC2's own green control. Omitted live carriers: `WIRE-INTO-PROJECT.md`, `tools/workflows/REVIEW-PROTOCOL.template.md`, `tools/drift-audit/SKILL.template.md`, `tools/drift-audit/README.md`. `memory/guides/REVIEW-PROTOCOL.md` is listed at two sites and carries at least four in cap context. | Delete the session-kickoff row and the README's matching claim, name that line as AC2's control, add the four carriers with dispositions, and add `adopt-drift-audit.sh --check` to §7. Drop the "eight carriers, twelve sites" total, since S1 already re-measures. |
| 5 | HIGH | unit 2 · S1 vs S2 vs S6 | One statement cannot satisfy both halves. The three siblings are bare assignments at `check-memory-hygiene.sh:24-26`, ABOVE the conf source at line 41, which is what makes them env-immune. `SPEC10_CUTOFF` sits at line 664 as `${SPEC10_CUTOFF:-2026-08-04}`. Retiring the env channel needs an unconditional assignment above line 41; blank-resolves-forward needs a conditional re-default after it. §10 says the unit "extends no seam", which is wrong. | Spell both statements in S1: an unconditional default above the source, plus a post-source fallback with the literal held once. Correct §10. |
| 6 | HIGH | unit 4 · S4 | "One written sentence satisfies both the hook and the ratchet" is false after the first raise, and the grammar never names its key. The ratchet's old value comes from the base blob (`drift_report.py:182`); the hook can only know the shipped constant. After a declared 8, a move to 10 needs 8 to 10 for the ratchet and 5 to 10 for the hook. All three keys default to 5, so nothing binds an attribution line to one key. The hook's search window is unspecified. | Define the old value as the previous declared value, require the key in the attribution line, state the hook's window explicitly, and say which side owns the regex. Add an acceptance criterion for the second raise. |
| 7 | HIGH | unit 4 · S2, §10 | The resolver already exists in the file being edited, and S2 picks a different anchor without saying so. `agent-cap.js:577-606` walks up bounded by a `.git` entry, handles the worktree case where `.git` is a file, and resolves through to the COMMON dir. It anchors on the payload cwd at line 637; `agent-cap.test.sh:363-366` records that choice as the test isolation mechanism. S2's own-location anchor makes the conf-fixture criteria unreachable by the existing harness. §10 says there is no seam for this shape. | Cite `gitCommonDir` in S2 and §10, state which directory the declaration is read from in a worktree (the repo root, not the common dir), and either reuse the payload-cwd anchor or add a scope item for the scratch-repo harness the criteria then need. |
| 8 | HIGH | unit 4 · S7, AC6 | S7 freezes a refusal message whose two load-bearing sentences this build makes false. `agent-cap.js:693-698` reads "The cap is the file constant" and "change it in tools/hooks/agent-cap.js and in memory/guides/REVIEW-PROTOCOL.md, where the rule is stated". After unit 4 the cap is not the constant when a declaration applies; after unit 5 the protocol no longer states it. S7 scopes only the comment. | Extend S7 to the message body: keep the refusal, redirect the remedy at `.agent-cap.conf`. Restate AC6 to observe the new remedy string. The existing arm keys on the prefix at `agent-cap.test.sh:343` and survives. |
| 9 | HIGH | unit 5 · S4 vs S6 | Two scope items specify incompatible waiver keys for one registry. S4 waives frozen records by path; S6 forbids any key but the matched text. The texts are not unique: "at most 5 agents TOTAL" appears in `AGENTS.md`, `README.md`, `WIRE-INTO-PROJECT.md`, `memory/map/features/agent-cap.md` and a frozen build record. A text-keyed waiver written for a frozen record silences the live carriers too. | State two mechanisms: a path-prefix exclusion set for frozen trees, and a text-keyed shrink-only registry for live exceptions that S6's rule binds. Or key rows on path plus text. The stale-waiver arm then needs both halves. |
| 10 | HIGH | units 1 and 2 · §4, §7 | The kit-version ordering lives only in the build README and in neither spec, which is the finding `TOOL-aLoosenedCeiling-1` folded at rev-3. `check-verdict-epoch.sh:16-19` states a topological rule. Both specs list the leg as green for themselves and neither says which lands later or which commit carries the bump. `aLoosenedCeiling-1` S6 carries that ordering in the spec text. | Give units 1 and 2 each a scope item naming the order and the commit that carries `KIT_MEMORY_TREE_VERSION`, on `aLoosenedCeiling-1` S6's wording. |
| 11 | HIGH | unit 5 · AC6 | AC6 names the two gates this repo measured as NOT firing, and cites the line recording that correction as its authority. `memory/guides/SESSION-KICKOFF.md:177-180` reads: map freshness, the kickoff ratchet, govkit selfcheck and selftest fired; "The coverage assert and handkept signal did NOT fire; this line claimed both and neither govkit gate." AC6 asserts a dossier claim and the handkept count. | Replace AC6 with observations on the manifest ratchet, the map freshness byte-compare, `govkit selfcheck` and `govkit selftest`. Add a scope item for the gate-leg descriptor rows. |
| 12 | HIGH | unit 4 · §4 "Why a file at the repo root" | The stated justification is false and the precedent it invokes went the other way. Root confs are four: `.memory-tree.conf`, `.codebase-map.conf`, `.lexicon.conf`, `.unattended.conf`. Kit-homed declarations are eight, including `tools/template-size-limits.txt`, created by `ddfa802` for `TOOL-aDeclaredCeiling-1` beside its gate rather than at the root. Unit 3 of this build argues the same way for its own value. `tools/hooks/` has no README, and `tools/hooks/kit.toml` has `home = "tools/hooks"` with per-file destinations, so a root example file has no expressible rule. | Either move the declaration beside the hook, matching `TOOL-aDeclaredCeiling-1` and deployable as written, or keep the root and correct the sentence, add the two new files to `tools/hooks/kit.toml`, add a govkit registry entry, and add `python tools/govkit/govkit.py selfcheck` to §7. |
| 13 | HIGH | unit 4 · S6, AC3, AC5 | Reporting fires only on denial, so an ignored declaration is invisible in the common case. `agent-cap.js` exits 0 silently on every approval path (lines 707, 731, 769). A malformed or unattributed raise usually leaves the fan-out under the shipped 5, which is allowed. AC3 and AC5 test denials only. | Report on the resolution rather than the verdict: emit on stderr whenever a declaration is present and not applied, allow or deny. Assert that on an ALLOWED call. Or state plainly that reporting is denial-only and is an audit aid. |
| 14 | MEDIUM | unit 1 · S1, AC3 | The key arithmetic is wrong. `check-memory-hygiene.sh:48` loops over eight keys, not six: `INDEX`, `GUIDE`, `BUILD_README` and `DOSSIER`, each with `_BYTES` and `_LINES`. `DOSSIER_CAP_*` landed with `TOOL-aRelaxedShard-1`, not in the previous build. | Six becomes eight, eight becomes ten, in S1 and AC3. Qualify §3's "landed in the previous build". |
| 15 | MEDIUM | unit 1 · S3, AC3 | Joining the existing loop does not buy the zero rejection AC3 demands. The zero arm at `check-memory-hygiene.sh:55-57` selects on `*_BYTES`; `ENTRY_CAP_CHARS` and `BUILD_README_ENTRY_CAP_CHARS` match no arm, so a declared 0 validates clean and then reds every measured line. The abort at line 59 is an `echo`, not a `fail` branch, so `check-arms.py` cannot catch a stale message. | S3 states the case pattern widens to cover the char keys with its own message. Decide the abort banner, which names check 6, and move the hand-written arm at `check-memory-hygiene.test.sh:1218` in the same commit. AC3 asserts the message text, not exit 2 alone. |
| 16 | MEDIUM | unit 3 · §4 | The motivating measurement is wrong, and it is the whole argument for the unit. The claim is that `TOOL-aLoosenedCeiling-3` came within two lines of pushing the literal out of the window. Measured at every commit carrying it: the justification is `.memory-tree.conf:107`, the key is line 113, distance 6, window 14, so the margin is 8 lines. | Replace with the measured figure, or with a case that is actually tight. If none exists, say so; the unit still stands on the adopter comment-density argument beside it. |
| 17 | MEDIUM | unit 3 · §5, AC5 | Both assert a property the engine already has, so AC5 is green before a line is written. `drift_report.py:190-193` already prints "within 14 lines above it", interpolating the constant. §5 describes that as absent. | Restate AC5 differentially: a fixture layer declaring a small window produces a message naming that number. |
| 18 | MEDIUM | unit 3 · S1, §4, AC3 | The declared value has no stated call path, and AC3 observes a harness that cannot see a fixture layer. The value is consumed at `drift_report.py:166` inside `_justified` and at line 193 inside `ratchet_findings`, which receives a list of ratchets, not the module. `load_project_layer` (line 105) resolves `drift_signals.py` from the kit dir, so `drift_report.py --check` in this tree can only load gov's own layer. | Name the plumbing: both functions take a lookback parameter defaulted to 14, read once at the site that already holds the layer. Move AC3's fixture observation to `selftest.py`, which copies the engine into a scratch repo. |
| 19 | MEDIUM | unit 5 · §4 Files touched | "The four gates a new leg trips together" is short, and two of the four are the ones measured as not firing (finding 11). The leg adds three depth-1 tracked paths under `tools/`; `govkit selfcheck` reports 48 tracked surface paths against 20 entries and 16 exemptions, and `registry.toml:164,168,172` declares sibling depth-1 paths individually. §7 lists no govkit leg. | Name the gates the build measures rather than a count, add registry rows to Files touched, and add `python tools/govkit/govkit.py selfcheck` and `python tools/govkit/selftest.py` to §7. |
| 20 | MEDIUM | unit 4 · §10 vs S5 | The reuse audit cites two readers as implementing S5's contract and neither does. `row_grammar.py`'s `pin_of` returns 0 on absence, which its docstring calls the STRICTEST value, and raises `Problem` on malformed. `lexicon_conf.py:36-38` says a refusal must "never" degrade into a silent default. S5 makes malformed fall back to 5, the loosest admissible value, in the unit §5 calls the security-shaped one. `TOOL-aLoosenedCeiling-2` chose an abort for the same class, and unit 1 S3 reuses it. | Strike or correct the §10 sentence. Either make a malformed declaration a named refusal that denies, matching all three cited precedents, or argue in §4 why this guard is the exception. Absent-means-shipped-default needs no analogy. |
| 21 | MEDIUM | unit 4 · §4 Alternatives | Two ratified decision rows extend the agent-cap argument past the channel, and neither is cited. `memory/DECISIONS.md:63` (`TOOL-aBoundedVerdict-1`) makes a second cap a driver file constant on the reused agent-cap argument. `memory/DECISIONS.md:52` (`TOOL-cBriefedPilot-2`) refuses a conf key because "a key lets a project declare zero directives, which is a global waiver carrying no name, no reason and no record". With finding 2 unresolved, S4 plus S8 is unit 4's whole answer to that objection. | Add both rows to §4 by name and answer them. If the tighten-only shape from `TOOL-cBriefedPilot-2` is rejected, say why, noting it needs no attribution grammar, no ratchet rows and no second parser. |
| 22 | MEDIUM | unit 4 · §4 "What this buys" | The §9 analogy names a control §9 does not name. `memory/guides/UNATTENDED-PROTOCOL.md:350-354` says what binds is "The same leg, re-run in a clone the run never touched by a party the run cannot execute code as"; it never mentions human diff review. Unit 5 §4 rejects relying on review, two files away. | Quote §9's actual sentence. With finding 2 fixed the binding control is the leg re-run where the run cannot reach; without it, say there is none. |
| 23 | MEDIUM | units 1 · S7 and 2 · S4 | Both require `tools/memory-tree/.memory-tree.conf.example` edits and neither has an acceptance criterion observing that file. This is the defect the prior build filed at rev-4: "S7's example-conf half had shipped UNDONE because no acceptance criterion observed that file". `kit-dogfood-parity.test.sh` pairs only the three doc templates. | One acceptance criterion per unit that greps the example conf for the declared keys. |
| 24 | MEDIUM | unit 4 · S1 | No conf grammar is stated, and the ratchet reads only one narrow shape. `_scalar_at` (`drift_report.py:148-151`) matches a whole line of key, optional quotes and digits, and skips comment lines. A trailing comment on the key line makes the ratchet blind with no output. | State the grammar in S1: one bare key-equals-integer per line, attribution on its own comment line above. Add an arm for the trailing-comment shape. |
| 25 | MEDIUM | unit 5 · S2 vs §3 | S2's prescribed phrasing "the declared cap" is singular and collapses the two rules §3 says must stay two. Unit 4 S1 creates three keys. `memory/gotchas/concurrency-is-not-a-budget.md` exists from conflating them once. | S2 names one phrasing per rule, keyed to the key it points at. Give S2 a disposition for rule-grammar sites that must keep an evaluable number, such as `REVIEW-PROTOCOL.md:110`'s `boundedParallel(thunks, 5)`. |
| 26 | MEDIUM | README · Order | Self-contradiction inside one paragraph. "Units 1, 2 and 3 are independent of each other and of 4/5 — three different files, three different kits" is followed by "Units 1 and 2 touch the same engine". Units 1 and 2 are both `check-memory-hygiene.sh` in the memory-tree kit. The prior build kept the mechanism-versus-write-set distinction that makes both sentences true. | Restore the split, or say unit 3 is independent of 1 and 2, and 1 and 2 share one file and sequence. |
| 27 | MEDIUM | unit 4 vs unit 5 | Unit 4 lands after unit 5's gate and writes a bare cap number into a shipped example file and new prose into a read-path member, satisfying neither of unit 5's standing rules. Unit 4 §7 does not list unit 5's gate, and §5 does not mention the read-path budget unit 5 S7 makes mandatory. | Add `check-agent-cap-restatement.sh` to unit 4 §7 with a waiver row or a digit-free example, and give unit 4 the read-path accounting sentence. |
| 28 | MEDIUM | unit 1 · §10 | The binding record cited is not the strongest one. `memory/builds/aRuledFrontispiece/spec/2026-08-16-spec-TOOL-aRuledFrontispiece-5.md` is CLOSED at rev-2 and its AC9 already promised this exact reword; the tree still carries the old literal in the engine at line 525 and the arm at `check-memory-hygiene.test.sh:640`, and the closing review re-filed it as LOW. | Cite that record in §10 as an unmet closed commitment and note in §4 that S4 and S5 repair it rather than re-open a settled wording decision. |
| 29 | LOW | unit 5 · AC1 | "At least ten sites" is not derivable from the spec's own table and is a floor below what a scan already finds. The population's file-type and path scope is stated nowhere; a cap-shaped regex over `tools/**/*.js` alone returns dozens of legitimate code hits. | Derive AC1 from S1's measurement rather than a literal, and state the extension and path scope in S1. |
| 30 | LOW | unit 5 · AC5 | Tautological: the read path must shrink "by the bytes S7 records", and S7's number is written after the measurement. `corpus_ids.py --report` prints `read path : 6 files, 92184 B`, of which `memory/guides/REVIEW-PROTOCOL.md` is 12371 B. | Drop the clause; require that the build record states both figures. |
| 31 | LOW | unit 4 · AC8 | AC8 specifies a raw `diff` where the existing leg compares CR-normalised streams (`agent-cap.test.sh:463`), and neither path is pinned `eol=lf`. The two criteria are not the same assertion, and the spec does not say it is tightening one. | Either have S11 cite the existing leg so no new arm is owed, or say AC8 tightens it and pin both paths in the same commit. |
| 32 | LOW | unit 4 · §5, §4 | "The hook runs on every tool call" is false; `.claude/settings.json:5` scopes it to the `Workflow` and `Agent` matcher, as §5 correctly states. The caching clause buys nothing: the hook is a fresh node process per call. | Correct the sentence and drop the caching clause. |
| 33 | LOW | unit 1 · S4, §4 | The replacement message text is never given and the constraints on it are not stated. `check-arms.py` takes the longest literal run after variable interpolation as the signature, raises below twelve characters, and does not count a `miss`-style or inline-`&&` arm. Separately, four in-file comments at lines 393, 428, 468 and 496 restate 300 or 350 and become wrong for an adopter who declares otherwise. | Name the replacement text in S4 with the length and positive-assertion constraints, and add the four comments to the inventory. |

## Refuted

- **Unit 3's lookback margin was one line.** The prior-art lens measured the justification 13 lines
  above the key against a 14-line window. Refuted. At every commit carrying that literal the
  justification is `.memory-tree.conf:107` and `READ_PATH_CEILING` is line 113 — distance 6, margin 8.
  The spec's "within two lines" is still wrong; finding 16 uses the correct measurement.
- **Three lenses gave three different carrier totals for the number 5** (12 files and 39 sites, 11 and
  28, and a third count). I adopted none of them. What I verified and state in finding 4 is the
  direction: one listed carrier has no cap site, four live carriers are missing, and one listed carrier
  is undercounted. The totals themselves are S1's job and no lens's arithmetic reproduced another's.
- **"REVIEW-PROTOCOL.md carries five sites at lines 7, 9, 20, 108 and 135."** Not reproduced as stated.
  Nineteen lines in that file contain a bare or prefixed 5; the cap-context ones I confirmed are lines
  7, 9, 108 and 110. The undercount claim survives; the specific line list does not.
- **"A gate does not assert the two hook copies are byte-identical."** Refuted as a premise for a
  finding: `agent-cap.test.sh:459-468` does assert it and is gate leg `agent-cap self-test`. What
  survives is the narrower point in finding 31, that the existing arm normalises line endings.
- **"`memory/map/features/install-prefix.md` omits the JavaScript prefix shape, which is why the reuse
  lookup could not return it."** Dropped as unverified. I did not run the lookup and did not read the
  dossier lines cited.
- **Unit 4 §3's "No value changes" contradicts the mechanism.** Not a defect. The full sentence is
  bounded by "until an owner declares otherwise" and matches the house pattern in unit 1 §3, unit 3 §3
  and `aLoosenedCeiling-2` §3.
- **Unit 2's premise that `SPEC10_CUTOFF` is not conf-settable today.** The conf is sourced at
  `check-memory-hygiene.sh:41` and the read is at line 664, so a conf declaration already wins. This
  makes the README's framing loose but changes nothing about what unit 2 builds, and finding 5 covers
  the part that matters. Not filed separately.

## What no lens checked

Four gaps. First, **buildability of unit 5's gate**: no lens tried to write the pattern, so the
false-positive rate over `tools/**/*.js`, the workflow harnesses and the frozen record trees is
unmeasured, and it is the largest cost risk in the build. Second, **whether the acceptance sets are
sufficient for Tier 2** rather than merely correct — every lens graded individual criteria and none
asked whether a unit's set closes its scope. Third, **the byte budgets after the edits**: unit 4 adds
two sections to `memory/guides/REVIEW-PROTOCOL.md` and unit 5 shrinks it, and nobody computed the net
against the read-path ceiling or checked `parallel-coding-governance.template.md` against its 48 KiB
gate. Fourth, **the specs against `memory/TEMPLATE-SPEC.md`'s own section canon and witness rules** —
all five were read for content and none was graded as a document by check 12's criteria.
