# TOOL-aWokenSentinel-9 — acceptance ledger

**Serves:** journal TOOL-aWokenSentinel-9

Every leg-shaped half below reads `observed at --close`: this pass ran no gate leg, no bar and no
suite. The pass verified with the direct checks the spec's section 6 names: the new
`TOOL-aWokenSentinel-9` block of `tools/unattended/unattended.test.sh` run ALONE, together with the
extraction block (for `want_unit`) and unit 7's fixture helpers, over the suite's sourced prologue
from a runner assembled under the session scratchpad (`runner9.sh`, `HERE` pinned by `KIT9`) with
the fixture's `mktemp -d` rooted at `%TEMP%/us9`; each staged break as a kit COPY under the
scratchpad (`kit-nofield`, `kit-twoline`, the whole kit dir so the lib sits beside the driver),
edited in binary mode by a scratchpad script that asserted the copy's shape; a `cmp` against a kit
copy holding `git show HEAD:tools/unattended/unattended.sh` (the order base d24fd7c0); the greps of
AC4 and AC5; and `adopt-unattended.sh --check`, `lexicon.py --check`, `check-install-prefix.sh`,
`check-spec-tokens.py` and `check-arms.py` each run ONCE as the checker, never as the bar. Those
stand in for the `unattended kit gate`, `unattended skill wiring`, `harness arms`, `memory hygiene`,
`spec tokens` and `lexicon naming predicates` legs, which run once at the close. No spec fold: the
field clause tests the `keepalive` fact before calling the helper, the spec's two conjuncts in the
cheaper order, and nothing else moved. `check-arms.py` names check 34 branches 4 and 7 unarmed at
`unattended.sh:2569` and `:2581`; those are unit 16's branches, promoted to unit 22 (RUN.md
2026-09-20T13:19:51Z), and this diff adds no `fail` branch.

**Evidences:** TOOL-aWokenSentinel-9
- AC1 — the block alone over the kit under test, `build_landed_fixture` with the sidecar holding an older `[]` line and a newest `{"utc":"2026-09-16T12:00:00Z","phase":"LANDING","session_crons":[{"id":"k1"}]}`: `run --status tRun` printed `unattended: tRun · phase LANDING · witness <sha> · next ARCH-tRun-1 — the unit · keepalive k1 present in the harness listing at 2026-09-16T12:00:00Z`; the last ` · ` field cut from the `· next` line was `keepalive k1 present in the harness listing at 2026-09-16T12:00:00Z`; `extract_next` over it printed `ARCH-tRun-1 — the unit`, byte-equal to `want_unit`. Listing `[]` at the same utc: `… · keepalive k1 absent in the harness listing at 2026-09-16T12:00:00Z`, no `k1 present`. RED against `kit-nofield` (the five-line field clause removed, `in the harness listing at` left 0 times in the copy): `FAIL missing:  · keepalive k1 present …`, `FAIL AC1 the keepalive field is the LAST field of the status line: expected [keepalive k1 present …], got [next ARCH-tRun-1 — the unit]`, `FAIL missing:  · keepalive k1 absent …`; green restored. OBSERVED.
- AC2 — `cmp` of `--status tRun` from the tip driver and from the `kit-base` copy on the no-sidecar fixture: `identical, 1 line(s)`; with a `[]` stop line present, `diff` base→tip showed the one line differing only by the appended ` · keepalive k1 absent in the harness listing at 2026-09-16T12:00:00Z`. In the block: `same "AC2 with no sidecar the line is byte-identical to before"` passed against `s9_before` captured on the same fixture before the sidecar was written, and `miss "$s9_before" "in the harness listing"` held. The `none` arm, `build_landed_fixture '/^keepalive: /d'` with a stop line naming `k1`: the line carried `· next ARCH-tRun-1 — the unit` and no `in the harness listing`. Both `same` and both `miss` rows stayed green on `kit-nofield`, as the spec says a field-clause copy must; the field's absence is what AC1 reds on. OBSERVED.
- AC3 — fixture with a parked row (`--park tRun --item x --reason y`), a two-line `resume.tRun.log` and a stop line naming `k1`: `check_status_one_line tRun` passed with `1` and the line it printed read `unattended: tRun · phase LANDING · witness <sha> · next ARCH-tRun-1 — the unit · parked 1 · resume-tick 2 attempt(s), last 2026-09-20T10:10:00Z · keepalive k1 present in the harness listing at 2026-09-16T12:00:00Z`; `extract_next` over it printed `ARCH-tRun-1 — the unit`; `run --resume tRun | head -n 1` carried the same keepalive field, count `1`. RED against `kit-twoline` (unit 17's copy, `printf 'x\n'` after the status printf, shape asserted `1 / 1`): `FAIL --status tRun is one stdout line: expected [1], got [2]` and nothing else red; the `kit-nofield` copy left this `same` green at `1`, which is why the break is unit 17's and not the field clause's. The status-and-resume agreement arm is untouched. OBSERVED; the suite whole is observed at --close.
- AC4 — `grep -c 'in the harness listing'` printed `1` over `tools/unattended/VERBS.template.md` and `1` over `memory/guides/UNATTENDED-VERBS.md` at the tip, `0` and `0` at HEAD; `bash tools/unattended/adopt-unattended.sh` re-rendered and `--check` exited 0 printing `unattended: in sync`. The render is 14676 B against the 61440 guide cap. OBSERVED; check 10's parity is observed at --close.
- AC5 — `grep -c 'run --status' tools/unattended/unattended.test.sh` printed `37` over `git show HEAD:` and `42` at the tip: the five direct readers this block adds (`s9_before`, present, absent, the no-sidecar `same`, the `none` arm); the AC3 fixture reads through `check_status_one_line`, the helper. `diff` of the `same "--status selects the same first row through the extracted helper"` line between HEAD and the tip printed nothing. OBSERVED.
- floor — the block executed 15 assertions (`n-before=23`, `n=38`, `st=0`) over the sourced prologue, the extraction block and unit 7's helpers. `FLOOR_ASSERTIONS` 946 → 961 and `FLOOR_SHARD_2` 750 → 765, region two, derivation in each pin's own comment; `FLOOR_SHARD_1` unmoved. The main loop's first green confirms the executed count; observed at --close.
- checkers — `lexicon.py --check` printed `lexicon OK — 2164 tracked file(s)` (no name minted, so no `--suggest`); `bash tools/check-install-prefix.sh` printed `install-prefix: clean — 276 shipped files, 11 declared waiver(s), 25 marked fixture line(s)` and `carried-prefix clean — 139 recorded file(s), 41 hand-justified, none rising`; `python tools/check-spec-tokens.py` exited 0 with this spec flipped to SPECCED for the run and back, no line naming this unit; `bash -n` over the driver and the suite exited 0; `git diff | cat -A` showed no `^M`. OBSERVED; the legs are observed at --close.
