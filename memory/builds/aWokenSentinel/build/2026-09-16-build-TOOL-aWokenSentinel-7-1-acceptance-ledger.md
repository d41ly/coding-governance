# TOOL-aWokenSentinel-7 — acceptance ledger

**Serves:** journal TOOL-aWokenSentinel-7

Every leg-shaped half below reads `observed at --close`: this pass ran no gate leg, no bar and no
suite. The pass verified with the direct checks the spec's section 6 names: the new
`TOOL-aWokenSentinel-7` block of `tools/unattended/unattended.test.sh` and the park-taxonomy
close-OK arm it adds one hit to, run ALONE over the suite's sourced prologue from a runner assembled
under the session scratchpad (`runner7.sh`, `HERE` pinned by `KIT7`) with the fixture's `mktemp -d`
rooted at `%TEMP%/us7`; each staged break as a kit COPY under `%TEMP%/us7/kit` (driver, lib, hook
and `run-lease.js` beside each other) edited in binary mode by a scratchpad script and restored
byte-equal afterwards; the greps of AC7, AC8, AC9 and AC12; `check-arms.py --report` sliced to the
driver; `gen_map.py --check`; and `adopt-unattended.sh --check`, `lexicon.py --check`,
`check-install-prefix.sh` and `check-spec-tokens.py` each run ONCE as the checker, never as the bar.
Those stand in for the `unattended kit gate`, `unattended skill wiring`, `harness arms`, `memory
hygiene`, `spec tokens`, `lexicon naming predicates`, `install-prefix` and `codebase-map` legs, which
run once at the close. The spec took a rev-5 fold at the pass, recorded in its section 9: the two
`fail` numbers, AC13's deferral marked in section 3, and two carrier edits shaped by byte caps the
spec's carrier table did not price. The dispatch was declared without `memory/LIVE.md` and
`memory/ledger`, which check 49 reserves to unit 16's never-written per-path rows; every sibling
since unit 16 declared the same way.

**Evidences:** TOOL-aWokenSentinel-7
- AC1 — the block alone over the kit under test, sidecar holding an older `[]` line and a newest `{"utc":"2026-09-16T12:00:00Z","phase":"LANDING","session_crons":[{"id":"k1"}]}`: `run --landed tRun` printed `UNATTENDED check 53 FAILED — the keepalive attestation is contradicted by the harness's own listing: … then re-run --landed: k1 listed at 2026-09-16T12:00:00Z`, no `phase LANDED`, and `sum` unchanged. RED against the kit copy with the `fail 53` line commented out (`: # fail 53 …`, staged 1 occurrence): `FAIL missing: the keepalive attestation is contradicted …` and `FAIL missing: k1 listed at 2026-09-16T12:00:00Z`; green with the line restored. OBSERVED.
- AC2 — newest line in phase `BUILDING`: `UNATTENDED check 54 FAILED — the stop-guard records this session and no stop after the close exists to check the reap against, … newest stop-guard record 2026-09-16T12:00:00Z in phase BUILDING`, `sum` unchanged; newest line with no `phase` key: the same refusal ending `in phase (unreadable)`, `sum` unchanged. RED against the kit copy with the `fail 54` line commented out: three `FAIL missing` rows (the literal, `in phase BUILDING`, `in phase (unreadable)`) plus AC14's first hit; green restored. OBSERVED.
- AC3 — newest line `LANDING` with `session_crons` `[]`: `unattended: keepalive-reaped: checked — k1 absent from the harness listing at 2026-09-16T12:00:00Z` then `unattended: phase LANDED · witness … · anchor remote …`; `landed-anchor: remote` in the record. OBSERVED.
- AC4 — no sidecar: `unattended: keepalive-reaped: attested, unchecked — no stop-guard record for this run (the hook is not wired, or the session was never bound)` then `phase LANDED`; no sidecar and `sed -i '/^keepalive: /d'` before the fixture commit: `unattended: keepalive-reaped: attested, unchecked — the record names no keepalive id` then `phase LANDED`. OBSERVED.
- AC5 — `check_status_one_line tRun` on the AC1, AC3 and AC4 fixtures, stdout to `$ORIGIN_DIR/s7.line` (the first cut wrote into `$TMP`, the fixture's own work tree, and `--landed`'s clean check refused every arm — moved outside the tree): each `same` passed with `1`. `cmp` of `--status tRun` from the tip driver and from a copy holding `git show HEAD:tools/unattended/unattended.sh` (the order base 6d869e2a) on each fixture: identical, one line, `unattended: tRun · phase LANDING · witness 95c3947d… · next ARCH-tRun-1 — the unit`. OBSERVED.
- AC6 — the park-taxonomy close-OK output (`bcreset; --preflight; keepalive-reaped: yes; parked-surfaced: yes, 2 surfaced; --close` with the two fixture overrides) carried `unattended: keepalive-reaped: attested; checked at --landed against the stop-guard's last harness listing` and `close OK`. RED against the kit copy with the `DOD_OUT=` line commented out: `FAIL missing: unattended: keepalive-reaped: attested; checked at --landed …`; green restored. OBSERVED.
- AC7 — `grep -c 'checked at --landed'` printed `1` over `tools/unattended/PROTOCOL.template.md` and `1` over `memory/guides/UNATTENDED-PROTOCOL.md`; `grep -c 'no script can observe them'` printed `0` over both Skill files; `grep -c 'harness listing'` printed `1` over `tools/unattended/VERBS.template.md` and `1` over `memory/guides/UNATTENDED-VERBS.md`; `bash tools/unattended/adopt-unattended.sh --check` exited 0 (`in sync`). The protocol render is 61402 B against `GUIDE_CAP_BYTES` 61440 (61436 at base): the row's +150 B paid by the −184 B history sentence, spec rev-5. OBSERVED; the parity legs are observed at --close.
- AC8 — `grep -c 'unenforceable by construction' memory/map/features/unattended.md` printed `0` at the tip and `1` over `git show HEAD:…`; `grep -c 'checked at'` printed `1` at the tip and `0` at HEAD; `wc -c` printed `20479` against `DOSSIER_CAP_BYTES="20480"` (20473 at base, so the bullet is 247 B for 241); `python3 tools/codebase-map/gen_map.py --check` exited 0. OBSERVED; the map leg is observed at --close.
- AC9 — `grep -c 'TOOL-aPromptedMandate-11 · CLOSED' memory/backlog/TOOL.md` printed `1` and `… · OPEN` printed `0`; one row moved, none added. OBSERVED.
- AC10 — the two RED readings under AC1 and AC2, each on the kit copy with its `fail` line commented and green with it restored; `check-arms.py --report` sliced to the driver's section lists `check 53 branch 1 line 2429 ARMED` and `check 54 branch 1 line 2425 ARMED`; the driver's unarmed rows are the nine pre-existing ones (checks 9, 27, 34 branches 4 and 7, …), none this unit's. OBSERVED; the `harness arms` leg is observed at --close.
- AC11 — the landed block executed 28 assertions (`n=20` → `48`) and the AC6 hit 1 (`48` → `51`, two of those three being the fixture arm's own), all region two; `FLOOR_ASSERTIONS` 917 → 946, `FLOOR_SHARD_2` 721 → 750, `FLOOR_SHARD_1` unmoved at 208, derivation in each pin's comment. Count confirmed at --close.
- AC12 — `grep -c 'rev-parse --git-dir' tools/unattended/unattended.sh` printed `0` at the tip and `0` over `git show HEAD:…`; `grep -c 'resolve_sidecar_dir'` printed `4` at the tip and `3` at HEAD, the one call in `read_stop_listing`. OBSERVED; unit 11's kit-gate check is observed at --close.
- AC13 — deferred: no bound run at build time. No `unattended/stop.*.log` exists under the git dir of any of the four worktrees, and `memory/builds/aWokenSentinel/RUN.md` carries no `session:` fact (preflighted before unit 1). The substring rule is marked UNVERIFIED in spec section 3 (rev-5); the first bound landing observes it.
- AC14 — on the `--landed` fixture with a `BUILDING` line present: `run --landed tRun` refused with check 54; one `Stop` payload (`session_id` `fixture-session`, `cwd` the fixture root via `cygpath -m`, `session_crons` `[{"id":"other-job"}]`) piped to `node tools/unattended/stop-guard.js` printed `{"decision":"block","reason":"stop-guard: this session holds unattended run tRun, phase LANDING, and its witness is already on the default branch, so the run is finished and unstamped; block 1/6. … Run `bash …/unattended.sh --landed tRun` now …"}`; the sidecar's newest line carried `"phase":"LANDING"` and `"session_crons":[{"id":"other-job"}]`; the second `run --landed tRun` printed `keepalive-reaped: checked — k1 absent from the harness listing at …` and `phase LANDED`. RED against the hook copy with unit 8's row reverted (`decision: 'allow'` for `FINISHED-UNSTAMPED`, staged 1 occurrence): `FAIL missing: "decision":"block"`, `FAIL missing: finished and unstamped`, `FAIL missing: --landed`; the second `--landed` still landed there because an allowing hook still writes the `LANDING` line, which is why the arm reads the hook's verdict and not only the phase; green with the row restored. OBSERVED.
- checkers — `python3 tools/lexicon/lexicon.py --suggest read_stop_listing --as sh.function` printed `OK`, as did `build_landed_fixture` and `remove_landed_fixture` (the suite's two fixture helpers were first spelled `land7`/`unland7`, and `lexicon.py --check` read `verb offenders 985 over pin 983` — renamed, and the check then printed `lexicon OK — 2163 tracked file(s)`); `bash tools/check-install-prefix.sh` printed `install-prefix: clean — 276 shipped files, 11 declared waiver(s), 25 marked fixture line(s), no undeclared root-install spelling` and `carried-prefix clean — 139 recorded file(s), 41 hand-justified, none rising`; `python3 tools/check-spec-tokens.py` exited 0 with `958 token(s) graded` and no line naming this unit; `bash -n` over the driver and the suite exited 0; every committed blob of the pass counts 0 `\r\n` by a python byte count and `git ls-files --eol` reads `i/lf w/lf` for each — the first cut cited `grep -c $'\r'`, which on this MSYS node reported every line of an LF-only driver as CR (the `msys-grep-counts-cr-on-every-line` class, hit in this pass). OBSERVED; the legs are observed at --close.
- checklist — `gotchas.py --for-diff HEAD~1..HEAD` selected 27 classes on the pass commit. Two touch the diff. `id-matched-as-a-substring`: the verb's `grep -qF` on the recorded id inside `session_crons` is the substring rule spec section 3 chose and section 3 now marks UNVERIFIED, because the listing's shape is unmeasured and a quoted-string match that misses the real spelling would be a check that cannot fail; an over-match (`k1` inside `k10`) errs toward a refusal naming id and utc, never toward a pass, so the class is accepted in the refusing direction and the first bound landing decides it. `msys-grep-counts-cr-on-every-line`: the checker row above is corrected. `empty-field-collapses-unless-it-is-last`: the listing is `read_stop_listing`'s LAST line and may be empty, read by `sed -n '3,$p'`, which is the shape the class permits. No other selected class names a line of this diff.
