# TOOL-aWokenSentinel-6 — acceptance ledger

**Serves:** journal TOOL-aWokenSentinel-6

Every leg-shaped half below reads `observed at --close`: this pass ran no gate leg, no bar and no
suite, per the build README's rule three. The pass verified with the direct checks the spec's
section 6 names — `grep -c`, `awk` region cuts, `cmp` and `wc -c` over the tracked carriers, each
run at the tip and, through `git show c8aaeb90:<path>`, at this unit's pass base — plus
`bash tools/unattended/adopt-unattended.sh --check` and `python tools/codebase-map/gen_map.py --check`
each run ONCE as the checker the criterion names, never as the bar. The pass base is `c8aaeb90`,
the tip of order 13; the spec's own base figures are its 2026-09-16 pins and are restated only
where the two differ. `unattended kit gate` (checks 10, 18, 22, 24), `unattended skill wiring`,
`memory hygiene`, `kickoff-manifest ratchet`, `codebase-map coverage + freshness`, `install-prefix`
and `spec tokens` run at `--close`.

Section 5 of the protocol was rewritten to a DERIVED budget: `61440 - wc -c` of the template with
section 5 cut out was 2477 bytes at the pass base, and the new section measures 2473, so the render
lands at 61436 against the 61440 cap. That also drained unit 10's `memory/project/curation-debt.txt`
row for the render: the registry's stale-row guard reds a row that waives a file passing checks 6,
7 and 8 unwaived, and the render now does. The dossier's paragraph was rewritten at 473 bytes
against the original's 479 plus one byte of headroom, so the dossier measures 20473 against 20480.

**Evidences:** TOOL-aWokenSentinel-6
- AC1 — over `tools/unattended/PROTOCOL.template.md` and `memory/guides/UNATTENDED-PROTOCOL.md`: `grep -c 'does not share the stalled session'` printed `1` (base `0`) for both; `grep -c '^## 5[.] The idle-wake and the keepalive'` printed `1` for both; the section 5 region cut by the spec's awk carried `stop-guard` 1, `stall-recorder` 1, `resume-tick` 1, `--liveness` 1, `--resume` 2, `AGENT` 2, `DRIVER` 1 (each `0` at base), `never ends its turn by asking` exactly once by `grep -o | wc -l` (base `0`), and `for four kit versions` `0` (base `1`). OBSERVED.
- AC2 — over `tools/unattended/SKILL.template.md` and `.claude/skills/unattended/SKILL.md`: `grep -c 'cannot be corrected in place'` printed `0` (base `1`) for both; the `## Resume` region carried `--resume <slug> --keepalive-id` 1 (base `0`) — AMENDED, rev-4: the spec's needle `--keepalive-id` read `1` at base too, because the replaced paragraph named the option; `grep -c '^## What wakes a stalled run'` printed `1` (base `0`); `grep -c 'schedule the idle-wake NOW'` printed `1`; the `## What wakes a stalled run` region, 8 lines, carried `stop-guard` 1, `stall-recorder` 1, `resume-tick` 1, `--liveness` 1, `--preflight` 0, `/session-kickoff` 0; the `## Reap` region carried `idle-wake` 1 (base `0`); the `## Before any path` region carried `never end the turn by asking` 1 (base `0`) — AMENDED, rev-4: the spec said "last paragraph" where §4 places the sentence at the end of "What the tick runs", and AC2 now grades the region. OBSERVED. Check 18's order is observed at --close.
- AC3 — `grep -c '^## The sidecar' tools/unattended/README.md` printed `1` (base `0`); the region below it carried `stop.<slug>.log` 1, `stall.<slug>.log` 1, `resume.<slug>.log` 1 by `grep -c -F`; `grep -c 'gov-resume-tick'` printed `2` at the tip and `2` at base. OBSERVED.
- AC4 — `grep -c 'the keepalive runs it' tools/unattended/VERBS.template.md` printed `0` (base `1`); `grep -c 'the idle-wake runs it'` printed `1` in the template and `1` in `memory/guides/UNATTENDED-VERBS.md`. OBSERVED. Check 10's byte-compare is observed at --close.
- AC5 — `cmp tools/unattended/PROTOCOL.template.md memory/guides/UNATTENDED-PROTOCOL.md` exited 0; `cmp tools/unattended/VERBS.template.md memory/guides/UNATTENDED-VERBS.md` exited 0; `bash tools/unattended/adopt-unattended.sh --check` exited 0 printing `unattended: in sync (skill rendered from template + .unattended.conf)`, with its INFO line that `gov-resume-tick` is unregistered on this node. OBSERVED.
- AC6 — in `.unattended.conf` the line above `STOP_GUARD_BLOCKS=` (43), `RESUME_STALE_BOUND=` (54), `RESUME_ATTEMPTS=` (57) and `RESUME_TURNS=` (59) each starts with `#`; in `tools/unattended/.unattended.conf.example` the same for lines 44, 53, 56 and 58; the read added no rationale, every knob carrying one from units 2, 5 and 10 as the spec expected; `grep -c 'idle-wake'` printed `3` (base `0`) for each file; `grep -c '^STOP_GUARD_BLOCKS='` printed `1` at the tip and `1` at base for each; the commit's `--stat` lists `memory/guides/SESSION-KICKOFF.md` beside `.unattended.conf`, re-stamped `last-audit: 2026-09-21T00:43:07+03:00 @ 4cf0944dbdce94714870f26760936bc5edabc64e`. OBSERVED. The ratchet leg is observed at --close.
- AC7 — `wc -c memory/map/features/unattended.md` printed `20473` against `DOSSIER_CAP_BYTES="20480"` read from `.memory-tree.conf`; `wc -c memory/guides/UNATTENDED-PROTOCOL.md` printed `61436` against the hygiene gate's default `GUIDE_CAP_BYTES=61440` (the file was 62367 at the pass base, over the cap under the unit 10 debt row this pass drained); `grep -c 'resume tick' memory/map/features/unattended.md` printed `1` (base `0`); `grep -c 'unenforceable by construction'` printed `1` at the tip and `1` at base; `python tools/codebase-map/gen_map.py --check` exited 0. OBSERVED. `memory hygiene` check 6 and the stale-row guard are observed at --close.
- AC8 — at the tip, `git grep -l 'cannot be corrected'` under the spec's exclusions matched `0` files (base `2`); `git grep -il keepalive` matched `35` files (base `35`; the spec's 2026-09-16 pin was 30, and the five since are the kit README plus `stop-guard.js`, `run-lease.js`, `resume-tick.sh` and `resume-tick.test.sh`); of the 35, eleven are in the commit's `--stat` and the remaining twenty-four are each named in section 3, verified by joining the sweep list against `git diff --cached --name-only` and then against section 3's text by basename, the rev-4 bullet covering the code, test, harness and registry carriers the spec had folded into "the driver's comments". Both counts are in the commit message. OBSERVED.
