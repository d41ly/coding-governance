# TOOL-aWokenSentinel-3 — acceptance ledger

**Serves:** journal TOOL-aWokenSentinel-3

Every leg-shaped half below reads `observed at --close`: this pass ran no gate leg, no bar and no
suite, per the build README's rule three. The hook observations ran each arm block of
`tools/unattended/stop-guard.test.sh` ALONE from its sourced prologue in a scratch runner under
the session scratchpad (`STOP_GUARD_TEST_TMP`, the seam the prologue declares for exactly this),
against the copied hook and the stub driver; AC11 against the real driver on a `git init` fixture
under `%TEMP%`, seeded by `adopt-unattended.test.sh`'s `seed()` extracted as one function. Every
arm was observed RED first on a hook copy carrying the break it names — the §4 table's rows each
negated, the crons summarised, the continuation dropped, the agent gate removed, the default
raised above the cap, the unwritable sidecar blocking, the bound unwired, the `gitdir:` file
ignored, the literal `absent` binding, the verdict parse missing — and GREEN against the built
hook. Thirteen arm blocks, 89 executed assertions plus the run_hook guard's one, 35 s of arm wall
summed, node `a` under MSYS, 2026-09-20. That block set stands in for the suite whole, which sits
on no bar leg and runs at the close under `run-unattended-gates.sh`.

**Evidences:** TOOL-aWokenSentinel-3
- AC1 — RED before the hook existed: `printf '%s' "$P" | node "$SP/absent/stop-guard.js"` printed `node:internal/modules/cjs/loader ... throw err` and `rc=1`. GREEN: the AC1 block over a record at BUILDING with `session: absent` printed `rc=0`, empty stdout, no file at the sidecar path (`AC1: 5 passed, 0 failed`), and a payload whose `session_id` IS the literal `absent` was silent too; RED with `session === 'absent'` dropped from `scanLeases` (`FAIL AC1 the literal absent never binds: expected [], got [{"decision":"block"...`)
- AC2 — the AC2 block over a record carrying the payload's session and the stub at `phase: BUILDING` / `verdict: LIVE`: `rc=0`, stdout one JSON object with `decision` `block` and a `reason` carrying `unattended run fx`, `block 1/6`, `unattended.sh --plan fx` and `phase BUILDING, liveness LIVE`; the sidecar's one line parsed by `python -c` reads `decision` `block`, `blocks` `1`, `reason` `run-open`, `phase` `BUILDING`, `session` the payload's, keys in the order `utc session decision reason phase verdict blocks background_tasks stop_hook_active session_crons`; a second stop read `block 2/6` from the file (`AC2: 14 passed, 0 failed`). The fixture's `<kit-rel>` rendered as `../kit` because the scratch kit sits outside the fixture root; in this tree `path.relative` gives `tools/unattended`
- AC3 — the stub at `verdict: TERMINAL`: `rc=0`, empty stdout, the line's `decision` `allow`, `reason` `terminal`, `blocks` `0` (`AC3: 5 passed`); RED with `TERMINAL` removed from `VERDICTS_ALLOW` (`FAIL AC3 terminal stdout empty: expected [], got [{"decision":"block"...`). `FINISHED-UNSTAMPED` not observed: unit 8's row
- AC4 — `background_tasks` of two objects at `LIVE`: allow, `reason` `background-tasks`, `background_tasks` `2`; an empty array blocks `1/6` (`AC4: 5 passed`); RED with the row's test raised to `> 999` (`FAIL AC4 background stdout empty`)
- AC5 — conf `STOP_GUARD_BLOCKS="2"`, two seeded block lines for this session: allow, `reason` `blocks-exhausted`, `blocks` `2`, no NOTE on stderr; one seeded line: `block 2/2`; two seeded lines for `other-session`, the seed asserted at two lines first: `block 1/2`, three lines after (`AC5: 9 passed`; the first reading of this arm passed VACUOUSLY on an empty seed after a helper rename left two unquoted calls unrenamed, which is why the seed count is now asserted before the feed); RED with the exhausted row's test raised to `> 9999` (`FAIL AC5 exhausted stdout empty`) and RED with the session filter dropped from `measureBlocks` (`FAIL AC5 another session's lines are not counted: missing [block 1/2]`)
- AC6 — no knob: `grep -c 'declares no STOP_GUARD_BLOCKS'` over stderr printed `1` and the reason read `block 1/6`; `STOP_GUARD_BLOCKS="zero"`: `rc=0`, empty stdout, the line's `reason` `knob-malformed`, stderr `not a positive integer`; `export STOP_GUARD_BLOCKS=3 # a comment` read `block 1/3` (`AC6: 7 passed`); RED with the `knob-malformed` row deleted (`FAIL AC6 malformed stdout empty: expected [], got [{"decision":"block"...`). figure: 6 is `BLOCKS_DEFAULT` in the hook, read from the reason and never typed by the arm
- AC7 — stub exit 1: `rc=0`, empty stdout, the line's `reason` `liveness-unreadable`, `verdict` `unreadable`, `phase` `unknown`; the stub printing no `verdict:` line: the same; the stub file moved aside (a failed spawn): the same (`AC7: 10 passed`); RED with the unreadable row flipped to block (`FAIL AC7 rc-1 stdout empty`, five of ten)
- AC8 — `session_crons` a two-object array with nested values and `stop_hook_active` true: the line's `session_crons` deep-equals the payload's under `python -c` (`same`), `stop_hook_active` `true`, the reason ends with `This is a continuation the hook already blocked once.`; a payload without the field wrote `null` and its reason carried no continuation (`AC8: 6 passed`); RED with the field written as `null` (`FAIL AC8 crons deep-equal: expected [same], got [differs]`) and RED with the sentence's line deleted (`FAIL AC8 reason ends with the continuation`)
- AC9 — a payload with `agent_id`: `rc=0`, empty stdout; `hook_event_name` `SubagentStop`: the same; neither wrote a line; `grep -c '"event": "Stop"'` over the fragment printed `1` and `grep -c SubagentStop` printed `0`; unparseable stdin `rc=0` silent (`AC9: 9 passed`); RED with the `agent_id` gate replaced by an event alternation (`FAIL AC9 agent_id stdout empty: expected [], got [{"decision":"block"...`)
- AC10 — `.git` a FILE holding `gitdir: <native path of $F/wt-git>`: the run blocked `1/6`, the line landed at `$F/wt-git/unattended/stop.fx.log`, `find "$F" -name stop.fx.log | grep -c ''` printed `1` (`AC10: 3 passed`); RED with `resolveRepo` returning the `.git` file itself as the git dir (`FAIL AC10 the worktree fixture binds and blocks`, `expected [1], got [0]`)
- AC11 — the fixture `%TEMP%/sg11f/real` built by the adopter suite's `seed()` (one commit, HEAD born), a record at BUILDING with `session:` equal to the payload's: the copied driver's own `bash tools/unattended/unattended.sh --liveness fx` printed `verdict: LIVE`, the hook at `$FR/tools/unattended/stop-guard.js` blocked `1/6` at `rc=0`, and the line's `verdict` equalled that read (`LIVE`) with `phase` `BUILDING` (`AC11: 5 passed`, 6.1 s); RED on a frozen kit copy whose `parseLiveness` reads `verdictx` (`FAIL AC11 line verdict is the driver's own: expected [LIVE], got [unreadable]`). The backslash fold of the driver path was staged out too and stayed GREEN on this node: bash under MSYS accepts a `C:\...` script path, so the fold is defensive against the charter §11 class and not load-bearing here
- AC12 — `python tools/settings-merge.py --fragment tools/unattended/stop-guard.fragment.json` printed `wired stop-guard * hook into .claude/settings.json`, `rc=0`; a second run printed `already wired`, `rc=0`, and `cmp` against the first result said byte-identical; `python -c` over the file printed `1 group(s) under Stop; matcher '*'; commands ['node "${CLAUDE_PROJECT_DIR}/tools/unattended/stop-guard.js"']`; the merger's `.bak` was removed before staging. Last write of the pass: the entry was made after every hook observation
- AC13 — with `hook_path` pointed at `{kit}/unattended/stop-gaurd.js`, `bash tools/check-hook-destinations.sh` printed `FAIL tools/unattended/stop-guard.fragment.json declares hook_path ... which NO kit.toml rule ships.`, `rc=1`; restored, it printed `hook-dest: 9 fragment(s) against 250 declared destination(s)`, `ok tools/unattended/stop-guard.fragment.json -> tools/unattended/stop-guard.js` and `clean — every declared hook path resolves`, `rc=0`; `git ls-tree -r --name-only HEAD | grep -c 'fragment.json$'` printed `8` at base. Run as the direct checker, untimed; the leg half is observed at --close
- AC14 — before wiring, `bash tools/unattended/adopt-unattended.sh --check` printed `the stop-guard hook is UNWIRED — .claude/settings.json carries no Stop entry under matcher * naming stop-guard.js` with the remedy naming the fragment, `rc=1`; after the merger, `hooks: 2 fragment(s) wired` and `in sync`, `rc=0`; over a copy with the `Stop` group deleted, declared through `GOV_SETTINGS_JSON`, `the stop-guard hook is UNWIRED` with the remedy naming that copy's path, `rc=1`. Run as the adopter's own probe; the `unattended skill wiring` leg is observed at --close
- AC15 — the BASE seed and BASE adopter (`git show HEAD:` copies) over a seeded fixture: the render exited 0 and `--check` printed `tools/unattended/gate-guard.fragment.json is missing from the kit, so the gate-guard wiring cannot be checked`, the fixture's kit dir listing carrying no fragment and no hook; the NEW seed and adopter, arm 1 and arm 1a run alone from the suite's prologue: `35 assertions, st=0`, which includes every fragment moved out (`no *.fragment.json in tools/unattended`, `rc=1`), restored (`hooks: 2 fragment(s) wired`), the pretty-printed `fx.fragment.json` naming `fx.js` (`the fx hook is UNWIRED`, remedy `--fragment tools/unattended/fx.fragment.json`, `rc=1`), the merger wiring it (`rc=0`, `hooks: 3 fragment(s) wired`), `fx.js` deleted (`names the hook fx.js, which is not a file in tools/unattended`, `rc=1`), and the fixture fragment removed with its group (`rc=0`). Observed RED first: the first run of the `fx` arm printed `declares no name or no marker` because the fixture fragment was one line and the adopter's sed reads one key per line; the arm now writes the shipped shape
- AC16 — `python tools/codebase-map/gen_map.py --check` before the regen printed `STALE: ... symbols.json` (and `inventories.json`, `MAP.md` — unit 14's gotcha class `borrowed-seed-inherits-its-head-state.md` had never been regenerated, so once this regen brought the inventory current the pre-commit coverage gate named it UNCLAIMED; the claim is one line in the unattended dossier's `gotcha-classes`, declared by a supplementary `--dispatch` and made here because claiming the key IS the unblock and the gate may not be exempted), `rc=1`; after `--write`, `--check` printed nothing and `rc=0`; `symbols.json` gained 100 lines for the two kit `.js` files
- AC17 — `python tools/lexicon/lexicon.py` printed `P1 verb graded=2238 offenders=983`, unchanged from base, `rc=0`, and `--suggest <name> --as js.function` answered `OK` for all eighteen names of the §4 inventory; re-run after the map regen armed the new files it read `offenders=996`, thirteen over the pin — the suite's sixteen shell helpers, thirteen of which led with no table verb (`ok`, `same`, `hit`, `payload`, `feed`, `sidecar`, `field`, `lines`, `reason`, `now_ms`, `seedline` among them) — so every helper was renamed to a declared verb (`print_ok`, `check_same`, `build_payload`, `run_hook`, `derive_sidecar`, `read_field`, `measure_lines`, `extract_reason`, `render_seedline`, ...) and the leg read `offenders=983` again, `rc=0`; `bash tools/check-install-prefix.sh` printed `UNRECORDED tools/unattended/stop-guard.test.sh 1` on the first run, and `carried-prefix clean — 137 recorded file(s), 39 hand-justified, none rising`, `rc=0`, after the hand-written row for the suite's one `KIT_REL` default and the `selftest-budgets.txt` row's raise 15 → 16. Run as direct checkers; the two legs are observed at --close
- AC18 — `grep -c 'stop-guard.test.sh' tools/unattended/kit.toml` printed `0` before the edit (the AC18 block's `expected [1], got [0]`) and `1` after; `node -e` over the copied hook printed `under` for `BLOCKS_DEFAULT <= HARNESS_CONSECUTIVE_CAP` and `8` for the cap (`AC18: 3 passed`); RED with `BLOCKS_DEFAULT` raised to 9 (`expected [under], got [over]`)
- AC19 — `<git-dir>/unattended` made a FILE: `rc=0`, empty stdout, stderr `sidecar-unwritable`, the file's bytes unchanged (`AC19: 8 passed`); RED with the failed write still blocking (`FAIL AC19 unwritable stdout empty ... "reason":"blocked anyway"`); the stub sleeping 3 s under `STOP_GUARD_LIVENESS_BOUND_MS=500`: allow, `reason` `liveness-unreadable`, `rc=0`, `the bound fired within 2 s (1006 ms)`; RED with the env override unwired so the 60 s constant bound it (`FAIL AC19 hung liveness stdout empty`, the stub's `LIVE` arriving after 3 s and blocking)

## What this ledger does not evidence

No kit gate, hygiene leg, spec-token leg, lexicon leg, install-prefix leg, hook-destinations leg,
skill-wiring leg or codebase-map leg ran inside this pass as a LEG; each ran once as the direct
checker its criterion names, and every one is `--close`'s. Neither `stop-guard.test.sh` nor
`adopt-unattended.test.sh` was run whole: the former's thirteen blocks and the latter's arms 1 and
1a ran one block at a time from a sourced prologue, so `FLOOR_ASSERTIONS=80` is pinned from the
summed per-block counts (90) at ~10% headroom and the budget row's 60 s from the summed arm walls
(35 s, each block paying the prologue again) times 1.5 floored at 60 — the main loop's first
VERIFYING reading is what both are re-derived from. The kit version did not move (unit 6's). The
knob's four declarations are unit 10's; until then this repo runs on the hook's announced default,
which the AC6 NOTE line prints on every bound stop. `checkSession` joined the hook's inventory at
rev-4 (§9), a divergence from the rev-3 list recorded before the code was written.
