# TOOL-aWokenSentinel-13 — acceptance ledger

**Serves:** journal TOOL-aWokenSentinel-13

Every leg-shaped half below reads `observed at --close`: this pass ran no gate leg, no bar and no
suite, per the build README's rule three. The pass verified with the direct checks the spec's
section 6 names: the new `U13` arm block of `tools/unattended/resume-tick.test.sh` run ALONE over
the suite's sourced prologue, from a runner assembled under the session scratchpad with `HERE`
pointed at the kit under test and then at three copies each carrying one staged break, the
`git init` fixture under `%TEMP%/rt13`; the BLOCK copy is staged inside the arm itself, because
unit 18 re-reads it and unit 24 replaces the inline `sed` with a helper; and
`check-install-prefix.sh`, `lexicon.py --check` and `check-spec-tokens.py` each run ONCE as the
checker, never as the bar. Those stand in for the `unattended kit gate`, `memory hygiene`, `spec
tokens`, `lexicon naming predicates` and `install-prefix (shipped surface)` legs, which run once
at the close. The tick itself needed no edit: unit 5's pass built the conf block and the header
sentence exactly as this spec's section 4 spells them, so the unit's diff to
`tools/unattended/resume-tick.sh` is empty and the five arms are the proof, as the spec's
Files-touched row anticipated.

**Evidences:** TOOL-aWokenSentinel-13
- AC1 — the U13 block alone over the fixture whose conf carries `RESUME_ATTEMPTS="2"` (appended before the hour-old commit through the new `CONF_EXTRA` seam, because a conf edited after it is a write dated now and the tree reads LIVE) with two attempt lines seeded at `NOW_UTC`: `rc=0`, the line `resume-tick: tRun · <fixture> · skip · ATTEMPTS EXHAUSTED · last <now> · out <sidecar>/resume.tRun.2.out`, no `declares no RESUME_ATTEMPTS` on stderr, the stub never invoked. RED against the SOURCE copy (`sed '/^\. "\$CONF"$/d'`, one line removed): `resumed · attempt 3`, the NOTE printed, the stub invoked — three red rows of the SOURCE run's thirteen. OBSERVED; the suite whole is observed at --close.
- AC2 — amended rev-4 — the BLOCK copy's reading at this order: the rev-3 text said two empty-path NOTEs and a launched attempt 1, which needs `CONF` set and empty and nothing does that; the tick runs under `set -u`, so the copy dies at the library's NOTE line — the section 9 rev-4 line logs it, a FACT-QUESTION the pass's probe resolved BEFORE the arm was written, the break unchanged. The amended criterion then observed by the U13 block alone: over a fixture declaring neither key, `declares no RESUME_ATTEMPTS` once and `declares no RESUME_TURNS` once on stderr, `measure_note_files` (the new helper: every `Declare one in <path> to change it` line whose path `test -f` accepts) printed `2`, and the two-space signature `Declare one in  to change it` matched zero lines; the no-conf refusal is the AC8 arm's existing rows (`rc=2`, `carries no .unattended.conf, so the tick has no bounds to read`), not restated. The BLOCK copy, made in the arm by `sed '/^CONF=/,/^\. "\$CONF"$/d'` over the tick and shape-checked at two `read_bound_key` calls, run over a fresh fixture: `rc=1`, stderr `<kit-block>/lib-unattended.sh: line 92: CONF: unbound variable`, zero `declares no` lines, zero launchers, `measure_note_files` printed `0` — the arm red on the copy by a count of zero.
- AC3 — the block over a fixture whose conf carries `RESUME_TURNS="7"`: `resumed · attempt 1`, `grep -c -- '--max-turns 7 '` over the launcher printed `1`, `stub.log` carries `--max-turns 7 ` in the `-p` argv, no `declares no RESUME_TURNS`. RED against the SOURCE copy: the launcher and the argv carry `40` and the NOTE prints — three red rows. OBSERVED.
- AC4 — the block over the fixture with `RESUME_ATTEMPTS="2"` at the root and `session: absent` in its own record, plus `git worktree add rt-wt4` whose conf the arm rewrites to `MEMORY_ROOT=mem2` and `RESUME_ATTEMPTS="6"`, a `tWt` record under `mem2/builds/` bound to the session, every written file `touch -d '-1 hour'` so the tree's newest write reads STALE against the one-second bound, and two attempt lines seeded in that worktree's own sidecar (`seed_log` took an optional slug and sidecar for it): `rc=0`, the one line `resume-tick: tWt · <rt-wt4> · skip · ATTEMPTS EXHAUSTED · last <now> · out <wt4-sidecar>/resume.tWt.2.out`, one decision line in all, no launcher under the worktree's sidecar, no NOTE. RED against the WORKTREE copy (line 248's subshell read of `MEMORY_ROOT` re-spelled as a source into the tick's own shell): `resumed · attempt 3` and one launcher, the worktree's `6` read in place of the root's `2` — two red rows. OBSERVED.
- AC5 — amended rev-4 — the rev-3 text named the DECLARED case as the CLEAR copy's red, but `. "$CONF"` overwrites an export with the declared `2` whether or not the keys were cleared, so the neither-declared case is the one that reds; the section 9 rev-4 line logs it, folded before the arm was committed. The amended criterion then observed by the U13 block with `RESUME_ATTEMPTS=abc` exported into the tick's environment: under the conf's `2` with two lines seeded, `rc=0`, `ATTEMPTS EXHAUSTED`, no `REFUSING`; with neither key declared and two lines seeded, the `declares no RESUME_ATTEMPTS` NOTE and `resumed · attempt 3`, the kit default's cap. RED against the CLEAR copy (`sed '/^RESUME_ATTEMPTS=""; RESUME_TURNS=""$/d'`): the neither-declared case printed `unattended: REFUSING - RESUME_ATTEMPTS is declared as 'abc', which is not a positive integer of attempts …` and launched nothing — two red rows — while the declared case stayed green there.
- S4 — `grep -cE "sed '/\^CONF=/" tools/unattended/resume-tick.test.sh` printed `1` at the tip, the count spec 24 AC3 expects here before unit 18 adds its own; `grep -c 'red against the BLOCK copy'` and `grep -c "sed '/^CONF=/"` over this spec printed `3` and `3`, the two phrases spec 18 AC4 greps for at least 1. OBSERVED.
- floor — the U13 block alone executed 27 assertions, green over the real tick twice (before and after the helper's rename); `FLOOR_ASSERTIONS` rises 61 → 84, the nine blocks' 94 executed at ~10% headroom, derivation in the pin's own comment. Spec 5's AC5 rows, which read the first NOTE's path with their own `sed` and `test -f`, now call `measure_note_files` and assert `2` — one implementation of the NOTE-path read (the checklist's second-implementation class), the AC7 block still 11 executed (the dropped line was an assignment) and re-run alone green. The main loop's first green at VERIFYING confirms the executed count; observed at --close.
- checkers — `bash tools/check-install-prefix.sh` printed `install-prefix: clean — 276 shipped files, 11 declared waiver(s), 25 marked fixture line(s), no undeclared root-install spelling` and `carried-prefix clean — 139 recorded file(s), 41 hand-justified, none rising`; `python tools/lexicon/lexicon.py --check` printed `lexicon OK — 2159 tracked file(s)` after `--suggest` renamed the helper from `count_note_files` to `measure_note_files` (the declaration says `measure` for a count that decides nothing); `python tools/check-spec-tokens.py` over this spec exited 0. OBSERVED; the legs are observed at --close.
