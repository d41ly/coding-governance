# TOOL-aWokenSentinel-5 — acceptance ledger

**Serves:** journal TOOL-aWokenSentinel-5

Every leg-shaped half below reads `observed at --close`: this pass ran no gate leg, no bar and no
suite, per the build README's rule three. The pass verified with the direct checks the spec's
section 6 names: each arm block of `tools/unattended/resume-tick.test.sh` run ALONE over the
suite's sourced prologue, from a runner assembled under the session scratchpad with `HERE` pointed
at the kit under test (or at a copy carrying one staged break) and the `git init` fixture under
`%TEMP%/rt5`; the new arm of `tools/unattended/unattended.test.sh` run alone the same way over that
suite's prologue; the driver, the adopter's `--check`, `gen_map.py --check`, `lexicon.py --check`
and `check-install-prefix.sh` each run ONCE as the checker the criterion names, never as the bar;
check 22's three-way join and check 32's count re-derived by hand with the checker's own
expressions. Those stand in for the `unattended kit gate`, `unattended skill wiring`, `memory
hygiene`, `spec tokens`, `lexicon naming predicates`, `kickoff-manifest ratchet`, `install-prefix
(shipped surface)`, `shell hygiene` and `codebase-map coverage + freshness` legs, which run once at
the close.

**Evidences:** TOOL-aWokenSentinel-5
- AC1 — the AC1 block alone printed `pass=15 fail=0`: one line `resume-tick: tRun · <fixture> · resumed · attempt 1 · out <sidecar>/resume.tRun.<stamp>.out`, the tick back in 2 s while the stub's log had no `done`, the argv carrying `--resume 11111111-2222-3333-4444-555555555555 --dangerously-skip-permissions --max-turns 40` and the payload's `--keepalive-id`, `auth status` logged first, one sidecar line matching the UTC regex with `attempt 1` and `pid-alive no`, the launcher beside it naming the same `.out`. RED against a tick copy whose `run_detached` ran the launcher synchronously (`returned within 5 s: got no: 18s`, `unexpected [done]`) and against one with the launch line commented out (three `missing` rows). OBSERVED.
- AC2 — the AC2 block alone printed `pass=10 fail=0` (re-run after the checklist pass below moved the login answer into a file): `STUB_LOGGED_IN=false` read `SKIP — the CLI is not logged in on this node; nothing can resume tRun`, `rc=0`, `argv auth status` logged and no `-p`, no sidecar line; a file touched under `<git-dir>/gate-logs/` read `skip · verdict LIVE` with no stub log at all. RED against a copy whose `check_login` returned 0 unconditionally (`-p` logged, sidecar written) and against one with the verdict gate removed (login probed on a LIVE record). The block's last two rows are the bug-class checklist's `bounded-through-a-pipe-is-unbounded` left-shifted: a stub whose `auth status` leaves a 20 s sleeper holding stdout still yields the logged-out skip and the tick is back in 2 s; RED at `22s` against a copy reading the answer through `$( )`. The LIVE half's gate log is dated five minutes ahead, because a file touched `now` read STALE two seconds later against the fixture's one-second bound — observed green and red on the clock alone before the fix. OBSERVED.
- AC3 — the AC3 block alone printed `pass=7 fail=0`: six lines stamped now read `skip · ATTEMPTS EXHAUSTED · last <now> · out <sidecar>/resume.tRun.6.out` with nothing invoked and six lines still; six lines stamped two hours ago launched `attempt 7`; five lines stamped now launched `attempt 6`. RED against a copy comparing with `-gt` (six newer lines launched a seventh). OBSERVED.
- AC4 — the AC4 block alone printed `pass=4 fail=0`: `sleep 300 &` read by `ps` as `sleep` under WINPID, recorded as `pid:`, the tick read `resumed`, the stub was invoked, the sidecar line carried `pid-alive yes`, and `tasklist //FI "PID eq <pid>"` printed `No tasks are running`. RED against a copy with the kill line commented out (`the sleep is gone from tasklist: expected [1], got [0]`). OBSERVED.
- AC5 — `grep -c '^read_bound_key() '` printed `1` for `tools/unattended/lib-unattended.sh` and `0` for `tools/unattended/unattended.sh`; the driver's `read_bound_key ` call count is `4` at the tip and `4` at the pass's parent `1392dc36` (the pinned base `5f9648d6` reads `3`, because unit 2 landed the fourth call after it — the count this unit did not move is the parent's); over the fixture with `GATE_BOUND="abc"`, `bash tools/unattended/unattended.sh --status tRun` printed `which is not a positive integer of seconds` once and exited 2; the tick over a conf declaring neither knob printed `declares no RESUME_ATTEMPTS` and `declares no RESUME_TURNS` once each on stderr, the path after `Declare one in ` naming the fixture's `.unattended.conf`, which `test -f` confirmed, and the AC3 arms acted on the default of 6. OBSERVED; the declared-key arms are unit 13's.
- AC6 — `git show --stat HEAD` of the pass commit lists `.unattended.conf` with `memory/guides/SESSION-KICKOFF.md` (observed at the commit); `grep -c 'RESUME_ATTEMPTS\|RESUME_TURNS'` printed `2` over the example and `1` over `kit.toml` (`0` and `0` at `5f9648d6`); the section 8 region of `memory/guides/UNATTENDED-PROTOCOL.md` cut by the spec's awk carries both keys (`2`); check 22's join re-derived with its own expressions read `undocumented: | phantom: | proj_extra: |`, empty in all three directions. The render is byte-identical to the template after `adopt-unattended.sh`. OBSERVED; the ratchet and check 22 as legs are observed at --close.
- AC7 — the AC7 block alone printed `pass=11 fail=0`: `--dry-run` over the STALE fixture printed the `resumed · attempt 1` line ending ` (dry-run)`, no stub log, no sidecar line, no launcher; a fixture with `session: absent` printed exactly `resume-tick: no bound run in 1 worktree(s)` at exit 0. RED against a copy with the dry-run return removed (stub invoked, sidecar and launcher written). OBSERVED.
- AC8 — `bash tools/check-install-prefix.sh --list` lists no row for `resume-tick.sh` (one row for the suite, its usage header, hand-justified); `grep -c 'dirname "$0"'` over the tick printed `1`; the AC8 block alone printed `pass=6 fail=0`, the non-repo root refused with `REFUSED — <dir> is not a git repository, so there is no worktree list to walk` at exit 2 and a conf-less root refused at exit 2. RED against a copy with the refusal removed (the `REFUSED` row missing). `check-install-prefix.sh` printed `carried-prefix clean — 139 recorded file(s), 41 hand-justified, none rising` after the budgets row rose 17 to 18 by hand. OBSERVED.
- AC9 — the new region-two block of `tools/unattended/unattended.test.sh` run alone over that suite's prologue read `n 20 -> 26, st=0`: with a two-line `resume.tRun.log` under the fixture's `<git-dir>/unattended`, `--status tRun` stayed one line carrying ` · resume-tick 2 attempt(s), last 2026-09-20T10:10:00Z`, `--resume tRun`'s first line carried the same field, and with the sidecar removed the line was byte-identical to the one captured before it; `grep -cE '^[^#]*rev-parse --git-dir'` over the driver printed `0`, as at the parent. RED against a driver copy printing the field whenever the sidecar path derives (`FAIL unexpected: resume-tick`). Floors raised 904 → 910 and 708 → 714 by the six. OBSERVED; the suite whole is observed at --close.
- AC10 — `bash tools/unattended/adopt-unattended.sh --check` on node `a` printed `unattended: INFO — no scheduled task named gov-resume-tick on this node; the resume tick is unregistered (the kit README has the line)` before `in sync`, `rc=0`; `schtasks //query //tn gov-resume-tick` answered `ERROR: The system cannot find the file specified.`; with a stub `schtasks` exiting 0 first on PATH the line read `INFO — the resume tick is registered as gov-resume-tick`, `rc=0`; `grep -c 'gov-resume-tick' tools/unattended/README.md` printed `2`. OBSERVED.
- AC11 — `python tools/lexicon/lexicon.py --check` printed `P1 verb graded=2309 offenders=983`, the `VERB_OFFENDER_PIN`, and `sh.function.conv 6 of 788` — twenty functions added, none an offender; `grep -c 'resume-tick' tools/run-gates/selftest-budgets.txt` printed `1` and `grep -c 'resume-tick.test.sh' tools/unattended/kit.toml` printed `1`, both `0` at base. OBSERVED.
- AC12 — the AC12 block alone printed `pass=8 fail=0`: a kit copy whose `unattended.sh` printed one line and exited 1 read `liveness probe failed: UNATTENDED check 52 FAILED — …(stubbed)`, launched nothing, wrote no line; a second worktree added without its conf read `resume-tick: <wt2> · skipped: no .unattended.conf in this worktree` beside the first tree's decision line, two lines in all. RED against a copy returning 0 from `read_liveness` regardless (`skip · verdict ` printed instead) and against one with the conf-less skip removed. OBSERVED.
- AC12 — amended rev-5 — the stub driver now NOTEs on stderr before its refusal and the criterion asks for the CHECK line, not the first merged one; the AC12 block alone printed `pass=9 fail=0`, `liveness probe failed: UNATTENDED check 52 FAILED — …(stubbed)` with `declares no GATE_BOUND` absent from the tick's stdout. RED against a tick copy taking the first merged line (`missing [… UNATTENDED check 52 …]`, `unexpected [declares no GATE_BOUND]`). OBSERVED, 2026-09-21.
- AC13 — the AC1 block alone printed `pass=21 fail=0`: the sidecar line ended `launched <pid>`, `check_pid_alive` read it `yes`, and a second tick printed one line `skip · IN-FLIGHT · launched <pid> alive since <utc>` with one `argv -p` in the stub log and one sidecar line; the hung half alone printed `pass=2 fail=0`: a seeded two-hour-old line carrying `launched <the sleep's WINPID>` read `resumed · attempt 2` and `tasklist` printed `No tasks are running` for the sleep. RED against a copy without the in-flight branch (`resumed · attempt 2`, two lines) and one without the launched-pid kill (`the hung launched pid is gone from tasklist: expected [1], got [0]`). OBSERVED, 2026-09-21.
- AC14 — the AC14 block alone printed `pass=10 fail=0`: an untracked `tDrop/RUN.md` with a live session read one line `skip · RUN.md is not tracked, and the tick launches only on a lease the index holds`, no stub log, no attempt line, no launcher; after `git add` the same tick read `tDrop · … · resumed · attempt 1`; with `tRun`'s working copy rewritten to `99999999-aaaa…` the launcher carried `--resume 11111111-…` and the disk id nowhere. RED against a tick copy globbing the filesystem (`the untracked record invokes nothing: expected [nothing], got [invoked]` and four more). OBSERVED, 2026-09-21.
- AC15 — the AC15 block alone printed `pass=6 fail=0`: `host: some-other-node` read `skip · leased on some-other-node, not this node compeeto-agent` at exit 0 with the sleep still listed, no stub log, no attempt line; `host:` set to `read_host_name`'s answer read `resumed · attempt 1` under `--dry-run`. RED against a tick copy without the host row (`resumed · attempt 1`, the sleep gone, the stub invoked). OBSERVED, 2026-09-21.
- AC16 — the AC16 block alone printed `pass=6 fail=0`: the LANDING record with `witness: <first commit>` under `GOV_DEFAULT_BRANCH=main` read `verdict: FINISHED-UNSTAMPED` from the driver and `resumed · attempt 1` from the tick, one attempt line, one launcher; with a gate log five minutes ahead, `skip · verdict FINISHED-UNSTAMPED`. RED against a tick copy acting on `STALE` alone (`skip · verdict FINISHED-UNSTAMPED`, no log, no launcher). OBSERVED, 2026-09-21.

## What this ledger does not evidence

No kit gate, hygiene leg, spec-token leg, lexicon leg as a leg, install-prefix leg as a leg, map
leg, shell-hygiene leg, manifest ratchet as a leg, or `*.test.sh` suite ran whole inside this pass;
every one is `--close`'s. The tick's suite `FLOOR_ASSERTIONS` is pinned at 55 from 61 executed
across the seven blocks run alone (6 11 10 15 7 4 8), which the whole suite observes at the close
and this pass did not. The budget row is sized from 36 s of wall across those seven runs, each
paying the prologue and a `git init`, x1.5 floored at 60. The POSIX arms of the
kill and the detach are UNVERIFIED (no registered node is POSIX). The `--dispatch` for this pass
omitted `memory/LIVE.md` and `memory/ledger/2026-09.md`, which the brief says to declare: the verb
refused the first declaration because unit 16's row on `memory/LIVE.md` is still open, as unit 4's
ledger already records, and this pass writes neither.


## The rev-5 fold, 2026-09-21

The rows dated 2026-09-21 are the closing diff review's round 1 folded into this unit (ids 1, 2,
3, 5 and 16), every arm run ALONE from the suite's sourced prologue with `HERE` pointed at the
kit under test or at a scratch copy carrying one staged break, the fixture under `%TEMP%/rt5`.
The pre-existing blocks were re-run the same way after the fold: AC8 6, AC7 11, AC2 10, AC3 7,
AC4 4, U12 6, U18 12 unchanged, U13 29 after its worktree record was STAGED (an unstaged one is
now the AC14 skip, which is the fold working). `FLOOR_ASSERTIONS` 97 -> 125 from 139 executed.
No suite ran whole; the close observes the floors.