# TOOL-aWokenSentinel-2 — acceptance ledger

**Serves:** journal TOOL-aWokenSentinel-2

Every leg-shaped half below reads `observed at --close`: this pass ran no gate, no leg and no
suite, per the build README's rule three. The one check the pass verified with is the unit's own
arm block, sourced beside the suite's prologue (plus `build_audit_fixture`, the `--audit` STALLED
arm and the NOCONF arm) into a scratch runner and run ALONE — 75 executed assertions in the block
on node `a` under MSYS (n 26 → 101, two of them the MSYS-only `tasklist`-stub arm) plus one on the
NOCONF fixture, green against the built driver, and RED against eight frozen kit copies each
missing one graded line: the no-record `fail 52`, the dead-probe `fail 52` (replaced by `:`, since
deleting it leaves an empty `then`), the gate-log join, the below-sum NOTE, the
`state=finished-unstamped` assignment, the stall-line read, the `.` in the transcript encoding,
and the MSYS arm of `check_pid_alive`. Each break was caught by the assertion that grades that
line and by nothing else; the `--audit` STALLED arm stayed green under every break but the two
that broke the shared driver's parse. That block stands in for `tools/unattended/unattended.test.sh`
whole, which sits on no bar leg.

**Evidences:** TOOL-aWokenSentinel-2
- AC1 — `run --liveness tNoRun` over a reset fixture printed `UNATTENDED check 52 FAILED — no run-state file, so there is no run whose liveness can be graded: memory/builds/tNoRun/RUN.md`, no `verdict:` line, exit 1; `run --liveness` with no slug printed `UNATTENDED check 1 FAILED`, exit 1; RED with the `fail 52` line removed (`AC1 no run-state file exits 1: expected [1], got [0]` and `FAIL unexpected: verdict:`); `python tools/memory-tree/check-arms.py --report` shows `check 52 branch 1 line 3204 ARMED` and `check 52 branch 2 line 3256 ARMED`, the driver at `branches 203 (floor 104) armed 196 (floor 101)`
- AC2 — over the `build_audit_fixture` record mutated to `LANDED`, `run --liveness tRun` printed `state: terminal`, `verdict: TERMINAL`, exit 0; `printf '%s\n' "$out" | grep -c ':'` printed `13`, `grep -cvE '^[a-z-]+: '` printed `0`, and the key column read `phase state default-branch session pid keepalive pid-alive last-move last-move-source transcript last-stall stale verdict`; over the same record at `BUILDING` the count was `13` again with `state: live` and `verdict: LIVE`
- AC3 — at `LANDING` with the witness `$BASE` (asserted first: `git rev-parse refs/remotes/origin/main` equals `$BASE`), the verb printed `state: finished-unstamped`, `default-branch: refs/remotes/origin/main`, `verdict: FINISHED-UNSTAMPED`, exit 0; with the witness at the unit branch's HEAD, `state: live` and no `verdict: FINISHED-UNSTAMPED`; under `env -u GOV_DEFAULT_BRANCH` with no `origin/HEAD` in the fixture, `default-branch: unresolved`, `state: live`, no `default-branch: refs/`, exit 0; RED with the `state=finished-unstamped` line replaced by `:` (`FAIL missing: state: finished-unstamped`)
- AC4 — the fixture's `pid: 999999999` printed `pid-alive: no`; a `sleep 60 &` whose `ps -p "$!"` WINPID column the arm wrote into `pid:` printed `pid-alive: yes`, and `no` after `kill` + `wait`; `pid: absent` printed `pid-alive: unknown`; under MSYS with `$TMP/stubtl/tasklist` (exit 1, no output) ahead on PATH, `pid-alive: unknown`; RED with the MSYS `case` arm renamed so `kill -0` ran on the Windows pid (`FAIL missing: pid-alive: yes` and `FAIL missing: pid-alive: unknown`)
- AC5 — `mkconf "true" "true" "" "3600" "" "1800" "7" "60"`, the clock aged an hour by `GIT_COMMITTER_DATE` over a clean tree, `gate-logs` removed, `CLAUDE_CONFIG_DIR=$ORIGIN_DIR/cfg-empty` — `last-move-source: commit`, `transcript: absent`, `stale: yes`, `verdict: STALE`, exit 0; after `touch "$(git rev-parse --git-dir)/gate-logs/leg.log"` — `last-move-source: gate-log`, `stale: no`, `verdict: LIVE`; RED with the gate-log join removed (`FAIL missing: last-move-source: gate-log`); after `touch` of `$ORIGIN_DIR/cfg/projects/<root with :\/. → ->/fixture-session.jsonl` — `transcript: <that path>`, `last-move-source: transcript`, `verdict: LIVE`, the fixture root itself carrying a `.` (`tmp.U68GiCBOMN`); RED with `.` dropped from the `tr` set (`FAIL missing: transcript: …`); after `touch scratch.txt` on the STALE fixture — `last-move-source: write`, `stale: no`. amended rev-4: the config root sits under `$ORIGIN_DIR`, not `$TMP`, because `$TMP` is the fixture tree and the first run read `write` on the mtime tie
- AC6 — on the NOCONF heredoc, `declares no RESUME_STALE_BOUND, so a run reads STALE after the derived default of 5400s` counted `1`; with `"abc"` in the eighth positional, `REFUSING - RESUME_STALE_BOUND is declared as`, no `verdict:`, exit 2; with `"60"` under the fixture's 3600 + 1800, `RESUME_STALE_BOUND (60s) is below GATE_BOUND + UNIT_STALL_BOUND (5400s), so a full bar's silence reads STALE and an out-of-process resumer may kill a healthy bar` counted `1` and the verdict still printed; RED with the NOTE line removed (`expected [1], got [0]`); `grep -c '^read_bound_key RESUME_STALE_BOUND ' tools/unattended/unattended.sh` printed `1`, `grep -cE '^read_bound_key [A-Z_]+ '` printed `4`, `grep -cE '^read_bound_key [A-Z_]+ [0-9]'` printed `0`; `grep -c '^RESUME_STALE_BOUND='` printed `1` over `.unattended.conf` and `1` over `tools/unattended/.unattended.conf.example`, `grep -c 'RESUME_STALE_BOUND' tools/unattended/kit.toml` printed `1`, and the section 8 region of `memory/guides/UNATTENDED-PROTOCOL.md` cut by the spec's awk carried the key once. Leg half, check 22 — observed at --close
- AC7 — `printf '2026-09-16T00:00:00Z fixture-session rate_limit {}\n' > "$(git rev-parse --git-dir)/unattended/stall.tRun.log"` then `run --liveness tRun` printed `last-stall: 2026-09-16T00:00:00Z fixture-session rate_limit {}`; removed, `last-stall: none`; RED with the `tail -n 1` read replaced by `:`; `grep -c 'resolve_sidecar_dir' tools/unattended/unattended.sh` printed `2`; `grep -cE '^[^#]*rev-parse --git-dir' tools/unattended/unattended.sh` printed `1`
- AC8 — over a dirty fixture with `$TMP/stub/stat` (exit 1) ahead on PATH, the verb printed `UNATTENDED check 52 FAILED` and `the liveness cannot be measured on this node, because a probe it needs answered nothing, so no verdict is answerable and a zero from a dead probe would read as moved-just-now:` (the arm greps up to the colon; on this repo's own dirty tree the probe named `stat -c %Y on .claude/skills/unattended/SKILL.md`), no `verdict:`, exit 1; RED with that `fail 52` replaced by `:` (`AC8 a dead mtime probe exits 1: expected [1], got [0]`); `grep -c 'read_tree_clocks' tools/unattended/unattended.sh` printed `5`; the `--audit` STALLED arm in the same runner printed `· last-write none ·` and `· STALLED` at exit 0 against the extracted clocks
- AC9 — `grep -c 'unattended.sh --liveness' tools/unattended/unattended.sh` printed `1`; `grep -cP '^- \x60--liveness\x60'` printed `1` over `tools/unattended/VERBS.template.md` and `1` over `memory/guides/UNATTENDED-VERBS.md`; `grep -c 'unattended.sh --liveness'` printed `1` over `tools/unattended/SKILL.template.md` and `1` over `.claude/skills/unattended/SKILL.md`; `grep -c -- '--liveness' tools/unattended/unattended.sh` printed `7`; the renders came from `bash tools/unattended/adopt-unattended.sh` and its `--check` printed `unattended: in sync`, exit 0, run as the adopter's own probe and not as a leg. Leg half, check 26's three joins — observed at --close
- AC10 — the staged set holds `.unattended.conf` and `memory/guides/SESSION-KICKOFF.md` together; `last-audit` reads `2026-09-20T20:53:11+03:00 @ 4cf0944dbdce94714870f26760936bc5edabc64e`, the `git merge-base origin/main HEAD` of this branch, an ancestor of HEAD and twelve commits later than the `286b62d1` stamp it replaces; the commit body carries the `manifest-audit:` delta line; `git grep -lE '^\*\*Status:\*\* CLOSED' -- memory/builds/aWokenSentinel/spec/` lists this spec after staging. amended rev-4: the criterion's "the commit's parent or a later sha" contradicted the manifest's own stamp rule and now states it. Leg half, `manifest-check.sh` check 5 — observed at --close
- AC11 — the `--liveness` block alone (with `build_audit_fixture` prepended) read `n=101 st=0` on node `a`, 2026-09-21: on the live-sleep fixture `pid-image: claude.exe` printed `pid-alive: no`, `pid-image: sleep.exe` (what `read_pid_image` printed for the WINPID) printed `yes`, and `pid-image: absent` printed `yes`. RED against a library copy whose `check_pid_alive` answers `yes` whatever its second argument (`missing: pid-alive: no`). OBSERVED; the rev-5 fold of the closing review's id 2, with `check_pid_alive` moved to `lib-unattended.sh`.

## What this ledger does not evidence

No kit gate, harness-arms leg (beyond the `--report` read above), hygiene leg, spec-token leg,
lexicon leg, install-prefix leg or codebase-map leg ran inside this pass; every one is `--close`'s
and each row above says so. The five function names were asked of `python tools/lexicon/lexicon.py
--suggest <name> --as sh.function`, which answered OK for each — a query, not the leg. The suite's
`FLOOR_ASSERTIONS` and `FLOOR_SHARD_2` rose by 74, the count every platform executes (73 in the
block plus the NOCONF line; MSYS executes two more); the suite whole was not run, so the sharded
totals are the prior measurement plus that count and not a fresh one. Whether `_` is replaced in
the transcript directory encoding stays UNVERIFIED, as the spec records. The build README's
authored roster row for this unit moved `PLANNED` to `CLOSED` beside the spec header.
