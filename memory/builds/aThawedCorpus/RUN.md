# aThawedCorpus - run state

Created by `unattended.sh --preflight`. The unit list is NOT copied here — it is DERIVED
from the build README on every read, so it cannot go stale between them. This file holds
only what nothing else does: the phase and its witness, the keepalive id, the pinned BASE
with its anchor evidence, and the parked decisions.

<!-- run:generated -->
<!-- /run:generated -->

## Run facts
units-at-landing: TOOL-aThawedCorpus-5 TOOL-aThawedCorpus-4 TOOL-aThawedCorpus-1 TOOL-aThawedCorpus-2 TOOL-aThawedCorpus-3
unpushed-at-landing: 0
landed-anchor: remote
parked-surfaced: yes, 2 surfaced
keepalive-reaped: yes
witness: cf82c2de73112c8dfe81559f403c6c048b5152ff
phase: LANDED
branch-sha: 4f406bf73d203fe80f99d6b71576d63fa44dbc81
branch-ref: refs/heads/branch/memory-toolkit-closed-records-cache-8eee95
mode: prompt
anchor-kind: run-branch
keepalive: 01b60017
anchor-url: https://github.com/d41ly/coding-governance.git
anchor-sha: f5dff6aee0b0a0177fac8ec842532b461eeca71f
anchor-ref: refs/heads/main
base: 4f406bf73d203fe80f99d6b71576d63fa44dbc81

## Parked

2026-08-27T09:55:20Z rescope · item add TOOL-aThawedCorpus-4 · reason the per-check measurement found check 23 carries the same per-item spawn defect as check 21 — one awk per record for the acceptance ledger, plus two basename spawns and a cut per spec. Speccing it inside unit 1 would put two mechanisms in one spec and make the closing diff unable to say which half a finding lands on.

2026-08-27T11:21:18Z decision · item Should check-memory-hygiene.sh become ONE scanner process rather than a shell script that spawns per check? The owner asked this directly mid-run, 2026-08-27. · reason OPTIONS SEEN. (a) Collapse the two per-item loops to awk passes — what units -4 and -1 build: 1398 s to roughly 80 s, ~4300 spawns to 3, low risk, no gate property lost. (b) Rewrite the whole checker in Python — 1398 s to roughly 5 s, but check-arms.py discovers the 20 armed fail branches by scanning TRACKED SHELL and .memory-tree.conf pins check-memory-hygiene.sh:20:20, so the harness meta-gate goes blind on its own subject. (c) THE ONE I WOULD RECOMMEND, and it is the file's own established pattern rather than a new idea: one Python SCANNER reads the corpus once and emits a findings stream; the shell keeps all 20 fail branches and only prints what the scanner found. That is exactly what check 21 already does with gen_build_index.py --print-bindings, and the checker's own header records why the split is drawn there. It gets one process AND keeps the arms gate. WHY I REFUSED IT HERE. It ports the scanning half of checks 1-12, 21, 22 and 23 into Python in one diff, on the script that IS the merge bar, inside a run whose mandate is a performance fix. The measured remainder after (a) is roughly 80 s, so (c) buys a further ~75 s and costs a full Tier-2 build with a byte-identity proof over every check. That is a separate authorization, not a widening of this one. Units -2 and -3 as specced are worth less than (c) and should probably be reconsidered against it.

2026-08-27T11:32:24Z rescope · item add TOOL-aThawedCorpus-2 · reason RECONSTRUCTED 2026-08-28 by the session merging this build, NOT written by the run. The roster at the pinned BASE 4f406bf7 held TOOL-aThawedCorpus-1 alone; commit 0aa31097 created the spec files for -2, -3 and -4 in one go, and only -4 received an add row, at 09:55:20Z. The timestamp on this row is that commit's, because it is the earliest moment the record PROVES the unit was in scope -- it is not an observed decision moment, and no such moment was ever recorded. Both units were retired later, at 13:00:59Z and 13:01:02Z, so their departure was on the record while their arrival was not. Recorded now rather than smoothed over, because check 24 exists to refuse exactly the silent scope move this was, and satisfying it with an invented timestamp would defeat the check while passing it.

2026-08-27T11:32:25Z rescope · item add TOOL-aThawedCorpus-3 · reason RECONSTRUCTED 2026-08-28 by the session merging this build, NOT written by the run. The roster at the pinned BASE 4f406bf7 held TOOL-aThawedCorpus-1 alone; commit 0aa31097 created the spec files for -2, -3 and -4 in one go, and only -4 received an add row, at 09:55:20Z. The timestamp on this row is that commit's, because it is the earliest moment the record PROVES the unit was in scope -- it is not an observed decision moment, and no such moment was ever recorded. Both units were retired later, at 13:00:59Z and 13:01:02Z, so their departure was on the record while their arrival was not. Recorded now rather than smoothed over, because check 24 exists to refuse exactly the silent scope move this was, and satisfying it with an invented timestamp would defeat the check while passing it.

2026-08-27T12:34:24Z review · item TOOL-aThawedCorpus-5 · reason verdict BLOCKED · blockers 5

2026-08-27T12:36:28Z rescope · item add TOOL-aThawedCorpus-5 · reason RECONSTRUCTED 2026-08-28 by the session merging this build, NOT written by the run. Commit 5477b28f -- the spec audit that returned BLOCKED -- created this unit's spec. The review row at 12:34:24Z already names it, so it was in scope by then, which makes this row's timestamp slightly LATER than the row that first cites it. Left visible rather than smoothed: the observed moment was never recorded, and back-dating to hide the ordering would be inventing the fact this row exists to admit is missing.

2026-08-27T13:00:59Z rescope · item retire TOOL-aThawedCorpus-3 · reason Dead twice over, both verified against main after regrounding. PREMISE: tools/gate-legs.json on main declares a ceiling on 85 of 86 legs with memory hygiene at 1270, and run-gates.sh kills a leg that outlives it — the declare-or-be-reported mechanism this unit was specced to invent already shipped as TOOL-aBoundedCeiling-1. MECHANISM: the spec audit showed a PATH shim cannot see a fork, and a $( ) command substitution and every pipeline stage fork a subshell with no exec — which is the exact class this build measured in checks 21 and 23, so the counter would have missed its own subject.

2026-08-27T13:01:02Z rescope · item retire TOOL-aThawedCorpus-2 · reason Retired on three audit findings verified against main. (1) The guard pre-pass at run-gates.sh:762 writes skip before the reuse loop at :855, which begins with a [ -f rc ] continue — so guard-skip PREEMPTS reuse and a newly guarded leg can never print GATE reuse, making the unit's stated goal a misdescription of what it would buy. (2) input_key is an internal shell function with no print mode, so the arm has no surface to assert on. (3) memory hygiene is UNGUARDED today, which is why pre-push runs it on both the forced and the scoped branch — and that is precisely the compensating control TOOL-aThawedCorpus-5 depends on. Declaring a guard on it would open the coverage hole unit 5's own risk section relies on not existing. After the collapses the leg sits far under its declared 1270 ceiling, so the saving does not pay for the hazard.

2026-08-27T15:43:22Z decision · item Hygiene check 14 reds on main: TOOL-aBoundedCeiling-2, -3 and -4 are cited but never defined, and ORPHAN_ID_PIN is 0. · reason PROVEN INHERITED, not introduced: reproduced in a clean detached worktree of main with nothing from this build present. aBoundedCeiling's README cites three ids in its parked section for units it DROPPED without ever writing a spec, so each is cited and none is defined. OPTIONS SEEN. (a) Register them in memory/project/id-orphan-waiver.txt — tried and REJECTED on measurement: ORPHAN_ID_PIN counts waiver ROWS, not unwaived orphans, so three rows against a pin of 0 reds just as loudly, and the row comments naming each id made corpus_ids resolve them as definitions, turning the rows stale in the same breath. (b) Move ORPHAN_ID_PIN 0 -> 3. That edits .memory-tree.conf, a governance carrier, and moves a shrink-only ratchet the WRONG way on another build's behalf. (c) Author three WONTDO stub specs so the ids resolve. That invents prose for another build's dropped units, and M2 is explicit that a build's own folder owns its own prose. WHY I REFUSED. (b) is M3 veto 2 — a change to a governance carrier — and (c) is veto 2's sibling plus a scope this mandate does not reach. Both are owner turns. The bar was already red here before this run started and is no redder for landing this one; the honest close is a recorded gates-green override naming this, not a quiet edit to somebody else's ratchet. The drain is aBoundedCeiling adjudicating its own three parked decisions.

2026-08-27T18:14:35Z override · item gates-green · reason Owner instructed 'skip the bar' at 2026-08-27T21:0x after three attempts were starved by the repository turnstile: two were killed while queued (1189s and 303s, never reaching a leg) and the third was stopped on that instruction. TWO of the three starvations were my own fault - a killed bar leaves a queue ticket and the next run waits behind the corpse until the 1800s TTL reaps it. WHAT IS KNOWN WITHOUT THE FULL BAR, each run directly: check-memory-hygiene.test.sh PASS 254 assertions; check-arms.py --check green with ARMS_FLOORS 20:20 unchanged; corpus_ids.py --check green except the parked inherited orphans; manifest-check.sh green including checks 5 and 9; check-verdict-epoch.sh satisfied by the 2.48 bump; run-gates.gov.test.sh's new F3 arm observed RED against a staged guard and green restored; and both collapse units verified byte-identical on stdout AND stderr over the full corpus plus a seeded differential covering outcomes the live corpus never produces. WHAT IS NOT KNOWN: whether any leg unrelated to this diff is red, and the bar would have been RED regardless on hygiene check 14, which is PROVEN inherited - it reproduces in a clean detached worktree of main with nothing from this build present, and its repair is an owner turn under M3 veto 2. This run does NOT push. The lander runs the bar, bypassing it is banned, and the owner has just said to skip it - so the push is deliberately left as the owner's call rather than taken under a mandate that was written for an unattended finish.
