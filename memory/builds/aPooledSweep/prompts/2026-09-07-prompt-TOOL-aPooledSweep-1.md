# The prompt this build was started from

**Serves:** research TOOL-aPooledSweep-1

Node `a`, 2026-09-07. Handed as `--prompt` with the prose inline; there was no path, so the bytes
below are the value verbatim rather than a copy of a file that could be edited after the run began.

---

Another CG work session aQuenchedHarness has just landed its work which is mostly incomplete - the
harness and the on-demand runner exist; but only one suite of fifty-eight was ported. The survey
found none of the other eleven costliest portable under the unit's own rules, and AC5 is recorded
NOT MET with the arithmetic rather than the threshold lowered to fit.

Your task is to see why it was impossible to finish posting the gates and complete the job.

---

## Reading it

"Posting the gates" is read as PORTING the gates — the subject of `TOOL-aQuenchedHarness-6`, which
is the unit that recorded AC5 not met. Nothing else in the parent build is a posting act, and the
sentence before it names the port.

Two deliverables, taken in order: the diagnosis of why the port could not finish, and the
completion of the job it was serving. The job is the parent build's stated goal — the on-demand
sweep costing minutes rather than hours — and NOT the port itself, which was one chosen mechanism
for it. Reading the port as the job would make "complete it" mean finishing a 58-suite rewrite the
parent measured as unreachable; reading the goal as the job admits a cheaper mechanism, which is
what this build found.
