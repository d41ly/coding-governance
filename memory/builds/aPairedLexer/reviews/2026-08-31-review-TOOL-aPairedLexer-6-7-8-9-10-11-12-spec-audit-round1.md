**Serves:** spec-audit TOOL-aPairedLexer-6 TOOL-aPairedLexer-7 TOOL-aPairedLexer-8 TOOL-aPairedLexer-9 TOOL-aPairedLexer-10 TOOL-aPairedLexer-11 TOOL-aPairedLexer-12

# Spec audit round 1 — the seven units promoted out of the non-convergent closing review

*Node a, 2026-08-31, round 1. The closing diff review of `TOOL-aPairedLexer-1..4` went NON-CONVERGENT
under BUILD-METHOD M4 — round 1 confirmed 2 blockers, round 2 confirmed 5 — so the review loop
stopped and every standing blocker became a unit. These seven specs are those units. A spec authored
this run is unreviewed by definition, which is why all seven are in scope. A parallel fan of primed
finder lenses over the seven specs, the two scanners they change, the extractor the seventh changes,
and the shipped test suite they inherit, then skeptics prompted to REFUTE each finding by
re-deriving it against the source at the pinned base. Every behavioural claim below is an exit code
measured by piping a real `Workflow` payload into the real hook, or a view printed by calling the
real scanner — never a reading of prose. Every fixture is `node --check` clean under an `async`
wrapper, and the synthesis pass re-ran the load-bearing measurements rather than inheriting them.*

Reviewed subjects, each pinned at the blob actually read:

- `memory/builds/aPairedLexer/spec/2026-08-31-spec-TOOL-aPairedLexer-6.md@2561f4a34cc9`
- `memory/builds/aPairedLexer/spec/2026-08-31-spec-TOOL-aPairedLexer-7.md@3c3a62451064`
- `memory/builds/aPairedLexer/spec/2026-08-31-spec-TOOL-aPairedLexer-8.md@7dfb704cefca`
- `memory/builds/aPairedLexer/spec/2026-08-31-spec-TOOL-aPairedLexer-9.md@a5c7cd68ad30`
- `memory/builds/aPairedLexer/spec/2026-08-31-spec-TOOL-aPairedLexer-10.md@ff1356200ae2`
- `memory/builds/aPairedLexer/spec/2026-08-31-spec-TOOL-aPairedLexer-11.md@c46c5266957d`
- `memory/builds/aPairedLexer/spec/2026-08-31-spec-TOOL-aPairedLexer-12.md@3db4835af427`

Round 1.

## Verdict: BLOCKED

The counts sit here rather than on the heading, because that line's token is a closed set and a
tally appended to it turns a structural check into a semantic one.

Seven blocker rows over four distinct blocking defects, and they are not prose defects. Three of the
four are MEASURED admits that survive all seven units landing — the same file, the same rule, the
same fail-open direction that has now shipped four consecutive times under an all-green suite. The
fourth is a fail-open the set would INTRODUCE. If this set is built as written, the build closes the
four defects the review promoted and ships at least three new ones, which is the shape the promotion
was supposed to end.

**The load-bearing sentence.** Six of the seven specs are correct about the mechanism they name and
wrong about its EXTENT. Unit 6 names the declined slash and covers one of the two openers a declined
slash can leak, in one of the two scanners that read it. Unit 8 names the previous-token problem and
words its predicate so a member access matches it. Unit 9 names the two-views split and never says
which view detects. Unit 12 names the phantom span and then argues its safety from a real one. In
every case the ACCEPTANCE CRITERIA are written against the instance the review happened to hand over,
so none of the four could fail if the fix were wrong in the way it actually is wrong.

## Review shape

- raw 43 · confirmed 17 · refuted 26 · unverified 0 · precision 0.40
- confirmed by severity: 7 BLOCKER · 7 HIGH · 3 MEDIUM · 0 LOW
- 17 rows over **12 distinct defects** — four clusters (rows 1/26, 18/36, 6/16, 21/31) and eight singles

Precision 0.40 is BELOW the charter's ~0.5 floor (§8), and the diagnosis is not the lens count. The
refuted 26 are dominated by findings argued from spec prose alone — "this AC seems weak", "this risk
line seems optimistic" — while every one of the 17 confirmations carries either a patched-and-run
measurement or a grep that settles the question outright. Round 2, if there is one, should refuse a
finding about a verdict that arrives without a run, exactly as round 2 of the diff review did. The
lens coverage was right; the evidence bar per finding was not.

Severities here are **this report's adjudication**, not the finders' filings. The rule applied is the
one round 1 of the previous spec audit set: a measured DENY→ADMIT on `agent-cap` that the specced
change INTRODUCES or LEAVES STANDING is a BLOCKER, and so is a specced mechanism measured to damage a
committed artifact. Two filed HIGH were promoted under it — **18** (the same measured admit as **36**,
filed from a different angle, and one measurement cannot hold two severities) and **6** (unit 9's S2
read literally flips a shipped green DENY arm to ADMIT, which is the blocker rule, not a wording
nit). Nothing was demoted. **21** and **31** are kept at HIGH rather than promoted because the damage
lands on a committed inventory rather than on the fan-out guard, and this repo has no `.ts` layer to
suffer it today.

## Confirmed findings

### BLOCKER — 7

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 26 | `…-6.md` §2 S1 and §3 (non-goals), unpinned by §6 | S1 gives the declined-slash `dirty` flag to `renderCodeView` ONLY and §3 explicitly excludes `blankLiterals` — but §1 justifies that exclusion with the claim that `blankLiterals` "was given `dirty` in `TOOL-aPairedLexer-4` for exactly this reason". That claim is false. `dirty = true` occurs exactly ONCE in the file, at `agent-cap.js:650`, inside the unpaired-quote branch; it never fires on a declined slash. MEASURED at the tip: `const a = 1` / `` if (a) /`/.test(s) `` / `await boundedParallel(thunks, 50)` / `` if (a) /`/.test(s) `` gives `blankLiterals` `clean=true`, `dirty=false`, `code[2] === ""` — the call site blanked out of the view — and the hook exits **0**. The control without the two phantom lines exits **2**. After all seven units land it still exits 0, because unit 6 widens the other scanner's signal and nothing gates rules 3 or 5 on it. | Add **S1b**: `blankLiterals` sets the SAME per-line `dirty` on a declined slash, and §3 drops the exclusion. Better: land the decline signal INSIDE the shared predicate unit 8 creates, so the two views cannot answer it differently by construction. | Two arms staged RED before wiring, asserting the SIGNAL and not only the verdict: on that fixture `blankLiterals(script).clean === false`, AND the hook exits 2 naming the call-site cap of 50. Wire both into the `agent-cap self-test` leg. |
| 1 | `…-6.md` §2 S1/S2 and §3 first non-goal — no §6 criterion anywhere in the seven | The same defect measured from the rules' side, and the reason it is not a duplicate of 26: **no AC in any of the seven units observes rule 3 or rule 5 on this trigger.** With unit 6 simulated on `renderCodeView` exactly as S1/S2 word it, `const n = 1` / `` if (n) /`/.test(s) `` / `const v = byRef[finding.ref]` / repeat-trigger exits **0** (rule 5 blind) and the same wrapper around `boundedParallel(L.map(...), 500)` exits **0** (rule 3 blind); both trigger-deleted controls exit 2, and both were re-measured at the tip by this pass. Units 7 and 8 do not reach it — the `/` follows `)`, which spec 8 §3 explicitly keeps as DIVISION. The set therefore closes D3 for rules 1 and 2 and leaves the identical fail-open in the two rules that read the other scanner: D6's own shape, "fixed next door, left standing here", repeating inside the build that promoted D6. The backlog row §3 defers to (`TOOL-aPairedLexer-5`) is about scanner duplication, not about this signal. | Extend S1 to BOTH scanners, and add two acceptance criteria: the rule-5 `.ref` fixture above exits 2, and the rule-3 cap-500 variant exits 2, each with its trigger-deleted control. | One arm per RULE, not per fixture: for each of rules 1, 2, 3 and 5, a declined-slash phantom span over that rule's own offending construct must deny. That is the class arm; four fixtures of one shape is the instance arm that has now missed this four times. |
| 36 | `…-6.md` §2 S1, no §6 criterion covers it | S1 arms `dirty` only on "a backtick occurs later on that same line". The record it never cites — `TOOL-aLexedStripper-5`, which DELETED `renderCodeView`'s block-comment branch because a `/*` inside a regex literal was indistinguishable from a real one (source comment at `agent-cap.js:280-281`, restored by `TOOL-aPairedLexer-4` S2) — names the OTHER opener a declined slash leaks. MEASURED at the tip: `const a = 1` / `if (a) /x[/*]y/.test(s)` / `const r = await parallel(D.map((d) => () => agent(d)))` / `if (a) /z[*/]w/.test(s)` exits **0**; the control without the two slash lines exits **2**. Both slashes follow `)`, so units 7 and 8 leave them declined; the first leaks `/*` into block mode, the second closes it, `stack` is empty and `mode === 'code'` at EOF, no backtick appears anywhere, `dirty` never fires. A raw `parallel(` through the guard whose entire job is refusing one, after all seven units land. | S1 sets `dirty` when a DECLINED `/` is followed on the same line by any character the code path would have acted on had it not been inside the regex body — a backtick, a quote, `/*`, `*/` — not a backtick alone. The cheapest correct spelling is to scan the declined regex body's own extent rather than the rest of the line. Cite `TOOL-aLexedStripper-5` in §10; no spec in the set currently does. | Make AC2 TABLE-DRIVEN over the opener set, one row per opener, the same shape unit 8 S3 uses for its keyword class. The existing arms `rule1: a raw primitive below a regex holding a block opener still denies` and `renderCodeView: a regex-borne /* closed by a later ordinary */ denies` both write the regex after `=` — a RECOGNISED position — so they pin only the recognised case and no arm covers the declined one. |
| 18 | `…-6.md` §2 S1 against §6 AC2 and the §3 claim that the declined-slash class is closed | The same measurement as 36, filed from the coverage side and adjudicated at the same severity: S1's dirty condition is written over the delimiter the D3 fixture happened to use, so it gates the INSTANCE and the spec's title claims the CLASS. A second `node --check` clean witness, measured at the tip: `const a = 1` / `if (a) /[/*]/.test(s)` / a raw `parallel(D.map(...))` / `if (a) /[*/]/.test(s)` exits **0**, with `renderCodeView.unterminated === false` and `blankLiterals` `clean === true` — both printed — and the primitive line blanked to `""` in BOTH views. No non-goal covers it: §3 excludes only "changing which positions are regex positions", and unit 8 §3 explicitly hands the closing-bracket residual back to this unit. | As 36. The two rows share one S1 amendment; they are listed separately because each was confirmed on its own fixture and 18 is the one that shows both views gutted simultaneously. | As 36, plus one arm asserting that on a declined-slash line carrying `/*`, the two scanners AGREE — `renderCodeView.unterminated` and `blankLiterals.dirty` are both true. Disagreement between the views is the class `TOOL-aPairedLexer-5` tracks and the class that produced three of this file's four fail-opens. |
| 27 | `…-8.md` §5 (risks) and §2 S1, unpinned by §6 | §5 asserts the error direction is fail-CLOSED ("recognising a regex blanks it rather than exposing a call"). That is BACKWARDS for rules 1 and 2, and the file says so verbatim at its own `offendingLines` comment: "Seeing LESS is safe for a rule whose findings are permissions and dangerous for a rule whose findings are denials, and this is the second kind." S1 never says the trailing keyword must not be preceded by `.`, and §4 spells the predicate as "a word-boundary match on the trailing word decides". MEASURED with S1 implemented exactly that way in both scanners: `const y = obj.in / 2; parallel(items.map(f)); const z = 9 / 3` goes from exit **2** at the tip (re-measured by this pass) to exit **0**, the code view rendering as `const y = obj.in` + whitespace + `3` — the raw `parallel(` blanked out by a span running from the slash after `obj.in` to the slash in `9 / 3`. `x.of` flips identically; the keyword-free control `q.z / 2` stays at 2. `obj.in`, `x.of`, `m.delete`, `p.new`, `r.case` are all legal member accesses. Unit 6's `dirty` fires only on DECLINED slashes, so this over-ACCEPTED one is silent: a fail-open introduced by the unit meant to narrow the lexer, surviving all seven. | S1 states the predicate refuses when the keyword is preceded by `.` — a `(?:^\|[^.\w$])(return\|typeof\|…)\s*$` guard, measured to keep the fixture above at exit 2 — and §5's sentence is rewritten to say over-recognition is fail-OPEN for rules 1 and 2. | Extend AC3's table with a NEGATIVE column: for each keyword in the closed set, `obj.<kw> / 2` is DIVISION and `<kw> /re/` is a regex. AC5 pins only the closing-bracket case, which never reaches a member-access keyword. Same table, both directions — a positive-only table cannot tell the two apart, which is the lexicon rule §12 states for verb rows. |
| 16 | `…-9.md` §2 S1/S2 against §6 AC5 | S1/S2 split rule 3 across two views but never say which view DETECTS call sites, helper definitions and `gov:bounded-fanout` markers — and `capFindings` today runs ONE `code` array for detection, for `joinCall(code, i, col)` column offsets, and for `intConsts`. S2's "the fallback view is used ONLY to supply `intConsts` bindings" does settle it — everything else reads `_bl.code` — and that is what breaks. MEASURED by this pass: for the shipped arm `rule3: a cap of 500 below an unterminated BLOCK comment denies` (`agent-cap.test.sh:944`), `_bl.code` is `["const c = ","","",""]` — the call-site line is EMPTY. Detection over that view finds nothing and the arm flips deny→ADMIT, taking rule 3 blind on every non-clean script: a fresh fail-open. The other reading (detect on the fallback, join on `_bl.code`) leaves `joinCall` walking a different string at a fallback-derived column, returning null, so the arm denies for "never closes its parens" — green, and observing nothing. AC5 exempts only arms a sibling re-baselines BY NAME, and unit 10 re-baselines a different one. | State the interface in S1: detection, `intConsts` and the `lines[]` reporting stay on the fallback view; `joinCall`/`topLevelArgs` RE-DERIVE their own match column in `_bl.code` rather than reusing the fallback column; and a call site the paren-safe view cannot show at all is a DENY naming the ambiguity, never a dropped finding. | An AC — and an arm — for a call site inside a span `_bl.code` blanks (the unterminated block-comment arm above), asserting it still denies AND that the message still names the width. A deny whose message lost the number is the hollow-arm case, and only asserting the message text separates the two. |
| 6 | `…-9.md` §2 S2 versus §6 AC5 | The same contradiction from the second shipped arm, and the reason it is a separate row: it shows §2 and §6 of ONE spec requiring opposite things, with the builder left to resolve it at build time and no recorded decision either way. `blankLiterals` emits nothing while in `tmpl` mode, so for `rule3: a cap of 500 below an unterminated backtick denies` (`agent-cap.test.sh:936`) `_bl.code` blanks every line after the open backtick and `HELPERS.exec` finds no call site at all — green→red, which AC5 forbids. Resolving toward S2's literal wording is a new fail-open; resolving toward AC5 silently narrows S2 to something it does not say. | Say which view supplies the scan LINE SET, in S2's own words. The intended split — fallback view supplies the lines and the `intConsts` bindings, `_bl.code` supplies `joinCall`/`topLevelArgs` for an argument on a line already found — is not what S2 says today. | An AC naming the `unterminated backtick` arm as the one that pins the line-set decision, so a build that resolves the contradiction the other way reds instead of shipping. |

### HIGH — 7

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 4 | `…-6.md` §6 AC2; same gap at `…-7.md` AC2, `…-8.md` AC3, `…-11.md` AC3 — no §2 in the set scopes it | Four criteria call internal functions directly (`renderCodeView(...).unterminated`, "the code view of `return /re/`", "`fanoutFindings` and `capFindings` resolve the SAME const table"), and there is no seam to call them through. Verified: `tools/hooks/agent-cap.js` has no `module.exports` and no `exports.`, `main()` is called unconditionally at the bottom so even a `require` would consume stdin and exit, and nothing in the repo requires it — `agent-cap.test.sh` only pipes a JSON payload through `node "$HOOK"`. These four are exactly the criteria each spec nominates as its durable half: "pins the SIGNAL, not only the verdict", "survives a rewrite of the heuristic", "so the two cannot diverge again". With no seam the build either invents one unscoped or quietly downgrades each to an end-to-end verdict arm — which is the instance arm that failed to see D2. | Put the seam in unit 6's §2 (it lands first, order 5): a `require.main === module` guard plus an exports block, or a `--selftest` argv branch, named in §7. Then each of the four ACs states the command that observes it. Note that the seam is owed in BOTH copies, or the mirror-drift arm reds. | Once the seam exists, every "structural" AC in the set states its invocation verbatim. A criterion that names no command is not a criterion, and this set has four of them. |
| 38 | `…-6.md` §5 (risks) and §6 AC4 | §5 prices over-triggering `dirty` as "PRECISION only … Fail-closed by construction", but the per-line view it routes rule 1 back to is the view a CLOSED backlog row (`memory/backlog/TOOL.md:271`, `TOOL-aLexedStripper-3`, "landed as `TOOL-aPairedLexer-2` … prose in a lens prompt admits") records as the false-positive source unit 2 was built to remove. MEASURED with unit 6 simulated per S1/S2: a line reading `const label = args.n / args.total +` then a backtick-quoted `%` / a `PROMPT` template naming `parallel(` in prose / `await boundedParallel([a, b], 5)` flips ADMIT→DENY. The trigger is not exotic — it is ordinary DIVISION followed by a template on one line — and `dirty` is file-scoped, so one such line anywhere routes the WHOLE script to the per-line view. §4's "the change cannot regress in either direction" is true against 1.9 and false against the tip it lands on. | Carry that script as an AC with its verdict DECIDED: if it is to DENY, record it as a partial re-baselining of `TOOL-aLexedStripper-3`/`TOOL-aPairedLexer-2` and supersede the closed row; if it is to ADMIT, S1 must scope `dirty` to lines where the declined slash actually leaks an opener into code mode. Either answer is defensible; the absence is not. | AC4 as written ("a lens prompt … and no ambiguous slash") takes the CLEAN-view path and passes whether or not the precision claim holds — a control that cannot fail, in the unit whose premise is fail-opens under an all-green suite. Replace it with a fixture that reaches the new fallback. |
| 3 | `…-6.md` §5 (risks) and §6 AC4 — no criterion for the false-deny direction | The same gap measured on a second, more ordinary script: a `LENSES` array whose prompt names a primitive, plus `` log(`progress ${done / total}`) `` on one line, plus `boundedParallel(LENSES.map(...), 5)` — exit **0** at the tip, exit **2** after simulated unit 6. Listed separately from 38 because it shows the trigger arriving through a template INTERPOLATION rather than through a concatenation, i.e. the shape a real workflow writes without thinking. The only shipped arm that would catch this class is `rule1: after a closing bracket a slash is division`, and I read it: its division is on line 2 and its backtick on line 3, so it survives by an accident of line layout, not by design. | As 38 — one AC in the false-DENY direction, with the class of legal script that now reds named explicitly if the answer is "deny". | Move the accident into a criterion: an arm with the declined slash and the backtick on the SAME line, which is the layout `dirty` keys on. Pin it as the precision control for `TOOL-aPairedLexer-2`'s win. |
| 19 | `…-10.md` §6 AC3, against `…-9.md` §2 S1 (which lands first, order 8) | AC3 pins the max-merge on the shipped arm `rule3: prose inside a template cannot lower a real cap` (`agent-cap.test.sh:1048`), but unit 9 lands first and stops that fixture reaching the merge. MEASURED by this pass on the arm's real bytes — its template is UNTERMINATED, which is load-bearing and which the arm's own comment says is load-bearing — `_bl.code` is `` ["const K = 500","const L = [{a:1},{a:2}]","const doc = `","",""] ``: the `boundedParallel(..., K)` line is empty in the view unit 9 routes joins to. After unit 9 the arm denies because the call site cannot be joined, never because 500 beat the prose 5. The sibling max arm `rule3: a const inside a block comment cannot lower a real cap` measures identically, so after unit 9 NO arm exercises the max-merge — the one half of unit 10 that is not a behaviour inversion ships with nothing that can fail if it is dropped. Unit 10 §3 names unit 9 explicitly, so the ordering was in view. | Give AC3 a fixture whose call-site line SURVIVES `_bl.code`: the ambiguous-position trigger plus a real `const K = 500` and a prose `const K = 5` inside a CLOSED template — the shape unit 9's own AC1 fixture uses. Assert the RESOLVED VALUE is 500, not merely that the script denies. | Every deny-expecting AC in this set asserts an exit code; two of them are already green for the wrong reason. Make the max-merge arm assert the denial MESSAGE names 500. Rule order and parse failure both produce a bare `2`, and only the text separates them. |
| 37 | `…-11.md` §6 AC1 | AC1 borrows the phrase "an ambiguous-position declined slash" from `…-9.md` §4, but rule 2's fallback keys on `renderCodeView.unterminated` while rules 3 and 5 key on `blankLiterals`' `dirty` — two different mechanisms behind one phrase — so AC1's stated verdicts are false for every fixture the phrase admits except one. MEASURED at the tip and under a simulated 7+8: (a) D6's shape with unit 9 §4's stated trigger `if (a) /won't/.test(s)` exits **2**, not the `0` AC1 asserts — `renderCodeView` treats an unpaired `'` as an ordinary character, never enters a mode, and its block branch blanks the fabricated `const K = 5`; (b) the review's ORIGINAL D6 fixture (a `return`-position regex matching a markdown fence, three backticks inside the literal) exits 0 at the tip but **2** under the simulated patches, so "under both lexer patches it exits 0" fails for it too; (c) only a trigger carrying a BACKTICK reproduces — 0 at the tip, 0 under the patches. A builder who writes the fixture the sibling specs prescribe gets a green DENY that proves nothing about D6. | AC1 names the trigger literally as an ambiguous-position declined slash HOLDING A BACKTICK, with the measured verdicts for that exact fixture (tip 0, 1.9 0, 1.10 0, under 6+7+8 0). §4 adds one line stating WHY unit 11's trigger differs from units 9's and 10's: rule 2 needs `renderCodeView` unterminated, rules 3 and 5 need `blankLiterals` dirty. | Every fixture in this set that relies on a mis-lex carries its own trigger-deleted control AND its own before/after exit codes in the spec. A deny-expecting arm that denies at BASE too is the "green and evidence of nothing" case, and only the paired before/after separates it. |
| 21 | `…-12.md` §3 (first bullet) and §4, against §1's own premise | The safety argument — "a definition cannot live inside a template literal, so blanking there can only ever remove noise" — is about a REAL template, while §1's premise is that the span is PHANTOM, opened by a regex-borne backtick, and therefore holds LIVE CODE. `map_lib.py`'s own `render_comment_free` docstring records the price of blanking a live region verbatim: eight real definitions removed from this repo's own tracked JavaScript. Its first paragraph documents the mutual-exclusion class — comments and strings exclude each other and no sequence of independent passes can say so — and S1 adds a comment-only pass inside the span with no string tracking. Implemented as written on `` export const RX = /`/; `` / `const s = "/*";` / `export const KEEP = 1;` / `const t = "*/";` / `` export const T = `x`; ``, `KEEP` is present at the tip and LOST. `scan_js_definitions` reads this repo's own `.js` layer, so §4's "nothing in this tree would notice" is wrong about the loss direction even though it is right about the ghost. | Rewrite the §3/§4 argument to address the PHANTOM case, and either blank only LINE comments inside the span or track strings within it. | An AC gating the class rather than the fixture: `enumerate_exports` plus `scan_js_definitions` over this repo's tracked `.js` produce a symbol set IDENTICAL to the pre-change run. That makes the recorded eight-definition regression runnable instead of remembered. |
| 31 | `…-12.md` §3 (non-goals) and §5 (risks), unpinned by §6 AC3 | The same premise failure measured through the extractor. With S1 implemented as §4/§10 describe it — the code path's blanking, one branch over, recursed into the closed span — `` export const RX = /`/; `` / `const R = /a\/*b/;` / `export const REAL = 1;` / `const x = 2; /* real */` / `` export const T = `x`; `` yields `['REAL','RX','T']` at the tip and `['RX','T']` under S1: a real export lost to a regex-borne `/*` inside the phantom span, balanced by a real `*/` also inside it. The unit extends the file's existing ceiling 1 INTO template spans while claiming it cannot lose anything. §6 cannot see it — AC4's line count is preserved (5 vs 5), AC3's two ceilings are untouched, AC1/AC2 look only at `GHOST`. | Add an AC: a statement-leading `export` inside a phantom span is still enumerated (that fixture returns `['REAL','RX','T']`). Then either bound the in-span blanking to LINE comments, or keep the block arm and DECLARE the new loss as a ceiling in the same arm where ceilings 1 and 2 are declared. | `test_enumerate_exports_regex_borne_comment_opener` already names the class in its own title and pins two of its directions. Complete it to a table with one row per direction — ghost added, definition lost to a line comment, definition lost to a block comment — so the arm's name and its coverage stop disagreeing. |

### MEDIUM — 3

| # | Where | Claim | Fix | Left-shift gate |
|---|-------|-------|-----|-----------------|
| 9 | `…-8.md` §2 S2 — AC4 does not observe it | S2 requires BOTH scanners to call the shared predicate; AC4 only asserts `grep -c` finds the keyword list exactly ONCE — equally true when `renderCodeView` calls the predicate and `blankLiterals` keeps the bare character test. Simulated both ways: AC1 and AC2 are rule-1/rule-2 shapes and pass on the one-scanner patch, AC3 observes "the code view" (singular), AC5 is a rule-1 division control. The divergence is real and measured: `` function isFence(l) { return /^`/.test(l) } `` / `const L = [{a:1},{a:2}]` / `boundedParallel(L.map(...), 500)` / repeat-trigger exits **0** with the one-scanner patch and **2** with both — rules 3 and 5 reading a phantom template while rules 1 and 2 see the regex blanked. That is the class §4 calls "load-bearing, not tidiness", unobserved by the criterion written to enforce it. | Supplement AC4 with an AGREEMENT assertion: for a script containing `return /re/`, `renderCodeView` and `blankLiterals` both blank the literal — the same shape spec 11 AC3 uses for the const table, applied to the scanners. | Generalise it: one arm asserting the two scanners agree on a corpus of regex-position fixtures. `grep -c` counts copies of a STRING; only a run counts copies of a BEHAVIOUR, and every fail-open in this file so far has been a behaviour copy. |
| 10 | `…-10.md` §2 S2 (the `\b(?:const\|let\|var)\s+(\w+)\s*=` sweep) — no §6 criterion | Every AC is satisfied by S1's union/delete rule alone, so S2 can be omitted entirely and nothing reds. Measured on AC1's fixture shape: `consts(_bl.code)` is `[]` and `consts(fallback)` is `[["K",5]]` — K is bound by ONE view, so S1 deletes it and the script denies without the sweep contributing. AC2 is the clean-view control, AC3 is S1's max rule, AC4 is S3, AC5 pins the clean path, AC6 is the suite. S2 is nonetheless separable behaviour: a name BOTH views bind, which the clean view also saw in an unresolvable declaration, survives S1 and dies under S2. An unimplemented S2 ships as "done". | Add an AC whose fixture has a real `const K = args.width` OUTSIDE any mis-lexed span (so the clean view has SEEN the name but bound nothing) and the prose integer inside one, asserting the denial names K as unresolvable — and confirm it fails with S1 alone. | The general rule this instance is a case of: every scope bullet in a spec owes at least one criterion that fails when only THAT bullet is dropped. Three of the four scope bullets in this unit have one. |
| 30 | `…-10.md` §5 (risks) | "The precision cost is bounded by how often a view is not clean, and `-6` widens that deliberately" assumes unit 6's flag reaches this unit's gate. It does not: `-6` widens `renderCodeView.unterminated`, while this merge runs under `if (!_bl.clean)` — a different signal on a different scanner, and unit 6 §3 says so in as many words. MEASURED on the balanced declined-slash phantom: `_bl.clean === true` while unit 6's `renderCodeView.unterminated === true`, so `-6` adds exactly ZERO to the population this merge runs over. The bound is computed against the wrong population, and the sentence actively conceals finding 26 — a reader checking "is the `blankLiterals` side covered?" is told it is being widened deliberately. | Name `_bl.clean` in the risks line and state the real widening: none, unless spec 6 gains the `blankLiterals` half. If spec 6 is amended per 26, re-derive the bound against `blankLiterals` and say so. | Not gateable, so it joins the documented checklist (§7/§10): **a risk line that names a mechanism names the SIGNAL and the FUNCTION it is read in.** Three separate rows in this report are one signal being confused with the other. |

## What was attacked and held

Stated because a refuted attack is evidence, and 26 of the 43 raw rows were refuted.

- **The trigger-survival claim (attack a) holds for units 9 and 10 and fails for unit 11.** Units 9 §4
  and 10 §4 are correct that an ambiguous-position trigger survives units 7 and 8, and I re-measured
  it. Unit 11 inherits the phrase without inheriting the mechanism, and that is finding 37 — the only
  place the claim breaks, and it breaks completely.
- **Unit 10's deliberate re-baselining (attack b) is SOUND.** S3 inverts `rule3: an exposed const
  resolves the cap and the script admits` from ADMIT to DENY, and the justification survives:
  under S1 the clean view binds nothing there, so the cap is resolved out of a view the file has
  already declared untrustworthy, which is the fail-open direction. §4 names the arm, records the
  reason, and keeps the arm rather than deleting it — the treatment `TOOL-aPairedLexer-4` gave the
  block-comment ceiling. This is the one inversion in the set and it is not merely convenient. What
  is NOT sound is AC3, the arm meant to hold the other half steady — finding 19.
- **The unit 6 / units 9-10-11 combination (attack c) is INCOHERENT, and the incoherence is
  finding 30's, not a fourth defect.** Unit 6 widens `renderCodeView.unterminated`; units 9, 10 and 11
  gate on `blankLiterals.clean` for rules 3 and 5, and on `renderCodeView.unterminated` for rule 2.
  The two populations are disjoint on exactly the shape this build exists to close, so unit 6 widens
  the rule-3/rule-5 fallback population by zero. Unit 6's precision cost is therefore real and
  UNBOUNDED in the direction §5 dismisses (findings 3 and 38), and its coverage is zero in the
  direction §5 claims (findings 1, 26, 30).
- **Unit 12's no-regex-model argument (attack d) does not survive.** It is a correct argument about
  a real template and the unit exists for a phantom one; findings 21 and 31 measure both directions
  of the loss it says cannot happen. The unit's GHOST half — the defect it was promoted for — is
  correctly specified and its AC1/AC2 pair does observe it.
- **Unit 7 is the healthy one.** `let prev = ';'` is a one-character fix, its §4 explains why an
  instance arm is not enough, AC3 is a real isolation control and AC4 stages the mirror-drift arm
  RED before assuming it. Its only confirmed row is the shared AC-seam gap (finding 4), which is not
  about its fix.
- Refuted classes worth naming, so round 2 does not re-file them: the keyword list in unit 8 §4 IS
  closed and correct for the statement positions it claims; unit 9's choice between the review's two
  candidate fixes is defensible and §8 states why; unit 11's S3 agreement arm is the right SHAPE even
  though it has no seam to run through; and the seven `order` values are consistent with each other
  and with every §3 cross-reference.

## Cross-spec agreement (M2)

M2 requires the set to agree with itself on interfaces, ordering and acceptance. It does not, in
three places, and all three are already rows above rather than separate findings:

1. **Interface** — unit 9 §2 leaves the view→responsibility mapping unstated, and units 10 and 11
   both build on the mapping it does not state (findings 16, 6, 19).
2. **Ordering** — the order values are internally consistent (6, 7, 8, 9, 10, 11, 12 at orders 5-11)
   and unit 6 §4 correctly identifies itself as the repair that survives units 7 and 8. But unit 10
   AC3 pins a fixture unit 9 invalidates one order earlier, and unit 11 AC1 pins verdicts taken on a
   fixture units 7 and 8 invalidate three orders earlier (findings 19, 37).
3. **Acceptance** — four ACs across three specs assume a test seam no spec creates (finding 4), and
   the phrase "an ambiguous-position declined slash" is used across units 9, 10 and 11 to mean two
   different mechanisms (finding 37).

The seven specs share one research prompt and one `Also serves` fan, so the cross-references are
complete; what is missing is agreement on what the words in them mean.

## Round 2 exit condition

Round 2's exit condition is a strictly falling confirmed-blocker count. **This round sets the ceiling
at 7**, over four distinct defects. Under BUILD-METHOD M4 a second non-convergent round on the same
spec set does not promote further units — the specs are amended in place and re-audited, because a
spec defect has no shipped artifact to promote.

Two amendments would close five of the seven blocker rows: giving unit 6's decline signal to BOTH
scanners over the FULL opener set (rows 26, 1, 36, 18), and stating unit 9's view→responsibility
mapping in S1 (rows 16, 6). The remaining blocker, row 27, needs one clause in unit 8 S1 and one
corrected sentence in its §5.
