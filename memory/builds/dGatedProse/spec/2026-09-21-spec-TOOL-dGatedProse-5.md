# TOOL-dGatedProse-5 — the corpus answers for its readers before the check that asks

**Status:** SPECCED · rev-2 · 2026-09-21 · node d · Tier-2 · base fcbfba5f · streams tooling · order 1

<!-- gen:spec-records -->

*No record names this unit.*

<!-- /gen:spec-records -->

## 1. Goal

Every live section-2 item that `TOOL-dGatedProse-1`'s trigger fires on gains its reader clause in
this unit's commits, whichever node owns the spec, BEFORE the check that grades one exists. The
owner ruled on 2026-09-21 that the residual red unit 1's F8 records is fixed now rather than
scheduled, and the ordering is what makes that free: this unit is order 1 and unit 1 is order 2, so
a clause written here is additive prose that grades nothing, and check 25 arrives onto a corpus its
own predicate already passes. The unit is several commits, one per owning node as the collision plan
in section 4 lays out, and they land together as this unit.

The precedent is `TOOL-aTetheredRecord-3`, which brought 76 records into conformance while its
parser sat inert for exactly this reason. This unit is that shape with one difference the collision
plan has to carry: almost every file belongs to another build, on nodes `a`, `b` and `d`, and three
of the fourteen specs are INPROGRESS.

## 2. Scope (IN)

- **S1** — the POPULATION, re-derived at the build pass and never read from this spec. A probe
  implementing unit 1 rev-3's trigger, with its verb match case-insensitive across the strict list
  and the stems alike, runs against the tree at the pass's own base, prints its item set, and prints
  the two-way difference against the section-4 census: items that entered the population since
  2026-09-21 and items that left it. The write set is the DERIVED set. Observed by AC1.
- **S2** — the CLAUSE on every derived item, with all three markers present and exactly one clause
  per item. A second clause on one item is a defect rather than redundancy, because both halves are
  then graded twice with two different answers. Observed by AC2 and AC9.
- **S3** — the RESOLUTION of every by-name token the pass writes, against the tracked tree outside
  `memory/builds/*/spec/`, with the exclusion pathspec verified rather than trusted. Unit 1 measured
  that trap: the pathspec it wrote returned 2072 files against 1556 tracked non-spec files, and an
  exclusion that silently fails makes every name resolve against the spec corpus that named it.
  Observed by AC3.
- **S4** — the GENUINE set. Section 4 names fifteen items where something the item withdraws is read
  in this tree, found by reading the code, and each of those items receives the readers named there,
  by bare path or symbol. Three of them are the items the original dry run named, and they are the
  reason this class was ruled gateable at all. Three more are items rev-1 answered with an escape
  that a skeptic showed to have readers, which is the direction no shape check can catch afterwards.
  Observed by AC5 and AC12.
- **S5** — the ESCAPED set. Section 4 names eight items whose honest answer is an escape spelling,
  and each receives that spelling followed by its reason on the same half. A bare escape is a
  refusal, not an answer. Observed by AC4.
- **S6** — the item that already carries a clause, `TOOL-dGatedProse-3` S8, is VERIFIED and not
  rewritten. It was written as voluntary and the trigger fires on it in fact; its halves are graded
  either way, so the pass checks them and leaves the bytes alone. Observed by AC7.
- **S7** — the COLLISION protocol and the terminal-status exclusion, stated as a procedure the pass
  follows rather than a hope. An item whose label has gone, whose text no longer fires, or whose
  spec has reached a terminal status leaves the write set and is recorded as left out. Observed by
  AC1 and AC8.
- **S8** — the CENSUS record under this build's `build/` folder: the probe's output, the per-item
  verdict, the reader evidence by path, and the two-way difference of S1. It is the provenance for
  every clause a reviewer will read as an assertion. Observed by AC1 and AC6.
- **S9** — the RECIPROCAL edge. The order re-stamp was made at spec time, when the main loop
  renumbered the build on 2026-09-21, so no sibling header is this unit's edit. What this unit's own
  **hands-off** still needs is its mirror on unit 1's file, one **consumes-from**
  `TOOL-dGatedProse-5` bullet. The pass reads unit 1's edges section at its base and, only where the
  bullet is absent, writes it with the revision entry that records it, in the node-`d` commit.
  Observed by AC10 and AC11.

## 3. Non-goals (OUT)

- **No check, no arm, no fixture, no engine edit.** Check 25, its `fail 25` branch, the hoist, the
  fourteen fixtures and the catalog entry are unit 1's whole scope. This unit writes markdown into
  spec files and touches no executable.
- **No completeness claim.** A clause this unit writes names the readers it FOUND. It never asserts
  the list is exhaustive, and section 4 measures why: the control revision `TOOL-dLoggedFlight-22`
  listed three readers fewer than it had and every name it did list resolved, so a resolution-graded
  clause passes a spec with the defect this whole strand exists to catch.
- **The NAMED GAP is not closed.** Unit 1 measured eight live items whose retirement the trigger
  cannot see, one of them genuine. They get no clause here. A clause is graded wherever it appears,
  so writing one on an untriggered item buys a graded obligation with no refusal behind it, and the
  gap belongs to whatever predicate can reach it.
- **No repair of the specs this unit edits beyond the clause.** Several of the items describe work
  that has since landed, so their line citations are stale and their status tokens are arguably
  wrong. Correcting any of that mixes a mechanical insertion with judgement about another node's
  build and makes the pass unreviewable. `TOOL-aTetheredRecord-3` drew the same line in its own
  section 3 and it held.
- **No waiver registry.** F8's third option was a drainable handoff registry beside the arm, one row
  per file-and-item pair. The owner chose the corpus pass, so nothing here builds a registry, and
  `memory/project/spec-token-waivers.txt` stays the only mechanism of that shape in the tree.
- **Unit 1's section-4 census tables and its S14 are not rewritten here.** Both are superseded by
  this unit, and folding them is unit 1's own revision. This unit touches at most one edge bullet of
  that file and the revision entry recording it, and no prose.

### Edges

- **hands-off** `TOOL-dGatedProse-1` — a corpus its trigger already passes, so its S14 collapses to
  a verification. This unit writes against that unit's rev-3 TEXT — the closed verb list, the five
  identifier shapes with their five exclusions, the six kind nouns, and the clause grammar with its
  two escapes — plus the ruling of 2026-09-21 that the verb match is case-insensitive for the strict
  list too, which unit 1 states only for the stems; declaring it there is that unit's fold. The text
  exists before either unit is built, so the relation is this hand-off to a later order and not a
  **consumes-from**, whose target would run after this one. The reciprocity join prints on the
  DECLARING file at `tools/memory-tree/check-memory-hygiene.sh:1800`, so until unit 1 carries
  **consumes-from** `TOOL-dGatedProse-5` the red lands on THIS spec and not on that one; S9 is the
  backstop that closes it.
- **hands-off** external — the eight items of unit 1's named gap, and the completeness half no
  predicate in this build grades. Both are recorded as open classes rather than work.

## 4. Design

### The population, re-derived rather than inherited

Three populations nest, and the outer two are unit 1's inheritance rather than this unit's choice:
check 25 grades check 12's selection, filtered to LIVE. Liveness is the engine's own negative test
at `tools/memory-tree/check-memory-hygiene.sh:1385`, `hdr !~ /^\*\*Status:\*\* (CLOSED|WONTDO)/`,
and not the positive whitelist at `tools/check-spec-tokens.py:87`, which excludes `DEFERRED` and
would drop `dScaffoldedMirror-9` out of this pass.

**OWN-PROBE, 2026-09-21, at base fcbfba5f.** The probe is `probe_pop.py`, written to this session's
scratchpad and UNTRACKED, so nothing in the tree resolves it and the figures below are PINNED to
that base. Every one of them is re-derived at the build pass under S1.

| population | size | derivation |
|---|---|---|
| tracked spec-shaped files | 642 | check 12's own path regex |
| at or after `SPEC_FORMAT_CUTOFF` 2026-07-15 | 640 | check 12's selection, inherited whole |
| LIVE within that selection | 37 | the `:1385` liveness test |
| triggered items | 23 across 14 specs | unit 1 rev-3's shipped trigger |
| node `a` | 15 items across 10 specs | the status header's own `node` field |
| node `b` | 2 items across 1 spec | the same |
| node `d` | 6 items across 3 specs | the same |

**Re-derived at rev-2, 2026-09-21, at 32f2eb71**, by `probe_pop_rev2.py` in the same scratchpad,
under the main loop's ruling that the verb match is case-insensitive: every figure in the table
stands, 23 items across 14 specs. The case-sensitive reading of the strict list gives 22 across 14,
and the one item it moves is `dPolishedVitrine-1` S5, whose verb is the capitalised `DROPS`. A
skeptic's independent probe reproduced both figures and the same single-item difference. So the
ruling puts that item IN, and the reason is the corpus's own habit: it writes a retirement verb in
capitals for emphasis on the items that matter most, and a case-sensitive trigger would pass those
silently, which is the false-pass direction a shape check exists to close.

### Reconciling 23 against unit 1's 22, because the difference is the whole argument for S1

Unit 1 rev-3 states 22 items across 13 specs, split as 5 node-`d` items and 17 items in eleven
node-`a` and node-`b` specs. This probe agrees with the second figure EXACTLY: 15 node-`a` items in
ten specs plus 2 node-`b` items in one spec is 17 across 11, item label for item label. It finds one
more node-`d` item, `TOOL-dGatedProse-3` S8.

Two further agreements make the divergence a single item rather than two implementations disagreeing.
The imperative stems add exactly 10 items over the strict list, which is the delta unit 1 states. And
rev-3's space-bearing exclusion drops exactly the two items unit 1 names, `aBatchedLintel-1` S3 and
`aMendedLedger-6-u6` S5, with no third.

**Why the one item moved, verified rather than assumed.** `TOOL-dGatedProse-3` S8 fires on the strict
phrase `is replaced by`, which sits in its sentence about the kit-version constant. That phrase
entered that file in commit `c7750bf8` — the SAME commit that landed unit 1 at rev-3. So 22 was true
when unit 1 measured it and 23 by the time it was committed. Unit 1's own edges section reaches the
same item from the other side and calls its clause optional, on the ground that `retired` is in
neither the strict list nor the stems; that is true of `retired` and misses `is replaced by` four
words earlier in the same item.

The corpus cost of the difference is zero, because that item already carries a clause. What changes
is the count, and the claim that the clause there is voluntary. One commit moved the population under
a figure published in that same commit, which is the entire case for deriving the write set at the
pass instead of reading this table.

### The enumeration, every item, with its verdict

`GENUINE` means something the item withdraws — a name, a file, a row or a value — is read in this
tree, and the clause names those readers; section 4 names them below. `ESCAPE` means nothing the
item withdraws is read here. The escape spelling is a factual claim about the tree and not a
classification of the sentence, so an item a reader would call a false trigger still answers with
its readers wherever readers exist.

| spec | node | status | item | the retired thing | verdict |
|---|---|---|---|---|---|
| `aGradedDoorway/spec/2026-08-29-spec-TOOL-aGradedDoorway-7.md` | a | INPROGRESS | S2 | `SHARD_ARITY`'s value of 2, raised to 8 | GENUINE |
| `aQuarriedLantern/spec/2026-08-03-spec-aQuarriedLantern-1.md` | a | INPROGRESS | S6 | nothing — runtime cache eviction | ESCAPE |
| `aMendedLedger/spec/2026-08-09-spec-aMendedLedger-1.md` | a | SPECCED | S1 | the three `.md` stubs under `memory/project/` | GENUINE |
| `aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-2-u1-journal-relocation.md` | a | SPECCED | S5 | four paths under `memory/project/` | GENUINE |
| `aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-3-u2-ledger-retirement.md` | a | SPECCED | S2 | `memory/project/in-flight/` and its keeper | GENUINE |
| `aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-3-u2-ledger-retirement.md` | a | SPECCED | S3 | `signal_ledger`'s pin of 4 | GENUINE |
| `aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-3-u2-ledger-retirement.md` | a | SPECCED | S6 | the ledger lens of a review harness | GENUINE |
| `aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-4-u3-hygiene-gate.md` | a | SPECCED | S3 | two session-ledger lines of `index_set()` | GENUINE |
| `aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-4-u3-hygiene-gate.md` | a | SPECCED | S4 | two alternatives of the `ex7` selector | GENUINE |
| `aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-4-u3-hygiene-gate.md` | a | SPECCED | S7 | the session machinery of the scaffolder | GENUINE |
| `aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-5-u5-merge-driver.md` | a | SPECCED | S5 | nothing — a merge case names the verb | ESCAPE |
| `aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-6-u6-doc-truth.md` | a | SPECCED | S11 | a playbook version marker and one sentence | GENUINE |
| `aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-7-u8-keyed-corpus.md` | a | SPECCED | S5 | nothing — two test arms name the verb | ESCAPE |
| `aMendedLedger/spec/units/2026-08-10-spec-aMendedLedger-8-u9-driver-redesign.md` | a | SPECCED | S2 | `split_regions` and its three companions | GENUINE |
| `aMendedLedger/spec/units/2026-08-10-spec-aMendedLedger-8-u9-driver-redesign.md` | a | SPECCED | S11 | the reach of a node-local `merge.conflictStyle` value | GENUINE |
| `bConvergentLodestar/spec/2026-07-22-spec-bConvergentLodestar-1.md` | b | SPECCED | S2 | nothing — an artifact churn condition | ESCAPE |
| `bConvergentLodestar/spec/2026-07-22-spec-bConvergentLodestar-1.md` | b | SPECCED | S3 | `reuse-discovery.js`, in another repo | ESCAPE |
| `dGatedProse/spec/2026-09-20-spec-TOOL-dGatedProse-3.md` | d | SPECCED | S8 | the kit-version constant's value | GENUINE |
| `dPolishedVitrine/spec/2026-09-12-spec-TOOL-dPolishedVitrine-1.md` | d | INPROGRESS | S5 | two rows of a carried-prefix registry | GENUINE |
| `dPolishedVitrine/spec/2026-09-12-spec-TOOL-dPolishedVitrine-1.md` | d | INPROGRESS | S10 | nothing — a runbook block's own cleanup | ESCAPE |
| `dScaffoldedMirror/spec/2026-08-24-spec-dScaffoldedMirror-9.md` | d | DEFERRED | S5 | three `*_OFFENDER_PIN` keys and their holes | GENUINE |
| `dScaffoldedMirror/spec/2026-08-24-spec-dScaffoldedMirror-9.md` | d | DEFERRED | S6 | 70 lines of comment archaeology | ESCAPE |
| `dScaffoldedMirror/spec/2026-08-24-spec-dScaffoldedMirror-9.md` | d | DEFERRED | S7 | nothing — a new verb's runtime behaviour | ESCAPE |

Paths are relative to `memory/builds/`, because the full ones do not fit a table cell at this width
and the prefix is the same for all 23.

Fifteen GENUINE and eight ESCAPE. The difference from unit 1's reading runs in both directions and
is not a disagreement about the trigger. Three items unit 1 called innocent or borderline —
`aGradedDoorway-7` S2, `aMendedLedger-1` S1 and `aMendedLedger-8-u9` S11 — have readers that
resolve, so an escape on any of them would be a false statement check 25 passes forever. Two items
unit 1 did not name among its innocents — `aMendedLedger-5-u5` S5 and `aMendedLedger-7-u8` S5 —
fire on a verb that names a git merge case and two test arms, and withdraw nothing. Unit 1 was
counting sentences a reader would call false positives; this pass records what each item's honest
ANSWER is, which is a claim about the tree. One GENUINE item, `aMendedLedger-8-u9` S2, still answers
its by-value half with `NO VALUE READERS`, because what it withdraws had names and no value.

### The genuine set, and the readers found by reading the code

Each row was verified against source at writing time, and rev-2 re-verified at 32f2eb71 every
citation it touched. Where a retirement has already landed, the readers are the sites that survived
it, which is what makes them findable at all.

**`aMendedLedger-1` S1, `aMendedLedger-2-u1` S5 and `aMendedLedger-3-u2` S2 — the `memory/project/`
drain.** S1 and u1 S5 are one retirement written at two levels. The master's "three `.md` stubs"
are, by its own table at `memory/builds/aMendedLedger/spec/2026-08-09-spec-aMendedLedger-1.md:170`
to `:172`, `MEMORY.md`, `IN-FLIGHT.md` and `project/README.md` under `memory/project/`, which are the
files u1 S5 deletes by path, so the two items get one answer. The trigger firing on S1's build slug
rather than on the stubs decides where a clause is required, not what it says. By name:
`tools/memory-tree/check-memory-hygiene.sh:497`-`:498` is check 3's own comment and enumerates
exactly the retired members; `:502` classifies every `project/` entry as a file or a directory, and
the directory classification is what u2 S2's keeper file was holding open; the closed case opening
at `:510` names none of them, so a tree still carrying one reds.
`tools/memory-tree/check-memory-hygiene.test.sh:1855` asserts the scaffolder writes none of the five
retired names, and `memory/archive/ledger/README.md:12` and `WIRE-INTO-PROJECT.md:315` name
`IN-FLIGHT.md`. By value, for the three stubs: their CONTENT moved and is read where it landed.
`WIRE-INTO-PROJECT.md:321` tells an adopter to fold `IN-FLIGHT.md`'s protocol prose into the archive
README, which carries it under its own heading, and
`memory/builds/bThriftyBellows/build/2026-07-16-build-bThriftyBellows-1.md:5` carries the index
digest folded there when `MEMORY.md` retired. By value, for u2 S2's directory:
`tools/drift-audit/drift_report.py:2031` reads the rows under `project/in-flight/`, and
`tools/drift-audit/drift_signals.py:133`-`:139` declares that probe's population empty because the
directory is gone.

**`aMendedLedger-3-u2` S3 — `signal_ledger`'s pin of 4.** One of the three the dry run named. By
name: `tools/drift-audit/drift_signals.py:139` carries the signal in `DECLARED_EMPTY` and
`tools/drift-audit/drift_signals.py:201` opens the `PINS` table whose comment records that this
signal now carries no pin; `tools/drift-audit/drift_report.py:2156` and
`tools/drift-audit/drift_report.py:2189` both read `DECLARED_EMPTY`, and
`tools/drift-audit/drift_report.py:2060` copies `PINS` off the project layer;
`tools/drift-audit/selftest.py:280` arms a `DECLARED_EMPTY` fixture and
`tools/drift-audit/selftest.py:676` rewrites a `PINS` row to test pin semantics. By value: a pin
absent from `PINS` falls back to the bare tolerance, so the comparison a run makes changes VALUE and
not merely label — `tools/drift-audit/drift_report.py:1364` states that fallback in terms.

**`aMendedLedger-3-u2` S6 — the ledger lens of `tools/workflows/drift-audit-state.js`.** One of the
three. By name: `skills/session-kickoff/SKILL.md` is not its reader, but four others are —
`tools/drift-audit/SKILL.template.md:62` and the rendered `.claude/skills/drift-audit/SKILL.md:62`
both tell a session to narrow that harness's lens list, `tools/check-kit-versions.sh:221` asserts its
`meta.version`, and `memory/map/features/review-harnesses.md:22` claims it in the map with
`memory/map/generated/inventories.json:145` carrying the generated key. By value:
`tools/drift-audit/README.md:27` records that the return key stopped being an ARRAY of lens slugs and
became an INTEGER count of lenses, which is a reader of the very lens set this item re-keys.

**`aMendedLedger-4-u3` S3 — two session-ledger lines out of `index_set()`.** One of the three, and
the dry run's description of it is exact. By name: `tools/memory-tree/check-memory-hygiene.sh:657`
defines it, `:686` computes `INDEX_SET` once for checks 6 and 7, the print mode on the following line
publishes it, `tools/memory-tree/corpus_ids.py:563` and `tools/memory-tree/corpus_ids.py:1105` both
reason about what it filters, `tools/memory-tree/hygiene-parity.test.sh:246` asserts a check-7 row
inside a real member, and two dossiers claim the behaviour at
`memory/map/features/memory-tree-hygiene.md:101` and `memory/map/features/unattended.md:204`. By
value: check 6's per-class caps and check 7's exemption are both computed over that membership, so
which files are capped IS the value; the selector that consumes it is at
`tools/memory-tree/check-memory-hygiene.sh:770`.

**`aMendedLedger-4-u3` S4 — the `ex7` alternatives.** By name:
`tools/memory-tree/check-memory-hygiene.sh:758` is the selector and `:759` is the second spelling the
item names, which today APPENDS rather than rewriting. By value:
`tools/memory-tree/check-memory-hygiene.sh:770` filters `INDEX_SET` through that regex, so its value
decides check 7's population outright.

**`aMendedLedger-4-u3` S7 — the session machinery of the scaffolder.** By name:
`tools/memory-tree/adopt-memory-tree.sh` is named by `WIRE-INTO-PROJECT.md`, by `README.md`, by
`memory/HYGIENE.md` and by `memory/map/features/annotation-style.md`. By value: what the scaffolder
writes is what check 3 then grades, so the registries it writes feed the count of `project/*.txt`
files at `tools/memory-tree/check-memory-hygiene.sh:509`, and the scaffolder's own comment block
warns that a number written beside the thing it counts is how one registry shipped missing.

**`aMendedLedger-6-u6` S11 — the playbook version marker and the sentence it carried.** By name:
`skills/session-kickoff/SKILL.md:85` greps the repo for that marker, `WIRE-INTO-PROJECT.md:1563`
tells an adopter to re-pull by diffing against its version, and two dossiers record the marker's
lockstep history. By value: the re-pull decision compares the adopter's version against upstream's,
which reads the marker's value and nothing else. The file the item names is gone from the live
tree: the template was renamed to `coding-governance-agents.template.md`, and the old name survives
as a file only in the versioned snapshots under `memory/archive/`. The clause therefore lists the
four live readers and carries the rename in its by-name prose rather than the old name as a token.
That token still RESOLVES, because review records outside the spec corpus spell it, so listing it
would pass the resolution test while pointing at no live reader.

**`aMendedLedger-8-u9` S2 — `split_regions` and its companions.** By name: the symbol is gone from
`tools/memory-tree/merge-rows.py` already, and two sites survived it —
`tools/check-wiring.sh:765` describes what it did to three unkeyable lines, and
`memory/map/features/memory-tree-merge-driver.md:196` records the withdrawn seam and what replaced
it. By value: `NO VALUE READERS`, and the reason is in that dossier line — the three-region model
was the seam's whole content, so nothing read a number out of it.

**`aMendedLedger-8-u9` S11 — the reach of a node-local `merge.conflictStyle`.** What the item
withdraws is not the config key, which stays, but the path by which a node's `diff3` or `zdiff3`
value reaches the driver's reconciliation rules. By name: `tools/memory-tree/merge-rows.py:493` pins
`-c merge.conflictStyle=merge` at the single `git merge-file` call, which is the mechanism this item
exists to choose, and `:79`-`:82` of the same file state why;
`tools/memory-tree/merge-rows.test.sh:1206` and `:1210` spell it; and
`memory/map/features/memory-tree-merge-driver.md:116`, `:120` and `:166` record the pin and its
price. By value: section 32 of `tools/memory-tree/merge-rows.test.sh`, headed with
this unit's own fork F8, asserts at `:1206`-`:1208` that `diff3` changes the region shape, then at
`:1209`-`:1222` sets the config to each of `diff3` and `zdiff3` and asserts the exit code, the heading
count, the row count and zero markers under each — a reader of the config's VALUE.

**`aGradedDoorway-7` S2 — `SHARD_ARITY`'s value of 2.** The item raises it to 8, so the value 2 is
what it withdraws, and that value has readers. By name: the two suites declare it, at
`tools/unattended/check-unattended.test.sh:29` and `tools/unattended/unattended.test.sh:32`, and
`tools/run-gates/run-gates.gov.test.sh:395`-`:400` reads each script's declaration against the
manifest's `--shard` callers. By value: the arity check
at `tools/unattended/check-unattended.test.sh:46`-`:48` compares a caller's shard count against the
value and bounds the index by it, and the per-shard floor case at `:3315`-`:3319` carries exactly two
shard arms, so at 8 every shard from 3 upward falls to the unsharded floor and grades against the
whole suite's count. `tools/unattended/unattended.test.sh:49`-`:51` and `:5771`-`:5775` repeat both.
Those are the sites "fix what breaks" will meet.

**`TOOL-dGatedProse-3` S8 — the kit-version constant's value.** Already claused, verified not
rewritten, and its four by-name tokens all resolve. S6 covers it.

**One rule for a withdrawn value, and it sorts two items together.** `TOOL-dGatedProse-3` S8
replaces a constant's value and `aGradedDoorway-7` S2 raises one. The by-value half asks who reads
the VALUE, and for a constant those are exactly the comparisons against it, so both items are
GENUINE and both answer with those sites. A `NO VALUE READERS` on the pin would assert the absence
of readers the code shows.

**`dPolishedVitrine-1` S5 — two rows of the carried-prefix registry.** By name:
`tools/check-install-prefix.sh:82` declares the mode, `tools/check-install-prefix.sh:461` is the
write path, and `tools/check-install-prefix.sh:348` is the comment that made the arm a ban;
`memory/gotchas/line-keyed-registry-reds-on-a-file-that-grew.md:35` records the refusal the remedy
prints. By value: each row is a path with a COUNT, and `--check` compares that count against a live
one — `memory/backlog/TOOL.md:314` records a live defect that exists only because the comparison
reads the number.

**`dScaffoldedMirror-9` S5 — the three `*_OFFENDER_PIN` keys.** By name:
`tools/lexicon/lexicon.py:120` maps both key names, `tools/lexicon/kit.toml:138` is the declared
hole, and `tools/drift-audit/drift_signals.py:270` with
`tools/drift-audit/drift_signals.py:305` are the ratchet rows the item also takes out. By value:
`tools/lexicon/lexicon.py:3280` is `if len(unwaived) > pin`, so the pin's NUMBER is compared on every
run, and `tools/lexicon/lexicon.py:3352` prints the moved pair.

### The escaped set, and the reason each escape is honest

| item | escape spellings | the reason the clause carries |
|---|---|---|
| `aQuarriedLantern-1` S6 | `NO VALUE READERS` | runtime eviction of cache directories, not a retirement of any name |
| `aMendedLedger-5-u5` S5 | `NO VALUE READERS` | the verb names a git merge interaction the new suite must cover |
| `aMendedLedger-7-u8` S5 | `NO VALUE READERS` | the verb names two existing test arms being re-pointed |
| `bConvergentLodestar-1` S2 | `NO VALUE READERS` | the verb sits in the condition under which a generated artifact churns |
| `bConvergentLodestar-1` S3 | both | the retired file and its readers are the inCMS repo's, absent here |
| `dPolishedVitrine-1` S10 | `NO VALUE READERS` | a runbook block cleans up its own pins when it finishes |
| `dScaffoldedMirror-9` S6 | `NO VALUE READERS` | 70 lines of comment archaeology; comments have no readers to inventory |
| `dScaffoldedMirror-9` S7 | `NO VALUE READERS` | the item ADDS two verbs; the verb that fires is one of their behaviours |

Seven of the eight carry `NO VALUE READERS` on the by-value half and prose with no backticked token
on the by-name half. That by-name half passes by vacuity under unit 1's S4 rather than by escape: a
half that lists no name leaves `READER NOT IN TREE` nothing to cover, so the clause does not write
it there.

`bConvergentLodestar-1` S3 is the by-name escape's designed constituency and the only item that
carries it. `reuse-discovery.js` resolves nowhere in the tracked tree outside the spec corpus,
verified, because it is a file in the upstream product repo. That is a reason, not a defect, and it
is precisely what `READER NOT IN TREE` exists to say; its by-value half carries `NO VALUE READERS`
for the same reason.

### The clause forms this unit writes

Four forms, with invented identifiers so nothing below reads as a claim about this tree. The clause
sits on the item's own line or on any line beneath it, and a sub-bullet indented under a column-0
item is a continuation of that item.

```markdown
- **S4** — retire the `shard_arity` pin and the two rows it carried.
  - **Readers:** by name: `check_shard_split` and `report_shard_rows` spell the pin.
    by value: `test_fresh_three_shapes` reads a row by a cell count of six.

- **S5** — raise `shard_arity` to 8 and fix what breaks.
  - **Readers:** by name: `tools/shard/split.sh` declares it. by value: `tools/shard/split.sh`
    compares a caller's arity against it and keeps one floor per shard for two shards only.

- **S6** — a build deletes sibling cache folders whose recorded `probe_root` has gone.
  - **Readers:** by name: nothing tracked is withdrawn; the verb is runtime eviction.
    by value: NO VALUE READERS — the evicted folders are untracked, and nothing tracked reads them.

- **S7** — the upstream repo drops `legacy_probe.js` on its next pull.
  - **Readers:** by name: READER NOT IN TREE — the file and its readers are the upstream
    product repo's, and nothing here reads either. by value: NO VALUE READERS — same reason.
```

**Every token a clause carries is a bare path or a bare symbol, never a path with a line number.**
Unit 1's resolution join is a content match — one batched `git grep -l -F -f` over the tracked tree
outside the spec corpus — and unit 1 states its citation-tail strip for the shape test only.
Measured at 32f2eb71 by this session's `probe_resolve_fold.py`:
`tools/memory-tree/check-memory-hygiene.sh` resolves, and the same token with `:497` appended does
not. The line numbers section 4 cites are provenance and belong in the census record, and
`TOOL-dGatedProse-3` S8's existing clause already has this shape. The same content match means a
token resolves when another tracked file SPELLS it, not when a file of that name exists, which is
why AC3 resolves every written token rather than trusting a path's existence.

### The collision plan, because these are other sessions' live files

Three of the fourteen specs are INPROGRESS — `aGradedDoorway-7`, `aQuarriedLantern-1` and
`dPolishedVitrine-1` — and one has already moved under this measurement: `dPolishedVitrine-1` was
rev-8 when unit 1 wrote its census and is rev-10 today. That is the risk arriving before the pass
does, and it is why S1 derives rather than reads.

The pass, in order:

1. Pin BASE with `git rev-parse` against an immutable sha, never a moving ref.
2. Run the probe and write its output into the census record. This is the write set.
3. Print the two-way difference against the table above, both directions, and record both. An item
   that entered the population is written; an item that left is recorded with the reason.
4. Write clauses one commit per OWNING NODE, node `d` last. A conflict is then confined to one
   node's files and revertible without touching the others. S9's bullet, where it is owed, rides in
   the node-`d` commit.
5. Re-run the probe over the edited tree and confirm every derived item carries a clause.

What the pass does if a file moved under it. The ITEM LABEL is the anchor, never a line number: every
citation in the table above is a label, and the probe re-locates it. An item whose label has gone, or
whose text no longer fires the trigger, leaves the write set and is recorded. A spec that reached
`CLOSED` or `WONTDO` between the census and the write leaves the population by the liveness test and
is not edited at all, because a landed record is frozen and this repo does not rewrite one.

If another session has written a clause on the same item first, the reconcile is ADDITIVE and never
pick-a-side: one clause survives per item and its halves carry the union of both sides' names. Two
clauses on one item is the defect S2 forbids, so the union is the only correct merge.

### The order, and the one join it leaves open

The main loop renumbered the build on 2026-09-21: this unit is `order 1`, and units 1, 2, 3 and 4
moved to orders 2, 3, 4 and 5, every header re-stamped at spec time. That is the whole-tail shift
F2 recommended, so no two units share an order and no sibling header is this unit's edit.

The edges arm joins orders as well as verbs: a **consumes-from** whose target sits at a LATER order
reds, and a **hands-off** whose target sits at an EARLIER one reds. Checked against every sibling
edge at the new numbering: unit 1 at order 2 hands off to units 2, 3 and 4; unit 2 at order 3
consumes from unit 1 and hands off to units 3 and 4; unit 3 at order 4 consumes from units 1 and 2
and hands off to unit 4; unit 4 at order 5 consumes from units 1, 2 and 3; and this unit at order 1
hands off to unit 1. Every order join is green. The trigger this unit writes against is a text and
not a build output, which is why a hand-off to a later order carries the relation.

The join still open is RECIPROCITY. This spec's **hands-off** `TOOL-dGatedProse-1` has no mirror in
unit 1's edges section at 32f2eb71, and the red prints on this spec. The mirror is one bullet, and
its right carrier is unit 1's own spec fold, because a spec set pushed before this unit is built
carries the red to the push boundary. S9 is the backstop: the pass checks unit 1's edges at its base
and writes the bullet only where it is absent.

### Inventory

| identifier | where it is graded |
|---|---|
| `TOOL-dGatedProse-5` | this spec's H1, the build README roster, and the generated build-order region |
| the census record's filename | hygiene check 5's recording-name grammar, under this build's `build/` |

This unit mints no symbol, no flag, no conf key and no check number. It declares no naming cell,
because it adds no identifier to any graded surface.

### Migration

The unit IS a migration, and it has no mechanical half: no rename, no key, no schema and no adopter
conf. An adopter installing the memory-tree kit at unit 1's version receives check 25 live over their
own live specs with nothing to set, and this pass buys them nothing — their corpus work is their own.
That is unit 1's declared non-goal, restated here only because a migration section that said nothing
about adopters would read as though this pass covered them.

### Files touched (estimate)

| file | change |
|---|---|
| eleven node-`a` and node-`b` live specs | one clause per derived item, additive — about 37 lines |
| two node-`d` live specs | one clause per derived item, additive — about 12 lines |
| `memory/builds/dGatedProse/spec/2026-09-20-spec-TOOL-dGatedProse-1.md` | one reciprocal edge bullet and the revision entry recording it, only where the spec set has not carried them — 2 lines at most |
| the census record under this build's `build/` folder | new, the probe output and the per-item verdict |
| `memory/builds/dGatedProse/README.md` | REGENERATED, never hand-edited |

The per-file clause count is DERIVED at the pass and the 37 and 12 above are estimates from the
census, not a contract.

### Alternatives rejected

**Land unit 1 first and carry the red.** This is F8's second option and the charter argues against it
directly: a leg nobody can green is a leg people learn to ignore, and `memory hygiene` has no guard,
so every bar on every node would carry it.

**A drainable handoff registry.** F8's third option, and the mechanism exists in the tree already for
spec-token hits. It is more machinery than the corpus work it defers, and the owner ruled the corpus
work instead.

**Put the clauses in unit 1's own commit.** That is unit 1's S14, and it is what the split undoes: a
clause and the check that grades it in one commit means the pass has no green state to land against,
and any error in a clause reds the same bar that the new arm reds, with no way to tell them apart.

## 5. Production-readiness checklist

- security — N/A. Additive prose into tracked markdown, no execution surface, no input from outside
  the tree.
- perf / scale — the probe walks 640 files and reads each once; seconds. The pass itself is an edit.
- error / empty / loading states — the one real state is an EMPTY derived population, which would
  mean the trigger or the corpus moved out from under this spec. The pass then writes no clause and
  records the zero rather than reporting a silent success, because a zero that looks like a pass is
  the class this build exists to close.
- observability — the census record is the whole of it, and it carries the probe output, the per-item
  verdict, the reader evidence and the two-way difference.
- risks — the collision with three INPROGRESS specs, and the half-declared edge pair until its
  mirror lands on unit 1's file. Both are named above with their remedies.
- testing — no suite. The observation is the probe re-run of S1 and the greps of section 6, and there
  is nothing to unit-test because the unit adds no code.
- migration — the section above. No mechanical half.
- user docs — N/A. Nothing user-facing changes, and the author-facing rule for the clause is unit 1's
  S13 in the spec template.

## 6. Acceptance criteria

- **AC1** — When the build pass re-runs `probe_pop.py`, or its own re-implementation of the same
  trigger, against the base it pinned with `git rev-parse`, the census record carries the probe's
  item set AND the two-way difference against section 4's table, both directions, and the write set
  equals the derived set.
  figure: DERIVED — the item count in the record is the probe's output at the pass, never the 23
  pinned here.
  Red when: the record states 23 with no probe output beneath it, so an item that entered the
  population after 2026-09-21 ships unclaused.
- **AC2** — When `git grep -c 'Readers:'` runs over each edited spec, every file's count equals the
  number of items the census attributes to that file — not merely at least it, because a duplicate
  clause on one item is a defect.
  Red when: a count is low, meaning a derived item was missed, or high, meaning one item carries two
  clauses whose halves answer differently.
- **AC3** — When every by-name token the pass wrote is resolved against the tracked tree outside the
  spec corpus, each one either resolves or carries the by-name escape with a reason, and the
  exclusion pathspec is verified against `git ls-files` for the spec glob before the resolution is
  believed.
  Red when: the pathspec silently fails and returns more files than the tree tracks outside the spec
  corpus, so every name resolves against the specs that named it and the arm cannot fail.
- **AC4** — When each of the eight escaped items is read, its `NO VALUE READERS` or
  `READER NOT IN TREE` spelling is followed by at least one further non-space character on the same
  half.
  Red when: an item carries a bare escape, which is a blank being filled rather than a question
  being answered.
- **AC5** — When S3 and S6 of
  `memory/builds/aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-3-u2-ledger-retirement.md`
  and S3 of
  `memory/builds/aMendedLedger/spec/units/2026-08-09-spec-aMendedLedger-4-u3-hygiene-gate.md` are
  read, each carries the readers section 4 names for it, by path, on its by-name half. Those three
  are the items the original dry run named.
  Red when: any of the three answers its by-name half with an escape, because the readers for all
  three were found in code and an escape there would be false.
- **AC6** — When `git diff --unified=0` is read hunk by hunk over every edited spec other than unit
  1's, every hunk is an addition, except the header line where F1's resolution moves the rev and the
  date.
  Red when: a hunk rewrites or takes out a line of another node's spec, which is the non-goal this
  criterion exists to observe.
- **AC7** — When the diff for `memory/builds/dGatedProse/spec/2026-09-20-spec-TOOL-dGatedProse-3.md`
  is read, it carries no change to S8's existing clause.
  Red when: the pass rewrites an existing clause that already resolves, turning a verification into
  an edit of another unit's live spec.
- **AC8** — When a spec named in section 4's table carries `CLOSED` or `WONTDO` at write time, the
  census records it as left out and its file is absent from the diff.
  Red when: a frozen record gains a clause, which no liveness test would ever have asked for.
- **AC9** — When `probe_pop.py` runs over this spec file itself, every item it fires on carries a
  clause, and the census states that count.
  figure: DERIVED — the count comes from the probe over this file, not from an assertion here that
  it is zero.
  Red when: this spec ships a triggered item with no clause, so the unit that brings the corpus into
  compliance is itself a finding at order 2.
- **AC10** — When `python tools/memory-tree/gen_build_index.py --check` runs after the pass, it
  reports no stale region for this build, and the README's roster names five ids.
  Red when: the roster still names four, so the generated build-order region contradicts the header
  this spec carries.
- **AC11** — When `memory/builds/dGatedProse/spec/2026-09-20-spec-TOOL-dGatedProse-1.md` is read
  after the pass, its header carries `order 2` and its edges section carries the reciprocal
  **consumes-from** bullet naming this unit, whichever commit wrote it.
  Red when: the bullet is absent, so the half-declared pair reds on THIS spec's file and the remedy
  is not where the message points.
- **AC12** — When S2 of
  `memory/builds/aGradedDoorway/spec/2026-08-29-spec-TOOL-aGradedDoorway-7.md`, S1 of
  `memory/builds/aMendedLedger/spec/2026-08-09-spec-aMendedLedger-1.md` and S11 of
  `memory/builds/aMendedLedger/spec/units/2026-08-10-spec-aMendedLedger-8-u9-driver-redesign.md`
  are read, each clause's by-value half names at least one of the readers section 4 found for it,
  and none of the three carries `NO VALUE READERS`.
  Red when: any of the three carries that escape, which asserts no by-value reader where resolvable
  ones exist, and check 25 would pass the false statement on every run after.

## 7. Gates

`memory hygiene` · `spec tokens (a spec's own names resolve)` · `build README slot contract`

This unit adds no gate arm, so there is no `New arm:` line to write, and it moves no assertion floor.
Every leg it names is one it must keep green rather than one it changes, which is the whole shape of
a corpus pass that lands before its checker.

**One leg is expected RED from this spec landing until the mirror bullet lands on unit 1's file, and
the reason is the join's own design.** The **hands-off** bullet on `TOOL-dGatedProse-1` has no
mirror in that unit's edges section at 32f2eb71, and the reciprocity message prints on the DECLARING
file. So `memory hygiene` reds naming THIS spec until unit 1 carries **consumes-from**
`TOOL-dGatedProse-5`. Unit 1's own spec fold is the carrier that closes it before any push; S9
writes it at the pass only if that fold has not. The join is held under `--staged`, so it binds at
the push boundary and not at commit time — a pre-commit run will not tell you whether it is closed.

`verdict epoch` is NOT expected red here, because this unit touches no behaviour-bearing line of the
engine and no kit version. That leg's red belongs to the commits of units 1 and 2, at orders 2 and
3, under the epoch rule, and it closes at order 4, where `TOOL-dGatedProse-3` makes the build's one
version move.

## 8. Open questions

- **F1 — does a clause bump the edited spec's rev?** The template's rule is that `rev-N` bumps on ANY
  material content change and that every rev gets a revision-log line, and a clause the gate grades
  is material by definition. Against that: the bump moves the header date on eleven node-`a` and node-`b`
  live specs and widens the diff surface a conflict can land on. Two consequences either way, both
  checked. Bumping needs the revision entry in the same edit, because check 12 asserts the header rev
  appears in section 9. Not bumping reds nothing at all. The scope-token rule on a revision entry is
  gated by FILENAME date, so only the `dGatedProse` and `dPolishedVitrine` specs owe one.
  RECOMMENDATION: bump, with the entry, and write the scope token everywhere for one house style.
- **F2 — the minimal re-stamp, or the whole tail?** The owner's re-ordering names one pair: this unit
  at order 1 and unit 1 at order 2. That would leave units 1 and 2 sharing order 2, which the format
  reads as a declared parallel group, and they are not parallel — unit 2 consumes unit 1's template
  edit. RESOLVED (agent, 2026-09-21): the whole tail, taken by the main loop's renumbering at spec
  time. Units 1 to 4 carry orders 2 to 5 in their headers, every order join is green at the new
  numbering, checked edge by edge in section 4, and no header edit is left to this unit's pass.
- **F3 — is `aMendedLedger-8-u9` S11 genuine after all?** Unit 1 called it borderline, on the ground
  that what is neutralised is a config's reachability and no name leaves the tree.
  RESOLVED (agent, 2026-09-21): GENUINE, decided by reading the code rather than the sentence. The
  config key has three resolvable carriers outside the spec corpus, and section 32 of
  `tools/memory-tree/merge-rows.test.sh` sets it to each of `diff3` and `zdiff3` and asserts what
  the driver does under each, which is a reader of its value. None of those readers is invented to
  satisfy a marker; the escape was the false statement. Section 4 names them.
- **F4 — who folds unit 1's superseded census?** Its section-4 tables and its S14 both describe work
  this unit has done, and this spec touches at most one edge bullet of that file and no prose.
  Leaving the stale census in place means a reader of unit 1 sees a corpus pass that is not its own.
  RECOMMENDATION: unit 1's own next revision, rather than a prose edit from here that would contest
  a file its author is holding.

## 9. Revision log

- rev-1 · 2026-09-21 · initial draft. The population is this session's own probe over the 37 live
  specs at base `fcbfba5f`, reconciled item by item against unit 1 rev-3's census, and every reader
  named in section 4 was verified against source at writing time.
- rev-2 · 2026-09-21 · S4 S5 S6 S9 · §3 · §4 · §7 · §8 · AC4 AC11 AC12 · folded against a skeptic
  that reproduced the population exactly and returned NEEDS_FOLD, every citation touched re-derived
  at source at 32f2eb71. Three escapes became GENUINE with readers in both halves:
  `aMendedLedger-1` S1 shares its three stubs with `aMendedLedger-2-u1` S5 and now shares its
  answer; `aGradedDoorway-7` S2 and `TOOL-dGatedProse-3` S8 are sorted by one rule for a withdrawn
  value; and `aMendedLedger-8-u9` S11 has three carriers of `merge.conflictStyle` and a test arm
  reading its value, which RESOLVES F3. The split is fifteen and eight, AC12 observes the three, and
  the clause form for a raised pin now shows readers. Under the main loop's case-insensitive ruling
  the population is re-derived at 23 items across 14 specs, `dPolishedVitrine-1` S5 in on its
  `DROPS`. The main loop's renumbering RESOLVES F2 and narrows S9 to the mirror bullet, and the gates
  note closes `verdict epoch` at order 4. Also folded: the escaped table's both-halves column, since
  a by-name half listing no name passes by vacuity; three INPROGRESS specs, not two; the unit as one
  commit per owning node; the external consumes-from edge, deleted because its prose dodged the
  sibling join and the hands-off carries the relation; bare tokens in every clause, measured; the
  drain's by-value readers, where rev-1 cited a `.txt` count the drained files never entered; and
  the archived template name, which resolves by content and leaves its clause.

## 10. Reuse audit

No existing seam fits, and the evidence is the probe's own output.
`python tools/codebase-map/reuse_lookup.py "add a Readers clause to every triggered scope item across
live specs"` ranks 1371 symbols and returns nothing but name-stem matches on `read` — `read_text`,
`read_journal`, `read_gloss` — none of which reads a spec's section 2, plus affordance seams for
`run-gates` and `govkit`. That is the correct answer rather than a gap: this unit adds no code, so
there is no seam to extend. What it does reuse is a METHOD, and that one is cited —
`memory/builds/aTetheredRecord/spec/2026-08-16-spec-TOOL-aTetheredRecord-3.md` brought 76 records
into conformance against an inert parser, and its own section 3 drew the same no-content-edit line
this spec draws.

Recall terms used: corpus pass, readers clause, retirement, live spec, cross-node edit, additive
prose, hygiene check, cutoff, grandfather, ratchet, waiver registry, scope join.
