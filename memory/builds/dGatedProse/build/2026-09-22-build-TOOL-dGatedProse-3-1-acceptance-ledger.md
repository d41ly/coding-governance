# The acceptance ledger — the guide cap raised on both axes, the kit version moved once, and the leg runs owed

**Serves:** journal TOOL-dGatedProse-3

Node `d`, 2026-09-22, the build pass of `TOOL-dGatedProse-3` against spec rev-5, closed at rev-6, at
order 4 on `branch/spec-prose-gates-b41f7c`. The pass base is `bb9499e1`, unit 2's closing commit.
Three commits carry the work: `f632fbde` raises the pair and moves every live assertion of it,
`ddd08686` moves the kit version from 2.82 to 2.83, and `ddeb5139` rewords the three records this
unit added so that none of them spells a retired figure. By the owner's rule no suite and no gate leg
was run. Every observation below was made by calling the code directly from python files in the
session scratchpad, which are untracked. The engine was run only over SCRATCH trees built for the
purpose, never over this worktree. Each line says what was run and what it printed:

- the CAP observations built fresh git trees holding a fixture and a minimal conf, or a verbatim copy
  of the conf example, and ran the engine once per tree. Every red arm was a second tree differing
  from the green one by the one value the criterion names, and each red tree turned its finding on.
- the VERSION observations read git and files only: the marker grep, the four template markers
  against the constant, one render with the four live copies checked unchanged, and the epoch rule
  re-derived from the checker's own stated W and S, without running the checker.

The figures in the engine's reason block and the decision row were re-measured at the commit, as
AC14 requires, and differ from the spec's reading of the day before. The refill window is 21 days to
2026-09-22 rather than 20, so the rate is 509.9 B and 2.67 lines per day, and the raise buys 33365 B,
about 65 days, and 496 lines, about 186 days. Bytes still bind first.

Everything a suite or a leg observes is OWED to the one run after all five units are built, and
each line names the break that stages it red.

**Evidences:** TOOL-dGatedProse-3
- AC1 — `tools/memory-tree/check-memory-hygiene.sh` — OWED to the post-build memory hygiene run over the tree. Observed directly: over a scratch tree holding this worktree's seven guides byte for byte and no debt row, check 6 names no file under the guides directory. Red when the byte key is declared back at 61440 in that tree's conf: check 6 then names `memory/guides/UNATTENDED-PROTOCOL.md`.
- AC2 — `tools/memory-tree/check-memory-hygiene.sh` — observed directly: the constants line reads 98304 and 1200, and the quotient is 81.92. The two one-key raises give 131.07 and 51.20.
- AC3 — `KIT_MEMORY_TREE_VERSION` — observed directly: 2.82 at the pinned base `bd44d3ff` and 2.83 at the tip, both read from the tree, and the same line carries the marker at 2.83 exactly once.
- AC4 — `tools/check-kit-versions.sh` — OWED to the post-build kit version markers and kit/dogfood doc parity runs. Observed directly: all four tracked shipped templates carry a marker equal to the constant, and one render by the parity script's render mode left the four live copies unchanged. Red when a template keeps the old marker, which that checker names at its comparison line.
- AC5 — `tools/memory-tree/.memory-tree.conf.example` — observed directly: a scratch tree whose conf is a verbatim copy of the example holds a guide of 1100 lines and 90000 B, and check 6 is silent. Four red trees each changed one value back, to a byte key of 61440, a line key of 750, a byte key of 81920 and a line key of 1000. Check 6 named the fixture in all four.
- AC6 — `memory/backlog/TOOL.md` — observed directly: the four passes re-run at the tip. Every line asserting a current guide bound spells 98304 and 1200. The only record lines left spelling a retired figure are `memory/DECISIONS.md` rows 62 and 132 and `memory/backlog/TOOL.md` rows 56, 296 and 368. Row 354 was re-measured in place, to 66.1% with 496 lines left. Pass 4's other hits are `INDEX_CAP_BYTES` carriers and one count of 750 things. Before the rewording commit, the reason block, the new decision row and the drain line also spelled the retired figures, and the passes returned all three.
- AC7 — `memory/DECISIONS.md` — observed directly: exactly one added row, `TOOL-dGatedProse-3`, 289 bytes under the 300 entry budget. It names the ratified pair, 509.9 B per day and about 65 days, `TOOL-dLoggedFlight-33`, and the 2026-09-21 re-ruling over the 81920 and 1000 ratified that day. It says it supersedes `TOOL-dSpentCeiling-3`'s value, and it states the superseded pair only as superseded.
- AC8 — `memory/project/curation-debt.txt` — OWED to the post-build full-tree memory hygiene run, where the guard binds. Observed directly: the registry holds four path rows, and its header names this unit as the drain. A scratch tree with the real guides and no row printed no stale-entry finding. The red tree kept the protocol row under the raised cap, and the guard named that path as a row that hides nothing.
- AC9 — `tools/memory-tree/check-memory-hygiene.test.sh` — OWED to the full memory-hygiene self-test run. Observed directly: the fixture lines were lifted from the suite and run in a scratch tree. Check 6 names the tfixture guide with `1211L > 98304B/1200L`, the arm's exact string, and leaves the green twide counterpart silent. Red when the fixture is left at 760 rows: check 6 is then silent on it at the raised cap.
- AC10 — `tools/memory-tree/check-memory-hygiene.test.sh` — observed statically: `FLOOR_ASSERTIONS` reads 411 at unit 1's closing commit and at the tip. The suite's diff since then removes one assertion call and adds one, the repaired arm, and the regrown fixture and its comments change no count.
- AC11 — `memory/map/features/memory-tree-hygiene.md` — observed directly: the guides row names `GUIDE_CAP_BYTES` at 96 KB, 98304, and `GUIDE_CAP_LINES` at 1200, both by default, and the word hardcoded is gone from that row. A map regeneration changed no generated artifact.
- AC12 — `tools/memory-tree/check-verdict-epoch.sh` — OWED to the post-build verdict epoch run, and its staged-break liveness to the verdict-epoch self-test run. Observed directly: the marker grep for the retired value returns no tracked file outside the build records. Re-deriving the checker's rule over `bd44d3ff..HEAD` gives W and S as the same commit, `ddd08686`, the bump, which dates itself. The cap commit `f632fbde` is the last earlier behaviour-bearing commit, and the rewording commit moves comments only. Red when the bump sits before the cap commit: the ancestry test between the cap commit and its parent fails.
- AC13 — `**Readers:**` — OWED to the post-build memory hygiene run, where check 25 grades the clause. Observed directly, at author time: each of the four by-name readers spells the constant, and each of the three by-value readers compares or derives from its value. `tools/memory-tree/corpus_ids.py` is not listed.
- AC14 — `GUIDE_CAP_BYTES=` — observed directly: the comment block above that line is ten lines long and bounded by the index-class line. It carries 2026-09-22 and `TOOL-dLoggedFlight-33`, names both keys as MOVED, and states 509.9 B per day with about 65 days of headroom. It names the protocol in words and not by path, because a kit file names nothing outside itself by literal.
