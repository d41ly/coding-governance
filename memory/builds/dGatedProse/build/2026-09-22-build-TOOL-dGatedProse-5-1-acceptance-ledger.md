# The acceptance ledger — the corpus pass, 36 clauses written and one verified, at order 1

**Serves:** journal TOOL-dGatedProse-5

Every line is OBSERVED. Each observation is the pass's own instrument, written fresh for this build
pass and run from the session scratchpad as a python file: its implementation of
`TOOL-dGatedProse-1`'s trigger and resolver as that spec's rev-8 text states them, a verifier that
re-reads the written clauses against the census record's token rows and the diff against BASE, and
a staged-break arm proving the verifier can red. No merge bar, no gate leg and no self-test suite was
run, by the owner's rule; the three legs this unit's section 7 names are owed to the run after all
five units are built. BASE is `e61b1cea`, pinned with `git rev-parse` before any edit. The census
record, `2026-09-22-build-TOOL-dGatedProse-5-corpus-census.md` beside this file, carries the figures
each line below cites.

The live population the pass derived is the census's 37 items across 19 specs label for label, so
the write set needed no row moved for S1's difference. One token row moved for another reason, a
false reader found at source, and the spec's rev-8 corrected it in the first commit, before any
clause was written.

**Evidences:** TOOL-dGatedProse-5
- AC1 — `git rev-parse` — BASE pinned at `e61b1cea`; the pass's trigger read every blob out of that commit and, separately, the clean tree, and both printed 37 items across 19 specs, byte-identical; the census record carries the item set and the two-way difference, none entered and none left, and the re-derivation over the edited tree returns the same 37 labels, so the write set is the derived set.
- AC2 — `**Readers:**` — over the diff against BASE, each of the 18 edited corpus specs adds exactly as many lines carrying the marker as the census attributes write-set items to it, 36 in all, and each of the 37 items carries the marker exactly once; a staged second clause on `aMendedLedger-4-u3` S4 turned the verifier red and was restored.
- AC3 — `reuse-discovery.js` — the pass's resolver, run and recorded in the census record at commit `4d294391` before any clause commit, derived 2465 tracked files and 371 readers from `git ls-files`; of 61 distinct tokens, 50 resolve by identity and 10 by content, each spelled by code, and `reuse-discovery.js` alone stays unresolved, under READER NOT IN TREE with its reason; no token is a record path, and over the edited tree the 53 distinct by-name tokens written resolve the same way.
- AC4 — `NO VALUE READERS` — each of the 9 escaped items carries its escape followed by a reason on the same half, and `bConvergentLodestar-1` S3's by-name half carries READER NOT IN TREE followed by one; the verifier's bare-escape arm found none.
- AC5 — `READER NOT IN TREE` — every one of the 28 GENUINE items carries on its by-name half every token its census row lists, the two prose rows carry no token there, and no GENUINE by-name half carries READER NOT IN TREE; the `dScaffoldedMirror-9` S5 row is the one rev-8 corrected; dropping `tools/run-gates/run-gates.gov.test.sh` from `aGradedDoorway-7` S2 turned the verifier red and was restored.
- AC6 — `git diff --unified=0` — read hunk by hunk over the 18 edited corpus specs: every hunk is an addition except the header line of `dPolishedVitrine-1` and `dPolishedVitrine-14`, the two F1 binds, where only the rev and the date move; this unit's own spec is outside the criterion's population, since its red names another node's spec, and its one rewritten passage is the rev-8 fold the method requires before a divergence.
- AC7 — `2026-09-20-spec-TOOL-dGatedProse-3.md` — absent from the diff against BASE; its S8 clause carries the three markers once each and in order, its four by-name tokens resolve by identity, and its by-value half carries eleven tokens and no escape.
- AC8 — `CLOSED` — none of the 19 specs carrying a write-set item is CLOSED or WONTDO at BASE or when the clauses were written: four INPROGRESS, one BLOCKED, two DEFERRED, twelve SPECCED; the verifier's terminal-status arm found no edited spec terminal.
- AC9 — `2026-09-21-spec-TOOL-dGatedProse-5.md` — the pass's trigger run over this file's section 2 items, whatever its status, fires on 0 of its 9 items, at rev-8 before the status flip and again after it; the census record states the zero.
- AC10 — `gen_build_index.py --check` — run after the final staging, it exits 0 with no stale region, and the build README's roster names five ids.
- AC11 — `TOOL-dGatedProse-5` — units 1, 2 and 4 each carry a consumes-from bullet naming this unit in their Edges sections at BASE, this spec carries the three mirroring hands-off bullets, and unit 1's header carries `order 2`; no bullet was owed.
- AC12 — `NO VALUE READERS` — every GENUINE item's by-value half carries every token its census row lists, and only `aMendedLedger-8-u9` S2 carries the escape, followed by its reason; putting the escape on `aGradedDoorway-7` S2's by-value half turned the verifier red and was restored.
- AC14 — `2026-08-20-spec-TOOL-aPacedTurnstile-14.md` — the census record names reading B and the word boundary, and the write set holds `aMendedLedger-6-u6` S1 and S5, `aPacedTurnstile-14` S9 and `dPolishedVitrine-14` S2; reading A gives 33 items across 17 specs and differs from reading B by exactly those four.
- AC16 — `by value:` — every one of the 36 written clauses, and the verified one, carries the three markers in order and each once inside the item it answers, re-read through the engine's item accumulator over the edited tree.
- AC17 — `rev-N` — `dPolishedVitrine-1` moves from rev-10 to rev-11 and `dPolishedVitrine-14` from rev-4 to rev-5, each with a section 9 entry at the new rev naming S5 and S10 or S2; the sixteen unbound edited specs keep their headers, and F1 binds this spec too, whose rev-8 carries its own entry.
