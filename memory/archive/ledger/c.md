# In-flight ledger — node `c`

| Node | Branch / worktree | Streams | Slug · ids | Status |
|---|---|---|---|---|
| `c` | `main` (doc-only, no product code) | playbook — port from inCMS DES-bMirroredLexicon-1: runtime-derive-vs-artifact fork (§12) + a "gate's own hardcoded vocabulary drifts" recurring-bug-class (§10), both into `domain-rules.md`; size-locked template untouched | **cDerivedGlossary** · PLAY-cDerivedGlossary-1 · seq PLAY-1 | **merged** (direct doc-only commit on `main`, NOT pushed) — node `c` (agent-0 @ DESKTOP-8BKM8GN) registered same commit; awaiting push ask |
| `c` | `main` (kit only, no playbook/template change) | tooling — port the inCMS `ARCH-bCandidLoupe-2/-3` drift-audit tooling into a project-agnostic kit: engine + project layer + adopt/`--check` + rendered Skill + selftest, plus the two Tier-2 workflows | **cSightedPlumb** · TOOL-cSightedPlumb-1 · seq TOOL-1 | **in-flight — kit 1.0 BUILT + dogfooded on this repo, 3 gate legs wired, NOT pushed.** Selftest 19/19: every gateable signal is silent on a clean fixture and FIRES on a violating one. First run here found 3 signals over pin; 2 were my own oracles' false positives (a parity-comparison sha read as work; a probe comparing charter bullets to legs, numbers that can never be equal) — both oracles fixed, not pinned over. 1 real, in node `b`'s row, left for `b` to prune since `b` owns that file. Pins seeded at measured 1/2/1. Detail in the spec. |
