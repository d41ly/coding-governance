# tooling backlog — rotated 2026-08-17 (second)

> Terminal rows moved byte-identical out of `../backlog/TOOL.md` when the union of this branch's
> rows and main's crossed the index size cap. Two independent rotations happened on the two
> sides; every row absent from the merged index was verified present in an archive before the
> merge was written.

> **SUPERSEDED 2026-09-13 by `TOOL-cSpliceWarden-7`.** The header below closes "Nothing edited,
> nothing deleted", and this file has since had rows deleted. Seventeen of its twenty-three rows were
> also rows of `TOOL.2026-08-17.md`, byte-identical in status and body, because two builds rotated to
> this date independently and each archived the overlap. Under the `cut` rotation mode this repo
> declares in `.memory-tree.conf`, an id belongs to exactly ONE file, and those seventeen belonged to
> two.
>
> They are removed HERE and kept THERE, so the archive pair now partitions the family. Nothing is
> lost by the deletion and that was checked rather than assumed: every removed row's status token and
> body were byte-identical to the surviving copy, so the bytes still exist, one file over. The six
> rows below are the ones only this file ever carried — `TOOL-cBriefedPilot-30`, `-31`, `-32`, `-34`,
> `-35` and `-38` — and they are why the file stays rather than being folded into its twin.
>
> The two alternatives were costed and refused. Folding this file into `TOOL.2026-08-17.md` and
> deleting it destroys a ratified record to reach the same end. Declaring cross-ARCHIVE duplication
> outside what `cut` asserts would weaken an invariant so the data passes, which is the wrong
> direction: the invariant is the point.
>
> This note is prose, never a row. A dash-led line carrying an id would key as a backlog row under
> hygiene check 20 and join this file's id set.

- TOOL-cBriefedPilot-30 · CLOSED · CLOSED by TOOL-cSettledDocket-1 — `--park` writes §2's DECISION kind, with the command in the Skill an agent reads
- TOOL-cBriefedPilot-31 · CLOSED · CLOSED by TOOL-cSettledDocket-2 — check 16 joins the EFFECTIVE set, and `DIRECTIVES_EXTRA_TABLE` gives a project its row source
- TOOL-cBriefedPilot-32 · CLOSED · CLOSED by TOOL-cSettledDocket-3 — both assertions hoisted above the Tier-1 cut and keyed on section TITLE, not number
- TOOL-cBriefedPilot-34 · CLOSED · CLOSED by TOOL-cSettledDocket-4 — the hygiene suite's 52 inline sites count; the label no longer says `helper`
- TOOL-cBriefedPilot-35 · CLOSED · CLOSED by TOOL-cSettledDocket-5 — a leg over the derived population, and that suite already counted
- TOOL-cBriefedPilot-38 · CLOSED · CLOSED by TOOL-cSettledDocket-6 — the standing frozen-versus-live fixture, which survived main's redesign of what it tests
