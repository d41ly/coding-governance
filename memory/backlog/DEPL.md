# deployer backlog

> Mutable. Each row leads with one status token (OPEN…WONTDO).
- DEPL-dSettledRoster-1 · OPEN · `govkit apply` appends its `eol=lf` pins LAST to a target's `.gitattributes` (`govkit.py:2011`), and last-match-wins, so gov silently overrides a target that set the opposite. Live since `0daa263`; the DEPL-aTetheredConvoy-6 fork was never owner-reviewed
- DEPL-dSettledRoster-2 · OPEN · a `govkit apply` against a `kind = "manifest"` target runs that target's own committed command TWICE — `read_gate_verdicts` at `govkit.py:1955` and `:2418`. Deliberate and disclosed, but the DEPL-aTetheredConvoy-4 fork was never owner-reviewed
- DEPL-aSealedCaravan-3 · OPEN · `WIRE-INTO-PROJECT.md` sends the reader to a section 2a for the agent-instructions kit and no such section exists; that kit is also the only `--all`-selectable one with no version constant and no runbook section at all
- DEPL-aFerriedDossier-1 · CLOSED · the hand-fork update path exists: `govkit adopt` (DEPL-dCarriedReceipt-13, spec CLOSED rev-8) writes the receipt an installed tree never had, and WIRE-INTO-PROJECT section 5b is the runbook for it → builds/dGaugedVintage/
- DEPL-aFerriedDossier-2 · OPEN · Widen unit 3 S5's fixture family with a non-default-prefix entry and one adopt-existing variant. Cheaper than any single fix it protects: without the first, the prefix fixes can land and regress unobserved. → builds/aFerriedDossier/
- DEPL-aFerriedDossier-3 · OPEN · 13 defects inCMS measured in this tree with line numbers: the discarded prefix answer, the literal probe, the unresolved sentinel, the unconditional engine write, absolute gov_source, an unhandled gov-side rename. → builds/aFerriedDossier/
- DEPL-dCarriedReceipt-1 · CLOSED · `{relpath}` has two resolutions: `rule_relpath` (:172) resolves it against the rule base and its docstring calls the basename form a measured defect; `resolve_dests` (:2085), the seam plan and apply call, IS that defect → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-2 · CLOSED · roles attributes/gate-leg/ci dispatch to `refuse`, which runs after the write branch and only makes r.problems non-empty — so the receipt re-stamp is skipped forever. Becomes report, plus a pins arm → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-3 · CLOSED · `cmd_intake` emits a literal `prefix = "tools"` (:3206) whatever `--answer prefix=` says, so NC's committed deploy.toml is a file the tool that owns it could not have written. Two lines → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-4 · CLOSED · `coverage_rows()` + `plan --coverage`: file-level adoption gaps, joining planned_writes (:1359) against tracked(target) (:111). Needs only deploy.toml, so it measures inCMS today → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-5 · CLOSED · `[[decline]]` in deploy.toml plus three staleness arms, so a deliberate omission is legible and a decline whose file gov withdrew reds itself. Without it coverage gets skimmed → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-6 · CLOSED · the silenced-gate-leg bar: a claimed kit's `[[gate_leg]]` argv naming an untracked engine. Fires 17x on inCMS, 1x on NC — and NC's hit is a real gov defect shipped to every adopter → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-7 · CLOSED · a receipt row carries two identities, `gov_oid` and `oid` read index-side, instead of one sha256 of worktree bytes. Without it a default Windows clone reads ~96% false divergence → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-8 · CLOSED · a merge result is stamped as gov's own (:3096), so the next gov commit reads the row `stale` (:2845) and raw-overwrites it (:3071) — every preserved hand-edit dies one update later, reported clean → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-9 · CLOSED · `carry` rungs (verbatim/eol/relocate) recomputed by proof each run over a needle map DERIVED from the receipt's own path pairs, never authored. Reaches an adopter at a non-default prefix → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-10 · CLOSED · role `forked`, report-only, for the files where gov is the derivative — three carry a `FORKED from` header. One automatic update on `recall/extract.py` kills the adopter's retrieval CLI → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-11 · CLOSED · rename detection over `git diff --find-renames`; `withdrawn` stops deleting silently. Today a gov rename deletes the adopter's file, drops the row so nothing notices, and lands no replacement → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-12 · CLOSED · `cmd_update` has no in-progress guard at all and `apply`'s stats `target/.git/MERGE_HEAD` as a path — dead in a linked worktree, the layout adopters are told to use. Plus an O_EXCL lock → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-13 · CLOSED · `govkit adopt` — the receipt bootstrap for an already-installed tree, rung-major attribution over gov history, read-only by default. Closes DEPL-aFerriedDossier-1. Without it nothing else ships → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-14 · CLOSED · post-write verification: re-run each touched kit's own `[check].argv` and roll back from recorded OIDs on red. A clean three-way can still land a plausible, wrong, executable file → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-15 · CLOSED · gov stops shipping literal `tools/<kit>/` paths inside kit bodies — 59 files do. The durable fix for the class `carry` only papers over; make check-install-prefix.sh prefix-parametric → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-16 · OPEN · memory-tree renders a record's whole Serves id list into one build-README table row, so a build with 15 units cannot carry ONE spec-audit naming them all: 15 ids blow the 350-char entry cap whatever the filename. Split as a workaround → builds/dCarriedReceipt/
- DEPL-dGaugedVintage-1 · CLOSED · `selfcheck` check 5 asserts an entry's `version_from` file exists and matches one line, never that it sits inside that entry's installed population — so a kit could ship unpinnable. Latent: every include covers it today. 5b only notes → builds/dGaugedVintage/
- DEPL-dGaugedVintage-2 · CLOSED · Status sweep. DEPL-aFerriedDossier-1's declared closer DEPL-dCarriedReceipt-13 (`govkit adopt`) shipped and its spec reads CLOSED rev-8, but the row reads SPECCED — as do dCarriedReceipt-1..15. The rows outlived their specs → builds/dGaugedVintage/
- DEPL-dGaugedVintage-3 · CLOSED · `memory-recall` is a registry DEFAULT whose `query.py` is role `forked` (`kit.toml:77`), which `LANDABLE_ROLES` excludes (`govkit.py:236`), so `apply` lands no CLI yet renders a Skill telling the agent to run it → builds/dGaugedVintage/
- DEPL-dGaugedVintage-4 · CLOSED · drift-audit ships THREE version values — `adopt-drift-audit.sh` @1.2, four files @1.4, README and harnesses @1.8 — against constant 1.8. `check-kit-versions.sh` asserts 3 of 8 marker sites, so five stale markers ship green → builds/dGaugedVintage/
- DEPL-dGaugedVintage-5 · CLOSED · Four of fifteen versioned entries ship NO `gov:kit` marker anywhere: check-wiring, codebase-map, kickoff-manifest, playbook-render. `check-kit-versions.sh:84` calls the marker where a deployer reads a version in an adopting tree → builds/dGaugedVintage/
- DEPL-dGaugedVintage-6 · CLOSED · the Install block in `tools/drift-audit/README.md` copies the kit to the ROOT prefix then invokes it at the `tools/` prefix; the two lines contradict each other, the second cannot run, and every later step agrees with the second → builds/dGaugedVintage/
- DEPL-dGaugedVintage-7 · CLOSED · `check-install-prefix.sh` ratchets hit LINES per file (`grep -c`), so a second root-prefix literal added to a line that already carries one, or one kit's path swapped for another's, keeps the count and passes green → builds/dGaugedVintage/
- DEPL-dGaugedVintage-8 · CLOSED · `update` skips attribution for every row whose evidence is `unattributed` (`govkit.py:5529`) yet re-stamps `gov_commit` forward on each `--write` (`:6204`). 47 of inCMS's 95 receipt rows are ungraded and drift further every run → builds/dGaugedVintage/
- DEPL-dGaugedVintage-9 · CLOSED · No verb reports a per-KIT version delta. The receipt stores each row's kit version string and nothing reads it back; `entry_version` (`govkit.py:333`) resolves gov's own constant, never the target's → builds/dGaugedVintage/
- DEPL-dGaugedVintage-10 · CLOSED · Nothing asserts the gov checkout is current with its remote — the only vintage guards are ancestry and reachability (`govkit.py:3769`). `update --to HEAD` on a stale clone reports `current` for rows gov has since moved → builds/dGaugedVintage/
- DEPL-dGaugedVintage-11 · CLOSED · `derive_carry_map` (`govkit.py:4840`) drops any gov directory fanning into more than one target directory, silencing the `relocate` rung for every kit shipping a rendered SKILL.md beside its engine files — seven on this adopter → builds/dGaugedVintage/
- DEPL-dGaugedVintage-12 · CLOSED · `check-install-prefix.sh` counts occurrences but not IDENTITY, so one kit's path swapped for another's at equal count still passes. Needs a third ledger field AND a second comparison in the ratchet's awk → builds/dGaugedVintage/
- DEPL-dGaugedVintage-13 · CLOSED · Gate the class DEPL-dGaugedVintage-2 swept by hand: a backlog row whose id matches a CLOSED spec while the row itself is non-terminal. Both sides are already parsed by the memory-tree kit, so it is cheap → builds/dGaugedVintage/


- DEPL-aTetheredConvoy-8 · CLOSED · Reconciled `TOOL-dClosedLexicon-13` against receipt schema 2. One of the four filed questions was the schema; two were arms riding the playbook role unit 1 corrected, one was fixture order, plus a vacuous `SKIP` mark. → builds/aTetheredConvoy/
- DEPL-aTetheredConvoy-9 · OPEN · `apply over the DEFAULT selection ran` needs unit 5's `[[outcome]]` evaluator AND a fixture whose adopters can run; today two exit 1 and four rendered destinations are absent. Not weakened to green. → builds/aTetheredConvoy/
- DEPL-dRetiredFork-11 · OPEN · `govkit update` names a `--add-kits` flag the parser does not define (`govkit.py:5870`, its only occurrence); the arm at `selftest.py:745` asserts the STRING is in stdout, so it grades the message → builds/dRetiredFork/
- DEPL-dRetiredFork-12 · OPEN · gov intake leaves gov-identical files in no register: 11 at NicoCares, one byte-identical to gov HEAD. A receipt omitting a file it could match by OID is surface no ratchet sees → builds/dRetiredFork/
- DEPL-dRatifiedSeam-2 · OPEN · LANDED SOURCES SIT OUTSIDE THE VERIFY AND ROLLBACK PASS, so a kit whose only change is a landed file is never verified and cannot be rolled back. Deferred: the fix restructures the data-loss machinery. See the review record → tools/govkit/govkit.py
- DEPL-dRatifiedSeam-3 · OPEN · `rename_dests` IS POPULATED LAZILY, so a rename destination whose row exits before `classify_row` is neither claimed nor decided, and lands as new. Fix: populate eagerly per kit. See the build's review record → tools/govkit/govkit.py
- DEPL-dRatifiedSeam-4 · OPEN · THE TRACKED-COUNT PREDICATE NEEDS A SET AND THE OBVIOUS FIX IS WRONG: `set(before) <= set(after)` reds on a legal RENAME, measured. It must excuse renames and withdrawals. See the build's review record → tools/govkit/selftest.py
- DEPL-dRatifiedSeam-5 · OPEN · `index_read` HAS NO LIVENESS ON GIT'S EXIT CODE: it batches 400 paths with `check=False`, so one bad path aborts the chunk and every row reads absent. Latent, and the dead-probe shape. See the build's review record → tools/govkit/govkit.py
