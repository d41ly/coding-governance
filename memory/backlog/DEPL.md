# deployer backlog

> Mutable. Each row leads with one status token (OPEN…WONTDO).
- DEPL-dSettledRoster-1 · OPEN · `govkit apply` appends its `eol=lf` pins LAST to a target's `.gitattributes` (`govkit.py:2011`), and last-match-wins, so gov silently overrides a target that set the opposite. Live since `0daa263`; the DEPL-aTetheredConvoy-6 fork was never owner-reviewed
- DEPL-dSettledRoster-2 · OPEN · a `govkit apply` against a `kind = "manifest"` target runs that target's own committed command TWICE — `read_gate_verdicts` at `govkit.py:1955` and `:2418`. Deliberate and disclosed, but the DEPL-aTetheredConvoy-4 fork was never owner-reviewed
- DEPL-aSealedCaravan-3 · OPEN · `WIRE-INTO-PROJECT.md` sends the reader to a section 2a for the agent-instructions kit and no such section exists; that kit is also the only `--all`-selectable one with no version constant and no runbook section at all
- DEPL-aFerriedDossier-1 · OPEN · aTetheredConvoy gives every govkit-INSTALLED repo an update path and a hand-forked repo none; grep adopt-existing across all seven specs returns zero, and the OUT clause presupposes a receipt. Ask as a pair: bootstrap one, grow one. → builds/aFerriedDossier/
- DEPL-aFerriedDossier-2 · OPEN · Widen unit 3 S5's fixture family with a non-default-prefix entry and one adopt-existing variant. Cheaper than any single fix it protects: without the first, the prefix fixes can land and regress unobserved. → builds/aFerriedDossier/
- DEPL-aFerriedDossier-3 · OPEN · 13 defects inCMS measured in this tree with line numbers: the discarded prefix answer, the literal probe, the unresolved sentinel, the unconditional engine write, absolute gov_source, an unhandled gov-side rename. → builds/aFerriedDossier/
- DEPL-dCarriedReceipt-1 · SPECCED · `{relpath}` has two resolutions: `rule_relpath` (:172) resolves it against the rule base and its docstring calls the basename form a measured defect; `resolve_dests` (:2085), the seam plan and apply call, IS that defect → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-2 · SPECCED · roles attributes/gate-leg/ci dispatch to `refuse`, which runs after the write branch and only makes r.problems non-empty — so the receipt re-stamp is skipped forever. Becomes report, plus a pins arm → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-3 · SPECCED · `cmd_intake` emits a literal `prefix = "tools"` (:3206) whatever `--answer prefix=` says, so NC's committed deploy.toml is a file the tool that owns it could not have written. Two lines → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-4 · SPECCED · `coverage_rows()` + `plan --coverage`: file-level adoption gaps, joining planned_writes (:1359) against tracked(target) (:111). Needs only deploy.toml, so it measures inCMS today → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-5 · SPECCED · `[[decline]]` in deploy.toml plus three staleness arms, so a deliberate omission is legible and a decline whose file gov withdrew reds itself. Without it coverage gets skimmed → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-6 · SPECCED · the silenced-gate-leg bar: a claimed kit's `[[gate_leg]]` argv naming an untracked engine. Fires 17x on inCMS, 1x on NC — and NC's hit is a real gov defect shipped to every adopter → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-7 · SPECCED · a receipt row carries two identities, `gov_oid` and `oid` read index-side, instead of one sha256 of worktree bytes. Without it a default Windows clone reads ~96% false divergence → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-8 · SPECCED · a merge result is stamped as gov's own (:3096), so the next gov commit reads the row `stale` (:2845) and raw-overwrites it (:3071) — every preserved hand-edit dies one update later, reported clean → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-9 · SPECCED · `carry` rungs (verbatim/eol/relocate) recomputed by proof each run over a needle map DERIVED from the receipt's own path pairs, never authored. Reaches an adopter at a non-default prefix → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-10 · SPECCED · role `forked`, report-only, for the files where gov is the derivative — three carry a `FORKED from` header. One automatic update on `recall/extract.py` kills the adopter's retrieval CLI → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-11 · SPECCED · rename detection over `git diff --find-renames`; `withdrawn` stops deleting silently. Today a gov rename deletes the adopter's file, drops the row so nothing notices, and lands no replacement → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-12 · SPECCED · `cmd_update` has no in-progress guard at all and `apply`'s stats `target/.git/MERGE_HEAD` as a path — dead in a linked worktree, the layout adopters are told to use. Plus an O_EXCL lock → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-13 · SPECCED · `govkit adopt` — the receipt bootstrap for an already-installed tree, rung-major attribution over gov history, read-only by default. Closes DEPL-aFerriedDossier-1. Without it nothing else ships → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-14 · SPECCED · post-write verification: re-run each touched kit's own `[check].argv` and roll back from recorded OIDs on red. A clean three-way can still land a plausible, wrong, executable file → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-15 · SPECCED · gov stops shipping literal `tools/<kit>/` paths inside kit bodies — 59 files do. The durable fix for the class `carry` only papers over; make check-install-prefix.sh prefix-parametric → builds/dCarriedReceipt/
- DEPL-dCarriedReceipt-16 · OPEN · memory-tree renders a record's whole Serves id list into one build-README table row, so a build with 15 units cannot carry ONE spec-audit naming them all: 15 ids blow the 350-char entry cap whatever the filename. Split as a workaround → builds/dCarriedReceipt/
- DEPL-dGaugedVintage-1 · OPEN · `selfcheck` check 5 asserts an entry's `version_from` file exists and matches one line, never that it sits inside that entry's installed population — so a kit could ship unpinnable. Latent: every include covers it today. 5b only notes → builds/dGaugedVintage/
- DEPL-dGaugedVintage-2 · OPEN · Status sweep. DEPL-aFerriedDossier-1's declared closer DEPL-dCarriedReceipt-13 (`govkit adopt`) shipped and its spec reads CLOSED rev-8, but the row reads SPECCED — as do dCarriedReceipt-1..15. The rows outlived their specs → builds/dGaugedVintage/


- DEPL-aTetheredConvoy-8 · CLOSED · Reconciled `TOOL-dClosedLexicon-13` against receipt schema 2. One of the four filed questions was the schema; two were arms riding the playbook role unit 1 corrected, one was fixture order, plus a vacuous `SKIP` mark. → builds/aTetheredConvoy/
- DEPL-aTetheredConvoy-9 · OPEN · `apply over the DEFAULT selection ran` needs unit 5's `[[outcome]]` evaluator AND a fixture whose adopters can run; today two exit 1 and four rendered destinations are absent. Not weakened to green. → builds/aTetheredConvoy/
