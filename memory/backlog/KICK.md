# kickoff backlog

> Mutable. Each row leads with one status token (OPEN…WONTDO).
- KICK-aHonedRuleset-1 · OPEN · charter §1's merge exception still names `the merge-base otherwise` while the stamp rule now says `HEAD on any branch`. It is the MERGE rule, not the stamp rule, so nothing joins them. Carriers: `AGENTS.md` and its source → coding-governance-agents.template.md
- KICK-cSettledDocket-1 · CLOSED · builds/aHonedRuleset/ · the manifest stamp rule could not satisfy its own check 5 on a feature branch: merge-base PREDATES any watched file the branch changed, so check 5 failed until you stamped HEAD. Cost 3 attempts. Fixed: the rule is now `HEAD on any branch`
