# kickoff backlog

> Mutable. Each row leads with one status token (OPEN…WONTDO).
- KICK-cSettledDocket-1 · OPEN · the manifest stamp rule cannot satisfy its own check 5 on a feature branch: it says sha = merge-base with the default branch, but merge-base PREDATES any watched file the branch changed, so check 5 fails until you stamp HEAD instead. Cost three attempts
- KICK-aReplayedCard-4 · OPEN · `KICK-aReplayedCard-3` AC1/AC2 and `TOOL-aReplayedCard-3` AC2 are owed at the first post-landing kickoff and resume: the installed engine is the primary's junction. The ledgers under `builds/aReplayedCard/build/` name each → skills/session-kickoff/SKILL.md
- KICK-aReplayedCard-5 · OPEN · the engine is 63 B under its gate; the next engine edit splits Steps 0–2b into a second file with its own size leg and moves `scratch-guard.test.sh`'s AC9 span extractor with it → skills/session-kickoff/SKILL.md
