# tooling backlog

> Mutable. Each row leads with one status token (OPEN…WONTDO).
> **Rotated 2026-08-14** — 48 terminal rows moved byte-identical to [`../archive/TOOL.2026-08-14.md`](../archive/TOOL.2026-08-14.md), covering `TOOL-aRuledParchment-2` through `TOOL-cFinalBerth-2`. Nothing deleted; the all-time id-collision grep still reaches them there.
> **Rotated 2026-08-17** — 89 terminal rows moved byte-identical to [`../archive/TOOL.2026-08-17.md`](../archive/TOOL.2026-08-17.md). TWO concurrent builds rotated on this date and that file is their UNION, deduplicated by id, so no id range names it.
- TOOL-aGradedMandate-13 · OPEN · THE EMPTY-ROSTER VACUITY IS CLOSED IN THE LEG AND STILL OPEN IN THE DRIVER. `TOOL-aGradedMandate-6` gave `pinned_units` a refusal for a build README whose units region is well-formed but names no id, because `region` exits 0 with empty stdout for a pair enclosing nothing and every membership test against an empty roster answers `absent`. `check_authorization` in `tools/unattended/unattended.sh` has the identical shape and no such refusal: an id-less BASE region passes the marker gate, `comm -23` over two empty sets yields nothing, and the subset test is vacuously satisfied, with the `UNITS_REGION_CUTOFF` refusal unreachable because the pair IS present. MEASURED 2026-08-31: seven tracked build READMEs are in exactly that shape. Found by round 2 of aGradedMandate's closing diff review, finding H2, and NOT built there because the item it guards is `authorization-reachable`, which takes no override — a wrong refusal in it is unclearable, and no further review was available to grade the change → tools/unattended/
- TOOL-aGradedMandate-14 · OPEN · TWO SKIP ANNOUNCEMENTS EACH CERTIFY THAT THE OTHER ARM RAN, AND NEITHER CHECKS. `check-unattended.sh:1709` prints `check 24's ADD arm skipped … (the RETIRE and supersession arms still ran)` and `:1722` prints the mirror, but the two guards are independent and share a fallback commit, so when BOTH baselines refuse the leg prints two lines each asserting the other arm ran and neither did. An announced skip that overstates its own coverage is worse than a silent one, because it reads as a partial verdict. Introduced by the fix for round 1's whole-check skip in the same build. LATENT: not reachable for any tracked record today, since the two live records both resolve. Candidate: compute both dispositions first, then print one of three lines — ADD skipped, RETIRE skipped, or both → tools/unattended/
- TOOL-aGradedMandate-15 · OPEN · CHECK 24 JOINS UNIT IDS BY MENTION RATHER THAN BY OWNERSHIP, which is the class `row_ids_of` closed in the driver in the same build and left open one file over. Both arms extract ids with a bare `grep -oE` over generated unit ROWS, so an id mentioned in a row's title text is read as that row's own id. `row_ids_of` takes each row's FIRST id and is the driver's answer to exactly this; the checker cannot call it, because the two files are copy-installed standalone and share no library. Measured over this corpus: 80 generated regions, zero foreign ids — a shape rather than a live instance. Candidate: spell the same first-id projection in the checker, the way the two files already spell the `| WONTDO |` predicate identically → tools/unattended/
- TOOL-aGradedMandate-12 · OPEN · THE UNATTENDED KIT'S OWN BUDGET CEILINGS ARE MEASURED ON AN IDLE NODE AND THIS FLEET IS NOT ONE. `run-unattended-gates.sh --checks` reported all three checks `ok` and then RED, because the kit gate took 845s against a declared 120s ceiling measured at 28s. MEASURED 2026-08-31 on node a: seven concurrent `check-unattended.sh` processes were running from sibling worktrees at the time, several of them another session's. The ceiling is not wrong about an idle node and it is useless on a busy one, and raising it to fit a contaminated reading is exactly what that file's header forbids. THE SHAPE OF THE PROBLEM: a wall-clock ceiling over a leg whose cost is dominated by contention it cannot see reports the FLEET's load as the LEG's cost, so the verdict carries no information about the subject. Candidates: measure CPU rather than wall clock, which is what the leg actually spends and what a ceiling can bound; or have the runner report concurrent sibling processes beside the elapsed figure so a breach is legible as contention rather than regression. Neither is designed. Found while closing aGradedMandate, whose own units did not touch that file → tools/unattended/
- TOOL-aFlaggedScaffold-3 · OPEN · `govkit update` CANNOT LAND A SOURCE GOV STARTED SHIPPING. Its classification loop is `for row in rows_all` over the RECEIPT, so it moves, renames and withdraws rows that already exist and has no arm for a descriptor source with no row. Measured on the inCMS adopter: the lexicon kit's `canon.py` was added upstream at `dec46b82`, was present in gov at the exact sha that adopter pulled (`136d26f8`), is claimed by `include = "**"`, and `plan --coverage` reports it as a GAP — and `update --write --to 136d26f8` still left it out. `lexicon.py` imports canon at MODULE level, so every entry point of that adopter's kit died with ModuleNotFoundError for six days under a green bar. Twelve of its fourteen claimed kits have descriptor sources with no receipt row; how many are real gaps versus sanctioned divergence needs per-kit judgement and was not measured. `apply` is not the workaround — it also runs the kit's `[adopt]`, which for an adopter holding a kit deliberately INERT is a posture flip. There is no verb that lands new kit bytes without adopting → tools/govkit/govkit.py
- TOOL-aFlaggedScaffold-4 · OPEN · `govkit apply` CRASHES WITH A TRACEBACK on a large adopter under Windows: the renormalize step's dirty check passes every lf-pinned path as argv (`git diff --name-only HEAD -- <lf_paths>`), which exceeds the 32 KiB command line. Measured on inCMS, whose `.gitattributes` is 28 KB: `FileNotFoundError: [WinError 206] The filename or extension is too long`. WORSE THAN A REFUSAL, because it fires AFTER the write loop and AFTER configure: the observed target was left with its new files staged, `.lexicon.conf` scaffolded, a Skill rendered, a stale write lock under its governance dir, and no receipt update — a half-applied install from a verb that reported nothing. Candidate: `--pathspec-from-file=-` with `--pathspec-file-nul` over stdin, which removes the argv bound entirely. The failing case is any adopter with more lf pins than the OS will carry → tools/govkit/govkit.py
- TOOL-aFlaggedScaffold-5 · OPEN · `govkit check` CALLS A KIT ADOPTED ON THE EXIT CODE ALONE and never runs the probe that would contradict it. `run_kit_check` ends `return ("adopted" if rc == 0 else "landed-but-inert")`, and `classify_outcome` — the function whose whole docstring is that an `[[outcome]]` block decides the meaning of an exit code by a filesystem PROBE — has exactly ONE call site, inside `_cmd_apply`. So the lexicon descriptor's `[[outcome]] code = 0` with `probe = { must_exist = ".claude/skills/lexicon/SKILL.md" }` is dead code for `check`. Measured on inCMS: `adopt-lexicon.sh --check` exits 0 by ABSENCE ("NOT ADOPTED ... a legal state"), so `check` printed `lexicon: adopted` for a kit with no conf, no Skill and no importable module. That docstring says `adopted` is "a check arm that ran and passed"; the arm passes when the kit is not there → tools/govkit/govkit.py
- TOOL-aFlaggedScaffold-1 · CLOSED · `scaffold_lexicon.py` GUARDED ITS ARGV BY ARITY ALONE, so `scaffold_lexicon.py --help` was a well-formed one-argument call and `--help` became the DESTINATION: the run derived a whole seed and wrote it to a file literally named `--help`. Not hypothetical — it shipped. Reported by the inCMS adopter 2026-08-29; the file was created there 2026-08-23, committed in `11dc524f9`, pushed, and then survived every leg of a 62-leg bar. NOTHING THERE COULD SEE IT: no leg enumerates root-level filenames, `adopt-lexicon.sh --check` looks for `.lexicon.conf` BY NAME so a stray sibling is invisible to it, and the kit was adopted INERT so no lexicon leg was wired at all. The WRAPPER already refused an unknown flag; the script it calls did not, and the script is the one that WRITES. A file whose name is a flag is also a live hazard for every unquoted glob in its directory afterwards — `wc -l *` in that adopter's root printed wc's usage instead of counting. Guard is `argv[1].startswith("-")`; `./--help` still reaches a genuine leading-dash path. Left-shifted into six arms over `--help` and `-h`, observed RED first (exit 0, success line, file on disk) → tools/lexicon/scaffold_lexicon.py
- TOOL-aFlaggedScaffold-2 · CLOSED · THE SAME CLASS ONE KIT OVER, and worse: `extract.py <repo> --help` is a well-formed three-argument call, so `--help` reached `outdir` and the `mkdir(parents=True)` below it CREATED A DIRECTORY named `--help`, exit 0. Reproduced 2026-08-29 in a throwaway repo. Found by sweeping every kit script that takes a positional path behind an arity-only guard — the sweep `TOOL-aFlaggedScaffold-1` demanded rather than a second report; `lexicon_conf.py` validates its flag token explicitly and `bench.py` only READS both positionals, so this was the only other live instance. A directory is the worse half: `rm -rf --help` does not remove it either. Both positional slots now refuse a leading dash. NOT COVERED and deliberately so: `bench.py`'s `--json <value>` will still write a file named for the next token if the value is omitted, which is the ordinary option-value footgun and not this class. Left-shifted into one arm in the kit's own selftest, registered in `order[]` so the arity assert counts it, observed RED first → tools/memory-recall/extract.py
- TOOL-aSiftedFork-7 · OPEN · THE RUN-GATES CANARY IS NON-DETERMINISTIC ON THIS HOST AND NAMES A DIFFERENT CAUSE EACH TIME, which matters more than an ordinary flake because this leg IS the bar's own liveness assertion -- the arm that proves the runner can still move. OBSERVED 2026-08-28, four runs of an UNCHANGED suite: (1) GREEN, 134 assertions, inside a full bar; (2) five clamp arms `BOTH expired` plus `executed 128 assertions, below the pinned floor 132`; (3) one arm, `GATE_JOBS='nonsense' never terminated while its width-1 control finished - the clamp let it spin`; (4) a different arm again, `GATE_JOBS suppressed the row's OTHER knob - the override is not width-only`, on a profile row reading `width 3, timeout off, ceilings INERT`. Runs 2-4 are RED and no two agree. THE COMMON FACTOR IS `timeout`: this host sometimes has a runnable one and sometimes does not, and the suite's guards cover only PART of that -- arms 1c/1d/1e and 4g/4h announce a SKIP, while the clamp loop and the profile-row arm do not, so they red blaming the clamp or blaming GATE_JOBS for what is a property of the box. Run (4) is the sharpest: the row differs because ceilings are INERT with no timeout, and the arm attributes that to GATE_JOBS, a knob it never checked. THAT IS THE THIRD INSTANCE of `names a cause it never checked` in this one suite -- `TOOL-aSiftedFork-6` is the second and this session already fixed the first. Candidate: hoist the HAVE_TIMEOUT guard to every arm whose subject is bounded execution, not just the six that carry it, and make the profile-row arm compare only the fields its own knob owns. A leg that reds differently every run cannot be read, and this one is the leg that says the bar is alive → tools/run-gates/
- TOOL-aSiftedFork-6 · OPEN · THE CANARY'S CLAMP ARMS USE `timeout` UNGUARDED, so a host that has none reds the bar with a message blaming the clamp. Arms 1c/1d/1e and 4g/4h are guarded on HAVE_TIMEOUT and announce a SKIP; the GATE_JOBS clamp loop and `clamp_expired_verdict` are not, and they call `timeout "$CLAMP_BUDGET" bash -c ...` directly. OBSERVED 2026-08-28 by putting a `timeout` on PATH that always exits 127, which is what the shell returns for a MISSING binary too, so this is the real no-timeout host and not only a synthetic one: five `GATE_JOBS='...' did not clamp to a working width` failures, plus `the expiry verdict did not report an undecidable host`, plus the distinguishability arm redding because both outcomes came back identical. NOTE THE TWO HOST CLASSES ARE DIFFERENT and only one is handled: the bar's own host has a `timeout` that WORKS but rejects `-k`, which is why 1c/1d/1e skip there while the clamp arms run fine. A host with no `timeout` at all takes neither path. THE PREDICATE IS THE DEFECT: `clamp_expired_verdict` branches on `crc = 124` and treats everything else as `the clamp let it spin`, so a control that ERRORED is reported as one that FINISHED -- naming a cause it never checked, the same class `TOOL-aSiftedFork`'s canary work already removed one level up. Candidate: guard the clamp loop on HAVE_TIMEOUT with an announced SKIP like its siblings, and split 127 from 124 in the verdict. Found while verifying that this session's assertion-floor hoist actually held, which it did → tools/run-gates/
- TOOL-aSiftedFork-5 · OPEN · THE VERDICT-EPOCH REMEDY NAMES THREE CARRIERS AND THE VERSION CHECKER WANTS FIVE, so following the failure message exactly still reds the bar on the next leg. MEASURED 2026-08-28 bumping memory-tree 2.48 -> 2.49: `tools/memory-tree/check-verdict-epoch.sh` prints `Bump it in ALL THREE places, which must move together` and lists `check-memory-hygiene.sh`, `HYGIENE.template.md` and `memory/HYGIENE.md`. Doing exactly that left `tools/check-kit-versions.sh` reporting 2 problems: `BUILD-METHOD.template.md` and `SPEC-TEMPLATE.template.md` also carry the marker and were not named. THE WORDING IS THE DEFECT, not the omission alone -- `ALL THREE` is an explicit completeness claim, so a reader who follows it has been told the set is closed when it is not, and the second red arrives after the first is fixed and looks like a new problem. Candidate: derive the carrier list in the message from the same source `check-kit-versions.sh` scans, so the remedy cannot drift from the checker; failing that, drop the count from the sentence, because a number typed beside a population it does not derive is this repo's own named class. Found while landing the aThawedCorpus merge → tools/memory-tree/
- TOOL-aSiftedFork-4 · OPEN · THE EQUIVALENCE HARNESS CANNOT SEE THE CLASS IT EXISTS TO CATCH, which is this repo's own could-not-fail shape one level up. the equivalence harness in that build's `build/` folder (on `branch/acollapsedscan-followups`, so no path to it resolves from here) compares the candidate only against a hand-written `old()` reimplementation rather than the retired code, and its hyphenated-family case HARDCODES `grep -oE "^(MY-FAM)-..."` instead of deriving the alternation from the conf under test. It therefore holds FAMILIES fixed and can never exercise the one input class where check 21's two implementations actually part company. MEASURED 2026-08-28 by driving that harness's own case sweep under five confs instead of one: the builtins candidate diverged from retired 20, 1, 21 and 21 times; the awk candidate diverged 0 times in every configuration, about 1960 cases. Both numbers came from the SAME harness — the method was sound and its inputs were not. Candidate: vary FAMILIES across prefix-overlapping and metacharacter confs, and drive both sides from the same conf rather than from a literal. That single change catches everything TOOL-aSiftedFork-3 describes, and it is the reason three review rounds on aCollapsedScan-13 found three divergences and still shipped two live ones → tools/memory-tree/
- TOOL-aSiftedFork-3 · OPEN · NOTHING VALIDATES `FAMILIES`, so a declared family code reaches check 21's predicate raw and the predicate's meaning depends on which implementation is installed. Executed grep over `tools/memory-tree/` 2026-08-28 found no validator anywhere. The retired code and the awk rewrite both match the family as an ERE; a code containing a regex metacharacter therefore matches inputs the adopter never declared, and no other check reds first. This is the substrate under the aCollapsedScan-13 regression rather than that build's own mistake: a literal-prefix implementation and an ERE implementation only disagree because the declaration permits inputs on which they can. Candidate: refuse a family code that is not `[A-Za-z0-9]+`, or refuse one that is a prefix of another declared code, at conf-read time where every checker already reads it. Cheap, one predicate, and it retires an entire divergence class rather than one instance of it → tools/memory-tree/
- TOOL-aSiftedFork-2 · OPEN · CHECK 23's AWK STREAM SWALLOWS THE DIAGNOSTIC ITS PREDECESSOR PRINTED, which is this repo's named liveness rule broken in the one file that argues it hardest. The retired per-record loop and the aCollapsedScan candidate both surface awk's `cannot open file` on a record that is TRACKED but absent from the worktree; the landed awk rewrite reads those paths with `getline < f`, whose failure is a return value nothing tests, so the record is skipped in silence. REPRODUCED 2026-08-28 against an index-present, worktree-absent record, again with a git-quoted non-ASCII name, and again with an absent SPEC. `git ls-files` enumerates the INDEX, so this input is real rather than hypothetical, and the rewrite's own header even names that fact as the reason it chose a stream over awk operands. It got the abort right and lost the report. Candidate: test the `getline` return and emit the same finding, which is one branch inside a loop that already exists → tools/memory-tree/
- TOOL-aSiftedFork-1 · OPEN · THE AWK CHECK-23 COMMENT IS WRONG BY OMISSION about the change it is documenting. It justifies moving from per-record awk to a path STREAM solely by awk aborting the whole pass on an operand it cannot open, and never says the rewrite ALSO changed how paths are split: the retired `for sp in $alspecs` word-splits, so a tracked path carrying a space or a glob character was silently skipped, double-graded, or graded as the wrong file, and the line-oriented stream is not. The behaviour moved TOWARD correctness — verified 2026-08-28 on a spec under a sub-folder containing a space, legal to checks 4 and 5, which retired skips while printing a false `check 23 measured NO unit`, and on two builds whose names differ only by glob metacharacters, where retired grades one unit twice and the other never. That makes the silent improvement more important to write down, not less: it is the only record that the pre-rewrite gate was green over files it never opened. Candidate: state it in the block header beside the operand-abort argument → tools/memory-tree/
- TOOL-aBoundedCeiling-13 · OPEN · CARRY-FORWARD from `TOOL-aBoundedCeiling-5`, retired WONTDO at the aBoundedCeiling landing on owner instruction to merge at 2.5 of 3 units. Its spec is at rev-3 and is the design; only S6 was built (the live KeyError in govkit apply below the version floor, repaired). WHAT REMAINS: a `ceiling` key on `[[gate_leg]]` rows in the kit descriptors; the govkit emitter writing it; a CEILING_FLOOR_RUN_GATES version floor resolved from the target installed runner, built as the sibling of check_target_reads_subject; the KIT_RUN_GATES_VERSION bump at run-gates.sh:19 plus its README marker; the check-7h parity arm in govkit.py; and S7, the receipt-joined override predicate that lets a slow-node adopter keep its own ceiling. WHY IT MATTERS LESS THAN IT LOOKS, recorded so nobody re-derives it: the adopter half of the original ask is substantially met without this, because TOOL-aBoundedCeiling-6 ships in the unattended kit and bounds whatever gate command an adopter declares -- which is the scripts/gate.sh case observed hanging 3h19m on node a. This unit extends the PER-LEG ceiling to adopters, which is additive. The spec was audited over two rounds and its section 8 forks are RESOLVED, so a later build starts from a design rather than a blank page → tools/govkit/
- TOOL-aBoundedCeiling-12 · OPEN · THE TURNSTILE REAPS A DEAD HOLDER AND NEVER A DEAD WAITER, so one killed bar wedges every later bar in that repository PERMANENTLY. MEASURED 2026-08-27: a `push-main.sh` landing sat 6858 s -- one hour fifty-four minutes -- at `queued at position 4` with ZERO legs run and no per-leg log written, at 0.09 s of CPU per 8 s. The queue directory `<git-common-dir>/gate-bar-queue` held four tickets and THREE were dead pids (2851350, 2916916, 3030709) left by bars killed earlier that session; the fourth was the lander. Clearing the three dead tickets by hand advanced it 4 -> 3 -> 1 and it acquired immediately. THE ASYMMETRY IS THE DEFECT: `ts_alive` guards the BEACON, and run-gates.sh even prints `reaping the beacon of a dead holder`, but the acquire predicate is only `my ticket sorts first in the directory` with no liveness test on the tickets themselves. A dead ticket sorts first forever. This is the SECOND wedge from it in one session and it is fully reproducible: kill any bar mid-queue, then run another. NOT a load problem and not the slowness this build was about -- a wedged bar reports nothing at all, which is how it was mistaken for a dead lander. Candidate: apply the same ts_alive test the beacon already uses when scanning the queue head, and drop a ticket whose pid is gone. That is one predicate reusing a function ten lines away. Also worth a liveness line: a bar that has queued longer than one TTL without acquiring should say what it is waiting on and whether that thing is alive → tools/run-gates/
- TOOL-aBoundedCeiling-11 · OPEN · A RUN CAN REACH A TERMINAL PHASE BY HAND, SKIPPING EVERY FACT THE VERB RECORDS, and one did. `ce4ef9f3` -- a commit whose subject is about govkit token guards -- edited `memory/builds/dCarriedReceipt/RUN.md` from `phase: LANDING` to `phase: LANDED` directly. `--landed` was never called, so `landed-anchor`, `unpushed-at-landing` and the frozen roster were never written, and check 15 has been RED on main ever since. The Skill is explicit that `--landed` re-observes the remote and that this is the one phase claim a run cannot simply assert -- which is exactly the property a hand edit removes. Repaired 2026-08-27 under TOOL-aBoundedCeiling-9 by writing a VERIFIED `landed-anchor: remote` (the witness and the landing commits are all ancestors of origin/main) with its provenance stated in the record; `unpushed-at-landing` was deliberately NOT reconstructed, because the driver measures it at landing time and 126 commits have landed since. THE CLASS IS UNGATED: nothing refuses a hand-written terminal phase, and the phase field is an ordinary line in a tracked file. Candidates: a check asserting that any record at a terminal phase carries the full fact set that verb writes -- which is cheap, catches this instance, and would have caught it on the commit that made it; or a stronger form joining the phase to the commit that introduced it. The weaker form is one predicate over records that already exist → tools/unattended/
- TOOL-aBoundedCeiling-10 · OPEN · A HELD LEG CANNOT NOTICE ITS OWN DRIFT, and two independent instances were found in one session on 2026-08-27. (1) `run-gates canary` was RED on an inherited stale byte-pin: `325d5f55` changed the runner summary from `kit self-tests` to `every self-test` by owner ruling and never updated the pin at `run-gates.test.sh:734`. The canary carries `chunk: selftests` so it is HELD on every default bar, and the drift survived from that commit until a full `GATE_SELFTESTS=1` run. (2) `unattended kit gate` is RED on main for a different reason with the same shape, tracked as TOOL-aBoundedCeiling-9. THE GENERAL DEFECT: holding a self-test off the bar for COST also removes the only thing that would report it breaking, so a held leg accumulates drift silently and the next full bar inherits somebody else stale red. That is the price of the 2026-08-23 ruling, and it is not an argument against the ruling -- it is an argument for a cheap periodic signal that held legs still pass. Candidates: a scheduled or pre-push-sampled run of the held population; or a much cheaper SHAPE check over the pins a held suite owns, run on every bar, that catches a byte-pin whose subject moved without running the suite. Neither is designed. Found while landing aBoundedCeiling, whose own ceilings make this cost legible but do not remove it → tools/run-gates/
- TOOL-aBoundedCeiling-9 · OPEN · the `unattended kit gate` leg is RED ON MAIN and has been since dCarriedReceipt landed: check 15 refuses `memory/builds/dCarriedReceipt/RUN.md` for claiming LANDED while naming no `landed-anchor`, with its own first commit after the declared `LANDED_ANCHOR_CUTOFF` of 2026-08-21. Verified 2026-08-27 against `origin/main`: the blob there carries no such key either, so this is not a working-tree artifact. THE MECHANISM IS THE INTERESTING HALF and it is structural rather than a slip: `--landed` writes that record AFTER the lander returns, so the record that reds this leg comes into existence after the leg that would have caught it has already run. Every landing therefore has one commit in which the bar cannot see the record it is about to be judged on, and the next run inherits the red. That is the same shape as the close/land ordering parked under aBoundedCeiling. Candidates: have `--landed` refuse to write a record it knows the leg will reject; or make the leg grade the record only from the commit that follows it; or move the anchor write earlier. Found while landing aBoundedCeiling, whose own units did not touch that file → tools/unattended/
- TOOL-aBoundedCeiling-7 · OPEN · `.githooks/pre-push` HARDCODES `tools/gate-legs.json` and `tools/run-gates/gate-fingerprint.sh`, but the install prefix is configurable (`prefix` in `deploy.toml`, defaulted to `tools` in `govkit.py`). In an adopter that installed at any other prefix, forcing predicates 6 and 7 — the diff touches the leg manifest, and the recorded manifest blob differs — match NOTHING and silently never fire, so a manifest-wide change lands there scoped against a green earned on a different leg set. Green-by-absence at the push boundary, in the hook that decides whether the authoritative bar is owed. The hook is shipped VERBATIM to every push-main adopter, so the defect ships too. Found while speccing `TOOL-aBoundedCeiling-1`, whose landing fires both predicates in gov and neither in such an adopter. Fix: resolve the prefix the way the rest of the hook resolves the tree, or read it from `deploy.toml`; a path that resolves to nothing must be a REFUSAL rather than a non-match, because those two are indistinguishable today → .githooks/pre-push
- TOOL-aBoundedCeiling-8 · OPEN · `tools/run-gates/run-gates.turnstile.test.sh` arm 4c (AC4c) CANNOT FAIL. Its behavioural half greps for `stalled holder` and then takes `&& ok ... || ok "the successor did not need to reap ... the ceiling is not reachable on this host at this timing"` — BOTH branches are `ok`, so the reap behaviour it names is asserted by nothing. The only `nope` in that arm is a grep for the literal source comment `ponytail: a single leg longer than TS_TTL` in `run-gates.sh`, which grades a comment and not a mechanism. That is the could-not-fail class charter §7 names, inside the suite that guards the concurrency holder, and it is exactly the shape `memory/gotchas/bounded-through-a-pipe-is-unbounded.md` warns about under "Arming it" — an arm asserting the message rather than measuring the clock. The spec it implements (`memory/builds/aPacedTurnstile/spec/2026-08-18-spec-TOOL-aPacedTurnstile-4.md` around :402) also claims the arm asserts `the successor's run still reports every leg`, which the shipped test does not assert at all. Found by a read-only probe while speccing `TOOL-aBoundedCeiling-1`; NOT fixed there, because that unit deliberately leaves `PROF_TIMEOUT` at 0 and touching this arm needs its failing case observed first, which is a unit → tools/run-gates/run-gates.turnstile.test.sh
- TOOL-dHonouredPark-1 · CLOSED · the authored `roster:units` pair becomes MANDATORY and its presence gated (owner, 2026-08-25). Turns `build-complete` term 3 into a real check instead of one passing vacuously on 51 of 62 builds, and closes TOOL-aPacedTurnstile-14. Work: 51 build READMEs gain a pair, the gate gains a presence assertion and its arm, and the term's semantics move from nothing-to-compare to a planned-unit-with-no-spec is a finding. `scan_canon` already reserves the pair's position after the last canonical slot, so the canon needs no change
- TOOL-dHonouredPark-2 · CLOSED · `memory/guides/BUILD-METHOD.md`'s declared budget rises to 350 lines (owner, 2026-08-25), from 310, which it was already 2 over at 470bb09b. The raise fixes the breach and NOT the blindness: M1 declares the pair and no gate enforces it, which is why a governance carrier sat over its own stated constraint unnoticed. Whether that pair ever gets a leg is a separate question nobody has been asked
- TOOL-dHonouredPark-3 · CLOSED · `tools/dead-path-waivers.txt` re-keys from `<path>:<line>` onto the surrounding line's TEXT (owner, 2026-08-25). Line keying unpinned twice inside one build, and the second re-key was first done by proximity and scrambled three rows onto the wrong reasons. Text keying survives an insertion above the hit and breaks only on rewording, which is rare and loud; it keeps the per-hit precision that path-only keying loses in a file carrying several waived mentions
- TOOL-dHonouredPark-4 · CLOSED · `--plan` and `--status` disagree about which unit is next since the roster sorts by build order: `--status` reads the generated units region, `--plan` sorts tracked specs by id. Neither is wrong about STATUS. Options: sort `--plan` by the same key, which couples the driver to the order verb; have `--plan` read the rendered region, which is the shape `check_authorization` already uses; or document the divergence. RULED 2026-08-25 and built
- TOOL-dFramedEntrypoint-1 · OPEN · two documents tell a workflow author that a bare `parallel([...])` over five or fewer literal elements passes `agent-cap` unmarked, and it does not. `AGENTS.md` §8 and `tools/hooks/README.md` both carry the sentence immediately after the bullet BANNING the raw primitive, so it reads as an exemption from that bullet. `tools/hooks/agent-cap.js` applies the array-literal allowance only to rule (b) — the RECEIVER of an `agent(` fan — while the raw primitive is denied unconditionally unless its line carries `gov:bounded-fanout`. Observed live: a five-thunk literal fan was denied on the first Workflow call of this build, and the denial message was the only correct statement of the rule available. Fix is two sentences, in the file that owns each; the hook is right and the prose is wrong
- TOOL-dScriptedRepeat-15 · CLOSED · the kit gate is 469 process spawns per invocation and the suite invokes it 243 times, so the suite’s unit of cost is ~114,000 process creations. MEASURED, correcting every earlier claim in this row: the leg is not compute-bound at all (`real 14.4s user 0.33s sys 0.62s` inside its own fixture, 93% waiting), an on-access antivirus scanner fronts every `exec` on node `d` at 0.019-0.039 s a spawn, and 469 x 0.022 s reproduces the 10.7 s invocation exactly. The "~28x", the "~23 min of fixture reset" (a reset is 0.123 s) and the 13.2 s quotient are all superseded in the build’s bar-cost record. THE LEVER TAKEN was spawn count: three loops that ran a grep or a `bash -c` per (item, file) now read each file once, 469 -> 220, counted from an execution trace both ways. Per-arm scoping was DECLINED with numbers - it saves ~320 s more, needs 243 call-site edits and carries the vacuity its own AC was written for, and does not change the outcome. The ceiling is re-declared 900 -> 1800 s with the derivation beside it. NOT OBSERVED: the suite end to end, because the owner stopped these suites; the equivalence that replaces it is 17 staged breaks, 16 of them red, byte-identical before and after → `builds/dScriptedRepeat/`
- TOOL-dUnstalledConvoy-24 · OPEN · `--close` STAGES the LANDING phase and nothing commits it, so a run that merges from another tree carries BUILDING into the merge and `--landed` refuses with `check 31`. Hit at this build own landing 2026-08-21: the fix was to re-run `--close` on the merged tree, which costs a full bar and is not obvious from the refusal text. The refusal is CORRECT — the phase attests that the DoD was evaluated, so an agent must not hand-write it — but the flow leaves a staged file the run is never told to commit. Candidates: `--close` commits the phase itself the way `--landed` callers expect, or its success message names the commit as the next step, or `check 31` detects a STAGED LANDING and says so instead of reporting the committed phase. Any of the three ends the trap; picking one is the work.
- TOOL-dSettledRoster-6 · OPEN · THREE `resolve_bash` copies exist and two of them probe a weaker property than the third: `corpus_ids.py:158` and `govkit.py:442` ask `-c ":"` (does this bash start), while `profile_bar.py:239` asks `test -f "$script"` (can it SEE the script), which is the property that actually matters and the one the WSL launcher fails. Found by the `TOOL-dSettledRoster-5` §10 reuse audit, which also corrected a gotcha record that named two. Converge on the stronger probe, or record why three shapes are right
- TOOL-dScriptedRepeat-1 · CLOSED · playbook mode: a THIRD `authorized-by:` member (`recipe`) with an attended entry point, a PLAYBOOK TEMPLATE whose every step is `GATE <leg>` or `CHECK <why>`, per-piece and set-scoped records with their own Definition-of-Done items, a `proposal` park kind, and the Skill's four start paths. TEN of eleven units built and CLOSED; unit 8 (the output-scope refusal) WITHDRAWN after its hermetic arm could not be fired, its two real defects fixed and recorded. Round-1 Tier-2 over the landing diff returned BLOCKED at 32 confirmed / 16 distinct defects, precision 0.91 — both blockers were gates that cannot fail in code this build shipped green, and the whole set is folded → `builds/dScriptedRepeat/`
- TOOL-dScriptedRepeat-12 · CLOSED · the six built units could not flip to CLOSED: main's memory-hygiene check 22 arrived mid-build and requires every numbered acceptance criterion of a closed Tier-2 unit to be evidenced by a journal record. The real debt was units 6 and 7, whose `dod_met` branches shipped with NO test arm at all. Fourteen arms written, then the ledger from the observations rather than from the specs — which caught three criteria a spec-compiled ledger would have recorded as met, including one this build ratified and never implemented (`--override pieces-complete` was accepted) → `builds/dScriptedRepeat/`
- TOOL-dScriptedRepeat-13 · CLOSED · the bypass-flag guard on the two evidence-record writers had no reader, and rev-2 moved the fix out of the file rev-1 named. Measured before building: `GITLS`, `declared_scalar` and the playbook enumeration are all ZERO in `check-unattended.sh` and non-zero in `check-playbook.sh`, so building rev-1 would have inlined a third copy of a parser past a gate that compares exactly two. It is check 10 of the playbook leg instead, over the census’s own declared `records` roots. The arm that matters asserts SHARING rather than liveness - it MOVES the declared root and reds unless both readers follow - and both write-time refusals cite check 10 again now that a check 10 exists → `builds/dScriptedRepeat/`
- TOOL-dScriptedRepeat-14 · CLOSED · a build README can assert a MECHANISM its own spec set revised, and nothing read the pair. `build_readme_mechanism_drift` in the drift audit reports it, report-only: a backticked mechanism token in a README’s AUTHORED prose where that build’s spec revision log names the same token at an entry dated later than the README line’s own `git blame` date. Ship band measured before wiring: 31 rows over 8 of 60 READMEs, 13% against a 25% ceiling, 7 of the 8 being builds other than this one. THE MOTIVATING INSTANCE IS INVISIBLE TO IT and that is the finding - spec 6 contradicted the README’s `--counts` sentence while never spelling `--counts` anywhere, so no token-identity predicate can see that pair, and neither comparison variant tried over the corpus does. So the fold item ships BESIDE the signal, in `BUILD-METHOD.md` M8, saying in its own words that the signal covers only the token-identical subset → `builds/dScriptedRepeat/`
- TOOL-dUnstalledConvoy-23 · OPEN · redesign the dispatch write-set grading, which ships DARK behind `DISPATCH_GRADING` (default blank) after four adversarial rounds failed to make it correct. Reports: `memory/builds/dUnstalledConvoy/reviews/` — rounds found 16, 10, 8 and 10 defects and the repairs introduced 5, 3 and 5, with no fall in the introduction rate. The two failure modes to design AGAINST, both reproduced end to end: a refusal no run can clear in band (round 3, terminal in an unattended run), and a driver call that retracts a failure the leg already emitted (round 4, rc=1 to rc=0 over one violating tree). The round-4 report's own conclusion is the design constraint — one predicate served three callers whose edges disagree, so the answer is separate predicates per question, not a cleverer shared one. The declaration recording, condition 1 and condition 3 all work and are not in scope to change.
- TOOL-dUnstalledConvoy-19 · OPEN · APPENDING to a test suite strands the arms, and every static signal says they are fine. `tools/unattended/unattended.test.sh` ends with `exit "$st"`, so anything added after it is dead — hit TWICE in one session, for `--rescope` and again for `--dispatch`, about fifty arms in total, while the suite printed PASS and `check-arms.py` text-matched every one. The existing guard is FLOOR_ASSERTIONS, which was 338 against an executed 398: a sixty-arm slack that hides fifty stranded arms exactly. Raised to 445 here, which fixes the instance and not the class. The class wants a STRUCTURAL arm — no executable line may follow the suite's terminal `exit` — which is one grep and cannot go slack the way a numeric floor does. `check-arms.py` cannot see it either, and its blindness to reachability is already recorded as `TOOL-cBriefedPilot-27`
- TOOL-dUnstalledConvoy-18 · OPEN · a PYTHON rewrite of a shell source silently eats a LONE CR. `open(p).read()` and `Path.read_text()` use universal newlines, so a bare CR *inside* a line becomes a newline — and `tools/unattended/check-unattended.sh` carries three of them, inside `sub(/<CR>$/,"")` awk programs. Hit on 2026-08-21: the edit looked clean, the diff looked clean, and check 18 died with `awk: unexpected newline` because its program had been split in half. Measured the blast radius across every file this session rewrote — exactly one carried lone CRs, so the damage was contained, but only by luck. The fix at the call site is `open(p, newline="")` on BOTH read and write. The GATE this wants is a byte-level arm: no tracked file gains or loses a lone CR in a diff that does not say it means to. Same family as the eol traps already catalogued, one layer up — those watch line ENDINGS, and this is a CR that is not one
- TOOL-dUnstalledConvoy-17 · OPEN · `memory/guides/UNATTENDED-PROTOCOL.md` section 7 is titled "The verbs" and lists eight of them, omitting `--park` and `--attest` — both of which the Skill documents and both of which write the run-state file. A reader who trusts the section's own title learns a verb set smaller than the driver's, and `--rescope` had to be APPENDED rather than placed, because the list's ordering principle cannot be inferred from an incomplete set. The driver already asserts its THREE enumerations name one set (header docstring, usage line, unknown-argument refusal); the protocol is a fourth carrier of that same set and nothing joins it to the other three
- TOOL-dUnstalledConvoy-16 · OPEN · a correction to a claim held by SEVERAL carriers lands in one of them and nothing notices. Hit here: `memory/guides/REVIEW-PROTOCOL.md` was corrected when the sidechain-inheritance claim was measured false, while `coding-governance-agents.template.md` and its render `AGENTS.md` kept the refuted sentence and SHIPPED it to every adopter. No backlog row covered it and no gate could see it, because the three carriers state the claim in different words — a byte-comparison finds nothing and there is no shared token to key on. Same shape as the two OPEN rows about a kit-version remedy naming three carriers where six exist, one layer up: the defect is that a claim's carrier SET is nowhere declared. Candidate: a claims registry naming each cross-carrier assertion and the files that state it, asserted in both directions like the deploy surface already is
- TOOL-dUnstalledConvoy-15 · OPEN · the bare-interpreter class survives outside govkit: `tools/lexicon/selftest.py` executes `["bash", "tools/lexicon/adopt-lexicon.sh", "--check"]` twice, and `tools/govkit/selftest.py` writes bare-`bash` argv into fixture manifests that are then executed. Both are GREEN today only because the legs they run need no module the WSL python lacks, which makes them latent rather than safe — the same call under a kit that grows a 3.11 dependency reds the bar with a traceback naming a `/mnt/c/...` path. `govkit.exec_argv` exists and is the seam; what is missing is a rule that every python-side execution of a leg argv routes through it, and the source-level arm that would gate it beyond the two govkit files it currently covers
- TOOL-dUnstalledConvoy-14 · OPEN · hygiene check 12 asserts the HEADER rev appears in section 9 and says NOTHING about the log itself, so a spec can carry two lines with the same rev number, in the wrong order, with the gate green. Hit on 2026-08-20: a batch fold anchored its revision line on `- rev-1` against a spec already at rev-2, producing a second rev-2 above rev-1 while the real rev-2 sat below it. Section 9 is described as the rev high-water a resumed session reads, so a duplicated or misordered log is read by exactly the reader it was written for. Two cheap arms: rev numbers in a log are UNIQUE, and they DESCEND. Both are one `awk` pass over a section the check already extracts
- TOOL-dUnstalledConvoy-13 · CLOSED · a build's spec-audit record cannot fit the generated record-bindings row under `BUILD_README_ENTRY_CAP_CHARS` once the build passes about eleven units, BY CONSTRUCTION: the row carries the record filename, its path and every id the record serves, and the binding grammar expands a contiguous range at authoring time so the ids cannot be abbreviated. Measured on dUnstalledConvoy - 13 units, 438 chars against the 350 tier, while the longest other line in the same file is 319. The remedy available today is a curation-debt row, which silences checks 6 and 7 on the whole file to buy one row; the renderer should instead wrap or abbreviate the serves column, or the tier should price a generated row separately from an authored one. Same renderer-shaped class as the `aRuledFrontispiece` debt row, one column over
- TOOL-dSettledRoster-4 · OPEN · `check-verdict-epoch.sh`'s remediation message names THREE carriers to bump (`check-memory-hygiene.sh`, `HYGIENE.template.md`, `memory/HYGIENE.md`) and there are SIX `gov:kit memory-tree@` markers: `BUILD-METHOD.template.md`, `SPEC-TEMPLATE.template.md`, `memory/TEMPLATE-SPEC.md` and `memory/guides/BUILD-METHOD.md` are absent from it. Following the message exactly leaves the bar RED on `kit version markers` — hit on 2026-08-20, and it is this repo's own named defect, a fix naming more than one carrier landing in only one. Derive the list from the markers. HIT AGAIN 2026-08-21 during dUnstalledConvoy: following the message exactly bumped three carriers, the bar redded on the other three, and the fix cost a second full-bar cycle — the same defect twice is what a papercut looks like before somebody fixes it
- TOOL-dSettledRoster-3 · CLOSED · the `govkit acceptance matrix` leg was RED on clean `main` and blocked every push through `push-main.sh`. This row's stated root cause was WRONG and the correction is the value here: it blamed `adopt-playbook.sh`'s `-c 'import sys'` probe for selecting an interpreter below the kit's floor. MEASURED on node d 2026-08-20, the probe never got the chance — `subprocess.run(["bash", ...])` resolves the WSL launcher in System32 before Git's bash, so the adopter ran under a DIFFERENT OPERATING SYSTEM whose python is 3.10.12 without `tomllib`, while `shutil.which("bash")` in the same process returned Git's bash with python 3.14.5 and `tomllib`. The tell was in the traceback all along: a `/mnt/c/...` path no Git-Bash tool can emit. Node-SPECIFIC — it needs WSL installed, which is why nodes a-c never saw it. Fixed by `govkit.resolve_bash`/`resolve_shell_argv`, which name the EXECUTABLE and accept a candidate only if it RUNS. Class: `memory/gotchas/subprocess-resolves-a-different-shell.md`.
  TWO SESSIONS FIXED THIS INDEPENDENTLY in the same week and the merge is the interesting part. Node d's `main` landed `resolve_bash`/`resolve_shell_argv` wired at four python-side leg executions; the `dUnstalledConvoy` branch landed a `shutil.which`-based `resolve_argv` wired at five. The merge kept main's implementation — `shutil.which` does not RUN the candidate, which is the same mistake `tools/lib/resolve-python.sh` documents for the MS-Store stub — and took the branch's FIFTH site, the `[[hole]]` discharge probe, whose shipped commands are literally `["bash", "-c", ...]` and which main's pass left unwrapped. Neither pass found every site alone. `selftest.py` now carries a SOURCE-level class arm asserting no leg argv in `govkit.py` or `matrix.py` is executed without the resolver — the behavioural arms were green in both misses, because the function was never what was broken. That arm's first draft redded on fixture text `matrix.py` writes into a synthetic target, so it now ignores matches inside string literals; observed RED on a staged unwrapping before landing.
  Latent siblings NOT fixed: `tools/lexicon/selftest.py` runs a bare `bash` twice and is green today because its leg needs no 3.11 module — TOOL-dUnstalledConvoy-15 carries that.
- TOOL-dSettledRoster-1 · OPEN · `AGENTS.md`, the one file every session is pointed at, is reachable by no PATH gate. `check-install-prefix.sh`'s population is `tools/* skills/* `.githooks/* *.template.* *.fragment.json` and measured 200 tracked files with zero named `AGENTS.md`; hygiene check 15 scans `memory/` only. `corpus_ids.py` check 16 does read it, and since TOOL-dSpentCeiling-1 it reads it STRUCTURALLY rather than as a byte BUDGET — but it grades the charter's CITATIONS, never the charter file itself, so the gap this row names is unchanged
- TOOL-aMeteredTurnstile-6 · OPEN · the bar did not get slower, PROCESS CREATION did: measured 2026-08-20 on node a against the 2026-08-11 baseline, `bash -c true` 22.5ms -> 581ms, `git rev-parse` 23.9ms -> 727ms, `python -c pass` 103.7ms -> 1297ms, `mktemp -d`+`git init` 2751ms. CPU 39% with CPU queue 0 and disk queue 0, so it is serialized per-op latency, not contention. Defender real-time is ON; exclusions need admin to read. Widening the pool is 26% SLOWER at width 24 vs 8 — measured, do not retry. REFINED 2026-08-21 by TOOL-aScannedThrottle-1 §3.8: the tax is LOAD-DEPENDENT, not standing, so this row's "serialized per-op latency, not contention" conclusion does not survive. Re-measured on a QUIET box, median of 15: `bash -c true` 32.5ms, `git rev-parse` 48.0ms, `python -c pass` 151.2ms, `mktemp -d`+`git init` 158.1ms — within 1.4-2.1x of the 2026-08-11 baseline (`git rev-parse` is the 2.01x, and it is the op that scales), and `git init` 17x faster than the degraded reading above, which was taken under four concurrent bars
- TOOL-aMeteredTurnstile-2 · OPEN · the bar LEAKS scratch git repos into TMPDIR and never sweeps them: 786 dirs / 6862 top-level entries on node a, 531 from 2026-08-18 alone, each holding .git plus tools/. `du -sh /tmp` did not finish in 4 min. ISOLATED cost: `git init` 2350ms there vs 1564ms in a clean dir on the same volume, so ~1.5x on the bar's hottest op — a multiplier on TOOL-aMeteredTurnstile-6, not the cause. Give each leg's scratch an EXIT trap and sweep. RE-MEASURED 2026-08-21 by TOOL-aScannedThrottle-1 §5, WORSE than recorded: 1053 dirs against this row's 786, +34%, with 291 created on the measurement day alone. `run-gates.sh` never sets `TMPDIR`, and one export there would redirect all 63 `mktemp -d` sites at once
- TOOL-aMeteredTurnstile-3 · OPEN · `gate-timings.tsv` evicts on the RUN, never on the manifest: the carry-forward predicate at run-gates.sh:290 is "did this run measure it", so a renamed leg keeps its row forever. Measured 88 rows against 85 legs, 3 orphans holding 965s = 12% inflation on any sum. REFINED 2026-08-21 by TOOL-aScannedThrottle-1 §5 — MECHANISM REAL, INSTANCE GONE: the LIVE ledger is clean at 0 orphans, 88 rows against 88 legs. The orphan rows sit in the dead `gate-timings.tsv`, which nothing has read since the rename to `gate-ledger.tsv`: measured 2026-08-20 at 964.2s across the then-live per-worktree copies, and 13.96s across FOUR orphan rows in the common-dir copy that survives at HEAD (`memory hygiene (20 checks)`, `marker contract (4 readers)`, `verifier fan-out`, `template size <=32KiB`; re-derived 2026-08-21), the worktree cleanup at 49aea26 having removed the rest. So the eviction predicate is still wrong and nothing is currently inflated by it — RE-DERIVE the sizing before pricing a fix, and note this row's surviving "12% inflation" is the 2026-08-20 reading of a population that no longer exists. The comment at :287 says a dropped leg falls out on its own — TRUE of dispatch order, FALSE of the file, and it is the write path it describes
- TOOL-aMeteredTurnstile-4 · CLOSED · `AGENTS.md` states bar figures in prose beside the source that owns them, breaking the charter's own rule. RE-CITED 2026-08-21 by TOOL-aScannedThrottle-1 §5 — this row's original citation (`AGENTS.md:492`, "335s serial to ~95s at width 8") no longer resolves anywhere in the file, so following it led to prose that is not the defect. THREE live claims, all wrong. The wall/leg-sum pair states 873s where the measured mean across four GATE_FULL bars is 1001.3s — the mean is 14.7% ABOVE the charter's figure, and the charter's figure is 12.8% BELOW the mean; one gap, two readings, so name the base. The dispatch-cache sentence names `gate-timings.tsv`, a DEAD file since the rename to `gate-ledger.tsv` — so the charter names a file nothing reads as the live scheduling input, which is the subject of this build's own headline finding. And `AGENTS.md:480` states that EACH self-test leg carries a `guard`; derived from `tools/gate-legs.json` on 2026-08-21, four `selftests`-chunk legs carry none. CLOSED 2026-08-21 on the owner's decision: all three corrected. The dead filename and the guard universal now point at their sources, and the wall/leg-sum pair is re-measured to 393s/3085s with both halves of its gov-canary arm moved together
- TOOL-aMeteredTurnstile-5 · OPEN · longest-first dispatch pessimises time-to-first-signal by construction: 8 slots filled by the 8 longest legs means nothing reports until the 8th longest returns. MEASURED on node a — zero of 86 legs reported for 9.3 minutes, 17 by +20 min, then 50 more in the next 5. Makespan-optimal and feedback-worst; report time-to-first-signal as its own number. CONFIRMED and BOUND TO R1 2026-08-21 by TOOL-aScannedThrottle-1 §4: 669.1s to first signal with a warm ledger against 5.1s with a cold one, a 131x swing decided solely by ledger warmth. It is the COST of fixing TOOL-aScannedThrottle-8, not an independent item — ship that fix with a reserved short-leg slot, or it trades a real 16% of span for a perceived regression. DECOUPLED 2026-08-21: TOOL-aShardedFloor-4 is WONTDO, so this is no longer the cost of anything — it stands alone on its own merit, which is that 669.1s to first signal is bad on a warm ledger regardless of what dispatch buys
- TOOL-aPromptedMandate-14 · OPEN · `marker-contract.test.sh` gates the four readers of the generated region's MARKERS and nothing gates readers of its ROW GRAMMAR, which is why a THIRD open-coded copy (`verb_landed`) survived a fix whose whole rationale was that copies diverge. Generalise the contract to the row selector, or route every reader through `unit_rows`
- TOOL-aPromptedMandate-11 · OPEN · `keepalive-reaped` is attestable but not checkable: the driver records an id it can never verify, so an agent can attest it in good faith and be wrong. MEASURED here — this run asserted twice that two keepalives died with their processes because the store is session-scoped, and `CronList` showed both still firing. Needs a read-back the agent can be held to
- TOOL-aPromptedMandate-8 · CLOSED · `build-complete` is unsatisfiable for any build following the method: `unit_rows` selects `^| [` across the WHOLE gen:build-index region, which renders a UNITS table and a RECORDS table, and record rows carry no `| CLOSED |`. M4 mandates review records, M8 a closing review. Measured on LANDED builds: aBranchedMandate 7 such rows, aStandingWrit 2 — built as TOOL-aPromptedMandate-12
- TOOL-aPromptedMandate-9 · OPEN · nothing in the driver's precondition chain has a timeout and the slowest link is NOT the ls-remote `TOOL-aBoundedVerdict-10` suspects: measured under four concurrent bars on node a, `check-wiring.sh --check` took 1m22s at 2s user / 4s sys (I/O-bound) while ls-remote answered in 2.1s, so --preflight alone exceeds a two-minute budget
- TOOL-aPromptedMandate-10 · CLOSED · the run-gates canary reports `GATE_JOBS='0' never terminated — the clamp let it spin` under heavy machine load, then passes in isolation on a fresh TMPDIR. Its non-termination verdict is a TIMEOUT and cannot tell a spinning clamp from a starved one; the message asserts the former — built as TOOL-aPromptedMandate-13
- TOOL-aPromptedMandate-7 · OPEN · the leg's SOURCE-level no-write arm (check-unattended.test.sh) greps for `mv|rm|cp|sed -i|tee` preceded by any non-alnum, so it matches those substrings inside IDENTIFIERS: a variable named `rmode` reds it as a write. Cost me a rename; the next person pays the same. Anchor on a word boundary or on a command position
- TOOL-aPromptedMandate-1 · SPECCED · the prompt-mode declaration: `authorized-by: prompt` in the build README's front matter, read at BASE through the parse check_authorization already does, recorded as `mode:` in Run facts and second-opinioned by the leg. Measured first: the branch anchor already accepts a run-authored, pushed build folder, so no authorization code moves
- TOOL-aPromptedMandate-2 · SPECCED · RESEARCHING and TESTING join PHASES_CORE, CORE_FLOOR 10:8 -> 12:8. Core rather than PHASES_EXTRA (measured working) because the conf reserves the core vocabulary to the kit and this mode depends on it
- TOOL-aPromptedMandate-3 · SPECCED · a new BUILD-METHOD M12 states the research->test->choose loop so a directive can point at it; M1's stated budget rises 250->290 lines / 20->22 KB with the reason in place. Owner-ratified at kickoff, because M3 veto 2 makes a governance carrier's stated constraint an owner turn
- TOOL-aPromptedMandate-4 · SPECCED · `researched:M12:prompt` and `solution-tested:M12:prompt` join DIRECTIVES_CORE, introducing the layer's first SCOPED member (closed set all/prompt, absent field = all). DIRECTIVES_FLOOR 11->13; leg check 16 gains a fourth arm that must refuse an absent scope column rather than compare two empty sets
- TOOL-aPromptedMandate-5 · SPECCED · the Skill's prompt start path: orient, ONE AskUserQuestion iff under-determined, write the build folder carrying the prose and the answers, commit, PUSH, preflight, kickoff hand-back. The turn sits before the push so it is provably older than the authorization, which is stronger than a driver-side refusal
- TOOL-aPromptedMandate-6 · SPECCED · the driver-then-leg cross-component leg, closing TOOL-aStandingWrit-8. Four arms from this build's own measurements; arm 3 asserts exit 0 AND empty output, because an incomplete fixture returned a green code while other checks failed
- TOOL-aWalkedCorpus-2 · DEFERRED · a recall quality floor. Measured NOT buildable against bench.py as it exists: it always exits 0, its flag set is closed, it needs an uncommitted data dir, and verbatim.json pins it byte-for-byte. See the spec's §1 for what a successor needs
- TOOL-aDeclaredCeiling-1 · OPEN · make the template ceiling a DECLARED pin rather than a shell constant, the way .memory-tree.conf's READ_PATH_CEILING declares a byte budget with each movement justified beside the number. Recorded by TOOL-aSiftedPlaybook-1 §4 as this repo's established answer
- TOOL-aDeclaredCeiling-2 · OPEN · the recall corpus cannot reach a constraint declared in a conf file. Its root is $MEMORY_ROOT and confs sit at the repo root, so READ_PATH_CEILING was live prior art TOOL-aSiftedPlaybook-1 §10's own query could not surface. A gap in what retrieval indexes
- TOOL-aDeclaredCeiling-3 · OPEN · a slug-carrying backlog row minted after a run reaches LANDED permanently reds unattended check 8: gen_build_index derives the README's ids: as an OUTPUT, so the README moves while a terminal run's frozen copy cannot, and the driver rightly refuses to reopen it
- TOOL-aRootedPrefix-3 · OPEN · hygiene checks 6/7 measure RAW working-tree bytes, so an adopter without the eol=lf pin still gets a platform-dependent cap and entry budget — normalize before measuring, as `check-template-size.sh` already does
- TOOL-aNumeralWarden-2 · OPEN · agent-cap's enclosing-opener walk is defeated by two nested wrappers or 59 lines of distance between the `.map` and the `agent(` call; it needs a statement-level walk, not an opener count, and the 58/59 boundary is unfixtured
- TOOL-aNumeralWarden-3 · OPEN · a drift-audit pin RAISE is indistinguishable from a population drain to every gate leg; `--check` compares only `value > pin`, so `ORPHAN_ID_PIN` 4 -> 5 and `handkept` 1 -> 7 both landed unchallenged
- TOOL-aCandidStub-1 · OPEN · `tools/hooks/agent-cap.js` blesses an identifier bound from an EMPTY array literal and never re-examines it when a later statement grows it, so `const batches = []` plus a per-finding `push` fans one agent per item past hook and gate alike — reproduced at exit 0
- TOOL-aCandidStub-2 · OPEN · `tools/memory-tree/adopt-memory-tree.sh` scaffolds no `gotchas/`, and `gotchas.py` short-circuits on an empty record set, so a fresh tree gets checks 17-19 green over zero records and no catalogue — this repo's own `vacuous-selector-empty-population` class
- TOOL-aUnmannedHelm-2 · OPEN · the `ledger_dir` in `tools/drift-audit/drift_report.py` points at an authored run-state directory under `memory/project/` that hygiene check 3 forbids, so the two kits disagree about where one may live and the probe stays DECLARED_EMPTY
- TOOL-aCandidStub-4 · OPEN · `tools/memory-tree/merge-rows.py` degrades to conflict markers under `git merge` in a LINKED WORKTREE: its grammar load raises ConfError against a conf path inside the memory-recall kit dir. Run directly on the same stages it merges CLEAN
- TOOL-aSealedCaravan-1 · OPEN · `install-prefix-waivers.txt` keys each waiver on `<path>:<line>`, so any edit ABOVE a waived line unpins it and the gate reds on a merge that touched nothing it guards — hit on its first real merge. Key on the matched text or a marker comment
- TOOL-aUnmannedHelm-10 · OPEN · three root docs (`AGENTS.md`, `WIRE-INTO-PROJECT.md`, `.gitattributes`) come out of a linked worktree CRLF with no `eol=lf` pin; nothing byte-reads them today, so this is latent, but `check-wiring.sh`'s eol population is scoped to `.claude/` and will never see them
- TOOL-aStandingWrit-1 · SPECCED · the unattended run authorizes on the committed build README at BASE rather than an owner-authored mandate block, `RUN.md` becomes wholly generated, and a `--plan` verb derives the gap list from spec status headers and review records
- TOOL-aStandingWrit-2 · OPEN · SECURITY in landed code: the pinned BASE anchors on a remote-tracking ref that `unattended.sh:119-120` says needs a push to move. `git update-ref` moves it offline; reproduced with a control, a forged anchor passes preflight, the leg and close
- TOOL-aStandingWrit-5 · OPEN · `.githooks/pre-commit`'s branch guard fails CLOSED on a bogus `GOV_DEFAULT_BRANCH` but is DISABLED by an honest-looking one: setting it to the branch already checked out makes the primary-tree guard pass. Lower severity than the pre-push twin, which is now fixed
- TOOL-aStandingWrit-6 · OPEN · the leg's BASE path now OBSERVES the remote (TOOL-aBranchedMandate-3), but `GOV_DEFAULT_BRANCH` and `refs/remotes/*` still feed its OTHER consumers in the same file. Narrowed, not closed: S6 repaired one predicate's path
- TOOL-aStandingWrit-7 · OPEN · nothing binds the EXECUTING kit code to code an owner approved: a run may edit the driver, the leg or the protocol and commit them, and every parity leg compares two files that same run can change together. This bounds every property the kit asserts
- TOOL-aStandingWrit-8 · CLOSED · TOOL-aPromptedMandate-6 built `cross-component.test.sh`: the driver and THEN the leg over one tree, six arms each first performed by hand. The gap was real — writing arm 5b found that a re-preflight cannot forge a mode disagreement, because `mode:` and `base:` are both pinned once
- TOOL-aStandingWrit-9 · OPEN · `--plan`'s FORKED axis reads only §8's first non-blank line, so a spec whose first line is `none` and whose later bullets are unresolved classifies READY; `check-arms.py` cannot see this class because it is an unreached success path, not a fail branch
- TOOL-aTimedTurnstile-1 · CLOSED · `run-gates.sh` runs all 47 legs SERIALLY: warm 382.1s, cold 607.3s, real runner 335.2s. A scratchpad 8-way runner does the same 47 legs in 79.9s with `fails=0` — the heavy legs are already hermetic. The template this repo SHIPS already requires concurrent legs. CLOSED 2026-08-21 by TOOL-aScannedThrottle-1 §5: FALSE at HEAD. The pool landed and the bar runs its legs concurrently at a declared width; the row's 79.9s target sits ~11x below the current floor, measured at 812-926s on the three of four GATE_FULL bars §2 reconstructed before the sweep (per-leg records for all four are gone as of 2026-08-21)
- TOOL-aTimedTurnstile-2 · OPEN · 29 of 47 legs are SELF-TESTS holding 96.7% of wall (368.7s); the 18 checking real repo state cost 12.7s. Only leg 8 has a guard. Over the last 12 commits a records-only one drops 335s to 12.7s. Owner call: guards at the push boundary make the full bar diff-scoped. STALE 2026-08-21 by TOOL-aScannedThrottle-1 §2 — the population moved: 88 legs, not 47, and leg-seconds are 4644-5245 against this row's 368.7s wall. The guard mechanism it asks for LANDED but NOT universally — derive the split from `tools/gate-legs.json` rather than from this row: on 2026-08-21 FOUR `selftests`-chunk legs carried no guard (`template size gate selftest`, `method-carriers self-test`, `agent-cap restatement self-test`, `testsuite counts self-test`), while `govkit selfcheck` and `testsuite counts (every bar self-test prints one)` are `declarations` legs and are correctly unguarded. What survives is the RATIO, unmeasured at the current leg count. Re-derive before acting on any figure here
- TOOL-aTimedTurnstile-3 · OPEN · with legs concurrent the floor is the longest leg UNDER LOAD (~76s, merge-rows replay), not the sum — each leg dilates 1.66x at 8-way. `check-memory-hygiene.test.sh` invokes the 4.6s hygiene gate 13 times. Sharing fixture setup is the only route below ~80s. REFINED 2026-08-21 by TOOL-aScannedThrottle-1 §2 and §3.9 — the DILATION holds and the FLOOR does not. 1.66x is confirmed at 1.5-1.85x (the canary costs 317s standalone against 466-587s as a width-8 leg). But the floor is 812-926s on the three bars §2 reconstructed before the sweep (per-leg records for all four are gone as of 2026-08-21), not ~76s, and the longest leg is an `unattended` selftest rather than merge-rows replay. "Sharing fixture setup is the only route" is refuted: R2's sharding buys 27.6-30.5%. Correct the floor figure, or the row argues from a number 11x low
- TOOL-aTimedTurnstile-4 · OPEN · cold/warm is 1.59x (607.3s vs 382.1s) and every fresh worktree pays it — a new unit's first run is the cold one. Spawn tax here: `python -c pass` 103.7ms, `bash -c true` 22.5ms, `git rev-parse` 23.9ms. Defender exclusions are a no-code lever. KEPT OPEN 2026-08-21 by TOOL-aScannedThrottle-1 §6 — these spawn figures are the 2026-08-11 BASELINE that build measured against in §3.8, and R4 rests its Defender refutation on them, so they are load-bearing elsewhere and must not be edited away. UNVERIFIED at the current leg count: every bar in that sample had a warm filesystem cache, so the 1.59x cold/warm factor is carried forward untested, and R4 prices the no-code lever UNQUANTIFIED — measure with `New-MpPerformanceRecording` before excluding anything
- TOOL-aTimedTurnstile-6 · OPEN · two classes want `memory/gotchas/` records: a poll tick is a PROCESS here (75ms per 50ms sleep, 317s of a 617s run), and `jobs -rp` counts only RUNNING jobs so a just-forked worker reads absent. Each needs an INDEX re-render plus a dossier claim. KEPT OPEN 2026-08-21 by TOOL-aScannedThrottle-1 §5's extension — NOT re-measured by that build, and named here so the omission is a recorded answer rather than a silent one. Its 617s run predates the 88-leg bar. The poll-tick mechanism is independently corroborated at §3.6, which counts up to ~480 process creations per nested canary run from the same shape
- TOOL-aTimedTurnstile-8 · OPEN · review aTimedTurnstile-1 gates A3/B2, one grep leg each: no `*.test.sh` may compare a `_ms` variable to a hardcoded literal without a same-run baseline, and no `[ "$VAR" -lt` on env input without an arithmetic clamp. Both are classes this build created
- TOOL-aTimedTurnstile-7 · OPEN · review aTimedTurnstile-1 gate A2: `check-arms.py` forbids absence-only arms yet cannot see this class — its population needs a `fail() {` helper and excludes `*.test.sh`, which is why F1, F4 and F5 shipped together. Needs its own spec
- TOOL-aSealedCaravan-2 · OPEN · `check-arms.py` scans tracked `*.sh` only, so no Python write path is armed — `govkit.py` becomes the repo's strongest new one and its least armed. Extending it is a governance-carrier change and an owner turn. Raised out of DEPL-aSealedCaravan-2 F3
- TOOL-aSealedCaravan-4 · OPEN · the memory-tree renders are install-prefix-correct but NOT MEMORY_ROOT-correct — at a renamed root the rendered HYGIENE and BUILD-METHOD still name `memory/` throughout, and the templates say so in prose instead of substituting it
- TOOL-aSealedCaravan-5 · OPEN · the row-keyed merge driver is INERT in a linked worktree: an absolute `GIT_DIR` makes `recall_conf`'s `git -C <kit> rev-parse --show-toplevel` return the KIT dir, so the conf lookup refuses. Hit live; the e2e arm's fixture is a normal repo and cannot see it
- TOOL-cFinalBerth-4 · OPEN · the three named refusals `aMooredAnchor` S4 scoped for check 9's silent exits never landed; its rev-4 carried-forward list omits S4 and the spec closed. With no `origin/HEAD` the block is skipped in silence, and check 15's ancestry half inherits it
- TOOL-cFinalBerth-5 · OPEN · the run-gates canary's `par*2 < ser` ratio FLIPS run to run on one tree: red then GREEN on two consecutive full bars at 550d9b6, no edit between; the red missed by 4.5% (7705x2 vs 14753). Its fixtures are fixed sleeps, so the margin is overhead-dependent by construction. NOTED 2026-08-21 by TOOL-aScannedThrottle-1 §5: did NOT reproduce in that build's four-bar sample. The canary's between-run spread there was 25.9% across comparable bars, which is consistent with this row's mechanism and is not an independent observation of it
- TOOL-cTracedPromise-2 · OPEN · the §6 acceptance-witness rule, deferred from cTracedPromise on cost: require each AC bullet to name a machine-checkable witness. Candidates measured at 123 of 359 AC bullets across 33 CLOSED specs
- TOOL-cTracedPromise-3 · OPEN · tighten `closed_specs_with_no_product_commit` to an id-only key, the unit-level claim. Blocked on id-in-subject being universal: today it misses 3 of 13 against slug-or-id's 1
- TOOL-cTracedPromise-4 · OPEN · `check-memory-hygiene.test.sh` printed an AUTHORED assertion count, stuck at 130 while arms landed under it. The numeral is deleted; derive one from the `hit`/`miss` and inline `st=1` arms instead
- TOOL-cTracedPromise-5 · OPEN · an adopter who fills `TRACE_CUTOFF` before closing any spec after it reds `drift-audit records` with DEAD PROBE — the hole pushes them straight into it. Document `DECLARED_EMPTY` as the interim state
- TOOL-cTracedPromise-6 · OPEN · hygiene check 10 resolves a rotated index's live counterpart by fixed path, so it is blind to every `memory/backlog/*.md` shard; a 48-row rotation went unnoticed. Resolve it anywhere under the memory root
- TOOL-cTracedPromise-7 · OPEN · the unset-`TRACE_CUTOFF` row prints `DEAD PROBE` though `--check` ignores it: the status ladder reads `live` and never `gateable`. Print the note instead, so `not asked` and `blind` stop reading alike
- TOOL-cBriefedPilot-7 · OPEN · the `build-complete` DoD item: `--close` blocks while a roster unit is unbuilt. D8's "land only when fully done" had no checker
- TOOL-cBriefedPilot-8 · OPEN · the `closing-review-recorded` DoD item: a tracked review under this build names the run's pinned BASE
- TOOL-cBriefedPilot-13 · OPEN · leg check 17 grades every parked waiver line for a declared handle and a non-empty reason; shape only, and P1 is why
- TOOL-cBriefedPilot-14 · OPEN · leg check 18 asserts the Skill names kickoff AFTER preflight — order, not presence
- TOOL-cBriefedPilot-22 · OPEN · check 16 arm A's join extended to the protocol's own §3 phase list and §4 DoD table, which are joined to PHASES_CORE and DOD_CORE by no leg today
- TOOL-dClosedLexicon-1 · SPECCED · an opt-in lexicon kit gating three declared naming predicates, with the seed table derived-then-frozen, coverage modes per language, and the pin-direction guard CUT on two independent defects → `builds/dClosedLexicon/`
- TOOL-dClosedLexicon-2 · SPECCED · wires the verb table into the codebase-map ratchet and two drift-audit signals; adds no new gate leg, and records that the ratchet DELETION direction is the load-bearing half → `builds/dClosedLexicon/`
- TOOL-dClosedLexicon-14 · OPEN · govkit `check` printed `playbook: landed-but-inert` and exited 0 over a target where the playbook was never installed: the hole's `! grep -qE …` over two ABSENT files turns grep's rc 2 into success. Green-by-absence in a verb whose whole job is verifying
- TOOL-dClosedLexicon-15 · CLOSED · SUPERSEDED BY THE TREE, re-measured 2026-08-30 at 093730e4 by `TOOL-aScouredKit-2`'s convergence lens. The row said the `playbook` entry declared both files `project-owned`, so the default selection installed no playbook. It now declares ONE file at `role = "seed"`, which is landable per `LANDABLE_ROLES` in `tools/govkit/govkit.py`; the descriptor's own comment records the repair, and a measured default `apply` writes the charter to `AGENTS.md`. The lead sentence is left rewritten rather than deleted because a reader who greps `project-owned` must not find a false claim standing under an OPEN token → tools/govkit/entries/playbook.kit.toml
- TOOL-aBranchedMandate-5 · OPEN · `adopt-drift-audit.sh` diffs its render with no `[ -s ]` test, so empty-vs-empty PASSES; both sibling adopters guard it
- TOOL-aBranchedMandate-6 · CLOSED · `memory-recall` selftest cleanup cannot remove read-only git objects on Windows; 3,616 leaked scratch repos, re-measured at 4,905 on 2026-08-20 and 71% of this node's whole `%TEMP%`. `cleanup()` now clears the read-only bit and retries, and a final arm FAILS on any survivor — staged red first at 29 of 32 surviving — built as TOOL-aTetheredScratch-2
- TOOL-aTetheredScratch-4 · OPEN · A TEST FIXTURE COMMITTED INTO THE LIVE REPO. Two commits authored `t <t@e>` titled `init` landed on branch/temp-files-user-root-3e6e22 at 2026-08-20 03:40:27 and 03:43:57, each carrying the session's whole working tree; squashed away, tree byte-identical, nothing lost. `tools/check-wiring.test.sh` line 90 is the only line in the tree combining an UNGUARDED `cd "$D"` with `git commit -q --allow-empty -m init`, and `--allow-empty` commits a POPULATED index (proven in a scratch repo) rather than an empty one — so a failed `cd` lands the caller's staged work as a fixture commit. Fix is `cd "$D" || exit 2` on :87 and :90. LOOSE END: that line also runs `git config user.email t@e` first, and neither the worktree nor the common config carries it, so the reconstruction is not complete — settle that before closing · 2026-08-20 the two-line guard LANDED: `cd "$D" || exit 2` on :87 and :90, which is the idiom line 14 already used. Reproduced first in a scratch repo — UNGUARDED the caller gains a commit authored `t <t@e>` AND its local user.email is overwritten to t@e; GUARDED the subshell exits 2, the caller keeps 0 commits and its own identity. The LOOSE END is still open and is now sharper, not settled: no `t <t@e>` commit is reachable from main, and 5c83c18 at 04:02 — 19 minutes after the accident — is authored egor@compeeto.com, so the identity write did NOT persist. A failed `cd` at :90 would have persisted it, so the reconstruction still needs a mechanism that leaves no config trace
- TOOL-aTetheredScratch-3 · OPEN · `tools/memory-recall/selftest.py` leaves 2 EMPTY `tmp.*` dirs per run that never route through `cleanup()`, so the survivor arm added by TOOL-aTetheredScratch-2 cannot see them. Measured on a controlled TMPDIR: 32 swept, 2 unswept. Three orders of magnitude smaller than the leak just closed, and a different mechanism — find who creates them before widening the arm. ADJUDICATED 2026-08-21 by TOOL-aScannedThrottle-1 §5.1 as IN SCOPE BUT IMMATERIAL to the bar's wall clock: 2 empty dirs per run of one selftest, against the 1053 populated scratch repos `TOOL-aMeteredTurnstile-2` measures, so it moves no measurable span. Recorded rather than skipped; a first reading excluded it for naming the scratchpad directory, which its text does not say
- TOOL-aBranchedMandate-8 · OPEN · `--preflight` OVERWRITES a live non-terminal run-state file, losing the keepalive id and re-pinning the anchor
- TOOL-aBranchedMandate-9 · OPEN · `--preflight`'s rotation and scaffold run BEFORE checks that can return 1, stranding the record with no repair verb
- TOOL-aBranchedMandate-10 · OPEN · `map_extractors.template.py` unions the two JS scans with a bare `+`, contradicting its own dedupe line above
- TOOL-aBranchedMandate-11 · OPEN · `govkit.py`'s `blocked` skip reason is unreachable behind `cmd_apply`'s early merged-rule refusal
- TOOL-cBriefedPilot-23 · OPEN · a newline in an `--override` or `--abort` reason forges a second parked line, because `park()` writes it verbatim into a region with a line grammar
- TOOL-cBriefedPilot-24 · OPEN · `--plan` cannot see a unit BUILT on the run's branch but not landed: specs stay OPEN until the build lands (measured — cKeyedLaunchpad has 7 built units, all OPEN), so `next:
- TOOL-cBriefedPilot-25 · OPEN · only `--preflight` re-splices RUN.md's generated region and it refuses a dirty tree, so a mid-run edit to the build README strands the copy until the tree is clean and the keepalive id is to hand
- TOOL-cBriefedPilot-26 · OPEN · the driver's remote observation is environment-sensitive: the anchor arm passes under `bash unattended.test.sh` and FAILS under a python subprocess, measured 2026-08-15
- TOOL-cBriefedPilot-27 · OPEN · `check-arms` certifies an assertion EXISTS in a test file; the suite certifies which lines RUN. Neither notices arms appended past the summary — unit 12 landed nine that never executed, and both gates were green
- TOOL-cBriefedPilot-28 · OPEN · the parallelism routes are re-openable on evidence: R2+R5 needs E3 and E4 RUN (they never were); R3 needs the CLI on PATH; R1 needs a budget key without a prompt id
- TOOL-cBriefedPilot-29 · OPEN · M7 re-reads BUILD-METHOD.md WHOLE at every pass boundary and nothing counts that RECURRING cost. Check 16 measures the path ONCE; the per-boundary re-read is invisible to it. Figures deliberately not restated here — read them from `corpus_ids.py --report`, since both the ones this row used to carry went stale
- TOOL-cSettledDocket-7 · OPEN · `tools/memory-recall/recall-opened.test.sh` is tracked and NO gate leg runs it, so `check-testsuite-counts` cannot see it. A suite nobody runs is one nobody notices going quiet — wire it or record why not
- TOOL-cSettledDocket-8 · OPEN · `DIRECTIVES_EXTRA_TABLE` has one reader, check 16. Unit 2's 'shown to the agent' half is unmet: the rendered Skill does not point at the project's table, so a declared extra is joined but never displayed
- TOOL-cSettledDocket-9 · OPEN · a terminal spec whose Open-questions section is present but EMPTY stays silent: the `q == 0` refusal fires only when the range never opens. Present-and-empty still reads as resolved
- TOOL-cSettledDocket-10 · OPEN · `check-testsuite-counts` says 'every bar self-test' and grades only `*.test.sh`; eleven python self-tests on the manifest are out of population. Widen the selector or narrow the sentence
- TOOL-cSettledDocket-11 · OPEN · check 8 exempts a TERMINAL record by clearing `rd`, skipping the malformed-MARKER refusal as well as the emptiness one — the over-wide scoping cBriefedPilot-36 shipped. Scope it to emptiness alone
- TOOL-cSettledDocket-12 · OPEN · `tools/lexicon/selftest.py` failed the bar on a `shutil.rmtree` traceback and passed on re-run — a Windows temp-dir race, not a defect
- TOOL-cSettledDocket-13 · OPEN · `unattended.test.sh` loops the verb set across three carriers, all inside the driver, so a verb no agent is told about is invisible to it — that gap shipped `--park` as a blocker
- TOOL-cSettledDocket-14 · OPEN · the same gate on the conf axis: every key the engine READS must appear in the protocol's key table and the shipped conf example. `DIRECTIVES_EXTRA_TABLE` landed with one reader and no catalogue entry
- TOOL-cSettledDocket-15 · OPEN · citing a non-terminal spec id from product source has red the bar three times; the remedy (paraphrase, never cite) is recorded and nothing enforces it
- TOOL-cSettledDocket-16 · CLOSED · the TOOL index was at its 20480-byte cap with nothing terminal left to rotate, so new rows were paid for by shortening old ones. Closed by TOOL-aRelaxedShard-1: the bound is now a declared 61440 and the index sits at 33%
- TOOL-aTetheredConvoy-1 · OPEN · the playbook template reached origin/main carrying UNRESOLVED conflict markers, and both gates over it pass with the markers present; `git diff --check` caught it, and that is not a leg. Wire a marker scan over the shipped product surface
- TOOL-aBoundedVerdict-6 · OPEN · the run-state authored region's 8 KB spill rule becomes load-bearing once `--park` makes parking cheap: parks are rare hand-edits today, so the spill has never fired, and crossing the cap mid-flight reds the bar and blocks `--close`
- TOOL-aBoundedVerdict-7 · OPEN · nothing records inter-unit dependencies; a build README's authored order carries them in prose, so `TOOL-aBoundedVerdict-3` S3's every-remaining-unit-depends-on-it test is the run's judgment. A front-matter dependency field would make it derivable — its own unit
- TOOL-aBoundedVerdict-32 · OPEN · `TOOL-cBriefedPilot-18` AC9 was CLAIMED at a close it was not met at. It reads "§1's roster bullet in both halves no longer reads `Opt-in by presence`, grepped and found zero"; at that spec's own base the phrase was present once in each half of the protocol pair, and `git log -S` over either half returned exactly one commit — `6200674`, which ADDED it, five days before that spec closed. It was made true later and by another unit: `fd8f6fa` removed the phrase as `TOOL-aBoundedVerdict-11`'s landing. Recorded rather than repaired — the spec is CLOSED and on another node, and a claim that was false when written is not fixed by a later commit making it true. What this row is FOR: nothing gates an acceptance criterion against the tree at close time, which is the class, and it is why the audit found three defects in a document two parity legs call green
- TOOL-aBoundedVerdict-8 · OPEN · no leg asserts a review subject AT the cap with a non-clean verdict was actually DISPOSED of, so `TOOL-aBoundedVerdict-1` S8 is advisory. The check spans three units' mechanisms (cap, park verb, halt code); write it once all three have landed, not before
- TOOL-aBoundedVerdict-9 · OPEN · hygiene check 10 is INERT for backlog archives: it derives the live index as stem-dot-md at the MEMORY ROOT, but a backlog index lives one level down, so the derived path never exists and the `[ -f ]` guard skips it. No backlog rotation reference is checked
- TOOL-aBoundedVerdict-10 · OPEN · `unattended driver selftest` HANGS on node `a` inside its first `--preflight` (traced), zero output at 240s. It wedges the WHOLE bar at 46/65: `run-gates.sh` has no per-leg deadline, and the driver's `ls-remote` anchor call has no timeout either. Two fixes. ANCHOR HALF DONE (2026-08-20, aBoundedVerdict) — the ANCHOR half is DONE (2026-08-20): every remote observation in the driver and the leg now goes through one bounded helper, file-captured because a command substitution around `timeout` bounds the verdict and not the clock. The PER-LEG DEADLINE half is not, and stays open here; it belongs to `run-gates.sh`, which is a different kit and a different owner. Note while resolving: a sibling row measured `check-wiring.sh --check` at 1m22s against `ls-remote` at 2.1s under load, so the anchor call was never the slowest link in `--preflight` — bounding it removes an unbounded WAIT, not the latency || AND SEPARATELY, NOT REPRODUCED 2026-08-21 by TOOL-aScannedThrottle-1 §5.1 — that leg COMPLETED in all three bars §2 reconstructed before the sweep, at 812.1 / 925.5 / 887.9s, so the 240-second symptom did not recur. Per-leg OUTPUT timing was never reconstructed and the traced `--preflight` hang is unaddressed, so this is not a refutation: the row stays OPEN on its own terms, and its no-per-leg-deadline clause is untouched
- TOOL-aTetheredRecord-1 · CLOSED · mint the five missing spec H1 ids; the orphan waiver drains empty and `ORPHAN_ID_PIN` falls 5 -> 0 → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-2 · CLOSED · the record-to-spec binding grammar and its read-only parser; it classifies and never raises → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-3 · CLOSED · the retrofit: every record gains a binding line, by a precedence in which the filename is not evidence → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-4 · CLOSED · check 21 — four branches over five armed fail sites; the filename projects the header and cannot drift → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-5 · CLOSED · the generated region renders each record under the ids it serves, plus the two coverage joins → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-6 · CLOSED · the adopter path: their first run is RED, and the migration is ordered — mechanical, then measured → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-7 · CLOSED · the rename: every record filename names a spec via a REDEFINED ordinal, with no grammar change → `builds/aTetheredRecord/`
- TOOL-aRelaxedShard-2 · OPEN · check 6's build-README class (25,600 B, `cl=0`) has NO byte-axis arm: `tRunOk/README.md` and `tRunBig/README.md` are front-matter stubs and nothing sizes one past the cap, so a class whose only bound is bytes is unarmed. Found regrounding TOOL-aRelaxedShard-1
- TOOL-aRelaxedShard-3 · CLOSED by deletion (TOOL-dSpentCeiling-1) · the complaint was that a byte budget over a file whose length tracks OPEN-BUILD COUNT measures parallelism. It did, and the budget is gone rather than corrected: `memory/LIVE.md` is still generated and still tracks that count, but nothing sums it any more. Measured while closing: over the seven days to 2026-08-25 `LIVE.md` SHRANK 303 B while the read path grew 43460 B, so the parallelism term was real but was never the driver — two kit-rendered guides supplied 64.9% of that growth
- TOOL-aWiredReckoning-1 · OPEN · govkit's 45 new defs skip the verb table; VERB_OFFENDER_PIN raised 417->463. Curate them, drop it
- TOOL-aWalkedCorpus-6 · OPEN · govkit has no rule kind meaning 'in the kit dir, deliberately NOT in the payload'. project-owned drops it from the ** pool but derives an ORDER telling the adopter to supply a file gov withholds
- TOOL-aWalkedCorpus-10 · OPEN · codebase-map's `seeded-extractors-or-crashed` outcome stays unmarked because its NAME admits it cannot tell a seed from a crash. Split it in two so the seeding half can be an accepted stop, or rename it to what it detects
- TOOL-aWalkedCorpus-5 · OPEN · govkit selfcheck partitions gate-leg guard pathspecs into four PREFIX classes and a repo-root file (.memory-tree.conf) lands in none, so a leg cannot guard on a root conf. Hit wiring the recall floor; the guard was narrowed instead
- TOOL-aWalkedCorpus-4 · OPEN · memory-recall selftest.py mints its two synthetic corpus ids in a LIVE family, so any record quoting them trips hygiene check 14 as orphans. Sibling fixtures use a dead family. Hit twice in one hour: a review record, then the row recording it
- TOOL-aLoosenedCeiling-5 · OPEN · `tools/install-prefix-waivers.txt` keys on `<path>:<line>`, so any insertion above a waived hit unpins it silently. TOOL-aLoosenedCeiling unpinned the same two rows TWICE in one build. The lexicon's waivers key on the matched TEXT for exactly this reason
- TOOL-aLoosenedCeiling-6 · CLOSED · `tools/workflows/tier2-review.js` writes a `**Serves:**` line with no kind token, so hygiene check 21 reds on every closing review record until it is hand-edited. M8 already makes the agent rename the file; the kind belongs in the harness. CLOSED by TOOL-aBoundedVerdict-14 S7: the synth prompt now instructs a diff-review binding line carrying an id, because a kind alone is malformed under the grammar
- TOOL-aBranchedMandate-12 · CLOSED · `--close` discards `$GATE_CMD`'s output, so a blocked close names no leg
- TOOL-aBranchedMandate-13 · CLOSED · `build-complete` reports a bare unmet when the README has no roster region
- TOOL-aPacedTurnstile-8 · CLOSED · shard `unattended driver selftest` (659.9s) and `unattended gate selftest` (634.6s): together 32% of all leg time, and the first alone is 75.6% of the 873s wall, so the bar's floor is ~660s at any width on any hardware. Every scheduling mechanism in aPacedTurnstile competes for the remaining 213s. Measured on node a at 6517579. RE-STAMPED 2026-08-21 by TOOL-aScannedThrottle-1 §4 R2, and FIRST BY MEASURED SPAN of everything that build ranked — a claim of this ranking, not a state the backlog can hold, which has no priority field. RETRACT this row's "~660s at any width" floor and the "remaining 213s" hung off it: measured at 812-926s on the three bars §2 reconstructed. It is the ONE change that moves the span: sharding both buys 292s (27.6%) on one measured bar and 282s (30.5%) on another. Three numbers decide how. The driver alone buys 38.9s (3.7%), because the gate selftest just becomes the new floor, so BOTH must move together or the work is wasted. TWO shards each is sufficient: past that the bar is throughput-bound at ~766s and further splitting buys exactly zero, so target that crossover and not a floor number. Each split adds ~2.75s of leg-seconds for one fresh scratch repo. CLOSED 2026-08-21 by TOOL-aShardedFloor-2 and -3, which sharded BOTH into two by an argument contract rather than a file split. The bar is now THROUGHPUT-bound: longest leg 336s against leg-seconds/width 386s
- TOOL-aPacedTurnstile-9 · OPEN · prove each leg `guard` COMPLETE, the sound fix TOOL-aPacedTurnstile-7 defers: run a leg with everything OUTSIDE its guard perturbed and assert the verdict does not move. Today the canary checks only that a guard RESOLVES to a tracked path, so under-declaration is unchecked — and once the push boundary is diff-scoped a too-narrow guard costs a wrong merge verdict rather than a late signal
- TOOL-aPacedTurnstile-10 · OPEN · the row-keyed merge driver FAILS on every real `git merge` and its gate cannot see it. Git exports `GIT_DIR` to a merge driver; under it `git -C <kitdir> rev-parse --show-toplevel` returns the `-C` dir itself, so `recall_conf.repo_root()` resolves to `tools/memory-recall/`, `load_conf` finds no `.memory-tree.conf` there and `resolve()` refuses. Hit on this build's reground of `memory/backlog/TOOL.md`; reproduced with one variable — `GIT_DIR=$(git rev-parse --absolute-git-dir)` alone flips `repo_root()` from the repo root to the kit dir. It fails CLOSED (conflict, not silent take-ours), which is `TOOL-aMendedLedger-8`'s property holding, so the cost is a hand-resolve rather than a wrong merge. `merge-rows.test.sh` is green because it calls the driver directly (`DRV="bash tools/lib/pyrun.sh …merge-rows.py"`) and never through a configured `git merge`, so git's own invocation environment is absent from all 46 of its cases. Two halves: anchor the root on `__file__` rather than shelling out to `rev-parse`, and add one case that drives a real `git merge` with `merge.rows.driver` wired
- TOOL-aDeclaredBound-6 · OPEN · `tools/workflows/tier2-review.js` describes its own find phase twice and the two disagree: line 7 says `≤5 concurrent`, line 128 says `ONE ≤6-wide wave`, and the code fans at 5. Stale prose in the harness the BINDING protocol points at
- TOOL-aPacedTurnstile-11 · OPEN · govkit's selfcheck cannot see that an entry SPELLING another entry's command must hard-depend on it. `.githooks/pre-push` invokes the merge-bar runner, but a `requires` edge only ORDERS a selection - it pulls no missing kit in - so a target can receive the merge-bar hook without the merge bar and the deployer reports success. Deferred out of `TOOL-aPacedTurnstile-1` by that spec's section 8 fork B, which ships the default-selection line instead: adding the arm there would change govkit's contract inside a unit that only moves the runner. The arm belongs beside the other cross-entry assertions in `tools/govkit/selftest.py`, keyed on the command string an entry declares
- TOOL-aPacedTurnstile-12 · OPEN · govkit's selfcheck joins a descriptor's `[[gate_leg]]` rows to the repo's `tools/gate-legs.json` by NAME only and never compares the two declared GUARDS, while `govkit.py`'s emit verb copies a descriptor's guard verbatim into a target's manifest. So a leg can be guarded correctly in gov and narrowly in the kit that ships it, and every gate stays green: the run-gates canary checks only that a guard RESOLVES to a tracked path. Found as a live instance in `TOOL-aPacedTurnstile-7` round 3 (T10) - the kit/dogfood parity leg, whose gov row `-7` S10 widens to `memory/guides/` and whose `tools/memory-tree/kit.toml` row would have stayed narrow with nothing able to observe it. `-7` AC9b now arms the descriptor side inside the memory-tree kit's own suite, which closes THIS leg; the general join is here. Narrower than `TOOL-aPacedTurnstile-9`, which defers proving a guard COMPLETE - this only asks that two declarations of one leg's guard agree
- TOOL-aPacedTurnstile-13 · OPEN · the fingerprint helper `TOOL-aPacedTurnstile-5` S5 specifies is empty-on-any-failure, which makes an EQUALITY join fail OPEN rather than closed. Empty is right for the REUSE consumer (`TOOL-aPacedTurnstile-6` reads it as 'no reuse') and wrong for the equality consumer: `TOOL-aPacedTurnstile-7` predicate 0 compares a stored digest against a fresh one, and empty == empty reports AGREEMENT, so a doubly-failed fingerprint SCOPES the push boundary instead of forcing it. One sentinel serving two consumers whose safe directions are opposite. Raised and REFUTED as a finding against the round-4 fold, correctly - it is a pre-existing property of the sentinel, and S7's failed-nothing precondition blocks the write side, so realising it needs a run that failed at both ends AND a record written from it. Filed rather than dropped: the fix is a distinguishable failure token rather than empty, or predicate 0 treating an empty value on either side as a force. Recorded under Refuted in the round-4 review under `memory/builds/aPacedTurnstile/reviews/`
- TOOL-aPacedTurnstile-14 · CLOSED · `memory/builds/aPacedTurnstile/README.md` carries NO roster marker pair, so the roster is unreadable to every verb that wants it: `--plan` silently falls back to "tracked specs under spec/" (which cannot see a planned unit nobody has specced - the exact case that join exists to report), and the `build-complete` DoD item blocks with "no well-formed roster marker pair" rather than with a unit list. Found at --close on the aPacedTurnstile run. NOT fixed there deliberately: editing the authorization artifact at the moment a gate blocks on it is the wrong instinct, and it would not have unblocked anything anyway, since six units are OPEN and the item stays unmet either way. The fix is to wrap the README's authored Units table in the roster marker pair - and worth checking the other build READMEs in the corpus for the same gap, since nothing gates the markers' PRESENCE, only their well-formedness once present CLOSED by TOOL-dHonouredPark-1: the pair is now mandatory on every tracked build README and gated by the slot leg's trigger 4, and aPacedTurnstile's own README gained one in that unit's migration
- TOOL-aPacedTurnstile-15 · OPEN · the unattended kit has no check that the PRIMARY tree is landable, and `tools/push-main.sh` is the only permitted lander but refuses everywhere except there. Hit at the close of the aPacedTurnstile run: the primary tree's `main` was clean and on main - so push-main would have SUCCEEDED - while carrying 11 unpushed commits, three of them a different build entirely - the aDeclaredBound spec set, mid-flight, its round-1 audit BLOCKED on 33 findings (its ids are deliberately NOT cited here: they are defined on the primary tree's main and not in this branch, so a citation would be an orphan, which is itself a small proof of the coupling this row is about). Landing would have pushed that build's unreviewed work to the remote under aPacedTurnstile's mandate, irreversibly, with every gate green and no verb saying a word. The `gates-green` DoD item runs the bar on the RUN's tip, not on what the landing push would actually carry, and `--landed` only observes ancestry AFTER the fact. Two halves worth building: a preflight or close-time check that the primary tree's HEAD is an ancestor of the run's own tip (or is at origin/main), naming any commit the landing would carry that the run did not make; and a decision about whether the lander should be runnable from a linked worktree at all, since forcing every landing through one shared tree is what couples independent builds' landings together in the first place
- TOOL-aFusedCharter-4 · CLOSED · the unattended kit deadlocks when several builds land on one trunk. `--landed` refuses before the push (check 32: HEAD not an ancestor of the advertised tip); the push refuses while more than one run-state is non-terminal (check 7); and check 15 correctly refuses a hand-written LANDED whose witness is not on the remote. Three builds at LANDING therefore cannot be resolved by any in-band verb — measured 2026-08-19 with aDeclaredBound, aPromptedMandate and aFusedCharter, resolved only by marking two of them ABORTED. The checks are each right; the gap is that nothing lets a run reach LANDED as part of the same push that makes it true. → memory/builds/aFusedCharter/ — CLOSED by aUnblockedFleet: check 7 no longer FAILS on the count, it reports it, so three builds at LANDING wedge nothing. Two of the three guards this row names were always correct and are untouched; the one that made them jointly unsatisfiable is gone.
- TOOL-aBoundedVerdict-20 · OPEN · the aBoundedVerdict spec audit's UNFOLDED residue: 6 high clusters (AC8's spelled-numeral grep, the missing ACs for -2's S10, the fork marks four specs claim and do not carry, the plan-verb-vs-classification-table FORKED disagreement, -18's F1 resolved against an open row saying the opposite, and -13's half of TOOL-aBoundedVerdict-10) plus 31 medium and 10 low findings. Source on disk: `memory/builds/aBoundedVerdict/reviews/2026-08-19-review-TOOL-aBoundedVerdict-1-2.md`
- TOOL-aBoundedVerdict-29 · OPEN · `check-verdict-epoch.sh`'s remedy names THREE files to bump; the kit has FIVE marker sites, so following it exactly leaves `check-kit-versions.sh` red on BUILD-METHOD and SPEC-TEMPLATE. Two gates disagree about one kit's marker population. GENERALIZES: drift-audit also has five sites (constant, README, two workflow `meta.version`, two `gov:kit` markers) and a 1.4->1.5 bump took THREE `check-kit-versions.sh` rounds to settle. The remedy should be DERIVED from `kit.toml`, not enumerated in prose
- TOOL-aBoundedVerdict-30 · CLOSED · drift-audit's `non_terminal_specs_cited_by_product_source` read 10 against pin 2. NOT the deadlock the row claimed: 3 hits were a PREFIX bug (`-F` with no `-w`, so `-1` matched inside `-11`), 2 were shipped source naming UNBUILT ids, and 5 were units genuinely CLOSED by the landing. Fixed at the oracle plus a red-proved sibling arm; kit 1.5
- TOOL-aBoundedVerdict-31 · OPEN · every `TemporaryDirectory()` in the kits can red the bar on WINDOWS for a reason unrelated to what it grades: a held handle at block exit raises PermissionError [WinError 32] AFTER the arms pass. Blocked a push whose 80 arms were green. Fixed in `tools/lexicon/selftest.py` only; 9+ other files carry the same pattern and none tolerate cleanup errors
- TOOL-aBoundedVerdict-28 · OPEN · `BUILD-METHOD.md` is at 249 lines against its own ≤250 self-declared cap after -14 S5, so the next unit that touches M8 must DISPLACE rather than add. M1 states the rule and no gate reads it — the hygiene cap for a guide is 750 lines, ten times looser
- TOOL-aBoundedVerdict-27 · OPEN · `unattended.test.sh`'s 344 assertions share ONE mutable fixture tree with undeclared coupling: an arm placed between coupled arms, or above the helpers it calls, fails indistinguishably from a wrong predicate. Cost four passes in one unit
- TOOL-aBoundedVerdict-26 · OPEN · `run-gates.sh | tail -N` returns TAIL's exit status, so the idiom that makes the bar readable discards the only thing it is for. Reported a RED bar as exit 0 during this run — the same class as the close path's discarded refusals, one altitude up
- TOOL-aBoundedVerdict-25 · OPEN · `check-unattended.sh` carries three RAW CR bytes inside CR-stripping awk programs, and any text-mode reader turns a lone CR into a newline, splitting those programs silently. Hit while appending a check: it broke check 18, which that edit never touched
- TOOL-aBoundedVerdict-24 · CLOSED · a run that CLOSES but cannot LAND blocks every later run's bar. LANDING is not in PHASES_TERMINAL, so the record stays live and leg check 7 counts it forever — aPacedTurnstile parked there and now reds the bar for aBoundedVerdict — CLOSED by aUnblockedFleet: LANDING is still non-terminal and leg check 7 still counts it, but the count no longer reds the bar. A run parked at LANDING is now reported, not enforced against.
- TOOL-aBoundedVerdict-22 · OPEN · the resolver ban in `unattended.test.sh` greps `(python3?|py) `, which matches a bare `.py ` FILENAME before a flag and not just an unresolved launcher, so a refusal's remedy naming a script trips it. Worked around by putting the name last; narrow the regex
- TOOL-aBoundedVerdict-23 · OPEN · `--status`/`--resume` pick the next unit as the first non-terminal row in RENDER order (id-sorted), not the roster's DEPENDENCY order, so a resuming run can be sent to the wrong unit. Observed: reported `-1` where the roster puts `-11` first
- TOOL-aScannedThrottle-9 · OPEN · LEFT-SHIFT from TOOL-aScannedThrottle-1's closing review: eight of eighteen round-2 defects were ONE class — a review finding naming three carriers of a fact, fixed at one, which leaves the copies in open disagreement where before they were wrong together. Recorded as the acute-form section of `memory/gotchas/two-answers-to-one-question.md` - it is that class drifting DURING the edit meant to converge it - because no gate for it exists. The mechanical form is a `multi-site-fix-parity` leg: for a commit claiming to resolve a review finding, extract the file:line sites that record names and red when the commit touches some and not others. Its failing case has NOT been observed, so it is a row and not a leg
- TOOL-aScannedThrottle-10 · OPEN · LEFT-SHIFT, four gates that review proposed, none built and none seen RED, so each needs its failing case observed before it is wired: `disposition-parity`, `figure-carriers`, `derived-count-in-prose`, `census-emits-ids`. Each is specified in the Left-shift section of `memory/builds/aScannedThrottle/reviews/2026-08-21-review-TOOL-aScannedThrottle-1-diff-2.md` and is NOT restated here. The one worth knowing without opening it: `disposition-parity` must assert in BOTH directions, because the one-directional version certifies the report and misses the row, which is how F1 and D1 both landed
- TOOL-aScannedThrottle-11 · OPEN · LEFT-SHIFT, three classes both review rounds named UNGATEABLE, for the §10 checklist rather than a leg: an adjudication that EXCLUDES a record quotes that record's own words; a disposition VERB covers every clause the cell quotes, or the cell quotes only the clause it dispositions; a corroborating derivation is INDEPENDENT of the value it corroborates. The instance behind each is in `memory/builds/aScannedThrottle/reviews/2026-08-21-review-TOOL-aScannedThrottle-1-diff-2.md`
- TOOL-aShardedFloor-1 · CLOSED · `run-gates evidence` was UNRUNNABLE inside a bar whose parent exports GATE_BASE, which `.githooks/pre-push` does: `rec_run` passed the ambient env into the nested runner it grades, which resolved a sha its scratch lacks, ran everything and skipped nothing, so the arm whose subject IS a skipped leg refused. Latent. FIXED: `rec_run` clears the GATE_* vars. Class `memory/gotchas/inputs-inside-the-subjects-reach.md`
- TOOL-aScannedThrottle-8 · OPEN · RE-MINTED from `-1` 2026-08-21: that seq is the build UNIT's, and one id naming two unrelated subjects resolves to neither. The dispatch hint is stored PER-WORKTREE and was renamed `gate-timings.tsv` -> `gate-ledger.tsv` without migrating: measured 2026-08-20 at 24 of 26 worktrees with no ledger — re-derive with `git worktree list` before pricing, the population MOVES and the cleanup at 49aea26 took it to 6 the same night — and an absent cache scores every leg 0.0 so `sorted` returns MANIFEST order, silently. Controlled pair in one worktree, hint absent then present: floor leg dispatch rank 55/87 -> 1/87, start +157.9s -> +0.0s, span 970.0s -> 925.5s, packing 0.84 -> 1.00 — and the second run's floor leg was 14% SLOWER. Costs 15.6-16.3% of span on every cold worktree, which is where every new unit starts. Read the ledger from the git COMMON dir with a per-worktree fallback, fall back to the legacy filename on read, and SAY when the hint is missing. Ship it with a reserved short-leg slot or it makes time-to-first-signal 131x worse (TOOL-aMeteredTurnstile-5). RE-PRICED 2026-08-21 at 38.5s / 8.9%, NOT the 15.6-16.3% above: sharding the selftests made the bar throughput-bound, and this row's whole mechanism is starting the FLOOR leg earlier. Controlled pair in memory/builds/aShardedFloor/build/. Price from 8.9%
- TOOL-aScannedThrottle-2 · CLOSED · the turnstile queue wait is printed to stdout at `run-gates.sh:518` and recorded NOWHERE: `$gd/gate-queue-status` is deleted at :505 when the wait ends, the run-record header has 19 keys and none is the wait, and `gate-last-summary.txt` carries only profile/chunks/verdict. A lander queueing behind one peer experiences ~31-35 min where every measurable span reads ~16. One `queued` key at :708-737; `profile_bar.py` already reasons about queue time and cannot see it. CLOSED 2026-08-21 by TOOL-aShardedFloor-1: paired `queued`/`queued_from` header keys plus a summary line, zero new stdout bytes, and a dash rather than a zero when the turnstile was off or unresolved
- TOOL-aScannedThrottle-3 · OPEN · ZERO Defender exclusions are confirmed on node a (`Get-MpPreference` answers "N/A: Must be an administrator to view exclusions"), real-time + behaviour + on-access + script scanning all ON, Defender the only AV, no EDR. Third-party measurement of THIS workload shape (parallel agent-driven Bash on Windows) got 64.78s -> 7.58s, 8.5x, from excluding two Git-for-Windows folders. But the cost is NOT established here: this node measured `bash -c true` at 22.5ms on 2026-08-11 with Defender on and unexcluded, so exclusions cannot recover a regression Defender was not causing. Run `New-MpPerformanceRecording` over one bar and `Get-MpPerformanceReport -TopPaths -TopProcesses` BEFORE adding any exclusion
- TOOL-aScannedThrottle-4 · OPEN · HVCI/VBS is ON and ENFORCING on node a (`Win32_DeviceGuard` SecurityServicesRunning=2, VirtualizationBasedSecurityStatus=2, CodeIntegrityPolicyEnforcementStatus=2, OS 26200), and Microsoft's own Q&A carries a third-party 3-7x CreateProcess slowdown on exactly this shape — a parent spawning short-lived children and capturing their output — attributed in-thread by MS staff to SYNCHRONOUS antimalware inspection under Device Guard, unresolved. It explains nothing about a REGRESSION if it predates the 2026-08-11 baseline. The whole branch turns on one discriminator: establish the enablement date. Owner's setting, not an agent's
- TOOL-aScannedThrottle-5 · OPEN · `run-gates canary` (466-587s in-bar, 317s standalone, 10-11% of leg-seconds) is NOT on the critical path: deleting it entirely moves the bar 0.0%, and splitting it moves it 0.0% even after the floors are sharded, because splitting divides work without removing it and the bar is then throughput-bound. Traced on a quiet box, 99.9% attributed: 72% of it is five lines — `:458` 88.7s (4 reps x a 30-leg width-1 nested run), `:583` 57.5s (19 nested 2-leg runs), `:229` 55.7s (4 rendezvous runs), `:743` 23.3s (a deliberate 20s sleep), `:395` 21.2s. Runner overhead solves to ~1.5s fixed + ~0.74s per leg, which is half the canary and 1.3% of the bar. Only worth attacking AFTER TOOL-aPacedTurnstile-8; then halving it buys ~4% of span
- TOOL-aScannedThrottle-6 · OPEN · legs DILATE 1.5-1.85x inside the pool: `run-gates canary` costs 317s standalone on a quiet box and 466-587s as a bar leg at width 8. Every standalone leg timing therefore understates the bar by roughly half again, which is a correction that belongs on any future sharding estimate — a shard measured alone will look cheaper than it lands. Confirms TOOL-aTimedTurnstile-3's 1.66x at the current leg count
- TOOL-aScannedThrottle-7 · OPEN · `run-gates evidence` reds under load on a 5-SECOND bound: `run-gates.evidence.test.sh:225` gives a nested `GATE_FULL` bar `timeout -s KILL 5` to write its run-record header, and `run-gates.sh:600-606` measures 2136ms to header-on-disk in a TWO-FILE scratch repo. Failed both scoped bars run from this worktree today ("no header after a hard kill — the crash case is unreadable") and passed in the peer's uncontended bar at e6098aa, so it is load-dependent, not base-deterministic. Same class as the turnstile arm's 3s window graded against a >4s startup floor: a fixed wall-clock bound on a box whose process creation moves 25x. Grade the arm against a same-run control rather than a literal
- TOOL-dScrubbedConduit-2 · OPEN · playbook.fixture.md hardcodes `tools/unattended/` in outputs/grain/records/legs, so check-playbook exits 1 for any adopter at another prefix. Obstacle is kit.toml shipping it role="engine" (verbatim, no placeholder pass), not the record filenames. → tools/unattended/playbook.fixture.md
- TOOL-dScrubbedConduit-3 · OPEN · `[[hole]]` discharge probes are run by nothing on the bar. keepalive-tool-names has never passed in gov and nobody noticed. Fixing one probe leaves the next equally unobserved. → tools/govkit/registry.toml
- TOOL-dUnstalledConvoy-34 · OPEN · three driver refusal messages have no arm asserting them, all in the `--record-piece`/`--record-set` bypass-flag family and all predating `TOOL-dUnstalledConvoy-33`. Found by the suite's own arm-coverage check on the one run of it this session made. NOT fixed: outside that unit's scope, and confirming a fix needs the suite the 2026-08-23 standing owner instruction forbids running. Whoever picks it up should verify with a hermetic probe built from the suite prologue, the way `-33` did, and hand the owner the full-suite command rather than running it. → tools/unattended/unattended.sh
- TOOL-dUnstalledConvoy-35 · OPEN · `signal_lexicon_ratified_stale` compares the ratification stamp against the last commit touching `LANGS=` at all, armed or dark. A `dark` declaration is a refusal to EXTRACT, so it provably adds no definition to the corpus the VERBS table governs and cannot invalidate a curation — but it reds the signal exactly as an armed language would. Widen it to ignore dark-only surface changes, with an arm proving an ARMED addition still fires. Deliberately not done at the close of `TOOL-dUnstalledConvoy-26`: a signal edited to clear its own red is the shape nobody should trust. → tools/drift-audit/drift_report.py
- TOOL-dUnstalledConvoy-36 · OPEN · `refusal_join.py`'s JOIN half has never executed. Its docstring promises "every refusal branch in the deployer is reached by an arm that asserts it", and the leg runs it with no reached-set, so every run prints `enumeration only` and exits 0 — enumeration reading as coverage. Found by the closing review of `TOOL-dUnstalledConvoy-26`, which also found six unarmed refusal branches the join would have caught (armed by hand since). Wire a reached-set: a trace hook in `selftest.py` writing the anchors it asserted, the way `corpus_ids.py` already does, then join. The BRANCH_PIN raise to 161 in that build is a floor, not the mechanism. → tools/govkit/refusal_join.py
- TOOL-dUnstalledConvoy-37 · OPEN · `core.hooksPath` is repo-global and absolute, so in this multi-worktree layout the hook that gates a push comes from whatever the PRIMARY tree has checked out. Measured at `TOOL-dUnstalledConvoy-26`'s landing: the primary tree was on `contrib/incms-memory-recall`, so the push ran that branch's `.githooks/pre-push` — no gate-env sourcing, no predicate 8, 39 legs held and the boundary's new coverage check simply absent. The pushed tree was verified by a separate full bar, so nothing wrong shipped; the BOUNDARY was not the one that shipped. `tools/check-wiring.sh` already resolves `core.hooksPath` at SessionStart and could compare the resolved `pre-push` against the tracked blob at HEAD. Needs a decision first: does a mismatch RED, or report? Class recorded at `memory/gotchas/hookspath-resolves-into-another-checkout.md`. → tools/check-wiring.sh
- TOOL-dUnstalledConvoy-38 · OPEN · `--landed`'s check 34 compares the lander marker against the run's own HEAD, which a `--no-ff` merge landing can never satisfy — and `--no-ff` is the landing shape the charter §3 MANDATES. `push-main.sh` pushes the merge commit and writes that sha; the run's HEAD is the merge's second parent, so the marker "names a different commit" and the run cannot reach LANDED. Worked around at `TOOL-dUnstalledConvoy-26`'s landing by fast-forwarding the run's branch onto the merge first, which is true but obscures the defect. The predicate wants: the marker's commit is on the remote default branch AND has the witness as an ancestor. Also worth a look while there — `--landed` printed check 26 ("already finished") in the same invocation that wrote `phase: LANDED`, so the verb evaluates its own idempotence guard after its own write. → tools/unattended/unattended.sh
- TOOL-dNarrowedAnchor-2 · CLOSED · Not stale WORDING — stale FIXTURES. All four red arms in unattended.test.sh asserted branches their fixtures had stopped reaching: two needles named a message no driver here carries, the write-time bypass pair sat after a `reset_tree` that wiped `pc/`, and `--plan tPlanEmpty` ran against a build a later `reset_tree` had deleted (that last one is TOOL-dHonouredPark-4 S5 fallout, not this build's). Suite now PASS at 849 assertions. The reason it took three runs to see: `hit()` printed only the needle and discarded the actual output, so every red read "the driver did not say X" with no way to learn what it did say — fixed in the same commit. → tools/unattended/unattended.test.sh
- TOOL-dScaffoldedMirror-2 · CLOSED · PHASE 0. Honest reporting and per-predicate liveness. `lexicon.py:455` folds `len(funcs) + len(types_)` into one `populations[ext]`, so `.js` reports a healthy 89 while P2 grades ZERO classes and the DEAD PROBE arm cannot see it — measured 2026-08-24: 10 tracked `.js` files, 0 `class` definitions, `lexicon OK`. The green line prints no offender count and no population at all, so a reader cannot tell this repo from one with zero findings. `--measure` prints UNDECLARED EXTENSIONS, DEAD PROBE, UNSELECTIVE LAYERS RULE and STALE WAIVERS as `# NOTE:` comments and returns 0 unconditionally. Split populations per PREDICATE, print `graded/offenders/waived` on green as well as red, RED on an armed predicate whose population is 0. The failing case is sitting UNSTAGED in the tree, which makes this the one unit observable RED today → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-3 · WONTDO · PHASE 0. Corpus scoping derived from the `gov:kit` receipt, never authored. `lexicon.py:125-129` is `git ls-files` with no exclusion, so the kit grades and SEEDS itself — ten of this repo's 463 offenders are its own files, and `tracked`, `ext`, `python`, `probe`, `glob` are exactly the non-verbs that bled into the demo's proposed vocabulary. A derived proposal computed over a corpus containing the deriver is untrustworthy on arrival, which makes this a prerequisite for every other derivation rather than a phase of its own. Three controls answer the earlier refusal of a free-form `CORPUS_GLOBS` key: print `graded N of M · excluded K by <rule>` every run, RED on a rule matching zero tracked files, cap the excluded fraction → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-4 · DEFERRED · PHASE 0, and a hard prerequisite for `-9`. Waiver hardening: re-key all three registries from bare `text` to `path::name` and require a non-empty reason. `load_waivers` (`lexicon.py:195-208`) does `s.split(None, 1)` and never validates the second field, and P1's key is the bare identifier — so waiving `enc` waives it in every file forever, and 46 of 395 distinct names span multiple files covering 114 of 463 occurrences (24.6%). This is NOT a re-run of the refused `<path>:<line>` keying (`TOOL-aLoosenedCeiling-5`); that failed because an edit ABOVE a waived line unpinned it, and `path::name` carries no line number. Say so in the record or a reviewer will read it as overturning a settled decision. Under `-9` the waiver becomes the only remaining dodge → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-5 · WONTDO · PHASE 0, and it DIES at `-9`. Three `RATCHETS` rows for the pins at `drift_signals.py:265`, plus closing the delete-then-re-add hole at `drift_report.py:198-221`, whose guard `if now is None or was is None or now == was: continue` lets a vanishing key AND a reappearing key both pass silently — delete in commit 1, re-add at 900 in commit 2, zero findings. The conf already hand-writes `RAISED 412 -> 415` in exactly the format `ratchet_findings` parses. Label it a DOCUMENTATION ratchet and do not report it as answering the pressure demand: the 417 -> 463 move came with three paragraphs of prose and would have sailed through. `-9` deletes these three rows in its own commit and adds an assert that a `RATCHETS` row names a live key → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-6 · CLOSED · PHASE 0. Coverage floor plus a `LANGS` mode ratchet where `parser > probe > dark`. The largest unclosed hole the review found, and no design owned it: every fail-closed story covers the UNDECLARED (extension, predicate) pair and NONE covers the declared-dark one, which is the cheap move — one string edit empties the graded population and reds nothing. Measured here: 82 tracked `.sh` against 44 `.py`, so shell is this repo's largest source population by file count and holds ~518 definitions, while only 54 of the 126 definition-carrying files (42.9%) are covered by an armed mode. Failing case: flip `py` to `dark` and watch the fraction collapse 42.9% -> 7.9%; today that is `lexicon OK`, exit 0 → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-7 · CLOSED · PHASE 0, and it discharges `-17`. A `drift-audit` signal measuring offenders ADDED per definition ADDED between a recorded base and HEAD, both operands derived at both shas by the kit's own extractor — nothing authored, nothing raisable. `gateable: False` initially, with a liveness assertion so a broken derivation prints DEAD PROBE rather than a reassuring 0. This is the ONLY instrument anywhere that measures the actual demand (are new generations constrained), and it is what converts the 136-definitions/zero-offenders result from one agent's afternoon into a standing falsifiable claim. Measured cost 0.369 s on gov, 3.218 s on incms, sha-cacheable because a commit's tree is immutable. Not on the merge bar → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-8 · SPECCED · PHASE 2, and it RETIRES `-1` by deleting its subject. A kit-owned frozen `canon.py` of ~19 concept clusters with `role = engine`, plus a read-only `--probe`; `--scaffold`'s frequency allowlist is DELETED rather than patched a fourth time. THE RULE IS THE DESIGN: the canon's first element is the proposal UNCONDITIONALLY, at every dominance level, and the corpus is admitted as evidence for exactly one thing — which spellings become debt rows. The corpus votes to EXCLUDE, never to select. The dominance-table alternative was refuted as the demo defect one indirection down (a repo whose sessions all wrote `get_*` has D~1.0 and adopts `get`) and it contradicted its own day-1 claim. Two arms: 500 `frobnicate_*` definitions must not enter VERBS, and `get`=1/`fetch`=1/`load`=0 must propose `load`. Carries the 11 missing negative definitions and the structured-NOT assert → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-9 · DEFERRED · PHASE 4, blocked on `-4` and `-11`. The grandfather set with a provenance assert, replacing all three pins. `FREEZE_SHA` plus a shrink-only set keyed `path::name` plus five asserts, of which C is load-bearing: every grandfathered key must be DERIVABLE as an offender at `FREEZE_SHA`, so no edit to the present tree can top the file up. That is the only mechanism in the brief making an absorbing edit IMPOSSIBLE rather than expensive, and it is reviewer R4's own proposed repair to the cut pin-direction guard — guard on the offender IDENTITIES, not the count. Cost ordering inverts: today raise(1min) < waive(2min) < add-a-verb(5min) < rename(10min); afterwards raise does not exist. Freeze advancement FORBIDDEN in v1. Location is the kit dir with `role = seed`; siting it under the memory root instead reds HYGIENE check 3, measured → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-10 · CLOSED · PHASE 1, and the highest-VALUE unit in the build even though it is not the highest-ranked. Supply the vocabulary to the author: `--suggest <name>` (0.045 s, one deterministic line), a per-OBJECT `--brief` rather than a directory histogram (750 distinct off-table leading tokens in one incms directory make a top-9 line show 1.2% of the live vocabulary), and a rendered lexicon Skill under the skills root whose `--check` byte-compares against `.lexicon.conf` on an UNGUARDED gate leg. The charter POINTS and must be net-negative in bytes: `AGENTS.md` sits at 64,394 of 64,512 — 118 bytes — against a 1,787-byte table, and a staged 193-byte bullet reds the size gate by 87. `--brief` must declare its coverage mode and REFUSE on a dark extension, or an empty section reads as invent-freely → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-11 · DEFERRED · PHASE 3, blocked on a kit-owned `^visit_[A-Z]`/`generic_visit` exemption class. Replace `_python_defs`'s scope-blind `ast.walk` (`lexicon.py:140-169`) with a `NodeVisitor` emitting `(name, line, kind, scope, span, public)`, then ONE predicate: `len(name.strip(_)) >= 3` unless the enclosing scope is <=10 lines. The span condition IS the design and it is measured — the same rule fires 51 times above the cut line and 1,033 below it. Knobs are KIT CONSTANTS, never conf keys: an offender count selected from a published threshold menu is a pin wearing a threshold's clothes. Both arms required or it is a plain length rule — a parameter `d` in a 40-line function RED, the same parameter in a 4-line function GREEN. The prerequisite is real: a staged 8-method visitor reds the gate today at 471 over 463, with 17 `visit_*` names CPython mandates → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-12 · DEFERRED · PHASE 5, RESEARCH, only on the owner's word. The consistency instrument: group definitions by OBJECT (the subtokens after the leading one) and measure whether members agree on the leading token. No vocabulary, so §12's mirror ban does not apply, and both operands derive from the tree so it cannot be raised. Measured here: 824 defs, 589 carrying an object, 418 distinct objects, 47 spelled more than one way, CONSISTENCY 0.716. It finds 60 genuine conflicts P1 calls GREEN (`load_conf` x8 vs `read_conf`; `region` spelled five legal ways) and declines to fire on the 354 of 459 offender keys that conflict with nothing. FOUR disqualifying gaps keep it RESEARCH: silent on the first instance, a repo of all-`get_*` scores 1.000, it can never say `load` not `fetch`, and `tokens[1:]` keying makes roughly half the 47 groups artifacts. Build a real concept key and hand-measure precision on >=40 groups on gov AND incms first → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-13 · DEFERRED · the adopter's primary language is INVISIBLE to the kit, and this is the real blocker on probing anyone. Measured 2026-08-24 by a read-only run of the kit's own extractor against `incms/main`: 6,168 tracked files, 626 `.ts` and 572 `.tsx`, and no `PATTERN_SETS` entry for either — so armed coverage is 1,190 files, 19.3%. Bigger than this repo's dark `.sh` hole, and it lands on a repo that actually exists. The JS/TS lexer was deliberately NOT recommended yet and the objection is ORDER, not cost (a stdlib prototype hit 21.9 MB/s over 1,236 files): it would arm a casing rule over 1,072 PascalCase React components and a synonym predicate over a TypeScript corpus nobody has measured, and its conformance fixture round-trips through its own reading, which is the P3 tautology this kit already recorded. Revisit after `-8`, with fixtures extracted from real adopter files rather than authored → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-14 · CLOSED · 79 of 459 backfill keys (17%), agent-defaulted 2026-08-24 with the owner declining to override. Rename all 52 `t_*` to `test_*` — they live in exactly two files (`tools/memory-recall/selftest.py` 29, `tools/codebase-map/selftest.py` 23), both selftests, and `test` is ALREADY a reserved row meaning precisely this, so it costs no table change. Then reserve `cmd` as a structural row beside `main` and rename the 22 `do_*` (all in `memory-tree/`) to join govkit's 5 `cmd_*`. `cmd` over `do` despite being the more expensive rename: `main` is already reserved as a module's CLI entry point and a subcommand handler is that role one level down, so `cmd` names the role while `do` names nothing — and admitting `do` would be the table's first synonym row. Net 459 -> 380 before anything else is decided → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-15 · DEFERRED · nothing tells an adopter this kit exists, and it is upstream of the whole probe-the-adopters demand. `tools/govkit/check_runbook_parity.py` exits 1 naming 18 registry entries with no anchored runbook section — `lexicon` among them at output line 12 — and it has ZERO callers and is absent from `tools/gate-legs.json`. Verified 2026-08-24. A hand-wiring adopter is therefore never told the kit is deployable, which is the mechanical reason the live adoption count is one. Not the lexicon's own work, and deliberately filed as its own row: `incms/main` is a full gov adopter (6,168 files, `.governance/`, `.codebase-map.conf`, `.memory-tree.conf`) carrying no `.lexicon.conf`, and `swydee` is hand-wired with no `.governance/` at all → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-1 · CLOSED · `--scaffold` writes `SUFFIX_OFFENDER_PIN="0"` and `LAYER_OFFENDER_PIN="0"` as hardcoded literals (`scaffold_lexicon.py:106-107`) under a seed comment that calls all three MEASURED against this corpus at scaffold time. REPRODUCED 2026-08-24 on a fresh 19-file repo: a corpus carrying `class OrderManager` and `class OrderHelper` scaffolds green and then reds on its first gate run with `suffix offenders 2 over pin 0` — a pin the tool wrote itself. Fails SAFE (red, never a false green), and `lexicon.py:500` already names the defect in passing as the reason `--measure` exists, so the REMEDY verb shipped while the write path did not move. The fix is ASYMMETRIC: `layer`'s zero is correct BY CONSTRUCTION because `LAYERS` seeds empty and no layer offender can exist at scaffold time, so measure `suffix` the way `verb` is already measured and drop the MEASURED claim from the comment for `layer` alone. SECOND FINDING from the same run, same root: the scaffold runs BEFORE the conf it writes is tracked, so the derived `LANGS` cannot contain `conf` and the adopter's first `git add .lexicon.conf` reds the gate with UNDECLARED EXTENSIONS. Every fresh adoption hits it in the first five minutes; either scaffold `conf::dark` unconditionally or name the trap in the seed comment RETIRED 2026-08-25 by TOOL-dScaffoldedMirror-8 S5/S8, which deleted the subject rather than patching the write path a fourth time: the frequency allowlist is gone, all three pins are MEASURED, and `conf::dark` is seeded unconditionally. Verified by re-running the owner's original demo — all six deliberately-bad names are now offenders where all six passed green → tools/lexicon/scaffold_lexicon.py
- TOOL-dScaffoldedMirror-20 · OPEN · nothing checks this repo's own `.gitattributes` against the `[[lf_pin]]`s its kit descriptors declare, so the two can diverge silently in the direction that matters least visibly: an ADOPTER gets the pin and gov does not. Found as closing-review M9 — `tools/lexicon/kit.toml` declared `.claude/skills/lexicon/SKILL.md` and gov's own attributes carried no such line, for a file its gate byte-compares. The predicate must ask `git check-attr eol`, never string membership: a blanket `*.sh text eol=lf` legitimately covers three declared pins that no literal line names. Measured 2026-08-25 over all 23 declared pins — 10 literal, 13 needing `{kit}`/`{memory_root}`/`{prefix}` resolution — and exactly one was unhonoured. NEITHER obvious home works: `tools/check-wiring.sh` resolves python only to NAME it in a remedy string and deliberately executes none, so a TOML-reading arm there breaks its no-python property; and `drift_report.py` ships to adopters, who hold no descriptors at all, so the signal would be dead in every tree but this one. Wants a gov-internal leg, which is a fourth carrier (`tools/gate-legs.json`, a `[[gate_leg]]` claim, `tools/govkit/subject-pins.tsv`, the map dossier) and was not worth half-building inside a fix pass. → tools/govkit/
- TOOL-dScaffoldedMirror-21 · OPEN · `check-unattended.sh` check 29 has no grandfathering, so any run already IN FLIGHT when `TOOL-dNarrowedAnchor-1` landed became unlandable. The check asks whether a run's recorded BASE is an ancestor of the tip the REMOTE advertises for the default branch — which it becomes only once that branch is pushed, and the pre-push hook runs the bar before pushing. `.unattended.conf` sets `BYPASS_BAN="--no-verify"`, so the escape the hook names is one this project forbids. Measured 2026-08-25 on dScaffoldedMirror: bar 84/85 on merged main, this leg the only red, and `git merge-base --is-ancestor 500a5db6 ec17c20a` true — so the red self-heals the moment the push it blocks completes. NOT a defect in the rule, which is right, and not a general one: for a repo with no in-flight run the check never fires, since `prompt` and `recipe` are exempt and a conforming `slug` run has no second-anchor base. It bites the transitional window only. The kit already ships the mechanism — `LANDED_ANCHOR_CUTOFF`, read at `check-unattended.sh:893`, grandfathers anchors on records predating a cutoff — and check 29 was simply not given one. Owner took the one-off bypass on 2026-08-25 rather than have the run that benefits edit the gate that blocks it; this row is that decision's other half. → tools/unattended/check-unattended.sh
- TOOL-dScaffoldedMirror-22 · OPEN · `--landed` writes `phase: LANDED` BEFORE check 34 can refuse it, so a refused landing leaves a record claiming the terminal phase while missing the `landed-anchor` fact that `set_fact` writes further down the same function — `unattended.sh:1890`, unreachable once check 34 `return 1`s at `:1876`/`:1882`. The result reds check 15 forever, and check 26 then refuses `--park` and `--phase` on the same record because it is finished, so NO verb can repair what the verb broke. Observed 2026-08-25 on dScaffoldedMirror: the marker held a sha from the push-main run a red bar had blocked, `--landed` refused, and the record was left LANDED-with-no-anchor and unrepairable. Hand-completed with `landed-anchor: remote`, which git confirms independently — the witness `ea49714a` is an ancestor of `origin/main`. Same write-before-check family as `TOOL-dUnstalledConvoy-38` and found in the same invocation, but a distinct defect: -38 is that check 34 cannot be satisfied by a `--no-ff` landing at all, this one is that failing it corrupts the record on the way out. Fix is ordering — compute and validate, then write both facts together, so a refusal leaves the record untouched. REPRODUCED 2026-08-25 on dPromptedSeam, a second build and a second landing: --landed refused on check 34 (the -38 no-ff shape), wrote phase LANDED anyway, and left landed-anchor absent; fast-forwarding the branch onto the merge so check 34 could pass then hit check 26, which refuses a finished record — so the first failure makes the second unreachable and the pair is a trap, not two bugs. Hand-completed again with landed-anchor: remote and unpushed-at-landing: 0, both verified from git rather than assumed. Two instances in one day on two builds means this fires on every no-ff landing, which is the shape charter section 3 MANDATES. THIRD INSTANCE 2026-08-27 on dCarriedReceipt, found by `TOOL-aPrimedKeepalive-5`: the record had read `LANDED` with no `landed-anchor` since 2026-08-25 and had been redding the unattended leg's check 15 on `main` for two days with nobody noticing, which is the cost this row had not yet priced — the trap does not merely wedge the run that hits it, it leaves the whole fleet's bar red until a later run trips over it. Hand-completed again with `landed-anchor: remote`, verified independently: `git merge-base --is-ancestor 04c7da24 origin/main` exits 0. `unpushed-at-landing` was left ABSENT rather than defaulted to 0, because what local main carried at that landing on node d is not measurable from here and a zero meaning "could not measure" is this repo's own green-by-absence class. Row stays OPEN: the ordering fix in `verb_landed` is still unbuilt. → tools/unattended/unattended.sh
- TOOL-dNarrowedAnchor-3 · OPEN · `BUDGET_gate_selftest` was DERIVED, never observed, and the derivation was low by 2.6x: 1342 s extrapolated from a sharded pair against 3565 s measured end to end. Its own note closed with "nobody has run the suite end to end at this commit", which is exactly why it went unchallenged — a pin nobody could falsify is a pin nobody did. Raised to 3800 on the measurement. What is still NOT separated is whose cost: gate-legs.json records 3188 s before this build, so most of it predates TOOL-dNarrowedAnchor-1's four arms, but splitting them needs a baseline end-to-end run — another hour of the thing the ceiling bounds. Worth doing once, then re-deriving both halves. → tools/unattended/run-unattended-gates.sh
- TOOL-dCarriedReceipt-1 · OPEN · `--review`'s convergence predicate is scoped to ONE run's run-state file while M4's review loop spans runs, so a build specced in one session and built in another restarts the sequence at zero and a loop that has already failed to converge reads as `CONVERGING`. `review_counts` (`unattended.sh:3072`) reads only `<MEMORY_ROOT>/builds/<slug>/RUN.md`, and `--preflight` creates a fresh one per run. Measured here 2026-08-25: rounds 1-3 of dCarriedReceipt's spec audit ran in a prior session and left no `review` rows anywhere a later run can read; round 3 confirmed 3 blockers and round 4 confirmed 3, which is exactly `review_state`'s NON-CONVERGENT condition (`:3065`), and it returned `CONVERGING` because `n` was 0. The prior rounds are recoverable only from commit subjects. The fix is not obviously "read the retired records" — a run may legitimately re-open a subject a previous run closed — so the question is whether the sequence belongs to the SUBJECT rather than the run, which would put it in the build folder beside the spec. → tools/unattended/
- TOOL-dCarriedReceipt-2 · OPEN · `--review` makes `CONVERGED` terminal for a subject (`unattended.sh:3135`, refusing a later round because it "would rewrite that history") while M4 makes a spec whose rev moved since its last review UNREVIEWED again. The two rules meet head-on for any spec a round both clears of blockers and edits in the fold, which is the ordinary case: a round that finds one high and no blocker converges the subject and then changes the document. Recording the round honestly wedges the next one; deferring the record leaves the loop's own history incomplete. Worked around here 2026-08-25 by recording round 4 only for the two subjects carrying blockers and the four specs taking no edit at all, and deferring the nine that take a fold until their text is final — a judgement no rule states and nothing checks. Either the terminal rule needs a rev-keyed exception, or `CONVERGED` needs to mean "converged AT rev-N". → tools/unattended/
- TOOL-dCarriedReceipt-3 · OPEN · a FOLD pass legitimately spans many units, and the dispatch machinery cannot express one. `pass_commit` (`lib-unattended.sh:98-109`) closes a declaration only when some commit between the declaration anchor and HEAD carries the unit id IN ITS SUBJECT and touched a declared path, and the build method's M6 states the same rule as "commit at the end of every pass, with the unit id in the subject". Both assume one unit per pass. Measured here 2026-08-25: the round-4 fold was five concurrent passes over 11 specs declared through `--dispatch`, and its single commit subject could not name eleven ids, so none of the five declarations ever closed. The next round's `--dispatch` then refused all three of its declarations with check 49 -- "a sibling pass in the same group already declared" -- naming the round-4 rows, and the run had to fall back to sequencing a fold that was provably disjoint. The refusal is CORRECT and its message is the problem: it reports the collision without reporting that the colliding pass never closed or why, so the reader diagnoses a disjointness error that does not exist. Either the closing rule needs a shape a multi-unit pass can satisfy (a trailer listing ids, say, rather than a subject), or check 49's message must name the unclosed pass's missing commit. → tools/unattended/
- TOOL-dCarriedReceipt-4 · OPEN · a spec that is DELIBERATELY never closed cannot be cited from tracked source without permanently redding `non_terminal_specs_cited_by_product_source`. `memory/builds/aQuarriedLantern/spec/2026-08-03-spec-aQuarriedLantern-1.md` says so in its own status line -- "NOT CLOSED — §8 Q6 is deliberately open" -- and it has read INPROGRESS since 2026-08-03. Measured 2026-08-25: DEPL-dCarriedReceipt-10 S7 declares gov's three forked memory-recall sources and its `record` field wanted the id that ratified the fork, which is exactly that spec; citing it took the signal from a baseline of 2 to 3 permanently, against pin 2. Worked around by pointing `record` at DEPL-dCarriedReceipt-10 instead, which is terminal and is the unit that adjudicated the three declarations -- accurate, but it is not the id that ratified the FORK, and the field's own refusal message asks for that one. The signal cannot currently distinguish a spec parked open by design from one a run left non-terminal by accident, and those are opposite situations: the first is a stable fact and the second is a transient. Either the signal needs a way to exempt a deliberately-open spec (a status distinct from INPROGRESS, or a waiver registry like the gate's `*.txt` ones), or a deliberately-open spec needs a terminal companion id that product source can cite. → tools/drift-audit/
- TOOL-dHonouredPark-5 · OPEN · A bespoke review fan-out must assign finding ids in the ORCHESTRATOR, and `memory/guides/BUILD-METHOD.md` M4 does not say so. Measured on this build's own round-1 spec audit: four finder lenses each numbered from `F1`, so 67 findings carried 18 distinct ids and the verdict join attached most of them to a verdict written about a different subject — `F1` alone drew four contradictory verdicts. The whole verify stage was discarded and re-run. `tools/workflows/tier2-review.js` already assigns integer ids centrally for exactly this reason; M4 rules that harness out for spec audits because it reviews diffs, which is true and is not the point — the id rule is independent of the subject. M4 should carry the rule itself, so the next bespoke script inherits it. → memory/guides/BUILD-METHOD.md
- TOOL-dHonouredPark-6 · OPEN · The engine tolerates a build folder whose specs carry another build's id slug and the driver does not, and after this build's rename nothing in the corpus exercises the tolerant path. `gen_build_index.py:673` has `rosters()` fall back to front matter when a slug owns no ids, `:1420` rewrites `ids:` from that value, and `:1398` carries the comment that a folder "HOUSES" specs whose ids may belong elsewhere — while `roster_ids` and `unit_ids_of` (`unattended.sh:1487-1502`) both select `[A-Z]+-<folder-slug>-[0-9]+` and return EMPTY for such a build, refusing `build-complete` at term 2. A sweep of all 63 build folders now finds zero mismatches, so the engine's branch is unreachable from the corpus: either delete it, or state the case it is for and give it a fixture. Two tools, two answers to one question, and the answer nobody uses is the one with no test. → tools/memory-tree/gen_build_index.py
- TOOL-dHonouredPark-7 · OPEN · `plan_state` cannot tell a spec that DISCUSSES a resolution mark from one that CARRIES it, so any section 8 quoting the stamp — to withdraw it, to explain it, to warn about it — is classified RESOLVED. Reproduced on this build: `spec-dHonouredPark-2` rev-2 quoted a withdrawn stamp verbatim in the sentence withdrawing it, and `--plan dHonouredPark` printed that unit READY while its three siblings printed FORKED. The function's own comment block enumerates what it deliberately cannot see — an unresolved fork below an honest-looking `none` line — and does not list this one, so the gap reads as covered. The mark is matched over the whole squeezed section blob, which is load-bearing for wrapped marks and is why a naive fix breaks twelve tracked specs. Wants a discriminator that survives quoting: a leading marker position, a fenced-span exclusion, or an explicit `NOT A MARK` escape. Worked around in rev-3 by describing the stamp instead of writing its bytes, which is a spec-side dodge and not a fix. → tools/unattended/unattended.sh
- TOOL-dHonouredPark-8 · OPEN · `--review` keys its convergence loop on `--subject` alone, so a build's SPEC AUDIT and its CLOSING DIFF REVIEW collide when both name the build slug — and the unattended Skill tells you to use the slug for the diff review while the spec audit has no other natural subject for a set-wide round. Reproduced on this build: the spec audit CONVERGED at round 2 under subject `dHonouredPark`, and the diff review's round 1 was then refused by check 37 as rewriting a terminal loop, so its rounds live only in the review records. The two loops answer different questions and one of them is about a design; key on (kind, subject), the same split `**Serves:**` already makes with `spec-audit` and `diff-review`. → tools/unattended/unattended.sh
- TOOL-dHonouredPark-9 · OPEN · The verdict-epoch bump is only ever discovered AFTER a red bar. `check-verdict-epoch.sh` names the three sites and the render command, but it runs at the push boundary and compares COMMITTED ranges, so an author who edits a delegate learns they owed a constant only once the commit exists — three separate times in this build alone, each costing a fix commit whose whole content is the bump. A pre-commit warning on a STAGED delegate would cost one grep and remove the cycle; the tracked pre-commit hook already runs a fast leg and already knows the staged set. Not a red, just a warning, since the bump legitimately belongs in the same commit and the author is right there. → tools/memory-tree/check-verdict-epoch.sh
- TOOL-dHonouredPark-10 · OPEN · check 29 has no terminal-run exemption, so an ABORTED run that used the second anchor reds the bar permanently and the only exit is deleting the record it objects to. Hit on this build: the run declared mode `slug` on the run-branch anchor, aborted, and its RUN.md then blocked every push — the check asks whether the BASE is an ancestor of the remote's advertised default tip, which pushing would satisfy, but push-main gates before pushing. A check that forces destruction of its own evidence is the wrong shape. The anchor question is about AUTHORIZATION, and a run in a terminal phase has authorized everything it ever will; skip terminal records, or offer an override the way every other DoD item does. → tools/unattended/check-unattended.sh
- TOOL-dSpentCeiling-1 · CLOSED · the read-path byte budget is retired and check 16's rules 3 and 4 are structural. MEASURED before building: 27 movements of `READ_PATH_CEILING` between 2026-08-08 and 2026-08-25, 26 up and 1 down, and the single drop (`95431 -> 86476`, `d816c30b`) was forced by hygiene check 11's new 400-byte per-entry cap rather than by this ceiling — so the budget never once caused a trim. It was always a SECOND bound: check 6 caps all six members at 61440 B each, 368640 B of first bounds against 135706 B held. THE FINDING THE INSTRUMENT NEVER SURFACED: 54.0% of mandatory reading is rendered from other kits' templates and cannot be trimmed here at all, and those two files supplied 64.9% of the week's growth while `LIVE.md` SHRANK. An earlier draft argued the breach was structurally guaranteed because `DECISIONS.md` is append-only; REFUTED — it rotated on 2026-08-10. Owner cut the build from six units to one on 2026-08-25: no successor instrument. Rules 3 and 4 REPORT until memory-tree 2.44 so no adopter reds on upgrade for a pre-existing condition → `builds/dSpentCeiling/`
- TOOL-dSpentCeiling-4 · OPEN · a kit's RELEASE can spend another kit's read budget, and no gate anywhere prices it. Measured 2026-08-25 while retiring the read-path ceiling: 73268 B of this repo's 135706 B of mandatory reading — 54.0% — is rendered from kit templates (`memory/guides/BUILD-METHOD.md` carries a `gov:kit memory-tree@` marker, `memory/guides/UNATTENDED-PROTOCOL.md` a `gov:kit unattended@` one), and over the seven days to that date those two supplied 28216 B of 43460 B of growth. An adopter cannot trim either one; only the kit owner can. Every one of the ceiling's 26 raises was argued as though the adopting repo's own prose were the driver. Wants a size discipline on RENDERED docs, owned by the kit that renders them — candidates: a per-template ceiling in `kit.toml` beside the `[[render]]` row, or a drift signal over rendered-vs-authored bytes. NOT scoped here → tools/memory-tree/
- TOOL-dSpentCeiling-5 · OPEN · the hygiene check COUNT is hand-typed in at least three places and they already disagree: `tools/memory-tree/README.md` and the map dossier's H1 say 21, the dossier body says 22, and `check-verdict-epoch.sh:47` says 19. Nothing derives it and nothing gates it, so each new check makes the spread worse. Found while retiring the read-path budget, which deliberately did NOT renumber check 16 into two checks for exactly this reason — a fourth wrong count is not an improvement. The fix is to DERIVE the count (the engine can print it, the way it already prints its index set and its version) and have the carriers cite the derivation → tools/memory-tree/
- TOOL-dSpentCeiling-6 · OPEN · two waiver registries pin LINE NUMBERS into files that get edited, so an edit anywhere above a pinned line silently invalidates it and the gate reds on a row that was correct when written. `tools/install-prefix-waivers.txt` and `tools/dead-path-waivers.txt` both do this. MEASURED during TOOL-dSpentCeiling-1: the install-prefix pins for `corpus_ids.py` had to be re-pinned THREE times in one build (706 -> 781 -> 833 -> 836) and the dead-path pins once, none of it because the waived thing changed — only because lines above it moved. The waiver text stayed true throughout, which is the tell: the row's IDENTITY should not be its line number. This is NOT a re-run of the refused `<path>:<line>` keying for lexicon waivers (`TOOL-aLoosenedCeiling-5`) — that was refused because an edit above a waived line UNPINS it, which is exactly the defect being reported here, so the two agree rather than conflict. Candidates: key on the matched TEXT the way `check-dead-paths.sh` derives its needles, or on `<path>::<needle>`, or have the checker offer a `--repin` mode so the fix is mechanical rather than manual → tools/
- TOOL-dSpentCeiling-7 · OPEN · FLIP `READ_PATH_RULES_GATE` to True in `tools/memory-tree/corpus_ids.py`, which is what ends check 16's grace and makes rules 3 and 4 red. It ships False in memory-tree 2.42: those rules had never run in any tree that did not declare the retired `READ_PATH_CEILING`, so gating them on upgrade would red an adopter for a pre-existing condition. A tree that DID declare the ceiling is not graced at all and gates today — including this one — so the exposure is adopters only. WHY IT IS A DELIBERATE FLIP rather than a version comparison: the first cut keyed on `KIT_MEMORY_TREE_VERSION >= (2, 44)` and the round-1 review measured that dead on arrival — the engine went 2.41 -> 2.46 in under two days across concurrent branches for unrelated reasons, and two branches minted identical version literals from one base, so the flip would have arrived by MERGE ORDER rather than by anyone's decision. Keying on the version also required reading it through a print mode BELOW the conf source, which handed a project conf a one-line spoof of the constant whose own comment says it cannot be spoofed. PRECONDITION for flipping: adopters have had one release carrying the announcement, and this repo's own rules 3 and 4 report nothing → tools/memory-tree/corpus_ids.py
- TOOL-dSpentCeiling-8 · OPEN · THE FULL BAR IS NONDETERMINISTIC UNDER ITS OWN CONCURRENCY, on at least two legs, which makes any `GATE_FULL=1 GATE_SELFTESTS=1` green an unreproducible claim. MEASURED 2026-08-25 across three runs of the merged dSpentCeiling tree: run 1 redded `run-gates turnstile` (`TERM arm: the run never claimed the beacon, so the release assertion proves nothing`), run 2 redded `row-keyed merge driver replay` (`nesting: the driver's file differs from git merge-file's`), run 3 was GREEN 85/85 — with NO commit and no working-tree change between runs 2 and 3. Both legs pass STANDALONE on this branch and at `origin/main`, and both subjects are byte-identical to main's (`git diff origin/main..HEAD` over `tools/run-gates/`, `tools/lib/` and all three `merge-rows.*` files is empty), so neither is a regression — they are load-sensitive arms surfacing on a saturated 85-leg run. THE DEFECT IS THE ARMS, not the mechanisms they guard: each reports a red that cannot distinguish 'the mechanism is broken' from 'this machine was too busy', so its finding is uninformative in exactly the run where it fires, and a session cannot tell a real break from noise without re-running. Candidates: a bounded retry on the claim, or a declared timing budget the arm REFUSES against rather than fails — a probe that cannot answer should say so. Reproduce cheaply with `GATE_JOBS=1` (the documented concurrency rollback) → tools/run-gates/
- TOOL-aNamedGesture-1 · CLOSED · the prompt path's authorizing parameter is NAMED, as the conf key `AUTH_PARAM` rendered into the Skill, defaulting to the token that names the mode it starts. `SKILL.template.md` has said `Only when the invocation carries the authorizing parameter` since `TOOL-aPromptedMandate-5`, whose own scope item S2 was `the parameter is named`; it never was, so the gesture separating prose from a run that merges and pushes with no owner turn is a phrase an agent interprets. The parameter now TAKES the build too: its value is a prompt file path or a literal prose block, and a path is resolved and carried by CONTENT because a build folder may not authorize by reference to an editable file (owner, 2026-08-25)
- TOOL-aNamedGesture-2 · OPEN · the kickoff manifest's `last-audit` stamp read `2026-08-26T10:10:00+03:00` while node `a`'s clock said `2026-08-25T21:58:16+03:00`, twelve hours behind it. The ratchet's stated rule is that the datetime ALWAYS ADVANCES, and `manifest-check.sh` does not enforce it — it validates the ISO shape and the sha's ancestry and nothing else. So an honest stamp from a node whose clock trails the last stamper's goes BACKWARDS and no check notices, while a node that obeys the stated rule has to write a future time. Either the rule should be enforced and skew becomes a refusal, or it should be dropped and the sha left as the only ordering. Found while re-stamping for `TOOL-aNamedGesture-1`; stamped honestly, so this repo's own history now carries the backwards step as evidence
- TOOL-aNamedGesture-3 · CLOSED · SUPERSEDED at the landing merge by `9f239849` on main, which populated the fixture's generated-units region and wrote a matching non-terminal spec so `--plan` returns a verdict · `tools/unattended/cross-component.test.sh` is RED on pristine main, not just on a feature branch. Measured 2026-08-25 in a throwaway worktree at 381008a1: five FAIL lines, arms 3 and 3b, all one root cause — `UNATTENDED check 30 FAILED — check 30 walked no build whose --plan returned a verdict`. The fixture builds one build under its own scratch memory root and check 30's vacuity guard then finds no build whose `--plan` returns a verdict, so the guard fires on the FIXTURE's population rather than on a defect. On the aNamedGesture tip the same message appears on arm 5 alone, one FAIL line, which is why the suite has to be baselined before a branch is blamed for it. Sibling of TOOL-dNarrowedAnchor-2, which recorded the same shape for `unattended.test.sh`. Either the fixture must produce a build `--plan` can grade, or check 30's guard must not fire on a single-build fixture tree; picking one is the work
- TOOL-dTieredTribunal-4 · OPEN · the `args`-must-be-an-object guard is not ported into the two drift-audit harnesses and the defect is live at HEAD: `drift-audit-state.js:47` and `drift-audit-code.js:48` are both `const a = args || {}` with no `JSON.parse`, so a caller handing either a prose string falls back to `REPO = '.'` and audits whatever directory the process is standing in, silently and with confident findings. `tier2-review.js:32-60` carries the parse-then-validate guard that closes exactly this, added by `TOOL-aGuardedTally-1` S5 after that harness twice reviewed a DIFFERENT repository than the one it was briefed on. The Workflow tool delivers `args` as a STRING even when the caller hands it JSON, which is why parsing comes first. Deliberately OUT of `TOOL-dTieredTribunal-3`, whose scope is the trust accounting alone and whose §3 names this as a non-goal and cites this row; recorded by the round-1 fold as finding 49's cheaper arm. Whoever ports it observes the guard's PASSING case as well as its refusal — the first cut of the tier2 guard tested `typeof a !== 'object'` and refused every legitimate caller → tools/workflows/
- TOOL-aCollapsedScan-4 · OPEN · `unattended kit gate` stays over its declared `BUDGET_kit_gate=120` after TOOL-aCollapsedScan-1. The spawn fix took check 30's walk from 235 s to 94 s and the leg from 305 s to roughly 164 s on node `a`, so the ceiling is still breached by about 44 s and no further spawn cut reaches it: `plan_state` costs one awk per graded unit and is unfoldable (two harnesses slice its body out of the shipped bytes), the driver's own startup is 0.28 s × the build count, and the rest is `region` and `git ls-files` work that is genuinely per-build. TWO candidates, and they are not alternatives so much as an order: (1) change-scope check 30 to builds whose `README.md` or `spec/*.md` moved since the recorded green, full corpus under `GATE_FULL=1`, with `_pv_seen` restated over the scoped population or it reds on every commit touching no build; (2) re-declare the ceiling with the reason beside it, which the file's own header says is a legitimate outcome. DO NOT scope it by `RUN.md` — refused with measurement in TOOL-aCollapsedScan-2. Reproduce with `bash tools/unattended/run-unattended-gates.sh --checks` → tools/unattended/
- TOOL-aCollapsedScan-5 · OPEN · THE ORIGINAL FACT IS NOW FALSE and the ASK SURVIVES. Re-measured 2026-08-30 at 093730e4 by `TOOL-aScouredKit-1`'s wave-1 audit: all 86 legs in `tools/gate-legs.json` carry a `ceiling` key, 0 without, landed at `ee0e7547`. What this row asked for that still does not exist is COST policing: the ceilings are a HANG bound sized about 10x the measured time (median 37.8x, worst 1229x), which is a ratified owner decision and not slack, so nothing on the bar notices a leg that gets steadily slower while staying far under its ceiling. The lead sentence was rewritten in place because a reader greps `ceiling`, finds "none exist", and rebuilds what already shipped. MEASURED CONSEQUENCE: check 30 added roughly 235 s to an unguarded merge-bar leg on 2026-08-25 and the first person to notice was an adopter wondering why the bar got slow, eight days after the ceiling that would have caught it was written. Candidates: a `ceiling` key in `tools/gate-legs.json` that `run-gates.sh` grades against `<git-dir>/gate-ledger.tsv`, which already carries one row per leg with its own seconds, so the measurement half already exists → tools/gate-legs.json
- TOOL-dTieredTribunal-5 · CLOSED · the record is written at `memory/gotchas/degradation-known-but-unreported.md` by `TOOL-dTieredTribunal-15`. RULED 2026-08-26 (`TOOL-dTieredTribunal-9`): the mechanism is a `memory/gotchas/` RECORD, not a scanner. Write it and this row closes. A CHAINED TERNARY ORDERS ITS CASES SO THE MOST SERIOUS IS THE LEAST REACHABLE, and a counter that reaches the CALLER never reaches the REPORT. Two findings of one shape, both confirmed by `TOOL-dTieredTribunal`'s closing diff review, both fixed in place at three files and two files respectively, and NEITHER left-shifted into a gate or a `memory/gotchas/` class — which `memory/guides/BUILD-METHOD.md` M8 says is what makes a finding return. D1: all three harnesses under `tools/workflows/` wrote `note:` with the degraded-otherwise arm above `!synth`, so the string naming a dead synthesis was emitted only when nothing else had degraded; the demote-on-conflict rule landed by `TOOL-dTieredTribunal-3` made `unverified.length` non-zero more often and so narrowed that path further. D2: the seven trust counters were returned to the caller and interpolated into no synthesis prompt, so a run with four of five lenses dead wrote a durable report the prompt instructs to read a zero as positive evidence. Both are the false-clean family and neither has a static predicate that is obviously safe: a ban on ternary ordering would red every legitimate chain, and "every returned counter must appear in a prompt string" is satisfiable by a comment. Candidates: a gotcha record naming the family so `gotchas.py --for-paths tools/workflows/` puts it on the checklist, which is cheap and weak; or a scanner asserting that a file returning a counter named in a closed list also interpolates it, which is stronger and needs its failing case observed first. NOT done in the build that found it, deliberately: writing that record after the closing review adds unreviewed prose at the exact point that build measured as the dominant defect source, and the owner gets the trade rather than the run taking it → tools/workflows/
- TOOL-dTieredTribunal-6 · OPEN · The RECLASSIFICATION that created this row was ratified 2026-08-26 (`TOOL-dTieredTribunal-10`); the QUESTION below is still open and still the owner's. `tools/check-kit-versions.sh` pairs `meta.version` with the same-line `gov:kit` marker for BOTH drift-audit harnesses at `:180-193`, and does not do so for `tools/workflows/tier2-review.js`, whose `:24` row is a bare presence check that the file carries SOME version-shaped token. So a bump moving one of that file's two same-line tokens and not the other is invisible to the unguarded `kit version markers` leg. Found by `TOOL-dTieredTribunal`'s round-2 spec audit as finding 35, whose TEXT arm folded (section 7 of that unit no longer calls the leg the reason to care about its version bump) and whose GATE arm did not, because a gate is a mechanism and folding one into a spec-text round smuggles it past the round meant to price it. `tools/workflows/kit.toml` declares `version_from` over that same field, so the field IS the review-harness kit version and the asymmetry is not cosmetic. A new gate owes its failing case OBSERVED before it lands, which is a unit rather than a fold. Carried here from spec-1 section 8, where it had been filed as a fork of `TOOL-dTieredTribunal-1`; it is not one, because that unit is complete and correct whether or not the assertion exists and its section 3 already excludes new gate legs → tools/check-kit-versions.sh
- TOOL-aCollapsedScan-6 · OPEN · NOTHING GATES A DUPLICATE ID in a backlog shard or in `memory/DECISIONS.md`, and both carry duplicates today. MEASURED 2026-08-26 while landing aCollapsedScan: an additive merge reconcile kept BOTH the pre-ruling and the ruled text of `TOOL-dTieredTribunal-5` and `-6`, and the full bar ran GREEN 39/39 with both duplicates in the file. Repaired in `3dd03ea1`. `memory/DECISIONS.md` separately carries three duplicate ids (`TOOL-aUnmannedHelm-5`, `TOOL-aUnmannedHelm-6`, `TOOL-aWrittenMethod-1`) at every ref back to `da9e4cd2`, so this is not a one-off. A duplicate id is worse than a missing one: two rows answer to the same name and a reader cannot tell which is current. The check is one `sort | uniq -d` over the id column of each shard and of the decision log, which makes it a hygiene leg rather than a project. Decide first whether the decision log's three are legitimate (an append-only log MAY cite one id twice if the second supersedes) or debt to drain → tools/memory-tree/
- TOOL-aCollapsedScan-7 · OPEN · the row-keyed merge driver is INERT for `memory/backlog/*.md` and `memory/DECISIONS.md` on node `a`: it aborts with `ConfError: refused: no .memory-tree.conf at tools/memory-recall/.memory-tree.conf` and writes a conflict rather than taking a side. FAILED TWICE in one landing, 2026-08-26, so this is reliable rather than incidental. The fail-closed behaviour is CORRECT and is not the complaint — the complaint is that `merge.rows.driver` is wired, reports itself wired at SessionStart, and cannot actually merge a row, so every shared-index merge is hand-reconciled by whoever is landing. That is how TOOL-aCollapsedScan-6's duplicate rows got in. The driver resolves its conf under `tools/memory-recall/`, which declares none by design because memory-recall reads the memory-tree kit's project layer; the resolution is looking in the wrong place, or the wiring points at the wrong entry point. Reproduce by merging any branch that touches a backlog shard → tools/memory-tree/
- TOOL-aResumedRelay-1 · OPEN · the lexicon VERB table needs a CURATION PASS over the deployer's naming, and the pin move that provoked it is recorded in `.lexicon.conf` rather than here. `VERB_OFFENDER_PIN` rose 384 -> 452 at the landing of `DEPL-dCarriedReceipt`: 70 added, 0 drained, a strict superset, all seventy named in the conf. A RENAME was considered and refused for two reasons worth keeping. The charter's own line is that the table's value is SCOPING rather than spelling, and renaming a fixture builder like `a13_gov` to lead with a table verb buys compliance while answering no question about what the function does. The engine half is worse: `classify_row` is cited BY NAME in seven ratified spec files, so renaming it would falsify the record those specs ARE, and editing closed specs to match a rename is not a thing a build may do to units it did not open. The pass should decide whether `demand`, `index`, `blob`, `carry`, `gov` and `step` belong in the table — six leading tokens account for most of the deployer's offenders and every one of them names a real operation this engine performs — or whether the names should change. Either answer drops the pin by a large step in one edit, which is the shape `signal` already has a precedent for in that file → tools/lexicon/
- TOOL-dTieredTribunal-22 · OPEN · the uncounted `filter(Boolean)` predicate, the follow-up `TOOL-dTieredTribunal-14`'s non-goal promised and no row carried. The research measured it reddening two shipped harnesses; the unit declined it as out of scope and named a record that did not exist. Filed by the post-acceptance spec audit, which also disproved the id-minting obstacle the non-goal cited — the charter forecloses it and this session minted three ids of its own
- TOOL-dTieredTribunal-23 · OPEN · the protocol gate arm `TOOL-dTieredTribunal-13`'s F1 resolved to record as a follow-up: assert that every branch of `scan` which can ADD to the bounded set is reachable from a test arm, so a branch that silently stops accepting is caught by absence rather than by a later bypass. Filed by the post-acceptance spec audit — F1's RESOLVED text promised the record and no row carried it
- TOOL-dTieredTribunal-28 · OPEN · `unattended.sh --landed` MUTATES BEFORE IT VALIDATES. Observed 2026-08-27: it wrote `phase: LANDED` into `RUN.md` and then failed check 34 with exit 1, leaving the working tree claiming a phase the driver's own checker refuses — an operator who committed that would have a LANDED record no gate accepts, and one who re-ran the verb would read the mutated phase as evidence the first run worked. Every other verb in this driver refuses first. Two halves: hoist check 34 and its siblings above the write, and add an arm asserting that a refused `--landed` leaves `RUN.md` byte-identical
- TOOL-dTieredTribunal-27 · OPEN · `tools/check-install-prefix.sh --write-ratchet` does not reach a FIXED POINT in one pass. The ratchet counts `tools/<kit>/` literals per file and `tools/install-prefix-carried.txt` is itself a list of such paths, so writing it moves its own row and the very next `--check` reds with `ROSE tools/install-prefix-carried.txt`. Hit during this build's landing: the printed remedy is "re-run --write-ratchet", and one re-run is not enough — a second converges. Two halves: exclude the ratchet file from its own population the way `check-review-join.sh` excludes the hook it delegates to, and make `--write-ratchet` iterate to a fixed point or say it did not reach one
- TOOL-dTieredTribunal-24 · OPEN · two mechanical arms for the `amendment-leaves-its-other-half-standing` class, which the post-acceptance spec audit hit four times in one session: a fork or criterion whose RESOLVED text promises a follow-up must name a LIVE backlog row, and a file named in a spec's section 2 must appear in its section 4 table. Neither catches the blocker that record describes; both catch the two cheapest instances of it, and both are greps over a spec the hygiene gate already parses
- TOOL-dTieredTribunal-21 · OPEN · `tools/govkit/selftest.py` writes a fixture named `x.json` into the PROCESS CWD rather than into its scratch dir, so a plain `python tools/govkit/selftest.py` from the repo root leaves an empty `x.json` behind in the tracked tree. Found when `git add -A` staged it during `TOOL-dTieredTribunal-11`'s round-2 fold, one `git rm --cached` away from being committed. The fixture is at `:987` in the manifest-grammar arm. Two halves: root the fixture on the arm's own tempdir, and add an arm asserting the suite leaves `git status --porcelain` unchanged — a self-test that dirties the tree it grades is a probe whose own side effects reach its subject
- TOOL-dTieredTribunal-16 · OPEN · `tools/workflows/tier2-review.js` computes every liveness counter it needs and hands NONE of them to the agent that writes the record. Its synthesis prompt names the raw, confirmed, refuted and unverified counts and the precision, and interpolates no lens or skeptic survival, so a run whose lenses half died writes a durable report that cannot say so. The two drift-audit siblings were fixed by `TOOL-dTieredTribunal-3`, which added a run-integrity block to each of their synthesis prompts; the reference harness they were copying from was not, and the closing review that found the class filed it as D2. `TOOL-dTieredTribunal-11` section 5 declines it explicitly and points at the gotcha record instead, so no unit of this build closes it. The class is `memory/gotchas/degradation-known-but-unreported.md` and this row is its live instance. The fix is the same block the siblings carry, adapted to this file's counter names, plus a criterion that reds while the prompt lacks it → tools/workflows/tier2-review.js
- TOOL-dTieredTribunal-17 · OPEN · `plan_state` in `tools/unattended/unattended.sh` maps a spec's sections by NUMBER — `## 2.` scope, `## 6.` acceptance, `## 7.` gates, `## 8.` forks — and a TIER-1 spec legitimately drops `## 5. Production-readiness checklist`, so every section from five onward is off by one. It therefore grades a Tier-1 spec's GATES as its acceptance criteria, its OPEN QUESTIONS as its gates, and its REVISION LOG as its open questions. Reproduced 2026-08-26 on `spec-dTieredTribunal-15`: `--plan` printed FORKED while its real section 7 carried a conforming `RESOLVED (agent, …, delegated)` mark, because the revision log has bullet items and no mark. The verdict is a FALSE POSITIVE and the shape is general — every Tier-1 spec in the corpus is graded on the wrong three sections, and `memory/TEMPLATE-SPEC.md` sanctions the nine-section canon explicitly, so this is not a malformed spec. A second-order hazard rides on it: a revision log line that happens to spell the conforming mark would RESOLVE the phantom section, which is `TOOL-dHonouredPark-7`'s quoting class one level over. The fix is to key on the heading TITLE rather than the ordinal, which is what the titles are for; note the sibling reader in the memory-tree kit grades the same grammar and the two are held in agreement by a case table rather than by shared code, so both move together. MEASURED: they already disagree — with the same spec CLOSED, `check-memory-hygiene.sh` exits 0 while `--plan` prints FORKED, which is the one case the case table exists to prevent → tools/unattended/unattended.sh
- TOOL-aReapedTicket-4 · OPEN · `ts_try_reap` in `tools/run-gates/run-gates.sh` reaps the holder beacon with a BARE `rm -rf "$TS_DIR_C"` at both of its two exit points, and the `aPacedTurnstile` spec that designed this mechanism explicitly prescribed a RENAME instead, with the reason stated: "The holder path is fixed, so deleting it directly can destroy a SUCCESSOR that claimed in the gap between reading the metadata and the delete. Renaming into the graveyard is atomic, exactly one racer wins" (`memory/builds/aPacedTurnstile/spec/2026-08-18-spec-TOOL-aPacedTurnstile-4.md`, the same paragraph whose TICKET half was also never built and which `TOOL-aReapedTicket-2` has now closed). Two waiters that both read a dead pid can both proceed to delete; the second delete lands on the FRESH beacon of a successor that claimed in between, and two bars then run — the exact condition the turnstile exists to prevent, arriving through its own reap path. The `nonce` guard does not cover it: that guards RELEASE, not REAP, and a reaper holds no nonce to compare. Found while grounding `TOOL-aReapedTicket-1`; deliberately NOT fixed there, because the failing case is a sub-millisecond race whose arm needs its own design and §7 forbids landing a guard whose failing case has not been observed. Note the observable property is testable WITHOUT racing it: two concurrent reapers against one dead beacon must produce exactly one reap line. Candidate fix is the spec's own — `mv` to a unique name, then delete, so exactly one racer wins and the winner takes the ordinary claim path → tools/run-gates/run-gates.sh
- TOOL-aReapedTicket-5 · OPEN · NARROWED by aUnblockedFleet (2026-08-31) to the STALENESS BOUND alone. Its blocking half is gone: neither `--preflight` nor leg check 7 refuses on the live count any more, so a run that dies in BUILDING, REVIEWING, RESEARCHING or TESTING no longer blocks every future unattended run on the repository. WHAT REMAINS, and it is now weaker rather than stronger: that abandoned record is still non-terminal forever, nothing distinguishes it from a live run, and the only thing that surfaces it is a REPORT an operator has to read — where it used to be a refusal they could not miss. The asymmetry this row named is unchanged and is why a signal is still owed: the phase is ASSERTED by the run itself, so a run that cannot assert leaves the strongest possible claim standing. The candidate second signal is still this row's own: a recorded pid that no longer answers, plus a record whose own last-commit date is past a declared bound — both already on disk, neither needing a new field. What aUnblockedFleet deliberately did NOT do is pick that bound, because a refusal is exactly what it was removing → tools/unattended/unattended.sh
- TOOL-aUnblockedFleet-7 · OPEN · THE LANDER MARKER IS SHARED BY EVERY WORKTREE OF ONE CLONE, so two unattended runs landing close together race on it. `.unattended.conf` declares `LANDER_MARKER="unattended-landed"`, resolved by both halves against `git rev-parse --git-common-dir`, and `--landed` requires the marker to name HEAD by EQUALITY. Run A pushes and writes the marker; run B pushes and overwrites it; run A's `--landed` then refuses naming both shas. It FAILS CLOSED, which is why aUnblockedFleet filed it rather than fixing it, and that build's removal of the live-run refusals makes it REACHABLE rather than creating it. Candidate: key the marker per branch or per run rather than per clone, so two landings cannot alias → tools/unattended/unattended.sh
- TOOL-aUnblockedFleet-8 · OPEN · TWO CONCURRENT UNATTENDED CLOSES IN ONE CLONE CONTEND ON THE MERGE BAR'S TURNSTILE, and the queue wait is charged against the run's own `GATE_BOUND`. `run-gates.sh` serializes one bar per repository keyed on the git common dir (`GATE_TURNSTILE` defaults to 1), `TS_MAXWAIT` derives to at least 7200s, and `unattended.sh` runs the bar under `timeout -k 5s $GATE_BOUND` with this project declaring 3600. So the second run can be killed for pure contention and reports `gates-green` unmet with the bar never having RETURNED — indistinguishable, to an operator, from a failing leg. aUnblockedFleet specced the fix as `TOOL-aUnblockedFleet-6` and RETIRED it when a spec audit refuted all three mechanisms: the queue-status path it read is per-worktree not per-common-dir, that file is deleted before the bar can be killed, and a `GATE_BOUND/4` queue bound at 900s against a 1560s bar means the queued close never acquires and always runs contended. THE PROPERTY A FIX NEEDS, which that audit named: `GATE_BOUND` minus the queue bound must exceed a CONTENDED bar, and nobody on this fleet has measured one → tools/run-gates/run-gates.sh · tools/unattended/unattended.sh
- TOOL-aGroundedOrientation-4 · OPEN · `--landed` FLIPS THE PHASE BEFORE ITS PRECONDITIONS PASS, and the resulting record cannot be repaired by any verb. Observed 2026-08-27 node `a` on `aGroundedOrientation`: `--landed` set `phase: LANDED`, then failed leg check 34 on a stale lander marker naming an EARLIER landing (`a9c6d386`, another session's push-main), returning rc=1. The record was left terminal with no `landed-anchor`, which check 15 reds for any record whose first commit is at or after `LANDED_ANCHOR_CUTOFF`. Re-running `--landed` then refuses with check 26 because the record is finished, so the driver has no path back. That is a wedged state reachable by a single failed verb, and every subsequent run on the box inherits the red — the same harm this run had already parked about `dTieredTribunal`. Repaired here the way `7cf96591` repaired that one: by hand, labelled `landed-anchor-source`, after verifying `git merge-base --is-ancestor <witness> origin/main`. Fix candidates: move the phase write AFTER the leg passes, or make the failure path roll the phase back, or admit a `--landed --repair` that re-runs the observation on a finished record. The marker check itself is CORRECT and should not be loosened — it caught real evidence of one landing standing in for another → tools/unattended/unattended.sh
- TOOL-aGradedDoorway-1 · CLOSED · `target_context` refused `prefix`/`kit_id`/`kit` from both target tables, which is a SUPERSET of the injection `ce4ef9f3` closed: the top-level `prefix` twenty lines above is target-supplied too and safe because it is graded STRICTLY. An adopter whose kit is not at `{prefix}/{kit_id}` lost its only way to say so. Now per table and per key: `[answers]` refuses all three, `[kit.<eid>]` takes `prefix` and `kit` through the strict class, `kit_id` is refused everywhere. Measured on the inCMS adopter: `adopt --re-adopt` 71 -> 93 rows, unseen receipt paths 20 -> 1 → memory/builds/aGradedDoorway/
- TOOL-aGradedDoorway-2 · INPROGRESS · `tools/unattended/adopt-unattended.test.sh` and `check-unattended.test.sh` spell their fixture targets with a LITERAL `tools/` while `seed()` installs the kit at a variable prefix. gov is correct by coincidence — its own prefix IS `tools`. An adopter must repath every one of them by hand, and a missed site is SILENT: `mutate`'s no-op guard fires and the arm asserts against a tree it did not change. Measured on the 1.11 pull into inCMS: NINE such sites arrived, into two suites that both seed at `scripts/unattended/`. Third recurrence — ARCH-dReadoptedConvoy-6 and ARCH-aThriftySentry-1 each cleared it once. DONE for adopt-unattended.test.sh (verified at three prefixes, 55 assertions each) and check-unattended.test.sh (145 sites; equivalence proven, foreign prefix unrun). check-playbook.test.sh is blocked on TOOL-dScrubbedConduit-2: its fixture hardcodes the prefix and ships role=engine → tools/unattended/
- TOOL-aGradedDoorway-3 · CLOSED · `tools/drift-audit/drift_report.py`'s default-branch probe was the ONE `subprocess.run` in that file without `encoding="utf-8"` — fourteen siblings carry it. `text=True` alone decodes with the platform default, which mis-decodes a non-ASCII branch name on a cp125x node and is a finding under a strict-encoding lint. Reported by the inCMS adopter, which has carried the one-line patch since ARCH-dReadoptedConvoy-1 S7. FIXED here; drift-audit selftest green → tools/drift-audit/drift_report.py
- TOOL-aGradedDoorway-4 · OPEN · check 21's build-README population uses a `*` pathspec, and git's `*` CROSSES `/`, so it collects every nested README under a build folder rather than the build READMEs themselves. Measured on the inCMS corpus: 41 non-build READMEs graded. That adopter narrows the selector to depth 3 and marks it INCMS PATCH C21; the same narrowing belongs here. Round-trip of ABL-dReadoptedConvoy-13 → tools/unattended/check-unattended.sh
- TOOL-aGradedDoorway-5 · OPEN · check 30 spawns a `--plan` subprocess per build, which costs a corpus-sized merge-bar leg on every node: measured at over 600 s on the inCMS corpus before that adopter fronted it with a TRACKED clean-key inventory and an `--emit-index` verb to print it. The key is slug + driver blob + tree oid, and a dirty build is never cached. Round-trip of ABL-aThriftySentry-1, whose other half — three fixes to gov's own arms — landed here at 1.11 → tools/unattended/check-unattended.sh
- TOOL-aGradedDoorway-6 · OPEN · `check-wiring.sh` carries prefix and launcher resolution for a non-`tools` install prefix in the inCMS adopter, plus arms gov has no counterpart for. Its `.test.sh` is retargeted to match. Neither half round-trips as written because gov cannot ship another repo's prefix, but the RESOLUTION half is the part that generalises and belongs here. Measured delta 122 lines over 692 → tools/check-wiring.sh
- TOOL-aGradedDoorway-7 · INPROGRESS · Five legs cost 6.1 h of a 6.7 h `--editor` bar and one costs 4 h 14 m alone. rev-3 folds in two measurements taken after ratification: the MERGE bar was never over 30 min (16.7 min at default width, floor-bound), and S3 timed one fixture invocation at real 128.0 s / user 12.1 s / sys 43.9 s, so check bodies are 9.5 percent of wall. Decomposition stays rejected and S4 now precedes S2 → memory/builds/aGradedDoorway/spec/
- TOOL-aGradedDoorway-8 · OPEN · The shard-join predicate ships only with the GOV-ONLY canary (`run-gates.gov.test.sh:346-403`), the suite explicitly allowed to hold claims about this repository, so an adopter that shards a kit suite gets complete-cover enforcement from nothing at all. The inCMS adopter ported it as `scripts/check-shard-join.py` at ARCH-aBridledVintage-8 rather than going without; the port is what belongs upstream, as a kit file rather than a canary arm → tools/run-gates/run-gates.gov.test.sh
- TOOL-aGradedDoorway-9 · OPEN · A run that HOLDS a leg and a run that PASSES it are indistinguishable from outside, and `run-gates.sh:1362` refuses only when EVERY leg is held. `drift-audit-selftest` sat unrun long enough for a twelve-arm breakage to reach an adopter, which is that gap firing. The adopter's answer at ARCH-aBridledVintage-8 S6 is a per-run report of the held legs and when each last produced a verdict, backed by a ledger in the git dir → tools/run-gates/run-gates.sh
- TOOL-aGradedDoorway-10 · OPEN · S3's spawn measurement is a fact about the NODE, not the kit, and no kit change recovers it: node `a` pays 251 ms per process creation against 19-39 ms on node `d`, and a shell builtin costs 0.053 ms. That single ratio is most of the gap between this suite costing 3565 s on `d` and 15270 s in the adopter on `a`. The remedy is an on-access-scanner exclusion for the repo and temp trees, which is an owner action on system settings → memory/builds/aGradedDoorway/build/
- TOOL-aLexedStripper-3 · OPEN · `agent-cap`'s rule 1 `offendingLines` reads the same per-line `stripStrings` view `TOOL-aLexedStripper-2` moved rule 2 off, so a raw `parallel(` or `pipeline(` written inside a lens PROMPT — prose, in a template literal, which cannot fan out — is denied as a call. Same false-positive class, one rule over. Deliberately out of that unit's scope: rule 1's fail-closed ceiling is documented in the file header and widening its blind spot needs its own fixtures in both directions → tools/hooks/agent-cap.js
- TOOL-aLexedStripper-4 · OPEN · `agent-cap`'s rule 3 `capFindings` reads `blankLiterals`, whose `let mode` sits OUTSIDE the per-line loop (`agent-cap.js:506`), so one unterminated template literal blanks every later line and rule 3 sees nothing below it. Pre-dates `TOOL-aLexedStripper-2` and is not introduced by it — that unit adds the fail-closed signal for rule 2 only, deliberately, to avoid re-baselining rule 3's arms. Rule 3 should consume the same signal → tools/hooks/agent-cap.js
- TOOL-aLexedStripper-7 · OPEN · `scan_js_definitions` in `tools/codebase-map/map_lib.py` strips BLOCK comments before LINE comments (`:461`, mirrored at `:539`), so a bare `/*` written inside a `//` comment opens a DOTALL block that runs to the next `*/` and swallows every definition between. This is over-strip class 4 from `TOOL-aLexedStripper-1` §4, in the definition probe rather than the token scan, and that unit did not touch it. Reproduced accidentally and exactly: a `//` comment in `tools/hooks/agent-cap.js` containing the two characters cost four definitions (`blankLiterals`, `fanoutFindings`, `openersOf`, `scan`), caught by the `js definition probe ⊇ the lexicon's own set` cross-check arm. Worked around by rewording the comment, which fixes the instance and not the class. MEASURED IN BOTH TREES with `map_lib`'s OWN `JS_DEFINITION_RULES` rather than a retyped regex, diffing probe-order stripping against line-comments-first: this repo now loses 0 across 2 carrier files, having lost 4 before the reword; `d41ly/incms` carries 35 files and loses 14 definitions across 9, including the `AccountDashboard`/`DashboardWidgetsPanel` pair and the `adminGetSafe` fetch helper — components a reuse probe is supposed to surface as the first step of every DoR there. Swallow SIZE is a bad proxy for harm and must not be used as one: `middleware.ts` loses 4368 characters and zero definitions while `AccountDashboard.tsx` loses 5968 and two real components, and a naive definition regex over-reports by matching English words in the swallowed prose. The proper fix is the same language-aware pass `_identifier_tokens` now uses, but it must PRESERVE LINE STRUCTURE because the probe substitutes newlines to keep line numbers, and it feeds committed artifacts (`symbols.json`), so it is its own unit rather than a closing-stage fold → tools/codebase-map/map_lib.py
- TOOL-aLexedStripper-8 · OPEN · the `codebase-map` 1.3 and `agent-cap` 1.9 fixes land upstream, but an adopter's RE-PULL still reds their kit-sync OID arm and this build told the `d41ly/incms` session otherwise. Measured in THAT tree, and every path below is theirs rather than one of ours: their `check_kit_sync.py` sets `ROLES_OWNED` to project-owned plus seed at line 64, so `engine` is NOT exempt; their generated `install.index` receipt records a blob OID per file; and the arm is a straight `row[oid] != oids.get(path)` whose own finding reads *"this file is vendored from coding-governance; land the change there and re-pull"*. So the check is DESIGNED to red until the receipt catches up, and the pull owes a `check-kit-sync.sh --write` regeneration on their side. What UPSTREAM actually bought is narrower than "no local work": it avoids the `engine` to `diverged` role flip and the divergence record whose marker their check 8 second arm then requires in the file — a permanent structural change under the local-patch route, absent under the pull. `ABL-dPinnedVintage-3` being BLOCKED stops their receipt FORMAT migration, not the routine regeneration. NOTE the asymmetry: their `agent-cap` copy is ALREADY role `diverged` with a delta marker and eleven pointer rewrites, so only `codebase-map` gets the clean stays-`engine` path. Filed because the session that needed it has ended and the claim was made in a transcript nobody will read. The first draft of this row cited their receipt as a ROOTED path and hygiene check 15 refused it — a path honest in one tree and dead in another, which is this build's own cross-tree class catching its own record → memory/builds/aLexedStripper/
- TOOL-aScouredKit-10 · OPEN · THE LEG-HOLD PREDICATE IS NOW WRITTEN TWICE AND NOTHING ASSERTS THE PAIR. `TOOL-aScouredKit-3` made `tools/govkit/govkit.py` agree with `tools/run-gates/run-gates.sh`'s hold predicate -- both hold a leg when `subject == kit` OR `chunk == selftests` -- but they are two implementations in two languages in two kits, and govkit cannot source the runner. The next edit to the runner's predicate drifts them apart exactly as before, silently, and the only witness would be a held count nobody re-derives. This is `two-readers-of-one-config-one-re-derived` with the config being a PREDICATE rather than a file, so the catalogued class does not quite name it. Candidate: an arm that drives `run-gates.sh` over a fixture manifest with one leg per hold-combination and compares the legs it actually skips against govkit's held set -- an assertion between the two RUNTIMES rather than between two readings of the source, which is the only form that cannot be satisfied by copying the bug. Found by the M6 bug-class checklist over the -3 commit, not by the audit that produced -3 → tools/govkit/govkit.py
- TOOL-aScouredKit-16 · OPEN · THE SEAM RANKING IS 63% NOISE AND TWO SHIPPED CONSUMERS ACT ON IT. `fan_in` in `tools/codebase-map/map_lib.py` counts bare identifier tokens with no symbol resolution, so a sibling definition of the same name, a stdlib attribute call, a local and a kwarg all score as references. Measured at 093730e4 by `TOOL-aScouredKit-1`'s wave-1 audit: 1626 of 2561 edges (63%) are files that merely DEFINE the same name, and 71 of 165 SEAMs fall below threshold on that filter alone. The top-ranked "seams" are `Path.read_text` (29/29), `Path.resolve` (25/25), `re.search` (21/21), `ArgumentParser.parse_args` (9/9) and sibling `main`s (34/34). `gen_map.py --seed-affordances --top 15` returns 15/15 junk. `map_diff.py --converge` APPENDED 31 WARN rows to the tracked reinvention-backlog under the map's own generated tree, first row `| main | main | ... | 34 |`; that file is append-only and deduped, so the fiction is permanent and it SHIPS to adopters (named without a rooted path: it is generated, and hygiene check 15 refuses a citation it cannot resolve). It also falsifies a recorded claim: `memory/builds/bConvergentLodestar/spec/` asserts the over-count is "mitigated by import/identifier scoping", and the measurement says it is not. TWO ARMS, and the cheap one is worth landing alone: gate `--converge`'s WARN emission and `--seed-affordances` off an unresolved index, THEN fix the ranking by subtracting same-name definers (the caller already holds `symbols.json`) and having `build_reference_index` record whether an occurrence was dot-prefixed. Both are on-demand code, so no committed artifact moves. What to do about the 31 rows already written is a decision, not a cleanup, because that file is append-only → tools/codebase-map/map_lib.py
- TOOL-aScouredKit-17 · OPEN · `dead_exports: 412` in `tools/codebase-map/map_diff.py` is 100% false positives and structurally pinned at roughly 59% of the corpus. Measured by wave-1: 385 of the 412 (93.4%) are module-private helpers zeroed by the `- {def_file}` subtraction in `fan_in`, and the printed caveat names the SMALLEST class present while omitting the one covering 93%. The figure cannot move on any repo that uses module-private helpers, which is every repo. Shares its root with `TOOL-aScouredKit-16` and should land with it; filed separately because the remedy differs — the ranking gets fixed, this number gets reworded or dropped, and a caveat naming the wrong class is worse than no caveat → tools/codebase-map/map_diff.py
- TOOL-aScouredKit-18 · OPEN · `tools/govkit/govkit.py`'s `read_descriptors` is `load_toml` and nothing else — there is no known-key allowlist for any descriptor table, so authoring a key costs nothing and no station ever objects. Measured by wave-1: injecting `bogus_key`, and misspelling `guard` as `gaurd`, both left `selfcheck` at exit 0 with zero mentions. THIS IS THE ENABLING CONDITION for four separately-found dead declarations — `requires_config_first` in the unattended descriptor, `placeholders` on `[[files]]` (whose rows have already drifted from their templates in 3 of 10 cases), a `kit = "cross-os"` naming a `[[block]]` from the `when:` namespace, and `drop_blocks` documented in `WIRE-INTO-PROJECT.md` as an operator control that `cmd_intake` has no answer, flag or prompt for. One allowlist with `r.fail` on an unrecognised key forecloses the whole class at authoring time. Separately, `govkit.py`'s UNGUARDED warning is predicated on `dropped and not guards`, the one state a dropped-guard typo cannot reach, so the warning cannot report the case it was written for → tools/govkit/govkit.py
- TOOL-aScouredKit-19 · OPEN · FIVE PYTHON READERS RE-PARSE `.memory-tree.conf` WITH A NAIVE `v.strip().strip('"').strip("'")` while the shell gate SOURCES it in bash — `corpus_ids.py`, `gotchas.py`, `gen_build_index.py`, `check-arms.py` and `row_grammar.py`. Reproduced by wave-1: `MEMORY_ROOT=memory   # note` takes `gotchas.py --check` from rc=1 to rc=0 over an IDENTICAL planted violation, because the Python half then walks a directory that does not exist. Coverage is REMOVED, not failed closed, and the gate stays green. Latent in this repo — no tracked conf here uses an inline comment or an `export` prefix — so the trigger is an adopter writing a legal spelling the kit's own example neither shows nor forbids. `TOOL-aScouredKit-5` fixed exactly this divergence in the drift-audit copy and its fixture now covers both spellings; this is the same class in a wider, riskier population. `gotchas.py` already imports `corpus_ids.py`, so routing all five through one parser costs no new coupling → tools/memory-tree/
- TOOL-aScouredKit-20 · OPEN · `tools/check-install-prefix.sh` IS BLIND TO THE CLASS IT GATES, in two independent ways, which is why wave-2's five hardcoded-prefix findings were all green on this leg. Both arms bind a kit-DIRECTORY segment (`$alt`) and an extension from `sh|py|js|md|json|toml`, so a literal naming a LOOSE file directly under `tools/` and any `.txt`/`.tsv`/`.conf`/`.example` path is invisible to both. MEASURED at 093730e4: 260 hits across 55 shipped files, INCLUDING the gate's own `WAIVERS` line. Widen the extension class and add a third arm over `git ls-files -- 'tools/*' | grep -v /`, using the same derivation discipline `$alt` already uses. Run `--list` over the tree before wiring it, expect roughly 55 rows, and note that about a fifth of the hits are scratch-repo fixture names inside `.test.sh` bodies — so the third arm needs a principled exclusion rather than a blanket widening → tools/check-install-prefix.sh
- TOOL-aScouredKit-21 · OPEN · `tools/check-testsuite-counts.sh` hardcodes `MANIFEST=tools/gate-legs.json` and a `memory/project/` waiver root, twenty lines from a sibling that derives the identical manifest and explains why: `tools/run-gates/run-gates.sh` uses `${GATE_LEGS:-$(dirname "$KITREL")/gate-legs.json}` and its comment says a hardcode "resolves to nothing at any other prefix". Reproduced by wave-2 at `prefix = "vendor/gov"`: exit 2, permanently, and `GATE_LEGS=` does not override it because this engine has no such seam. Its own descriptor repeats the literal beside a placeholder. `DEPL-dCarriedReceipt-15` already asks for the prefix-parametric fix and this instance sits OUTSIDE that row's measured 59-file population and outside `check-install-prefix.sh`'s needle, which is the only increment — fold it there rather than treating it as a fresh defect. SEPARATELY: `tools/govkit/entries/check-testsuite-counts.kit.toml` is the only descriptor in the tree with NO `[check]` table, which `run_kit_check` forbids — but that rule is reached only from `cmd_check` against a TARGET, so gov enforces it on adopters and never on itself. `selfcheck` carries the identical rule for `version_from` and no equivalent arm for `[check]`. Lift it into `selfcheck` → tools/check-testsuite-counts.sh
- TOOL-aScouredKit-22 · OPEN · `memory/HYGIENE.md`'s numbered catalog — the file whose own line 115 calls it the prose home for these rules — STOPS AT ITEM 22 while the shell implements 23 with three live `fail 23` arms. `grep -n "23"` over the whole file returns zero hits. `HYGIENE.template.md` ends at 22 as well and `kit-dogfood-parity.test.sh` byte-compares the pair, so the parity leg is green over a shared gap and no gate compares the catalog against the shell's numbers. The rule check 23 grades is not absent — its prose is under "## Acceptance ledger" — so the defect is that the numbering does not route there. Two byte-compared files must move together, which is why this is a unit rather than the fold `TOOL-aScouredKit-14` took → memory/HYGIENE.md
- TOOL-aScouredKit-23 · OPEN · `WIRE-INTO-PROJECT.md` (59833 B / 816 lines) and `.claude/skills/unattended/SKILL.md` (48767 B / 731 lines) HAVE NO DECLARED CEILING ANYWHERE — no row in `tools/template-size-limits.txt` or `tools/line-length-limits.txt`, no size leg in `tools/gate-legs.json`, and neither sits under `MEMORY_ROOT`, so hygiene check 6 never sees them. They are the two largest instruction documents in the product and the only two with nothing measuring them. A row alone is INERT: `check-template-size.sh` only measures a subject it is invoked on, so this needs a row AND a matching leg, seeded from the landed measurement. Related and separate: `AGENTS.md` sat 41 bytes under its 64512 ceiling before `TOOL-aScouredKit-14` recovered 405, all three product legs are unguarded, and `memory/guides/UNATTENDED-PROTOCOL.md` is at 92.9% of `GUIDE_CAP_BYTES` with 52 lines left. No prose cut is proposed for the SKILL: measured, it is a genuine re-derivation of the protocol rather than a copy, longest shared run 20 words of 7896 → tools/template-size-limits.txt
- TOOL-aScouredKit-24 · OPEN · ELEVEN OF 25 REGISTRY ENTRIES ARE NEVER PASSED TO `apply` BY ANY GATE, and 7 of those are named nowhere in `tools/govkit/selftest.py` or `tools/govkit/matrix.py` — their deployability rests entirely on `selfcheck`'s declaration arms, which grade a DESCRIPTOR and never an install. `SCRATCH_KITS`'s own comment states this ceiling outright ("that is a known ceiling, not an assertion about them"), so it is disclosed rather than hidden and this row is about draining it, not about a lie. Two adjacent record-vs-reality facts found with it: a `deployability.test.sh` under the govkit kit dir has never existed (`git log --all --diff-filter=A` is empty) though `DEPL-aTetheredConvoy-3` is CLOSED with an AC7 requiring it, and `lands_nothing`, the key that AC7 rests on, is in no descriptor. Deciding which of the eleven deserve a real install arm is per-entry judgement and was not attempted → tools/govkit/matrix.py
- TOOL-aScouredKit-25 · OPEN · `govkit update` CANNOT LAND A SOURCE GOV STARTED SHIPPING — re-measured live at 093730e4 by wave-2 rather than read, and still true. The classification loop iterates the RECEIPT, so a source file gov newly ships for a kit the target already claims is not in the iteration space at all, the verb reports the install clean, and the adopter finds out at the next ImportError. This is `TOOL-aFlaggedScaffold-3`, which already tracks it with the inCMS measurement; this row exists ONLY to record that it was independently re-confirmed at this sha and that it is half the answer to the owner's convergence question. Close this row against that one rather than working it twice → tools/govkit/govkit.py
- TOOL-aScouredKit-26 · OPEN · A CROSS-ENTRY DESTINATION TOKEN DOES NOT EXIST, and `TOOL-aScouredKit-15` worked around its absence. That unit gave `adopt-drift-audit.sh` a `DRIFT_WORKFLOWS_REL` override and an existence assertion, because the kit derives its SIBLING's directory by guessing the name `workflows` while govkit lands every kit at `{prefix}/{entry-id}`. The durable fix is for the descriptor to pass the sibling's resolved destination into the `[adopt]` argv — but `target_context` resolves `{kit}` for THIS entry only, and there is no `{kit.<other-eid>}` form. Any kit whose adopter must name a sibling's path has the same problem; drift-audit is the only one measured. Adding the token means deciding what it does when the named entry is not in the selection, which is a design question and not a patch → tools/govkit/govkit.py
- TOOL-aScouredKit-27 · OPEN · `chunk` DECIDES WHETHER A LEG RUNS AND DOES NOT TRAVEL TO ADOPTERS. `tools/run-gates/run-gates.sh` holds a leg when `subject == kit` OR `chunk == selftests`, and `TOOL-aScouredKit-3` made gov's own pin file and held-count read both — but `chunk` still appears in no kit descriptor, in no `[[gate_leg]]` claim, and in no row govkit emits into a target's manifest. So three legs held in THIS repo (`push-main self-test`, `pre-push self-test`, `run-gates canary`) reach an adopter's bar unheld. The fix is NOT a one-liner and half-building it is worse than the gap: `subject` is emitted only under `check_target_reads_subject`, because `tools/gate-legs.json` has a PINNED key set asserted by the `run-gates canary` leg, and writing an unknown key into a target whose run-gates predates it REDS THEIR CANARY as a side effect of a routine `apply` — the deployer breaking a target's gate while installing something else. `chunk` needs its own floor predicate, or a decision that the two share one, plus a `[[gate_leg]]` field and an agreement arm in `selfcheck`. Found by this build's own closing review → tools/govkit/govkit.py
- TOOL-aScouredKit-28 · OPEN · A QUOTED CONF VALUE WITH A TRAILING COMMENT DIVERGES FROM BASH IN BOTH PYTHON PARSERS, identically, so `TOOL-aScouredKit-5`'s equivalence fix did not reach it. Reproduced at 093730e4: for `DISCIPLINES="tooling playbook"   # note`, bash sourcing yields `tooling playbook` while both `map_lib.load_conf` and `drift_report.load_conf` yield `"tooling`. The value starts with a quote and does NOT end with one, so the quoted branch declines it and the unquoted branch takes `.split()[0]`, keeping the opening quote. `MEMORY_ROOT` reads through that same parser, so an adopter who quotes it and comments the line gets a memory root with a leading `"` and every downstream path is wrong. NOT introduced by -5 and not hidden by it: -5 made the drift-audit copy EQUAL to `map_lib`, which was its goal, and this is a defect they now share. The fixture case is deliberately NOT added yet, because adding it reds the selftest until `map_lib` is fixed too and both kits ship independently — sequence the parser fix first. Found by this build's own closing review → tools/codebase-map/map_lib.py
- TOOL-aScouredKit-29 · OPEN · `tools/govkit/selftest.py`'s memory-recall leak arm is a SUBSTRING TEST ITS POPULATION SATISFIES TRIVIALLY, so a real leak passes it unchanged. Reported by this build's closing review with the line cited; not fixed here because the arm's intent has to be recovered before its predicate can be rewritten, and guessing at what it meant to assert is how a check ends up grading something else with the same confidence. Read the arm, decide what it is for, then give it a fixture that fails without the property → tools/govkit/selftest.py
- TOOL-aScouredKit-33 · OPEN · THE WAIVER PRODUCER STILL WORD-SPLITS while its consumers no longer do. `tools/check-install-prefix.sh` reads its registry with `awk '{print $1}'`, so a waiver row naming a path that contains a space is truncated at the space — and `TOOL-aScouredKit-6`'s fold hardened the hit PRODUCER with `-0` and both CONSUMERS with `while read`, which means a spaced hit is now reported correctly and can never be matched by a waiver, so it cannot be waived at all. Latent in this repo (0 spaced tracked paths, measured) and reachable in any adopter tree, whose files this same shipped gate grades. Found by this build's own closing review round 3, filed rather than folded because the waiver grammar is a FORMAT question — a path with a space needs a quoting or a field-count rule, and inventing one inside a fold is how a registry format gets decided by accident → tools/check-install-prefix.sh
- TOOL-aScouredKit-34 · OPEN · A MALFORMED DIRECTIVE SCOPE IS REPORTED AS THE RUN'S FAULT. `check_waiver_scope` in `tools/unattended/unattended.sh` compares a directive's declared scope against `all` and the run's own mode, so a scope that is not a member of the vocabulary at all — reachable, because a project appends its own directives through `.unattended.conf`'s `DIRECTIVES_EXTRA` — falls into `fail 45` and is reported as "a mode this run is not". That blames the RUN for a defect in the DECLARATION, and it is the reading under which an operator changes the wrong thing. `TOOL-aScouredKit-7` wired the then-dead `is_scope` in to fix exactly this and then DELETED it again, because the guard could not be ARMED and a guard nobody can make fire is what this kit's own gates refuse. THREE FIXTURE ATTEMPTS AND WHY EACH FAILED, recorded so the next one does not re-derive them: (1) appending to `.unattended.conf` leaves the tree dirty, so preflight refuses at check 2 long before the waiver scope is read — the fixture must `fixture` (stage and commit) after the append; (2) the directive format is `handle:carrier:scope` and a TWO-field entry has no scope at all, which `scope_of` defaults to `all`, so the waiver is legally accepted; (3) with three fields and a committed conf the waiver was STILL accepted, cause not established — the next step is to instrument `scope_of`'s return inside the fixture rather than infer it from the outcome. Until that is settled, whether the branch is reachable is genuinely unknown, and this row says so rather than asserting the guard is needed → tools/unattended/unattended.sh
- TOOL-aProvenReuse-3 · OPEN · A NON-CONVERGENT REVIEW LOOP HAS NO LEGAL DISPOSITION WHEN ITS SUBJECT IS A SPEC. `memory/guides/BUILD-METHOD.md` M4 and `unattended.sh --review` both say every blocker still standing at the exit is PROMOTED to a unit of the build, specced at its tier and built. That works when the subject is a DIFF and a blocker is undone work. It has no referent when the subject is a SPEC: M2's decompose rule is one MECHANISM per spec, and a blocker like `section 8's fork resolution ratifies the option section 4 rejects` or `an acceptance criterion returns 7 hits and cannot pass` is not a mechanism, so the promoted unit is one M2 forbids — and its spec would then owe an audit, which is the regress non-convergence exists to stop. Reproduced on this build: rounds 1 and 2 each confirmed exactly 4 blockers, the loop exited NON-CONVERGENT, and the run folded all four as a `rev-N` bump instead, recording the departure in the build README. Either M4 needs a second disposition for a spec-subject exit (fold-and-stop, which is what every run will actually do), or the promotion sentence needs to say it is diff-subject only. Found by the run that hit it → memory/guides/BUILD-METHOD.md
- TOOL-aProvenReuse-4 · OPEN · ONE OF M5'S TWO PROBES IS UNOBSERVABLE. `tools/memory-recall/query.py` logs every query to `<git-common-dir>/recall/queries.jsonl`, which is what `TOOL-aProvenReuse-2` reads to prove a recall probe ran; `tools/codebase-map/reuse_lookup.py` logs NOTHING, so the map half of the reuse obligation has no liveness evidence at all and its §10 citation stays pure prose. Deliberately not built by `aProvenReuse`: closing it needs either a cross-kit dependency (codebase-map writing to a memory-recall convention) or a third telemetry format and a third kit version bump, so it fails protocol §11's strictly-beneficial test and is a backlog row rather than an adoption. The cheap shape if it is ever taken: codebase-map writes its own `<git-common-dir>/codebase-map/lookups.jsonl` with the same row grammar, and `reuse-probed` counts either log — no kit learns another kit's path → tools/codebase-map/reuse_lookup.py
- TOOL-aProvenReuse-6 · OPEN · THE BOUNDED-OBSERVATION ARMS ARE WALL-CLOCK ASSERTIONS AND FLAKE UNDER FLEET LOAD. `tools/unattended/unattended.test.sh` proves `run_bounded` reaches `check_wiring` and the `gates-green` arm by asserting that a run against a 2s GATE_BOUND returns in under 25s. Measured on node `a` 2026-08-31 across three runs of shard 2/2 on ONE unchanged tree: the run with no competition PASSED at 669 assertions with zero failures, and two runs overlapping other Claude sessions executing these same suites on sibling worktrees FAILED at 35s, 44s and 66s. `ps` showed twelve concurrent test-suite processes from three worktrees. The arms are not wrong about the mechanism -- the `did not answer within the declared` message fires correctly every time, so the BOUND is observed -- they are wrong to measure the bound's effect in wall clock on a machine the suite does not own. A 12.5x margin was not enough. Options, none costed: assert on the driver's own reported elapsed rather than the harness's, raise the margin and say what load it survives, or mark the arms load-sensitive and skip them when a sibling suite is running. Found by the run that hit it three times; nothing here is a regression, and the diff that found it touches zero lines of run_bounded, check_wiring, GATE_BOUND or RB_OUT -> tools/unattended/unattended.test.sh
