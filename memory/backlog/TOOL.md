# tooling backlog

> Mutable. Each row leads with one status token (OPEN…WONTDO).
> **Rotated 2026-08-14** — 48 terminal rows moved byte-identical to [`../archive/TOOL.2026-08-14.md`](../archive/TOOL.2026-08-14.md), covering `TOOL-aRuledParchment-2` through `TOOL-cFinalBerth-2`. Nothing deleted; the all-time id-collision grep still reaches them there.
> **Rotated 2026-08-17** — 89 terminal rows moved byte-identical to [`../archive/TOOL.2026-08-17.md`](../archive/TOOL.2026-08-17.md). TWO concurrent builds rotated on this date and that file is their UNION, deduplicated by id, so no id range names it.
- TOOL-dFramedEntrypoint-1 · OPEN · two documents tell a workflow author that a bare `parallel([...])` over five or fewer literal elements passes `agent-cap` unmarked, and it does not. `AGENTS.md` §8 and `tools/hooks/README.md` both carry the sentence immediately after the bullet BANNING the raw primitive, so it reads as an exemption from that bullet. `tools/hooks/agent-cap.js` applies the array-literal allowance only to rule (b) — the RECEIVER of an `agent(` fan — while the raw primitive is denied unconditionally unless its line carries `gov:bounded-fanout`. Observed live: a five-thunk literal fan was denied on the first Workflow call of this build, and the denial message was the only correct statement of the rule available. Fix is two sentences, in the file that owns each; the hook is right and the prose is wrong
- TOOL-dScriptedRepeat-15 · CLOSED · the kit gate is 469 process spawns per invocation and the suite invokes it 243 times, so the suite’s unit of cost is ~114,000 process creations. MEASURED, correcting every earlier claim in this row: the leg is not compute-bound at all (`real 14.4s user 0.33s sys 0.62s` inside its own fixture, 93% waiting), an on-access antivirus scanner fronts every `exec` on node `d` at 0.019-0.039 s a spawn, and 469 x 0.022 s reproduces the 10.7 s invocation exactly. The "~28x", the "~23 min of fixture reset" (a reset is 0.123 s) and the 13.2 s quotient are all superseded in the build’s bar-cost record. THE LEVER TAKEN was spawn count: three loops that ran a grep or a `bash -c` per (item, file) now read each file once, 469 -> 220, counted from an execution trace both ways. Per-arm scoping was DECLINED with numbers - it saves ~320 s more, needs 243 call-site edits and carries the vacuity its own AC was written for, and does not change the outcome. The ceiling is re-declared 900 -> 1800 s with the derivation beside it. NOT OBSERVED: the suite end to end, because the owner stopped these suites; the equivalence that replaces it is 17 staged breaks, 16 of them red, byte-identical before and after → `builds/dScriptedRepeat/`
- TOOL-dUnstalledConvoy-24 · OPEN · `--close` STAGES the LANDING phase and nothing commits it, so a run that merges from another tree carries BUILDING into the merge and `--landed` refuses with `check 31`. Hit at this build own landing 2026-08-21: the fix was to re-run `--close` on the merged tree, which costs a full bar and is not obvious from the refusal text. The refusal is CORRECT — the phase attests that the DoD was evaluated, so an agent must not hand-write it — but the flow leaves a staged file the run is never told to commit. Candidates: `--close` commits the phase itself the way `--landed` callers expect, or its success message names the commit as the next step, or `check 31` detects a STAGED LANDING and says so instead of reporting the committed phase. Any of the three ends the trap; picking one is the work.
- TOOL-dSettledRoster-6 · OPEN · THREE `resolve_bash` copies exist and two of them probe a weaker property than the third: `corpus_ids.py:158` and `govkit.py:442` ask `-c ":"` (does this bash start), while `profile_bar.py:239` asks `test -f "$script"` (can it SEE the script), which is the property that actually matters and the one the WSL launcher fails. Found by the `TOOL-dSettledRoster-5` §10 reuse audit, which also corrected a gotcha record that named two. Converge on the stronger probe, or record why three shapes are right
- TOOL-dScriptedRepeat-1 · CLOSED · playbook mode: a THIRD `authorized-by:` member (`recipe`) with an attended entry point, a PLAYBOOK TEMPLATE whose every step is `GATE <leg>` or `CHECK <why>`, per-piece and set-scoped records with their own Definition-of-Done items, a `proposal` park kind, and the Skill's four start paths. TEN of eleven units built and CLOSED; unit 8 (the output-scope refusal) WITHDRAWN after its hermetic arm could not be fired, its two real defects fixed and recorded. Round-1 Tier-2 over the landing diff returned BLOCKED at 32 confirmed / 16 distinct defects, precision 0.91 — both blockers were gates that cannot fail in code this build shipped green, and the whole set is folded → `builds/dScriptedRepeat/`
- TOOL-dScriptedRepeat-12 · CLOSED · the six built units could not flip to CLOSED: main's memory-hygiene check 22 arrived mid-build and requires every numbered acceptance criterion of a closed Tier-2 unit to be evidenced by a journal record. The real debt was units 6 and 7, whose `dod_met` branches shipped with NO test arm at all. Fourteen arms written, then the ledger from the observations rather than from the specs — which caught three criteria a spec-compiled ledger would have recorded as met, including one this build ratified and never implemented (`--override pieces-complete` was accepted) → `builds/dScriptedRepeat/`
- TOOL-dScriptedRepeat-13 · CLOSED · the bypass-flag guard on the two evidence-record writers had no reader, and rev-2 moved the fix out of the file rev-1 named. Measured before building: `GITLS`, `declared_scalar` and the playbook enumeration are all ZERO in `check-unattended.sh` and non-zero in `check-playbook.sh`, so building rev-1 would have inlined a third copy of a parser past a gate that compares exactly two. It is check 10 of the playbook leg instead, over the census’s own declared `records` roots. The arm that matters asserts SHARING rather than liveness - it MOVES the declared root and reds unless both readers follow - and both write-time refusals cite check 10 again now that a check 10 exists → `builds/dScriptedRepeat/`
- TOOL-dScriptedRepeat-14 · CLOSED · a build README can assert a MECHANISM its own spec set revised, and nothing read the pair. `build_readme_mechanism_drift` in the drift audit reports it, report-only: a backticked mechanism token in a README’s AUTHORED prose where that build’s spec revision log names the same token at an entry dated later than the README line’s own `git blame` date. Ship band measured before wiring: 31 rows over 8 of 60 READMEs, 13% against a 25% ceiling, 7 of the 8 being builds other than this one. THE MOTIVATING INSTANCE IS INVISIBLE TO IT and that is the finding - spec 6 contradicted the README’s `--counts` sentence while never spelling `--counts` anywhere, so no token-identity predicate can see that pair, and neither comparison variant tried over the corpus does. So the fold item ships BESIDE the signal, in `BUILD-METHOD.md` M8, saying in its own words that the signal covers only the token-identical subset → `builds/dScriptedRepeat/`
- TOOL-dUnstalledConvoy-23 · OPEN · redesign the dispatch write-set grading, which ships DARK behind `DISPATCH_GRADING` (default blank) after four adversarial rounds failed to make it correct. Reports: `memory/builds/dUnstalledConvoy/reviews/` — rounds found 16, 10, 8 and 10 defects and the repairs introduced 5, 3 and 5, with no fall in the introduction rate. The two failure modes to design AGAINST, both reproduced end to end: a refusal no run can clear in band (round 3, terminal in an unattended run), and a driver call that retracts a failure the leg already emitted (round 4, rc=1 to rc=0 over one violating tree). The round-4 report's own conclusion is the design constraint — one predicate served three callers whose edges disagree, so the answer is separate predicates per question, not a cleverer shared one. The declaration recording, condition 1 and condition 3 all work and are not in scope to change.
- TOOL-dUnstalledConvoy-19 · OPEN · APPENDING to a test suite strands the arms, and every static signal says they are fine. `tools/unattended/unattended.test.sh` ends with `exit "$st"`, so anything added after it is dead — hit TWICE in one session, for `--rescope` and again for `--dispatch`, about fifty arms in total, while the suite printed PASS and `check-arms.py` text-matched every one. The existing guard is FLOOR_ASSERTIONS, which was 338 against an executed 398: a sixty-arm slack that hides fifty stranded arms exactly. Raised to 445 here, which fixes the instance and not the class. The class wants a STRUCTURAL arm — no executable line may follow the suite's terminal `exit` — which is one grep and cannot go slack the way a numeric floor does. `check-arms.py` cannot see it either, and its blindness to reachability is already recorded as `TOOL-cBriefedPilot-27`
- TOOL-dUnstalledConvoy-18 · OPEN · a PYTHON rewrite of a shell source silently eats a LONE CR. `open(p).read()` and `Path.read_text()` use universal newlines, so a bare CR *inside* a line becomes a newline — and `tools/unattended/check-unattended.sh` carries three of them, inside `sub(/<CR>$/,"")` awk programs. Hit on 2026-08-21: the edit looked clean, the diff looked clean, and check 18 died with `awk: unexpected newline` because its program had been split in half. Measured the blast radius across every file this session rewrote — exactly one carried lone CRs, so the damage was contained, but only by luck. The fix at the call site is `open(p, newline="")` on BOTH read and write. The GATE this wants is a byte-level arm: no tracked file gains or loses a lone CR in a diff that does not say it means to. Same family as the eol traps already catalogued, one layer up — those watch line ENDINGS, and this is a CR that is not one
- TOOL-dUnstalledConvoy-17 · OPEN · `memory/guides/UNATTENDED-PROTOCOL.md` section 7 is titled "The verbs" and lists eight of them, omitting `--park` and `--attest` — both of which the Skill documents and both of which write the run-state file. A reader who trusts the section's own title learns a verb set smaller than the driver's, and `--rescope` had to be APPENDED rather than placed, because the list's ordering principle cannot be inferred from an incomplete set. The driver already asserts its THREE enumerations name one set (header docstring, usage line, unknown-argument refusal); the protocol is a fourth carrier of that same set and nothing joins it to the other three
- TOOL-dUnstalledConvoy-16 · OPEN · a correction to a claim held by SEVERAL carriers lands in one of them and nothing notices. Hit here: `memory/guides/REVIEW-PROTOCOL.md` was corrected when the sidechain-inheritance claim was measured false, while `coding-governance-agents.template.md` and its render `AGENTS.md` kept the refuted sentence and SHIPPED it to every adopter. No backlog row covered it and no gate could see it, because the three carriers state the claim in different words — a byte-comparison finds nothing and there is no shared token to key on. Same shape as the two OPEN rows about a kit-version remedy naming three carriers where six exist, one layer up: the defect is that a claim's carrier SET is nowhere declared. Candidate: a claims registry naming each cross-carrier assertion and the files that state it, asserted in both directions like the deploy surface already is
- TOOL-dUnstalledConvoy-15 · OPEN · the bare-interpreter class survives outside govkit: `tools/lexicon/selftest.py` executes `["bash", "tools/lexicon/adopt-lexicon.sh", "--check"]` twice, and `tools/govkit/selftest.py` writes bare-`bash` argv into fixture manifests that are then executed. Both are GREEN today only because the legs they run need no module the WSL python lacks, which makes them latent rather than safe — the same call under a kit that grows a 3.11 dependency reds the bar with a traceback naming a `/mnt/c/...` path. `govkit.exec_argv` exists and is the seam; what is missing is a rule that every python-side execution of a leg argv routes through it, and the source-level arm that would gate it beyond the two govkit files it currently covers
- TOOL-dUnstalledConvoy-14 · OPEN · hygiene check 12 asserts the HEADER rev appears in section 9 and says NOTHING about the log itself, so a spec can carry two lines with the same rev number, in the wrong order, with the gate green. Hit on 2026-08-20: a batch fold anchored its revision line on `- rev-1` against a spec already at rev-2, producing a second rev-2 above rev-1 while the real rev-2 sat below it. Section 9 is described as the rev high-water a resumed session reads, so a duplicated or misordered log is read by exactly the reader it was written for. Two cheap arms: rev numbers in a log are UNIQUE, and they DESCEND. Both are one `awk` pass over a section the check already extracts
- TOOL-dUnstalledConvoy-13 · CLOSED · a build's spec-audit record cannot fit the generated record-bindings row under `BUILD_README_ENTRY_CAP_CHARS` once the build passes about eleven units, BY CONSTRUCTION: the row carries the record filename, its path and every id the record serves, and the binding grammar expands a contiguous range at authoring time so the ids cannot be abbreviated. Measured on dUnstalledConvoy - 13 units, 438 chars against the 350 tier, while the longest other line in the same file is 319. The remedy available today is a curation-debt row, which silences checks 6 and 7 on the whole file to buy one row; the renderer should instead wrap or abbreviate the serves column, or the tier should price a generated row separately from an authored one. Same renderer-shaped class as the `aRuledFrontispiece` debt row, one column over
- TOOL-dSettledRoster-4 · OPEN · `check-verdict-epoch.sh`'s remediation message names THREE carriers to bump (`check-memory-hygiene.sh`, `HYGIENE.template.md`, `memory/HYGIENE.md`) and there are SIX `gov:kit memory-tree@` markers: `BUILD-METHOD.template.md`, `SPEC-TEMPLATE.template.md`, `memory/TEMPLATE-SPEC.md` and `memory/guides/BUILD-METHOD.md` are absent from it. Following the message exactly leaves the bar RED on `kit version markers` — hit on 2026-08-20, and it is this repo's own named defect, a fix naming more than one carrier landing in only one. Derive the list from the markers. HIT AGAIN 2026-08-21 during dUnstalledConvoy: following the message exactly bumped three carriers, the bar redded on the other three, and the fix cost a second full-bar cycle — the same defect twice is what a papercut looks like before somebody fixes it
- TOOL-dSettledRoster-3 · CLOSED · the `govkit acceptance matrix` leg was RED on clean `main` and blocked every push through `push-main.sh`. This row's stated root cause was WRONG and the correction is the value here: it blamed `adopt-playbook.sh`'s `-c 'import sys'` probe for selecting an interpreter below the kit's floor. MEASURED on node d 2026-08-20, the probe never got the chance — `subprocess.run(["bash", ...])` resolves the WSL launcher in System32 before Git's bash, so the adopter ran under a DIFFERENT OPERATING SYSTEM whose python is 3.10.12 without `tomllib`, while `shutil.which("bash")` in the same process returned Git's bash with python 3.14.5 and `tomllib`. The tell was in the traceback all along: a `/mnt/c/...` path no Git-Bash tool can emit. Node-SPECIFIC — it needs WSL installed, which is why nodes a-c never saw it. Fixed by `govkit.resolve_bash`/`resolve_shell_argv`, which name the EXECUTABLE and accept a candidate only if it RUNS. Class: `memory/gotchas/subprocess-resolves-a-different-shell.md`.
  TWO SESSIONS FIXED THIS INDEPENDENTLY in the same week and the merge is the interesting part. Node d's `main` landed `resolve_bash`/`resolve_shell_argv` wired at four python-side leg executions; the `dUnstalledConvoy` branch landed a `shutil.which`-based `resolve_argv` wired at five. The merge kept main's implementation — `shutil.which` does not RUN the candidate, which is the same mistake `tools/lib/resolve-python.sh` documents for the MS-Store stub — and took the branch's FIFTH site, the `[[hole]]` discharge probe, whose shipped commands are literally `["bash", "-c", ...]` and which main's pass left unwrapped. Neither pass found every site alone. `selftest.py` now carries a SOURCE-level class arm asserting no leg argv in `govkit.py` or `matrix.py` is executed without the resolver — the behavioural arms were green in both misses, because the function was never what was broken. That arm's first draft redded on fixture text `matrix.py` writes into a synthetic target, so it now ignores matches inside string literals; observed RED on a staged unwrapping before landing.
  Latent siblings NOT fixed: `tools/lexicon/selftest.py` runs a bare `bash` twice and is green today because its leg needs no 3.11 module — TOOL-dUnstalledConvoy-15 carries that.
- TOOL-dSettledRoster-1 · OPEN · `AGENTS.md`, the one file every session is pointed at, is reachable by no PATH gate. `check-install-prefix.sh`'s population is `tools/* skills/* `.githooks/* *.template.* *.fragment.json` and measured 200 tracked files with zero named `AGENTS.md`; hygiene check 15 scans `memory/` only. `corpus_ids.py` check 16 does read it, and since TOOL-dSpentCeiling-1 it reads it STRUCTURALLY rather than as a byte BUDGET — but it grades the charter's CITATIONS, never the charter file itself, so the gap this row names is unchanged
- TOOL-aMeteredTurnstile-6 · OPEN · the bar did not get slower, PROCESS CREATION did: measured 2026-08-20 on node a against the 2026-08-11 baseline, `bash -c true` 22.5ms -> 581ms, `git rev-parse` 23.9ms -> 727ms, `python -c pass` 103.7ms -> 1297ms, `mktemp -d`+`git init` 2751ms. CPU 39% with CPU queue 0 and disk queue 0, so it is serialized per-op latency, not contention. Defender real-time is ON; exclusions need admin to read. Widening the pool is 26% SLOWER at width 24 vs 8 — measured, do not retry. REFINED 2026-08-21 by TOOL-aScannedThrottle-1 §3.8: the tax is LOAD-DEPENDENT, not standing, so this row's "serialized per-op latency, not contention" conclusion does not survive. Re-measured on a QUIET box, median of 15: `bash -c true` 32.5ms, `git rev-parse` 48.0ms, `python -c pass` 151.2ms, `mktemp -d`+`git init` 158.1ms — within 1.4-2.1x of the 2026-08-11 baseline (`git rev-parse` is the 2.01x, and it is the op that scales), and `git init` 17x faster than the degraded reading above, which was taken under four concurrent bars
- TOOL-aMeteredTurnstile-2 · OPEN · the bar LEAKS scratch git repos into TMPDIR and never sweeps them: 786 dirs / 6862 top-level entries on node a, 531 from 2026-08-18 alone, each holding .git plus tools/. `du -sh /tmp` did not finish in 4 min. ISOLATED cost: `git init` 2350ms there vs 1564ms in a clean dir on the same volume, so ~1.5x on the bar's hottest op — a multiplier on TOOL-aMeteredTurnstile-6, not the cause. Give each leg's scratch an EXIT trap and sweep. RE-MEASURED 2026-08-21 by TOOL-aScannedThrottle-1 §5, WORSE than recorded: 1053 dirs against this row's 786, +34%, with 291 created on the measurement day alone. `run-gates.sh` never sets `TMPDIR`, and one export there would redirect all 63 `mktemp -d` sites at once
- TOOL-aMeteredTurnstile-3 · OPEN · `gate-timings.tsv` evicts on the RUN, never on the manifest: the carry-forward predicate at run-gates.sh:290 is "did this run measure it", so a renamed leg keeps its row forever. Measured 88 rows against 85 legs, 3 orphans holding 965s = 12% inflation on any sum. REFINED 2026-08-21 by TOOL-aScannedThrottle-1 §5 — MECHANISM REAL, INSTANCE GONE: the LIVE ledger is clean at 0 orphans, 88 rows against 88 legs. The orphan rows sit in the dead `gate-timings.tsv`, which nothing has read since the rename to `gate-ledger.tsv`: measured 2026-08-20 at 964.2s across the then-live per-worktree copies, and 13.96s across FOUR orphan rows in the common-dir copy that survives at HEAD (`memory hygiene (20 checks)`, `marker contract (4 readers)`, `verifier fan-out`, `template size <=32KiB`; re-derived 2026-08-21), the worktree cleanup at 49aea26 having removed the rest. So the eviction predicate is still wrong and nothing is currently inflated by it — RE-DERIVE the sizing before pricing a fix, and note this row's surviving "12% inflation" is the 2026-08-20 reading of a population that no longer exists. The comment at :287 says a dropped leg falls out on its own — TRUE of dispatch order, FALSE of the file, and it is the write path it describes
- TOOL-aMeteredTurnstile-4 · CLOSED · `AGENTS.md` states bar figures in prose beside the source that owns them, breaking the charter's own rule. RE-CITED 2026-08-21 by TOOL-aScannedThrottle-1 §5 — this row's original citation (`AGENTS.md:492`, "335s serial to ~95s at width 8") no longer resolves anywhere in the file, so following it led to prose that is not the defect. THREE live claims, all wrong. The wall/leg-sum pair states 873s where the measured mean across four GATE_FULL bars is 1001.3s — the mean is 14.7% ABOVE the charter's figure, and the charter's figure is 12.8% BELOW the mean; one gap, two readings, so name the base. The dispatch-cache sentence names `gate-timings.tsv`, a DEAD file since the rename to `gate-ledger.tsv` — so the charter names a file nothing reads as the live scheduling input, which is the subject of this build's own headline finding. And `AGENTS.md:480` states that EACH self-test leg carries a `guard`; derived from `tools/gate-legs.json` on 2026-08-21, four `selftests`-chunk legs carry none. CLOSED 2026-08-21 on the owner's decision: all three corrected. The dead filename and the guard universal now point at their sources, and the wall/leg-sum pair is re-measured to 393s/3085s with both halves of its gov-canary arm moved together
- TOOL-aMeteredTurnstile-5 · OPEN · longest-first dispatch pessimises time-to-first-signal by construction: 8 slots filled by the 8 longest legs means nothing reports until the 8th longest returns. MEASURED on node a — zero of 86 legs reported for 9.3 minutes, 17 by +20 min, then 50 more in the next 5. Makespan-optimal and feedback-worst; report time-to-first-signal as its own number. CONFIRMED and BOUND TO R1 2026-08-21 by TOOL-aScannedThrottle-1 §4: 669.1s to first signal with a warm ledger against 5.1s with a cold one, a 131x swing decided solely by ledger warmth. It is the COST of fixing TOOL-aScannedThrottle-8, not an independent item — ship that fix with a reserved short-leg slot, or it trades a real 16% of span for a perceived regression. DECOUPLED 2026-08-21: TOOL-aShardedFloor-4 is WONTDO, so this is no longer the cost of anything — it stands alone on its own merit, which is that 669.1s to first signal is bad on a warm ledger regardless of what dispatch buys
- TOOL-aPromptedMandate-14 · OPEN · `marker-contract.test.sh` gates the four readers of the generated region's MARKERS and nothing gates readers of its ROW GRAMMAR, which is why a THIRD open-coded copy (`verb_landed`) survived a fix whose whole rationale was that copies diverge. Generalise the contract to the row selector, or route every reader through `unit_rows`
- TOOL-aPromptedMandate-11 · OPEN · `keepalive-reaped` is attestable but not checkable: the driver records an id it can never verify, so an agent can attest it in good faith and be wrong. MEASURED here — this run asserted twice that two keepalives died with their processes because the store is session-scoped, and `CronList` showed both still firing. Needs a read-back the agent can be held to
- TOOL-aPromptedMandate-8 · CLOSED · `build-complete` is unsatisfiable for any build following the method: `unit_rows` selects `^| [` across the WHOLE gen:build-index region, which renders a UNITS table and a RECORDS table, and record rows carry no `| CLOSED |`. M4 mandates review records, M8 a closing review. Measured on LANDED builds: aBranchedMandate 7 such rows, aStandingWrit 2 — built as TOOL-aPromptedMandate-12
- TOOL-aPromptedMandate-9 · OPEN · nothing in the driver's precondition chain has a timeout and the slowest link is NOT the ls-remote `TOOL-aBoundedVerdict-10` suspects: measured under four concurrent bars on node a, `check-wiring.sh --check` took 1m22s at 2s user / 4s sys (I/O-bound) while ls-remote answered in 2.1s, so --preflight alone exceeds a two-minute budget
- TOOL-aPromptedMandate-10 · CLOSED · the run-gates canary reports `GATE_JOBS='0' never terminated — the clamp let it spin` under heavy machine load, then passes in isolation on a fresh TMPDIR. Its non-termination verdict is a TIMEOUT and cannot tell a spinning clamp from a starved one; the message asserts the former — built as TOOL-aPromptedMandate-13
- TOOL-aPromptedMandate-7 · OPEN · the leg's SOURCE-level no-write arm (check-unattended.test.sh) greps for `mv|rm|cp|sed -i|tee` preceded by any non-alnum, so it matches those substrings inside IDENTIFIERS: a variable named `rmode` reds it as a write. Cost me a rename; the next person pays the same. Anchor on a word boundary or on a command position
- TOOL-aPromptedMandate-1 · SPECCED · the prompt-mode declaration: `authorized-by: prompt` in the build README's front matter, read at BASE through the parse check_authorization already does, recorded as `mode:` in Run facts and second-opinioned by the leg. Measured first: the branch anchor already accepts a run-authored, pushed build folder, so no authorization code moves
- TOOL-aPromptedMandate-2 · SPECCED · RESEARCHING and TESTING join PHASES_CORE, CORE_FLOOR 10:8 -> 12:8. Core rather than PHASES_EXTRA (measured working) because the conf reserves the core vocabulary to the kit and this mode depends on it
- TOOL-aPromptedMandate-3 · SPECCED · a new BUILD-METHOD M12 states the research->test->choose loop so a directive can point at it; M1's stated budget rises 250->290 lines / 20->22 KB with the reason in place. Owner-ratified at kickoff, because M3 veto 2 makes a governance carrier's stated constraint an owner turn
- TOOL-aPromptedMandate-4 · SPECCED · `researched:M12:prompt` and `solution-tested:M12:prompt` join DIRECTIVES_CORE, introducing the layer's first SCOPED member (closed set all/prompt, absent field = all). DIRECTIVES_FLOOR 11->13; leg check 16 gains a fourth arm that must refuse an absent scope column rather than compare two empty sets
- TOOL-aPromptedMandate-5 · SPECCED · the Skill's prompt start path: orient, ONE AskUserQuestion iff under-determined, write the build folder carrying the prose and the answers, commit, PUSH, preflight, kickoff hand-back. The turn sits before the push so it is provably older than the authorization, which is stronger than a driver-side refusal
- TOOL-aPromptedMandate-6 · SPECCED · the driver-then-leg cross-component leg, closing TOOL-aStandingWrit-8. Four arms from this build's own measurements; arm 3 asserts exit 0 AND empty output, because an incomplete fixture returned a green code while other checks failed
- TOOL-aWalkedCorpus-2 · DEFERRED · a recall quality floor. Measured NOT buildable against bench.py as it exists: it always exits 0, its flag set is closed, it needs an uncommitted data dir, and verbatim.json pins it byte-for-byte. See the spec's §1 for what a successor needs
- TOOL-aDeclaredCeiling-1 · OPEN · make the template ceiling a DECLARED pin rather than a shell constant, the way .memory-tree.conf's READ_PATH_CEILING declares a byte budget with each movement justified beside the number. Recorded by TOOL-aSiftedPlaybook-1 §4 as this repo's established answer
- TOOL-aDeclaredCeiling-2 · OPEN · the recall corpus cannot reach a constraint declared in a conf file. Its root is $MEMORY_ROOT and confs sit at the repo root, so READ_PATH_CEILING was live prior art TOOL-aSiftedPlaybook-1 §10's own query could not surface. A gap in what retrieval indexes
- TOOL-aDeclaredCeiling-3 · OPEN · a slug-carrying backlog row minted after a run reaches LANDED permanently reds unattended check 8: gen_build_index derives the README's ids: as an OUTPUT, so the README moves while a terminal run's frozen copy cannot, and the driver rightly refuses to reopen it
- TOOL-aRootedPrefix-3 · OPEN · hygiene checks 6/7 measure RAW working-tree bytes, so an adopter without the eol=lf pin still gets a platform-dependent cap and entry budget — normalize before measuring, as `check-template-size.sh` already does
- TOOL-aNumeralWarden-2 · OPEN · agent-cap's enclosing-opener walk is defeated by two nested wrappers or 59 lines of distance between the `.map` and the `agent(` call; it needs a statement-level walk, not an opener count, and the 58/59 boundary is unfixtured
- TOOL-aNumeralWarden-3 · OPEN · a drift-audit pin RAISE is indistinguishable from a population drain to every gate leg; `--check` compares only `value > pin`, so `ORPHAN_ID_PIN` 4 -> 5 and `handkept` 1 -> 7 both landed unchallenged
- TOOL-aCandidStub-1 · OPEN · `tools/hooks/agent-cap.js` blesses an identifier bound from an EMPTY array literal and never re-examines it when a later statement grows it, so `const batches = []` plus a per-finding `push` fans one agent per item past hook and gate alike — reproduced at exit 0
- TOOL-aCandidStub-2 · OPEN · `tools/memory-tree/adopt-memory-tree.sh` scaffolds no `gotchas/`, and `gotchas.py` short-circuits on an empty record set, so a fresh tree gets checks 17-19 green over zero records and no catalogue — this repo's own `vacuous-selector-empty-population` class
- TOOL-aUnmannedHelm-2 · OPEN · the `ledger_dir` in `tools/drift-audit/drift_report.py` points at an authored run-state directory under `memory/project/` that hygiene check 3 forbids, so the two kits disagree about where one may live and the probe stays DECLARED_EMPTY
- TOOL-aCandidStub-4 · OPEN · `tools/memory-tree/merge-rows.py` degrades to conflict markers under `git merge` in a LINKED WORKTREE: its grammar load raises ConfError against a conf path inside the memory-recall kit dir. Run directly on the same stages it merges CLEAN
- TOOL-aSealedCaravan-1 · OPEN · `install-prefix-waivers.txt` keys each waiver on `<path>:<line>`, so any edit ABOVE a waived line unpins it and the gate reds on a merge that touched nothing it guards — hit on its first real merge. Key on the matched text or a marker comment
- TOOL-aUnmannedHelm-10 · OPEN · three root docs (`AGENTS.md`, `WIRE-INTO-PROJECT.md`, `.gitattributes`) come out of a linked worktree CRLF with no `eol=lf` pin; nothing byte-reads them today, so this is latent, but `check-wiring.sh`'s eol population is scoped to `.claude/` and will never see them
- TOOL-aStandingWrit-1 · SPECCED · the unattended run authorizes on the committed build README at BASE rather than an owner-authored mandate block, `RUN.md` becomes wholly generated, and a `--plan` verb derives the gap list from spec status headers and review records
- TOOL-aStandingWrit-2 · OPEN · SECURITY in landed code: the pinned BASE anchors on a remote-tracking ref that `unattended.sh:119-120` says needs a push to move. `git update-ref` moves it offline; reproduced with a control, a forged anchor passes preflight, the leg and close
- TOOL-aStandingWrit-5 · OPEN · `.githooks/pre-commit`'s branch guard fails CLOSED on a bogus `GOV_DEFAULT_BRANCH` but is DISABLED by an honest-looking one: setting it to the branch already checked out makes the primary-tree guard pass. Lower severity than the pre-push twin, which is now fixed
- TOOL-aStandingWrit-6 · OPEN · the leg's BASE path now OBSERVES the remote (TOOL-aBranchedMandate-3), but `GOV_DEFAULT_BRANCH` and `refs/remotes/*` still feed its OTHER consumers in the same file. Narrowed, not closed: S6 repaired one predicate's path
- TOOL-aStandingWrit-7 · OPEN · nothing binds the EXECUTING kit code to code an owner approved: a run may edit the driver, the leg or the protocol and commit them, and every parity leg compares two files that same run can change together. This bounds every property the kit asserts
- TOOL-aStandingWrit-8 · CLOSED · TOOL-aPromptedMandate-6 built `cross-component.test.sh`: the driver and THEN the leg over one tree, six arms each first performed by hand. The gap was real — writing arm 5b found that a re-preflight cannot forge a mode disagreement, because `mode:` and `base:` are both pinned once
- TOOL-aStandingWrit-9 · OPEN · `--plan`'s FORKED axis reads only §8's first non-blank line, so a spec whose first line is `none` and whose later bullets are unresolved classifies READY; `check-arms.py` cannot see this class because it is an unreached success path, not a fail branch
- TOOL-aTimedTurnstile-1 · CLOSED · `run-gates.sh` runs all 47 legs SERIALLY: warm 382.1s, cold 607.3s, real runner 335.2s. A scratchpad 8-way runner does the same 47 legs in 79.9s with `fails=0` — the heavy legs are already hermetic. The template this repo SHIPS already requires concurrent legs. CLOSED 2026-08-21 by TOOL-aScannedThrottle-1 §5: FALSE at HEAD. The pool landed and the bar runs its legs concurrently at a declared width; the row's 79.9s target sits ~11x below the current floor, measured at 812-926s on the three of four GATE_FULL bars §2 reconstructed before the sweep (per-leg records for all four are gone as of 2026-08-21)
- TOOL-aTimedTurnstile-2 · OPEN · 29 of 47 legs are SELF-TESTS holding 96.7% of wall (368.7s); the 18 checking real repo state cost 12.7s. Only leg 8 has a guard. Over the last 12 commits a records-only one drops 335s to 12.7s. Owner call: guards at the push boundary make the full bar diff-scoped. STALE 2026-08-21 by TOOL-aScannedThrottle-1 §2 — the population moved: 88 legs, not 47, and leg-seconds are 4644-5245 against this row's 368.7s wall. The guard mechanism it asks for LANDED but NOT universally — derive the split from `tools/gate-legs.json` rather than from this row: on 2026-08-21 FOUR `selftests`-chunk legs carried no guard (`template size gate selftest`, `method-carriers self-test`, `agent-cap restatement self-test`, `testsuite counts self-test`), while `govkit selfcheck` and `testsuite counts (every bar self-test prints one)` are `declarations` legs and are correctly unguarded. What survives is the RATIO, unmeasured at the current leg count. Re-derive before acting on any figure here
- TOOL-aTimedTurnstile-3 · OPEN · with legs concurrent the floor is the longest leg UNDER LOAD (~76s, merge-rows replay), not the sum — each leg dilates 1.66x at 8-way. `check-memory-hygiene.test.sh` invokes the 4.6s hygiene gate 13 times. Sharing fixture setup is the only route below ~80s. REFINED 2026-08-21 by TOOL-aScannedThrottle-1 §2 and §3.9 — the DILATION holds and the FLOOR does not. 1.66x is confirmed at 1.5-1.85x (the canary costs 317s standalone against 466-587s as a width-8 leg). But the floor is 812-926s on the three bars §2 reconstructed before the sweep (per-leg records for all four are gone as of 2026-08-21), not ~76s, and the longest leg is an `unattended` selftest rather than merge-rows replay. "Sharing fixture setup is the only route" is refuted: R2's sharding buys 27.6-30.5%. Correct the floor figure, or the row argues from a number 11x low
- TOOL-aTimedTurnstile-4 · OPEN · cold/warm is 1.59x (607.3s vs 382.1s) and every fresh worktree pays it — a new unit's first run is the cold one. Spawn tax here: `python -c pass` 103.7ms, `bash -c true` 22.5ms, `git rev-parse` 23.9ms. Defender exclusions are a no-code lever. KEPT OPEN 2026-08-21 by TOOL-aScannedThrottle-1 §6 — these spawn figures are the 2026-08-11 BASELINE that build measured against in §3.8, and R4 rests its Defender refutation on them, so they are load-bearing elsewhere and must not be edited away. UNVERIFIED at the current leg count: every bar in that sample had a warm filesystem cache, so the 1.59x cold/warm factor is carried forward untested, and R4 prices the no-code lever UNQUANTIFIED — measure with `New-MpPerformanceRecording` before excluding anything
- TOOL-aTimedTurnstile-6 · OPEN · two classes want `memory/gotchas/` records: a poll tick is a PROCESS here (75ms per 50ms sleep, 317s of a 617s run), and `jobs -rp` counts only RUNNING jobs so a just-forked worker reads absent. Each needs an INDEX re-render plus a dossier claim. KEPT OPEN 2026-08-21 by TOOL-aScannedThrottle-1 §5's extension — NOT re-measured by that build, and named here so the omission is a recorded answer rather than a silent one. Its 617s run predates the 88-leg bar. The poll-tick mechanism is independently corroborated at §3.6, which counts up to ~480 process creations per nested canary run from the same shape
- TOOL-aTimedTurnstile-8 · OPEN · review aTimedTurnstile-1 gates A3/B2, one grep leg each: no `*.test.sh` may compare a `_ms` variable to a hardcoded literal without a same-run baseline, and no `[ "$VAR" -lt` on env input without an arithmetic clamp. Both are classes this build created
- TOOL-aTimedTurnstile-7 · OPEN · review aTimedTurnstile-1 gate A2: `check-arms.py` forbids absence-only arms yet cannot see this class — its population needs a `fail() {` helper and excludes `*.test.sh`, which is why F1, F4 and F5 shipped together. Needs its own spec
- TOOL-aSealedCaravan-2 · OPEN · `check-arms.py` scans tracked `*.sh` only, so no Python write path is armed — `govkit.py` becomes the repo's strongest new one and its least armed. Extending it is a governance-carrier change and an owner turn. Raised out of DEPL-aSealedCaravan-2 F3
- TOOL-aSealedCaravan-4 · OPEN · the memory-tree renders are install-prefix-correct but NOT MEMORY_ROOT-correct — at a renamed root the rendered HYGIENE and BUILD-METHOD still name `memory/` throughout, and the templates say so in prose instead of substituting it
- TOOL-aSealedCaravan-5 · OPEN · the row-keyed merge driver is INERT in a linked worktree: an absolute `GIT_DIR` makes `recall_conf`'s `git -C <kit> rev-parse --show-toplevel` return the KIT dir, so the conf lookup refuses. Hit live; the e2e arm's fixture is a normal repo and cannot see it
- TOOL-cFinalBerth-4 · OPEN · the three named refusals `aMooredAnchor` S4 scoped for check 9's silent exits never landed; its rev-4 carried-forward list omits S4 and the spec closed. With no `origin/HEAD` the block is skipped in silence, and check 15's ancestry half inherits it
- TOOL-cFinalBerth-5 · OPEN · the run-gates canary's `par*2 < ser` ratio FLIPS run to run on one tree: red then GREEN on two consecutive full bars at 550d9b6, no edit between; the red missed by 4.5% (7705x2 vs 14753). Its fixtures are fixed sleeps, so the margin is overhead-dependent by construction. NOTED 2026-08-21 by TOOL-aScannedThrottle-1 §5: did NOT reproduce in that build's four-bar sample. The canary's between-run spread there was 25.9% across comparable bars, which is consistent with this row's mechanism and is not an independent observation of it
- TOOL-cTracedPromise-2 · OPEN · the §6 acceptance-witness rule, deferred from cTracedPromise on cost: require each AC bullet to name a machine-checkable witness. Candidates measured at 123 of 359 AC bullets across 33 CLOSED specs
- TOOL-cTracedPromise-3 · OPEN · tighten `closed_specs_with_no_product_commit` to an id-only key, the unit-level claim. Blocked on id-in-subject being universal: today it misses 3 of 13 against slug-or-id's 1
- TOOL-cTracedPromise-4 · OPEN · `check-memory-hygiene.test.sh` printed an AUTHORED assertion count, stuck at 130 while arms landed under it. The numeral is deleted; derive one from the `hit`/`miss` and inline `st=1` arms instead
- TOOL-cTracedPromise-5 · OPEN · an adopter who fills `TRACE_CUTOFF` before closing any spec after it reds `drift-audit records` with DEAD PROBE — the hole pushes them straight into it. Document `DECLARED_EMPTY` as the interim state
- TOOL-cTracedPromise-6 · OPEN · hygiene check 10 resolves a rotated index's live counterpart by fixed path, so it is blind to every `memory/backlog/*.md` shard; a 48-row rotation went unnoticed. Resolve it anywhere under the memory root
- TOOL-cTracedPromise-7 · OPEN · the unset-`TRACE_CUTOFF` row prints `DEAD PROBE` though `--check` ignores it: the status ladder reads `live` and never `gateable`. Print the note instead, so `not asked` and `blind` stop reading alike
- TOOL-cBriefedPilot-7 · OPEN · the `build-complete` DoD item: `--close` blocks while a roster unit is unbuilt. D8's "land only when fully done" had no checker
- TOOL-cBriefedPilot-8 · OPEN · the `closing-review-recorded` DoD item: a tracked review under this build names the run's pinned BASE
- TOOL-cBriefedPilot-13 · OPEN · leg check 17 grades every parked waiver line for a declared handle and a non-empty reason; shape only, and P1 is why
- TOOL-cBriefedPilot-14 · OPEN · leg check 18 asserts the Skill names kickoff AFTER preflight — order, not presence
- TOOL-cBriefedPilot-22 · OPEN · check 16 arm A's join extended to the protocol's own §3 phase list and §4 DoD table, which are joined to PHASES_CORE and DOD_CORE by no leg today
- TOOL-dClosedLexicon-1 · SPECCED · an opt-in lexicon kit gating three declared naming predicates, with the seed table derived-then-frozen, coverage modes per language, and the pin-direction guard CUT on two independent defects → `builds/dClosedLexicon/`
- TOOL-dClosedLexicon-2 · SPECCED · wires the verb table into the codebase-map ratchet and two drift-audit signals; adds no new gate leg, and records that the ratchet DELETION direction is the load-bearing half → `builds/dClosedLexicon/`
- TOOL-dClosedLexicon-14 · OPEN · govkit `check` printed `playbook: landed-but-inert` and exited 0 over a target where the playbook was never installed: the hole's `! grep -qE …` over two ABSENT files turns grep's rc 2 into success. Green-by-absence in a verb whose whole job is verifying
- TOOL-dClosedLexicon-15 · OPEN · the `playbook` entry declares both files `project-owned`, which is not landable, while its own `why_no_adopter` says "installation is a copy to an owner-chosen path" — so the DEFAULT selection installs no playbook. One of the two is wrong; OWNER-facing
- TOOL-aBranchedMandate-5 · OPEN · `adopt-drift-audit.sh` diffs its render with no `[ -s ]` test, so empty-vs-empty PASSES; both sibling adopters guard it
- TOOL-aBranchedMandate-6 · CLOSED · `memory-recall` selftest cleanup cannot remove read-only git objects on Windows; 3,616 leaked scratch repos, re-measured at 4,905 on 2026-08-20 and 71% of this node's whole `%TEMP%`. `cleanup()` now clears the read-only bit and retries, and a final arm FAILS on any survivor — staged red first at 29 of 32 surviving — built as TOOL-aTetheredScratch-2
- TOOL-aTetheredScratch-4 · OPEN · A TEST FIXTURE COMMITTED INTO THE LIVE REPO. Two commits authored `t <t@e>` titled `init` landed on branch/temp-files-user-root-3e6e22 at 2026-08-20 03:40:27 and 03:43:57, each carrying the session's whole working tree; squashed away, tree byte-identical, nothing lost. `tools/check-wiring.test.sh` line 90 is the only line in the tree combining an UNGUARDED `cd "$D"` with `git commit -q --allow-empty -m init`, and `--allow-empty` commits a POPULATED index (proven in a scratch repo) rather than an empty one — so a failed `cd` lands the caller's staged work as a fixture commit. Fix is `cd "$D" || exit 2` on :87 and :90. LOOSE END: that line also runs `git config user.email t@e` first, and neither the worktree nor the common config carries it, so the reconstruction is not complete — settle that before closing · 2026-08-20 the two-line guard LANDED: `cd "$D" || exit 2` on :87 and :90, which is the idiom line 14 already used. Reproduced first in a scratch repo — UNGUARDED the caller gains a commit authored `t <t@e>` AND its local user.email is overwritten to t@e; GUARDED the subshell exits 2, the caller keeps 0 commits and its own identity. The LOOSE END is still open and is now sharper, not settled: no `t <t@e>` commit is reachable from main, and 5c83c18 at 04:02 — 19 minutes after the accident — is authored egor@compeeto.com, so the identity write did NOT persist. A failed `cd` at :90 would have persisted it, so the reconstruction still needs a mechanism that leaves no config trace
- TOOL-aTetheredScratch-3 · OPEN · `tools/memory-recall/selftest.py` leaves 2 EMPTY `tmp.*` dirs per run that never route through `cleanup()`, so the survivor arm added by TOOL-aTetheredScratch-2 cannot see them. Measured on a controlled TMPDIR: 32 swept, 2 unswept. Three orders of magnitude smaller than the leak just closed, and a different mechanism — find who creates them before widening the arm. ADJUDICATED 2026-08-21 by TOOL-aScannedThrottle-1 §5.1 as IN SCOPE BUT IMMATERIAL to the bar's wall clock: 2 empty dirs per run of one selftest, against the 1053 populated scratch repos `TOOL-aMeteredTurnstile-2` measures, so it moves no measurable span. Recorded rather than skipped; a first reading excluded it for naming the scratchpad directory, which its text does not say
- TOOL-aBranchedMandate-8 · OPEN · `--preflight` OVERWRITES a live non-terminal run-state file, losing the keepalive id and re-pinning the anchor
- TOOL-aBranchedMandate-9 · OPEN · `--preflight`'s rotation and scaffold run BEFORE checks that can return 1, stranding the record with no repair verb
- TOOL-aBranchedMandate-10 · OPEN · `map_extractors.template.py` unions the two JS scans with a bare `+`, contradicting its own dedupe line above
- TOOL-aBranchedMandate-11 · OPEN · `govkit.py`'s `blocked` skip reason is unreachable behind `cmd_apply`'s early merged-rule refusal
- TOOL-cBriefedPilot-23 · OPEN · a newline in an `--override` or `--abort` reason forges a second parked line, because `park()` writes it verbatim into a region with a line grammar
- TOOL-cBriefedPilot-24 · OPEN · `--plan` cannot see a unit BUILT on the run's branch but not landed: specs stay OPEN until the build lands (measured — cKeyedLaunchpad has 7 built units, all OPEN), so `next:
- TOOL-cBriefedPilot-25 · OPEN · only `--preflight` re-splices RUN.md's generated region and it refuses a dirty tree, so a mid-run edit to the build README strands the copy until the tree is clean and the keepalive id is to hand
- TOOL-cBriefedPilot-26 · OPEN · the driver's remote observation is environment-sensitive: the anchor arm passes under `bash unattended.test.sh` and FAILS under a python subprocess, measured 2026-08-15
- TOOL-cBriefedPilot-27 · OPEN · `check-arms` certifies an assertion EXISTS in a test file; the suite certifies which lines RUN. Neither notices arms appended past the summary — unit 12 landed nine that never executed, and both gates were green
- TOOL-cBriefedPilot-28 · OPEN · the parallelism routes are re-openable on evidence: R2+R5 needs E3 and E4 RUN (they never were); R3 needs the CLI on PATH; R1 needs a budget key without a prompt id
- TOOL-cBriefedPilot-29 · OPEN · M7 re-reads BUILD-METHOD.md WHOLE at every pass boundary and nothing counts that RECURRING cost. Check 16 measures the path ONCE; the per-boundary re-read is invisible to it. Figures deliberately not restated here — read them from `corpus_ids.py --report`, since both the ones this row used to carry went stale
- TOOL-cSettledDocket-7 · OPEN · `tools/memory-recall/recall-opened.test.sh` is tracked and NO gate leg runs it, so `check-testsuite-counts` cannot see it. A suite nobody runs is one nobody notices going quiet — wire it or record why not
- TOOL-cSettledDocket-8 · OPEN · `DIRECTIVES_EXTRA_TABLE` has one reader, check 16. Unit 2's 'shown to the agent' half is unmet: the rendered Skill does not point at the project's table, so a declared extra is joined but never displayed
- TOOL-cSettledDocket-9 · OPEN · a terminal spec whose Open-questions section is present but EMPTY stays silent: the `q == 0` refusal fires only when the range never opens. Present-and-empty still reads as resolved
- TOOL-cSettledDocket-10 · OPEN · `check-testsuite-counts` says 'every bar self-test' and grades only `*.test.sh`; eleven python self-tests on the manifest are out of population. Widen the selector or narrow the sentence
- TOOL-cSettledDocket-11 · OPEN · check 8 exempts a TERMINAL record by clearing `rd`, skipping the malformed-MARKER refusal as well as the emptiness one — the over-wide scoping cBriefedPilot-36 shipped. Scope it to emptiness alone
- TOOL-cSettledDocket-12 · OPEN · `tools/lexicon/selftest.py` failed the bar on a `shutil.rmtree` traceback and passed on re-run — a Windows temp-dir race, not a defect
- TOOL-cSettledDocket-13 · OPEN · `unattended.test.sh` loops the verb set across three carriers, all inside the driver, so a verb no agent is told about is invisible to it — that gap shipped `--park` as a blocker
- TOOL-cSettledDocket-14 · OPEN · the same gate on the conf axis: every key the engine READS must appear in the protocol's key table and the shipped conf example. `DIRECTIVES_EXTRA_TABLE` landed with one reader and no catalogue entry
- TOOL-cSettledDocket-15 · OPEN · citing a non-terminal spec id from product source has red the bar three times; the remedy (paraphrase, never cite) is recorded and nothing enforces it
- TOOL-cSettledDocket-16 · CLOSED · the TOOL index was at its 20480-byte cap with nothing terminal left to rotate, so new rows were paid for by shortening old ones. Closed by TOOL-aRelaxedShard-1: the bound is now a declared 61440 and the index sits at 33%
- TOOL-aTetheredConvoy-1 · OPEN · the playbook template reached origin/main carrying UNRESOLVED conflict markers, and both gates over it pass with the markers present; `git diff --check` caught it, and that is not a leg. Wire a marker scan over the shipped product surface
- TOOL-aBoundedVerdict-6 · OPEN · the run-state authored region's 8 KB spill rule becomes load-bearing once `--park` makes parking cheap: parks are rare hand-edits today, so the spill has never fired, and crossing the cap mid-flight reds the bar and blocks `--close`
- TOOL-aBoundedVerdict-7 · OPEN · nothing records inter-unit dependencies; a build README's authored order carries them in prose, so `TOOL-aBoundedVerdict-3` S3's every-remaining-unit-depends-on-it test is the run's judgment. A front-matter dependency field would make it derivable — its own unit
- TOOL-aBoundedVerdict-32 · OPEN · `TOOL-cBriefedPilot-18` AC9 was CLAIMED at a close it was not met at. It reads "§1's roster bullet in both halves no longer reads `Opt-in by presence`, grepped and found zero"; at that spec's own base the phrase was present once in each half of the protocol pair, and `git log -S` over either half returned exactly one commit — `6200674`, which ADDED it, five days before that spec closed. It was made true later and by another unit: `fd8f6fa` removed the phrase as `TOOL-aBoundedVerdict-11`'s landing. Recorded rather than repaired — the spec is CLOSED and on another node, and a claim that was false when written is not fixed by a later commit making it true. What this row is FOR: nothing gates an acceptance criterion against the tree at close time, which is the class, and it is why the audit found three defects in a document two parity legs call green
- TOOL-aBoundedVerdict-8 · OPEN · no leg asserts a review subject AT the cap with a non-clean verdict was actually DISPOSED of, so `TOOL-aBoundedVerdict-1` S8 is advisory. The check spans three units' mechanisms (cap, park verb, halt code); write it once all three have landed, not before
- TOOL-aBoundedVerdict-9 · OPEN · hygiene check 10 is INERT for backlog archives: it derives the live index as stem-dot-md at the MEMORY ROOT, but a backlog index lives one level down, so the derived path never exists and the `[ -f ]` guard skips it. No backlog rotation reference is checked
- TOOL-aBoundedVerdict-10 · OPEN · `unattended driver selftest` HANGS on node `a` inside its first `--preflight` (traced), zero output at 240s. It wedges the WHOLE bar at 46/65: `run-gates.sh` has no per-leg deadline, and the driver's `ls-remote` anchor call has no timeout either. Two fixes. ANCHOR HALF DONE (2026-08-20, aBoundedVerdict) — the ANCHOR half is DONE (2026-08-20): every remote observation in the driver and the leg now goes through one bounded helper, file-captured because a command substitution around `timeout` bounds the verdict and not the clock. The PER-LEG DEADLINE half is not, and stays open here; it belongs to `run-gates.sh`, which is a different kit and a different owner. Note while resolving: a sibling row measured `check-wiring.sh --check` at 1m22s against `ls-remote` at 2.1s under load, so the anchor call was never the slowest link in `--preflight` — bounding it removes an unbounded WAIT, not the latency || AND SEPARATELY, NOT REPRODUCED 2026-08-21 by TOOL-aScannedThrottle-1 §5.1 — that leg COMPLETED in all three bars §2 reconstructed before the sweep, at 812.1 / 925.5 / 887.9s, so the 240-second symptom did not recur. Per-leg OUTPUT timing was never reconstructed and the traced `--preflight` hang is unaddressed, so this is not a refutation: the row stays OPEN on its own terms, and its no-per-leg-deadline clause is untouched
- TOOL-aTetheredRecord-1 · CLOSED · mint the five missing spec H1 ids; the orphan waiver drains empty and `ORPHAN_ID_PIN` falls 5 -> 0 → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-2 · CLOSED · the record-to-spec binding grammar and its read-only parser; it classifies and never raises → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-3 · CLOSED · the retrofit: every record gains a binding line, by a precedence in which the filename is not evidence → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-4 · CLOSED · check 21 — four branches over five armed fail sites; the filename projects the header and cannot drift → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-5 · CLOSED · the generated region renders each record under the ids it serves, plus the two coverage joins → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-6 · CLOSED · the adopter path: their first run is RED, and the migration is ordered — mechanical, then measured → `builds/aTetheredRecord/`
- TOOL-aTetheredRecord-7 · CLOSED · the rename: every record filename names a spec via a REDEFINED ordinal, with no grammar change → `builds/aTetheredRecord/`
- TOOL-aRelaxedShard-2 · OPEN · check 6's build-README class (25,600 B, `cl=0`) has NO byte-axis arm: `tRunOk/README.md` and `tRunBig/README.md` are front-matter stubs and nothing sizes one past the cap, so a class whose only bound is bytes is unarmed. Found regrounding TOOL-aRelaxedShard-1
- TOOL-aRelaxedShard-3 · CLOSED by deletion (TOOL-dSpentCeiling-1) · the complaint was that a byte budget over a file whose length tracks OPEN-BUILD COUNT measures parallelism. It did, and the budget is gone rather than corrected: `memory/LIVE.md` is still generated and still tracks that count, but nothing sums it any more. Measured while closing: over the seven days to 2026-08-25 `LIVE.md` SHRANK 303 B while the read path grew 43460 B, so the parallelism term was real but was never the driver — two kit-rendered guides supplied 64.9% of that growth
- TOOL-aWiredReckoning-1 · OPEN · govkit's 45 new defs skip the verb table; VERB_OFFENDER_PIN raised 417->463. Curate them, drop it
- TOOL-aWalkedCorpus-6 · OPEN · govkit has no rule kind meaning 'in the kit dir, deliberately NOT in the payload'. project-owned drops it from the ** pool but derives an ORDER telling the adopter to supply a file gov withholds
- TOOL-aWalkedCorpus-10 · OPEN · codebase-map's `seeded-extractors-or-crashed` outcome stays unmarked because its NAME admits it cannot tell a seed from a crash. Split it in two so the seeding half can be an accepted stop, or rename it to what it detects
- TOOL-aWalkedCorpus-5 · OPEN · govkit selfcheck partitions gate-leg guard pathspecs into four PREFIX classes and a repo-root file (.memory-tree.conf) lands in none, so a leg cannot guard on a root conf. Hit wiring the recall floor; the guard was narrowed instead
- TOOL-aWalkedCorpus-4 · OPEN · memory-recall selftest.py mints its two synthetic corpus ids in a LIVE family, so any record quoting them trips hygiene check 14 as orphans. Sibling fixtures use a dead family. Hit twice in one hour: a review record, then the row recording it
- TOOL-aLoosenedCeiling-5 · OPEN · `tools/install-prefix-waivers.txt` keys on `<path>:<line>`, so any insertion above a waived hit unpins it silently. TOOL-aLoosenedCeiling unpinned the same two rows TWICE in one build. The lexicon's waivers key on the matched TEXT for exactly this reason
- TOOL-aLoosenedCeiling-6 · CLOSED · `tools/workflows/tier2-review.js` writes a `**Serves:**` line with no kind token, so hygiene check 21 reds on every closing review record until it is hand-edited. M8 already makes the agent rename the file; the kind belongs in the harness. CLOSED by TOOL-aBoundedVerdict-14 S7: the synth prompt now instructs a diff-review binding line carrying an id, because a kind alone is malformed under the grammar
- TOOL-aBranchedMandate-12 · CLOSED · `--close` discards `$GATE_CMD`'s output, so a blocked close names no leg
- TOOL-aBranchedMandate-13 · CLOSED · `build-complete` reports a bare unmet when the README has no roster region
- TOOL-aPacedTurnstile-8 · CLOSED · shard `unattended driver selftest` (659.9s) and `unattended gate selftest` (634.6s): together 32% of all leg time, and the first alone is 75.6% of the 873s wall, so the bar's floor is ~660s at any width on any hardware. Every scheduling mechanism in aPacedTurnstile competes for the remaining 213s. Measured on node a at 6517579. RE-STAMPED 2026-08-21 by TOOL-aScannedThrottle-1 §4 R2, and FIRST BY MEASURED SPAN of everything that build ranked — a claim of this ranking, not a state the backlog can hold, which has no priority field. RETRACT this row's "~660s at any width" floor and the "remaining 213s" hung off it: measured at 812-926s on the three bars §2 reconstructed. It is the ONE change that moves the span: sharding both buys 292s (27.6%) on one measured bar and 282s (30.5%) on another. Three numbers decide how. The driver alone buys 38.9s (3.7%), because the gate selftest just becomes the new floor, so BOTH must move together or the work is wasted. TWO shards each is sufficient: past that the bar is throughput-bound at ~766s and further splitting buys exactly zero, so target that crossover and not a floor number. Each split adds ~2.75s of leg-seconds for one fresh scratch repo. CLOSED 2026-08-21 by TOOL-aShardedFloor-2 and -3, which sharded BOTH into two by an argument contract rather than a file split. The bar is now THROUGHPUT-bound: longest leg 336s against leg-seconds/width 386s
- TOOL-aPacedTurnstile-9 · OPEN · prove each leg `guard` COMPLETE, the sound fix TOOL-aPacedTurnstile-7 defers: run a leg with everything OUTSIDE its guard perturbed and assert the verdict does not move. Today the canary checks only that a guard RESOLVES to a tracked path, so under-declaration is unchecked — and once the push boundary is diff-scoped a too-narrow guard costs a wrong merge verdict rather than a late signal
- TOOL-aPacedTurnstile-10 · OPEN · the row-keyed merge driver FAILS on every real `git merge` and its gate cannot see it. Git exports `GIT_DIR` to a merge driver; under it `git -C <kitdir> rev-parse --show-toplevel` returns the `-C` dir itself, so `recall_conf.repo_root()` resolves to `tools/memory-recall/`, `load_conf` finds no `.memory-tree.conf` there and `resolve()` refuses. Hit on this build's reground of `memory/backlog/TOOL.md`; reproduced with one variable — `GIT_DIR=$(git rev-parse --absolute-git-dir)` alone flips `repo_root()` from the repo root to the kit dir. It fails CLOSED (conflict, not silent take-ours), which is `TOOL-aMendedLedger-8`'s property holding, so the cost is a hand-resolve rather than a wrong merge. `merge-rows.test.sh` is green because it calls the driver directly (`DRV="bash tools/lib/pyrun.sh …merge-rows.py"`) and never through a configured `git merge`, so git's own invocation environment is absent from all 46 of its cases. Two halves: anchor the root on `__file__` rather than shelling out to `rev-parse`, and add one case that drives a real `git merge` with `merge.rows.driver` wired
- TOOL-aDeclaredBound-6 · OPEN · `tools/workflows/tier2-review.js` describes its own find phase twice and the two disagree: line 7 says `≤5 concurrent`, line 128 says `ONE ≤6-wide wave`, and the code fans at 5. Stale prose in the harness the BINDING protocol points at
- TOOL-aPacedTurnstile-11 · OPEN · govkit's selfcheck cannot see that an entry SPELLING another entry's command must hard-depend on it. `.githooks/pre-push` invokes the merge-bar runner, but a `requires` edge only ORDERS a selection - it pulls no missing kit in - so a target can receive the merge-bar hook without the merge bar and the deployer reports success. Deferred out of `TOOL-aPacedTurnstile-1` by that spec's section 8 fork B, which ships the default-selection line instead: adding the arm there would change govkit's contract inside a unit that only moves the runner. The arm belongs beside the other cross-entry assertions in `tools/govkit/selftest.py`, keyed on the command string an entry declares
- TOOL-aPacedTurnstile-12 · OPEN · govkit's selfcheck joins a descriptor's `[[gate_leg]]` rows to the repo's `tools/gate-legs.json` by NAME only and never compares the two declared GUARDS, while `govkit.py`'s emit verb copies a descriptor's guard verbatim into a target's manifest. So a leg can be guarded correctly in gov and narrowly in the kit that ships it, and every gate stays green: the run-gates canary checks only that a guard RESOLVES to a tracked path. Found as a live instance in `TOOL-aPacedTurnstile-7` round 3 (T10) - the kit/dogfood parity leg, whose gov row `-7` S10 widens to `memory/guides/` and whose `tools/memory-tree/kit.toml` row would have stayed narrow with nothing able to observe it. `-7` AC9b now arms the descriptor side inside the memory-tree kit's own suite, which closes THIS leg; the general join is here. Narrower than `TOOL-aPacedTurnstile-9`, which defers proving a guard COMPLETE - this only asks that two declarations of one leg's guard agree
- TOOL-aPacedTurnstile-13 · OPEN · the fingerprint helper `TOOL-aPacedTurnstile-5` S5 specifies is empty-on-any-failure, which makes an EQUALITY join fail OPEN rather than closed. Empty is right for the REUSE consumer (`TOOL-aPacedTurnstile-6` reads it as 'no reuse') and wrong for the equality consumer: `TOOL-aPacedTurnstile-7` predicate 0 compares a stored digest against a fresh one, and empty == empty reports AGREEMENT, so a doubly-failed fingerprint SCOPES the push boundary instead of forcing it. One sentinel serving two consumers whose safe directions are opposite. Raised and REFUTED as a finding against the round-4 fold, correctly - it is a pre-existing property of the sentinel, and S7's failed-nothing precondition blocks the write side, so realising it needs a run that failed at both ends AND a record written from it. Filed rather than dropped: the fix is a distinguishable failure token rather than empty, or predicate 0 treating an empty value on either side as a force. Recorded under Refuted in the round-4 review under `memory/builds/aPacedTurnstile/reviews/`
- TOOL-aPacedTurnstile-14 · OPEN · `memory/builds/aPacedTurnstile/README.md` carries NO roster marker pair, so the roster is unreadable to every verb that wants it: `--plan` silently falls back to "tracked specs under spec/" (which cannot see a planned unit nobody has specced - the exact case that join exists to report), and the `build-complete` DoD item blocks with "no well-formed roster marker pair" rather than with a unit list. Found at --close on the aPacedTurnstile run. NOT fixed there deliberately: editing the authorization artifact at the moment a gate blocks on it is the wrong instinct, and it would not have unblocked anything anyway, since six units are OPEN and the item stays unmet either way. The fix is to wrap the README's authored Units table in the roster marker pair - and worth checking the other build READMEs in the corpus for the same gap, since nothing gates the markers' PRESENCE, only their well-formedness once present
- TOOL-aPacedTurnstile-15 · OPEN · the unattended kit has no check that the PRIMARY tree is landable, and `tools/push-main.sh` is the only permitted lander but refuses everywhere except there. Hit at the close of the aPacedTurnstile run: the primary tree's `main` was clean and on main - so push-main would have SUCCEEDED - while carrying 11 unpushed commits, three of them a different build entirely - the aDeclaredBound spec set, mid-flight, its round-1 audit BLOCKED on 33 findings (its ids are deliberately NOT cited here: they are defined on the primary tree's main and not in this branch, so a citation would be an orphan, which is itself a small proof of the coupling this row is about). Landing would have pushed that build's unreviewed work to the remote under aPacedTurnstile's mandate, irreversibly, with every gate green and no verb saying a word. The `gates-green` DoD item runs the bar on the RUN's tip, not on what the landing push would actually carry, and `--landed` only observes ancestry AFTER the fact. Two halves worth building: a preflight or close-time check that the primary tree's HEAD is an ancestor of the run's own tip (or is at origin/main), naming any commit the landing would carry that the run did not make; and a decision about whether the lander should be runnable from a linked worktree at all, since forcing every landing through one shared tree is what couples independent builds' landings together in the first place
- TOOL-aFusedCharter-4 · OPEN · the unattended kit deadlocks when several builds land on one trunk. `--landed` refuses before the push (check 32: HEAD not an ancestor of the advertised tip); the push refuses while more than one run-state is non-terminal (check 7); and check 15 correctly refuses a hand-written LANDED whose witness is not on the remote. Three builds at LANDING therefore cannot be resolved by any in-band verb — measured 2026-08-19 with aDeclaredBound, aPromptedMandate and aFusedCharter, resolved only by marking two of them ABORTED. The checks are each right; the gap is that nothing lets a run reach LANDED as part of the same push that makes it true. → memory/builds/aFusedCharter/
- TOOL-aBoundedVerdict-20 · OPEN · the aBoundedVerdict spec audit's UNFOLDED residue: 6 high clusters (AC8's spelled-numeral grep, the missing ACs for -2's S10, the fork marks four specs claim and do not carry, the plan-verb-vs-classification-table FORKED disagreement, -18's F1 resolved against an open row saying the opposite, and -13's half of TOOL-aBoundedVerdict-10) plus 31 medium and 10 low findings. Source on disk: `memory/builds/aBoundedVerdict/reviews/2026-08-19-review-TOOL-aBoundedVerdict-1-2.md`
- TOOL-aBoundedVerdict-29 · OPEN · `check-verdict-epoch.sh`'s remedy names THREE files to bump; the kit has FIVE marker sites, so following it exactly leaves `check-kit-versions.sh` red on BUILD-METHOD and SPEC-TEMPLATE. Two gates disagree about one kit's marker population. GENERALIZES: drift-audit also has five sites (constant, README, two workflow `meta.version`, two `gov:kit` markers) and a 1.4->1.5 bump took THREE `check-kit-versions.sh` rounds to settle. The remedy should be DERIVED from `kit.toml`, not enumerated in prose
- TOOL-aBoundedVerdict-30 · CLOSED · drift-audit's `non_terminal_specs_cited_by_product_source` read 10 against pin 2. NOT the deadlock the row claimed: 3 hits were a PREFIX bug (`-F` with no `-w`, so `-1` matched inside `-11`), 2 were shipped source naming UNBUILT ids, and 5 were units genuinely CLOSED by the landing. Fixed at the oracle plus a red-proved sibling arm; kit 1.5
- TOOL-aBoundedVerdict-31 · OPEN · every `TemporaryDirectory()` in the kits can red the bar on WINDOWS for a reason unrelated to what it grades: a held handle at block exit raises PermissionError [WinError 32] AFTER the arms pass. Blocked a push whose 80 arms were green. Fixed in `tools/lexicon/selftest.py` only; 9+ other files carry the same pattern and none tolerate cleanup errors
- TOOL-aBoundedVerdict-28 · OPEN · `BUILD-METHOD.md` is at 249 lines against its own ≤250 self-declared cap after -14 S5, so the next unit that touches M8 must DISPLACE rather than add. M1 states the rule and no gate reads it — the hygiene cap for a guide is 750 lines, ten times looser
- TOOL-aBoundedVerdict-27 · OPEN · `unattended.test.sh`'s 344 assertions share ONE mutable fixture tree with undeclared coupling: an arm placed between coupled arms, or above the helpers it calls, fails indistinguishably from a wrong predicate. Cost four passes in one unit
- TOOL-aBoundedVerdict-26 · OPEN · `run-gates.sh | tail -N` returns TAIL's exit status, so the idiom that makes the bar readable discards the only thing it is for. Reported a RED bar as exit 0 during this run — the same class as the close path's discarded refusals, one altitude up
- TOOL-aBoundedVerdict-25 · OPEN · `check-unattended.sh` carries three RAW CR bytes inside CR-stripping awk programs, and any text-mode reader turns a lone CR into a newline, splitting those programs silently. Hit while appending a check: it broke check 18, which that edit never touched
- TOOL-aBoundedVerdict-24 · OPEN · a run that CLOSES but cannot LAND blocks every later run's bar. LANDING is not in PHASES_TERMINAL, so the record stays live and leg check 7 counts it forever — aPacedTurnstile parked there and now reds the bar for aBoundedVerdict
- TOOL-aBoundedVerdict-22 · OPEN · the resolver ban in `unattended.test.sh` greps `(python3?|py) `, which matches a bare `.py ` FILENAME before a flag and not just an unresolved launcher, so a refusal's remedy naming a script trips it. Worked around by putting the name last; narrow the regex
- TOOL-aBoundedVerdict-23 · OPEN · `--status`/`--resume` pick the next unit as the first non-terminal row in RENDER order (id-sorted), not the roster's DEPENDENCY order, so a resuming run can be sent to the wrong unit. Observed: reported `-1` where the roster puts `-11` first
- TOOL-aScannedThrottle-9 · OPEN · LEFT-SHIFT from TOOL-aScannedThrottle-1's closing review: eight of eighteen round-2 defects were ONE class — a review finding naming three carriers of a fact, fixed at one, which leaves the copies in open disagreement where before they were wrong together. Recorded as the acute-form section of `memory/gotchas/two-answers-to-one-question.md` - it is that class drifting DURING the edit meant to converge it - because no gate for it exists. The mechanical form is a `multi-site-fix-parity` leg: for a commit claiming to resolve a review finding, extract the file:line sites that record names and red when the commit touches some and not others. Its failing case has NOT been observed, so it is a row and not a leg
- TOOL-aScannedThrottle-10 · OPEN · LEFT-SHIFT, four gates that review proposed, none built and none seen RED, so each needs its failing case observed before it is wired: `disposition-parity`, `figure-carriers`, `derived-count-in-prose`, `census-emits-ids`. Each is specified in the Left-shift section of `memory/builds/aScannedThrottle/reviews/2026-08-21-review-TOOL-aScannedThrottle-1-diff-2.md` and is NOT restated here. The one worth knowing without opening it: `disposition-parity` must assert in BOTH directions, because the one-directional version certifies the report and misses the row, which is how F1 and D1 both landed
- TOOL-aScannedThrottle-11 · OPEN · LEFT-SHIFT, three classes both review rounds named UNGATEABLE, for the §10 checklist rather than a leg: an adjudication that EXCLUDES a record quotes that record's own words; a disposition VERB covers every clause the cell quotes, or the cell quotes only the clause it dispositions; a corroborating derivation is INDEPENDENT of the value it corroborates. The instance behind each is in `memory/builds/aScannedThrottle/reviews/2026-08-21-review-TOOL-aScannedThrottle-1-diff-2.md`
- TOOL-aShardedFloor-1 · CLOSED · `run-gates evidence` was UNRUNNABLE inside a bar whose parent exports GATE_BASE, which `.githooks/pre-push` does: `rec_run` passed the ambient env into the nested runner it grades, which resolved a sha its scratch lacks, ran everything and skipped nothing, so the arm whose subject IS a skipped leg refused. Latent. FIXED: `rec_run` clears the GATE_* vars. Class `memory/gotchas/inputs-inside-the-subjects-reach.md`
- TOOL-aScannedThrottle-8 · OPEN · RE-MINTED from `-1` 2026-08-21: that seq is the build UNIT's, and one id naming two unrelated subjects resolves to neither. The dispatch hint is stored PER-WORKTREE and was renamed `gate-timings.tsv` -> `gate-ledger.tsv` without migrating: measured 2026-08-20 at 24 of 26 worktrees with no ledger — re-derive with `git worktree list` before pricing, the population MOVES and the cleanup at 49aea26 took it to 6 the same night — and an absent cache scores every leg 0.0 so `sorted` returns MANIFEST order, silently. Controlled pair in one worktree, hint absent then present: floor leg dispatch rank 55/87 -> 1/87, start +157.9s -> +0.0s, span 970.0s -> 925.5s, packing 0.84 -> 1.00 — and the second run's floor leg was 14% SLOWER. Costs 15.6-16.3% of span on every cold worktree, which is where every new unit starts. Read the ledger from the git COMMON dir with a per-worktree fallback, fall back to the legacy filename on read, and SAY when the hint is missing. Ship it with a reserved short-leg slot or it makes time-to-first-signal 131x worse (TOOL-aMeteredTurnstile-5). RE-PRICED 2026-08-21 at 38.5s / 8.9%, NOT the 15.6-16.3% above: sharding the selftests made the bar throughput-bound, and this row's whole mechanism is starting the FLOOR leg earlier. Controlled pair in memory/builds/aShardedFloor/build/. Price from 8.9%
- TOOL-aScannedThrottle-2 · CLOSED · the turnstile queue wait is printed to stdout at `run-gates.sh:518` and recorded NOWHERE: `$gd/gate-queue-status` is deleted at :505 when the wait ends, the run-record header has 19 keys and none is the wait, and `gate-last-summary.txt` carries only profile/chunks/verdict. A lander queueing behind one peer experiences ~31-35 min where every measurable span reads ~16. One `queued` key at :708-737; `profile_bar.py` already reasons about queue time and cannot see it. CLOSED 2026-08-21 by TOOL-aShardedFloor-1: paired `queued`/`queued_from` header keys plus a summary line, zero new stdout bytes, and a dash rather than a zero when the turnstile was off or unresolved
- TOOL-aScannedThrottle-3 · OPEN · ZERO Defender exclusions are confirmed on node a (`Get-MpPreference` answers "N/A: Must be an administrator to view exclusions"), real-time + behaviour + on-access + script scanning all ON, Defender the only AV, no EDR. Third-party measurement of THIS workload shape (parallel agent-driven Bash on Windows) got 64.78s -> 7.58s, 8.5x, from excluding two Git-for-Windows folders. But the cost is NOT established here: this node measured `bash -c true` at 22.5ms on 2026-08-11 with Defender on and unexcluded, so exclusions cannot recover a regression Defender was not causing. Run `New-MpPerformanceRecording` over one bar and `Get-MpPerformanceReport -TopPaths -TopProcesses` BEFORE adding any exclusion
- TOOL-aScannedThrottle-4 · OPEN · HVCI/VBS is ON and ENFORCING on node a (`Win32_DeviceGuard` SecurityServicesRunning=2, VirtualizationBasedSecurityStatus=2, CodeIntegrityPolicyEnforcementStatus=2, OS 26200), and Microsoft's own Q&A carries a third-party 3-7x CreateProcess slowdown on exactly this shape — a parent spawning short-lived children and capturing their output — attributed in-thread by MS staff to SYNCHRONOUS antimalware inspection under Device Guard, unresolved. It explains nothing about a REGRESSION if it predates the 2026-08-11 baseline. The whole branch turns on one discriminator: establish the enablement date. Owner's setting, not an agent's
- TOOL-aScannedThrottle-5 · OPEN · `run-gates canary` (466-587s in-bar, 317s standalone, 10-11% of leg-seconds) is NOT on the critical path: deleting it entirely moves the bar 0.0%, and splitting it moves it 0.0% even after the floors are sharded, because splitting divides work without removing it and the bar is then throughput-bound. Traced on a quiet box, 99.9% attributed: 72% of it is five lines — `:458` 88.7s (4 reps x a 30-leg width-1 nested run), `:583` 57.5s (19 nested 2-leg runs), `:229` 55.7s (4 rendezvous runs), `:743` 23.3s (a deliberate 20s sleep), `:395` 21.2s. Runner overhead solves to ~1.5s fixed + ~0.74s per leg, which is half the canary and 1.3% of the bar. Only worth attacking AFTER TOOL-aPacedTurnstile-8; then halving it buys ~4% of span
- TOOL-aScannedThrottle-6 · OPEN · legs DILATE 1.5-1.85x inside the pool: `run-gates canary` costs 317s standalone on a quiet box and 466-587s as a bar leg at width 8. Every standalone leg timing therefore understates the bar by roughly half again, which is a correction that belongs on any future sharding estimate — a shard measured alone will look cheaper than it lands. Confirms TOOL-aTimedTurnstile-3's 1.66x at the current leg count
- TOOL-aScannedThrottle-7 · OPEN · `run-gates evidence` reds under load on a 5-SECOND bound: `run-gates.evidence.test.sh:225` gives a nested `GATE_FULL` bar `timeout -s KILL 5` to write its run-record header, and `run-gates.sh:600-606` measures 2136ms to header-on-disk in a TWO-FILE scratch repo. Failed both scoped bars run from this worktree today ("no header after a hard kill — the crash case is unreadable") and passed in the peer's uncontended bar at e6098aa, so it is load-dependent, not base-deterministic. Same class as the turnstile arm's 3s window graded against a >4s startup floor: a fixed wall-clock bound on a box whose process creation moves 25x. Grade the arm against a same-run control rather than a literal
- TOOL-dScrubbedConduit-2 · OPEN · playbook.fixture.md hardcodes `tools/unattended/` in outputs/grain/records/legs, so check-playbook exits 1 for any adopter at another prefix. Obstacle is kit.toml shipping it role="engine" (verbatim, no placeholder pass), not the record filenames. → tools/unattended/playbook.fixture.md
- TOOL-dScrubbedConduit-3 · OPEN · `[[hole]]` discharge probes are run by nothing on the bar. keepalive-tool-names has never passed in gov and nobody noticed. Fixing one probe leaves the next equally unobserved. → tools/govkit/registry.toml
- TOOL-dUnstalledConvoy-34 · OPEN · three driver refusal messages have no arm asserting them, all in the `--record-piece`/`--record-set` bypass-flag family and all predating `TOOL-dUnstalledConvoy-33`. Found by the suite's own arm-coverage check on the one run of it this session made. NOT fixed: outside that unit's scope, and confirming a fix needs the suite the 2026-08-23 standing owner instruction forbids running. Whoever picks it up should verify with a hermetic probe built from the suite prologue, the way `-33` did, and hand the owner the full-suite command rather than running it. → tools/unattended/unattended.sh
- TOOL-dUnstalledConvoy-35 · OPEN · `signal_lexicon_ratified_stale` compares the ratification stamp against the last commit touching `LANGS=` at all, armed or dark. A `dark` declaration is a refusal to EXTRACT, so it provably adds no definition to the corpus the VERBS table governs and cannot invalidate a curation — but it reds the signal exactly as an armed language would. Widen it to ignore dark-only surface changes, with an arm proving an ARMED addition still fires. Deliberately not done at the close of `TOOL-dUnstalledConvoy-26`: a signal edited to clear its own red is the shape nobody should trust. → tools/drift-audit/drift_report.py
- TOOL-dUnstalledConvoy-36 · OPEN · `refusal_join.py`'s JOIN half has never executed. Its docstring promises "every refusal branch in the deployer is reached by an arm that asserts it", and the leg runs it with no reached-set, so every run prints `enumeration only` and exits 0 — enumeration reading as coverage. Found by the closing review of `TOOL-dUnstalledConvoy-26`, which also found six unarmed refusal branches the join would have caught (armed by hand since). Wire a reached-set: a trace hook in `selftest.py` writing the anchors it asserted, the way `corpus_ids.py` already does, then join. The BRANCH_PIN raise to 161 in that build is a floor, not the mechanism. → tools/govkit/refusal_join.py
- TOOL-dUnstalledConvoy-37 · OPEN · `core.hooksPath` is repo-global and absolute, so in this multi-worktree layout the hook that gates a push comes from whatever the PRIMARY tree has checked out. Measured at `TOOL-dUnstalledConvoy-26`'s landing: the primary tree was on `contrib/incms-memory-recall`, so the push ran that branch's `.githooks/pre-push` — no gate-env sourcing, no predicate 8, 39 legs held and the boundary's new coverage check simply absent. The pushed tree was verified by a separate full bar, so nothing wrong shipped; the BOUNDARY was not the one that shipped. `tools/check-wiring.sh` already resolves `core.hooksPath` at SessionStart and could compare the resolved `pre-push` against the tracked blob at HEAD. Needs a decision first: does a mismatch RED, or report? Class recorded at `memory/gotchas/hookspath-resolves-into-another-checkout.md`. → tools/check-wiring.sh
- TOOL-dUnstalledConvoy-38 · OPEN · `--landed`'s check 34 compares the lander marker against the run's own HEAD, which a `--no-ff` merge landing can never satisfy — and `--no-ff` is the landing shape the charter §3 MANDATES. `push-main.sh` pushes the merge commit and writes that sha; the run's HEAD is the merge's second parent, so the marker "names a different commit" and the run cannot reach LANDED. Worked around at `TOOL-dUnstalledConvoy-26`'s landing by fast-forwarding the run's branch onto the merge first, which is true but obscures the defect. The predicate wants: the marker's commit is on the remote default branch AND has the witness as an ancestor. Also worth a look while there — `--landed` printed check 26 ("already finished") in the same invocation that wrote `phase: LANDED`, so the verb evaluates its own idempotence guard after its own write. → tools/unattended/unattended.sh
- TOOL-dNarrowedAnchor-2 · OPEN · `unattended.test.sh` carries three arms asserting a bypass-flag refusal wording this repo's own driver does not contain: `grep -c` finds it twice in the suite and ZERO times in `unattended.sh`, so those arms cannot pass. MEASURED on a pristine `main` worktree at `9ddcc5c9` — the suite fails 3 arms before any change, and it failed identically at TOOL-dNarrowedAnchor-1's base. The wording that IS in the driver reads "check 10 of the playbook leg greps every tracked record..."; the stale one reads "exactly as bad as a run-state file carrying one", which is the ADOPTER's fork of that message — so the arms look like they were written against inCMS's copy and never re-pointed. Nobody saw it because the kit self-tests left the bar at the 2026-08-23 owner ruling, which is exactly the exposure that ruling accepted. Fix the three needles, then re-run. → tools/unattended/unattended.test.sh
- TOOL-dScaffoldedMirror-2 · CLOSED · PHASE 0. Honest reporting and per-predicate liveness. `lexicon.py:455` folds `len(funcs) + len(types_)` into one `populations[ext]`, so `.js` reports a healthy 89 while P2 grades ZERO classes and the DEAD PROBE arm cannot see it — measured 2026-08-24: 10 tracked `.js` files, 0 `class` definitions, `lexicon OK`. The green line prints no offender count and no population at all, so a reader cannot tell this repo from one with zero findings. `--measure` prints UNDECLARED EXTENSIONS, DEAD PROBE, UNSELECTIVE LAYERS RULE and STALE WAIVERS as `# NOTE:` comments and returns 0 unconditionally. Split populations per PREDICATE, print `graded/offenders/waived` on green as well as red, RED on an armed predicate whose population is 0. The failing case is sitting UNSTAGED in the tree, which makes this the one unit observable RED today → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-3 · WONTDO · PHASE 0. Corpus scoping derived from the `gov:kit` receipt, never authored. `lexicon.py:125-129` is `git ls-files` with no exclusion, so the kit grades and SEEDS itself — ten of this repo's 463 offenders are its own files, and `tracked`, `ext`, `python`, `probe`, `glob` are exactly the non-verbs that bled into the demo's proposed vocabulary. A derived proposal computed over a corpus containing the deriver is untrustworthy on arrival, which makes this a prerequisite for every other derivation rather than a phase of its own. Three controls answer the earlier refusal of a free-form `CORPUS_GLOBS` key: print `graded N of M · excluded K by <rule>` every run, RED on a rule matching zero tracked files, cap the excluded fraction → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-4 · DEFERRED · PHASE 0, and a hard prerequisite for `-9`. Waiver hardening: re-key all three registries from bare `text` to `path::name` and require a non-empty reason. `load_waivers` (`lexicon.py:195-208`) does `s.split(None, 1)` and never validates the second field, and P1's key is the bare identifier — so waiving `enc` waives it in every file forever, and 46 of 395 distinct names span multiple files covering 114 of 463 occurrences (24.6%). This is NOT a re-run of the refused `<path>:<line>` keying (`TOOL-aLoosenedCeiling-5`); that failed because an edit ABOVE a waived line unpinned it, and `path::name` carries no line number. Say so in the record or a reviewer will read it as overturning a settled decision. Under `-9` the waiver becomes the only remaining dodge → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-5 · WONTDO · PHASE 0, and it DIES at `-9`. Three `RATCHETS` rows for the pins at `drift_signals.py:265`, plus closing the delete-then-re-add hole at `drift_report.py:198-221`, whose guard `if now is None or was is None or now == was: continue` lets a vanishing key AND a reappearing key both pass silently — delete in commit 1, re-add at 900 in commit 2, zero findings. The conf already hand-writes `RAISED 412 -> 415` in exactly the format `ratchet_findings` parses. Label it a DOCUMENTATION ratchet and do not report it as answering the pressure demand: the 417 -> 463 move came with three paragraphs of prose and would have sailed through. `-9` deletes these three rows in its own commit and adds an assert that a `RATCHETS` row names a live key → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-6 · CLOSED · PHASE 0. Coverage floor plus a `LANGS` mode ratchet where `parser > probe > dark`. The largest unclosed hole the review found, and no design owned it: every fail-closed story covers the UNDECLARED (extension, predicate) pair and NONE covers the declared-dark one, which is the cheap move — one string edit empties the graded population and reds nothing. Measured here: 82 tracked `.sh` against 44 `.py`, so shell is this repo's largest source population by file count and holds ~518 definitions, while only 54 of the 126 definition-carrying files (42.9%) are covered by an armed mode. Failing case: flip `py` to `dark` and watch the fraction collapse 42.9% -> 7.9%; today that is `lexicon OK`, exit 0 → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-7 · CLOSED · PHASE 0, and it discharges `-17`. A `drift-audit` signal measuring offenders ADDED per definition ADDED between a recorded base and HEAD, both operands derived at both shas by the kit's own extractor — nothing authored, nothing raisable. `gateable: False` initially, with a liveness assertion so a broken derivation prints DEAD PROBE rather than a reassuring 0. This is the ONLY instrument anywhere that measures the actual demand (are new generations constrained), and it is what converts the 136-definitions/zero-offenders result from one agent's afternoon into a standing falsifiable claim. Measured cost 0.369 s on gov, 3.218 s on incms, sha-cacheable because a commit's tree is immutable. Not on the merge bar → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-8 · SPECCED · PHASE 2, and it RETIRES `-1` by deleting its subject. A kit-owned frozen `canon.py` of ~19 concept clusters with `role = engine`, plus a read-only `--probe`; `--scaffold`'s frequency allowlist is DELETED rather than patched a fourth time. THE RULE IS THE DESIGN: the canon's first element is the proposal UNCONDITIONALLY, at every dominance level, and the corpus is admitted as evidence for exactly one thing — which spellings become debt rows. The corpus votes to EXCLUDE, never to select. The dominance-table alternative was refuted as the demo defect one indirection down (a repo whose sessions all wrote `get_*` has D~1.0 and adopts `get`) and it contradicted its own day-1 claim. Two arms: 500 `frobnicate_*` definitions must not enter VERBS, and `get`=1/`fetch`=1/`load`=0 must propose `load`. Carries the 11 missing negative definitions and the structured-NOT assert → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-9 · DEFERRED · PHASE 4, blocked on `-4` and `-11`. The grandfather set with a provenance assert, replacing all three pins. `FREEZE_SHA` plus a shrink-only set keyed `path::name` plus five asserts, of which C is load-bearing: every grandfathered key must be DERIVABLE as an offender at `FREEZE_SHA`, so no edit to the present tree can top the file up. That is the only mechanism in the brief making an absorbing edit IMPOSSIBLE rather than expensive, and it is reviewer R4's own proposed repair to the cut pin-direction guard — guard on the offender IDENTITIES, not the count. Cost ordering inverts: today raise(1min) < waive(2min) < add-a-verb(5min) < rename(10min); afterwards raise does not exist. Freeze advancement FORBIDDEN in v1. Location is the kit dir with `role = seed`; siting it under the memory root instead reds HYGIENE check 3, measured → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-10 · CLOSED · PHASE 1, and the highest-VALUE unit in the build even though it is not the highest-ranked. Supply the vocabulary to the author: `--suggest <name>` (0.045 s, one deterministic line), a per-OBJECT `--brief` rather than a directory histogram (750 distinct off-table leading tokens in one incms directory make a top-9 line show 1.2% of the live vocabulary), and a rendered lexicon Skill under the skills root whose `--check` byte-compares against `.lexicon.conf` on an UNGUARDED gate leg. The charter POINTS and must be net-negative in bytes: `AGENTS.md` sits at 64,394 of 64,512 — 118 bytes — against a 1,787-byte table, and a staged 193-byte bullet reds the size gate by 87. `--brief` must declare its coverage mode and REFUSE on a dark extension, or an empty section reads as invent-freely → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-11 · DEFERRED · PHASE 3, blocked on a kit-owned `^visit_[A-Z]`/`generic_visit` exemption class. Replace `_python_defs`'s scope-blind `ast.walk` (`lexicon.py:140-169`) with a `NodeVisitor` emitting `(name, line, kind, scope, span, public)`, then ONE predicate: `len(name.strip(_)) >= 3` unless the enclosing scope is <=10 lines. The span condition IS the design and it is measured — the same rule fires 51 times above the cut line and 1,033 below it. Knobs are KIT CONSTANTS, never conf keys: an offender count selected from a published threshold menu is a pin wearing a threshold's clothes. Both arms required or it is a plain length rule — a parameter `d` in a 40-line function RED, the same parameter in a 4-line function GREEN. The prerequisite is real: a staged 8-method visitor reds the gate today at 471 over 463, with 17 `visit_*` names CPython mandates → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-12 · DEFERRED · PHASE 5, RESEARCH, only on the owner's word. The consistency instrument: group definitions by OBJECT (the subtokens after the leading one) and measure whether members agree on the leading token. No vocabulary, so §12's mirror ban does not apply, and both operands derive from the tree so it cannot be raised. Measured here: 824 defs, 589 carrying an object, 418 distinct objects, 47 spelled more than one way, CONSISTENCY 0.716. It finds 60 genuine conflicts P1 calls GREEN (`load_conf` x8 vs `read_conf`; `region` spelled five legal ways) and declines to fire on the 354 of 459 offender keys that conflict with nothing. FOUR disqualifying gaps keep it RESEARCH: silent on the first instance, a repo of all-`get_*` scores 1.000, it can never say `load` not `fetch`, and `tokens[1:]` keying makes roughly half the 47 groups artifacts. Build a real concept key and hand-measure precision on >=40 groups on gov AND incms first → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-13 · DEFERRED · the adopter's primary language is INVISIBLE to the kit, and this is the real blocker on probing anyone. Measured 2026-08-24 by a read-only run of the kit's own extractor against `incms/main`: 6,168 tracked files, 626 `.ts` and 572 `.tsx`, and no `PATTERN_SETS` entry for either — so armed coverage is 1,190 files, 19.3%. Bigger than this repo's dark `.sh` hole, and it lands on a repo that actually exists. The JS/TS lexer was deliberately NOT recommended yet and the objection is ORDER, not cost (a stdlib prototype hit 21.9 MB/s over 1,236 files): it would arm a casing rule over 1,072 PascalCase React components and a synonym predicate over a TypeScript corpus nobody has measured, and its conformance fixture round-trips through its own reading, which is the P3 tautology this kit already recorded. Revisit after `-8`, with fixtures extracted from real adopter files rather than authored → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-14 · CLOSED · 79 of 459 backfill keys (17%), agent-defaulted 2026-08-24 with the owner declining to override. Rename all 52 `t_*` to `test_*` — they live in exactly two files (`tools/memory-recall/selftest.py` 29, `tools/codebase-map/selftest.py` 23), both selftests, and `test` is ALREADY a reserved row meaning precisely this, so it costs no table change. Then reserve `cmd` as a structural row beside `main` and rename the 22 `do_*` (all in `memory-tree/`) to join govkit's 5 `cmd_*`. `cmd` over `do` despite being the more expensive rename: `main` is already reserved as a module's CLI entry point and a subcommand handler is that role one level down, so `cmd` names the role while `do` names nothing — and admitting `do` would be the table's first synonym row. Net 459 -> 380 before anything else is decided → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-15 · DEFERRED · nothing tells an adopter this kit exists, and it is upstream of the whole probe-the-adopters demand. `tools/govkit/check_runbook_parity.py` exits 1 naming 18 registry entries with no anchored runbook section — `lexicon` among them at output line 12 — and it has ZERO callers and is absent from `tools/gate-legs.json`. Verified 2026-08-24. A hand-wiring adopter is therefore never told the kit is deployable, which is the mechanical reason the live adoption count is one. Not the lexicon's own work, and deliberately filed as its own row: `incms/main` is a full gov adopter (6,168 files, `.governance/`, `.codebase-map.conf`, `.memory-tree.conf`) carrying no `.lexicon.conf`, and `swydee` is hand-wired with no `.governance/` at all → `builds/dScaffoldedMirror/`
- TOOL-dScaffoldedMirror-1 · CLOSED · `--scaffold` writes `SUFFIX_OFFENDER_PIN="0"` and `LAYER_OFFENDER_PIN="0"` as hardcoded literals (`scaffold_lexicon.py:106-107`) under a seed comment that calls all three MEASURED against this corpus at scaffold time. REPRODUCED 2026-08-24 on a fresh 19-file repo: a corpus carrying `class OrderManager` and `class OrderHelper` scaffolds green and then reds on its first gate run with `suffix offenders 2 over pin 0` — a pin the tool wrote itself. Fails SAFE (red, never a false green), and `lexicon.py:500` already names the defect in passing as the reason `--measure` exists, so the REMEDY verb shipped while the write path did not move. The fix is ASYMMETRIC: `layer`'s zero is correct BY CONSTRUCTION because `LAYERS` seeds empty and no layer offender can exist at scaffold time, so measure `suffix` the way `verb` is already measured and drop the MEASURED claim from the comment for `layer` alone. SECOND FINDING from the same run, same root: the scaffold runs BEFORE the conf it writes is tracked, so the derived `LANGS` cannot contain `conf` and the adopter's first `git add .lexicon.conf` reds the gate with UNDECLARED EXTENSIONS. Every fresh adoption hits it in the first five minutes; either scaffold `conf::dark` unconditionally or name the trap in the seed comment RETIRED 2026-08-25 by TOOL-dScaffoldedMirror-8 S5/S8, which deleted the subject rather than patching the write path a fourth time: the frequency allowlist is gone, all three pins are MEASURED, and `conf::dark` is seeded unconditionally. Verified by re-running the owner's original demo — all six deliberately-bad names are now offenders where all six passed green → tools/lexicon/scaffold_lexicon.py
- TOOL-dScaffoldedMirror-20 · OPEN · nothing checks this repo's own `.gitattributes` against the `[[lf_pin]]`s its kit descriptors declare, so the two can diverge silently in the direction that matters least visibly: an ADOPTER gets the pin and gov does not. Found as closing-review M9 — `tools/lexicon/kit.toml` declared `.claude/skills/lexicon/SKILL.md` and gov's own attributes carried no such line, for a file its gate byte-compares. The predicate must ask `git check-attr eol`, never string membership: a blanket `*.sh text eol=lf` legitimately covers three declared pins that no literal line names. Measured 2026-08-25 over all 23 declared pins — 10 literal, 13 needing `{kit}`/`{memory_root}`/`{prefix}` resolution — and exactly one was unhonoured. NEITHER obvious home works: `tools/check-wiring.sh` resolves python only to NAME it in a remedy string and deliberately executes none, so a TOML-reading arm there breaks its no-python property; and `drift_report.py` ships to adopters, who hold no descriptors at all, so the signal would be dead in every tree but this one. Wants a gov-internal leg, which is a fourth carrier (`tools/gate-legs.json`, a `[[gate_leg]]` claim, `tools/govkit/subject-pins.tsv`, the map dossier) and was not worth half-building inside a fix pass. → tools/govkit/
- TOOL-dScaffoldedMirror-21 · OPEN · `check-unattended.sh` check 29 has no grandfathering, so any run already IN FLIGHT when `TOOL-dNarrowedAnchor-1` landed became unlandable. The check asks whether a run's recorded BASE is an ancestor of the tip the REMOTE advertises for the default branch — which it becomes only once that branch is pushed, and the pre-push hook runs the bar before pushing. `.unattended.conf` sets `BYPASS_BAN="--no-verify"`, so the escape the hook names is one this project forbids. Measured 2026-08-25 on dScaffoldedMirror: bar 84/85 on merged main, this leg the only red, and `git merge-base --is-ancestor 500a5db6 ec17c20a` true — so the red self-heals the moment the push it blocks completes. NOT a defect in the rule, which is right, and not a general one: for a repo with no in-flight run the check never fires, since `prompt` and `recipe` are exempt and a conforming `slug` run has no second-anchor base. It bites the transitional window only. The kit already ships the mechanism — `LANDED_ANCHOR_CUTOFF`, read at `check-unattended.sh:893`, grandfathers anchors on records predating a cutoff — and check 29 was simply not given one. Owner took the one-off bypass on 2026-08-25 rather than have the run that benefits edit the gate that blocks it; this row is that decision's other half. → tools/unattended/check-unattended.sh
- TOOL-dScaffoldedMirror-22 · OPEN · `--landed` writes `phase: LANDED` BEFORE check 34 can refuse it, so a refused landing leaves a record claiming the terminal phase while missing the `landed-anchor` fact that `set_fact` writes further down the same function — `unattended.sh:1890`, unreachable once check 34 `return 1`s at `:1876`/`:1882`. The result reds check 15 forever, and check 26 then refuses `--park` and `--phase` on the same record because it is finished, so NO verb can repair what the verb broke. Observed 2026-08-25 on dScaffoldedMirror: the marker held a sha from the push-main run a red bar had blocked, `--landed` refused, and the record was left LANDED-with-no-anchor and unrepairable. Hand-completed with `landed-anchor: remote`, which git confirms independently — the witness `ea49714a` is an ancestor of `origin/main`. Same write-before-check family as `TOOL-dUnstalledConvoy-38` and found in the same invocation, but a distinct defect: -38 is that check 34 cannot be satisfied by a `--no-ff` landing at all, this one is that failing it corrupts the record on the way out. Fix is ordering — compute and validate, then write both facts together, so a refusal leaves the record untouched. → tools/unattended/unattended.sh
- TOOL-dSpentCeiling-1 · CLOSED · the read-path byte budget is retired and check 16's rules 3 and 4 are structural. MEASURED before building: 27 movements of `READ_PATH_CEILING` between 2026-08-08 and 2026-08-25, 26 up and 1 down, and the single drop (`95431 -> 86476`, `d816c30b`) was forced by hygiene check 11's new 400-byte per-entry cap rather than by this ceiling — so the budget never once caused a trim. It was always a SECOND bound: check 6 caps all six members at 61440 B each, 368640 B of first bounds against 135706 B held. THE FINDING THE INSTRUMENT NEVER SURFACED: 54.0% of mandatory reading is rendered from other kits' templates and cannot be trimmed here at all, and those two files supplied 64.9% of the week's growth while `LIVE.md` SHRANK. An earlier draft argued the breach was structurally guaranteed because `DECISIONS.md` is append-only; REFUTED — it rotated on 2026-08-10. Owner cut the build from six units to one on 2026-08-25: no successor instrument. Rules 3 and 4 REPORT until memory-tree 2.43 so no adopter reds on upgrade for a pre-existing condition → `builds/dSpentCeiling/`
- TOOL-dSpentCeiling-4 · OPEN · a kit's RELEASE can spend another kit's read budget, and no gate anywhere prices it. Measured 2026-08-25 while retiring the read-path ceiling: 73268 B of this repo's 135706 B of mandatory reading — 54.0% — is rendered from kit templates (`memory/guides/BUILD-METHOD.md` carries a `gov:kit memory-tree@` marker, `memory/guides/UNATTENDED-PROTOCOL.md` a `gov:kit unattended@` one), and over the seven days to that date those two supplied 28216 B of 43460 B of growth. An adopter cannot trim either one; only the kit owner can. Every one of the ceiling's 26 raises was argued as though the adopting repo's own prose were the driver. Wants a size discipline on RENDERED docs, owned by the kit that renders them — candidates: a per-template ceiling in `kit.toml` beside the `[[render]]` row, or a drift signal over rendered-vs-authored bytes. NOT scoped here → tools/memory-tree/
- TOOL-dSpentCeiling-5 · OPEN · the hygiene check COUNT is hand-typed in at least three places and they already disagree: `tools/memory-tree/README.md` and the map dossier's H1 say 21, the dossier body says 22, and `check-verdict-epoch.sh:47` says 19. Nothing derives it and nothing gates it, so each new check makes the spread worse. Found while retiring the read-path budget, which deliberately did NOT renumber check 16 into two checks for exactly this reason — a fourth wrong count is not an improvement. The fix is to DERIVE the count (the engine can print it, the way it already prints its index set and its version) and have the carriers cite the derivation → tools/memory-tree/
