# unarmed-branches.txt — `fail` branches with no positive assertion naming their own
# failure text. SHRINK-ONLY: a row leaves when its branch gains an arm, and check-arms
# reds if a pinned branch is armed, if a pinned branch or its gate disappears, or if a
# message is reworded out from under its signature.
# Fields: gate<TAB>check<TAB>ordinal<TAB>signature.
#
# NOT empty, and the distinction matters more than the count: a row here is a branch no fixture can
# reach, carrying the REASON it cannot be armed — "not yet written" and "cannot be written from
# here" are indistinguishable in a bare pin, and only one of them is acceptable. `check-arms.py
# --report` prints the live count; this comment does not, because a number written here rots
# silently, and this header claimed EMPTY while carrying a row until TOOL-aTetheredRecord-4.
tools/unattended/unattended.sh	9	1	cannot stage the run-state file, and the gate leg's whole per-run population is the index, so an unstaged run is invisible to every check it has
#
# THE TWO ROTATION ROWS (kit 1.6) carry their reason, per the rule above. Both sit BELOW two
# pre-write refusals that a fixture CAN reach — a derived name occupied by a non-file, and one
# occupied by a file with different bytes — and both were left as branches rather than deleted
# because the alternative is a silent misfiling:
#   27  `archive_name_of` returns non-zero only when the record's phase is empty or `git hash-object`
#       fails over a file that exists and was just read. The phase cannot be empty here — `is_terminal`
#       has already matched it against the closed PHASES_TERMINAL set — so reaching this needs a git
#       or filesystem fault, which no fixture in this suite can produce.
#   The 27 row's ORDINAL moved 2 -> 4 on 2026-08-20, and the branch it pins did not change. Two
#   `fail 27` branches were inserted ABOVE it inside `observe_anchor` when the remote observation
#   became bounded, and ordinals are assigned by line order within a check — so every pinned row
#   below an insertion point goes stale at once, with a message that reads like a rewording. Both new
#   branches are ARMED rather than pinned, because both are reachable: a dead TMPDIR makes the
#   scratch-file guard fire, and a stub `git` exiting 124 drives the timeout refusal without spending
#   the declared bound.
#   29  `GIT mv -f` after both pre-write tests cleared the destination. MEASURED: the two reachable
#       failure modes were the ones now refused above it (a directory destination exits 0 and files
#       the record INSIDE it; a differing file is refused by name), so what is left is an index or
#       filesystem fault. Trying to arm this is what FOUND the directory case.
tools/unattended/unattended.sh	27	4	cannot derive an archive name for the finished record, so there is nothing safe to retire it to and the run does not start
tools/unattended/unattended.sh	29	2	cannot retire the finished record, and a half-rotated build is worse than an unrotated one — the run does not start and nothing was moved
#
# CHECK 30's SECOND BRANCH (TOOL-dHonouredPark). This check drives the REAL driver over the
# real corpus, so its bad shape - a NOT A UNIT row beside a terminal verdict - exists only
# when the DRIVER is wrong. No fixture can arrange that, which is the distinction this file's
# header draws: not "an arm nobody wrote" but "an arm that cannot be written from here".
# It WAS observed red, against a staged revert of the driver's `_graded` branch: it named all
# five affected builds and went green on restore. Its sibling liveness branch IS armed.
#
# TOOL-aGradedMandate, 2026-08-31. Eight rows below, and the honest reason is one sentence: the
# fixtures that would arm them live in check-unattended.test.sh, and the owner instructed this run
# to skip that suite mid-build. These are NOT unreachable branches — they are unwritten arms, which
# is the weaker of the two states this file's header distinguishes, and saying so is the point.
# THREE OF THE EIGHT WERE OBSERVED RED against staged breaks on the live tree and are marked so:
# check 2 ordinal 10 against PARK_ACTS_OWED="retire supercede", and check 16 ordinals 16 and 17
# against a driver carrying a bogus member and a Skill naming an invented one. The other five are
# degradation branches — a constant the leg cannot read, a template the kit does not ship — and are
# unobserved. They drain when that suite is next run and its arms are written.
tools/unattended/check-unattended.sh	2	10	the driver declares owed rescope ACTS but this leg cannot find the closed act alternation --rescope validates against, so the act axis would be graded against nothing
tools/unattended/check-unattended.sh	2	11	the parked-ACT taxonomy names an act --rescope's own closed case cannot accept, so no row can ever carry it and the surfaced count is silently narrower than the set says
tools/unattended/check-unattended.sh	2	12	the driver declares no readable PARK_ACTS_OWED, so the act axis of the surfaced count would range over an empty set and a retirement would silently stop being owed
tools/unattended/check-unattended.sh	16	13	cannot read DOD_NO_OVERRIDE from the driver, so the join below would compare the Skill against an empty set and pass by finding nothing
tools/unattended/check-unattended.sh	16	14	the kit ships no SKILL.template.md, so the non-overridable set an agent reads cannot be joined to the constant the verb enforces
tools/unattended/check-unattended.sh	16	15	the Skill template carries no non-overridable paragraph this leg can read, so the join would compare the driver's set against nothing and pass by finding nothing; the sentence it looks for names the items and the item names are backticked
tools/unattended/check-unattended.sh	16	16	the driver refuses an override on an item the Skill's non-overridable paragraph does not name, so a run meets a refusal its own instructions said could not happen
tools/unattended/check-unattended.sh	16	17	the Skill's non-overridable paragraph names an item the driver does not refuse an override on, so a run is told a route is closed that is open
# `git hash-object` on a path this verb has already proven is a tracked file cannot fail without
# git itself failing, so there is no constructible fixture. Pinned rather than armed with a
# synthetic break, which proves the mechanism for the synthetic value and not the real one.
tools/unattended/unattended.sh	49	8	cannot hash the brief, so the row would carry no join
# The order-gate blocker message needs TWO rostered units carrying `order` verbs, and the tRun
# fixture's units region emits one row. A second rostered unit changes a fixture shared by this
# whole suite, which the standing instruction on this node forbids running. TOOL-dBriefedPass-6
# carries the separate fixture; TOOL-dBriefedPass-3 rev-5 DECLARES the branch unexercised.
tools/unattended/unattended.sh	49	19	and an earlier step still holds a unit that is neither terminal nor dispatched
# The SIBLING malformed-order refusal sits inside the same sibling loop, so it needs the same two
# rostered units the blocker message does and is unarmable from the shared fixture for the same
# reason. TOOL-dBriefedPass-6 arms both together.
tools/unattended/unattended.sh	49	18	a sibling spec status header carries something shaped like the build-order verb that does not conform, so this build's order cannot be sequenced against it
