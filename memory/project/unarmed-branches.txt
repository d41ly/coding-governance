# unarmed-branches.txt — `fail` branches with no positive assertion naming their own
# failure text. SHRINK-ONLY: a row leaves when its branch gains an arm, and check-arms
# reds if a pinned branch is armed, if a pinned branch or its gate disappears, or if a
# message is reworded out from under its signature.
# Fields: gate<TAB>check<TAB>ordinal<TAB>signature.
#
# NOT empty, and the distinction matters more than the count: a row here is a branch no fixture can
# reach, carrying the REASON it cannot be armed — "not yet written" and "cannot be written from
# here" are indistinguishable in a bare pin, and only one of them is acceptable. `check-arms.py
# --report` prints the live count; this comment does not, because a number written here rots
# silently, and this header claimed EMPTY while carrying a row until TOOL-aTetheredRecord-4.
#
# EMPTY OF ROWS since TOOL-aRepatriatedFork-18: every pin gov held was for a gate the unattended
# kit ships, so they moved to the SIDECAR beside those gates, `unarmed-branches.txt` in that kit's
# own directory, and travel with them. This file is for gates THIS tree owns.
