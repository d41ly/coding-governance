# method-carriers.txt — every file that POINTS AT memory/guides/BUILD-METHOD.md, declared.
# One '<repo-relative-path> · <why>' row each. The 'why' is the sentence a future author reads when
# deciding whether their new carrier is the next pointer or the first summary.
# Keyed on PATH alone, never <path>:<line> — that keying is what unpinned install-prefix-waivers.txt.
.claude/skills/unattended/SKILL.md · rendered; its step 0 tells an unattended run to read the method first
AGENTS.md · the charter's Conventions row, the only carrier that reaches a session with no invocation
skills/session-kickoff/SKILL.md · Step 5b's hand-back loads the method before the first pass
tools/memory-tree/README.md · holds the method's displaced sections, and names the file they came from
tools/memory-tree/adopt-memory-tree.sh · renders the method into an adopting tree
tools/unattended/SKILL.template.md · the source of the rendered Skill's step 0
tools/unattended/unattended.sh · verb_resume echoes the method path to a resuming run
tools/memory-tree/HYGIENE.template.md · the shipped hygiene rules describe this registry, and naming what it guards names the method
.memory-tree.conf · its READ_PATH_WAIVER comment points at the retirement exhibit, which names the method as the largest read-path member it measured
tools/memory-tree/kit.toml · the deployer descriptor names the method as a RENDERED artifact of this kit — a destination, never a restatement
tools/unattended/check-unattended.sh · check 16 arm B resolves each cited M-section against the method's own heading grammar; it POINTS at the sections rather than restating any rule
tools/unattended/PROTOCOL.template.md · §10 names the build method as the carrier every directive points into, and §1 names it a run-time dependency; both POINT, neither restates a rule
tools/drift-audit/drift_signals.py · TRACE_CUTOFF cites the commit that landed the method, because that commit is what dates the commit-subject convention this signal judges
tools/gate-legs.json · a GUARD pathspec names the method file, because the kit/dogfood parity leg validates it — a path the leg reads, never a restatement of what the method says
tools/workflows/unattended-build.js · the build harness POINTS at the method: its stage prompts tell each agent to read BUILD-METHOD.md whole before acting, and its BUILD prompt cites M4's two dispositions by name rather than restating either. It states no rule the method does not.
