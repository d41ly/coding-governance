# curation-debt.txt — index files pending slimming (exempt from checks 6/7/8 while listed). Empty = fully strict.
# Two build READMEs entered the index set at TOOL-aRuledFrontispiece-5's tier (BUILD_README_CAP_BYTES / BUILD_README_ENTRY_CAP_CHARS)
# already above it. Both are other builds' records, so slimming them is not this build's to do —
# Slimming them is not this build's to do: each is another build's record and its prose is another
# node's judgement. That is the whole reason, and it rests on nothing else. An earlier version of
# this line credited TOOL-aMouldedFolio-3 with a principle about which folder owns which prose; no
# such sentence is in that record, whose S2 says the reverse - 31 READMEs edited BY THE GENERATOR,
# "No file is hand-edited" - with an acceptance criterion gating against the hand-edit alternative.
# Corrected under TOOL-dFramedEntrypoint-8, which found the miscitation while researching this same
# registry. Shrink-only:
# each row drains when that build's owner curates it.
memory/builds/cBriefedPilot/README.md
memory/builds/aUnmannedHelm/README.md
memory/builds/aBoundedVerdict/README.md
memory/builds/aDrainedSluice/README.md
# This build's own README: 11 units with long spec titles put three GENERATED unit-table rows
# over the per-line tier and the file over the byte tier. Both are renderer-shaped problems
# (the row carries the full title AND the path), not curation ones, and reshaping the renderer
# during a merge is the wrong moment. Drains when the row shape is revisited.
memory/builds/aRuledFrontispiece/README.md
# memory/backlog/TOOL.md — a MERGE-INDUCED overflow, not a curation lapse. Both parents were legal
# and within a hair of the cap (theirs 20435 B, ours 20479 B, cap 20480); the row driver unioned
# them correctly to 86 rows / 21762 B. Rotation could NOT fix it when this was written: TERMINAL is
# ("CLOSED","WONTDO") and exactly ONE row qualified, so rotating shed less than the rotation note
# it must add.
#
# THAT PREMISE EXPIRED, 2026-08-21. Re-derived on the same tree: 159 rows, 71855 B against a cap
# of 61440, and NINETEEN rows are terminal. aShardedFloor closed four of them in one landing.
# Rotation is now the live remedy rather than a rejected one, and this row is no longer
# describing why it cannot be drained — it is describing a drain nobody has performed. The count
# is deliberately re-derived here rather than trusted: the original was true and stopped being
# true without anyone editing it, which is the whole class this file exists to make visible.
# This is
# TOOL-cSettledDocket-16 arriving — "new rows are now paid for by shortening old ones" — and paying
# that fee by trimming 1 KB of other builds' prose mid-merge is the wrong moment, on the same
# reasoning the aRuledFrontispiece row above already carries.
# BLAST RADIUS, stated because a debt row is wider than the failure that earned it: this silences
# checks 6, 7 AND 8 on TOOL.md, not just the byte cap. MEASURED before listing it — the hygiene
# run that caught check 6 reported neither 7 nor 8, and the longest row is exactly 300 chars
# against a 300 budget. So nothing is hidden today, but that row is AT the limit and the check
# that would catch it going over is now off. Drains when the TOOL index is curated or split.
memory/backlog/TOOL.md
# memory/builds/dUnstalledConvoy/README.md - a GENERATED record-bindings row that scales with the
# UNIT COUNT. This build's spec audit legitimately serves all 13 units, so the Records-table row the
# renderer emits carries 13 ids and measures 438 chars against the 350 tier. No authoring choice
# inside the record shortens it: the binding grammar expands a contiguous range at authoring time,
# so the record must spell every id, and splitting one review into two records to shorten a row
# would be two answers to one question. Renderer-shaped, exactly like the aRuledFrontispiece row
# above, and TOOL-dUnstalledConvoy-13 is the row that records the structural cause.
# BLAST RADIUS, measured before listing: this silences checks 6 and 7 on this file. Check 8's
# population is the backlog shards alone, so it never reached this path. Check 6 passes today with
# room - 17996 B against a 25600 cap - and the longest line that is NOT the bindings row measures
# 319 chars against the 350 tier. So nothing is hidden today. Drains when the row shape is revisited.
memory/builds/dUnstalledConvoy/README.md
# memory/builds/dFramedEntrypoint/README.md - the SAME generated record-bindings row that earned
# dUnstalledConvoy its row above, reproduced by the build that exists to remove it. The round-1 spec
# audit legitimately serves all eight units, so the Records row the renderer emits spells eight ids
# and measures 357 chars against the 350 tier. The binding grammar expands a contiguous range at
# authoring time, so no authoring choice shortens it, and narrowing the binding would make the
# "ids no spec-audit record has ever named" join - one of the two signals this build is protecting -
# assert something false.
# BLAST RADIUS, measured before listing: this silences checks 6 and 7 on this file. Check 8's
# population is the backlog shards alone and never reached this path. Check 6 passes with room today
# (4589 B against a 25600 cap) and the longest line that is NOT the bindings row measures 264 chars
# against the 350 tier, so nothing is hidden. DRAINS AT TOOL-dFramedEntrypoint-5, which deletes the
# Records table outright - this row is a dated debt with a named discharge, not an open-ended waiver.
memory/builds/dFramedEntrypoint/README.md
