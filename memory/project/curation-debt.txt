# curation-debt.txt - index files pending slimming (exempt from checks 6/7/8 while listed). Empty = fully strict.
#
# FIVE ROWS DRAINED AT TOOL-dFramedEntrypoint-5, which removed the document-inventory region and the
# records table from every build README. Every one of the five was RENDERER-SHAPED - a generated row
# or a generated region blowing a cap on an authored-prose class - and each said so in its own text.
# Drained: aPacedTurnstile, aDrainedSluice, dUnstalledConvoy, aRuledFrontispiece, dFramedEntrypoint.
# Measured, not assumed: with the whole registry emptied, the hygiene gate reds on exactly the
# subjects below and on nothing else, so the five carry no remaining load.
#
# TOOL-dUnstalledConvoy-13 IS CLOSED BY THIS. Its row said a build's spec-audit record cannot fit the
# generated bindings row once a build passes about eleven units, because the row must spell every id
# it serves and the binding grammar expands a range at authoring time. There is no bindings row now.
#
# WHAT IS LEFT is authored prose over an authored cap, which is what this registry was always for.

# memory/builds/aBoundedVerdict/README.md - 38261 B against a 25600 cap, and five AUTHORED lines over
# the 350-char tier. Not renderer-shaped: this is the largest authored half in the corpus and slimming
# it is that build's own curation, not this one's. It fell from 44872 B when the two regions went, so
# the removal helped and did not finish the job.
memory/builds/aBoundedVerdict/README.md
# memory/builds/cBriefedPilot/README.md - 27895 B against the same cap, down from 32023 B. Authored
# prose again; same disposition as the row above.
memory/builds/cBriefedPilot/README.md
# memory/builds/aUnmannedHelm/README.md - ONE authored line at 419 chars against the 350 tier. The
# file is 12158 B and comfortably inside the byte cap, so this row buys exactly one line.
# BLAST RADIUS: listing it silences checks 6 AND 7 on the whole file to waive that single line. That
# is wider than the fault and is the shape this registry keeps being criticised for; it stays only
# because narrowing it needs a per-line waiver grammar this registry does not have.
memory/builds/aUnmannedHelm/README.md
# memory/backlog/TOOL.md - a MERGE-INDUCED overflow, not a curation lapse, and the one row here whose
# subject is not a build README. Both parents were legal and within a hair of the cap; the row driver
# unioned them correctly and the result was over.
#
# THE ORIGINAL PREMISE EXPIRED, 2026-08-21: rotation was rejected because exactly ONE row was
# terminal, so rotating shed less than the rotation note it must add. Re-derived on the same tree,
# nineteen rows were terminal. Rotation is the live remedy and this row now describes a drain nobody
# has performed rather than one that cannot be.
# BLAST RADIUS, stated because a debt row is wider than the failure that earned it: this silences
# checks 6, 7 AND 8 on TOOL.md, not just the byte cap. Check 8 is NOT idle here - with the registry
# emptied it reds on line 34 - so this row is currently hiding one real status-token fault as well as
# the width. Drains when the TOOL index is rotated or split.
memory/backlog/TOOL.md
