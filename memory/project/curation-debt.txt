# curation-debt.txt — index files pending slimming (exempt from checks 6/7/8 while listed). Empty = fully strict.
# Two build READMEs entered the index set at TOOL-aRuledFrontispiece-5's tier (BUILD_README_CAP_BYTES
# already above it. Both are other builds' records, so slimming them is not this build's to do —
# TOOL-aMouldedFolio-3 is the precedent that a build's own folder owns its own prose. Shrink-only:
# each row drains when that build's owner curates it.
memory/builds/cBriefedPilot/README.md
memory/builds/aUnmannedHelm/README.md
memory/builds/aBoundedVerdict/README.md
memory/builds/aDrainedSluice/README.md
# This build's own README: 11 units with long spec titles put three GENERATED unit-table rows
# over the per-line tier and the file over the byte tier. Both are renderer-shaped problems
# (the row carries the full title AND the path), not curation ones, and reshaping the renderer
# during a merge is the wrong moment. Drains when the row shape is revisited.
memory/builds/aRuledFrontispiece/README.md
# memory/backlog/TOOL.md — a MERGE-INDUCED overflow, not a curation lapse. Both parents were legal
# and within a hair of the cap (theirs 20435 B, ours 20479 B, cap 20480); the row driver unioned
# them correctly to 86 rows / 21762 B. Rotation cannot fix it: TERMINAL is ("CLOSED","WONTDO") and
# exactly ONE row qualifies, so rotating sheds less than the rotation note it must add. This is
# TOOL-cSettledDocket-16 arriving — "new rows are now paid for by shortening old ones" — and paying
# that fee by trimming 1 KB of other builds' prose mid-merge is the wrong moment, on the same
# reasoning the aRuledFrontispiece row above already carries.
# BLAST RADIUS, stated because a debt row is wider than the failure that earned it: this silences
# checks 6, 7 AND 8 on TOOL.md, not just the byte cap. MEASURED before listing it — the hygiene
# run that caught check 6 reported neither 7 nor 8, and the longest row is exactly 300 chars
# against a 300 budget. So nothing is hidden today, but that row is AT the limit and the check
# that would catch it going over is now off. Drains when the TOOL index is curated or split.
memory/backlog/TOOL.md
