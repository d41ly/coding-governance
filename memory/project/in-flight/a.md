# In-flight ledger — node `a`

| Node | Branch / worktree | Streams | Slug · ids | Status |
|---|---|---|---|---|
| `a` | `main` (kit + docs, no product code) | tooling — spec-format discipline port (kit check 12 + template + self-test + dogfood arm) | **aRuledParchment** · TOOL-aRuledParchment-1/-2 · seq TOOL-2 | **merged:267234f** (spec-format discipline; on `main`) |
| `a` | `main` (specs only, doc-only) | playbook + tooling — **gate-economy uplift** ported from inCMS ARCH-aTrimmedGauntlet-2: 6 specs (full-run-at-push-boundary, fail-closed scoping, vacuous-ratchet bug class, manifest runner, pre-push enforcement, bookkeeping-before-push) | **aPrunedCeremony** · PLAY-aPrunedCeremony-1..4 · TOOL-aPrunedCeremony-1/-2 · seq PLAY-5 TOOL-2 | **merged:c3357b71** — 6 gate-economy specs built + landed (playbook rewords + domain-rules §10 + retire pushed-state token; manifest runner + no-hardcode canary; pre-push full-gate hook); full suite 16 legs green |
| `a` | `feature/leased-gauntlet-a` (worktree `coding-governance-lg`, off `d5ada669`) | tooling — **TOOL-aLeasedGauntlet-1** (ports inCMS ARCH-aLeasedGauntlet-1): `push-main` reconcile-before-gate lander + pre-push marker gate + **fail-CLOSED default-branch resolution** (was fail-OPEN — silently assumed `main`, bypassing the gate on a non-main default) + durable gate summary. Closing review `wf_2a93910e` folded 7 findings (2 MED). Build `memory/tooling/builds/2026-07-20-TOOL-aLeasedGauntlet/`. | **aLeasedGauntlet** · TOOL-aLeasedGauntlet-1 · seq TOOL-1 | **merged:cd5b0e9** — landing via `tools/push-main.sh` (full suite 16/16 green). _Prune once ancestor._ |
| `a` | `main` (kit + docs, no product code) | tooling — **TOOL-aQuarriedLantern-1** memory-recall kit port from inCMS: U1 the kit (conf layer, forked extract/query, cache freshness on the resolved conf), U2 the invocation surfaces (rendered Skill, opt-in `recall-opened` hook, `settings-merge.py --fragment`), U3 every registration surface (WIRE §3c + adopter gate-wiring step, check-wiring cases, kickoff Step 4 probe, template bullet). Build `memory/tooling/builds/2026-08-03-TOOL-aQuarriedLantern/`. | **aQuarriedLantern** · TOOL-aQuarriedLantern-1/-2/-3 · seq TOOL-3 | **in-flight** — U1-U3 landed; closing review `aQuarriedLantern-2` folded (F1-F14, 3 groups); pre-push |
| `a` | `main` (spec first — build on approval) | tooling — **TOOL-aBatchedLintel-1** ports inCMS `PERF-aSlothfulCapstan-1`: collapse kit hygiene checks 12 and 7 to one awk each, carry the trailing-blank divergence, and add the differential parity harness. Build `memory/tooling/builds/2026-08-03-TOOL-aBatchedLintel/`. | **aBatchedLintel** · TOOL-aBatchedLintel-1/-2 · seq TOOL-2 | **in-flight — BUILT.** Spec rev-2 (review `wf_1b771a5c-a41`, 11 findings folded). Parity byte-identical vs `e8d046cc` over the real corpus and 28 shapes, full and `--staged`; self-test 14 -> 30 assertions; kit 1.3 -> 1.4. NOT merged. |
| `a` | `branch/port-ledger-ttrove-governance-8b6ea4` (worktree `.claude/worktrees/port-ledger-ttrove-governance-8b6ea4`, off `42c3f4dc`) | tooling — **TOOL-aFoldedQuarry-1** folds inCMS `ARCH-dQuarriedLedger-1` + `ARCH-dWinnowedTrove-2` into the memory-tree kit as a parameterised port, then re-dogfoods on this repo's `memory/`. Six units: U6 indexed review join, U1 flatten, U2 generated build index, U3 corpus ids, U4 gotchas corpus, U5 harness disciplines. Build `memory/tooling/builds/2026-08-08-TOOL-aFoldedQuarry/`. | **aFoldedQuarry** · TOOL-aFoldedQuarry-1..-6 · seq TOOL-6 | **in-flight** — U6 + U1 + U2 landed (23/23 legs green; kit 1.4 -> 1.5, tree flattened, the authored tree file replaced by a DERIVED build index); U3-U5 pending |
