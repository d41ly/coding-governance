# In-flight ledger — node `a`

| Node | Branch / worktree | Streams | Slug · ids | Status |
|---|---|---|---|---|
| `a` | `main` (kit + docs, no product code) | tooling — spec-format discipline port (kit check 12 + template + self-test + dogfood arm) | **aRuledParchment** · TOOL-aRuledParchment-1/-2 · seq TOOL-2 | **merged** (direct commit on `main`, NOT pushed) |
| `a` | `main` (specs only, doc-only) | playbook + tooling — **gate-economy uplift** ported from inCMS ARCH-aTrimmedGauntlet-2: 6 specs (full-run-at-push-boundary, fail-closed scoping, vacuous-ratchet bug class, manifest runner, pre-push enforcement, bookkeeping-before-push) | **aPrunedCeremony** · PLAY-aPrunedCeremony-1..4 · TOOL-aPrunedCeremony-1/-2 · seq PLAY-4 TOOL-2 | **SPECCED** — 6 specs authored + adversarially reviewed (wf_2f11fd07, 16 confirmed folded to rev-2); awaiting owner build-approval (no code). Key fold: template ≤32 KiB budget forces domain-rules externalize + in-place rewords |
