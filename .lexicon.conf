# .lexicon.conf — the naming lexicon this repo declares. Read by tools/lexicon/lexicon.py through
# tools/lexicon/lexicon_conf.py, which is the ONE reader; nothing else parses this file.
#
# GRAMMAR: single-line keys use the sibling KEY=VALUE form (`.memory-tree.conf`, `.codebase-map.conf`)
# — a value with spaces is double-quoted and no comment follows it on the line. Block keys are a
# `KEY:` header followed by INDENTED rows, ending at the first non-indented line.
#
# CURATED from the derived seed on 2026-08-16, and the derivation is worth recording because it is
# the argument for curating. `--scaffold` proposed 25 rows by leading-token frequency and ten of them
# were not verbs at all: `t`, `do`, `git`, `kit`, `signal`, `bounded`, `all`, `repo`, `is`, `no` —
# leading tokens of `git_x`, `signal_ledger`, `boundedK`, `all_inventories`, `is_x`. A table seeded
# with those would have been a mirror of the identifiers it grades, which is the one shape a naming
# gate must not have. What is below is PRESCRIPTIVE: it says what this repo should be called, not
# what it is called.

# Type-name suffixes that name a responsibility nobody scoped. Prescriptive, and safe to inherit.
BANNED_SUFFIXES="Manager Helper Util Utils Handler Processor Data Info"

# <ext>:<pattern-set-id>:<mode>, one per extension PRESENT in this corpus. An extension here with no
# declaration is a named refusal, never a silent skip. `sh` is DARK on purpose and not by omission:
# a regex over shell function definitions would look like coverage while silently skipping what it
# forgot, which is the law `map_extractors.py` already wrote down for the recall corpus.
LANGS="<none>::dark conf::dark example::dark gitattributes::dark gitignore::dark js:js-regex:probe json::dark md::dark py:python-ast:parser sh:shell-tokens:parser toml::dark tsv::dark txt::dark"
# `tsv` arrives with TOOL-dUnstalledConvoy-29's generated subject pin. DARK, and that is a
# refusal rather than a shrug: a tab-separated record declares no functions, so there is
# nothing for a verb table to grade — and the check REDS on an undeclared extension rather
# than skipping it, which is why this line had to be written the day the file landed.

# EXTRACTOR SETS ARE DECLARABLE TOO, which is what lets an adopter arm a language this kit does not
# ship without editing an `engine`-role file the next upgrade overwrites. A `PATTERNS:` block holds
# one row per `<pattern-set-id>.<functions|types|imports>`; the rest of the row is one Python regex,
# taken verbatim to end of line, compiled with `re.M`, and carrying EXACTLY ONE capturing group —
# the NAME. Zero groups, two groups or a regex that will not compile is a refusal naming this file
# and the line, never a quietly dropped row.
#
# Rows merge over the shipped sets PER KEY, so a row may FIX one shipped regex without disarming the
# other two, and every run prints which shipped keys a declaration replaced. An unshipped id builds
# a new set whose unnamed parts are empty.
#
#   PATTERNS:
#     ts-regex.functions  ^\s*(?:export\s+)?function\s+([A-Za-z_$][\w$]*)
#     ts-regex.types      ^\s*(?:export\s+)?(?:interface|class)\s+([A-Za-z_$][\w$]*)
#
# THE BOUNDARY, because a capability with no stated edge grows one by accident. A regex set is a
# `probe` and stays incomplete BY CONSTRUCTION — that is the mode's whole declaration. Arming a
# language here buys grading, never a lexer, and `sh` stays dark for exactly the reason three
# paragraphs up. This repo declares no rows of its own: it carries no language the kit does not
# already ship, so the resolved sets equal the shipped ones and every count below is unchanged.
# TOOL-aSurfacedLexicon-9.

# MEASURED against this corpus after curation. A TWO-SIDED EQUALITY since TOOL-aSurfacedLexicon-4:
# a count that FALLS reds exactly as one that rises does, because a drain nobody wrote down is a
# pin nobody re-measures. Honestly
# non-zero on day one — `ORPHAN_ID_PIN` is the precedent for a pin that is not a target. The value
# counts every DEFINITION the declared extractors see (Python via `ast`, JavaScript via the probe
# set) whose leading token is outside the table below. It is large because the table is prescriptive
# and this corpus predates it, which is the expected shape rather than a backlog.
# RAISED 412 -> 415 at the merge of main (aSiftedPlaybook), and the justification is here rather
# than in a commit message because a pin move that has to be archaeologised is a pin move nobody can
# check. The three are named, and they are ALL of it — measured by diffing the added `def`s:
#   _scalar_at        (verb `scalar`)     tools/drift-audit/drift_report.py
#   _justified        (verb `justified`)  tools/drift-audit/drift_report.py
#   ratchet_findings  (verb `ratchet`)    tools/drift-audit/drift_report.py
# All three arrive with drift-audit 1.3's ratchet guard from another node. This is corpus GROWTH, not
# a drain and not a weakening: no previously-counted offender disappeared, and the offender set is a
# strict superset of the 412. They are not waived, because a waiver asserts "deliberate exception",
# which is a claim about another unit's intent this build is not entitled to make; a raise says only
# "the corpus grew and here is exactly by what". The next curation pass of VERBS should decide
# whether `scalar`, `justified` and `ratchet` belong in the table or the names should change.
# RAISED 415 -> 417 by TOOL-dClosedLexicon-2, and the two are NAMED: `signal_lexicon_verbs_unused`
# and `signal_lexicon_ratified_stale`, both in `tools/drift-audit/drift_report.py`. They lead with
# `signal`, which is not in the table — and they are NOT renamed, because that engine names all six
# of its existing signals the same way and a table young enough to have shipped this week does not
# get to break a convention older than itself. The honest options were a raise or a waiver; a raise
# says "the corpus grew and here is exactly by what", which is what happened. If `signal` is ever
# admitted to VERBS, this pin drops by 8 in one edit.
# RAISED 417 -> 463, and TWO nodes raised this pin from 417 independently before either saw the
# other. Both raises are kept below as history, because the disagreement is the useful part:
#   - node a's reconcile measured 463 (45 added, 1 drained) over a tree carrying aRuledFrontispiece.
#   - origin/main's aWalkedCorpus reconcile measured 450 (33 added, strict superset) over a tree
#     that did NOT yet carry `gen_build_index.py`'s changes. 450 was correct for what it could see.
# RE-MEASURED over the MERGED tree, which is the only operand either raise was really about: 463,
# by 45 ADDED and 1 DRAINED. The 450 side's arrivals are a strict SUBSET of the 45 — its 33 are the
# deployer, which node a counted too — and its claim that its own two new engines (`check-recall.py`,
# `test_recall_floor.py`) add ZERO is CONFIRMED here: neither appears in the merged delta.
# The DRAIN is stated first because the prior two raises could both claim a strict superset and this
# one CANNOT. It is `corpus_files` in tools/memory-recall/query.py, and the mechanism is NOT a rename:
# the 417 baseline carried TWO enumerators, `query.py::corpus_files` AND `extract.py::corpus_files`,
# and TOOL-aWalkedCorpus-1 ("the two corpus enumerators become one", 0f8142a) deleted the query.py
# one. `extract.py::corpus_files` was already counted at 417 and still is; `extract.py::corpus_inputs`
# below is a genuinely NEW function, not the drained one wearing another name. An earlier draft of
# this block said it was a rename — that reading did not reproduce, and it was caught by a reviewer
# re-deriving the set rather than by any gate, because the ARITHMETIC was right either way. The 45,
# by file:
#   tools/govkit/govkit.py (20)               _is _sha canonical_ctx classify_outcome classify_row
#                                             cmd_update destinations_for entry_version eol_population
#                                             exempt_leg expand_rules find_block fnmatchcase_path
#                                             hook_probe lf_pins marker_pair rule_relpath step
#                                             three_way validate_gate_runner
#   tools/memory-tree/gen_build_index.py (12) _bind _expand_ids _id_alternation _marker_index _rows
#                                             _wrap_ids do_check_format do_print_bindings insert_region
#                                             record_paths slot_violations spec_ids
#   tools/govkit/selftest.py (7)              govkit_kind_marks govkit_steps hook_target kit pin_target
#                                             runner_target stale_target
#   tools/govkit/refusal_join.py (3)          _is_refusal enumerate_branches population
#   tools/govkit/matrix.py (2)                git shape
#   tools/memory-recall/extract.py (1)        corpus_inputs
# 32 of the 45 are the govkit deployer, which landed its three new modules and its selftest without
# the verb table applied. That is NOT what a raise is meant to absorb: the first two raises were 3 and
# 2 names, and this is a whole kit. It is raised rather than waived for the reason both nodes gave
# independently — a raise says "the corpus grew and here is exactly by what", a waiver would claim an
# intent neither reconcile is entitled to assert — and curating govkit's names is TOOL-aWiredReckoning-1,
# not a silent absorption. `refusal_join.py` anchors branches on module-function-ordinal, so renaming
# that surface moves those anchors and is a build, not an edit.
# LOWERED 463 -> 384 by TOOL-dScaffoldedMirror-14, and this is the first DOWNWARD move this key
# has ever made: the four entries above it are raises. Seventy-nine offenders left the set by
# being RENAMED rather than absorbed — 52 `t_*` self-test arms became `test_*`, a verb the table
# already declared, and 27 subcommand entry points converged on `cmd_*` under one new reserved
# row. Derived, not chosen: `python tools/lexicon/lexicon.py --measure` prints it.
# RAISED 384 -> 452 by DEPL-dCarriedReceipt (the receipt build), landed from node `a`. Corpus
# GROWTH and not a drain: measured 70 ADDED and 0 DRAINED against `origin/main`, so the offender set
# is a strict SUPERSET of the 384 and no previously-counted offender disappeared. The corpus grew by
# roughly seven thousand lines across `tools/govkit/govkit.py` and `tools/govkit/selftest.py` — a new
# verb, a coverage join, a decline contract, a gate-leg bar and their arms — and this is exactly what
# that cost in names. All seventy are NAMED, per this file's own convention, because a pin move that
# has to be archaeologised is a pin move nobody can check:
#   _boom9 _names _older _plant7 _sha_nocr _tot5 a13_gov a13_kit a13_porcelain a13_receipt
#   a13_row a13_target a4_gap_total a4_gaps a4_gov a5_cov a5_target a6_gov a6_kit a6_legs
#   a6_target base_at blob_oid carry_advance carry_gov carry_matches carry_target
#   carry_update clone_crlf coverage_rows decline_findings delta_target
#   demand_adopt_index_clean demand_claimed_paths_clean demand_forward_vintage
#   demand_index_resolved demand_no_operation_in_progress demand_published_vintage
#   demand_writable_target describe_write_lock dirty_claimed_paths fork_gov fork_kit
#   fork_target gblob git_path gout gov_run gov_tree_mode gov_update govkit_module
#   govkit_update_role identity_gov identity_target index_blob index_read
#   land_through_index lf_pin_block lock_of mine_count poison raw_write_cells
#   release_write_lock settle silenced_legs take_write_lock tally_of verdict_of vintage_gov
#   wt_target
#
# A RAISE RATHER THAN A RENAME, and the reason is the charter's own: the table's value is SCOPING,
# not spelling, and renaming `a13_gov` to lead with a table verb buys compliance and answers no
# question about what the function does. The engine half is worse than that — `classify_row` is
# cited BY NAME in seven ratified spec files, so renaming it would falsify the record those specs
# ARE. Not waived either: a waiver asserts "deliberate exception", which is a claim about another
# unit's intent this build is not entitled to make. A CURATION PASS is owed and is filed as its own
# backlog row; it should decide whether `demand`, `index`, `blob`, `carry` and `gov` belong in the
# table or the names should change, and that decision is worth more than this diff would have been.
# RAISED 452 -> 455 by the same build's CLOSING-REVIEW FOLD, three days into the same session.
# Corpus growth again, 3 ADDED and 0 DRAINED, and all three are NAMED per this file's convention:
#   _enclosing1 a5_cov_flag a6_target_norunner
# All three are selftest helpers the fold's own arms needed: an AST enclosing-function resolver for
# the declared-executing-surface join, a coverage runner that takes flags for the `--run-discharge`
# arms, and a no-runner target builder for the ORDER-branch arms that had no fixture at all. Each
# exists because a review finding demanded an arm, which is the trade this pin is recording: three
# more offenders bought the gate that catches a target-authored command execution.
# RAISED 455 -> 458 by ROUND 2 of the same closing review. Three ADDED, zero drained, named:
#   demand_safe_token _git_plumbing1 a_evil_target
# All three are the fold's own: the token-value guard the command-injection blocker needed,
# and two selftest helpers for the arms that reproduce it. The naming debt is real and
# TOOL-aResumedRelay-1 owns the curation pass; what this pin records is that closing an ACE
# cost three names.
# THE NAME LIST ABOVE WAS EMPTY until round 3 filled it. The entry said "named per this file's
# convention" and then named nothing, which is the one thing this convention exists to prevent --
# a pin that records a number and loses what bought it. Recovered from `git diff 267b598f 3dee7112`.
# RAISED 458 -> 460 by ROUND 3 of the same closing review. TWO ADDED, zero drained, named:
#   demand_contained_dest demand_contained_rows
# The path-containment guard for a REPRODUCED arbitrary file write: a target's own `prefix` carrying
# `../..` made `plan` preview and `apply` WRITE 26 files outside the target repository. It joins the
# `demand_*` family this engine already uses for "refuse unless", which is why it is a raise and not
# a rename -- `demand` is one of the six leading tokens TOOL-aResumedRelay-1's curation pass owns,
# and renaming one member out of a family to satisfy a table is the spelling compliance the charter
# warns against. What this pin records is that closing a traversal write cost two names -- the
# predicate and the one reader `plan` and `apply` share, which exists so the guard is not spelled
# twice in the two verbs that needed it.
# 460 -> 463 (2026-08-27). NOT a curation decision and not this build's code: the dTieredTribunal
# fold (2114c912) added three P1 offenders in .claude/hooks/agent-cap.js -- stripStrings,
# offendingLines, boundedK, intConsts -- and landed on main without moving this pin, so the
# `lexicon naming predicates` leg was RED on main before aBoundedCeiling branched. Raised here to
# unblock a landing the owner asked for, with the source named so the debt is attributable rather
# than absorbed. Draining it is a rename in that file, which is its author's call: TOOL-aResumedRelay-1
# already carries the standing curation ask and this is a second instance of the same shape.
# 463 -> 461 (2026-09-01, TOOL-dMispairedQuote-3). The debt the note above calls "a rename in that
# file, which is its author's call" is partly drained: `stripStrings` and `blankLiterals` became
# `renderStrippedView` and `renderBlankedLiterals` in both tracked copies, because that unit had to
# name six view functions and would not add a seventh offender to a pin that was shrink-only then and
# is a two-sided equality now. It is 461 and
# not 459 because the same unit adds definitions the leg grades: `graded` moves 1012 -> 1032. The
# number is READ OFF the tool, never predicted -- two predictions disagreed before this landed.
# LOWERED 461 -> 460 (2026-09-05, TOOL-aSurfacedLexicon-2), and the departure is NAMED like every
# arrival above it: `_glob_match` in tools/lexicon/lexicon.py, which led with `glob` and was one of
# the eight functions deleted with predicate P3. The other seven led with a declared verb and were
# never in this count. `graded` lands at 1036 on the tree that ships this pin, and the two units at
# this build order both move it: the first deletes the P3 predicate's functions and adds
# `check_self_containment`, which leads with `check` and is not an offender, and the second
# collapses five corpus walks and deletes two CLI modes. A comment that named one unit's
# intermediate figure would be wrong on the tree it actually lands in, which is why this one names
# the landed value. The number is READ OFF `python tools/lexicon/lexicon.py --check`, never
# predicted -- three predictions have now disagreed with it across this build.
# RAISED 460 -> 461 (2026-09-05, TOOL-aSurfacedLexicon-5), and the sole ARRIVAL is NAMED like every
# other entry in this block:
#   classify   (verb `classify`)   tools/lexicon/subtokens.py
# It is the convention classifier itself, and it is a RAISE rather than a rename on the charter's own
# argument: the table's value is SCOPING and not spelling, `classify` is the name the prototype used,
# and renaming it to lead with a declared verb would answer no question about what the function does.
# The curation pass that should decide whether `classify` joins VERBS is TOOL-aResumedRelay-1, which
# already carries this exact ask for six other leading tokens. Not waived either: a waiver asserts a
# deliberate exception, and this is a corpus that grew by one name.
# THE OTHER FIVE IDENTIFIERS THAT UNIT MINTS COST NOTHING, which is worth writing down because
# guessing it wrong is how a pin gets over-budgeted: `read_core`, `read_stem` and `check_convention`
# lead with declared verbs, `measure_conventions` with `measure`, and `_AFFIX` and `_FORMS` are
# module-body assignments P1 does not grade at all. `offenders` moves 460 -> 461: five definitions
# enter the population and exactly one of them is an offender. **`graded` is deliberately NOT stated
# here.** A previous line in this block did state it, as 1041 -> 1046, and the tree it landed on
# prints 1049 -- because three units land in one pass and every one of them changes the population,
# so an arithmetic written per unit is wrong for the tree that carries it. The OFFENDER count is what
# this pin gates and it is per-name, which is why it survives the same pass. The
# failing case was OBSERVED first: with the definitions in place and this pin still reading 460,
# `python tools/lexicon/lexicon.py --check` printed `lexicon: verb offenders 461 over pin 460` and
# exited 1. The number is READ OFF that command, never predicted.
# RAISED 461 -> 968 (2026-09-05, TOOL-aSurfacedLexicon-14), and this is the largest single move
# this key has ever made. THE ARRIVAL IS ONE LANGUAGE, not a list of names: `sh` left `dark` for the
# `shell-tokens` parser, which put every extracted shell function in front of P1 for the first time.
# Naming all 507 individually is what every entry above does and it is refused here, because 507
# rows would record a corpus rather than a change: what arrived is the EXTENSION, and the names are
# derivable from it in one command. Measured, never predicted:
#   python tools/lexicon/lexicon.py --check
# `offenders` lands at 968 on the tree that ships this pin, which is what this key is set to.
# **`graded` is deliberately NOT stated here**, for the reason two entries above: it moves with
# every unit that adds a definition, several land in one pass, and a figure written per-change is
# wrong for the tree it arrives in. It was stated here once, as 1665, and the tree that shipped it
# printed 1678. Read it off the command; the OFFENDER count is what this pin gates.
# The 507 lead with the test-harness idioms this repo writes by the hundred -- `verb`, `fail`, `ok`,
# `bad`, `mk`, `say`, `is` -- none in the table and none renamed here. A RAISE and not a waiver, on
# this file's standing argument: a waiver asserts a deliberate exception, and this is a corpus that
# grew by one armed language. The curation pass that should decide whether any of those leading
# tokens joins VERBS is TOOL-aResumedRelay-1, which already carries the same ask for six others.
# THE SPEC BUDGETED 508 AND IT WAS AN UPPER BOUND, stated as one: 508 was the NAIVE same-line
# pattern's figure over 608 names, and the parser reads 607 -- it declines the JavaScript
# `function f()` inside a `<<'EOF'` heredoc in tools/hooks/agent-cap.test.sh that the regex counted.
# One fewer name, one fewer offender. The six identifiers this unit mints in tools/lexicon/ cost
# ZERO: `parse_shell_defs`, `scan_shell_tokens`, `add_word`, `add_char`, `read_quoted` and
# `read_braced` all lead with a declared verb, measured as `graded` 1049 -> 1055 with `offenders`
# unmoved at 461 before the LANGS edit landed.
VERB_OFFENDER_PIN="968"
SUFFIX_OFFENDER_PIN="0"

# The date and node that CURATED the seed below.
#
# RE-STAMPED 2026-08-16 -> 2026-08-24 by TOOL-dUnstalledConvoy-26, and WHAT THIS RE-STAMP COVERS is
# narrower than a curation pass, so read this before treating it as one. The only change to the
# language surface was `tsv::dark`, declared because TOOL-dUnstalledConvoy-29 generates
# `tools/govkit/subject-pins.tsv` and this check REDS on an undeclared extension rather than
# skipping it. A `dark` declaration is a refusal to extract, so it cannot add one definition to the
# corpus the VERBS table governs — the table certifies exactly the population it certified before.
# The seed itself was NOT re-curated and the three names its own comments flag for the next pass
# (`scalar`, `justified`, `ratchet`) are still unresolved.
#
# The signal cannot see that distinction: `signal_lexicon_ratified_stale` compares the stamp date
# against the last commit that touched `LANGS=` at all, armed or dark. Widening it to ignore
# dark-only changes is TOOL-dUnstalledConvoy-35, opened rather than done here, because a signal
# edited to clear its own red at the close of a build is the shape nobody should trust.
# RE-STAMPED 2026-08-24 -> 2026-09-05 by TOOL-aSurfacedLexicon-14, and this one IS a language-surface
# change: `sh` moved from `dark` to the `shell-tokens` parser, which is the first extension this
# declaration has ever ARMED rather than declared dark. The stamp is re-written in the same commit as
# the LANGS edit because `signal_lexicon_ratified_stale` compares this date against the commit date
# of the last change to that line, and an unrestamped arming reds the `drift-audit records` leg --
# which carries no guard and therefore runs on every bar. The VERBS seed itself was NOT re-curated
# and the leading tokens its own comments flag for the next pass are still unresolved.
ratified="2026-09-05 node a"

# The (language, surface) cells this repo grades a NAME SPELLING in, distinct from the verb table
# above: P1 asks which verb a name leads with, a cell asks what shape the whole name is.
#
# `sh.function` IS THE FIRST ROW, and its convention was fixed BEFORE the parser that produces its
# population ever ran. The source is the Google Shell Style Guide's Function Names rule -- lower
# case, underscores separating words -- which is prescriptive, external to this tree, and older than
# every file it grades. That ordering is the whole point: a standard read off the population it
# grades is a mirror, which is the one shape a naming gate must not have, and choosing `snake` while
# holding a freshly measured name set is how a gate acquires that shape by accident rather than by
# intent. The corpus is evidence for exactly ONE thing here -- how far it sits from the rule -- and
# the answer is six names of 607, so the external rule and this repo were never in conflict.
#
# NO `sh.type` ROW, and that is a refusal rather than an omission: shell has no type names in any
# sense this kit grades, so `parse_shell_defs` returns an empty list for that surface and a cell
# would grade nothing forever.
#
# `py.constant` IS ARMED ON THE PUBLIC SIMPLE READING, and the other two readings are recorded here
# because the choice between them is the whole reason every report row prints its population RULE.
# Owner ruling Q6, 2026-09-04. Three readings of "a module constant" are defensible, they are the
# three rows below, and only one of them yields a clean zero — which it does because the predicate
# selects the names it grades, not because the corpus is tidy. The SPAN between the widest and the
# armed reading is not stated here: it is the subtraction of two rows that already sit five lines
# down, and it was wrong by 83 names the last time it was. TOOL-aSurfacedLexicon-6.
#
# THE COUNTING RULE, stated so a second party reproduces the numbers rather than re-deriving a
# different set: module BODY statements only, no descent into a class or function body; both
# `ast.Assign` and `ast.AnnAssign`; a bound name is an `ast.Name` reached through `Tuple`, `List`
# and `Starred` targets ONLY, so `d[k] = v` and `obj.attr = v` bind nothing; and the screaming test
# strips leading and trailing underscores, then requires a non-empty remainder matching `[A-Z0-9_]+`.
# It is implemented in `scan_module_constants` and `extract_bound_names` in tools/lexicon/lexicon.py,
# which is the ONE carrier — this comment records the READINGS, never a second copy of the walk, and
# the kit's own selftest asserts the armed row's two figures against these lines so they cannot rot
# apart.
#
#   command, the armed row: python tools/lexicon/lexicon.py --check
#   command, all three:     the walk in `scan_module_constants`, run with its narrowing removed
#
#   every module-body target, tuple unpack and AnnAssign included   736 graded, 489 screaming, 247 bad
#   simple single-`Name` targets, AnnAssign included                523 graded, 483 screaming,  40 bad
#   public simple targets, no leading underscore  <-- ARMED         352 graded, 352 screaming,   0 bad
#
# TWO SENSITIVITIES, both of which produced a wrong number before the rule was written down.
# Dropping `AnnAssign` moves the three graded counts to 693, 480 and 318 — a 34-name swing on the
# armed row alone. Counting every `ast.Name` under a target rather than only the ones the rule binds
# moves the first row to 749; that 13-name gap is exactly the subscript and attribute targets.
#
# THE FIGURES ARE THE LANDED ONES, not this unit's arithmetic. TOOL-aSurfacedLexicon-6 mints three
# public module constants of its own (`CELL_POPULATION_RULES`, `PREDICATE_SURFACES`,
# `UNDECLARED_CELL_ARMED`), deletes one (`CELL_SURFACES`), and adds a page of underscore-prefixed
# fixture bindings to the kit's selftest — which move the DENOMINATOR without touching the graded
# count. A figure stated per-change would have been wrong for the tree it arrives in, which is the
# mistake the VERBS block above records twice. Read off the command, never predicted.
#
# MOVED by TOOL-aSurfacedLexicon-13 (the prefix selector), and the two arrivals are NAMED like every
# entry in the VERBS block above: `SELECTOR_KINDS` and `_SEL_LIT_RE`, both module-body assignments in
# tools/lexicon/lexicon_conf.py. The first is public and simple, so it joins the ARMED row; the
# second is underscore-led, so it moves the denominator alone. All five figures above moved with
# them and every one was re-derived from the walk rather than adjusted by arithmetic. The unit adds
# three function definitions and ZERO verb offenders, so VERB_OFFENDER_PIN does not move.
#
# MOVED AGAIN by TOOL-aSurfacedLexicon-7 (the DEBT/UNRULED split), and the ARMED row did not budge:
# still 352 / 352 / 0, exactly as above. The two readings ABOVE it did — 693 -> 736 and
# 506 -> 523 — because that unit's selftest arms bind roughly forty module-body names and every one
# of them is either underscore-led or a tuple target, so they land in the denominator and never in
# the graded set. Both sensitivities moved with them, 650/463/318 -> 693/480/318 and 706 -> 749, and
# the 34-name AnnAssign swing and the 13-name subscript gap are unchanged. Every figure was
# RE-DERIVED from the walk with its narrowing removed, never adjusted by arithmetic, and the kit's
# own selftest asserts the armed row's pair against these lines — which is what caught the drift
# rather than a reader noticing it. The unit adds four function definitions, all leading with a
# declared verb, so VERB_OFFENDER_PIN does not move and no PINS row is written here.
# A CELL MAY ROUTE A SUBSET OF ITSELF to a second convention, and this repo declares NO such row —
# fork F1 of TOOL-aSurfacedLexicon-13, shipped undeclared. The commented example is here rather than
# a live row because a selector needs a parent, `py.function` does not land until
# TOOL-aSurfacedLexicon-12's matrix, and the subsets this tree could route (110 leading `test_`, 32
# leading `cmd_`) all satisfy `snake` already — a row that can never fail is the shape this build
# exists to remove. The grammar is one selector clause on the row key, and the subset LEAVES the
# parent's population and takes its own pin:
#
#   CELLS:
#     py.function                  snake
#
#     py.function+prefix:Test      pascal
#
#   PINS:
#     py.function+prefix:Test.conv 2
#
# `kind` is `prefix` or `decorator`; a decorator selector on a non-`parser` language is a refusal
# naming both, a selector matching nothing is a DEAD CELL, and a name matching two selectors is
# refused rather than resolved by declaration order. tools/lexicon/README.md carries the rest.
CELLS:
  sh.function  snake

  py.constant  screaming

PINS:
  sh.function.conv  6

VERBS:
  build     create a new value and return it — NOT `create`, which is reserved for side-effecting setup
  load      read a store into memory — NOT `fetch`, which implies a network call
  read      pull bytes or records from a named source — NOT `get`, which says nothing about cost
  write     persist to a store — NOT `save`, which hides whether anything was already there
  parse     turn text into structure, raising on text that is not that structure — NOT `convert`
  render    turn structure into text — NOT `format`, which reads as cosmetic
  resolve   turn a name into the thing it denotes, RUNNING the candidate where that is the only proof — NOT `lookup`, a lookup returns a row; resolve returns the thing, RUNNING the candidate
  check     assert a predicate and return a verdict — NOT `validate`, which implies mutation on failure
  scan      walk a population looking for matches — NOT `search`, which implies stopping at the first
  extract   pull a declared shape out of a larger one — NOT `pluck`, pluck names the taking; extract names the declared shape taken
  measure   count a population and report the number, deciding nothing — NOT `count`, count is the arithmetic; measure counts a population and decides nothing
  derive    compute a value from a source so it never has to be authored — the §5 rule, as a verb — NOT `compute`, compute says a value was produced, not that it never has to be authored
  seed      write an initial value the tool will not overwrite again — NOT `install`, install claims the tool owns the result after; a seed is never overwritten
  init      set up state at construction — NOT `setup`
  run       execute a process to completion and report its outcome — NOT `execute`, the same word twice, and this repo already runs processes
  arm       make a dormant check live; its opposite is a check that cannot fail — NOT `enable`, enable reads as a feature flag; arm makes a dormant CHECK live
  add       append to an existing collection — NOT `append`, append claims a position at the end; add claims only membership
  remove    detach without destroying — NOT `delete`, which is irreversible
  set       assign a known value — NOT `update`, which implies a diff against prior state
  print     write to stdout for a human — never a return value in disguise — NOT `log`, log implies a level, a sink and a filter; print writes to stdout for a human
  main      a module's CLI entry point; reserved, one per module — NOT `start`, start names a lifecycle event; main is a module's one CLI entry point
  cmd       a subcommand entry point, dispatched by name; reserved — NOT `do`, which names no role
  test      a test function; reserved for harnesses — NOT `assert`, assert is one statement inside a test; test is what a harness collects
