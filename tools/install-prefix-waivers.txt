# install-prefix waivers — deliberate root-install spellings in SHIPPED files.
# One "<path>:<line>" per row, reason after whitespace. SHRINK-ONLY: the count may fall, never rise.
# A row whose spelling is gone reds as stale, so this file cannot quietly outlive what it excuses.
#
# Two classes only. (1) DUAL-SPELLING PROBES that keep working for the adopters this unit does not
# retrofit — each names the root form as a fallback on purpose. (2) The codebase-map REGEN_CMD
# legacy, preserved for adopters carrying a pre-1.1 gate file the maintenance rule never overwrites.

.githooks/pre-commit:37                 dual-spelling: the hygiene leg locates its gate at either prefix
tools/check-wiring.sh:158               dual-spelling probe: memory-recall fragment, both layouts
tools/check-wiring.sh:305               dual-spelling probe: the merge driver, both layouts
tools/check-wiring.sh:314               launcher probe: the gov-internal shim is the fallback after the kit launcher
tools/check-wiring.sh:337               prose naming the gov-internal shim's own dependency
tools/codebase-map/map_lib.py:1165      REGEN_CMD legacy, deliberate: a pre-1.1 GATE_FILE is project-owned and never overwritten
tools/codebase-map/gen_map.py:47        prose quoting that legacy literal
tools/codebase-map/adopt-codebase-map.sh:155  prose quoting that legacy literal
tools/memory-tree/README.md:108         the PUBLISHED root-prefix driver command; check-wiring.test.sh AC12 derives and requires it
tools/memory-tree/corpus_ids.py:659     check-15 selftest fixture: a wrong-prefix citation, built to prove the check fires
tools/memory-tree/corpus_ids.py:663     the tracked file that fixture cites at the correct prefix
